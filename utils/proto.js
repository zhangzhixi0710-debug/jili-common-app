/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import * as $protobuf from "protobufjs/minimal";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const bfs = $root.bfs = (() => {

    /**
     * Namespace bfs.
     * @exports bfs
     * @namespace
     */
    const bfs = {};

    bfs.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof bfs
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof bfs
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {bfs.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof bfs.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof bfs.Column
         * @static
         * @param {bfs.IColumn=} [properties] Properties to set
         * @returns {bfs.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link bfs.Column.verify|verify} messages.
         * @function encode
         * @memberof bfs.Column
         * @static
         * @param {bfs.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link bfs.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof bfs.Column
         * @static
         * @param {bfs.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof bfs.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {bfs.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.bfs.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof bfs.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {bfs.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof bfs.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof bfs.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {bfs.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.bfs.Column)
                return object;
            let message = new $root.bfs.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".bfs.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof bfs.Column
         * @static
         * @param {bfs.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof bfs.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof bfs.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/bfs.Column";
        };

        return Column;
    })();

    bfs.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof bfs
         * @interface IAwardData
         * @property {Array.<number>|null} [SerialVec] AwardData SerialVec
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof bfs
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {bfs.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.SerialVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData SerialVec.
         * @member {Array.<number>} SerialVec
         * @memberof bfs.AwardData
         * @instance
         */
        AwardData.prototype.SerialVec = $util.emptyArray;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof bfs.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof bfs.AwardData
         * @static
         * @param {bfs.IAwardData=} [properties] Properties to set
         * @returns {bfs.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link bfs.AwardData.verify|verify} messages.
         * @function encode
         * @memberof bfs.AwardData
         * @static
         * @param {bfs.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.SerialVec != null && message.SerialVec.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.SerialVec.length; ++i)
                    writer.int32(message.SerialVec[i]);
                writer.ldelim();
            }
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link bfs.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof bfs.AwardData
         * @static
         * @param {bfs.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof bfs.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {bfs.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.bfs.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.SerialVec && message.SerialVec.length))
                            message.SerialVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.SerialVec.push(reader.int32());
                        } else
                            message.SerialVec.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof bfs.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {bfs.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof bfs.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.SerialVec != null && message.hasOwnProperty("SerialVec")) {
                if (!Array.isArray(message.SerialVec))
                    return "SerialVec: array expected";
                for (let i = 0; i < message.SerialVec.length; ++i)
                    if (!$util.isInteger(message.SerialVec[i]))
                        return "SerialVec: integer[] expected";
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof bfs.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {bfs.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.bfs.AwardData)
                return object;
            let message = new $root.bfs.AwardData();
            if (object.SerialVec) {
                if (!Array.isArray(object.SerialVec))
                    throw TypeError(".bfs.AwardData.SerialVec: array expected");
                message.SerialVec = [];
                for (let i = 0; i < object.SerialVec.length; ++i)
                    message.SerialVec[i] = object.SerialVec[i] | 0;
            }
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof bfs.AwardData
         * @static
         * @param {bfs.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.SerialVec = [];
            if (options.defaults)
                object.Win = 0;
            if (message.SerialVec && message.SerialVec.length) {
                object.SerialVec = [];
                for (let j = 0; j < message.SerialVec.length; ++j)
                    object.SerialVec[j] = message.SerialVec[j];
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof bfs.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof bfs.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/bfs.AwardData";
        };

        return AwardData;
    })();

    bfs.AwardDataDetail = (function() {

        /**
         * Properties of an AwardDataDetail.
         * @memberof bfs
         * @interface IAwardDataDetail
         * @property {Array.<number>|null} [SerialVec] AwardDataDetail SerialVec
         * @property {number|null} [Win] AwardDataDetail Win
         * @property {number|null} [Symbol] AwardDataDetail Symbol
         * @property {number|null} [LineNum] AwardDataDetail LineNum
         */

        /**
         * Constructs a new AwardDataDetail.
         * @memberof bfs
         * @classdesc Represents an AwardDataDetail.
         * @implements IAwardDataDetail
         * @constructor
         * @param {bfs.IAwardDataDetail=} [properties] Properties to set
         */
        function AwardDataDetail(properties) {
            this.SerialVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardDataDetail SerialVec.
         * @member {Array.<number>} SerialVec
         * @memberof bfs.AwardDataDetail
         * @instance
         */
        AwardDataDetail.prototype.SerialVec = $util.emptyArray;

        /**
         * AwardDataDetail Win.
         * @member {number} Win
         * @memberof bfs.AwardDataDetail
         * @instance
         */
        AwardDataDetail.prototype.Win = 0;

        /**
         * AwardDataDetail Symbol.
         * @member {number} Symbol
         * @memberof bfs.AwardDataDetail
         * @instance
         */
        AwardDataDetail.prototype.Symbol = 0;

        /**
         * AwardDataDetail LineNum.
         * @member {number} LineNum
         * @memberof bfs.AwardDataDetail
         * @instance
         */
        AwardDataDetail.prototype.LineNum = 0;

        /**
         * Creates a new AwardDataDetail instance using the specified properties.
         * @function create
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {bfs.IAwardDataDetail=} [properties] Properties to set
         * @returns {bfs.AwardDataDetail} AwardDataDetail instance
         */
        AwardDataDetail.create = function create(properties) {
            return new AwardDataDetail(properties);
        };

        /**
         * Encodes the specified AwardDataDetail message. Does not implicitly {@link bfs.AwardDataDetail.verify|verify} messages.
         * @function encode
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {bfs.IAwardDataDetail} message AwardDataDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardDataDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.SerialVec != null && message.SerialVec.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.SerialVec.length; ++i)
                    writer.int32(message.SerialVec[i]);
                writer.ldelim();
            }
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Win);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.LineNum);
            return writer;
        };

        /**
         * Encodes the specified AwardDataDetail message, length delimited. Does not implicitly {@link bfs.AwardDataDetail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {bfs.IAwardDataDetail} message AwardDataDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardDataDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardDataDetail message from the specified reader or buffer.
         * @function decode
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {bfs.AwardDataDetail} AwardDataDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardDataDetail.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.bfs.AwardDataDetail();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.SerialVec && message.SerialVec.length))
                            message.SerialVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.SerialVec.push(reader.int32());
                        } else
                            message.SerialVec.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.Win = reader.double();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.LineNum = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardDataDetail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {bfs.AwardDataDetail} AwardDataDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardDataDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardDataDetail message.
         * @function verify
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardDataDetail.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.SerialVec != null && message.hasOwnProperty("SerialVec")) {
                if (!Array.isArray(message.SerialVec))
                    return "SerialVec: array expected";
                for (let i = 0; i < message.SerialVec.length; ++i)
                    if (!$util.isInteger(message.SerialVec[i]))
                        return "SerialVec: integer[] expected";
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            return null;
        };

        /**
         * Creates an AwardDataDetail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {bfs.AwardDataDetail} AwardDataDetail
         */
        AwardDataDetail.fromObject = function fromObject(object) {
            if (object instanceof $root.bfs.AwardDataDetail)
                return object;
            let message = new $root.bfs.AwardDataDetail();
            if (object.SerialVec) {
                if (!Array.isArray(object.SerialVec))
                    throw TypeError(".bfs.AwardDataDetail.SerialVec: array expected");
                message.SerialVec = [];
                for (let i = 0; i < object.SerialVec.length; ++i)
                    message.SerialVec[i] = object.SerialVec[i] | 0;
            }
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardDataDetail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {bfs.AwardDataDetail} message AwardDataDetail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardDataDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.SerialVec = [];
            if (options.defaults) {
                object.Win = 0;
                object.Symbol = 0;
                object.LineNum = 0;
            }
            if (message.SerialVec && message.SerialVec.length) {
                object.SerialVec = [];
                for (let j = 0; j < message.SerialVec.length; ++j)
                    object.SerialVec[j] = message.SerialVec[j];
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            return object;
        };

        /**
         * Converts this AwardDataDetail to JSON.
         * @function toJSON
         * @memberof bfs.AwardDataDetail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardDataDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardDataDetail
         * @function getTypeUrl
         * @memberof bfs.AwardDataDetail
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardDataDetail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/bfs.AwardDataDetail";
        };

        return AwardDataDetail;
    })();

    bfs.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof bfs
         * @interface ISinglePlate
         * @property {Array.<bfs.IColumn>|null} [PlateSymbol] SinglePlate PlateSymbol
         * @property {Array.<bfs.IAwardData>|null} [AwardDataVec] SinglePlate AwardDataVec
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {number|null} [Win] SinglePlate Win
         * @property {Array.<bfs.IAwardDataDetail>|null} [MergedADV] SinglePlate MergedADV
         * @property {number|null} [GetFreeCount] SinglePlate GetFreeCount
         * @property {number|null} [FreeRemainRound] SinglePlate FreeRemainRound
         * @property {number|null} [FreeNowRound] SinglePlate FreeNowRound
         * @property {number|null} [FreeTotalRound] SinglePlate FreeTotalRound
         * @property {boolean|null} [MaxFlag] SinglePlate MaxFlag
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof bfs
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {bfs.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.MergedADV = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate PlateSymbol.
         * @member {Array.<bfs.IColumn>} PlateSymbol
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SinglePlate AwardDataVec.
         * @member {Array.<bfs.IAwardData>} AwardDataVec
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate Win.
         * @member {number} Win
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Win = 0;

        /**
         * SinglePlate MergedADV.
         * @member {Array.<bfs.IAwardDataDetail>} MergedADV
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.MergedADV = $util.emptyArray;

        /**
         * SinglePlate GetFreeCount.
         * @member {number} GetFreeCount
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.GetFreeCount = 0;

        /**
         * SinglePlate FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeRemainRound = 0;

        /**
         * SinglePlate FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeNowRound = 0;

        /**
         * SinglePlate FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeTotalRound = 0;

        /**
         * SinglePlate MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof bfs.SinglePlate
         * @instance
         */
        SinglePlate.prototype.MaxFlag = false;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof bfs.SinglePlate
         * @static
         * @param {bfs.ISinglePlate=} [properties] Properties to set
         * @returns {bfs.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link bfs.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof bfs.SinglePlate
         * @static
         * @param {bfs.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.bfs.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.bfs.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.MergedADV != null && message.MergedADV.length)
                for (let i = 0; i < message.MergedADV.length; ++i)
                    $root.bfs.AwardDataDetail.encode(message.MergedADV[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.GetFreeCount != null && Object.hasOwnProperty.call(message, "GetFreeCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.GetFreeCount);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link bfs.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof bfs.SinglePlate
         * @static
         * @param {bfs.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof bfs.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {bfs.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.bfs.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.bfs.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.bfs.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.MergedADV && message.MergedADV.length))
                            message.MergedADV = [];
                        message.MergedADV.push($root.bfs.AwardDataDetail.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.GetFreeCount = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof bfs.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {bfs.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof bfs.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.bfs.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.bfs.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.MergedADV != null && message.hasOwnProperty("MergedADV")) {
                if (!Array.isArray(message.MergedADV))
                    return "MergedADV: array expected";
                for (let i = 0; i < message.MergedADV.length; ++i) {
                    let error = $root.bfs.AwardDataDetail.verify(message.MergedADV[i]);
                    if (error)
                        return "MergedADV." + error;
                }
            }
            if (message.GetFreeCount != null && message.hasOwnProperty("GetFreeCount"))
                if (!$util.isInteger(message.GetFreeCount))
                    return "GetFreeCount: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof bfs.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {bfs.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.bfs.SinglePlate)
                return object;
            let message = new $root.bfs.SinglePlate();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".bfs.SinglePlate.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".bfs.SinglePlate.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.bfs.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".bfs.SinglePlate.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".bfs.SinglePlate.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.bfs.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.MergedADV) {
                if (!Array.isArray(object.MergedADV))
                    throw TypeError(".bfs.SinglePlate.MergedADV: array expected");
                message.MergedADV = [];
                for (let i = 0; i < object.MergedADV.length; ++i) {
                    if (typeof object.MergedADV[i] !== "object")
                        throw TypeError(".bfs.SinglePlate.MergedADV: object expected");
                    message.MergedADV[i] = $root.bfs.AwardDataDetail.fromObject(object.MergedADV[i]);
                }
            }
            if (object.GetFreeCount != null)
                message.GetFreeCount = object.GetFreeCount | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof bfs.SinglePlate
         * @static
         * @param {bfs.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.MergedADV = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.Win = 0;
                object.GetFreeCount = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.bfs.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.bfs.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.MergedADV && message.MergedADV.length) {
                object.MergedADV = [];
                for (let j = 0; j < message.MergedADV.length; ++j)
                    object.MergedADV[j] = $root.bfs.AwardDataDetail.toObject(message.MergedADV[j], options);
            }
            if (message.GetFreeCount != null && message.hasOwnProperty("GetFreeCount"))
                object.GetFreeCount = message.GetFreeCount;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof bfs.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof bfs.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/bfs.SinglePlate";
        };

        return SinglePlate;
    })();

    bfs.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof bfs
         * @interface ISpinAck
         * @property {Array.<bfs.ISinglePlate>|null} [AckQueue] SpinAck AckQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof bfs
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {bfs.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.AckQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck AckQueue.
         * @member {Array.<bfs.ISinglePlate>} AckQueue
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.AckQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof bfs.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof bfs.SpinAck
         * @static
         * @param {bfs.ISpinAck=} [properties] Properties to set
         * @returns {bfs.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link bfs.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof bfs.SpinAck
         * @static
         * @param {bfs.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AckQueue != null && message.AckQueue.length)
                for (let i = 0; i < message.AckQueue.length; ++i)
                    $root.bfs.SinglePlate.encode(message.AckQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link bfs.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof bfs.SpinAck
         * @static
         * @param {bfs.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof bfs.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {bfs.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.bfs.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AckQueue && message.AckQueue.length))
                            message.AckQueue = [];
                        message.AckQueue.push($root.bfs.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof bfs.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {bfs.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof bfs.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AckQueue != null && message.hasOwnProperty("AckQueue")) {
                if (!Array.isArray(message.AckQueue))
                    return "AckQueue: array expected";
                for (let i = 0; i < message.AckQueue.length; ++i) {
                    let error = $root.bfs.SinglePlate.verify(message.AckQueue[i]);
                    if (error)
                        return "AckQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof bfs.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {bfs.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.bfs.SpinAck)
                return object;
            let message = new $root.bfs.SpinAck();
            if (object.AckQueue) {
                if (!Array.isArray(object.AckQueue))
                    throw TypeError(".bfs.SpinAck.AckQueue: array expected");
                message.AckQueue = [];
                for (let i = 0; i < object.AckQueue.length; ++i) {
                    if (typeof object.AckQueue[i] !== "object")
                        throw TypeError(".bfs.SpinAck.AckQueue: object expected");
                    message.AckQueue[i] = $root.bfs.SinglePlate.fromObject(object.AckQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof bfs.SpinAck
         * @static
         * @param {bfs.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AckQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.AckQueue && message.AckQueue.length) {
                object.AckQueue = [];
                for (let j = 0; j < message.AckQueue.length; ++j)
                    object.AckQueue[j] = $root.bfs.SinglePlate.toObject(message.AckQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof bfs.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof bfs.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/bfs.SpinAck";
        };

        return SpinAck;
    })();

    bfs.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof bfs
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof bfs
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {bfs.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof bfs.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof bfs.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof bfs.GameInfoData
         * @static
         * @param {bfs.IGameInfoData=} [properties] Properties to set
         * @returns {bfs.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link bfs.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof bfs.GameInfoData
         * @static
         * @param {bfs.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link bfs.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof bfs.GameInfoData
         * @static
         * @param {bfs.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof bfs.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {bfs.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.bfs.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof bfs.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {bfs.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof bfs.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof bfs.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {bfs.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.bfs.GameInfoData)
                return object;
            let message = new $root.bfs.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".bfs.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof bfs.GameInfoData
         * @static
         * @param {bfs.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof bfs.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof bfs.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/bfs.GameInfoData";
        };

        return GameInfoData;
    })();

    return bfs;
})();

export const jl3ct = $root.jl3ct = (() => {

    /**
     * Namespace jl3ct.
     * @exports jl3ct
     * @namespace
     */
    const jl3ct = {};

    jl3ct.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jl3ct
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jl3ct
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jl3ct.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jl3ct.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jl3ct.Column
         * @static
         * @param {jl3ct.IColumn=} [properties] Properties to set
         * @returns {jl3ct.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jl3ct.Column.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.Column
         * @static
         * @param {jl3ct.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jl3ct.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.Column
         * @static
         * @param {jl3ct.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jl3ct.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.Column)
                return object;
            let message = new $root.jl3ct.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3ct.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.Column
         * @static
         * @param {jl3ct.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jl3ct.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jl3ct.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.Column";
        };

        return Column;
    })();

    jl3ct.CColumn = (function() {

        /**
         * Properties of a CColumn.
         * @memberof jl3ct
         * @interface ICColumn
         * @property {Array.<jl3ct.ICSymbol>|null} [Col] CColumn Col
         */

        /**
         * Constructs a new CColumn.
         * @memberof jl3ct
         * @classdesc Represents a CColumn.
         * @implements ICColumn
         * @constructor
         * @param {jl3ct.ICColumn=} [properties] Properties to set
         */
        function CColumn(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CColumn Col.
         * @member {Array.<jl3ct.ICSymbol>} Col
         * @memberof jl3ct.CColumn
         * @instance
         */
        CColumn.prototype.Col = $util.emptyArray;

        /**
         * Creates a new CColumn instance using the specified properties.
         * @function create
         * @memberof jl3ct.CColumn
         * @static
         * @param {jl3ct.ICColumn=} [properties] Properties to set
         * @returns {jl3ct.CColumn} CColumn instance
         */
        CColumn.create = function create(properties) {
            return new CColumn(properties);
        };

        /**
         * Encodes the specified CColumn message. Does not implicitly {@link jl3ct.CColumn.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.CColumn
         * @static
         * @param {jl3ct.ICColumn} message CColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jl3ct.CSymbol.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CColumn message, length delimited. Does not implicitly {@link jl3ct.CColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.CColumn
         * @static
         * @param {jl3ct.ICColumn} message CColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.CColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.CColumn} CColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.CColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jl3ct.CSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.CColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.CColumn} CColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CColumn message.
         * @function verify
         * @memberof jl3ct.CColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jl3ct.CSymbol.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.CColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.CColumn} CColumn
         */
        CColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.CColumn)
                return object;
            let message = new $root.jl3ct.CColumn();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3ct.CColumn.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jl3ct.CColumn.Col: object expected");
                    message.Col[i] = $root.jl3ct.CSymbol.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.CColumn
         * @static
         * @param {jl3ct.CColumn} message CColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jl3ct.CSymbol.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this CColumn to JSON.
         * @function toJSON
         * @memberof jl3ct.CColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CColumn
         * @function getTypeUrl
         * @memberof jl3ct.CColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.CColumn";
        };

        return CColumn;
    })();

    jl3ct.CSymbol = (function() {

        /**
         * Properties of a CSymbol.
         * @memberof jl3ct
         * @interface ICSymbol
         * @property {number|null} [Symbol] CSymbol Symbol
         * @property {number|null} [JPState] CSymbol JPState
         * @property {number|null} [JPMult] CSymbol JPMult
         * @property {number|null} [Number] CSymbol Number
         */

        /**
         * Constructs a new CSymbol.
         * @memberof jl3ct
         * @classdesc Represents a CSymbol.
         * @implements ICSymbol
         * @constructor
         * @param {jl3ct.ICSymbol=} [properties] Properties to set
         */
        function CSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSymbol Symbol.
         * @member {number} Symbol
         * @memberof jl3ct.CSymbol
         * @instance
         */
        CSymbol.prototype.Symbol = 0;

        /**
         * CSymbol JPState.
         * @member {number} JPState
         * @memberof jl3ct.CSymbol
         * @instance
         */
        CSymbol.prototype.JPState = 0;

        /**
         * CSymbol JPMult.
         * @member {number} JPMult
         * @memberof jl3ct.CSymbol
         * @instance
         */
        CSymbol.prototype.JPMult = 0;

        /**
         * CSymbol Number.
         * @member {number} Number
         * @memberof jl3ct.CSymbol
         * @instance
         */
        CSymbol.prototype.Number = 0;

        /**
         * Creates a new CSymbol instance using the specified properties.
         * @function create
         * @memberof jl3ct.CSymbol
         * @static
         * @param {jl3ct.ICSymbol=} [properties] Properties to set
         * @returns {jl3ct.CSymbol} CSymbol instance
         */
        CSymbol.create = function create(properties) {
            return new CSymbol(properties);
        };

        /**
         * Encodes the specified CSymbol message. Does not implicitly {@link jl3ct.CSymbol.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.CSymbol
         * @static
         * @param {jl3ct.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.JPState != null && Object.hasOwnProperty.call(message, "JPState"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.JPState);
            if (message.JPMult != null && Object.hasOwnProperty.call(message, "JPMult"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.JPMult);
            if (message.Number != null && Object.hasOwnProperty.call(message, "Number"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Number);
            return writer;
        };

        /**
         * Encodes the specified CSymbol message, length delimited. Does not implicitly {@link jl3ct.CSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.CSymbol
         * @static
         * @param {jl3ct.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.CSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.JPState = reader.int32();
                        break;
                    }
                case 3: {
                        message.JPMult = reader.int32();
                        break;
                    }
                case 4: {
                        message.Number = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSymbol message.
         * @function verify
         * @memberof jl3ct.CSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                if (!$util.isInteger(message.JPState))
                    return "JPState: integer expected";
            if (message.JPMult != null && message.hasOwnProperty("JPMult"))
                if (!$util.isInteger(message.JPMult))
                    return "JPMult: integer expected";
            if (message.Number != null && message.hasOwnProperty("Number"))
                if (typeof message.Number !== "number")
                    return "Number: number expected";
            return null;
        };

        /**
         * Creates a CSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.CSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.CSymbol} CSymbol
         */
        CSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.CSymbol)
                return object;
            let message = new $root.jl3ct.CSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.JPState != null)
                message.JPState = object.JPState | 0;
            if (object.JPMult != null)
                message.JPMult = object.JPMult | 0;
            if (object.Number != null)
                message.Number = Number(object.Number);
            return message;
        };

        /**
         * Creates a plain object from a CSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.CSymbol
         * @static
         * @param {jl3ct.CSymbol} message CSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.JPState = 0;
                object.JPMult = 0;
                object.Number = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                object.JPState = message.JPState;
            if (message.JPMult != null && message.hasOwnProperty("JPMult"))
                object.JPMult = message.JPMult;
            if (message.Number != null && message.hasOwnProperty("Number"))
                object.Number = options.json && !isFinite(message.Number) ? String(message.Number) : message.Number;
            return object;
        };

        /**
         * Converts this CSymbol to JSON.
         * @function toJSON
         * @memberof jl3ct.CSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSymbol
         * @function getTypeUrl
         * @memberof jl3ct.CSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.CSymbol";
        };

        return CSymbol;
    })();

    jl3ct.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jl3ct
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jl3ct
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jl3ct.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jl3ct.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jl3ct.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jl3ct.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jl3ct.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jl3ct.AwardData
         * @static
         * @param {jl3ct.IAwardData=} [properties] Properties to set
         * @returns {jl3ct.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jl3ct.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.AwardData
         * @static
         * @param {jl3ct.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jl3ct.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.AwardData
         * @static
         * @param {jl3ct.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jl3ct.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.AwardData)
                return object;
            let message = new $root.jl3ct.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.AwardData
         * @static
         * @param {jl3ct.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jl3ct.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jl3ct.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.AwardData";
        };

        return AwardData;
    })();

    jl3ct.FeatureEffect = (function() {

        /**
         * Properties of a FeatureEffect.
         * @memberof jl3ct
         * @interface IFeatureEffect
         * @property {number|null} [ScatterPos] FeatureEffect ScatterPos
         * @property {Array.<number>|null} [EffectPos] FeatureEffect EffectPos
         * @property {number|null} [JPState] FeatureEffect JPState
         * @property {number|null} [Number] FeatureEffect Number
         */

        /**
         * Constructs a new FeatureEffect.
         * @memberof jl3ct
         * @classdesc Represents a FeatureEffect.
         * @implements IFeatureEffect
         * @constructor
         * @param {jl3ct.IFeatureEffect=} [properties] Properties to set
         */
        function FeatureEffect(properties) {
            this.EffectPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FeatureEffect ScatterPos.
         * @member {number} ScatterPos
         * @memberof jl3ct.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.ScatterPos = 0;

        /**
         * FeatureEffect EffectPos.
         * @member {Array.<number>} EffectPos
         * @memberof jl3ct.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.EffectPos = $util.emptyArray;

        /**
         * FeatureEffect JPState.
         * @member {number} JPState
         * @memberof jl3ct.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.JPState = 0;

        /**
         * FeatureEffect Number.
         * @member {number} Number
         * @memberof jl3ct.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.Number = 0;

        /**
         * Creates a new FeatureEffect instance using the specified properties.
         * @function create
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {jl3ct.IFeatureEffect=} [properties] Properties to set
         * @returns {jl3ct.FeatureEffect} FeatureEffect instance
         */
        FeatureEffect.create = function create(properties) {
            return new FeatureEffect(properties);
        };

        /**
         * Encodes the specified FeatureEffect message. Does not implicitly {@link jl3ct.FeatureEffect.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {jl3ct.IFeatureEffect} message FeatureEffect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ScatterPos != null && Object.hasOwnProperty.call(message, "ScatterPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ScatterPos);
            if (message.EffectPos != null && message.EffectPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.EffectPos.length; ++i)
                    writer.int32(message.EffectPos[i]);
                writer.ldelim();
            }
            if (message.JPState != null && Object.hasOwnProperty.call(message, "JPState"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.JPState);
            if (message.Number != null && Object.hasOwnProperty.call(message, "Number"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Number);
            return writer;
        };

        /**
         * Encodes the specified FeatureEffect message, length delimited. Does not implicitly {@link jl3ct.FeatureEffect.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {jl3ct.IFeatureEffect} message FeatureEffect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FeatureEffect message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.FeatureEffect} FeatureEffect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffect.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.FeatureEffect();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ScatterPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.EffectPos && message.EffectPos.length))
                            message.EffectPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.EffectPos.push(reader.int32());
                        } else
                            message.EffectPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.JPState = reader.int32();
                        break;
                    }
                case 4: {
                        message.Number = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FeatureEffect message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.FeatureEffect} FeatureEffect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeatureEffect message.
         * @function verify
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeatureEffect.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ScatterPos != null && message.hasOwnProperty("ScatterPos"))
                if (!$util.isInteger(message.ScatterPos))
                    return "ScatterPos: integer expected";
            if (message.EffectPos != null && message.hasOwnProperty("EffectPos")) {
                if (!Array.isArray(message.EffectPos))
                    return "EffectPos: array expected";
                for (let i = 0; i < message.EffectPos.length; ++i)
                    if (!$util.isInteger(message.EffectPos[i]))
                        return "EffectPos: integer[] expected";
            }
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                if (!$util.isInteger(message.JPState))
                    return "JPState: integer expected";
            if (message.Number != null && message.hasOwnProperty("Number"))
                if (typeof message.Number !== "number")
                    return "Number: number expected";
            return null;
        };

        /**
         * Creates a FeatureEffect message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.FeatureEffect} FeatureEffect
         */
        FeatureEffect.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.FeatureEffect)
                return object;
            let message = new $root.jl3ct.FeatureEffect();
            if (object.ScatterPos != null)
                message.ScatterPos = object.ScatterPos | 0;
            if (object.EffectPos) {
                if (!Array.isArray(object.EffectPos))
                    throw TypeError(".jl3ct.FeatureEffect.EffectPos: array expected");
                message.EffectPos = [];
                for (let i = 0; i < object.EffectPos.length; ++i)
                    message.EffectPos[i] = object.EffectPos[i] | 0;
            }
            if (object.JPState != null)
                message.JPState = object.JPState | 0;
            if (object.Number != null)
                message.Number = Number(object.Number);
            return message;
        };

        /**
         * Creates a plain object from a FeatureEffect message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {jl3ct.FeatureEffect} message FeatureEffect
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeatureEffect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.EffectPos = [];
            if (options.defaults) {
                object.ScatterPos = 0;
                object.JPState = 0;
                object.Number = 0;
            }
            if (message.ScatterPos != null && message.hasOwnProperty("ScatterPos"))
                object.ScatterPos = message.ScatterPos;
            if (message.EffectPos && message.EffectPos.length) {
                object.EffectPos = [];
                for (let j = 0; j < message.EffectPos.length; ++j)
                    object.EffectPos[j] = message.EffectPos[j];
            }
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                object.JPState = message.JPState;
            if (message.Number != null && message.hasOwnProperty("Number"))
                object.Number = options.json && !isFinite(message.Number) ? String(message.Number) : message.Number;
            return object;
        };

        /**
         * Converts this FeatureEffect to JSON.
         * @function toJSON
         * @memberof jl3ct.FeatureEffect
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeatureEffect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FeatureEffect
         * @function getTypeUrl
         * @memberof jl3ct.FeatureEffect
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FeatureEffect.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.FeatureEffect";
        };

        return FeatureEffect;
    })();

    jl3ct.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jl3ct
         * @interface IRoundInfo
         * @property {Array.<jl3ct.IColumn>|null} [MainPlateSymbol] RoundInfo MainPlateSymbol
         * @property {Array.<jl3ct.ICColumn>|null} [FreePlateSymbol] RoundInfo FreePlateSymbol
         * @property {Array.<jl3ct.ICColumn>|null} [FreePlateSymbolLog] RoundInfo FreePlateSymbolLog
         * @property {Array.<jl3ct.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [MGReelWeightResult] RoundInfo MGReelWeightResult
         * @property {Array.<jl3ct.IFeatureEffect>|null} [BlueData] RoundInfo BlueData
         * @property {Array.<jl3ct.IFeatureEffect>|null} [RedData] RoundInfo RedData
         * @property {Array.<jl3ct.IFeatureEffect>|null} [GreenData] RoundInfo GreenData
         * @property {Array.<boolean>|null} [AddCrack] RoundInfo AddCrack
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jl3ct
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jl3ct.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.MainPlateSymbol = [];
            this.FreePlateSymbol = [];
            this.FreePlateSymbolLog = [];
            this.AwardDataVec = [];
            this.BlueData = [];
            this.RedData = [];
            this.GreenData = [];
            this.AddCrack = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo MainPlateSymbol.
         * @member {Array.<jl3ct.IColumn>} MainPlateSymbol
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MainPlateSymbol = $util.emptyArray;

        /**
         * RoundInfo FreePlateSymbol.
         * @member {Array.<jl3ct.ICColumn>} FreePlateSymbol
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreePlateSymbol = $util.emptyArray;

        /**
         * RoundInfo FreePlateSymbolLog.
         * @member {Array.<jl3ct.ICColumn>} FreePlateSymbolLog
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreePlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jl3ct.IAwardData>} AwardDataVec
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo MGReelWeightResult.
         * @member {number} MGReelWeightResult
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGReelWeightResult = 0;

        /**
         * RoundInfo BlueData.
         * @member {Array.<jl3ct.IFeatureEffect>} BlueData
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BlueData = $util.emptyArray;

        /**
         * RoundInfo RedData.
         * @member {Array.<jl3ct.IFeatureEffect>} RedData
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RedData = $util.emptyArray;

        /**
         * RoundInfo GreenData.
         * @member {Array.<jl3ct.IFeatureEffect>} GreenData
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GreenData = $util.emptyArray;

        /**
         * RoundInfo AddCrack.
         * @member {Array.<boolean>} AddCrack
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrack = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jl3ct.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {jl3ct.IRoundInfo=} [properties] Properties to set
         * @returns {jl3ct.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jl3ct.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {jl3ct.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainPlateSymbol != null && message.MainPlateSymbol.length)
                for (let i = 0; i < message.MainPlateSymbol.length; ++i)
                    $root.jl3ct.Column.encode(message.MainPlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreePlateSymbol != null && message.FreePlateSymbol.length)
                for (let i = 0; i < message.FreePlateSymbol.length; ++i)
                    $root.jl3ct.CColumn.encode(message.FreePlateSymbol[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreePlateSymbolLog != null && message.FreePlateSymbolLog.length)
                for (let i = 0; i < message.FreePlateSymbolLog.length; ++i)
                    $root.jl3ct.CColumn.encode(message.FreePlateSymbolLog[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jl3ct.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeNowRound);
            if (message.MGReelWeightResult != null && Object.hasOwnProperty.call(message, "MGReelWeightResult"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.MGReelWeightResult);
            if (message.BlueData != null && message.BlueData.length)
                for (let i = 0; i < message.BlueData.length; ++i)
                    $root.jl3ct.FeatureEffect.encode(message.BlueData[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.RedData != null && message.RedData.length)
                for (let i = 0; i < message.RedData.length; ++i)
                    $root.jl3ct.FeatureEffect.encode(message.RedData[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.GreenData != null && message.GreenData.length)
                for (let i = 0; i < message.GreenData.length; ++i)
                    $root.jl3ct.FeatureEffect.encode(message.GreenData[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.AddCrack != null && message.AddCrack.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (let i = 0; i < message.AddCrack.length; ++i)
                    writer.bool(message.AddCrack[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jl3ct.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {jl3ct.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MainPlateSymbol && message.MainPlateSymbol.length))
                            message.MainPlateSymbol = [];
                        message.MainPlateSymbol.push($root.jl3ct.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.FreePlateSymbol && message.FreePlateSymbol.length))
                            message.FreePlateSymbol = [];
                        message.FreePlateSymbol.push($root.jl3ct.CColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FreePlateSymbolLog && message.FreePlateSymbolLog.length))
                            message.FreePlateSymbolLog = [];
                        message.FreePlateSymbolLog.push($root.jl3ct.CColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jl3ct.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.MGReelWeightResult = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.BlueData && message.BlueData.length))
                            message.BlueData = [];
                        message.BlueData.push($root.jl3ct.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                case 12: {
                        if (!(message.RedData && message.RedData.length))
                            message.RedData = [];
                        message.RedData.push($root.jl3ct.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                case 13: {
                        if (!(message.GreenData && message.GreenData.length))
                            message.GreenData = [];
                        message.GreenData.push($root.jl3ct.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                case 14: {
                        if (!(message.AddCrack && message.AddCrack.length))
                            message.AddCrack = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrack.push(reader.bool());
                        } else
                            message.AddCrack.push(reader.bool());
                        break;
                    }
                case 15: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainPlateSymbol != null && message.hasOwnProperty("MainPlateSymbol")) {
                if (!Array.isArray(message.MainPlateSymbol))
                    return "MainPlateSymbol: array expected";
                for (let i = 0; i < message.MainPlateSymbol.length; ++i) {
                    let error = $root.jl3ct.Column.verify(message.MainPlateSymbol[i]);
                    if (error)
                        return "MainPlateSymbol." + error;
                }
            }
            if (message.FreePlateSymbol != null && message.hasOwnProperty("FreePlateSymbol")) {
                if (!Array.isArray(message.FreePlateSymbol))
                    return "FreePlateSymbol: array expected";
                for (let i = 0; i < message.FreePlateSymbol.length; ++i) {
                    let error = $root.jl3ct.CColumn.verify(message.FreePlateSymbol[i]);
                    if (error)
                        return "FreePlateSymbol." + error;
                }
            }
            if (message.FreePlateSymbolLog != null && message.hasOwnProperty("FreePlateSymbolLog")) {
                if (!Array.isArray(message.FreePlateSymbolLog))
                    return "FreePlateSymbolLog: array expected";
                for (let i = 0; i < message.FreePlateSymbolLog.length; ++i) {
                    let error = $root.jl3ct.CColumn.verify(message.FreePlateSymbolLog[i]);
                    if (error)
                        return "FreePlateSymbolLog." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jl3ct.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.MGReelWeightResult != null && message.hasOwnProperty("MGReelWeightResult"))
                if (!$util.isInteger(message.MGReelWeightResult))
                    return "MGReelWeightResult: integer expected";
            if (message.BlueData != null && message.hasOwnProperty("BlueData")) {
                if (!Array.isArray(message.BlueData))
                    return "BlueData: array expected";
                for (let i = 0; i < message.BlueData.length; ++i) {
                    let error = $root.jl3ct.FeatureEffect.verify(message.BlueData[i]);
                    if (error)
                        return "BlueData." + error;
                }
            }
            if (message.RedData != null && message.hasOwnProperty("RedData")) {
                if (!Array.isArray(message.RedData))
                    return "RedData: array expected";
                for (let i = 0; i < message.RedData.length; ++i) {
                    let error = $root.jl3ct.FeatureEffect.verify(message.RedData[i]);
                    if (error)
                        return "RedData." + error;
                }
            }
            if (message.GreenData != null && message.hasOwnProperty("GreenData")) {
                if (!Array.isArray(message.GreenData))
                    return "GreenData: array expected";
                for (let i = 0; i < message.GreenData.length; ++i) {
                    let error = $root.jl3ct.FeatureEffect.verify(message.GreenData[i]);
                    if (error)
                        return "GreenData." + error;
                }
            }
            if (message.AddCrack != null && message.hasOwnProperty("AddCrack")) {
                if (!Array.isArray(message.AddCrack))
                    return "AddCrack: array expected";
                for (let i = 0; i < message.AddCrack.length; ++i)
                    if (typeof message.AddCrack[i] !== "boolean")
                        return "AddCrack: boolean[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.RoundInfo)
                return object;
            let message = new $root.jl3ct.RoundInfo();
            if (object.MainPlateSymbol) {
                if (!Array.isArray(object.MainPlateSymbol))
                    throw TypeError(".jl3ct.RoundInfo.MainPlateSymbol: array expected");
                message.MainPlateSymbol = [];
                for (let i = 0; i < object.MainPlateSymbol.length; ++i) {
                    if (typeof object.MainPlateSymbol[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.MainPlateSymbol: object expected");
                    message.MainPlateSymbol[i] = $root.jl3ct.Column.fromObject(object.MainPlateSymbol[i]);
                }
            }
            if (object.FreePlateSymbol) {
                if (!Array.isArray(object.FreePlateSymbol))
                    throw TypeError(".jl3ct.RoundInfo.FreePlateSymbol: array expected");
                message.FreePlateSymbol = [];
                for (let i = 0; i < object.FreePlateSymbol.length; ++i) {
                    if (typeof object.FreePlateSymbol[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.FreePlateSymbol: object expected");
                    message.FreePlateSymbol[i] = $root.jl3ct.CColumn.fromObject(object.FreePlateSymbol[i]);
                }
            }
            if (object.FreePlateSymbolLog) {
                if (!Array.isArray(object.FreePlateSymbolLog))
                    throw TypeError(".jl3ct.RoundInfo.FreePlateSymbolLog: array expected");
                message.FreePlateSymbolLog = [];
                for (let i = 0; i < object.FreePlateSymbolLog.length; ++i) {
                    if (typeof object.FreePlateSymbolLog[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.FreePlateSymbolLog: object expected");
                    message.FreePlateSymbolLog[i] = $root.jl3ct.CColumn.fromObject(object.FreePlateSymbolLog[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jl3ct.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jl3ct.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.MGReelWeightResult != null)
                message.MGReelWeightResult = object.MGReelWeightResult | 0;
            if (object.BlueData) {
                if (!Array.isArray(object.BlueData))
                    throw TypeError(".jl3ct.RoundInfo.BlueData: array expected");
                message.BlueData = [];
                for (let i = 0; i < object.BlueData.length; ++i) {
                    if (typeof object.BlueData[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.BlueData: object expected");
                    message.BlueData[i] = $root.jl3ct.FeatureEffect.fromObject(object.BlueData[i]);
                }
            }
            if (object.RedData) {
                if (!Array.isArray(object.RedData))
                    throw TypeError(".jl3ct.RoundInfo.RedData: array expected");
                message.RedData = [];
                for (let i = 0; i < object.RedData.length; ++i) {
                    if (typeof object.RedData[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.RedData: object expected");
                    message.RedData[i] = $root.jl3ct.FeatureEffect.fromObject(object.RedData[i]);
                }
            }
            if (object.GreenData) {
                if (!Array.isArray(object.GreenData))
                    throw TypeError(".jl3ct.RoundInfo.GreenData: array expected");
                message.GreenData = [];
                for (let i = 0; i < object.GreenData.length; ++i) {
                    if (typeof object.GreenData[i] !== "object")
                        throw TypeError(".jl3ct.RoundInfo.GreenData: object expected");
                    message.GreenData[i] = $root.jl3ct.FeatureEffect.fromObject(object.GreenData[i]);
                }
            }
            if (object.AddCrack) {
                if (!Array.isArray(object.AddCrack))
                    throw TypeError(".jl3ct.RoundInfo.AddCrack: array expected");
                message.AddCrack = [];
                for (let i = 0; i < object.AddCrack.length; ++i)
                    message.AddCrack[i] = Boolean(object.AddCrack[i]);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {jl3ct.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.MainPlateSymbol = [];
                object.FreePlateSymbol = [];
                object.FreePlateSymbolLog = [];
                object.AwardDataVec = [];
                object.BlueData = [];
                object.RedData = [];
                object.GreenData = [];
                object.AddCrack = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RoundWin = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.MGReelWeightResult = 0;
                object.MaxFlag = false;
            }
            if (message.MainPlateSymbol && message.MainPlateSymbol.length) {
                object.MainPlateSymbol = [];
                for (let j = 0; j < message.MainPlateSymbol.length; ++j)
                    object.MainPlateSymbol[j] = $root.jl3ct.Column.toObject(message.MainPlateSymbol[j], options);
            }
            if (message.FreePlateSymbol && message.FreePlateSymbol.length) {
                object.FreePlateSymbol = [];
                for (let j = 0; j < message.FreePlateSymbol.length; ++j)
                    object.FreePlateSymbol[j] = $root.jl3ct.CColumn.toObject(message.FreePlateSymbol[j], options);
            }
            if (message.FreePlateSymbolLog && message.FreePlateSymbolLog.length) {
                object.FreePlateSymbolLog = [];
                for (let j = 0; j < message.FreePlateSymbolLog.length; ++j)
                    object.FreePlateSymbolLog[j] = $root.jl3ct.CColumn.toObject(message.FreePlateSymbolLog[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jl3ct.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.MGReelWeightResult != null && message.hasOwnProperty("MGReelWeightResult"))
                object.MGReelWeightResult = message.MGReelWeightResult;
            if (message.BlueData && message.BlueData.length) {
                object.BlueData = [];
                for (let j = 0; j < message.BlueData.length; ++j)
                    object.BlueData[j] = $root.jl3ct.FeatureEffect.toObject(message.BlueData[j], options);
            }
            if (message.RedData && message.RedData.length) {
                object.RedData = [];
                for (let j = 0; j < message.RedData.length; ++j)
                    object.RedData[j] = $root.jl3ct.FeatureEffect.toObject(message.RedData[j], options);
            }
            if (message.GreenData && message.GreenData.length) {
                object.GreenData = [];
                for (let j = 0; j < message.GreenData.length; ++j)
                    object.GreenData[j] = $root.jl3ct.FeatureEffect.toObject(message.GreenData[j], options);
            }
            if (message.AddCrack && message.AddCrack.length) {
                object.AddCrack = [];
                for (let j = 0; j < message.AddCrack.length; ++j)
                    object.AddCrack[j] = message.AddCrack[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jl3ct.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jl3ct.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.RoundInfo";
        };

        return RoundInfo;
    })();

    jl3ct.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jl3ct
         * @interface ISpinAck
         * @property {Array.<jl3ct.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FolderIndex] SpinAck FolderIndex
         * @property {Array.<number>|null} [CrackLevels] SpinAck CrackLevels
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jl3ct
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jl3ct.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jl3ct.IRoundInfo>} RoundQueue
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FolderIndex.
         * @member {number} FolderIndex
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.FolderIndex = 0;

        /**
         * SpinAck CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3ct.SpinAck
         * @instance
         */
        SpinAck.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jl3ct.SpinAck
         * @static
         * @param {jl3ct.ISpinAck=} [properties] Properties to set
         * @returns {jl3ct.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jl3ct.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.SpinAck
         * @static
         * @param {jl3ct.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jl3ct.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            if (message.FolderIndex != null && Object.hasOwnProperty.call(message, "FolderIndex"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FolderIndex);
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jl3ct.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.SpinAck
         * @static
         * @param {jl3ct.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jl3ct.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                case 9: {
                        message.FolderIndex = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jl3ct.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jl3ct.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FolderIndex != null && message.hasOwnProperty("FolderIndex"))
                if (!$util.isInteger(message.FolderIndex))
                    return "FolderIndex: integer expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.SpinAck)
                return object;
            let message = new $root.jl3ct.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jl3ct.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jl3ct.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jl3ct.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FolderIndex != null)
                message.FolderIndex = object.FolderIndex | 0;
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3ct.SpinAck.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.SpinAck
         * @static
         * @param {jl3ct.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.CrackLevels = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
                object.FolderIndex = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jl3ct.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FolderIndex != null && message.hasOwnProperty("FolderIndex"))
                object.FolderIndex = message.FolderIndex;
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jl3ct.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jl3ct.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.SpinAck";
        };

        return SpinAck;
    })();

    jl3ct.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jl3ct
         * @interface IStateInfo
         * @property {Array.<number>|null} [CrackLevels] StateInfo CrackLevels
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jl3ct
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jl3ct.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3ct.StateInfo
         * @instance
         */
        StateInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jl3ct.StateInfo
         * @static
         * @param {jl3ct.IStateInfo=} [properties] Properties to set
         * @returns {jl3ct.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jl3ct.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.StateInfo
         * @static
         * @param {jl3ct.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jl3ct.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.StateInfo
         * @static
         * @param {jl3ct.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jl3ct.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.StateInfo)
                return object;
            let message = new $root.jl3ct.StateInfo();
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3ct.StateInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.StateInfo
         * @static
         * @param {jl3ct.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CrackLevels = [];
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jl3ct.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jl3ct.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.StateInfo";
        };

        return StateInfo;
    })();

    jl3ct.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jl3ct
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [JPOdds] GameInfoData JPOdds
         * @property {Array.<jl3ct.IColumn>|null} [BlueOdds] GameInfoData BlueOdds
         * @property {Array.<jl3ct.IColumn>|null} [CoinOdds] GameInfoData CoinOdds
         * @property {Array.<number>|null} [CrackLevels] GameInfoData CrackLevels
         * @property {number|null} [MaxBet] GameInfoData MaxBet
         * @property {Array.<number>|null} [BuyMul] GameInfoData BuyMul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jl3ct
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jl3ct.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.JPOdds = [];
            this.BlueOdds = [];
            this.CoinOdds = [];
            this.CrackLevels = [];
            this.BuyMul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<number>} JPOdds
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * GameInfoData BlueOdds.
         * @member {Array.<jl3ct.IColumn>} BlueOdds
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BlueOdds = $util.emptyArray;

        /**
         * GameInfoData CoinOdds.
         * @member {Array.<jl3ct.IColumn>} CoinOdds
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CoinOdds = $util.emptyArray;

        /**
         * GameInfoData CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CrackLevels = $util.emptyArray;

        /**
         * GameInfoData MaxBet.
         * @member {number} MaxBet
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.MaxBet = 0;

        /**
         * GameInfoData BuyMul.
         * @member {Array.<number>} BuyMul
         * @memberof jl3ct.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BuyMul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {jl3ct.IGameInfoData=} [properties] Properties to set
         * @returns {jl3ct.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jl3ct.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {jl3ct.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.JPOdds != null && message.JPOdds.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.JPOdds.length; ++i)
                    writer.int32(message.JPOdds[i]);
                writer.ldelim();
            }
            if (message.BlueOdds != null && message.BlueOdds.length)
                for (let i = 0; i < message.BlueOdds.length; ++i)
                    $root.jl3ct.Column.encode(message.BlueOdds[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.CoinOdds != null && message.CoinOdds.length)
                for (let i = 0; i < message.CoinOdds.length; ++i)
                    $root.jl3ct.Column.encode(message.CoinOdds[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            if (message.MaxBet != null && Object.hasOwnProperty.call(message, "MaxBet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.MaxBet);
            if (message.BuyMul != null && message.BuyMul.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.BuyMul.length; ++i)
                    writer.double(message.BuyMul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jl3ct.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {jl3ct.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPOdds.push(reader.int32());
                        } else
                            message.JPOdds.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.BlueOdds && message.BlueOdds.length))
                            message.BlueOdds = [];
                        message.BlueOdds.push($root.jl3ct.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.CoinOdds && message.CoinOdds.length))
                            message.CoinOdds = [];
                        message.CoinOdds.push($root.jl3ct.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                case 7: {
                        message.MaxBet = reader.double();
                        break;
                    }
                case 8: {
                        if (!(message.BuyMul && message.BuyMul.length))
                            message.BuyMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BuyMul.push(reader.double());
                        } else
                            message.BuyMul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i)
                    if (!$util.isInteger(message.JPOdds[i]))
                        return "JPOdds: integer[] expected";
            }
            if (message.BlueOdds != null && message.hasOwnProperty("BlueOdds")) {
                if (!Array.isArray(message.BlueOdds))
                    return "BlueOdds: array expected";
                for (let i = 0; i < message.BlueOdds.length; ++i) {
                    let error = $root.jl3ct.Column.verify(message.BlueOdds[i]);
                    if (error)
                        return "BlueOdds." + error;
                }
            }
            if (message.CoinOdds != null && message.hasOwnProperty("CoinOdds")) {
                if (!Array.isArray(message.CoinOdds))
                    return "CoinOdds: array expected";
                for (let i = 0; i < message.CoinOdds.length; ++i) {
                    let error = $root.jl3ct.Column.verify(message.CoinOdds[i]);
                    if (error)
                        return "CoinOdds." + error;
                }
            }
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                if (typeof message.MaxBet !== "number")
                    return "MaxBet: number expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul")) {
                if (!Array.isArray(message.BuyMul))
                    return "BuyMul: array expected";
                for (let i = 0; i < message.BuyMul.length; ++i)
                    if (typeof message.BuyMul[i] !== "number")
                        return "BuyMul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.GameInfoData)
                return object;
            let message = new $root.jl3ct.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jl3ct.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jl3ct.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i)
                    message.JPOdds[i] = object.JPOdds[i] | 0;
            }
            if (object.BlueOdds) {
                if (!Array.isArray(object.BlueOdds))
                    throw TypeError(".jl3ct.GameInfoData.BlueOdds: array expected");
                message.BlueOdds = [];
                for (let i = 0; i < object.BlueOdds.length; ++i) {
                    if (typeof object.BlueOdds[i] !== "object")
                        throw TypeError(".jl3ct.GameInfoData.BlueOdds: object expected");
                    message.BlueOdds[i] = $root.jl3ct.Column.fromObject(object.BlueOdds[i]);
                }
            }
            if (object.CoinOdds) {
                if (!Array.isArray(object.CoinOdds))
                    throw TypeError(".jl3ct.GameInfoData.CoinOdds: array expected");
                message.CoinOdds = [];
                for (let i = 0; i < object.CoinOdds.length; ++i) {
                    if (typeof object.CoinOdds[i] !== "object")
                        throw TypeError(".jl3ct.GameInfoData.CoinOdds: object expected");
                    message.CoinOdds[i] = $root.jl3ct.Column.fromObject(object.CoinOdds[i]);
                }
            }
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3ct.GameInfoData.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            if (object.MaxBet != null)
                message.MaxBet = Number(object.MaxBet);
            if (object.BuyMul) {
                if (!Array.isArray(object.BuyMul))
                    throw TypeError(".jl3ct.GameInfoData.BuyMul: array expected");
                message.BuyMul = [];
                for (let i = 0; i < object.BuyMul.length; ++i)
                    message.BuyMul[i] = Number(object.BuyMul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {jl3ct.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.JPOdds = [];
                object.BlueOdds = [];
                object.CoinOdds = [];
                object.CrackLevels = [];
                object.BuyMul = [];
            }
            if (options.defaults) {
                object.ShowExtra = false;
                object.MaxBet = 0;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = message.JPOdds[j];
            }
            if (message.BlueOdds && message.BlueOdds.length) {
                object.BlueOdds = [];
                for (let j = 0; j < message.BlueOdds.length; ++j)
                    object.BlueOdds[j] = $root.jl3ct.Column.toObject(message.BlueOdds[j], options);
            }
            if (message.CoinOdds && message.CoinOdds.length) {
                object.CoinOdds = [];
                for (let j = 0; j < message.CoinOdds.length; ++j)
                    object.CoinOdds[j] = $root.jl3ct.Column.toObject(message.CoinOdds[j], options);
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                object.MaxBet = options.json && !isFinite(message.MaxBet) ? String(message.MaxBet) : message.MaxBet;
            if (message.BuyMul && message.BuyMul.length) {
                object.BuyMul = [];
                for (let j = 0; j < message.BuyMul.length; ++j)
                    object.BuyMul[j] = options.json && !isFinite(message.BuyMul[j]) ? String(message.BuyMul[j]) : message.BuyMul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jl3ct.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jl3ct.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.GameInfoData";
        };

        return GameInfoData;
    })();

    jl3ct.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jl3ct
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         * @property {number|null} [BuyType] BuyReq BuyType
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jl3ct
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jl3ct.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jl3ct.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * BuyReq BuyType.
         * @member {number} BuyType
         * @memberof jl3ct.BuyReq
         * @instance
         */
        BuyReq.prototype.BuyType = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jl3ct.BuyReq
         * @static
         * @param {jl3ct.IBuyReq=} [properties] Properties to set
         * @returns {jl3ct.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jl3ct.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jl3ct.BuyReq
         * @static
         * @param {jl3ct.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            if (message.BuyType != null && Object.hasOwnProperty.call(message, "BuyType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.BuyType);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jl3ct.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct.BuyReq
         * @static
         * @param {jl3ct.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 2: {
                        message.BuyType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jl3ct.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                if (!$util.isInteger(message.BuyType))
                    return "BuyType: integer expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct.BuyReq)
                return object;
            let message = new $root.jl3ct.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.BuyType != null)
                message.BuyType = object.BuyType | 0;
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct.BuyReq
         * @static
         * @param {jl3ct.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseBet = 0;
                object.BuyType = 0;
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                object.BuyType = message.BuyType;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jl3ct.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jl3ct.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct.BuyReq";
        };

        return BuyReq;
    })();

    return jl3ct;
})();

export const jl3ct2 = $root.jl3ct2 = (() => {

    /**
     * Namespace jl3ct2.
     * @exports jl3ct2
     * @namespace
     */
    const jl3ct2 = {};

    jl3ct2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jl3ct2
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jl3ct2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jl3ct2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jl3ct2.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jl3ct2.Column
         * @static
         * @param {jl3ct2.IColumn=} [properties] Properties to set
         * @returns {jl3ct2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jl3ct2.Column.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.Column
         * @static
         * @param {jl3ct2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jl3ct2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.Column
         * @static
         * @param {jl3ct2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jl3ct2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.Column)
                return object;
            let message = new $root.jl3ct2.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3ct2.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.Column
         * @static
         * @param {jl3ct2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jl3ct2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jl3ct2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.Column";
        };

        return Column;
    })();

    jl3ct2.CColumn = (function() {

        /**
         * Properties of a CColumn.
         * @memberof jl3ct2
         * @interface ICColumn
         * @property {Array.<jl3ct2.ICSymbol>|null} [Col] CColumn Col
         */

        /**
         * Constructs a new CColumn.
         * @memberof jl3ct2
         * @classdesc Represents a CColumn.
         * @implements ICColumn
         * @constructor
         * @param {jl3ct2.ICColumn=} [properties] Properties to set
         */
        function CColumn(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CColumn Col.
         * @member {Array.<jl3ct2.ICSymbol>} Col
         * @memberof jl3ct2.CColumn
         * @instance
         */
        CColumn.prototype.Col = $util.emptyArray;

        /**
         * Creates a new CColumn instance using the specified properties.
         * @function create
         * @memberof jl3ct2.CColumn
         * @static
         * @param {jl3ct2.ICColumn=} [properties] Properties to set
         * @returns {jl3ct2.CColumn} CColumn instance
         */
        CColumn.create = function create(properties) {
            return new CColumn(properties);
        };

        /**
         * Encodes the specified CColumn message. Does not implicitly {@link jl3ct2.CColumn.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.CColumn
         * @static
         * @param {jl3ct2.ICColumn} message CColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jl3ct2.CSymbol.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CColumn message, length delimited. Does not implicitly {@link jl3ct2.CColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.CColumn
         * @static
         * @param {jl3ct2.ICColumn} message CColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.CColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.CColumn} CColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.CColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jl3ct2.CSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.CColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.CColumn} CColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CColumn message.
         * @function verify
         * @memberof jl3ct2.CColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jl3ct2.CSymbol.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.CColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.CColumn} CColumn
         */
        CColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.CColumn)
                return object;
            let message = new $root.jl3ct2.CColumn();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3ct2.CColumn.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jl3ct2.CColumn.Col: object expected");
                    message.Col[i] = $root.jl3ct2.CSymbol.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.CColumn
         * @static
         * @param {jl3ct2.CColumn} message CColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jl3ct2.CSymbol.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this CColumn to JSON.
         * @function toJSON
         * @memberof jl3ct2.CColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CColumn
         * @function getTypeUrl
         * @memberof jl3ct2.CColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.CColumn";
        };

        return CColumn;
    })();

    jl3ct2.FreePlateCSymbolData = (function() {

        /**
         * Properties of a FreePlateCSymbolData.
         * @memberof jl3ct2
         * @interface IFreePlateCSymbolData
         * @property {Array.<jl3ct2.ICColumn>|null} [FPSD] FreePlateCSymbolData FPSD
         */

        /**
         * Constructs a new FreePlateCSymbolData.
         * @memberof jl3ct2
         * @classdesc Represents a FreePlateCSymbolData.
         * @implements IFreePlateCSymbolData
         * @constructor
         * @param {jl3ct2.IFreePlateCSymbolData=} [properties] Properties to set
         */
        function FreePlateCSymbolData(properties) {
            this.FPSD = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreePlateCSymbolData FPSD.
         * @member {Array.<jl3ct2.ICColumn>} FPSD
         * @memberof jl3ct2.FreePlateCSymbolData
         * @instance
         */
        FreePlateCSymbolData.prototype.FPSD = $util.emptyArray;

        /**
         * Creates a new FreePlateCSymbolData instance using the specified properties.
         * @function create
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {jl3ct2.IFreePlateCSymbolData=} [properties] Properties to set
         * @returns {jl3ct2.FreePlateCSymbolData} FreePlateCSymbolData instance
         */
        FreePlateCSymbolData.create = function create(properties) {
            return new FreePlateCSymbolData(properties);
        };

        /**
         * Encodes the specified FreePlateCSymbolData message. Does not implicitly {@link jl3ct2.FreePlateCSymbolData.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {jl3ct2.IFreePlateCSymbolData} message FreePlateCSymbolData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreePlateCSymbolData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FPSD != null && message.FPSD.length)
                for (let i = 0; i < message.FPSD.length; ++i)
                    $root.jl3ct2.CColumn.encode(message.FPSD[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FreePlateCSymbolData message, length delimited. Does not implicitly {@link jl3ct2.FreePlateCSymbolData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {jl3ct2.IFreePlateCSymbolData} message FreePlateCSymbolData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreePlateCSymbolData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreePlateCSymbolData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.FreePlateCSymbolData} FreePlateCSymbolData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreePlateCSymbolData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.FreePlateCSymbolData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FPSD && message.FPSD.length))
                            message.FPSD = [];
                        message.FPSD.push($root.jl3ct2.CColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreePlateCSymbolData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.FreePlateCSymbolData} FreePlateCSymbolData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreePlateCSymbolData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreePlateCSymbolData message.
         * @function verify
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreePlateCSymbolData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FPSD != null && message.hasOwnProperty("FPSD")) {
                if (!Array.isArray(message.FPSD))
                    return "FPSD: array expected";
                for (let i = 0; i < message.FPSD.length; ++i) {
                    let error = $root.jl3ct2.CColumn.verify(message.FPSD[i]);
                    if (error)
                        return "FPSD." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FreePlateCSymbolData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.FreePlateCSymbolData} FreePlateCSymbolData
         */
        FreePlateCSymbolData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.FreePlateCSymbolData)
                return object;
            let message = new $root.jl3ct2.FreePlateCSymbolData();
            if (object.FPSD) {
                if (!Array.isArray(object.FPSD))
                    throw TypeError(".jl3ct2.FreePlateCSymbolData.FPSD: array expected");
                message.FPSD = [];
                for (let i = 0; i < object.FPSD.length; ++i) {
                    if (typeof object.FPSD[i] !== "object")
                        throw TypeError(".jl3ct2.FreePlateCSymbolData.FPSD: object expected");
                    message.FPSD[i] = $root.jl3ct2.CColumn.fromObject(object.FPSD[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FreePlateCSymbolData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {jl3ct2.FreePlateCSymbolData} message FreePlateCSymbolData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreePlateCSymbolData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FPSD = [];
            if (message.FPSD && message.FPSD.length) {
                object.FPSD = [];
                for (let j = 0; j < message.FPSD.length; ++j)
                    object.FPSD[j] = $root.jl3ct2.CColumn.toObject(message.FPSD[j], options);
            }
            return object;
        };

        /**
         * Converts this FreePlateCSymbolData to JSON.
         * @function toJSON
         * @memberof jl3ct2.FreePlateCSymbolData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreePlateCSymbolData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreePlateCSymbolData
         * @function getTypeUrl
         * @memberof jl3ct2.FreePlateCSymbolData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreePlateCSymbolData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.FreePlateCSymbolData";
        };

        return FreePlateCSymbolData;
    })();

    jl3ct2.CSymbol = (function() {

        /**
         * Properties of a CSymbol.
         * @memberof jl3ct2
         * @interface ICSymbol
         * @property {number|null} [Symbol] CSymbol Symbol
         * @property {number|null} [JPState] CSymbol JPState
         * @property {number|null} [JPMult] CSymbol JPMult
         * @property {number|null} [Number] CSymbol Number
         */

        /**
         * Constructs a new CSymbol.
         * @memberof jl3ct2
         * @classdesc Represents a CSymbol.
         * @implements ICSymbol
         * @constructor
         * @param {jl3ct2.ICSymbol=} [properties] Properties to set
         */
        function CSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSymbol Symbol.
         * @member {number} Symbol
         * @memberof jl3ct2.CSymbol
         * @instance
         */
        CSymbol.prototype.Symbol = 0;

        /**
         * CSymbol JPState.
         * @member {number} JPState
         * @memberof jl3ct2.CSymbol
         * @instance
         */
        CSymbol.prototype.JPState = 0;

        /**
         * CSymbol JPMult.
         * @member {number} JPMult
         * @memberof jl3ct2.CSymbol
         * @instance
         */
        CSymbol.prototype.JPMult = 0;

        /**
         * CSymbol Number.
         * @member {number} Number
         * @memberof jl3ct2.CSymbol
         * @instance
         */
        CSymbol.prototype.Number = 0;

        /**
         * Creates a new CSymbol instance using the specified properties.
         * @function create
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {jl3ct2.ICSymbol=} [properties] Properties to set
         * @returns {jl3ct2.CSymbol} CSymbol instance
         */
        CSymbol.create = function create(properties) {
            return new CSymbol(properties);
        };

        /**
         * Encodes the specified CSymbol message. Does not implicitly {@link jl3ct2.CSymbol.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {jl3ct2.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.JPState != null && Object.hasOwnProperty.call(message, "JPState"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.JPState);
            if (message.JPMult != null && Object.hasOwnProperty.call(message, "JPMult"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.JPMult);
            if (message.Number != null && Object.hasOwnProperty.call(message, "Number"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Number);
            return writer;
        };

        /**
         * Encodes the specified CSymbol message, length delimited. Does not implicitly {@link jl3ct2.CSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {jl3ct2.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.CSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.JPState = reader.int32();
                        break;
                    }
                case 3: {
                        message.JPMult = reader.int32();
                        break;
                    }
                case 4: {
                        message.Number = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSymbol message.
         * @function verify
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                if (!$util.isInteger(message.JPState))
                    return "JPState: integer expected";
            if (message.JPMult != null && message.hasOwnProperty("JPMult"))
                if (!$util.isInteger(message.JPMult))
                    return "JPMult: integer expected";
            if (message.Number != null && message.hasOwnProperty("Number"))
                if (typeof message.Number !== "number")
                    return "Number: number expected";
            return null;
        };

        /**
         * Creates a CSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.CSymbol} CSymbol
         */
        CSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.CSymbol)
                return object;
            let message = new $root.jl3ct2.CSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.JPState != null)
                message.JPState = object.JPState | 0;
            if (object.JPMult != null)
                message.JPMult = object.JPMult | 0;
            if (object.Number != null)
                message.Number = Number(object.Number);
            return message;
        };

        /**
         * Creates a plain object from a CSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {jl3ct2.CSymbol} message CSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.JPState = 0;
                object.JPMult = 0;
                object.Number = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                object.JPState = message.JPState;
            if (message.JPMult != null && message.hasOwnProperty("JPMult"))
                object.JPMult = message.JPMult;
            if (message.Number != null && message.hasOwnProperty("Number"))
                object.Number = options.json && !isFinite(message.Number) ? String(message.Number) : message.Number;
            return object;
        };

        /**
         * Converts this CSymbol to JSON.
         * @function toJSON
         * @memberof jl3ct2.CSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSymbol
         * @function getTypeUrl
         * @memberof jl3ct2.CSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.CSymbol";
        };

        return CSymbol;
    })();

    jl3ct2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jl3ct2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jl3ct2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jl3ct2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jl3ct2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jl3ct2.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jl3ct2.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jl3ct2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jl3ct2.AwardData
         * @static
         * @param {jl3ct2.IAwardData=} [properties] Properties to set
         * @returns {jl3ct2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jl3ct2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.AwardData
         * @static
         * @param {jl3ct2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jl3ct2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.AwardData
         * @static
         * @param {jl3ct2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jl3ct2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.AwardData)
                return object;
            let message = new $root.jl3ct2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.AwardData
         * @static
         * @param {jl3ct2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jl3ct2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jl3ct2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.AwardData";
        };

        return AwardData;
    })();

    jl3ct2.FeatureEffect = (function() {

        /**
         * Properties of a FeatureEffect.
         * @memberof jl3ct2
         * @interface IFeatureEffect
         * @property {number|null} [ScatterPos] FeatureEffect ScatterPos
         * @property {Array.<number>|null} [EffectPos] FeatureEffect EffectPos
         * @property {number|null} [JPState] FeatureEffect JPState
         * @property {number|null} [Number] FeatureEffect Number
         */

        /**
         * Constructs a new FeatureEffect.
         * @memberof jl3ct2
         * @classdesc Represents a FeatureEffect.
         * @implements IFeatureEffect
         * @constructor
         * @param {jl3ct2.IFeatureEffect=} [properties] Properties to set
         */
        function FeatureEffect(properties) {
            this.EffectPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FeatureEffect ScatterPos.
         * @member {number} ScatterPos
         * @memberof jl3ct2.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.ScatterPos = 0;

        /**
         * FeatureEffect EffectPos.
         * @member {Array.<number>} EffectPos
         * @memberof jl3ct2.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.EffectPos = $util.emptyArray;

        /**
         * FeatureEffect JPState.
         * @member {number} JPState
         * @memberof jl3ct2.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.JPState = 0;

        /**
         * FeatureEffect Number.
         * @member {number} Number
         * @memberof jl3ct2.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.Number = 0;

        /**
         * Creates a new FeatureEffect instance using the specified properties.
         * @function create
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {jl3ct2.IFeatureEffect=} [properties] Properties to set
         * @returns {jl3ct2.FeatureEffect} FeatureEffect instance
         */
        FeatureEffect.create = function create(properties) {
            return new FeatureEffect(properties);
        };

        /**
         * Encodes the specified FeatureEffect message. Does not implicitly {@link jl3ct2.FeatureEffect.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {jl3ct2.IFeatureEffect} message FeatureEffect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ScatterPos != null && Object.hasOwnProperty.call(message, "ScatterPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ScatterPos);
            if (message.EffectPos != null && message.EffectPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.EffectPos.length; ++i)
                    writer.int32(message.EffectPos[i]);
                writer.ldelim();
            }
            if (message.JPState != null && Object.hasOwnProperty.call(message, "JPState"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.JPState);
            if (message.Number != null && Object.hasOwnProperty.call(message, "Number"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Number);
            return writer;
        };

        /**
         * Encodes the specified FeatureEffect message, length delimited. Does not implicitly {@link jl3ct2.FeatureEffect.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {jl3ct2.IFeatureEffect} message FeatureEffect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FeatureEffect message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.FeatureEffect} FeatureEffect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffect.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.FeatureEffect();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ScatterPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.EffectPos && message.EffectPos.length))
                            message.EffectPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.EffectPos.push(reader.int32());
                        } else
                            message.EffectPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.JPState = reader.int32();
                        break;
                    }
                case 4: {
                        message.Number = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FeatureEffect message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.FeatureEffect} FeatureEffect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeatureEffect message.
         * @function verify
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeatureEffect.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ScatterPos != null && message.hasOwnProperty("ScatterPos"))
                if (!$util.isInteger(message.ScatterPos))
                    return "ScatterPos: integer expected";
            if (message.EffectPos != null && message.hasOwnProperty("EffectPos")) {
                if (!Array.isArray(message.EffectPos))
                    return "EffectPos: array expected";
                for (let i = 0; i < message.EffectPos.length; ++i)
                    if (!$util.isInteger(message.EffectPos[i]))
                        return "EffectPos: integer[] expected";
            }
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                if (!$util.isInteger(message.JPState))
                    return "JPState: integer expected";
            if (message.Number != null && message.hasOwnProperty("Number"))
                if (typeof message.Number !== "number")
                    return "Number: number expected";
            return null;
        };

        /**
         * Creates a FeatureEffect message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.FeatureEffect} FeatureEffect
         */
        FeatureEffect.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.FeatureEffect)
                return object;
            let message = new $root.jl3ct2.FeatureEffect();
            if (object.ScatterPos != null)
                message.ScatterPos = object.ScatterPos | 0;
            if (object.EffectPos) {
                if (!Array.isArray(object.EffectPos))
                    throw TypeError(".jl3ct2.FeatureEffect.EffectPos: array expected");
                message.EffectPos = [];
                for (let i = 0; i < object.EffectPos.length; ++i)
                    message.EffectPos[i] = object.EffectPos[i] | 0;
            }
            if (object.JPState != null)
                message.JPState = object.JPState | 0;
            if (object.Number != null)
                message.Number = Number(object.Number);
            return message;
        };

        /**
         * Creates a plain object from a FeatureEffect message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {jl3ct2.FeatureEffect} message FeatureEffect
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeatureEffect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.EffectPos = [];
            if (options.defaults) {
                object.ScatterPos = 0;
                object.JPState = 0;
                object.Number = 0;
            }
            if (message.ScatterPos != null && message.hasOwnProperty("ScatterPos"))
                object.ScatterPos = message.ScatterPos;
            if (message.EffectPos && message.EffectPos.length) {
                object.EffectPos = [];
                for (let j = 0; j < message.EffectPos.length; ++j)
                    object.EffectPos[j] = message.EffectPos[j];
            }
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                object.JPState = message.JPState;
            if (message.Number != null && message.hasOwnProperty("Number"))
                object.Number = options.json && !isFinite(message.Number) ? String(message.Number) : message.Number;
            return object;
        };

        /**
         * Converts this FeatureEffect to JSON.
         * @function toJSON
         * @memberof jl3ct2.FeatureEffect
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeatureEffect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FeatureEffect
         * @function getTypeUrl
         * @memberof jl3ct2.FeatureEffect
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FeatureEffect.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.FeatureEffect";
        };

        return FeatureEffect;
    })();

    jl3ct2.FeatureEffectData = (function() {

        /**
         * Properties of a FeatureEffectData.
         * @memberof jl3ct2
         * @interface IFeatureEffectData
         * @property {Array.<jl3ct2.IFeatureEffect>|null} [FED] FeatureEffectData FED
         */

        /**
         * Constructs a new FeatureEffectData.
         * @memberof jl3ct2
         * @classdesc Represents a FeatureEffectData.
         * @implements IFeatureEffectData
         * @constructor
         * @param {jl3ct2.IFeatureEffectData=} [properties] Properties to set
         */
        function FeatureEffectData(properties) {
            this.FED = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FeatureEffectData FED.
         * @member {Array.<jl3ct2.IFeatureEffect>} FED
         * @memberof jl3ct2.FeatureEffectData
         * @instance
         */
        FeatureEffectData.prototype.FED = $util.emptyArray;

        /**
         * Creates a new FeatureEffectData instance using the specified properties.
         * @function create
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {jl3ct2.IFeatureEffectData=} [properties] Properties to set
         * @returns {jl3ct2.FeatureEffectData} FeatureEffectData instance
         */
        FeatureEffectData.create = function create(properties) {
            return new FeatureEffectData(properties);
        };

        /**
         * Encodes the specified FeatureEffectData message. Does not implicitly {@link jl3ct2.FeatureEffectData.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {jl3ct2.IFeatureEffectData} message FeatureEffectData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffectData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FED != null && message.FED.length)
                for (let i = 0; i < message.FED.length; ++i)
                    $root.jl3ct2.FeatureEffect.encode(message.FED[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FeatureEffectData message, length delimited. Does not implicitly {@link jl3ct2.FeatureEffectData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {jl3ct2.IFeatureEffectData} message FeatureEffectData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffectData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FeatureEffectData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.FeatureEffectData} FeatureEffectData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffectData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.FeatureEffectData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FED && message.FED.length))
                            message.FED = [];
                        message.FED.push($root.jl3ct2.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FeatureEffectData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.FeatureEffectData} FeatureEffectData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffectData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeatureEffectData message.
         * @function verify
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeatureEffectData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FED != null && message.hasOwnProperty("FED")) {
                if (!Array.isArray(message.FED))
                    return "FED: array expected";
                for (let i = 0; i < message.FED.length; ++i) {
                    let error = $root.jl3ct2.FeatureEffect.verify(message.FED[i]);
                    if (error)
                        return "FED." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FeatureEffectData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.FeatureEffectData} FeatureEffectData
         */
        FeatureEffectData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.FeatureEffectData)
                return object;
            let message = new $root.jl3ct2.FeatureEffectData();
            if (object.FED) {
                if (!Array.isArray(object.FED))
                    throw TypeError(".jl3ct2.FeatureEffectData.FED: array expected");
                message.FED = [];
                for (let i = 0; i < object.FED.length; ++i) {
                    if (typeof object.FED[i] !== "object")
                        throw TypeError(".jl3ct2.FeatureEffectData.FED: object expected");
                    message.FED[i] = $root.jl3ct2.FeatureEffect.fromObject(object.FED[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FeatureEffectData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {jl3ct2.FeatureEffectData} message FeatureEffectData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeatureEffectData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FED = [];
            if (message.FED && message.FED.length) {
                object.FED = [];
                for (let j = 0; j < message.FED.length; ++j)
                    object.FED[j] = $root.jl3ct2.FeatureEffect.toObject(message.FED[j], options);
            }
            return object;
        };

        /**
         * Converts this FeatureEffectData to JSON.
         * @function toJSON
         * @memberof jl3ct2.FeatureEffectData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeatureEffectData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FeatureEffectData
         * @function getTypeUrl
         * @memberof jl3ct2.FeatureEffectData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FeatureEffectData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.FeatureEffectData";
        };

        return FeatureEffectData;
    })();

    jl3ct2.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jl3ct2
         * @interface IRoundInfo
         * @property {Array.<jl3ct2.IColumn>|null} [MainPlateSymbol] RoundInfo MainPlateSymbol
         * @property {Array.<jl3ct2.IFreePlateCSymbolData>|null} [FreePlateSymbol] RoundInfo FreePlateSymbol
         * @property {Array.<jl3ct2.IFreePlateCSymbolData>|null} [FreePlateSymbolLog] RoundInfo FreePlateSymbolLog
         * @property {Array.<jl3ct2.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [MGReelWeightResult] RoundInfo MGReelWeightResult
         * @property {Array.<jl3ct2.IFeatureEffectData>|null} [BlueData] RoundInfo BlueData
         * @property {Array.<jl3ct2.IFeatureEffectData>|null} [RedData] RoundInfo RedData
         * @property {Array.<jl3ct2.IFeatureEffectData>|null} [GreenData] RoundInfo GreenData
         * @property {Array.<boolean>|null} [AddCrack] RoundInfo AddCrack
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jl3ct2
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jl3ct2.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.MainPlateSymbol = [];
            this.FreePlateSymbol = [];
            this.FreePlateSymbolLog = [];
            this.AwardDataVec = [];
            this.BlueData = [];
            this.RedData = [];
            this.GreenData = [];
            this.AddCrack = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo MainPlateSymbol.
         * @member {Array.<jl3ct2.IColumn>} MainPlateSymbol
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MainPlateSymbol = $util.emptyArray;

        /**
         * RoundInfo FreePlateSymbol.
         * @member {Array.<jl3ct2.IFreePlateCSymbolData>} FreePlateSymbol
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreePlateSymbol = $util.emptyArray;

        /**
         * RoundInfo FreePlateSymbolLog.
         * @member {Array.<jl3ct2.IFreePlateCSymbolData>} FreePlateSymbolLog
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreePlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jl3ct2.IAwardData>} AwardDataVec
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo MGReelWeightResult.
         * @member {number} MGReelWeightResult
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGReelWeightResult = 0;

        /**
         * RoundInfo BlueData.
         * @member {Array.<jl3ct2.IFeatureEffectData>} BlueData
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BlueData = $util.emptyArray;

        /**
         * RoundInfo RedData.
         * @member {Array.<jl3ct2.IFeatureEffectData>} RedData
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RedData = $util.emptyArray;

        /**
         * RoundInfo GreenData.
         * @member {Array.<jl3ct2.IFeatureEffectData>} GreenData
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GreenData = $util.emptyArray;

        /**
         * RoundInfo AddCrack.
         * @member {Array.<boolean>} AddCrack
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrack = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jl3ct2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {jl3ct2.IRoundInfo=} [properties] Properties to set
         * @returns {jl3ct2.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jl3ct2.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {jl3ct2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainPlateSymbol != null && message.MainPlateSymbol.length)
                for (let i = 0; i < message.MainPlateSymbol.length; ++i)
                    $root.jl3ct2.Column.encode(message.MainPlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreePlateSymbol != null && message.FreePlateSymbol.length)
                for (let i = 0; i < message.FreePlateSymbol.length; ++i)
                    $root.jl3ct2.FreePlateCSymbolData.encode(message.FreePlateSymbol[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreePlateSymbolLog != null && message.FreePlateSymbolLog.length)
                for (let i = 0; i < message.FreePlateSymbolLog.length; ++i)
                    $root.jl3ct2.FreePlateCSymbolData.encode(message.FreePlateSymbolLog[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jl3ct2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeNowRound);
            if (message.MGReelWeightResult != null && Object.hasOwnProperty.call(message, "MGReelWeightResult"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.MGReelWeightResult);
            if (message.BlueData != null && message.BlueData.length)
                for (let i = 0; i < message.BlueData.length; ++i)
                    $root.jl3ct2.FeatureEffectData.encode(message.BlueData[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.RedData != null && message.RedData.length)
                for (let i = 0; i < message.RedData.length; ++i)
                    $root.jl3ct2.FeatureEffectData.encode(message.RedData[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.GreenData != null && message.GreenData.length)
                for (let i = 0; i < message.GreenData.length; ++i)
                    $root.jl3ct2.FeatureEffectData.encode(message.GreenData[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.AddCrack != null && message.AddCrack.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (let i = 0; i < message.AddCrack.length; ++i)
                    writer.bool(message.AddCrack[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jl3ct2.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {jl3ct2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MainPlateSymbol && message.MainPlateSymbol.length))
                            message.MainPlateSymbol = [];
                        message.MainPlateSymbol.push($root.jl3ct2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.FreePlateSymbol && message.FreePlateSymbol.length))
                            message.FreePlateSymbol = [];
                        message.FreePlateSymbol.push($root.jl3ct2.FreePlateCSymbolData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FreePlateSymbolLog && message.FreePlateSymbolLog.length))
                            message.FreePlateSymbolLog = [];
                        message.FreePlateSymbolLog.push($root.jl3ct2.FreePlateCSymbolData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jl3ct2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.MGReelWeightResult = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.BlueData && message.BlueData.length))
                            message.BlueData = [];
                        message.BlueData.push($root.jl3ct2.FeatureEffectData.decode(reader, reader.uint32()));
                        break;
                    }
                case 12: {
                        if (!(message.RedData && message.RedData.length))
                            message.RedData = [];
                        message.RedData.push($root.jl3ct2.FeatureEffectData.decode(reader, reader.uint32()));
                        break;
                    }
                case 13: {
                        if (!(message.GreenData && message.GreenData.length))
                            message.GreenData = [];
                        message.GreenData.push($root.jl3ct2.FeatureEffectData.decode(reader, reader.uint32()));
                        break;
                    }
                case 14: {
                        if (!(message.AddCrack && message.AddCrack.length))
                            message.AddCrack = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrack.push(reader.bool());
                        } else
                            message.AddCrack.push(reader.bool());
                        break;
                    }
                case 15: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainPlateSymbol != null && message.hasOwnProperty("MainPlateSymbol")) {
                if (!Array.isArray(message.MainPlateSymbol))
                    return "MainPlateSymbol: array expected";
                for (let i = 0; i < message.MainPlateSymbol.length; ++i) {
                    let error = $root.jl3ct2.Column.verify(message.MainPlateSymbol[i]);
                    if (error)
                        return "MainPlateSymbol." + error;
                }
            }
            if (message.FreePlateSymbol != null && message.hasOwnProperty("FreePlateSymbol")) {
                if (!Array.isArray(message.FreePlateSymbol))
                    return "FreePlateSymbol: array expected";
                for (let i = 0; i < message.FreePlateSymbol.length; ++i) {
                    let error = $root.jl3ct2.FreePlateCSymbolData.verify(message.FreePlateSymbol[i]);
                    if (error)
                        return "FreePlateSymbol." + error;
                }
            }
            if (message.FreePlateSymbolLog != null && message.hasOwnProperty("FreePlateSymbolLog")) {
                if (!Array.isArray(message.FreePlateSymbolLog))
                    return "FreePlateSymbolLog: array expected";
                for (let i = 0; i < message.FreePlateSymbolLog.length; ++i) {
                    let error = $root.jl3ct2.FreePlateCSymbolData.verify(message.FreePlateSymbolLog[i]);
                    if (error)
                        return "FreePlateSymbolLog." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jl3ct2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.MGReelWeightResult != null && message.hasOwnProperty("MGReelWeightResult"))
                if (!$util.isInteger(message.MGReelWeightResult))
                    return "MGReelWeightResult: integer expected";
            if (message.BlueData != null && message.hasOwnProperty("BlueData")) {
                if (!Array.isArray(message.BlueData))
                    return "BlueData: array expected";
                for (let i = 0; i < message.BlueData.length; ++i) {
                    let error = $root.jl3ct2.FeatureEffectData.verify(message.BlueData[i]);
                    if (error)
                        return "BlueData." + error;
                }
            }
            if (message.RedData != null && message.hasOwnProperty("RedData")) {
                if (!Array.isArray(message.RedData))
                    return "RedData: array expected";
                for (let i = 0; i < message.RedData.length; ++i) {
                    let error = $root.jl3ct2.FeatureEffectData.verify(message.RedData[i]);
                    if (error)
                        return "RedData." + error;
                }
            }
            if (message.GreenData != null && message.hasOwnProperty("GreenData")) {
                if (!Array.isArray(message.GreenData))
                    return "GreenData: array expected";
                for (let i = 0; i < message.GreenData.length; ++i) {
                    let error = $root.jl3ct2.FeatureEffectData.verify(message.GreenData[i]);
                    if (error)
                        return "GreenData." + error;
                }
            }
            if (message.AddCrack != null && message.hasOwnProperty("AddCrack")) {
                if (!Array.isArray(message.AddCrack))
                    return "AddCrack: array expected";
                for (let i = 0; i < message.AddCrack.length; ++i)
                    if (typeof message.AddCrack[i] !== "boolean")
                        return "AddCrack: boolean[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.RoundInfo)
                return object;
            let message = new $root.jl3ct2.RoundInfo();
            if (object.MainPlateSymbol) {
                if (!Array.isArray(object.MainPlateSymbol))
                    throw TypeError(".jl3ct2.RoundInfo.MainPlateSymbol: array expected");
                message.MainPlateSymbol = [];
                for (let i = 0; i < object.MainPlateSymbol.length; ++i) {
                    if (typeof object.MainPlateSymbol[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.MainPlateSymbol: object expected");
                    message.MainPlateSymbol[i] = $root.jl3ct2.Column.fromObject(object.MainPlateSymbol[i]);
                }
            }
            if (object.FreePlateSymbol) {
                if (!Array.isArray(object.FreePlateSymbol))
                    throw TypeError(".jl3ct2.RoundInfo.FreePlateSymbol: array expected");
                message.FreePlateSymbol = [];
                for (let i = 0; i < object.FreePlateSymbol.length; ++i) {
                    if (typeof object.FreePlateSymbol[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.FreePlateSymbol: object expected");
                    message.FreePlateSymbol[i] = $root.jl3ct2.FreePlateCSymbolData.fromObject(object.FreePlateSymbol[i]);
                }
            }
            if (object.FreePlateSymbolLog) {
                if (!Array.isArray(object.FreePlateSymbolLog))
                    throw TypeError(".jl3ct2.RoundInfo.FreePlateSymbolLog: array expected");
                message.FreePlateSymbolLog = [];
                for (let i = 0; i < object.FreePlateSymbolLog.length; ++i) {
                    if (typeof object.FreePlateSymbolLog[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.FreePlateSymbolLog: object expected");
                    message.FreePlateSymbolLog[i] = $root.jl3ct2.FreePlateCSymbolData.fromObject(object.FreePlateSymbolLog[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jl3ct2.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jl3ct2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.MGReelWeightResult != null)
                message.MGReelWeightResult = object.MGReelWeightResult | 0;
            if (object.BlueData) {
                if (!Array.isArray(object.BlueData))
                    throw TypeError(".jl3ct2.RoundInfo.BlueData: array expected");
                message.BlueData = [];
                for (let i = 0; i < object.BlueData.length; ++i) {
                    if (typeof object.BlueData[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.BlueData: object expected");
                    message.BlueData[i] = $root.jl3ct2.FeatureEffectData.fromObject(object.BlueData[i]);
                }
            }
            if (object.RedData) {
                if (!Array.isArray(object.RedData))
                    throw TypeError(".jl3ct2.RoundInfo.RedData: array expected");
                message.RedData = [];
                for (let i = 0; i < object.RedData.length; ++i) {
                    if (typeof object.RedData[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.RedData: object expected");
                    message.RedData[i] = $root.jl3ct2.FeatureEffectData.fromObject(object.RedData[i]);
                }
            }
            if (object.GreenData) {
                if (!Array.isArray(object.GreenData))
                    throw TypeError(".jl3ct2.RoundInfo.GreenData: array expected");
                message.GreenData = [];
                for (let i = 0; i < object.GreenData.length; ++i) {
                    if (typeof object.GreenData[i] !== "object")
                        throw TypeError(".jl3ct2.RoundInfo.GreenData: object expected");
                    message.GreenData[i] = $root.jl3ct2.FeatureEffectData.fromObject(object.GreenData[i]);
                }
            }
            if (object.AddCrack) {
                if (!Array.isArray(object.AddCrack))
                    throw TypeError(".jl3ct2.RoundInfo.AddCrack: array expected");
                message.AddCrack = [];
                for (let i = 0; i < object.AddCrack.length; ++i)
                    message.AddCrack[i] = Boolean(object.AddCrack[i]);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {jl3ct2.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.MainPlateSymbol = [];
                object.FreePlateSymbol = [];
                object.FreePlateSymbolLog = [];
                object.AwardDataVec = [];
                object.BlueData = [];
                object.RedData = [];
                object.GreenData = [];
                object.AddCrack = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RoundWin = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.MGReelWeightResult = 0;
                object.MaxFlag = false;
            }
            if (message.MainPlateSymbol && message.MainPlateSymbol.length) {
                object.MainPlateSymbol = [];
                for (let j = 0; j < message.MainPlateSymbol.length; ++j)
                    object.MainPlateSymbol[j] = $root.jl3ct2.Column.toObject(message.MainPlateSymbol[j], options);
            }
            if (message.FreePlateSymbol && message.FreePlateSymbol.length) {
                object.FreePlateSymbol = [];
                for (let j = 0; j < message.FreePlateSymbol.length; ++j)
                    object.FreePlateSymbol[j] = $root.jl3ct2.FreePlateCSymbolData.toObject(message.FreePlateSymbol[j], options);
            }
            if (message.FreePlateSymbolLog && message.FreePlateSymbolLog.length) {
                object.FreePlateSymbolLog = [];
                for (let j = 0; j < message.FreePlateSymbolLog.length; ++j)
                    object.FreePlateSymbolLog[j] = $root.jl3ct2.FreePlateCSymbolData.toObject(message.FreePlateSymbolLog[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jl3ct2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.MGReelWeightResult != null && message.hasOwnProperty("MGReelWeightResult"))
                object.MGReelWeightResult = message.MGReelWeightResult;
            if (message.BlueData && message.BlueData.length) {
                object.BlueData = [];
                for (let j = 0; j < message.BlueData.length; ++j)
                    object.BlueData[j] = $root.jl3ct2.FeatureEffectData.toObject(message.BlueData[j], options);
            }
            if (message.RedData && message.RedData.length) {
                object.RedData = [];
                for (let j = 0; j < message.RedData.length; ++j)
                    object.RedData[j] = $root.jl3ct2.FeatureEffectData.toObject(message.RedData[j], options);
            }
            if (message.GreenData && message.GreenData.length) {
                object.GreenData = [];
                for (let j = 0; j < message.GreenData.length; ++j)
                    object.GreenData[j] = $root.jl3ct2.FeatureEffectData.toObject(message.GreenData[j], options);
            }
            if (message.AddCrack && message.AddCrack.length) {
                object.AddCrack = [];
                for (let j = 0; j < message.AddCrack.length; ++j)
                    object.AddCrack[j] = message.AddCrack[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jl3ct2.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jl3ct2.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.RoundInfo";
        };

        return RoundInfo;
    })();

    jl3ct2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jl3ct2
         * @interface ISpinAck
         * @property {Array.<jl3ct2.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FolderIndex] SpinAck FolderIndex
         * @property {Array.<number>|null} [CrackLevels] SpinAck CrackLevels
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jl3ct2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jl3ct2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jl3ct2.IRoundInfo>} RoundQueue
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FolderIndex.
         * @member {number} FolderIndex
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.FolderIndex = 0;

        /**
         * SpinAck CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3ct2.SpinAck
         * @instance
         */
        SpinAck.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {jl3ct2.ISpinAck=} [properties] Properties to set
         * @returns {jl3ct2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jl3ct2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {jl3ct2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jl3ct2.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            if (message.FolderIndex != null && Object.hasOwnProperty.call(message, "FolderIndex"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FolderIndex);
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jl3ct2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {jl3ct2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jl3ct2.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                case 9: {
                        message.FolderIndex = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jl3ct2.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FolderIndex != null && message.hasOwnProperty("FolderIndex"))
                if (!$util.isInteger(message.FolderIndex))
                    return "FolderIndex: integer expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.SpinAck)
                return object;
            let message = new $root.jl3ct2.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jl3ct2.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jl3ct2.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jl3ct2.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FolderIndex != null)
                message.FolderIndex = object.FolderIndex | 0;
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3ct2.SpinAck.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {jl3ct2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.CrackLevels = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
                object.FolderIndex = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jl3ct2.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FolderIndex != null && message.hasOwnProperty("FolderIndex"))
                object.FolderIndex = message.FolderIndex;
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jl3ct2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jl3ct2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.SpinAck";
        };

        return SpinAck;
    })();

    jl3ct2.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jl3ct2
         * @interface IStateInfo
         * @property {Array.<number>|null} [CrackLevels] StateInfo CrackLevels
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jl3ct2
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jl3ct2.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3ct2.StateInfo
         * @instance
         */
        StateInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {jl3ct2.IStateInfo=} [properties] Properties to set
         * @returns {jl3ct2.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jl3ct2.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {jl3ct2.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jl3ct2.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {jl3ct2.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.StateInfo)
                return object;
            let message = new $root.jl3ct2.StateInfo();
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3ct2.StateInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {jl3ct2.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CrackLevels = [];
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jl3ct2.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jl3ct2.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.StateInfo";
        };

        return StateInfo;
    })();

    jl3ct2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jl3ct2
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [JPOdds] GameInfoData JPOdds
         * @property {Array.<jl3ct2.IColumn>|null} [BlueOdds] GameInfoData BlueOdds
         * @property {Array.<jl3ct2.IColumn>|null} [CoinOdds] GameInfoData CoinOdds
         * @property {Array.<number>|null} [CrackLevels] GameInfoData CrackLevels
         * @property {number|null} [MaxBet] GameInfoData MaxBet
         * @property {Array.<number>|null} [BuyMul] GameInfoData BuyMul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jl3ct2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jl3ct2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.JPOdds = [];
            this.BlueOdds = [];
            this.CoinOdds = [];
            this.CrackLevels = [];
            this.BuyMul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<number>} JPOdds
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * GameInfoData BlueOdds.
         * @member {Array.<jl3ct2.IColumn>} BlueOdds
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BlueOdds = $util.emptyArray;

        /**
         * GameInfoData CoinOdds.
         * @member {Array.<jl3ct2.IColumn>} CoinOdds
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CoinOdds = $util.emptyArray;

        /**
         * GameInfoData CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CrackLevels = $util.emptyArray;

        /**
         * GameInfoData MaxBet.
         * @member {number} MaxBet
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.MaxBet = 0;

        /**
         * GameInfoData BuyMul.
         * @member {Array.<number>} BuyMul
         * @memberof jl3ct2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BuyMul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {jl3ct2.IGameInfoData=} [properties] Properties to set
         * @returns {jl3ct2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jl3ct2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {jl3ct2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.JPOdds != null && message.JPOdds.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.JPOdds.length; ++i)
                    writer.int32(message.JPOdds[i]);
                writer.ldelim();
            }
            if (message.BlueOdds != null && message.BlueOdds.length)
                for (let i = 0; i < message.BlueOdds.length; ++i)
                    $root.jl3ct2.Column.encode(message.BlueOdds[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.CoinOdds != null && message.CoinOdds.length)
                for (let i = 0; i < message.CoinOdds.length; ++i)
                    $root.jl3ct2.Column.encode(message.CoinOdds[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            if (message.MaxBet != null && Object.hasOwnProperty.call(message, "MaxBet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.MaxBet);
            if (message.BuyMul != null && message.BuyMul.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.BuyMul.length; ++i)
                    writer.double(message.BuyMul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jl3ct2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {jl3ct2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPOdds.push(reader.int32());
                        } else
                            message.JPOdds.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.BlueOdds && message.BlueOdds.length))
                            message.BlueOdds = [];
                        message.BlueOdds.push($root.jl3ct2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.CoinOdds && message.CoinOdds.length))
                            message.CoinOdds = [];
                        message.CoinOdds.push($root.jl3ct2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                case 7: {
                        message.MaxBet = reader.double();
                        break;
                    }
                case 8: {
                        if (!(message.BuyMul && message.BuyMul.length))
                            message.BuyMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BuyMul.push(reader.double());
                        } else
                            message.BuyMul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i)
                    if (!$util.isInteger(message.JPOdds[i]))
                        return "JPOdds: integer[] expected";
            }
            if (message.BlueOdds != null && message.hasOwnProperty("BlueOdds")) {
                if (!Array.isArray(message.BlueOdds))
                    return "BlueOdds: array expected";
                for (let i = 0; i < message.BlueOdds.length; ++i) {
                    let error = $root.jl3ct2.Column.verify(message.BlueOdds[i]);
                    if (error)
                        return "BlueOdds." + error;
                }
            }
            if (message.CoinOdds != null && message.hasOwnProperty("CoinOdds")) {
                if (!Array.isArray(message.CoinOdds))
                    return "CoinOdds: array expected";
                for (let i = 0; i < message.CoinOdds.length; ++i) {
                    let error = $root.jl3ct2.Column.verify(message.CoinOdds[i]);
                    if (error)
                        return "CoinOdds." + error;
                }
            }
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                if (typeof message.MaxBet !== "number")
                    return "MaxBet: number expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul")) {
                if (!Array.isArray(message.BuyMul))
                    return "BuyMul: array expected";
                for (let i = 0; i < message.BuyMul.length; ++i)
                    if (typeof message.BuyMul[i] !== "number")
                        return "BuyMul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.GameInfoData)
                return object;
            let message = new $root.jl3ct2.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jl3ct2.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jl3ct2.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i)
                    message.JPOdds[i] = object.JPOdds[i] | 0;
            }
            if (object.BlueOdds) {
                if (!Array.isArray(object.BlueOdds))
                    throw TypeError(".jl3ct2.GameInfoData.BlueOdds: array expected");
                message.BlueOdds = [];
                for (let i = 0; i < object.BlueOdds.length; ++i) {
                    if (typeof object.BlueOdds[i] !== "object")
                        throw TypeError(".jl3ct2.GameInfoData.BlueOdds: object expected");
                    message.BlueOdds[i] = $root.jl3ct2.Column.fromObject(object.BlueOdds[i]);
                }
            }
            if (object.CoinOdds) {
                if (!Array.isArray(object.CoinOdds))
                    throw TypeError(".jl3ct2.GameInfoData.CoinOdds: array expected");
                message.CoinOdds = [];
                for (let i = 0; i < object.CoinOdds.length; ++i) {
                    if (typeof object.CoinOdds[i] !== "object")
                        throw TypeError(".jl3ct2.GameInfoData.CoinOdds: object expected");
                    message.CoinOdds[i] = $root.jl3ct2.Column.fromObject(object.CoinOdds[i]);
                }
            }
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3ct2.GameInfoData.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            if (object.MaxBet != null)
                message.MaxBet = Number(object.MaxBet);
            if (object.BuyMul) {
                if (!Array.isArray(object.BuyMul))
                    throw TypeError(".jl3ct2.GameInfoData.BuyMul: array expected");
                message.BuyMul = [];
                for (let i = 0; i < object.BuyMul.length; ++i)
                    message.BuyMul[i] = Number(object.BuyMul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {jl3ct2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.JPOdds = [];
                object.BlueOdds = [];
                object.CoinOdds = [];
                object.CrackLevels = [];
                object.BuyMul = [];
            }
            if (options.defaults) {
                object.ShowExtra = false;
                object.MaxBet = 0;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = message.JPOdds[j];
            }
            if (message.BlueOdds && message.BlueOdds.length) {
                object.BlueOdds = [];
                for (let j = 0; j < message.BlueOdds.length; ++j)
                    object.BlueOdds[j] = $root.jl3ct2.Column.toObject(message.BlueOdds[j], options);
            }
            if (message.CoinOdds && message.CoinOdds.length) {
                object.CoinOdds = [];
                for (let j = 0; j < message.CoinOdds.length; ++j)
                    object.CoinOdds[j] = $root.jl3ct2.Column.toObject(message.CoinOdds[j], options);
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                object.MaxBet = options.json && !isFinite(message.MaxBet) ? String(message.MaxBet) : message.MaxBet;
            if (message.BuyMul && message.BuyMul.length) {
                object.BuyMul = [];
                for (let j = 0; j < message.BuyMul.length; ++j)
                    object.BuyMul[j] = options.json && !isFinite(message.BuyMul[j]) ? String(message.BuyMul[j]) : message.BuyMul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jl3ct2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jl3ct2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.GameInfoData";
        };

        return GameInfoData;
    })();

    jl3ct2.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jl3ct2
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         * @property {number|null} [BuyType] BuyReq BuyType
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jl3ct2
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jl3ct2.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jl3ct2.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * BuyReq BuyType.
         * @member {number} BuyType
         * @memberof jl3ct2.BuyReq
         * @instance
         */
        BuyReq.prototype.BuyType = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {jl3ct2.IBuyReq=} [properties] Properties to set
         * @returns {jl3ct2.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jl3ct2.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {jl3ct2.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            if (message.BuyType != null && Object.hasOwnProperty.call(message, "BuyType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.BuyType);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jl3ct2.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {jl3ct2.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3ct2.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3ct2.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 2: {
                        message.BuyType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3ct2.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                if (!$util.isInteger(message.BuyType))
                    return "BuyType: integer expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3ct2.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3ct2.BuyReq)
                return object;
            let message = new $root.jl3ct2.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.BuyType != null)
                message.BuyType = object.BuyType | 0;
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {jl3ct2.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseBet = 0;
                object.BuyType = 0;
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                object.BuyType = message.BuyType;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jl3ct2.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jl3ct2.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3ct2.BuyReq";
        };

        return BuyReq;
    })();

    return jl3ct2;
})();

export const jl3cwh = $root.jl3cwh = (() => {

    /**
     * Namespace jl3cwh.
     * @exports jl3cwh
     * @namespace
     */
    const jl3cwh = {};

    jl3cwh.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jl3cwh
         * @interface IColumn
         * @property {Array.<number>|null} [Column] Column Column
         */

        /**
         * Constructs a new Column.
         * @memberof jl3cwh
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jl3cwh.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Column = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Column.
         * @member {Array.<number>} Column
         * @memberof jl3cwh.Column
         * @instance
         */
        Column.prototype.Column = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jl3cwh.Column
         * @static
         * @param {jl3cwh.IColumn=} [properties] Properties to set
         * @returns {jl3cwh.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jl3cwh.Column.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.Column
         * @static
         * @param {jl3cwh.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && message.Column.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Column.length; ++i)
                    writer.int32(message.Column[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jl3cwh.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.Column
         * @static
         * @param {jl3cwh.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Column && message.Column.length))
                            message.Column = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column.push(reader.int32());
                        } else
                            message.Column.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jl3cwh.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column")) {
                if (!Array.isArray(message.Column))
                    return "Column: array expected";
                for (let i = 0; i < message.Column.length; ++i)
                    if (!$util.isInteger(message.Column[i]))
                        return "Column: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.Column)
                return object;
            let message = new $root.jl3cwh.Column();
            if (object.Column) {
                if (!Array.isArray(object.Column))
                    throw TypeError(".jl3cwh.Column.Column: array expected");
                message.Column = [];
                for (let i = 0; i < object.Column.length; ++i)
                    message.Column[i] = object.Column[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.Column
         * @static
         * @param {jl3cwh.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Column = [];
            if (message.Column && message.Column.length) {
                object.Column = [];
                for (let j = 0; j < message.Column.length; ++j)
                    object.Column[j] = message.Column[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jl3cwh.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jl3cwh.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.Column";
        };

        return Column;
    })();

    jl3cwh.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jl3cwh
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jl3cwh
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jl3cwh.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jl3cwh.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jl3cwh.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jl3cwh.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jl3cwh.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jl3cwh.AwardData
         * @static
         * @param {jl3cwh.IAwardData=} [properties] Properties to set
         * @returns {jl3cwh.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jl3cwh.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.AwardData
         * @static
         * @param {jl3cwh.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jl3cwh.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.AwardData
         * @static
         * @param {jl3cwh.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jl3cwh.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.AwardData)
                return object;
            let message = new $root.jl3cwh.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.AwardData
         * @static
         * @param {jl3cwh.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jl3cwh.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jl3cwh.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.AwardData";
        };

        return AwardData;
    })();

    jl3cwh.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jl3cwh
         * @interface IRoundInfo
         * @property {Array.<jl3cwh.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<number>|null} [NewWildPos] RoundInfo NewWildPos
         * @property {Array.<number>|null} [NowWildPos] RoundInfo NowWildPos
         * @property {Array.<number>|null} [RedEnvelopePos] RoundInfo RedEnvelopePos
         * @property {number|null} [RedEnvelopeWin] RoundInfo RedEnvelopeWin
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {jl3cwh.IJPData|null} [JPPlate] RoundInfo JPPlate
         * @property {Array.<jl3cwh.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [GameType] RoundInfo GameType
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [WildSymbol] RoundInfo WildSymbol
         * @property {Array.<jl3cwh.IFGRedCoin>|null} [FGRedCoins] RoundInfo FGRedCoins
         * @property {Array.<boolean>|null} [AddCrack] RoundInfo AddCrack
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<number>|null} [AddCrackLv] RoundInfo AddCrackLv
         * @property {Array.<number>|null} [CrackLevels] RoundInfo CrackLevels
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jl3cwh
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jl3cwh.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.NewWildPos = [];
            this.NowWildPos = [];
            this.RedEnvelopePos = [];
            this.AwardDataVec = [];
            this.FGRedCoins = [];
            this.AddCrack = [];
            this.AddCrackLv = [];
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jl3cwh.IColumn>} PlateSymbol
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo NewWildPos.
         * @member {Array.<number>} NewWildPos
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NewWildPos = $util.emptyArray;

        /**
         * RoundInfo NowWildPos.
         * @member {Array.<number>} NowWildPos
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NowWildPos = $util.emptyArray;

        /**
         * RoundInfo RedEnvelopePos.
         * @member {Array.<number>} RedEnvelopePos
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RedEnvelopePos = $util.emptyArray;

        /**
         * RoundInfo RedEnvelopeWin.
         * @member {number} RedEnvelopeWin
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RedEnvelopeWin = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo JPPlate.
         * @member {jl3cwh.IJPData|null|undefined} JPPlate
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.JPPlate = null;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jl3cwh.IAwardData>} AwardDataVec
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo GameType.
         * @member {number} GameType
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GameType = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo WildSymbol.
         * @member {number} WildSymbol
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildSymbol = 0;

        /**
         * RoundInfo FGRedCoins.
         * @member {Array.<jl3cwh.IFGRedCoin>} FGRedCoins
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGRedCoins = $util.emptyArray;

        /**
         * RoundInfo AddCrack.
         * @member {Array.<boolean>} AddCrack
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrack = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo AddCrackLv.
         * @member {Array.<number>} AddCrackLv
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrackLv = $util.emptyArray;

        /**
         * RoundInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3cwh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {jl3cwh.IRoundInfo=} [properties] Properties to set
         * @returns {jl3cwh.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jl3cwh.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {jl3cwh.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jl3cwh.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.NewWildPos != null && message.NewWildPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.NewWildPos.length; ++i)
                    writer.int32(message.NewWildPos[i]);
                writer.ldelim();
            }
            if (message.NowWildPos != null && message.NowWildPos.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.NowWildPos.length; ++i)
                    writer.int32(message.NowWildPos[i]);
                writer.ldelim();
            }
            if (message.RedEnvelopePos != null && message.RedEnvelopePos.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.RedEnvelopePos.length; ++i)
                    writer.int32(message.RedEnvelopePos[i]);
                writer.ldelim();
            }
            if (message.RedEnvelopeWin != null && Object.hasOwnProperty.call(message, "RedEnvelopeWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RedEnvelopeWin);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.JPPlate != null && Object.hasOwnProperty.call(message, "JPPlate"))
                $root.jl3cwh.JPData.encode(message.JPPlate, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jl3cwh.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AwardTypeFlag);
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.GameType);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.FreeTotalRound);
            if (message.WildSymbol != null && Object.hasOwnProperty.call(message, "WildSymbol"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.WildSymbol);
            if (message.FGRedCoins != null && message.FGRedCoins.length)
                for (let i = 0; i < message.FGRedCoins.length; ++i)
                    $root.jl3cwh.FGRedCoin.encode(message.FGRedCoins[i], writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.AddCrack != null && message.AddCrack.length) {
                writer.uint32(/* id 16, wireType 2 =*/130).fork();
                for (let i = 0; i < message.AddCrack.length; ++i)
                    writer.bool(message.AddCrack[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.MaxFlag);
            if (message.AddCrackLv != null && message.AddCrackLv.length) {
                writer.uint32(/* id 18, wireType 2 =*/146).fork();
                for (let i = 0; i < message.AddCrackLv.length; ++i)
                    writer.int32(message.AddCrackLv[i]);
                writer.ldelim();
            }
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 19, wireType 2 =*/154).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jl3cwh.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {jl3cwh.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jl3cwh.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.NewWildPos && message.NewWildPos.length))
                            message.NewWildPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NewWildPos.push(reader.int32());
                        } else
                            message.NewWildPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.NowWildPos && message.NowWildPos.length))
                            message.NowWildPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NowWildPos.push(reader.int32());
                        } else
                            message.NowWildPos.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.RedEnvelopePos && message.RedEnvelopePos.length))
                            message.RedEnvelopePos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RedEnvelopePos.push(reader.int32());
                        } else
                            message.RedEnvelopePos.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.RedEnvelopeWin = reader.double();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.JPPlate = $root.jl3cwh.JPData.decode(reader, reader.uint32());
                        break;
                    }
                case 8: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jl3cwh.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 9: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 10: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 11: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 13: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 14: {
                        message.WildSymbol = reader.int32();
                        break;
                    }
                case 15: {
                        if (!(message.FGRedCoins && message.FGRedCoins.length))
                            message.FGRedCoins = [];
                        message.FGRedCoins.push($root.jl3cwh.FGRedCoin.decode(reader, reader.uint32()));
                        break;
                    }
                case 16: {
                        if (!(message.AddCrack && message.AddCrack.length))
                            message.AddCrack = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrack.push(reader.bool());
                        } else
                            message.AddCrack.push(reader.bool());
                        break;
                    }
                case 17: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 18: {
                        if (!(message.AddCrackLv && message.AddCrackLv.length))
                            message.AddCrackLv = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrackLv.push(reader.int32());
                        } else
                            message.AddCrackLv.push(reader.int32());
                        break;
                    }
                case 19: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jl3cwh.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.NewWildPos != null && message.hasOwnProperty("NewWildPos")) {
                if (!Array.isArray(message.NewWildPos))
                    return "NewWildPos: array expected";
                for (let i = 0; i < message.NewWildPos.length; ++i)
                    if (!$util.isInteger(message.NewWildPos[i]))
                        return "NewWildPos: integer[] expected";
            }
            if (message.NowWildPos != null && message.hasOwnProperty("NowWildPos")) {
                if (!Array.isArray(message.NowWildPos))
                    return "NowWildPos: array expected";
                for (let i = 0; i < message.NowWildPos.length; ++i)
                    if (!$util.isInteger(message.NowWildPos[i]))
                        return "NowWildPos: integer[] expected";
            }
            if (message.RedEnvelopePos != null && message.hasOwnProperty("RedEnvelopePos")) {
                if (!Array.isArray(message.RedEnvelopePos))
                    return "RedEnvelopePos: array expected";
                for (let i = 0; i < message.RedEnvelopePos.length; ++i)
                    if (!$util.isInteger(message.RedEnvelopePos[i]))
                        return "RedEnvelopePos: integer[] expected";
            }
            if (message.RedEnvelopeWin != null && message.hasOwnProperty("RedEnvelopeWin"))
                if (typeof message.RedEnvelopeWin !== "number")
                    return "RedEnvelopeWin: number expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.JPPlate != null && message.hasOwnProperty("JPPlate")) {
                let error = $root.jl3cwh.JPData.verify(message.JPPlate);
                if (error)
                    return "JPPlate." + error;
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jl3cwh.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.WildSymbol != null && message.hasOwnProperty("WildSymbol"))
                if (!$util.isInteger(message.WildSymbol))
                    return "WildSymbol: integer expected";
            if (message.FGRedCoins != null && message.hasOwnProperty("FGRedCoins")) {
                if (!Array.isArray(message.FGRedCoins))
                    return "FGRedCoins: array expected";
                for (let i = 0; i < message.FGRedCoins.length; ++i) {
                    let error = $root.jl3cwh.FGRedCoin.verify(message.FGRedCoins[i]);
                    if (error)
                        return "FGRedCoins." + error;
                }
            }
            if (message.AddCrack != null && message.hasOwnProperty("AddCrack")) {
                if (!Array.isArray(message.AddCrack))
                    return "AddCrack: array expected";
                for (let i = 0; i < message.AddCrack.length; ++i)
                    if (typeof message.AddCrack[i] !== "boolean")
                        return "AddCrack: boolean[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.AddCrackLv != null && message.hasOwnProperty("AddCrackLv")) {
                if (!Array.isArray(message.AddCrackLv))
                    return "AddCrackLv: array expected";
                for (let i = 0; i < message.AddCrackLv.length; ++i)
                    if (!$util.isInteger(message.AddCrackLv[i]))
                        return "AddCrackLv: integer[] expected";
            }
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.RoundInfo)
                return object;
            let message = new $root.jl3cwh.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jl3cwh.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jl3cwh.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jl3cwh.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.NewWildPos) {
                if (!Array.isArray(object.NewWildPos))
                    throw TypeError(".jl3cwh.RoundInfo.NewWildPos: array expected");
                message.NewWildPos = [];
                for (let i = 0; i < object.NewWildPos.length; ++i)
                    message.NewWildPos[i] = object.NewWildPos[i] | 0;
            }
            if (object.NowWildPos) {
                if (!Array.isArray(object.NowWildPos))
                    throw TypeError(".jl3cwh.RoundInfo.NowWildPos: array expected");
                message.NowWildPos = [];
                for (let i = 0; i < object.NowWildPos.length; ++i)
                    message.NowWildPos[i] = object.NowWildPos[i] | 0;
            }
            if (object.RedEnvelopePos) {
                if (!Array.isArray(object.RedEnvelopePos))
                    throw TypeError(".jl3cwh.RoundInfo.RedEnvelopePos: array expected");
                message.RedEnvelopePos = [];
                for (let i = 0; i < object.RedEnvelopePos.length; ++i)
                    message.RedEnvelopePos[i] = object.RedEnvelopePos[i] | 0;
            }
            if (object.RedEnvelopeWin != null)
                message.RedEnvelopeWin = Number(object.RedEnvelopeWin);
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.JPPlate != null) {
                if (typeof object.JPPlate !== "object")
                    throw TypeError(".jl3cwh.RoundInfo.JPPlate: object expected");
                message.JPPlate = $root.jl3cwh.JPData.fromObject(object.JPPlate);
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jl3cwh.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jl3cwh.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jl3cwh.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.WildSymbol != null)
                message.WildSymbol = object.WildSymbol | 0;
            if (object.FGRedCoins) {
                if (!Array.isArray(object.FGRedCoins))
                    throw TypeError(".jl3cwh.RoundInfo.FGRedCoins: array expected");
                message.FGRedCoins = [];
                for (let i = 0; i < object.FGRedCoins.length; ++i) {
                    if (typeof object.FGRedCoins[i] !== "object")
                        throw TypeError(".jl3cwh.RoundInfo.FGRedCoins: object expected");
                    message.FGRedCoins[i] = $root.jl3cwh.FGRedCoin.fromObject(object.FGRedCoins[i]);
                }
            }
            if (object.AddCrack) {
                if (!Array.isArray(object.AddCrack))
                    throw TypeError(".jl3cwh.RoundInfo.AddCrack: array expected");
                message.AddCrack = [];
                for (let i = 0; i < object.AddCrack.length; ++i)
                    message.AddCrack[i] = Boolean(object.AddCrack[i]);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.AddCrackLv) {
                if (!Array.isArray(object.AddCrackLv))
                    throw TypeError(".jl3cwh.RoundInfo.AddCrackLv: array expected");
                message.AddCrackLv = [];
                for (let i = 0; i < object.AddCrackLv.length; ++i)
                    message.AddCrackLv[i] = object.AddCrackLv[i] | 0;
            }
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3cwh.RoundInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {jl3cwh.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.NewWildPos = [];
                object.NowWildPos = [];
                object.RedEnvelopePos = [];
                object.AwardDataVec = [];
                object.FGRedCoins = [];
                object.AddCrack = [];
                object.AddCrackLv = [];
                object.CrackLevels = [];
            }
            if (options.defaults) {
                object.RedEnvelopeWin = 0;
                object.RoundWin = 0;
                object.JPPlate = null;
                object.AwardTypeFlag = 0;
                object.GameType = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.WildSymbol = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jl3cwh.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.NewWildPos && message.NewWildPos.length) {
                object.NewWildPos = [];
                for (let j = 0; j < message.NewWildPos.length; ++j)
                    object.NewWildPos[j] = message.NewWildPos[j];
            }
            if (message.NowWildPos && message.NowWildPos.length) {
                object.NowWildPos = [];
                for (let j = 0; j < message.NowWildPos.length; ++j)
                    object.NowWildPos[j] = message.NowWildPos[j];
            }
            if (message.RedEnvelopePos && message.RedEnvelopePos.length) {
                object.RedEnvelopePos = [];
                for (let j = 0; j < message.RedEnvelopePos.length; ++j)
                    object.RedEnvelopePos[j] = message.RedEnvelopePos[j];
            }
            if (message.RedEnvelopeWin != null && message.hasOwnProperty("RedEnvelopeWin"))
                object.RedEnvelopeWin = options.json && !isFinite(message.RedEnvelopeWin) ? String(message.RedEnvelopeWin) : message.RedEnvelopeWin;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.JPPlate != null && message.hasOwnProperty("JPPlate"))
                object.JPPlate = $root.jl3cwh.JPData.toObject(message.JPPlate, options);
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jl3cwh.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.WildSymbol != null && message.hasOwnProperty("WildSymbol"))
                object.WildSymbol = message.WildSymbol;
            if (message.FGRedCoins && message.FGRedCoins.length) {
                object.FGRedCoins = [];
                for (let j = 0; j < message.FGRedCoins.length; ++j)
                    object.FGRedCoins[j] = $root.jl3cwh.FGRedCoin.toObject(message.FGRedCoins[j], options);
            }
            if (message.AddCrack && message.AddCrack.length) {
                object.AddCrack = [];
                for (let j = 0; j < message.AddCrack.length; ++j)
                    object.AddCrack[j] = message.AddCrack[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.AddCrackLv && message.AddCrackLv.length) {
                object.AddCrackLv = [];
                for (let j = 0; j < message.AddCrackLv.length; ++j)
                    object.AddCrackLv[j] = message.AddCrackLv[j];
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jl3cwh.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jl3cwh.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.RoundInfo";
        };

        return RoundInfo;
    })();

    jl3cwh.JPData = (function() {

        /**
         * Properties of a JPData.
         * @memberof jl3cwh
         * @interface IJPData
         * @property {Array.<number>|null} [SymbolSerial] JPData SymbolSerial
         * @property {number|null} [Win] JPData Win
         * @property {number|null} [HitSymbol] JPData HitSymbol
         * @property {number|null} [JPType] JPData JPType
         * @property {boolean|null} [IsUpgraded] JPData IsUpgraded
         */

        /**
         * Constructs a new JPData.
         * @memberof jl3cwh
         * @classdesc Represents a JPData.
         * @implements IJPData
         * @constructor
         * @param {jl3cwh.IJPData=} [properties] Properties to set
         */
        function JPData(properties) {
            this.SymbolSerial = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPData SymbolSerial.
         * @member {Array.<number>} SymbolSerial
         * @memberof jl3cwh.JPData
         * @instance
         */
        JPData.prototype.SymbolSerial = $util.emptyArray;

        /**
         * JPData Win.
         * @member {number} Win
         * @memberof jl3cwh.JPData
         * @instance
         */
        JPData.prototype.Win = 0;

        /**
         * JPData HitSymbol.
         * @member {number} HitSymbol
         * @memberof jl3cwh.JPData
         * @instance
         */
        JPData.prototype.HitSymbol = 0;

        /**
         * JPData JPType.
         * @member {number} JPType
         * @memberof jl3cwh.JPData
         * @instance
         */
        JPData.prototype.JPType = 0;

        /**
         * JPData IsUpgraded.
         * @member {boolean} IsUpgraded
         * @memberof jl3cwh.JPData
         * @instance
         */
        JPData.prototype.IsUpgraded = false;

        /**
         * Creates a new JPData instance using the specified properties.
         * @function create
         * @memberof jl3cwh.JPData
         * @static
         * @param {jl3cwh.IJPData=} [properties] Properties to set
         * @returns {jl3cwh.JPData} JPData instance
         */
        JPData.create = function create(properties) {
            return new JPData(properties);
        };

        /**
         * Encodes the specified JPData message. Does not implicitly {@link jl3cwh.JPData.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.JPData
         * @static
         * @param {jl3cwh.IJPData} message JPData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.SymbolSerial != null && message.SymbolSerial.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.SymbolSerial.length; ++i)
                    writer.int32(message.SymbolSerial[i]);
                writer.ldelim();
            }
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Win);
            if (message.HitSymbol != null && Object.hasOwnProperty.call(message, "HitSymbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.HitSymbol);
            if (message.JPType != null && Object.hasOwnProperty.call(message, "JPType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.JPType);
            if (message.IsUpgraded != null && Object.hasOwnProperty.call(message, "IsUpgraded"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.IsUpgraded);
            return writer;
        };

        /**
         * Encodes the specified JPData message, length delimited. Does not implicitly {@link jl3cwh.JPData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.JPData
         * @static
         * @param {jl3cwh.IJPData} message JPData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.JPData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.JPData} JPData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.JPData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.SymbolSerial && message.SymbolSerial.length))
                            message.SymbolSerial = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.SymbolSerial.push(reader.int32());
                        } else
                            message.SymbolSerial.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.Win = reader.double();
                        break;
                    }
                case 3: {
                        message.HitSymbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.JPType = reader.int32();
                        break;
                    }
                case 5: {
                        message.IsUpgraded = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.JPData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.JPData} JPData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPData message.
         * @function verify
         * @memberof jl3cwh.JPData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.SymbolSerial != null && message.hasOwnProperty("SymbolSerial")) {
                if (!Array.isArray(message.SymbolSerial))
                    return "SymbolSerial: array expected";
                for (let i = 0; i < message.SymbolSerial.length; ++i)
                    if (!$util.isInteger(message.SymbolSerial[i]))
                        return "SymbolSerial: integer[] expected";
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.HitSymbol != null && message.hasOwnProperty("HitSymbol"))
                if (!$util.isInteger(message.HitSymbol))
                    return "HitSymbol: integer expected";
            if (message.JPType != null && message.hasOwnProperty("JPType"))
                if (!$util.isInteger(message.JPType))
                    return "JPType: integer expected";
            if (message.IsUpgraded != null && message.hasOwnProperty("IsUpgraded"))
                if (typeof message.IsUpgraded !== "boolean")
                    return "IsUpgraded: boolean expected";
            return null;
        };

        /**
         * Creates a JPData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.JPData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.JPData} JPData
         */
        JPData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.JPData)
                return object;
            let message = new $root.jl3cwh.JPData();
            if (object.SymbolSerial) {
                if (!Array.isArray(object.SymbolSerial))
                    throw TypeError(".jl3cwh.JPData.SymbolSerial: array expected");
                message.SymbolSerial = [];
                for (let i = 0; i < object.SymbolSerial.length; ++i)
                    message.SymbolSerial[i] = object.SymbolSerial[i] | 0;
            }
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.HitSymbol != null)
                message.HitSymbol = object.HitSymbol | 0;
            if (object.JPType != null)
                message.JPType = object.JPType | 0;
            if (object.IsUpgraded != null)
                message.IsUpgraded = Boolean(object.IsUpgraded);
            return message;
        };

        /**
         * Creates a plain object from a JPData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.JPData
         * @static
         * @param {jl3cwh.JPData} message JPData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.SymbolSerial = [];
            if (options.defaults) {
                object.Win = 0;
                object.HitSymbol = 0;
                object.JPType = 0;
                object.IsUpgraded = false;
            }
            if (message.SymbolSerial && message.SymbolSerial.length) {
                object.SymbolSerial = [];
                for (let j = 0; j < message.SymbolSerial.length; ++j)
                    object.SymbolSerial[j] = message.SymbolSerial[j];
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.HitSymbol != null && message.hasOwnProperty("HitSymbol"))
                object.HitSymbol = message.HitSymbol;
            if (message.JPType != null && message.hasOwnProperty("JPType"))
                object.JPType = message.JPType;
            if (message.IsUpgraded != null && message.hasOwnProperty("IsUpgraded"))
                object.IsUpgraded = message.IsUpgraded;
            return object;
        };

        /**
         * Converts this JPData to JSON.
         * @function toJSON
         * @memberof jl3cwh.JPData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPData
         * @function getTypeUrl
         * @memberof jl3cwh.JPData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.JPData";
        };

        return JPData;
    })();

    jl3cwh.FGRedCoin = (function() {

        /**
         * Properties of a FGRedCoin.
         * @memberof jl3cwh
         * @interface IFGRedCoin
         * @property {number|null} [Pos] FGRedCoin Pos
         * @property {number|null} [AddRound] FGRedCoin AddRound
         * @property {number|null} [Win] FGRedCoin Win
         */

        /**
         * Constructs a new FGRedCoin.
         * @memberof jl3cwh
         * @classdesc Represents a FGRedCoin.
         * @implements IFGRedCoin
         * @constructor
         * @param {jl3cwh.IFGRedCoin=} [properties] Properties to set
         */
        function FGRedCoin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FGRedCoin Pos.
         * @member {number} Pos
         * @memberof jl3cwh.FGRedCoin
         * @instance
         */
        FGRedCoin.prototype.Pos = 0;

        /**
         * FGRedCoin AddRound.
         * @member {number} AddRound
         * @memberof jl3cwh.FGRedCoin
         * @instance
         */
        FGRedCoin.prototype.AddRound = 0;

        /**
         * FGRedCoin Win.
         * @member {number} Win
         * @memberof jl3cwh.FGRedCoin
         * @instance
         */
        FGRedCoin.prototype.Win = 0;

        /**
         * Creates a new FGRedCoin instance using the specified properties.
         * @function create
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {jl3cwh.IFGRedCoin=} [properties] Properties to set
         * @returns {jl3cwh.FGRedCoin} FGRedCoin instance
         */
        FGRedCoin.create = function create(properties) {
            return new FGRedCoin(properties);
        };

        /**
         * Encodes the specified FGRedCoin message. Does not implicitly {@link jl3cwh.FGRedCoin.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {jl3cwh.IFGRedCoin} message FGRedCoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FGRedCoin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AddRound);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified FGRedCoin message, length delimited. Does not implicitly {@link jl3cwh.FGRedCoin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {jl3cwh.IFGRedCoin} message FGRedCoin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FGRedCoin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FGRedCoin message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.FGRedCoin} FGRedCoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FGRedCoin.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.FGRedCoin();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FGRedCoin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.FGRedCoin} FGRedCoin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FGRedCoin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FGRedCoin message.
         * @function verify
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FGRedCoin.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates a FGRedCoin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.FGRedCoin} FGRedCoin
         */
        FGRedCoin.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.FGRedCoin)
                return object;
            let message = new $root.jl3cwh.FGRedCoin();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from a FGRedCoin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {jl3cwh.FGRedCoin} message FGRedCoin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FGRedCoin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.AddRound = 0;
                object.Win = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this FGRedCoin to JSON.
         * @function toJSON
         * @memberof jl3cwh.FGRedCoin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FGRedCoin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FGRedCoin
         * @function getTypeUrl
         * @memberof jl3cwh.FGRedCoin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FGRedCoin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.FGRedCoin";
        };

        return FGRedCoin;
    })();

    jl3cwh.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jl3cwh
         * @interface ISpinAck
         * @property {Array.<jl3cwh.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {jl3cwh.IStateInfo|null} [LastInfo] SpinAck LastInfo
         * @property {number|null} [JPFrontBefore] SpinAck JPFrontBefore
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jl3cwh
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jl3cwh.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jl3cwh.IRoundInfo>} RoundQueue
         * @memberof jl3cwh.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jl3cwh.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jl3cwh.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LastInfo.
         * @member {jl3cwh.IStateInfo|null|undefined} LastInfo
         * @memberof jl3cwh.SpinAck
         * @instance
         */
        SpinAck.prototype.LastInfo = null;

        /**
         * SpinAck JPFrontBefore.
         * @member {number} JPFrontBefore
         * @memberof jl3cwh.SpinAck
         * @instance
         */
        SpinAck.prototype.JPFrontBefore = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jl3cwh.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {jl3cwh.ISpinAck=} [properties] Properties to set
         * @returns {jl3cwh.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jl3cwh.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {jl3cwh.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jl3cwh.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LastInfo != null && Object.hasOwnProperty.call(message, "LastInfo"))
                $root.jl3cwh.StateInfo.encode(message.LastInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.JPFrontBefore != null && Object.hasOwnProperty.call(message, "JPFrontBefore"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.JPFrontBefore);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jl3cwh.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {jl3cwh.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jl3cwh.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LastInfo = $root.jl3cwh.StateInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 5: {
                        message.JPFrontBefore = reader.double();
                        break;
                    }
                case 6: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jl3cwh.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LastInfo != null && message.hasOwnProperty("LastInfo")) {
                let error = $root.jl3cwh.StateInfo.verify(message.LastInfo);
                if (error)
                    return "LastInfo." + error;
            }
            if (message.JPFrontBefore != null && message.hasOwnProperty("JPFrontBefore"))
                if (typeof message.JPFrontBefore !== "number")
                    return "JPFrontBefore: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.SpinAck)
                return object;
            let message = new $root.jl3cwh.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jl3cwh.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jl3cwh.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jl3cwh.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LastInfo != null) {
                if (typeof object.LastInfo !== "object")
                    throw TypeError(".jl3cwh.SpinAck.LastInfo: object expected");
                message.LastInfo = $root.jl3cwh.StateInfo.fromObject(object.LastInfo);
            }
            if (object.JPFrontBefore != null)
                message.JPFrontBefore = Number(object.JPFrontBefore);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {jl3cwh.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.LastInfo = null;
                object.JPFrontBefore = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jl3cwh.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LastInfo != null && message.hasOwnProperty("LastInfo"))
                object.LastInfo = $root.jl3cwh.StateInfo.toObject(message.LastInfo, options);
            if (message.JPFrontBefore != null && message.hasOwnProperty("JPFrontBefore"))
                object.JPFrontBefore = options.json && !isFinite(message.JPFrontBefore) ? String(message.JPFrontBefore) : message.JPFrontBefore;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jl3cwh.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jl3cwh.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.SpinAck";
        };

        return SpinAck;
    })();

    jl3cwh.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jl3cwh
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [JPOdds] GameInfoData JPOdds
         * @property {jl3cwh.IStateInfo|null} [State] GameInfoData State
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jl3cwh
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jl3cwh.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.JPOdds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jl3cwh.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jl3cwh.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<number>} JPOdds
         * @memberof jl3cwh.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * GameInfoData State.
         * @member {jl3cwh.IStateInfo|null|undefined} State
         * @memberof jl3cwh.GameInfoData
         * @instance
         */
        GameInfoData.prototype.State = null;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {jl3cwh.IGameInfoData=} [properties] Properties to set
         * @returns {jl3cwh.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jl3cwh.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {jl3cwh.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.JPOdds != null && message.JPOdds.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.JPOdds.length; ++i)
                    writer.int32(message.JPOdds[i]);
                writer.ldelim();
            }
            if (message.State != null && Object.hasOwnProperty.call(message, "State"))
                $root.jl3cwh.StateInfo.encode(message.State, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jl3cwh.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {jl3cwh.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPOdds.push(reader.int32());
                        } else
                            message.JPOdds.push(reader.int32());
                        break;
                    }
                case 4: {
                        message.State = $root.jl3cwh.StateInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i)
                    if (!$util.isInteger(message.JPOdds[i]))
                        return "JPOdds: integer[] expected";
            }
            if (message.State != null && message.hasOwnProperty("State")) {
                let error = $root.jl3cwh.StateInfo.verify(message.State);
                if (error)
                    return "State." + error;
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.GameInfoData)
                return object;
            let message = new $root.jl3cwh.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jl3cwh.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jl3cwh.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i)
                    message.JPOdds[i] = object.JPOdds[i] | 0;
            }
            if (object.State != null) {
                if (typeof object.State !== "object")
                    throw TypeError(".jl3cwh.GameInfoData.State: object expected");
                message.State = $root.jl3cwh.StateInfo.fromObject(object.State);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {jl3cwh.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.JPOdds = [];
            }
            if (options.defaults) {
                object.ShowExtra = false;
                object.State = null;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = message.JPOdds[j];
            }
            if (message.State != null && message.hasOwnProperty("State"))
                object.State = $root.jl3cwh.StateInfo.toObject(message.State, options);
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jl3cwh.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jl3cwh.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.GameInfoData";
        };

        return GameInfoData;
    })();

    jl3cwh.WildRecordSet = (function() {

        /**
         * Properties of a WildRecordSet.
         * @memberof jl3cwh
         * @interface IWildRecordSet
         * @property {number|null} [Bet] WildRecordSet Bet
         * @property {Array.<number>|null} [Pos] WildRecordSet Pos
         */

        /**
         * Constructs a new WildRecordSet.
         * @memberof jl3cwh
         * @classdesc Represents a WildRecordSet.
         * @implements IWildRecordSet
         * @constructor
         * @param {jl3cwh.IWildRecordSet=} [properties] Properties to set
         */
        function WildRecordSet(properties) {
            this.Pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WildRecordSet Bet.
         * @member {number} Bet
         * @memberof jl3cwh.WildRecordSet
         * @instance
         */
        WildRecordSet.prototype.Bet = 0;

        /**
         * WildRecordSet Pos.
         * @member {Array.<number>} Pos
         * @memberof jl3cwh.WildRecordSet
         * @instance
         */
        WildRecordSet.prototype.Pos = $util.emptyArray;

        /**
         * Creates a new WildRecordSet instance using the specified properties.
         * @function create
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {jl3cwh.IWildRecordSet=} [properties] Properties to set
         * @returns {jl3cwh.WildRecordSet} WildRecordSet instance
         */
        WildRecordSet.create = function create(properties) {
            return new WildRecordSet(properties);
        };

        /**
         * Encodes the specified WildRecordSet message. Does not implicitly {@link jl3cwh.WildRecordSet.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {jl3cwh.IWildRecordSet} message WildRecordSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WildRecordSet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Bet);
            if (message.Pos != null && message.Pos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Pos.length; ++i)
                    writer.int32(message.Pos[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified WildRecordSet message, length delimited. Does not implicitly {@link jl3cwh.WildRecordSet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {jl3cwh.IWildRecordSet} message WildRecordSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WildRecordSet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WildRecordSet message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.WildRecordSet} WildRecordSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WildRecordSet.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.WildRecordSet();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Bet = reader.double();
                        break;
                    }
                case 2: {
                        if (!(message.Pos && message.Pos.length))
                            message.Pos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pos.push(reader.int32());
                        } else
                            message.Pos.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WildRecordSet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.WildRecordSet} WildRecordSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WildRecordSet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WildRecordSet message.
         * @function verify
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WildRecordSet.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Pos != null && message.hasOwnProperty("Pos")) {
                if (!Array.isArray(message.Pos))
                    return "Pos: array expected";
                for (let i = 0; i < message.Pos.length; ++i)
                    if (!$util.isInteger(message.Pos[i]))
                        return "Pos: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a WildRecordSet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.WildRecordSet} WildRecordSet
         */
        WildRecordSet.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.WildRecordSet)
                return object;
            let message = new $root.jl3cwh.WildRecordSet();
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Pos) {
                if (!Array.isArray(object.Pos))
                    throw TypeError(".jl3cwh.WildRecordSet.Pos: array expected");
                message.Pos = [];
                for (let i = 0; i < object.Pos.length; ++i)
                    message.Pos[i] = object.Pos[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a WildRecordSet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {jl3cwh.WildRecordSet} message WildRecordSet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WildRecordSet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Pos = [];
            if (options.defaults)
                object.Bet = 0;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Pos && message.Pos.length) {
                object.Pos = [];
                for (let j = 0; j < message.Pos.length; ++j)
                    object.Pos[j] = message.Pos[j];
            }
            return object;
        };

        /**
         * Converts this WildRecordSet to JSON.
         * @function toJSON
         * @memberof jl3cwh.WildRecordSet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WildRecordSet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WildRecordSet
         * @function getTypeUrl
         * @memberof jl3cwh.WildRecordSet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WildRecordSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.WildRecordSet";
        };

        return WildRecordSet;
    })();

    jl3cwh.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jl3cwh
         * @interface IStateInfo
         * @property {Array.<number>|null} [CrackLevels] StateInfo CrackLevels
         * @property {Array.<jl3cwh.IWildRecordSet>|null} [WildRecordVec] StateInfo WildRecordVec
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jl3cwh
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jl3cwh.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.CrackLevels = [];
            this.WildRecordVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3cwh.StateInfo
         * @instance
         */
        StateInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * StateInfo WildRecordVec.
         * @member {Array.<jl3cwh.IWildRecordSet>} WildRecordVec
         * @memberof jl3cwh.StateInfo
         * @instance
         */
        StateInfo.prototype.WildRecordVec = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {jl3cwh.IStateInfo=} [properties] Properties to set
         * @returns {jl3cwh.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jl3cwh.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {jl3cwh.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            if (message.WildRecordVec != null && message.WildRecordVec.length)
                for (let i = 0; i < message.WildRecordVec.length; ++i)
                    $root.jl3cwh.WildRecordSet.encode(message.WildRecordVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jl3cwh.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {jl3cwh.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.WildRecordVec && message.WildRecordVec.length))
                            message.WildRecordVec = [];
                        message.WildRecordVec.push($root.jl3cwh.WildRecordSet.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            if (message.WildRecordVec != null && message.hasOwnProperty("WildRecordVec")) {
                if (!Array.isArray(message.WildRecordVec))
                    return "WildRecordVec: array expected";
                for (let i = 0; i < message.WildRecordVec.length; ++i) {
                    let error = $root.jl3cwh.WildRecordSet.verify(message.WildRecordVec[i]);
                    if (error)
                        return "WildRecordVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.StateInfo)
                return object;
            let message = new $root.jl3cwh.StateInfo();
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3cwh.StateInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            if (object.WildRecordVec) {
                if (!Array.isArray(object.WildRecordVec))
                    throw TypeError(".jl3cwh.StateInfo.WildRecordVec: array expected");
                message.WildRecordVec = [];
                for (let i = 0; i < object.WildRecordVec.length; ++i) {
                    if (typeof object.WildRecordVec[i] !== "object")
                        throw TypeError(".jl3cwh.StateInfo.WildRecordVec: object expected");
                    message.WildRecordVec[i] = $root.jl3cwh.WildRecordSet.fromObject(object.WildRecordVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {jl3cwh.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.CrackLevels = [];
                object.WildRecordVec = [];
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            if (message.WildRecordVec && message.WildRecordVec.length) {
                object.WildRecordVec = [];
                for (let j = 0; j < message.WildRecordVec.length; ++j)
                    object.WildRecordVec[j] = $root.jl3cwh.WildRecordSet.toObject(message.WildRecordVec[j], options);
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jl3cwh.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jl3cwh.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.StateInfo";
        };

        return StateInfo;
    })();

    jl3cwh.JPInfo = (function() {

        /**
         * Properties of a JPInfo.
         * @memberof jl3cwh
         * @interface IJPInfo
         * @property {number|null} [Front] JPInfo Front
         */

        /**
         * Constructs a new JPInfo.
         * @memberof jl3cwh
         * @classdesc Represents a JPInfo.
         * @implements IJPInfo
         * @constructor
         * @param {jl3cwh.IJPInfo=} [properties] Properties to set
         */
        function JPInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPInfo Front.
         * @member {number} Front
         * @memberof jl3cwh.JPInfo
         * @instance
         */
        JPInfo.prototype.Front = 0;

        /**
         * Creates a new JPInfo instance using the specified properties.
         * @function create
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {jl3cwh.IJPInfo=} [properties] Properties to set
         * @returns {jl3cwh.JPInfo} JPInfo instance
         */
        JPInfo.create = function create(properties) {
            return new JPInfo(properties);
        };

        /**
         * Encodes the specified JPInfo message. Does not implicitly {@link jl3cwh.JPInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {jl3cwh.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Front != null && Object.hasOwnProperty.call(message, "Front"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Front);
            return writer;
        };

        /**
         * Encodes the specified JPInfo message, length delimited. Does not implicitly {@link jl3cwh.JPInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {jl3cwh.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.JPInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Front = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPInfo message.
         * @function verify
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Front != null && message.hasOwnProperty("Front"))
                if (typeof message.Front !== "number")
                    return "Front: number expected";
            return null;
        };

        /**
         * Creates a JPInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.JPInfo} JPInfo
         */
        JPInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.JPInfo)
                return object;
            let message = new $root.jl3cwh.JPInfo();
            if (object.Front != null)
                message.Front = Number(object.Front);
            return message;
        };

        /**
         * Creates a plain object from a JPInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {jl3cwh.JPInfo} message JPInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.Front = 0;
            if (message.Front != null && message.hasOwnProperty("Front"))
                object.Front = options.json && !isFinite(message.Front) ? String(message.Front) : message.Front;
            return object;
        };

        /**
         * Converts this JPInfo to JSON.
         * @function toJSON
         * @memberof jl3cwh.JPInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPInfo
         * @function getTypeUrl
         * @memberof jl3cwh.JPInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.JPInfo";
        };

        return JPInfo;
    })();

    jl3cwh.JPNotice = (function() {

        /**
         * Properties of a JPNotice.
         * @memberof jl3cwh
         * @interface IJPNotice
         * @property {number|Long|null} [AccountID] JPNotice AccountID
         * @property {number|null} [ApiID] JPNotice ApiID
         * @property {string|null} [NickName] JPNotice NickName
         * @property {number|null} [RatedJPWin] JPNotice RatedJPWin
         * @property {number|Long|null} [TimeStamp] JPNotice TimeStamp
         */

        /**
         * Constructs a new JPNotice.
         * @memberof jl3cwh
         * @classdesc Represents a JPNotice.
         * @implements IJPNotice
         * @constructor
         * @param {jl3cwh.IJPNotice=} [properties] Properties to set
         */
        function JPNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPNotice AccountID.
         * @member {number|Long} AccountID
         * @memberof jl3cwh.JPNotice
         * @instance
         */
        JPNotice.prototype.AccountID = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * JPNotice ApiID.
         * @member {number} ApiID
         * @memberof jl3cwh.JPNotice
         * @instance
         */
        JPNotice.prototype.ApiID = 0;

        /**
         * JPNotice NickName.
         * @member {string} NickName
         * @memberof jl3cwh.JPNotice
         * @instance
         */
        JPNotice.prototype.NickName = "";

        /**
         * JPNotice RatedJPWin.
         * @member {number} RatedJPWin
         * @memberof jl3cwh.JPNotice
         * @instance
         */
        JPNotice.prototype.RatedJPWin = 0;

        /**
         * JPNotice TimeStamp.
         * @member {number|Long} TimeStamp
         * @memberof jl3cwh.JPNotice
         * @instance
         */
        JPNotice.prototype.TimeStamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new JPNotice instance using the specified properties.
         * @function create
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {jl3cwh.IJPNotice=} [properties] Properties to set
         * @returns {jl3cwh.JPNotice} JPNotice instance
         */
        JPNotice.create = function create(properties) {
            return new JPNotice(properties);
        };

        /**
         * Encodes the specified JPNotice message. Does not implicitly {@link jl3cwh.JPNotice.verify|verify} messages.
         * @function encode
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {jl3cwh.IJPNotice} message JPNotice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AccountID != null && Object.hasOwnProperty.call(message, "AccountID"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.AccountID);
            if (message.ApiID != null && Object.hasOwnProperty.call(message, "ApiID"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ApiID);
            if (message.NickName != null && Object.hasOwnProperty.call(message, "NickName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.NickName);
            if (message.RatedJPWin != null && Object.hasOwnProperty.call(message, "RatedJPWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RatedJPWin);
            if (message.TimeStamp != null && Object.hasOwnProperty.call(message, "TimeStamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.TimeStamp);
            return writer;
        };

        /**
         * Encodes the specified JPNotice message, length delimited. Does not implicitly {@link jl3cwh.JPNotice.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {jl3cwh.IJPNotice} message JPNotice message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPNotice message from the specified reader or buffer.
         * @function decode
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3cwh.JPNotice} JPNotice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPNotice.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3cwh.JPNotice();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AccountID = reader.int64();
                        break;
                    }
                case 2: {
                        message.ApiID = reader.int32();
                        break;
                    }
                case 3: {
                        message.NickName = reader.string();
                        break;
                    }
                case 4: {
                        message.RatedJPWin = reader.double();
                        break;
                    }
                case 5: {
                        message.TimeStamp = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPNotice message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3cwh.JPNotice} JPNotice
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPNotice message.
         * @function verify
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPNotice.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AccountID != null && message.hasOwnProperty("AccountID"))
                if (!$util.isInteger(message.AccountID) && !(message.AccountID && $util.isInteger(message.AccountID.low) && $util.isInteger(message.AccountID.high)))
                    return "AccountID: integer|Long expected";
            if (message.ApiID != null && message.hasOwnProperty("ApiID"))
                if (!$util.isInteger(message.ApiID))
                    return "ApiID: integer expected";
            if (message.NickName != null && message.hasOwnProperty("NickName"))
                if (!$util.isString(message.NickName))
                    return "NickName: string expected";
            if (message.RatedJPWin != null && message.hasOwnProperty("RatedJPWin"))
                if (typeof message.RatedJPWin !== "number")
                    return "RatedJPWin: number expected";
            if (message.TimeStamp != null && message.hasOwnProperty("TimeStamp"))
                if (!$util.isInteger(message.TimeStamp) && !(message.TimeStamp && $util.isInteger(message.TimeStamp.low) && $util.isInteger(message.TimeStamp.high)))
                    return "TimeStamp: integer|Long expected";
            return null;
        };

        /**
         * Creates a JPNotice message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3cwh.JPNotice} JPNotice
         */
        JPNotice.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3cwh.JPNotice)
                return object;
            let message = new $root.jl3cwh.JPNotice();
            if (object.AccountID != null)
                if ($util.Long)
                    (message.AccountID = $util.Long.fromValue(object.AccountID)).unsigned = false;
                else if (typeof object.AccountID === "string")
                    message.AccountID = parseInt(object.AccountID, 10);
                else if (typeof object.AccountID === "number")
                    message.AccountID = object.AccountID;
                else if (typeof object.AccountID === "object")
                    message.AccountID = new $util.LongBits(object.AccountID.low >>> 0, object.AccountID.high >>> 0).toNumber();
            if (object.ApiID != null)
                message.ApiID = object.ApiID | 0;
            if (object.NickName != null)
                message.NickName = String(object.NickName);
            if (object.RatedJPWin != null)
                message.RatedJPWin = Number(object.RatedJPWin);
            if (object.TimeStamp != null)
                if ($util.Long)
                    (message.TimeStamp = $util.Long.fromValue(object.TimeStamp)).unsigned = false;
                else if (typeof object.TimeStamp === "string")
                    message.TimeStamp = parseInt(object.TimeStamp, 10);
                else if (typeof object.TimeStamp === "number")
                    message.TimeStamp = object.TimeStamp;
                else if (typeof object.TimeStamp === "object")
                    message.TimeStamp = new $util.LongBits(object.TimeStamp.low >>> 0, object.TimeStamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a JPNotice message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {jl3cwh.JPNotice} message JPNotice
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.AccountID = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.AccountID = options.longs === String ? "0" : 0;
                object.ApiID = 0;
                object.NickName = "";
                object.RatedJPWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.TimeStamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.TimeStamp = options.longs === String ? "0" : 0;
            }
            if (message.AccountID != null && message.hasOwnProperty("AccountID"))
                if (typeof message.AccountID === "number")
                    object.AccountID = options.longs === String ? String(message.AccountID) : message.AccountID;
                else
                    object.AccountID = options.longs === String ? $util.Long.prototype.toString.call(message.AccountID) : options.longs === Number ? new $util.LongBits(message.AccountID.low >>> 0, message.AccountID.high >>> 0).toNumber() : message.AccountID;
            if (message.ApiID != null && message.hasOwnProperty("ApiID"))
                object.ApiID = message.ApiID;
            if (message.NickName != null && message.hasOwnProperty("NickName"))
                object.NickName = message.NickName;
            if (message.RatedJPWin != null && message.hasOwnProperty("RatedJPWin"))
                object.RatedJPWin = options.json && !isFinite(message.RatedJPWin) ? String(message.RatedJPWin) : message.RatedJPWin;
            if (message.TimeStamp != null && message.hasOwnProperty("TimeStamp"))
                if (typeof message.TimeStamp === "number")
                    object.TimeStamp = options.longs === String ? String(message.TimeStamp) : message.TimeStamp;
                else
                    object.TimeStamp = options.longs === String ? $util.Long.prototype.toString.call(message.TimeStamp) : options.longs === Number ? new $util.LongBits(message.TimeStamp.low >>> 0, message.TimeStamp.high >>> 0).toNumber() : message.TimeStamp;
            return object;
        };

        /**
         * Converts this JPNotice to JSON.
         * @function toJSON
         * @memberof jl3cwh.JPNotice
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPNotice
         * @function getTypeUrl
         * @memberof jl3cwh.JPNotice
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPNotice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3cwh.JPNotice";
        };

        return JPNotice;
    })();

    return jl3cwh;
})();

export const jl3dp = $root.jl3dp = (() => {

    /**
     * Namespace jl3dp.
     * @exports jl3dp
     * @namespace
     */
    const jl3dp = {};

    jl3dp.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jl3dp
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jl3dp
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jl3dp.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jl3dp.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jl3dp.Column
         * @static
         * @param {jl3dp.IColumn=} [properties] Properties to set
         * @returns {jl3dp.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jl3dp.Column.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.Column
         * @static
         * @param {jl3dp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jl3dp.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.Column
         * @static
         * @param {jl3dp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jl3dp.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.Column)
                return object;
            let message = new $root.jl3dp.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3dp.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.Column
         * @static
         * @param {jl3dp.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jl3dp.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jl3dp.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.Column";
        };

        return Column;
    })();

    jl3dp.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jl3dp
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jl3dp
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jl3dp.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jl3dp.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jl3dp.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jl3dp.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jl3dp.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jl3dp.AwardData
         * @static
         * @param {jl3dp.IAwardData=} [properties] Properties to set
         * @returns {jl3dp.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jl3dp.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.AwardData
         * @static
         * @param {jl3dp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jl3dp.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.AwardData
         * @static
         * @param {jl3dp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jl3dp.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.AwardData)
                return object;
            let message = new $root.jl3dp.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.AwardData
         * @static
         * @param {jl3dp.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jl3dp.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jl3dp.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.AwardData";
        };

        return AwardData;
    })();

    jl3dp.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jl3dp
         * @interface IRoundInfo
         * @property {Array.<jl3dp.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jl3dp.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FireMultiple] RoundInfo FireMultiple
         * @property {number|null} [ThunderOdd] RoundInfo ThunderOdd
         * @property {boolean|null} [ReachMaxOdd] RoundInfo ReachMaxOdd
         * @property {Array.<boolean>|null} [AddCrack] RoundInfo AddCrack
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<number>|null} [AddCrackLv] RoundInfo AddCrackLv
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jl3dp
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jl3dp.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.AddCrack = [];
            this.AddCrackLv = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jl3dp.IColumn>} PlateSymbol
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jl3dp.IAwardData>} AwardDataVec
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FireMultiple.
         * @member {number} FireMultiple
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FireMultiple = 0;

        /**
         * RoundInfo ThunderOdd.
         * @member {number} ThunderOdd
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ThunderOdd = 0;

        /**
         * RoundInfo ReachMaxOdd.
         * @member {boolean} ReachMaxOdd
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ReachMaxOdd = false;

        /**
         * RoundInfo AddCrack.
         * @member {Array.<boolean>} AddCrack
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrack = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo AddCrackLv.
         * @member {Array.<number>} AddCrackLv
         * @memberof jl3dp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrackLv = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {jl3dp.IRoundInfo=} [properties] Properties to set
         * @returns {jl3dp.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jl3dp.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {jl3dp.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jl3dp.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jl3dp.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RoundWin);
            if (message.FireMultiple != null && Object.hasOwnProperty.call(message, "FireMultiple"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.FireMultiple);
            if (message.ThunderOdd != null && Object.hasOwnProperty.call(message, "ThunderOdd"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.ThunderOdd);
            if (message.ReachMaxOdd != null && Object.hasOwnProperty.call(message, "ReachMaxOdd"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.ReachMaxOdd);
            if (message.AddCrack != null && message.AddCrack.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.AddCrack.length; ++i)
                    writer.bool(message.AddCrack[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            if (message.AddCrackLv != null && message.AddCrackLv.length) {
                writer.uint32(/* id 13, wireType 2 =*/106).fork();
                for (let i = 0; i < message.AddCrackLv.length; ++i)
                    writer.int32(message.AddCrackLv[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jl3dp.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {jl3dp.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jl3dp.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jl3dp.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 8: {
                        message.FireMultiple = reader.double();
                        break;
                    }
                case 9: {
                        message.ThunderOdd = reader.double();
                        break;
                    }
                case 10: {
                        message.ReachMaxOdd = reader.bool();
                        break;
                    }
                case 11: {
                        if (!(message.AddCrack && message.AddCrack.length))
                            message.AddCrack = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrack.push(reader.bool());
                        } else
                            message.AddCrack.push(reader.bool());
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 13: {
                        if (!(message.AddCrackLv && message.AddCrackLv.length))
                            message.AddCrackLv = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrackLv.push(reader.int32());
                        } else
                            message.AddCrackLv.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jl3dp.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jl3dp.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FireMultiple != null && message.hasOwnProperty("FireMultiple"))
                if (typeof message.FireMultiple !== "number")
                    return "FireMultiple: number expected";
            if (message.ThunderOdd != null && message.hasOwnProperty("ThunderOdd"))
                if (typeof message.ThunderOdd !== "number")
                    return "ThunderOdd: number expected";
            if (message.ReachMaxOdd != null && message.hasOwnProperty("ReachMaxOdd"))
                if (typeof message.ReachMaxOdd !== "boolean")
                    return "ReachMaxOdd: boolean expected";
            if (message.AddCrack != null && message.hasOwnProperty("AddCrack")) {
                if (!Array.isArray(message.AddCrack))
                    return "AddCrack: array expected";
                for (let i = 0; i < message.AddCrack.length; ++i)
                    if (typeof message.AddCrack[i] !== "boolean")
                        return "AddCrack: boolean[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.AddCrackLv != null && message.hasOwnProperty("AddCrackLv")) {
                if (!Array.isArray(message.AddCrackLv))
                    return "AddCrackLv: array expected";
                for (let i = 0; i < message.AddCrackLv.length; ++i)
                    if (!$util.isInteger(message.AddCrackLv[i]))
                        return "AddCrackLv: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.RoundInfo)
                return object;
            let message = new $root.jl3dp.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jl3dp.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jl3dp.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jl3dp.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jl3dp.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jl3dp.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jl3dp.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FireMultiple != null)
                message.FireMultiple = Number(object.FireMultiple);
            if (object.ThunderOdd != null)
                message.ThunderOdd = Number(object.ThunderOdd);
            if (object.ReachMaxOdd != null)
                message.ReachMaxOdd = Boolean(object.ReachMaxOdd);
            if (object.AddCrack) {
                if (!Array.isArray(object.AddCrack))
                    throw TypeError(".jl3dp.RoundInfo.AddCrack: array expected");
                message.AddCrack = [];
                for (let i = 0; i < object.AddCrack.length; ++i)
                    message.AddCrack[i] = Boolean(object.AddCrack[i]);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.AddCrackLv) {
                if (!Array.isArray(object.AddCrackLv))
                    throw TypeError(".jl3dp.RoundInfo.AddCrackLv: array expected");
                message.AddCrackLv = [];
                for (let i = 0; i < object.AddCrackLv.length; ++i)
                    message.AddCrackLv[i] = object.AddCrackLv[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {jl3dp.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.AddCrack = [];
                object.AddCrackLv = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.RoundWin = 0;
                object.FireMultiple = 0;
                object.ThunderOdd = 0;
                object.ReachMaxOdd = false;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jl3dp.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jl3dp.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FireMultiple != null && message.hasOwnProperty("FireMultiple"))
                object.FireMultiple = options.json && !isFinite(message.FireMultiple) ? String(message.FireMultiple) : message.FireMultiple;
            if (message.ThunderOdd != null && message.hasOwnProperty("ThunderOdd"))
                object.ThunderOdd = options.json && !isFinite(message.ThunderOdd) ? String(message.ThunderOdd) : message.ThunderOdd;
            if (message.ReachMaxOdd != null && message.hasOwnProperty("ReachMaxOdd"))
                object.ReachMaxOdd = message.ReachMaxOdd;
            if (message.AddCrack && message.AddCrack.length) {
                object.AddCrack = [];
                for (let j = 0; j < message.AddCrack.length; ++j)
                    object.AddCrack[j] = message.AddCrack[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.AddCrackLv && message.AddCrackLv.length) {
                object.AddCrackLv = [];
                for (let j = 0; j < message.AddCrackLv.length; ++j)
                    object.AddCrackLv[j] = message.AddCrackLv[j];
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jl3dp.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jl3dp.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.RoundInfo";
        };

        return RoundInfo;
    })();

    jl3dp.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jl3dp
         * @interface ISpinAck
         * @property {Array.<jl3dp.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [FGReel] SpinAck FGReel
         * @property {number|null} [CrackLevels] SpinAck CrackLevels
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jl3dp
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jl3dp.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jl3dp.IRoundInfo>} RoundQueue
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck FGReel.
         * @member {number} FGReel
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.FGReel = 0;

        /**
         * SpinAck CrackLevels.
         * @member {number} CrackLevels
         * @memberof jl3dp.SpinAck
         * @instance
         */
        SpinAck.prototype.CrackLevels = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jl3dp.SpinAck
         * @static
         * @param {jl3dp.ISpinAck=} [properties] Properties to set
         * @returns {jl3dp.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jl3dp.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.SpinAck
         * @static
         * @param {jl3dp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jl3dp.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Bet);
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.FGReel != null && Object.hasOwnProperty.call(message, "FGReel"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FGReel);
            if (message.CrackLevels != null && Object.hasOwnProperty.call(message, "CrackLevels"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.CrackLevels);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jl3dp.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.SpinAck
         * @static
         * @param {jl3dp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jl3dp.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Bet = reader.double();
                        break;
                    }
                case 3: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.FGReel = reader.int32();
                        break;
                    }
                case 10: {
                        message.CrackLevels = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jl3dp.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jl3dp.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.FGReel != null && message.hasOwnProperty("FGReel"))
                if (!$util.isInteger(message.FGReel))
                    return "FGReel: integer expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels"))
                if (!$util.isInteger(message.CrackLevels))
                    return "CrackLevels: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.SpinAck)
                return object;
            let message = new $root.jl3dp.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jl3dp.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jl3dp.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jl3dp.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.FGReel != null)
                message.FGReel = object.FGReel | 0;
            if (object.CrackLevels != null)
                message.CrackLevels = object.CrackLevels | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.SpinAck
         * @static
         * @param {jl3dp.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.Bet = 0;
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.FGReel = 0;
                object.CrackLevels = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jl3dp.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.FGReel != null && message.hasOwnProperty("FGReel"))
                object.FGReel = message.FGReel;
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels"))
                object.CrackLevels = message.CrackLevels;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jl3dp.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jl3dp.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.SpinAck";
        };

        return SpinAck;
    })();

    jl3dp.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jl3dp
         * @interface IStateInfo
         * @property {Array.<number>|null} [CrackLevels] StateInfo CrackLevels
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jl3dp
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jl3dp.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3dp.StateInfo
         * @instance
         */
        StateInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jl3dp.StateInfo
         * @static
         * @param {jl3dp.IStateInfo=} [properties] Properties to set
         * @returns {jl3dp.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jl3dp.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.StateInfo
         * @static
         * @param {jl3dp.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jl3dp.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.StateInfo
         * @static
         * @param {jl3dp.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jl3dp.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.StateInfo)
                return object;
            let message = new $root.jl3dp.StateInfo();
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3dp.StateInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.StateInfo
         * @static
         * @param {jl3dp.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CrackLevels = [];
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jl3dp.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jl3dp.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.StateInfo";
        };

        return StateInfo;
    })();

    jl3dp.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jl3dp
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [CrackLevels] GameInfoData CrackLevels
         * @property {number|null} [MaxBet] GameInfoData MaxBet
         * @property {Array.<number>|null} [BuyMul] GameInfoData BuyMul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jl3dp
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jl3dp.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.CrackLevels = [];
            this.BuyMul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jl3dp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jl3dp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3dp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CrackLevels = $util.emptyArray;

        /**
         * GameInfoData MaxBet.
         * @member {number} MaxBet
         * @memberof jl3dp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.MaxBet = 0;

        /**
         * GameInfoData BuyMul.
         * @member {Array.<number>} BuyMul
         * @memberof jl3dp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BuyMul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {jl3dp.IGameInfoData=} [properties] Properties to set
         * @returns {jl3dp.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jl3dp.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {jl3dp.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            if (message.MaxBet != null && Object.hasOwnProperty.call(message, "MaxBet"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.MaxBet);
            if (message.BuyMul != null && message.BuyMul.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.BuyMul.length; ++i)
                    writer.double(message.BuyMul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jl3dp.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {jl3dp.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                case 4: {
                        message.MaxBet = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.BuyMul && message.BuyMul.length))
                            message.BuyMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BuyMul.push(reader.double());
                        } else
                            message.BuyMul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                if (typeof message.MaxBet !== "number")
                    return "MaxBet: number expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul")) {
                if (!Array.isArray(message.BuyMul))
                    return "BuyMul: array expected";
                for (let i = 0; i < message.BuyMul.length; ++i)
                    if (typeof message.BuyMul[i] !== "number")
                        return "BuyMul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.GameInfoData)
                return object;
            let message = new $root.jl3dp.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jl3dp.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3dp.GameInfoData.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            if (object.MaxBet != null)
                message.MaxBet = Number(object.MaxBet);
            if (object.BuyMul) {
                if (!Array.isArray(object.BuyMul))
                    throw TypeError(".jl3dp.GameInfoData.BuyMul: array expected");
                message.BuyMul = [];
                for (let i = 0; i < object.BuyMul.length; ++i)
                    message.BuyMul[i] = Number(object.BuyMul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {jl3dp.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.CrackLevels = [];
                object.BuyMul = [];
            }
            if (options.defaults) {
                object.ShowExtra = false;
                object.MaxBet = 0;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                object.MaxBet = options.json && !isFinite(message.MaxBet) ? String(message.MaxBet) : message.MaxBet;
            if (message.BuyMul && message.BuyMul.length) {
                object.BuyMul = [];
                for (let j = 0; j < message.BuyMul.length; ++j)
                    object.BuyMul[j] = options.json && !isFinite(message.BuyMul[j]) ? String(message.BuyMul[j]) : message.BuyMul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jl3dp.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jl3dp.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.GameInfoData";
        };

        return GameInfoData;
    })();

    jl3dp.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jl3dp
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         * @property {number|null} [BuyType] BuyReq BuyType
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jl3dp
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jl3dp.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jl3dp.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * BuyReq BuyType.
         * @member {number} BuyType
         * @memberof jl3dp.BuyReq
         * @instance
         */
        BuyReq.prototype.BuyType = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jl3dp.BuyReq
         * @static
         * @param {jl3dp.IBuyReq=} [properties] Properties to set
         * @returns {jl3dp.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jl3dp.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jl3dp.BuyReq
         * @static
         * @param {jl3dp.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            if (message.BuyType != null && Object.hasOwnProperty.call(message, "BuyType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.BuyType);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jl3dp.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3dp.BuyReq
         * @static
         * @param {jl3dp.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jl3dp.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3dp.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3dp.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 2: {
                        message.BuyType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3dp.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3dp.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jl3dp.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                if (!$util.isInteger(message.BuyType))
                    return "BuyType: integer expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3dp.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3dp.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3dp.BuyReq)
                return object;
            let message = new $root.jl3dp.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.BuyType != null)
                message.BuyType = object.BuyType | 0;
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3dp.BuyReq
         * @static
         * @param {jl3dp.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseBet = 0;
                object.BuyType = 0;
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                object.BuyType = message.BuyType;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jl3dp.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jl3dp.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3dp.BuyReq";
        };

        return BuyReq;
    })();

    return jl3dp;
})();

export const jl3lp = $root.jl3lp = (() => {

    /**
     * Namespace jl3lp.
     * @exports jl3lp
     * @namespace
     */
    const jl3lp = {};

    jl3lp.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jl3lp
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jl3lp
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jl3lp.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jl3lp.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jl3lp.Column
         * @static
         * @param {jl3lp.IColumn=} [properties] Properties to set
         * @returns {jl3lp.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jl3lp.Column.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.Column
         * @static
         * @param {jl3lp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jl3lp.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.Column
         * @static
         * @param {jl3lp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jl3lp.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.Column)
                return object;
            let message = new $root.jl3lp.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3lp.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.Column
         * @static
         * @param {jl3lp.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jl3lp.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jl3lp.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.Column";
        };

        return Column;
    })();

    jl3lp.CColumn = (function() {

        /**
         * Properties of a CColumn.
         * @memberof jl3lp
         * @interface ICColumn
         * @property {Array.<jl3lp.ICSymbol>|null} [Col] CColumn Col
         */

        /**
         * Constructs a new CColumn.
         * @memberof jl3lp
         * @classdesc Represents a CColumn.
         * @implements ICColumn
         * @constructor
         * @param {jl3lp.ICColumn=} [properties] Properties to set
         */
        function CColumn(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CColumn Col.
         * @member {Array.<jl3lp.ICSymbol>} Col
         * @memberof jl3lp.CColumn
         * @instance
         */
        CColumn.prototype.Col = $util.emptyArray;

        /**
         * Creates a new CColumn instance using the specified properties.
         * @function create
         * @memberof jl3lp.CColumn
         * @static
         * @param {jl3lp.ICColumn=} [properties] Properties to set
         * @returns {jl3lp.CColumn} CColumn instance
         */
        CColumn.create = function create(properties) {
            return new CColumn(properties);
        };

        /**
         * Encodes the specified CColumn message. Does not implicitly {@link jl3lp.CColumn.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.CColumn
         * @static
         * @param {jl3lp.ICColumn} message CColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jl3lp.CSymbol.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CColumn message, length delimited. Does not implicitly {@link jl3lp.CColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.CColumn
         * @static
         * @param {jl3lp.ICColumn} message CColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.CColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.CColumn} CColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.CColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jl3lp.CSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.CColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.CColumn} CColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CColumn message.
         * @function verify
         * @memberof jl3lp.CColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jl3lp.CSymbol.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.CColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.CColumn} CColumn
         */
        CColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.CColumn)
                return object;
            let message = new $root.jl3lp.CColumn();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jl3lp.CColumn.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jl3lp.CColumn.Col: object expected");
                    message.Col[i] = $root.jl3lp.CSymbol.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a CColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.CColumn
         * @static
         * @param {jl3lp.CColumn} message CColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jl3lp.CSymbol.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this CColumn to JSON.
         * @function toJSON
         * @memberof jl3lp.CColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CColumn
         * @function getTypeUrl
         * @memberof jl3lp.CColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.CColumn";
        };

        return CColumn;
    })();

    jl3lp.CSymbol = (function() {

        /**
         * Properties of a CSymbol.
         * @memberof jl3lp
         * @interface ICSymbol
         * @property {number|null} [Symbol] CSymbol Symbol
         * @property {number|null} [JPState] CSymbol JPState
         * @property {number|null} [JPMult] CSymbol JPMult
         * @property {number|null} [Number] CSymbol Number
         */

        /**
         * Constructs a new CSymbol.
         * @memberof jl3lp
         * @classdesc Represents a CSymbol.
         * @implements ICSymbol
         * @constructor
         * @param {jl3lp.ICSymbol=} [properties] Properties to set
         */
        function CSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSymbol Symbol.
         * @member {number} Symbol
         * @memberof jl3lp.CSymbol
         * @instance
         */
        CSymbol.prototype.Symbol = 0;

        /**
         * CSymbol JPState.
         * @member {number} JPState
         * @memberof jl3lp.CSymbol
         * @instance
         */
        CSymbol.prototype.JPState = 0;

        /**
         * CSymbol JPMult.
         * @member {number} JPMult
         * @memberof jl3lp.CSymbol
         * @instance
         */
        CSymbol.prototype.JPMult = 0;

        /**
         * CSymbol Number.
         * @member {number} Number
         * @memberof jl3lp.CSymbol
         * @instance
         */
        CSymbol.prototype.Number = 0;

        /**
         * Creates a new CSymbol instance using the specified properties.
         * @function create
         * @memberof jl3lp.CSymbol
         * @static
         * @param {jl3lp.ICSymbol=} [properties] Properties to set
         * @returns {jl3lp.CSymbol} CSymbol instance
         */
        CSymbol.create = function create(properties) {
            return new CSymbol(properties);
        };

        /**
         * Encodes the specified CSymbol message. Does not implicitly {@link jl3lp.CSymbol.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.CSymbol
         * @static
         * @param {jl3lp.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.JPState != null && Object.hasOwnProperty.call(message, "JPState"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.JPState);
            if (message.JPMult != null && Object.hasOwnProperty.call(message, "JPMult"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.JPMult);
            if (message.Number != null && Object.hasOwnProperty.call(message, "Number"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Number);
            return writer;
        };

        /**
         * Encodes the specified CSymbol message, length delimited. Does not implicitly {@link jl3lp.CSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.CSymbol
         * @static
         * @param {jl3lp.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.CSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.JPState = reader.int32();
                        break;
                    }
                case 3: {
                        message.JPMult = reader.int32();
                        break;
                    }
                case 4: {
                        message.Number = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSymbol message.
         * @function verify
         * @memberof jl3lp.CSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                if (!$util.isInteger(message.JPState))
                    return "JPState: integer expected";
            if (message.JPMult != null && message.hasOwnProperty("JPMult"))
                if (!$util.isInteger(message.JPMult))
                    return "JPMult: integer expected";
            if (message.Number != null && message.hasOwnProperty("Number"))
                if (typeof message.Number !== "number")
                    return "Number: number expected";
            return null;
        };

        /**
         * Creates a CSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.CSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.CSymbol} CSymbol
         */
        CSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.CSymbol)
                return object;
            let message = new $root.jl3lp.CSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.JPState != null)
                message.JPState = object.JPState | 0;
            if (object.JPMult != null)
                message.JPMult = object.JPMult | 0;
            if (object.Number != null)
                message.Number = Number(object.Number);
            return message;
        };

        /**
         * Creates a plain object from a CSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.CSymbol
         * @static
         * @param {jl3lp.CSymbol} message CSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.JPState = 0;
                object.JPMult = 0;
                object.Number = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                object.JPState = message.JPState;
            if (message.JPMult != null && message.hasOwnProperty("JPMult"))
                object.JPMult = message.JPMult;
            if (message.Number != null && message.hasOwnProperty("Number"))
                object.Number = options.json && !isFinite(message.Number) ? String(message.Number) : message.Number;
            return object;
        };

        /**
         * Converts this CSymbol to JSON.
         * @function toJSON
         * @memberof jl3lp.CSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSymbol
         * @function getTypeUrl
         * @memberof jl3lp.CSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.CSymbol";
        };

        return CSymbol;
    })();

    jl3lp.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jl3lp
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jl3lp
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jl3lp.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jl3lp.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jl3lp.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jl3lp.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jl3lp.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jl3lp.AwardData
         * @static
         * @param {jl3lp.IAwardData=} [properties] Properties to set
         * @returns {jl3lp.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jl3lp.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.AwardData
         * @static
         * @param {jl3lp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jl3lp.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.AwardData
         * @static
         * @param {jl3lp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jl3lp.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.AwardData)
                return object;
            let message = new $root.jl3lp.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.AwardData
         * @static
         * @param {jl3lp.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jl3lp.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jl3lp.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.AwardData";
        };

        return AwardData;
    })();

    jl3lp.FeatureEffect = (function() {

        /**
         * Properties of a FeatureEffect.
         * @memberof jl3lp
         * @interface IFeatureEffect
         * @property {number|null} [ScatterPos] FeatureEffect ScatterPos
         * @property {Array.<number>|null} [EffectPos] FeatureEffect EffectPos
         * @property {number|null} [JPState] FeatureEffect JPState
         * @property {number|null} [Number] FeatureEffect Number
         */

        /**
         * Constructs a new FeatureEffect.
         * @memberof jl3lp
         * @classdesc Represents a FeatureEffect.
         * @implements IFeatureEffect
         * @constructor
         * @param {jl3lp.IFeatureEffect=} [properties] Properties to set
         */
        function FeatureEffect(properties) {
            this.EffectPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FeatureEffect ScatterPos.
         * @member {number} ScatterPos
         * @memberof jl3lp.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.ScatterPos = 0;

        /**
         * FeatureEffect EffectPos.
         * @member {Array.<number>} EffectPos
         * @memberof jl3lp.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.EffectPos = $util.emptyArray;

        /**
         * FeatureEffect JPState.
         * @member {number} JPState
         * @memberof jl3lp.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.JPState = 0;

        /**
         * FeatureEffect Number.
         * @member {number} Number
         * @memberof jl3lp.FeatureEffect
         * @instance
         */
        FeatureEffect.prototype.Number = 0;

        /**
         * Creates a new FeatureEffect instance using the specified properties.
         * @function create
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {jl3lp.IFeatureEffect=} [properties] Properties to set
         * @returns {jl3lp.FeatureEffect} FeatureEffect instance
         */
        FeatureEffect.create = function create(properties) {
            return new FeatureEffect(properties);
        };

        /**
         * Encodes the specified FeatureEffect message. Does not implicitly {@link jl3lp.FeatureEffect.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {jl3lp.IFeatureEffect} message FeatureEffect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ScatterPos != null && Object.hasOwnProperty.call(message, "ScatterPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ScatterPos);
            if (message.EffectPos != null && message.EffectPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.EffectPos.length; ++i)
                    writer.int32(message.EffectPos[i]);
                writer.ldelim();
            }
            if (message.JPState != null && Object.hasOwnProperty.call(message, "JPState"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.JPState);
            if (message.Number != null && Object.hasOwnProperty.call(message, "Number"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Number);
            return writer;
        };

        /**
         * Encodes the specified FeatureEffect message, length delimited. Does not implicitly {@link jl3lp.FeatureEffect.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {jl3lp.IFeatureEffect} message FeatureEffect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FeatureEffect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FeatureEffect message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.FeatureEffect} FeatureEffect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffect.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.FeatureEffect();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ScatterPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.EffectPos && message.EffectPos.length))
                            message.EffectPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.EffectPos.push(reader.int32());
                        } else
                            message.EffectPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.JPState = reader.int32();
                        break;
                    }
                case 4: {
                        message.Number = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FeatureEffect message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.FeatureEffect} FeatureEffect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FeatureEffect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FeatureEffect message.
         * @function verify
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FeatureEffect.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ScatterPos != null && message.hasOwnProperty("ScatterPos"))
                if (!$util.isInteger(message.ScatterPos))
                    return "ScatterPos: integer expected";
            if (message.EffectPos != null && message.hasOwnProperty("EffectPos")) {
                if (!Array.isArray(message.EffectPos))
                    return "EffectPos: array expected";
                for (let i = 0; i < message.EffectPos.length; ++i)
                    if (!$util.isInteger(message.EffectPos[i]))
                        return "EffectPos: integer[] expected";
            }
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                if (!$util.isInteger(message.JPState))
                    return "JPState: integer expected";
            if (message.Number != null && message.hasOwnProperty("Number"))
                if (typeof message.Number !== "number")
                    return "Number: number expected";
            return null;
        };

        /**
         * Creates a FeatureEffect message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.FeatureEffect} FeatureEffect
         */
        FeatureEffect.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.FeatureEffect)
                return object;
            let message = new $root.jl3lp.FeatureEffect();
            if (object.ScatterPos != null)
                message.ScatterPos = object.ScatterPos | 0;
            if (object.EffectPos) {
                if (!Array.isArray(object.EffectPos))
                    throw TypeError(".jl3lp.FeatureEffect.EffectPos: array expected");
                message.EffectPos = [];
                for (let i = 0; i < object.EffectPos.length; ++i)
                    message.EffectPos[i] = object.EffectPos[i] | 0;
            }
            if (object.JPState != null)
                message.JPState = object.JPState | 0;
            if (object.Number != null)
                message.Number = Number(object.Number);
            return message;
        };

        /**
         * Creates a plain object from a FeatureEffect message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {jl3lp.FeatureEffect} message FeatureEffect
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FeatureEffect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.EffectPos = [];
            if (options.defaults) {
                object.ScatterPos = 0;
                object.JPState = 0;
                object.Number = 0;
            }
            if (message.ScatterPos != null && message.hasOwnProperty("ScatterPos"))
                object.ScatterPos = message.ScatterPos;
            if (message.EffectPos && message.EffectPos.length) {
                object.EffectPos = [];
                for (let j = 0; j < message.EffectPos.length; ++j)
                    object.EffectPos[j] = message.EffectPos[j];
            }
            if (message.JPState != null && message.hasOwnProperty("JPState"))
                object.JPState = message.JPState;
            if (message.Number != null && message.hasOwnProperty("Number"))
                object.Number = options.json && !isFinite(message.Number) ? String(message.Number) : message.Number;
            return object;
        };

        /**
         * Converts this FeatureEffect to JSON.
         * @function toJSON
         * @memberof jl3lp.FeatureEffect
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FeatureEffect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FeatureEffect
         * @function getTypeUrl
         * @memberof jl3lp.FeatureEffect
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FeatureEffect.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.FeatureEffect";
        };

        return FeatureEffect;
    })();

    jl3lp.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jl3lp
         * @interface IRoundInfo
         * @property {Array.<jl3lp.IColumn>|null} [MainPlateSymbol] RoundInfo MainPlateSymbol
         * @property {Array.<jl3lp.ICColumn>|null} [FreePlateSymbol] RoundInfo FreePlateSymbol
         * @property {Array.<jl3lp.ICColumn>|null} [FreePlateSymbolLog] RoundInfo FreePlateSymbolLog
         * @property {Array.<jl3lp.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [MGReelWeightResult] RoundInfo MGReelWeightResult
         * @property {Array.<jl3lp.IFeatureEffect>|null} [BlueData] RoundInfo BlueData
         * @property {Array.<jl3lp.IFeatureEffect>|null} [RedData] RoundInfo RedData
         * @property {Array.<jl3lp.IFeatureEffect>|null} [GreenData] RoundInfo GreenData
         * @property {Array.<boolean>|null} [AddCrack] RoundInfo AddCrack
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jl3lp
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jl3lp.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.MainPlateSymbol = [];
            this.FreePlateSymbol = [];
            this.FreePlateSymbolLog = [];
            this.AwardDataVec = [];
            this.BlueData = [];
            this.RedData = [];
            this.GreenData = [];
            this.AddCrack = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo MainPlateSymbol.
         * @member {Array.<jl3lp.IColumn>} MainPlateSymbol
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MainPlateSymbol = $util.emptyArray;

        /**
         * RoundInfo FreePlateSymbol.
         * @member {Array.<jl3lp.ICColumn>} FreePlateSymbol
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreePlateSymbol = $util.emptyArray;

        /**
         * RoundInfo FreePlateSymbolLog.
         * @member {Array.<jl3lp.ICColumn>} FreePlateSymbolLog
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreePlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jl3lp.IAwardData>} AwardDataVec
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo MGReelWeightResult.
         * @member {number} MGReelWeightResult
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGReelWeightResult = 0;

        /**
         * RoundInfo BlueData.
         * @member {Array.<jl3lp.IFeatureEffect>} BlueData
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BlueData = $util.emptyArray;

        /**
         * RoundInfo RedData.
         * @member {Array.<jl3lp.IFeatureEffect>} RedData
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RedData = $util.emptyArray;

        /**
         * RoundInfo GreenData.
         * @member {Array.<jl3lp.IFeatureEffect>} GreenData
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GreenData = $util.emptyArray;

        /**
         * RoundInfo AddCrack.
         * @member {Array.<boolean>} AddCrack
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrack = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jl3lp.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {jl3lp.IRoundInfo=} [properties] Properties to set
         * @returns {jl3lp.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jl3lp.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {jl3lp.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainPlateSymbol != null && message.MainPlateSymbol.length)
                for (let i = 0; i < message.MainPlateSymbol.length; ++i)
                    $root.jl3lp.Column.encode(message.MainPlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreePlateSymbol != null && message.FreePlateSymbol.length)
                for (let i = 0; i < message.FreePlateSymbol.length; ++i)
                    $root.jl3lp.CColumn.encode(message.FreePlateSymbol[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreePlateSymbolLog != null && message.FreePlateSymbolLog.length)
                for (let i = 0; i < message.FreePlateSymbolLog.length; ++i)
                    $root.jl3lp.CColumn.encode(message.FreePlateSymbolLog[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jl3lp.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeNowRound);
            if (message.MGReelWeightResult != null && Object.hasOwnProperty.call(message, "MGReelWeightResult"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.MGReelWeightResult);
            if (message.BlueData != null && message.BlueData.length)
                for (let i = 0; i < message.BlueData.length; ++i)
                    $root.jl3lp.FeatureEffect.encode(message.BlueData[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.RedData != null && message.RedData.length)
                for (let i = 0; i < message.RedData.length; ++i)
                    $root.jl3lp.FeatureEffect.encode(message.RedData[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.GreenData != null && message.GreenData.length)
                for (let i = 0; i < message.GreenData.length; ++i)
                    $root.jl3lp.FeatureEffect.encode(message.GreenData[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.AddCrack != null && message.AddCrack.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (let i = 0; i < message.AddCrack.length; ++i)
                    writer.bool(message.AddCrack[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jl3lp.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {jl3lp.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MainPlateSymbol && message.MainPlateSymbol.length))
                            message.MainPlateSymbol = [];
                        message.MainPlateSymbol.push($root.jl3lp.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.FreePlateSymbol && message.FreePlateSymbol.length))
                            message.FreePlateSymbol = [];
                        message.FreePlateSymbol.push($root.jl3lp.CColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FreePlateSymbolLog && message.FreePlateSymbolLog.length))
                            message.FreePlateSymbolLog = [];
                        message.FreePlateSymbolLog.push($root.jl3lp.CColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jl3lp.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.MGReelWeightResult = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.BlueData && message.BlueData.length))
                            message.BlueData = [];
                        message.BlueData.push($root.jl3lp.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                case 12: {
                        if (!(message.RedData && message.RedData.length))
                            message.RedData = [];
                        message.RedData.push($root.jl3lp.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                case 13: {
                        if (!(message.GreenData && message.GreenData.length))
                            message.GreenData = [];
                        message.GreenData.push($root.jl3lp.FeatureEffect.decode(reader, reader.uint32()));
                        break;
                    }
                case 14: {
                        if (!(message.AddCrack && message.AddCrack.length))
                            message.AddCrack = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrack.push(reader.bool());
                        } else
                            message.AddCrack.push(reader.bool());
                        break;
                    }
                case 15: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainPlateSymbol != null && message.hasOwnProperty("MainPlateSymbol")) {
                if (!Array.isArray(message.MainPlateSymbol))
                    return "MainPlateSymbol: array expected";
                for (let i = 0; i < message.MainPlateSymbol.length; ++i) {
                    let error = $root.jl3lp.Column.verify(message.MainPlateSymbol[i]);
                    if (error)
                        return "MainPlateSymbol." + error;
                }
            }
            if (message.FreePlateSymbol != null && message.hasOwnProperty("FreePlateSymbol")) {
                if (!Array.isArray(message.FreePlateSymbol))
                    return "FreePlateSymbol: array expected";
                for (let i = 0; i < message.FreePlateSymbol.length; ++i) {
                    let error = $root.jl3lp.CColumn.verify(message.FreePlateSymbol[i]);
                    if (error)
                        return "FreePlateSymbol." + error;
                }
            }
            if (message.FreePlateSymbolLog != null && message.hasOwnProperty("FreePlateSymbolLog")) {
                if (!Array.isArray(message.FreePlateSymbolLog))
                    return "FreePlateSymbolLog: array expected";
                for (let i = 0; i < message.FreePlateSymbolLog.length; ++i) {
                    let error = $root.jl3lp.CColumn.verify(message.FreePlateSymbolLog[i]);
                    if (error)
                        return "FreePlateSymbolLog." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jl3lp.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.MGReelWeightResult != null && message.hasOwnProperty("MGReelWeightResult"))
                if (!$util.isInteger(message.MGReelWeightResult))
                    return "MGReelWeightResult: integer expected";
            if (message.BlueData != null && message.hasOwnProperty("BlueData")) {
                if (!Array.isArray(message.BlueData))
                    return "BlueData: array expected";
                for (let i = 0; i < message.BlueData.length; ++i) {
                    let error = $root.jl3lp.FeatureEffect.verify(message.BlueData[i]);
                    if (error)
                        return "BlueData." + error;
                }
            }
            if (message.RedData != null && message.hasOwnProperty("RedData")) {
                if (!Array.isArray(message.RedData))
                    return "RedData: array expected";
                for (let i = 0; i < message.RedData.length; ++i) {
                    let error = $root.jl3lp.FeatureEffect.verify(message.RedData[i]);
                    if (error)
                        return "RedData." + error;
                }
            }
            if (message.GreenData != null && message.hasOwnProperty("GreenData")) {
                if (!Array.isArray(message.GreenData))
                    return "GreenData: array expected";
                for (let i = 0; i < message.GreenData.length; ++i) {
                    let error = $root.jl3lp.FeatureEffect.verify(message.GreenData[i]);
                    if (error)
                        return "GreenData." + error;
                }
            }
            if (message.AddCrack != null && message.hasOwnProperty("AddCrack")) {
                if (!Array.isArray(message.AddCrack))
                    return "AddCrack: array expected";
                for (let i = 0; i < message.AddCrack.length; ++i)
                    if (typeof message.AddCrack[i] !== "boolean")
                        return "AddCrack: boolean[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.RoundInfo)
                return object;
            let message = new $root.jl3lp.RoundInfo();
            if (object.MainPlateSymbol) {
                if (!Array.isArray(object.MainPlateSymbol))
                    throw TypeError(".jl3lp.RoundInfo.MainPlateSymbol: array expected");
                message.MainPlateSymbol = [];
                for (let i = 0; i < object.MainPlateSymbol.length; ++i) {
                    if (typeof object.MainPlateSymbol[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.MainPlateSymbol: object expected");
                    message.MainPlateSymbol[i] = $root.jl3lp.Column.fromObject(object.MainPlateSymbol[i]);
                }
            }
            if (object.FreePlateSymbol) {
                if (!Array.isArray(object.FreePlateSymbol))
                    throw TypeError(".jl3lp.RoundInfo.FreePlateSymbol: array expected");
                message.FreePlateSymbol = [];
                for (let i = 0; i < object.FreePlateSymbol.length; ++i) {
                    if (typeof object.FreePlateSymbol[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.FreePlateSymbol: object expected");
                    message.FreePlateSymbol[i] = $root.jl3lp.CColumn.fromObject(object.FreePlateSymbol[i]);
                }
            }
            if (object.FreePlateSymbolLog) {
                if (!Array.isArray(object.FreePlateSymbolLog))
                    throw TypeError(".jl3lp.RoundInfo.FreePlateSymbolLog: array expected");
                message.FreePlateSymbolLog = [];
                for (let i = 0; i < object.FreePlateSymbolLog.length; ++i) {
                    if (typeof object.FreePlateSymbolLog[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.FreePlateSymbolLog: object expected");
                    message.FreePlateSymbolLog[i] = $root.jl3lp.CColumn.fromObject(object.FreePlateSymbolLog[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jl3lp.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jl3lp.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.MGReelWeightResult != null)
                message.MGReelWeightResult = object.MGReelWeightResult | 0;
            if (object.BlueData) {
                if (!Array.isArray(object.BlueData))
                    throw TypeError(".jl3lp.RoundInfo.BlueData: array expected");
                message.BlueData = [];
                for (let i = 0; i < object.BlueData.length; ++i) {
                    if (typeof object.BlueData[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.BlueData: object expected");
                    message.BlueData[i] = $root.jl3lp.FeatureEffect.fromObject(object.BlueData[i]);
                }
            }
            if (object.RedData) {
                if (!Array.isArray(object.RedData))
                    throw TypeError(".jl3lp.RoundInfo.RedData: array expected");
                message.RedData = [];
                for (let i = 0; i < object.RedData.length; ++i) {
                    if (typeof object.RedData[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.RedData: object expected");
                    message.RedData[i] = $root.jl3lp.FeatureEffect.fromObject(object.RedData[i]);
                }
            }
            if (object.GreenData) {
                if (!Array.isArray(object.GreenData))
                    throw TypeError(".jl3lp.RoundInfo.GreenData: array expected");
                message.GreenData = [];
                for (let i = 0; i < object.GreenData.length; ++i) {
                    if (typeof object.GreenData[i] !== "object")
                        throw TypeError(".jl3lp.RoundInfo.GreenData: object expected");
                    message.GreenData[i] = $root.jl3lp.FeatureEffect.fromObject(object.GreenData[i]);
                }
            }
            if (object.AddCrack) {
                if (!Array.isArray(object.AddCrack))
                    throw TypeError(".jl3lp.RoundInfo.AddCrack: array expected");
                message.AddCrack = [];
                for (let i = 0; i < object.AddCrack.length; ++i)
                    message.AddCrack[i] = Boolean(object.AddCrack[i]);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {jl3lp.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.MainPlateSymbol = [];
                object.FreePlateSymbol = [];
                object.FreePlateSymbolLog = [];
                object.AwardDataVec = [];
                object.BlueData = [];
                object.RedData = [];
                object.GreenData = [];
                object.AddCrack = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RoundWin = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.MGReelWeightResult = 0;
                object.MaxFlag = false;
            }
            if (message.MainPlateSymbol && message.MainPlateSymbol.length) {
                object.MainPlateSymbol = [];
                for (let j = 0; j < message.MainPlateSymbol.length; ++j)
                    object.MainPlateSymbol[j] = $root.jl3lp.Column.toObject(message.MainPlateSymbol[j], options);
            }
            if (message.FreePlateSymbol && message.FreePlateSymbol.length) {
                object.FreePlateSymbol = [];
                for (let j = 0; j < message.FreePlateSymbol.length; ++j)
                    object.FreePlateSymbol[j] = $root.jl3lp.CColumn.toObject(message.FreePlateSymbol[j], options);
            }
            if (message.FreePlateSymbolLog && message.FreePlateSymbolLog.length) {
                object.FreePlateSymbolLog = [];
                for (let j = 0; j < message.FreePlateSymbolLog.length; ++j)
                    object.FreePlateSymbolLog[j] = $root.jl3lp.CColumn.toObject(message.FreePlateSymbolLog[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jl3lp.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.MGReelWeightResult != null && message.hasOwnProperty("MGReelWeightResult"))
                object.MGReelWeightResult = message.MGReelWeightResult;
            if (message.BlueData && message.BlueData.length) {
                object.BlueData = [];
                for (let j = 0; j < message.BlueData.length; ++j)
                    object.BlueData[j] = $root.jl3lp.FeatureEffect.toObject(message.BlueData[j], options);
            }
            if (message.RedData && message.RedData.length) {
                object.RedData = [];
                for (let j = 0; j < message.RedData.length; ++j)
                    object.RedData[j] = $root.jl3lp.FeatureEffect.toObject(message.RedData[j], options);
            }
            if (message.GreenData && message.GreenData.length) {
                object.GreenData = [];
                for (let j = 0; j < message.GreenData.length; ++j)
                    object.GreenData[j] = $root.jl3lp.FeatureEffect.toObject(message.GreenData[j], options);
            }
            if (message.AddCrack && message.AddCrack.length) {
                object.AddCrack = [];
                for (let j = 0; j < message.AddCrack.length; ++j)
                    object.AddCrack[j] = message.AddCrack[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jl3lp.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jl3lp.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.RoundInfo";
        };

        return RoundInfo;
    })();

    jl3lp.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jl3lp
         * @interface ISpinAck
         * @property {Array.<jl3lp.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FolderIndex] SpinAck FolderIndex
         * @property {Array.<number>|null} [CrackLevels] SpinAck CrackLevels
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jl3lp
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jl3lp.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jl3lp.IRoundInfo>} RoundQueue
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FolderIndex.
         * @member {number} FolderIndex
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.FolderIndex = 0;

        /**
         * SpinAck CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3lp.SpinAck
         * @instance
         */
        SpinAck.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jl3lp.SpinAck
         * @static
         * @param {jl3lp.ISpinAck=} [properties] Properties to set
         * @returns {jl3lp.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jl3lp.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.SpinAck
         * @static
         * @param {jl3lp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jl3lp.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            if (message.FolderIndex != null && Object.hasOwnProperty.call(message, "FolderIndex"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FolderIndex);
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jl3lp.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.SpinAck
         * @static
         * @param {jl3lp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jl3lp.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                case 9: {
                        message.FolderIndex = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jl3lp.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jl3lp.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FolderIndex != null && message.hasOwnProperty("FolderIndex"))
                if (!$util.isInteger(message.FolderIndex))
                    return "FolderIndex: integer expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.SpinAck)
                return object;
            let message = new $root.jl3lp.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jl3lp.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jl3lp.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jl3lp.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FolderIndex != null)
                message.FolderIndex = object.FolderIndex | 0;
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3lp.SpinAck.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.SpinAck
         * @static
         * @param {jl3lp.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.CrackLevels = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
                object.FolderIndex = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jl3lp.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FolderIndex != null && message.hasOwnProperty("FolderIndex"))
                object.FolderIndex = message.FolderIndex;
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jl3lp.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jl3lp.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.SpinAck";
        };

        return SpinAck;
    })();

    jl3lp.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jl3lp
         * @interface IStateInfo
         * @property {Array.<number>|null} [CrackLevels] StateInfo CrackLevels
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jl3lp
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jl3lp.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3lp.StateInfo
         * @instance
         */
        StateInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jl3lp.StateInfo
         * @static
         * @param {jl3lp.IStateInfo=} [properties] Properties to set
         * @returns {jl3lp.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jl3lp.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.StateInfo
         * @static
         * @param {jl3lp.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jl3lp.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.StateInfo
         * @static
         * @param {jl3lp.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jl3lp.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.StateInfo)
                return object;
            let message = new $root.jl3lp.StateInfo();
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3lp.StateInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.StateInfo
         * @static
         * @param {jl3lp.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CrackLevels = [];
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jl3lp.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jl3lp.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.StateInfo";
        };

        return StateInfo;
    })();

    jl3lp.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jl3lp
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [JPOdds] GameInfoData JPOdds
         * @property {Array.<jl3lp.IColumn>|null} [BlueOdds] GameInfoData BlueOdds
         * @property {Array.<jl3lp.IColumn>|null} [CoinOdds] GameInfoData CoinOdds
         * @property {Array.<number>|null} [CrackLevels] GameInfoData CrackLevels
         * @property {number|null} [MaxBet] GameInfoData MaxBet
         * @property {Array.<number>|null} [BuyMul] GameInfoData BuyMul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jl3lp
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jl3lp.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.JPOdds = [];
            this.BlueOdds = [];
            this.CoinOdds = [];
            this.CrackLevels = [];
            this.BuyMul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<number>} JPOdds
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * GameInfoData BlueOdds.
         * @member {Array.<jl3lp.IColumn>} BlueOdds
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BlueOdds = $util.emptyArray;

        /**
         * GameInfoData CoinOdds.
         * @member {Array.<jl3lp.IColumn>} CoinOdds
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CoinOdds = $util.emptyArray;

        /**
         * GameInfoData CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CrackLevels = $util.emptyArray;

        /**
         * GameInfoData MaxBet.
         * @member {number} MaxBet
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.MaxBet = 0;

        /**
         * GameInfoData BuyMul.
         * @member {Array.<number>} BuyMul
         * @memberof jl3lp.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BuyMul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {jl3lp.IGameInfoData=} [properties] Properties to set
         * @returns {jl3lp.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jl3lp.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {jl3lp.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.JPOdds != null && message.JPOdds.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.JPOdds.length; ++i)
                    writer.int32(message.JPOdds[i]);
                writer.ldelim();
            }
            if (message.BlueOdds != null && message.BlueOdds.length)
                for (let i = 0; i < message.BlueOdds.length; ++i)
                    $root.jl3lp.Column.encode(message.BlueOdds[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.CoinOdds != null && message.CoinOdds.length)
                for (let i = 0; i < message.CoinOdds.length; ++i)
                    $root.jl3lp.Column.encode(message.CoinOdds[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            if (message.MaxBet != null && Object.hasOwnProperty.call(message, "MaxBet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.MaxBet);
            if (message.BuyMul != null && message.BuyMul.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.BuyMul.length; ++i)
                    writer.double(message.BuyMul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jl3lp.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {jl3lp.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPOdds.push(reader.int32());
                        } else
                            message.JPOdds.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.BlueOdds && message.BlueOdds.length))
                            message.BlueOdds = [];
                        message.BlueOdds.push($root.jl3lp.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.CoinOdds && message.CoinOdds.length))
                            message.CoinOdds = [];
                        message.CoinOdds.push($root.jl3lp.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                case 7: {
                        message.MaxBet = reader.double();
                        break;
                    }
                case 8: {
                        if (!(message.BuyMul && message.BuyMul.length))
                            message.BuyMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BuyMul.push(reader.double());
                        } else
                            message.BuyMul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i)
                    if (!$util.isInteger(message.JPOdds[i]))
                        return "JPOdds: integer[] expected";
            }
            if (message.BlueOdds != null && message.hasOwnProperty("BlueOdds")) {
                if (!Array.isArray(message.BlueOdds))
                    return "BlueOdds: array expected";
                for (let i = 0; i < message.BlueOdds.length; ++i) {
                    let error = $root.jl3lp.Column.verify(message.BlueOdds[i]);
                    if (error)
                        return "BlueOdds." + error;
                }
            }
            if (message.CoinOdds != null && message.hasOwnProperty("CoinOdds")) {
                if (!Array.isArray(message.CoinOdds))
                    return "CoinOdds: array expected";
                for (let i = 0; i < message.CoinOdds.length; ++i) {
                    let error = $root.jl3lp.Column.verify(message.CoinOdds[i]);
                    if (error)
                        return "CoinOdds." + error;
                }
            }
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                if (typeof message.MaxBet !== "number")
                    return "MaxBet: number expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul")) {
                if (!Array.isArray(message.BuyMul))
                    return "BuyMul: array expected";
                for (let i = 0; i < message.BuyMul.length; ++i)
                    if (typeof message.BuyMul[i] !== "number")
                        return "BuyMul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.GameInfoData)
                return object;
            let message = new $root.jl3lp.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jl3lp.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jl3lp.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i)
                    message.JPOdds[i] = object.JPOdds[i] | 0;
            }
            if (object.BlueOdds) {
                if (!Array.isArray(object.BlueOdds))
                    throw TypeError(".jl3lp.GameInfoData.BlueOdds: array expected");
                message.BlueOdds = [];
                for (let i = 0; i < object.BlueOdds.length; ++i) {
                    if (typeof object.BlueOdds[i] !== "object")
                        throw TypeError(".jl3lp.GameInfoData.BlueOdds: object expected");
                    message.BlueOdds[i] = $root.jl3lp.Column.fromObject(object.BlueOdds[i]);
                }
            }
            if (object.CoinOdds) {
                if (!Array.isArray(object.CoinOdds))
                    throw TypeError(".jl3lp.GameInfoData.CoinOdds: array expected");
                message.CoinOdds = [];
                for (let i = 0; i < object.CoinOdds.length; ++i) {
                    if (typeof object.CoinOdds[i] !== "object")
                        throw TypeError(".jl3lp.GameInfoData.CoinOdds: object expected");
                    message.CoinOdds[i] = $root.jl3lp.Column.fromObject(object.CoinOdds[i]);
                }
            }
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jl3lp.GameInfoData.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            if (object.MaxBet != null)
                message.MaxBet = Number(object.MaxBet);
            if (object.BuyMul) {
                if (!Array.isArray(object.BuyMul))
                    throw TypeError(".jl3lp.GameInfoData.BuyMul: array expected");
                message.BuyMul = [];
                for (let i = 0; i < object.BuyMul.length; ++i)
                    message.BuyMul[i] = Number(object.BuyMul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {jl3lp.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.JPOdds = [];
                object.BlueOdds = [];
                object.CoinOdds = [];
                object.CrackLevels = [];
                object.BuyMul = [];
            }
            if (options.defaults) {
                object.ShowExtra = false;
                object.MaxBet = 0;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = message.JPOdds[j];
            }
            if (message.BlueOdds && message.BlueOdds.length) {
                object.BlueOdds = [];
                for (let j = 0; j < message.BlueOdds.length; ++j)
                    object.BlueOdds[j] = $root.jl3lp.Column.toObject(message.BlueOdds[j], options);
            }
            if (message.CoinOdds && message.CoinOdds.length) {
                object.CoinOdds = [];
                for (let j = 0; j < message.CoinOdds.length; ++j)
                    object.CoinOdds[j] = $root.jl3lp.Column.toObject(message.CoinOdds[j], options);
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                object.MaxBet = options.json && !isFinite(message.MaxBet) ? String(message.MaxBet) : message.MaxBet;
            if (message.BuyMul && message.BuyMul.length) {
                object.BuyMul = [];
                for (let j = 0; j < message.BuyMul.length; ++j)
                    object.BuyMul[j] = options.json && !isFinite(message.BuyMul[j]) ? String(message.BuyMul[j]) : message.BuyMul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jl3lp.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jl3lp.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.GameInfoData";
        };

        return GameInfoData;
    })();

    jl3lp.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jl3lp
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         * @property {number|null} [BuyType] BuyReq BuyType
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jl3lp
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jl3lp.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jl3lp.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * BuyReq BuyType.
         * @member {number} BuyType
         * @memberof jl3lp.BuyReq
         * @instance
         */
        BuyReq.prototype.BuyType = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jl3lp.BuyReq
         * @static
         * @param {jl3lp.IBuyReq=} [properties] Properties to set
         * @returns {jl3lp.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jl3lp.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jl3lp.BuyReq
         * @static
         * @param {jl3lp.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            if (message.BuyType != null && Object.hasOwnProperty.call(message, "BuyType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.BuyType);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jl3lp.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jl3lp.BuyReq
         * @static
         * @param {jl3lp.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jl3lp.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jl3lp.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jl3lp.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 2: {
                        message.BuyType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jl3lp.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jl3lp.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jl3lp.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                if (!$util.isInteger(message.BuyType))
                    return "BuyType: integer expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jl3lp.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jl3lp.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jl3lp.BuyReq)
                return object;
            let message = new $root.jl3lp.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.BuyType != null)
                message.BuyType = object.BuyType | 0;
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jl3lp.BuyReq
         * @static
         * @param {jl3lp.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseBet = 0;
                object.BuyType = 0;
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.BuyType != null && message.hasOwnProperty("BuyType"))
                object.BuyType = message.BuyType;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jl3lp.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jl3lp.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jl3lp.BuyReq";
        };

        return BuyReq;
    })();

    return jl3lp;
})();

export const jlaa = $root.jlaa = (() => {

    /**
     * Namespace jlaa.
     * @exports jlaa
     * @namespace
     */
    const jlaa = {};

    jlaa.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlaa
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlaa
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlaa.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlaa.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlaa.Column
         * @static
         * @param {jlaa.IColumn=} [properties] Properties to set
         * @returns {jlaa.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlaa.Column.verify|verify} messages.
         * @function encode
         * @memberof jlaa.Column
         * @static
         * @param {jlaa.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlaa.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaa.Column
         * @static
         * @param {jlaa.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlaa.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaa.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaa.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaa.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaa.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlaa.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaa.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaa.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaa.Column)
                return object;
            let message = new $root.jlaa.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlaa.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaa.Column
         * @static
         * @param {jlaa.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlaa.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlaa.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaa.Column";
        };

        return Column;
    })();

    jlaa.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlaa
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlaa
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlaa.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlaa.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlaa.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlaa.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlaa.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlaa.AwardData
         * @static
         * @param {jlaa.IAwardData=} [properties] Properties to set
         * @returns {jlaa.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlaa.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlaa.AwardData
         * @static
         * @param {jlaa.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlaa.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaa.AwardData
         * @static
         * @param {jlaa.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlaa.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaa.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaa.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaa.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaa.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlaa.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaa.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaa.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaa.AwardData)
                return object;
            let message = new $root.jlaa.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaa.AwardData
         * @static
         * @param {jlaa.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlaa.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlaa.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaa.AwardData";
        };

        return AwardData;
    })();

    jlaa.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlaa
         * @interface IRoundInfo
         * @property {Array.<jlaa.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlaa.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeLevel] RoundInfo FreeLevel
         * @property {number|null} [FreeEnergy] RoundInfo FreeEnergy
         * @property {number|null} [FreeUpgrade] RoundInfo FreeUpgrade
         * @property {number|null} [AddFGRound] RoundInfo AddFGRound
         * @property {Array.<jlaa.IColumn>|null} [PlateSymbolLog] RoundInfo PlateSymbolLog
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlaa
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlaa.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.PlateSymbolLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlaa.IColumn>} PlateSymbol
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlaa.IAwardData>} AwardDataVec
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeLevel.
         * @member {number} FreeLevel
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeLevel = 0;

        /**
         * RoundInfo FreeEnergy.
         * @member {number} FreeEnergy
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeEnergy = 0;

        /**
         * RoundInfo FreeUpgrade.
         * @member {number} FreeUpgrade
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeUpgrade = 0;

        /**
         * RoundInfo AddFGRound.
         * @member {number} AddFGRound
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddFGRound = 0;

        /**
         * RoundInfo PlateSymbolLog.
         * @member {Array.<jlaa.IColumn>} PlateSymbolLog
         * @memberof jlaa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlaa.RoundInfo
         * @static
         * @param {jlaa.IRoundInfo=} [properties] Properties to set
         * @returns {jlaa.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlaa.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlaa.RoundInfo
         * @static
         * @param {jlaa.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlaa.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlaa.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RoundWin);
            if (message.FreeLevel != null && Object.hasOwnProperty.call(message, "FreeLevel"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeLevel);
            if (message.FreeEnergy != null && Object.hasOwnProperty.call(message, "FreeEnergy"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeEnergy);
            if (message.FreeUpgrade != null && Object.hasOwnProperty.call(message, "FreeUpgrade"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeUpgrade);
            if (message.AddFGRound != null && Object.hasOwnProperty.call(message, "AddFGRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AddFGRound);
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlaa.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlaa.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaa.RoundInfo
         * @static
         * @param {jlaa.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlaa.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaa.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaa.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlaa.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlaa.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 6: {
                        message.FreeLevel = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeEnergy = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeUpgrade = reader.int32();
                        break;
                    }
                case 9: {
                        message.AddFGRound = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlaa.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaa.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaa.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlaa.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlaa.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlaa.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeLevel != null && message.hasOwnProperty("FreeLevel"))
                if (!$util.isInteger(message.FreeLevel))
                    return "FreeLevel: integer expected";
            if (message.FreeEnergy != null && message.hasOwnProperty("FreeEnergy"))
                if (!$util.isInteger(message.FreeEnergy))
                    return "FreeEnergy: integer expected";
            if (message.FreeUpgrade != null && message.hasOwnProperty("FreeUpgrade"))
                if (!$util.isInteger(message.FreeUpgrade))
                    return "FreeUpgrade: integer expected";
            if (message.AddFGRound != null && message.hasOwnProperty("AddFGRound"))
                if (!$util.isInteger(message.AddFGRound))
                    return "AddFGRound: integer expected";
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlaa.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaa.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaa.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaa.RoundInfo)
                return object;
            let message = new $root.jlaa.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlaa.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlaa.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlaa.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlaa.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlaa.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlaa.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeLevel != null)
                message.FreeLevel = object.FreeLevel | 0;
            if (object.FreeEnergy != null)
                message.FreeEnergy = object.FreeEnergy | 0;
            if (object.FreeUpgrade != null)
                message.FreeUpgrade = object.FreeUpgrade | 0;
            if (object.AddFGRound != null)
                message.AddFGRound = object.AddFGRound | 0;
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlaa.RoundInfo.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlaa.RoundInfo.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlaa.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaa.RoundInfo
         * @static
         * @param {jlaa.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.PlateSymbolLog = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.FreeLevel = 0;
                object.FreeEnergy = 0;
                object.FreeUpgrade = 0;
                object.AddFGRound = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlaa.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlaa.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeLevel != null && message.hasOwnProperty("FreeLevel"))
                object.FreeLevel = message.FreeLevel;
            if (message.FreeEnergy != null && message.hasOwnProperty("FreeEnergy"))
                object.FreeEnergy = message.FreeEnergy;
            if (message.FreeUpgrade != null && message.hasOwnProperty("FreeUpgrade"))
                object.FreeUpgrade = message.FreeUpgrade;
            if (message.AddFGRound != null && message.hasOwnProperty("AddFGRound"))
                object.AddFGRound = message.AddFGRound;
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlaa.Column.toObject(message.PlateSymbolLog[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlaa.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlaa.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaa.RoundInfo";
        };

        return RoundInfo;
    })();

    jlaa.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlaa
         * @interface ISpinAck
         * @property {Array.<jlaa.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [FreeTotalEnergy] SpinAck FreeTotalEnergy
         * @property {number|null} [Level4Mul] SpinAck Level4Mul
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlaa
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlaa.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlaa.IRoundInfo>} RoundQueue
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck FreeTotalEnergy.
         * @member {number} FreeTotalEnergy
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalEnergy = 0;

        /**
         * SpinAck Level4Mul.
         * @member {number} Level4Mul
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.Level4Mul = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlaa.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlaa.SpinAck
         * @static
         * @param {jlaa.ISpinAck=} [properties] Properties to set
         * @returns {jlaa.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlaa.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlaa.SpinAck
         * @static
         * @param {jlaa.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlaa.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.FreeTotalEnergy != null && Object.hasOwnProperty.call(message, "FreeTotalEnergy"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeTotalEnergy);
            if (message.Level4Mul != null && Object.hasOwnProperty.call(message, "Level4Mul"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Level4Mul);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlaa.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaa.SpinAck
         * @static
         * @param {jlaa.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlaa.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaa.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaa.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlaa.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeTotalEnergy = reader.int32();
                        break;
                    }
                case 8: {
                        message.Level4Mul = reader.double();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaa.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaa.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlaa.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlaa.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.FreeTotalEnergy != null && message.hasOwnProperty("FreeTotalEnergy"))
                if (!$util.isInteger(message.FreeTotalEnergy))
                    return "FreeTotalEnergy: integer expected";
            if (message.Level4Mul != null && message.hasOwnProperty("Level4Mul"))
                if (typeof message.Level4Mul !== "number")
                    return "Level4Mul: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaa.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaa.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaa.SpinAck)
                return object;
            let message = new $root.jlaa.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlaa.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlaa.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlaa.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.FreeTotalEnergy != null)
                message.FreeTotalEnergy = object.FreeTotalEnergy | 0;
            if (object.Level4Mul != null)
                message.Level4Mul = Number(object.Level4Mul);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaa.SpinAck
         * @static
         * @param {jlaa.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.FreeTotalEnergy = 0;
                object.Level4Mul = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlaa.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.FreeTotalEnergy != null && message.hasOwnProperty("FreeTotalEnergy"))
                object.FreeTotalEnergy = message.FreeTotalEnergy;
            if (message.Level4Mul != null && message.hasOwnProperty("Level4Mul"))
                object.Level4Mul = options.json && !isFinite(message.Level4Mul) ? String(message.Level4Mul) : message.Level4Mul;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlaa.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlaa.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaa.SpinAck";
        };

        return SpinAck;
    })();

    jlaa.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlaa
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         * @property {Array.<jlaa.IColumn>|null} [FGUpgrade] GameInfoAck FGUpgrade
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlaa
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlaa.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            this.FGUpgrade = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlaa.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoAck FGUpgrade.
         * @member {Array.<jlaa.IColumn>} FGUpgrade
         * @memberof jlaa.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.FGUpgrade = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {jlaa.IGameInfoAck=} [properties] Properties to set
         * @returns {jlaa.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlaa.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {jlaa.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.FGUpgrade != null && message.FGUpgrade.length)
                for (let i = 0; i < message.FGUpgrade.length; ++i)
                    $root.jlaa.Column.encode(message.FGUpgrade[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlaa.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {jlaa.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaa.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaa.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 2: {
                        if (!(message.FGUpgrade && message.FGUpgrade.length))
                            message.FGUpgrade = [];
                        message.FGUpgrade.push($root.jlaa.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaa.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.FGUpgrade != null && message.hasOwnProperty("FGUpgrade")) {
                if (!Array.isArray(message.FGUpgrade))
                    return "FGUpgrade: array expected";
                for (let i = 0; i < message.FGUpgrade.length; ++i) {
                    let error = $root.jlaa.Column.verify(message.FGUpgrade[i]);
                    if (error)
                        return "FGUpgrade." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaa.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaa.GameInfoAck)
                return object;
            let message = new $root.jlaa.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlaa.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.FGUpgrade) {
                if (!Array.isArray(object.FGUpgrade))
                    throw TypeError(".jlaa.GameInfoAck.FGUpgrade: array expected");
                message.FGUpgrade = [];
                for (let i = 0; i < object.FGUpgrade.length; ++i) {
                    if (typeof object.FGUpgrade[i] !== "object")
                        throw TypeError(".jlaa.GameInfoAck.FGUpgrade: object expected");
                    message.FGUpgrade[i] = $root.jlaa.Column.fromObject(object.FGUpgrade[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {jlaa.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.FGUpgrade = [];
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.FGUpgrade && message.FGUpgrade.length) {
                object.FGUpgrade = [];
                for (let j = 0; j < message.FGUpgrade.length; ++j)
                    object.FGUpgrade[j] = $root.jlaa.Column.toObject(message.FGUpgrade[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlaa.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlaa.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaa.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlaa;
})();

export const jlaf = $root.jlaf = (() => {

    /**
     * Namespace jlaf.
     * @exports jlaf
     * @namespace
     */
    const jlaf = {};

    jlaf.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlaf
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlaf
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlaf.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlaf.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlaf.Column
         * @static
         * @param {jlaf.IColumn=} [properties] Properties to set
         * @returns {jlaf.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlaf.Column.verify|verify} messages.
         * @function encode
         * @memberof jlaf.Column
         * @static
         * @param {jlaf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlaf.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.Column
         * @static
         * @param {jlaf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlaf.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.Column)
                return object;
            let message = new $root.jlaf.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlaf.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.Column
         * @static
         * @param {jlaf.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlaf.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlaf.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.Column";
        };

        return Column;
    })();

    jlaf.ColumnDouble = (function() {

        /**
         * Properties of a ColumnDouble.
         * @memberof jlaf
         * @interface IColumnDouble
         * @property {Array.<number>|null} [Col] ColumnDouble Col
         */

        /**
         * Constructs a new ColumnDouble.
         * @memberof jlaf
         * @classdesc Represents a ColumnDouble.
         * @implements IColumnDouble
         * @constructor
         * @param {jlaf.IColumnDouble=} [properties] Properties to set
         */
        function ColumnDouble(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnDouble Col.
         * @member {Array.<number>} Col
         * @memberof jlaf.ColumnDouble
         * @instance
         */
        ColumnDouble.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnDouble instance using the specified properties.
         * @function create
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {jlaf.IColumnDouble=} [properties] Properties to set
         * @returns {jlaf.ColumnDouble} ColumnDouble instance
         */
        ColumnDouble.create = function create(properties) {
            return new ColumnDouble(properties);
        };

        /**
         * Encodes the specified ColumnDouble message. Does not implicitly {@link jlaf.ColumnDouble.verify|verify} messages.
         * @function encode
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {jlaf.IColumnDouble} message ColumnDouble message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnDouble.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnDouble message, length delimited. Does not implicitly {@link jlaf.ColumnDouble.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {jlaf.IColumnDouble} message ColumnDouble message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnDouble.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnDouble message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.ColumnDouble} ColumnDouble
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnDouble.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.ColumnDouble();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnDouble message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.ColumnDouble} ColumnDouble
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnDouble.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnDouble message.
         * @function verify
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnDouble.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnDouble message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.ColumnDouble} ColumnDouble
         */
        ColumnDouble.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.ColumnDouble)
                return object;
            let message = new $root.jlaf.ColumnDouble();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlaf.ColumnDouble.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnDouble message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {jlaf.ColumnDouble} message ColumnDouble
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnDouble.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnDouble to JSON.
         * @function toJSON
         * @memberof jlaf.ColumnDouble
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnDouble.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnDouble
         * @function getTypeUrl
         * @memberof jlaf.ColumnDouble
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnDouble.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.ColumnDouble";
        };

        return ColumnDouble;
    })();

    jlaf.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlaf
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlaf
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlaf.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlaf.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlaf.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlaf.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlaf.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlaf.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlaf.AwardData
         * @static
         * @param {jlaf.IAwardData=} [properties] Properties to set
         * @returns {jlaf.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlaf.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlaf.AwardData
         * @static
         * @param {jlaf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlaf.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.AwardData
         * @static
         * @param {jlaf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlaf.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.AwardData)
                return object;
            let message = new $root.jlaf.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlaf.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.AwardData
         * @static
         * @param {jlaf.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlaf.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlaf.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.AwardData";
        };

        return AwardData;
    })();

    jlaf.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlaf
         * @interface IRoundInfo
         * @property {Array.<jlaf.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlaf.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {number|null} [FGNowBeans] RoundInfo FGNowBeans
         * @property {number|null} [FGNowLevel] RoundInfo FGNowLevel
         * @property {number|null} [MysterySymbol] RoundInfo MysterySymbol
         * @property {Array.<number>|null} [NewBeans] RoundInfo NewBeans
         * @property {Array.<number>|null} [UpdatePos] RoundInfo UpdatePos
         * @property {Array.<number>|null} [MysteryPos] RoundInfo MysteryPos
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AvgPrice] RoundInfo AvgPrice
         * @property {Array.<jlaf.IColumnDouble>|null} [Top3] RoundInfo Top3
         * @property {Array.<number>|null} [RandomTransSymbols] RoundInfo RandomTransSymbols
         * @property {Array.<number>|null} [RandomTransPos] RoundInfo RandomTransPos
         * @property {Array.<jlaf.IColumn>|null} [PlateSymbolLog] RoundInfo PlateSymbolLog
         * @property {Array.<number>|null} [NewBeansLog] RoundInfo NewBeansLog
         * @property {jlaf.IStateInfo|null} [State] RoundInfo State
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlaf
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlaf.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.NewBeans = [];
            this.UpdatePos = [];
            this.MysteryPos = [];
            this.Top3 = [];
            this.RandomTransSymbols = [];
            this.RandomTransPos = [];
            this.PlateSymbolLog = [];
            this.NewBeansLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlaf.IColumn>} PlateSymbol
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlaf.IAwardData>} AwardDataVec
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo FGNowBeans.
         * @member {number} FGNowBeans
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGNowBeans = 0;

        /**
         * RoundInfo FGNowLevel.
         * @member {number} FGNowLevel
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGNowLevel = 0;

        /**
         * RoundInfo MysterySymbol.
         * @member {number} MysterySymbol
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MysterySymbol = 0;

        /**
         * RoundInfo NewBeans.
         * @member {Array.<number>} NewBeans
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NewBeans = $util.emptyArray;

        /**
         * RoundInfo UpdatePos.
         * @member {Array.<number>} UpdatePos
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.UpdatePos = $util.emptyArray;

        /**
         * RoundInfo MysteryPos.
         * @member {Array.<number>} MysteryPos
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MysteryPos = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AvgPrice.
         * @member {number} AvgPrice
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AvgPrice = 0;

        /**
         * RoundInfo Top3.
         * @member {Array.<jlaf.IColumnDouble>} Top3
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Top3 = $util.emptyArray;

        /**
         * RoundInfo RandomTransSymbols.
         * @member {Array.<number>} RandomTransSymbols
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RandomTransSymbols = $util.emptyArray;

        /**
         * RoundInfo RandomTransPos.
         * @member {Array.<number>} RandomTransPos
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RandomTransPos = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolLog.
         * @member {Array.<jlaf.IColumn>} PlateSymbolLog
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo NewBeansLog.
         * @member {Array.<number>} NewBeansLog
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NewBeansLog = $util.emptyArray;

        /**
         * RoundInfo State.
         * @member {jlaf.IStateInfo|null|undefined} State
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.State = null;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlaf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlaf.RoundInfo
         * @static
         * @param {jlaf.IRoundInfo=} [properties] Properties to set
         * @returns {jlaf.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlaf.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlaf.RoundInfo
         * @static
         * @param {jlaf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlaf.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlaf.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AddRound);
            if (message.FGNowBeans != null && Object.hasOwnProperty.call(message, "FGNowBeans"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FGNowBeans);
            if (message.FGNowLevel != null && Object.hasOwnProperty.call(message, "FGNowLevel"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FGNowLevel);
            if (message.MysterySymbol != null && Object.hasOwnProperty.call(message, "MysterySymbol"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.MysterySymbol);
            if (message.NewBeans != null && message.NewBeans.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.NewBeans.length; ++i)
                    writer.int32(message.NewBeans[i]);
                writer.ldelim();
            }
            if (message.UpdatePos != null && message.UpdatePos.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.UpdatePos.length; ++i)
                    writer.int32(message.UpdatePos[i]);
                writer.ldelim();
            }
            if (message.MysteryPos != null && message.MysteryPos.length) {
                writer.uint32(/* id 12, wireType 2 =*/98).fork();
                for (let i = 0; i < message.MysteryPos.length; ++i)
                    writer.int32(message.MysteryPos[i]);
                writer.ldelim();
            }
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.RoundWin);
            if (message.AvgPrice != null && Object.hasOwnProperty.call(message, "AvgPrice"))
                writer.uint32(/* id 14, wireType 1 =*/113).double(message.AvgPrice);
            if (message.Top3 != null && message.Top3.length)
                for (let i = 0; i < message.Top3.length; ++i)
                    $root.jlaf.ColumnDouble.encode(message.Top3[i], writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.RandomTransSymbols != null && message.RandomTransSymbols.length) {
                writer.uint32(/* id 16, wireType 2 =*/130).fork();
                for (let i = 0; i < message.RandomTransSymbols.length; ++i)
                    writer.int32(message.RandomTransSymbols[i]);
                writer.ldelim();
            }
            if (message.RandomTransPos != null && message.RandomTransPos.length) {
                writer.uint32(/* id 17, wireType 2 =*/138).fork();
                for (let i = 0; i < message.RandomTransPos.length; ++i)
                    writer.int32(message.RandomTransPos[i]);
                writer.ldelim();
            }
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlaf.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 18, wireType 2 =*/146).fork()).ldelim();
            if (message.NewBeansLog != null && message.NewBeansLog.length) {
                writer.uint32(/* id 19, wireType 2 =*/154).fork();
                for (let i = 0; i < message.NewBeansLog.length; ++i)
                    writer.int32(message.NewBeansLog[i]);
                writer.ldelim();
            }
            if (message.State != null && Object.hasOwnProperty.call(message, "State"))
                $root.jlaf.StateInfo.encode(message.State, writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlaf.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.RoundInfo
         * @static
         * @param {jlaf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlaf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlaf.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.FGNowBeans = reader.int32();
                        break;
                    }
                case 8: {
                        message.FGNowLevel = reader.int32();
                        break;
                    }
                case 9: {
                        message.MysterySymbol = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.NewBeans && message.NewBeans.length))
                            message.NewBeans = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NewBeans.push(reader.int32());
                        } else
                            message.NewBeans.push(reader.int32());
                        break;
                    }
                case 11: {
                        if (!(message.UpdatePos && message.UpdatePos.length))
                            message.UpdatePos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.UpdatePos.push(reader.int32());
                        } else
                            message.UpdatePos.push(reader.int32());
                        break;
                    }
                case 12: {
                        if (!(message.MysteryPos && message.MysteryPos.length))
                            message.MysteryPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.MysteryPos.push(reader.int32());
                        } else
                            message.MysteryPos.push(reader.int32());
                        break;
                    }
                case 13: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 14: {
                        message.AvgPrice = reader.double();
                        break;
                    }
                case 15: {
                        if (!(message.Top3 && message.Top3.length))
                            message.Top3 = [];
                        message.Top3.push($root.jlaf.ColumnDouble.decode(reader, reader.uint32()));
                        break;
                    }
                case 16: {
                        if (!(message.RandomTransSymbols && message.RandomTransSymbols.length))
                            message.RandomTransSymbols = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RandomTransSymbols.push(reader.int32());
                        } else
                            message.RandomTransSymbols.push(reader.int32());
                        break;
                    }
                case 17: {
                        if (!(message.RandomTransPos && message.RandomTransPos.length))
                            message.RandomTransPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RandomTransPos.push(reader.int32());
                        } else
                            message.RandomTransPos.push(reader.int32());
                        break;
                    }
                case 18: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlaf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 19: {
                        if (!(message.NewBeansLog && message.NewBeansLog.length))
                            message.NewBeansLog = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NewBeansLog.push(reader.int32());
                        } else
                            message.NewBeansLog.push(reader.int32());
                        break;
                    }
                case 20: {
                        message.State = $root.jlaf.StateInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 21: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlaf.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlaf.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlaf.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.FGNowBeans != null && message.hasOwnProperty("FGNowBeans"))
                if (!$util.isInteger(message.FGNowBeans))
                    return "FGNowBeans: integer expected";
            if (message.FGNowLevel != null && message.hasOwnProperty("FGNowLevel"))
                if (!$util.isInteger(message.FGNowLevel))
                    return "FGNowLevel: integer expected";
            if (message.MysterySymbol != null && message.hasOwnProperty("MysterySymbol"))
                if (!$util.isInteger(message.MysterySymbol))
                    return "MysterySymbol: integer expected";
            if (message.NewBeans != null && message.hasOwnProperty("NewBeans")) {
                if (!Array.isArray(message.NewBeans))
                    return "NewBeans: array expected";
                for (let i = 0; i < message.NewBeans.length; ++i)
                    if (!$util.isInteger(message.NewBeans[i]))
                        return "NewBeans: integer[] expected";
            }
            if (message.UpdatePos != null && message.hasOwnProperty("UpdatePos")) {
                if (!Array.isArray(message.UpdatePos))
                    return "UpdatePos: array expected";
                for (let i = 0; i < message.UpdatePos.length; ++i)
                    if (!$util.isInteger(message.UpdatePos[i]))
                        return "UpdatePos: integer[] expected";
            }
            if (message.MysteryPos != null && message.hasOwnProperty("MysteryPos")) {
                if (!Array.isArray(message.MysteryPos))
                    return "MysteryPos: array expected";
                for (let i = 0; i < message.MysteryPos.length; ++i)
                    if (!$util.isInteger(message.MysteryPos[i]))
                        return "MysteryPos: integer[] expected";
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AvgPrice != null && message.hasOwnProperty("AvgPrice"))
                if (typeof message.AvgPrice !== "number")
                    return "AvgPrice: number expected";
            if (message.Top3 != null && message.hasOwnProperty("Top3")) {
                if (!Array.isArray(message.Top3))
                    return "Top3: array expected";
                for (let i = 0; i < message.Top3.length; ++i) {
                    let error = $root.jlaf.ColumnDouble.verify(message.Top3[i]);
                    if (error)
                        return "Top3." + error;
                }
            }
            if (message.RandomTransSymbols != null && message.hasOwnProperty("RandomTransSymbols")) {
                if (!Array.isArray(message.RandomTransSymbols))
                    return "RandomTransSymbols: array expected";
                for (let i = 0; i < message.RandomTransSymbols.length; ++i)
                    if (!$util.isInteger(message.RandomTransSymbols[i]))
                        return "RandomTransSymbols: integer[] expected";
            }
            if (message.RandomTransPos != null && message.hasOwnProperty("RandomTransPos")) {
                if (!Array.isArray(message.RandomTransPos))
                    return "RandomTransPos: array expected";
                for (let i = 0; i < message.RandomTransPos.length; ++i)
                    if (!$util.isInteger(message.RandomTransPos[i]))
                        return "RandomTransPos: integer[] expected";
            }
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlaf.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            if (message.NewBeansLog != null && message.hasOwnProperty("NewBeansLog")) {
                if (!Array.isArray(message.NewBeansLog))
                    return "NewBeansLog: array expected";
                for (let i = 0; i < message.NewBeansLog.length; ++i)
                    if (!$util.isInteger(message.NewBeansLog[i]))
                        return "NewBeansLog: integer[] expected";
            }
            if (message.State != null && message.hasOwnProperty("State")) {
                let error = $root.jlaf.StateInfo.verify(message.State);
                if (error)
                    return "State." + error;
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.RoundInfo)
                return object;
            let message = new $root.jlaf.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlaf.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlaf.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlaf.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlaf.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlaf.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlaf.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.FGNowBeans != null)
                message.FGNowBeans = object.FGNowBeans | 0;
            if (object.FGNowLevel != null)
                message.FGNowLevel = object.FGNowLevel | 0;
            if (object.MysterySymbol != null)
                message.MysterySymbol = object.MysterySymbol | 0;
            if (object.NewBeans) {
                if (!Array.isArray(object.NewBeans))
                    throw TypeError(".jlaf.RoundInfo.NewBeans: array expected");
                message.NewBeans = [];
                for (let i = 0; i < object.NewBeans.length; ++i)
                    message.NewBeans[i] = object.NewBeans[i] | 0;
            }
            if (object.UpdatePos) {
                if (!Array.isArray(object.UpdatePos))
                    throw TypeError(".jlaf.RoundInfo.UpdatePos: array expected");
                message.UpdatePos = [];
                for (let i = 0; i < object.UpdatePos.length; ++i)
                    message.UpdatePos[i] = object.UpdatePos[i] | 0;
            }
            if (object.MysteryPos) {
                if (!Array.isArray(object.MysteryPos))
                    throw TypeError(".jlaf.RoundInfo.MysteryPos: array expected");
                message.MysteryPos = [];
                for (let i = 0; i < object.MysteryPos.length; ++i)
                    message.MysteryPos[i] = object.MysteryPos[i] | 0;
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AvgPrice != null)
                message.AvgPrice = Number(object.AvgPrice);
            if (object.Top3) {
                if (!Array.isArray(object.Top3))
                    throw TypeError(".jlaf.RoundInfo.Top3: array expected");
                message.Top3 = [];
                for (let i = 0; i < object.Top3.length; ++i) {
                    if (typeof object.Top3[i] !== "object")
                        throw TypeError(".jlaf.RoundInfo.Top3: object expected");
                    message.Top3[i] = $root.jlaf.ColumnDouble.fromObject(object.Top3[i]);
                }
            }
            if (object.RandomTransSymbols) {
                if (!Array.isArray(object.RandomTransSymbols))
                    throw TypeError(".jlaf.RoundInfo.RandomTransSymbols: array expected");
                message.RandomTransSymbols = [];
                for (let i = 0; i < object.RandomTransSymbols.length; ++i)
                    message.RandomTransSymbols[i] = object.RandomTransSymbols[i] | 0;
            }
            if (object.RandomTransPos) {
                if (!Array.isArray(object.RandomTransPos))
                    throw TypeError(".jlaf.RoundInfo.RandomTransPos: array expected");
                message.RandomTransPos = [];
                for (let i = 0; i < object.RandomTransPos.length; ++i)
                    message.RandomTransPos[i] = object.RandomTransPos[i] | 0;
            }
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlaf.RoundInfo.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlaf.RoundInfo.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlaf.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            if (object.NewBeansLog) {
                if (!Array.isArray(object.NewBeansLog))
                    throw TypeError(".jlaf.RoundInfo.NewBeansLog: array expected");
                message.NewBeansLog = [];
                for (let i = 0; i < object.NewBeansLog.length; ++i)
                    message.NewBeansLog[i] = object.NewBeansLog[i] | 0;
            }
            if (object.State != null) {
                if (typeof object.State !== "object")
                    throw TypeError(".jlaf.RoundInfo.State: object expected");
                message.State = $root.jlaf.StateInfo.fromObject(object.State);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.RoundInfo
         * @static
         * @param {jlaf.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.NewBeans = [];
                object.UpdatePos = [];
                object.MysteryPos = [];
                object.Top3 = [];
                object.RandomTransSymbols = [];
                object.RandomTransPos = [];
                object.PlateSymbolLog = [];
                object.NewBeansLog = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.AddRound = 0;
                object.FGNowBeans = 0;
                object.FGNowLevel = 0;
                object.MysterySymbol = 0;
                object.RoundWin = 0;
                object.AvgPrice = 0;
                object.State = null;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlaf.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlaf.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.FGNowBeans != null && message.hasOwnProperty("FGNowBeans"))
                object.FGNowBeans = message.FGNowBeans;
            if (message.FGNowLevel != null && message.hasOwnProperty("FGNowLevel"))
                object.FGNowLevel = message.FGNowLevel;
            if (message.MysterySymbol != null && message.hasOwnProperty("MysterySymbol"))
                object.MysterySymbol = message.MysterySymbol;
            if (message.NewBeans && message.NewBeans.length) {
                object.NewBeans = [];
                for (let j = 0; j < message.NewBeans.length; ++j)
                    object.NewBeans[j] = message.NewBeans[j];
            }
            if (message.UpdatePos && message.UpdatePos.length) {
                object.UpdatePos = [];
                for (let j = 0; j < message.UpdatePos.length; ++j)
                    object.UpdatePos[j] = message.UpdatePos[j];
            }
            if (message.MysteryPos && message.MysteryPos.length) {
                object.MysteryPos = [];
                for (let j = 0; j < message.MysteryPos.length; ++j)
                    object.MysteryPos[j] = message.MysteryPos[j];
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AvgPrice != null && message.hasOwnProperty("AvgPrice"))
                object.AvgPrice = options.json && !isFinite(message.AvgPrice) ? String(message.AvgPrice) : message.AvgPrice;
            if (message.Top3 && message.Top3.length) {
                object.Top3 = [];
                for (let j = 0; j < message.Top3.length; ++j)
                    object.Top3[j] = $root.jlaf.ColumnDouble.toObject(message.Top3[j], options);
            }
            if (message.RandomTransSymbols && message.RandomTransSymbols.length) {
                object.RandomTransSymbols = [];
                for (let j = 0; j < message.RandomTransSymbols.length; ++j)
                    object.RandomTransSymbols[j] = message.RandomTransSymbols[j];
            }
            if (message.RandomTransPos && message.RandomTransPos.length) {
                object.RandomTransPos = [];
                for (let j = 0; j < message.RandomTransPos.length; ++j)
                    object.RandomTransPos[j] = message.RandomTransPos[j];
            }
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlaf.Column.toObject(message.PlateSymbolLog[j], options);
            }
            if (message.NewBeansLog && message.NewBeansLog.length) {
                object.NewBeansLog = [];
                for (let j = 0; j < message.NewBeansLog.length; ++j)
                    object.NewBeansLog[j] = message.NewBeansLog[j];
            }
            if (message.State != null && message.hasOwnProperty("State"))
                object.State = $root.jlaf.StateInfo.toObject(message.State, options);
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlaf.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlaf.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.RoundInfo";
        };

        return RoundInfo;
    })();

    jlaf.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlaf
         * @interface ISpinAck
         * @property {Array.<jlaf.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlaf
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlaf.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlaf.IRoundInfo>} RoundQueue
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlaf.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlaf.SpinAck
         * @static
         * @param {jlaf.ISpinAck=} [properties] Properties to set
         * @returns {jlaf.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlaf.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlaf.SpinAck
         * @static
         * @param {jlaf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlaf.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlaf.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.SpinAck
         * @static
         * @param {jlaf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlaf.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Bet = reader.double();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlaf.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlaf.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.SpinAck)
                return object;
            let message = new $root.jlaf.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlaf.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlaf.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlaf.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.SpinAck
         * @static
         * @param {jlaf.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlaf.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlaf.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlaf.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.SpinAck";
        };

        return SpinAck;
    })();

    jlaf.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlaf
         * @interface IStateInfo
         * @property {Array.<jlaf.IColumn>|null} [SyncPlateSymbol] StateInfo SyncPlateSymbol
         * @property {Array.<number>|null} [MysteryPos] StateInfo MysteryPos
         * @property {Array.<number>|null} [NewBeans] StateInfo NewBeans
         * @property {number|null} [AvgPrice] StateInfo AvgPrice
         * @property {number|null} [Bet] StateInfo Bet
         * @property {boolean|null} [Done] StateInfo Done
         * @property {Array.<jlaf.IColumnDouble>|null} [Top3] StateInfo Top3
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlaf
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlaf.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.SyncPlateSymbol = [];
            this.MysteryPos = [];
            this.NewBeans = [];
            this.Top3 = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo SyncPlateSymbol.
         * @member {Array.<jlaf.IColumn>} SyncPlateSymbol
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.SyncPlateSymbol = $util.emptyArray;

        /**
         * StateInfo MysteryPos.
         * @member {Array.<number>} MysteryPos
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.MysteryPos = $util.emptyArray;

        /**
         * StateInfo NewBeans.
         * @member {Array.<number>} NewBeans
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.NewBeans = $util.emptyArray;

        /**
         * StateInfo AvgPrice.
         * @member {number} AvgPrice
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.AvgPrice = 0;

        /**
         * StateInfo Bet.
         * @member {number} Bet
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.Bet = 0;

        /**
         * StateInfo Done.
         * @member {boolean} Done
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.Done = false;

        /**
         * StateInfo Top3.
         * @member {Array.<jlaf.IColumnDouble>} Top3
         * @memberof jlaf.StateInfo
         * @instance
         */
        StateInfo.prototype.Top3 = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlaf.StateInfo
         * @static
         * @param {jlaf.IStateInfo=} [properties] Properties to set
         * @returns {jlaf.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlaf.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlaf.StateInfo
         * @static
         * @param {jlaf.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.SyncPlateSymbol != null && message.SyncPlateSymbol.length)
                for (let i = 0; i < message.SyncPlateSymbol.length; ++i)
                    $root.jlaf.Column.encode(message.SyncPlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.MysteryPos != null && message.MysteryPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.MysteryPos.length; ++i)
                    writer.int32(message.MysteryPos[i]);
                writer.ldelim();
            }
            if (message.NewBeans != null && message.NewBeans.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.NewBeans.length; ++i)
                    writer.int32(message.NewBeans[i]);
                writer.ldelim();
            }
            if (message.AvgPrice != null && Object.hasOwnProperty.call(message, "AvgPrice"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.AvgPrice);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Bet);
            if (message.Done != null && Object.hasOwnProperty.call(message, "Done"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.Done);
            if (message.Top3 != null && message.Top3.length)
                for (let i = 0; i < message.Top3.length; ++i)
                    $root.jlaf.ColumnDouble.encode(message.Top3[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlaf.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.StateInfo
         * @static
         * @param {jlaf.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.SyncPlateSymbol && message.SyncPlateSymbol.length))
                            message.SyncPlateSymbol = [];
                        message.SyncPlateSymbol.push($root.jlaf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.MysteryPos && message.MysteryPos.length))
                            message.MysteryPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.MysteryPos.push(reader.int32());
                        } else
                            message.MysteryPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.NewBeans && message.NewBeans.length))
                            message.NewBeans = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NewBeans.push(reader.int32());
                        } else
                            message.NewBeans.push(reader.int32());
                        break;
                    }
                case 4: {
                        message.AvgPrice = reader.double();
                        break;
                    }
                case 5: {
                        message.Bet = reader.double();
                        break;
                    }
                case 6: {
                        message.Done = reader.bool();
                        break;
                    }
                case 7: {
                        if (!(message.Top3 && message.Top3.length))
                            message.Top3 = [];
                        message.Top3.push($root.jlaf.ColumnDouble.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlaf.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.SyncPlateSymbol != null && message.hasOwnProperty("SyncPlateSymbol")) {
                if (!Array.isArray(message.SyncPlateSymbol))
                    return "SyncPlateSymbol: array expected";
                for (let i = 0; i < message.SyncPlateSymbol.length; ++i) {
                    let error = $root.jlaf.Column.verify(message.SyncPlateSymbol[i]);
                    if (error)
                        return "SyncPlateSymbol." + error;
                }
            }
            if (message.MysteryPos != null && message.hasOwnProperty("MysteryPos")) {
                if (!Array.isArray(message.MysteryPos))
                    return "MysteryPos: array expected";
                for (let i = 0; i < message.MysteryPos.length; ++i)
                    if (!$util.isInteger(message.MysteryPos[i]))
                        return "MysteryPos: integer[] expected";
            }
            if (message.NewBeans != null && message.hasOwnProperty("NewBeans")) {
                if (!Array.isArray(message.NewBeans))
                    return "NewBeans: array expected";
                for (let i = 0; i < message.NewBeans.length; ++i)
                    if (!$util.isInteger(message.NewBeans[i]))
                        return "NewBeans: integer[] expected";
            }
            if (message.AvgPrice != null && message.hasOwnProperty("AvgPrice"))
                if (typeof message.AvgPrice !== "number")
                    return "AvgPrice: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Done != null && message.hasOwnProperty("Done"))
                if (typeof message.Done !== "boolean")
                    return "Done: boolean expected";
            if (message.Top3 != null && message.hasOwnProperty("Top3")) {
                if (!Array.isArray(message.Top3))
                    return "Top3: array expected";
                for (let i = 0; i < message.Top3.length; ++i) {
                    let error = $root.jlaf.ColumnDouble.verify(message.Top3[i]);
                    if (error)
                        return "Top3." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.StateInfo)
                return object;
            let message = new $root.jlaf.StateInfo();
            if (object.SyncPlateSymbol) {
                if (!Array.isArray(object.SyncPlateSymbol))
                    throw TypeError(".jlaf.StateInfo.SyncPlateSymbol: array expected");
                message.SyncPlateSymbol = [];
                for (let i = 0; i < object.SyncPlateSymbol.length; ++i) {
                    if (typeof object.SyncPlateSymbol[i] !== "object")
                        throw TypeError(".jlaf.StateInfo.SyncPlateSymbol: object expected");
                    message.SyncPlateSymbol[i] = $root.jlaf.Column.fromObject(object.SyncPlateSymbol[i]);
                }
            }
            if (object.MysteryPos) {
                if (!Array.isArray(object.MysteryPos))
                    throw TypeError(".jlaf.StateInfo.MysteryPos: array expected");
                message.MysteryPos = [];
                for (let i = 0; i < object.MysteryPos.length; ++i)
                    message.MysteryPos[i] = object.MysteryPos[i] | 0;
            }
            if (object.NewBeans) {
                if (!Array.isArray(object.NewBeans))
                    throw TypeError(".jlaf.StateInfo.NewBeans: array expected");
                message.NewBeans = [];
                for (let i = 0; i < object.NewBeans.length; ++i)
                    message.NewBeans[i] = object.NewBeans[i] | 0;
            }
            if (object.AvgPrice != null)
                message.AvgPrice = Number(object.AvgPrice);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Done != null)
                message.Done = Boolean(object.Done);
            if (object.Top3) {
                if (!Array.isArray(object.Top3))
                    throw TypeError(".jlaf.StateInfo.Top3: array expected");
                message.Top3 = [];
                for (let i = 0; i < object.Top3.length; ++i) {
                    if (typeof object.Top3[i] !== "object")
                        throw TypeError(".jlaf.StateInfo.Top3: object expected");
                    message.Top3[i] = $root.jlaf.ColumnDouble.fromObject(object.Top3[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.StateInfo
         * @static
         * @param {jlaf.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.SyncPlateSymbol = [];
                object.MysteryPos = [];
                object.NewBeans = [];
                object.Top3 = [];
            }
            if (options.defaults) {
                object.AvgPrice = 0;
                object.Bet = 0;
                object.Done = false;
            }
            if (message.SyncPlateSymbol && message.SyncPlateSymbol.length) {
                object.SyncPlateSymbol = [];
                for (let j = 0; j < message.SyncPlateSymbol.length; ++j)
                    object.SyncPlateSymbol[j] = $root.jlaf.Column.toObject(message.SyncPlateSymbol[j], options);
            }
            if (message.MysteryPos && message.MysteryPos.length) {
                object.MysteryPos = [];
                for (let j = 0; j < message.MysteryPos.length; ++j)
                    object.MysteryPos[j] = message.MysteryPos[j];
            }
            if (message.NewBeans && message.NewBeans.length) {
                object.NewBeans = [];
                for (let j = 0; j < message.NewBeans.length; ++j)
                    object.NewBeans[j] = message.NewBeans[j];
            }
            if (message.AvgPrice != null && message.hasOwnProperty("AvgPrice"))
                object.AvgPrice = options.json && !isFinite(message.AvgPrice) ? String(message.AvgPrice) : message.AvgPrice;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Done != null && message.hasOwnProperty("Done"))
                object.Done = message.Done;
            if (message.Top3 && message.Top3.length) {
                object.Top3 = [];
                for (let j = 0; j < message.Top3.length; ++j)
                    object.Top3[j] = $root.jlaf.ColumnDouble.toObject(message.Top3[j], options);
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlaf.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlaf.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.StateInfo";
        };

        return StateInfo;
    })();

    jlaf.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jlaf
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jlaf
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jlaf.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jlaf.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jlaf.BuyReq
         * @static
         * @param {jlaf.IBuyReq=} [properties] Properties to set
         * @returns {jlaf.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jlaf.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jlaf.BuyReq
         * @static
         * @param {jlaf.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jlaf.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.BuyReq
         * @static
         * @param {jlaf.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jlaf.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.BuyReq)
                return object;
            let message = new $root.jlaf.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.BuyReq
         * @static
         * @param {jlaf.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.BaseBet = 0;
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jlaf.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jlaf.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.BuyReq";
        };

        return BuyReq;
    })();

    jlaf.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlaf
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlaf
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlaf.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlaf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlaf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlaf.GameInfoData
         * @static
         * @param {jlaf.IGameInfoData=} [properties] Properties to set
         * @returns {jlaf.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlaf.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlaf.GameInfoData
         * @static
         * @param {jlaf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlaf.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlaf.GameInfoData
         * @static
         * @param {jlaf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlaf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlaf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlaf.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlaf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlaf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlaf.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlaf.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlaf.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlaf.GameInfoData)
                return object;
            let message = new $root.jlaf.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlaf.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlaf.GameInfoData
         * @static
         * @param {jlaf.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlaf.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlaf.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlaf.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlaf;
})();

export const jlail = $root.jlail = (() => {

    /**
     * Namespace jlail.
     * @exports jlail
     * @namespace
     */
    const jlail = {};

    jlail.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlail
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlail
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlail.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlail.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlail.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlail.Grid
         * @static
         * @param {jlail.IGrid=} [properties] Properties to set
         * @returns {jlail.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlail.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlail.Grid
         * @static
         * @param {jlail.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlail.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.Grid
         * @static
         * @param {jlail.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlail.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.Grid)
                return object;
            let message = new $root.jlail.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.Grid
         * @static
         * @param {jlail.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlail.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlail.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.Grid";
        };

        return Grid;
    })();

    jlail.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlail
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlail
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlail.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlail.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlail.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlail.MSymbol
         * @static
         * @param {jlail.IMSymbol=} [properties] Properties to set
         * @returns {jlail.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlail.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlail.MSymbol
         * @static
         * @param {jlail.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlail.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.MSymbol
         * @static
         * @param {jlail.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlail.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.MSymbol)
                return object;
            let message = new $root.jlail.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.MSymbol
         * @static
         * @param {jlail.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlail.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlail.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.MSymbol";
        };

        return MSymbol;
    })();

    jlail.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlail
         * @interface IMColumn
         * @property {Array.<jlail.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlail
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlail.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlail.IMSymbol>} MColumn
         * @memberof jlail.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlail.MColumn
         * @static
         * @param {jlail.IMColumn=} [properties] Properties to set
         * @returns {jlail.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlail.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlail.MColumn
         * @static
         * @param {jlail.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlail.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlail.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.MColumn
         * @static
         * @param {jlail.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlail.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlail.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlail.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.MColumn)
                return object;
            let message = new $root.jlail.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlail.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlail.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlail.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.MColumn
         * @static
         * @param {jlail.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlail.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlail.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlail.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.MColumn";
        };

        return MColumn;
    })();

    jlail.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlail
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlail.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlail
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlail.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlail.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlail.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlail.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlail.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlail.IGrid>} GridVec
         * @memberof jlail.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlail.AwardData
         * @static
         * @param {jlail.IAwardData=} [properties] Properties to set
         * @returns {jlail.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlail.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlail.AwardData
         * @static
         * @param {jlail.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlail.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlail.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.AwardData
         * @static
         * @param {jlail.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlail.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlail.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlail.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.AwardData)
                return object;
            let message = new $root.jlail.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlail.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlail.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlail.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.AwardData
         * @static
         * @param {jlail.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlail.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlail.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlail.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.AwardData";
        };

        return AwardData;
    })();

    jlail.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlail
         * @interface IComboStageInfo
         * @property {Array.<jlail.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlail.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlail.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlail
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlail.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlail.IMColumn>} ComboStageSymbol
         * @memberof jlail.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlail.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlail.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlail.IAwardData>} AwardDataVec
         * @memberof jlail.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlail.IMColumn>} FillSymbols
         * @memberof jlail.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlail.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {jlail.IComboStageInfo=} [properties] Properties to set
         * @returns {jlail.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlail.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {jlail.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlail.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlail.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlail.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlail.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {jlail.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlail.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlail.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlail.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlail.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlail.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlail.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.ComboStageInfo)
                return object;
            let message = new $root.jlail.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlail.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlail.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlail.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlail.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlail.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlail.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlail.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlail.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlail.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {jlail.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlail.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlail.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlail.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlail.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlail.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlail.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlail
         * @interface IRoundInfo
         * @property {Array.<jlail.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [EnterChest] RoundInfo EnterChest
         * @property {number|null} [MaxLength] RoundInfo MaxLength
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlail
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlail.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlail.IComboStageInfo>} ComboStageData
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo EnterChest.
         * @member {number} EnterChest
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.EnterChest = 0;

        /**
         * RoundInfo MaxLength.
         * @member {number} MaxLength
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxLength = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlail.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlail.RoundInfo
         * @static
         * @param {jlail.IRoundInfo=} [properties] Properties to set
         * @returns {jlail.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlail.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlail.RoundInfo
         * @static
         * @param {jlail.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlail.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.EnterChest != null && Object.hasOwnProperty.call(message, "EnterChest"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.EnterChest);
            if (message.MaxLength != null && Object.hasOwnProperty.call(message, "MaxLength"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.MaxLength);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.ScatterCount);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlail.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.RoundInfo
         * @static
         * @param {jlail.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlail.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.EnterChest = reader.double();
                        break;
                    }
                case 6: {
                        message.MaxLength = reader.int32();
                        break;
                    }
                case 7: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlail.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlail.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.EnterChest != null && message.hasOwnProperty("EnterChest"))
                if (typeof message.EnterChest !== "number")
                    return "EnterChest: number expected";
            if (message.MaxLength != null && message.hasOwnProperty("MaxLength"))
                if (!$util.isInteger(message.MaxLength))
                    return "MaxLength: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.RoundInfo)
                return object;
            let message = new $root.jlail.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlail.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlail.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlail.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.EnterChest != null)
                message.EnterChest = Number(object.EnterChest);
            if (object.MaxLength != null)
                message.MaxLength = object.MaxLength | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.RoundInfo
         * @static
         * @param {jlail.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.EnterChest = 0;
                object.MaxLength = 0;
                object.ScatterCount = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlail.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.EnterChest != null && message.hasOwnProperty("EnterChest"))
                object.EnterChest = options.json && !isFinite(message.EnterChest) ? String(message.EnterChest) : message.EnterChest;
            if (message.MaxLength != null && message.hasOwnProperty("MaxLength"))
                object.MaxLength = message.MaxLength;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlail.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlail.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.RoundInfo";
        };

        return RoundInfo;
    })();

    jlail.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlail
         * @interface ISpinAck
         * @property {Array.<jlail.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlail
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlail.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlail.IRoundInfo>} RoundQueue
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlail.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlail.SpinAck
         * @static
         * @param {jlail.ISpinAck=} [properties] Properties to set
         * @returns {jlail.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlail.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlail.SpinAck
         * @static
         * @param {jlail.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlail.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlail.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.SpinAck
         * @static
         * @param {jlail.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlail.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlail.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlail.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.SpinAck)
                return object;
            let message = new $root.jlail.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlail.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlail.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlail.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.SpinAck
         * @static
         * @param {jlail.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlail.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlail.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlail.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.SpinAck";
        };

        return SpinAck;
    })();

    jlail.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlail
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlail
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlail.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlail.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlail.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlail.GameInfoData
         * @static
         * @param {jlail.IGameInfoData=} [properties] Properties to set
         * @returns {jlail.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlail.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlail.GameInfoData
         * @static
         * @param {jlail.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlail.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlail.GameInfoData
         * @static
         * @param {jlail.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlail.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlail.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlail.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlail.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlail.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlail.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlail.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlail.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlail.GameInfoData)
                return object;
            let message = new $root.jlail.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlail.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlail.GameInfoData
         * @static
         * @param {jlail.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlail.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlail.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlail.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlail;
})();

export const jlap = $root.jlap = (() => {

    /**
     * Namespace jlap.
     * @exports jlap
     * @namespace
     */
    const jlap = {};

    jlap.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlap
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlap
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlap.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlap.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlap.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlap.Grid
         * @static
         * @param {jlap.IGrid=} [properties] Properties to set
         * @returns {jlap.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlap.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlap.Grid
         * @static
         * @param {jlap.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlap.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.Grid
         * @static
         * @param {jlap.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlap.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.Grid)
                return object;
            let message = new $root.jlap.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.Grid
         * @static
         * @param {jlap.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlap.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlap.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.Grid";
        };

        return Grid;
    })();

    jlap.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlap
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         * @property {number|null} [Energy] MSymbol Energy
         * @property {boolean|null} [Framed] MSymbol Framed
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlap
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlap.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlap.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlap.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * MSymbol Energy.
         * @member {number} Energy
         * @memberof jlap.MSymbol
         * @instance
         */
        MSymbol.prototype.Energy = 0;

        /**
         * MSymbol Framed.
         * @member {boolean} Framed
         * @memberof jlap.MSymbol
         * @instance
         */
        MSymbol.prototype.Framed = false;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlap.MSymbol
         * @static
         * @param {jlap.IMSymbol=} [properties] Properties to set
         * @returns {jlap.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlap.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlap.MSymbol
         * @static
         * @param {jlap.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            if (message.Energy != null && Object.hasOwnProperty.call(message, "Energy"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Energy);
            if (message.Framed != null && Object.hasOwnProperty.call(message, "Framed"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Framed);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlap.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.MSymbol
         * @static
         * @param {jlap.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                case 3: {
                        message.Energy = reader.int32();
                        break;
                    }
                case 4: {
                        message.Framed = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlap.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            if (message.Energy != null && message.hasOwnProperty("Energy"))
                if (!$util.isInteger(message.Energy))
                    return "Energy: integer expected";
            if (message.Framed != null && message.hasOwnProperty("Framed"))
                if (typeof message.Framed !== "boolean")
                    return "Framed: boolean expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.MSymbol)
                return object;
            let message = new $root.jlap.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            if (object.Energy != null)
                message.Energy = object.Energy | 0;
            if (object.Framed != null)
                message.Framed = Boolean(object.Framed);
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.MSymbol
         * @static
         * @param {jlap.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
                object.Energy = 0;
                object.Framed = false;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            if (message.Energy != null && message.hasOwnProperty("Energy"))
                object.Energy = message.Energy;
            if (message.Framed != null && message.hasOwnProperty("Framed"))
                object.Framed = message.Framed;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlap.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlap.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.MSymbol";
        };

        return MSymbol;
    })();

    jlap.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlap
         * @interface IMColumn
         * @property {Array.<jlap.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlap
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlap.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlap.IMSymbol>} MColumn
         * @memberof jlap.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlap.MColumn
         * @static
         * @param {jlap.IMColumn=} [properties] Properties to set
         * @returns {jlap.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlap.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlap.MColumn
         * @static
         * @param {jlap.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlap.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlap.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.MColumn
         * @static
         * @param {jlap.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlap.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlap.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlap.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.MColumn)
                return object;
            let message = new $root.jlap.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlap.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlap.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlap.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.MColumn
         * @static
         * @param {jlap.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlap.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlap.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlap.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.MColumn";
        };

        return MColumn;
    })();

    jlap.MysColumn = (function() {

        /**
         * Properties of a MysColumn.
         * @memberof jlap
         * @interface IMysColumn
         * @property {number|null} [Column] MysColumn Column
         * @property {number|null} [Type] MysColumn Type
         */

        /**
         * Constructs a new MysColumn.
         * @memberof jlap
         * @classdesc Represents a MysColumn.
         * @implements IMysColumn
         * @constructor
         * @param {jlap.IMysColumn=} [properties] Properties to set
         */
        function MysColumn(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MysColumn Column.
         * @member {number} Column
         * @memberof jlap.MysColumn
         * @instance
         */
        MysColumn.prototype.Column = 0;

        /**
         * MysColumn Type.
         * @member {number} Type
         * @memberof jlap.MysColumn
         * @instance
         */
        MysColumn.prototype.Type = 0;

        /**
         * Creates a new MysColumn instance using the specified properties.
         * @function create
         * @memberof jlap.MysColumn
         * @static
         * @param {jlap.IMysColumn=} [properties] Properties to set
         * @returns {jlap.MysColumn} MysColumn instance
         */
        MysColumn.create = function create(properties) {
            return new MysColumn(properties);
        };

        /**
         * Encodes the specified MysColumn message. Does not implicitly {@link jlap.MysColumn.verify|verify} messages.
         * @function encode
         * @memberof jlap.MysColumn
         * @static
         * @param {jlap.IMysColumn} message MysColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MysColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && Object.hasOwnProperty.call(message, "Column"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Column);
            if (message.Type != null && Object.hasOwnProperty.call(message, "Type"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Type);
            return writer;
        };

        /**
         * Encodes the specified MysColumn message, length delimited. Does not implicitly {@link jlap.MysColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.MysColumn
         * @static
         * @param {jlap.IMysColumn} message MysColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MysColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MysColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.MysColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.MysColumn} MysColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MysColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.MysColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Column = reader.int32();
                        break;
                    }
                case 2: {
                        message.Type = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MysColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.MysColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.MysColumn} MysColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MysColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MysColumn message.
         * @function verify
         * @memberof jlap.MysColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MysColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column"))
                if (!$util.isInteger(message.Column))
                    return "Column: integer expected";
            if (message.Type != null && message.hasOwnProperty("Type"))
                if (!$util.isInteger(message.Type))
                    return "Type: integer expected";
            return null;
        };

        /**
         * Creates a MysColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.MysColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.MysColumn} MysColumn
         */
        MysColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.MysColumn)
                return object;
            let message = new $root.jlap.MysColumn();
            if (object.Column != null)
                message.Column = object.Column | 0;
            if (object.Type != null)
                message.Type = object.Type | 0;
            return message;
        };

        /**
         * Creates a plain object from a MysColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.MysColumn
         * @static
         * @param {jlap.MysColumn} message MysColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MysColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Column = 0;
                object.Type = 0;
            }
            if (message.Column != null && message.hasOwnProperty("Column"))
                object.Column = message.Column;
            if (message.Type != null && message.hasOwnProperty("Type"))
                object.Type = message.Type;
            return object;
        };

        /**
         * Converts this MysColumn to JSON.
         * @function toJSON
         * @memberof jlap.MysColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MysColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MysColumn
         * @function getTypeUrl
         * @memberof jlap.MysColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MysColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.MysColumn";
        };

        return MysColumn;
    })();

    jlap.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlap
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlap.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlap
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlap.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlap.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlap.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlap.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlap.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlap.IGrid>} GridVec
         * @memberof jlap.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlap.AwardData
         * @static
         * @param {jlap.IAwardData=} [properties] Properties to set
         * @returns {jlap.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlap.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlap.AwardData
         * @static
         * @param {jlap.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlap.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlap.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.AwardData
         * @static
         * @param {jlap.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlap.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlap.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlap.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.AwardData)
                return object;
            let message = new $root.jlap.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlap.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlap.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlap.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.AwardData
         * @static
         * @param {jlap.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlap.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlap.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlap.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.AwardData";
        };

        return AwardData;
    })();

    jlap.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlap
         * @interface IComboStageInfo
         * @property {Array.<jlap.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlap.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlap.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlap.IMColumn>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlap
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlap.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlap.IMColumn>} ComboStageSymbol
         * @memberof jlap.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlap.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlap.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlap.IAwardData>} AwardDataVec
         * @memberof jlap.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlap.IMColumn>} FillSymbols
         * @memberof jlap.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlap.IMColumn>} ChangeSymbols
         * @memberof jlap.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {jlap.IComboStageInfo=} [properties] Properties to set
         * @returns {jlap.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlap.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {jlap.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlap.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlap.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlap.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlap.MColumn.encode(message.ChangeSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlap.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {jlap.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlap.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlap.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlap.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlap.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlap.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlap.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlap.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlap.MColumn.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.ComboStageInfo)
                return object;
            let message = new $root.jlap.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlap.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlap.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlap.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlap.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlap.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlap.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlap.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlap.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlap.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlap.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlap.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlap.MColumn.fromObject(object.ChangeSymbols[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {jlap.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlap.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlap.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlap.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlap.MColumn.toObject(message.ChangeSymbols[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlap.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlap.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlap.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlap
         * @interface IRoundInfo
         * @property {Array.<jlap.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {Array.<jlap.IMysColumn>|null} [MysteryColumns] RoundInfo MysteryColumns
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlap
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlap.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            this.MysteryColumns = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlap.IComboStageInfo>} ComboStageData
         * @memberof jlap.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlap.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlap.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlap.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo MysteryColumns.
         * @member {Array.<jlap.IMysColumn>} MysteryColumns
         * @memberof jlap.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MysteryColumns = $util.emptyArray;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlap.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlap.RoundInfo
         * @static
         * @param {jlap.IRoundInfo=} [properties] Properties to set
         * @returns {jlap.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlap.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlap.RoundInfo
         * @static
         * @param {jlap.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlap.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.MysteryColumns != null && message.MysteryColumns.length)
                for (let i = 0; i < message.MysteryColumns.length; ++i)
                    $root.jlap.MysColumn.encode(message.MysteryColumns[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlap.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.RoundInfo
         * @static
         * @param {jlap.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlap.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.MysteryColumns && message.MysteryColumns.length))
                            message.MysteryColumns = [];
                        message.MysteryColumns.push($root.jlap.MysColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlap.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlap.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.MysteryColumns != null && message.hasOwnProperty("MysteryColumns")) {
                if (!Array.isArray(message.MysteryColumns))
                    return "MysteryColumns: array expected";
                for (let i = 0; i < message.MysteryColumns.length; ++i) {
                    let error = $root.jlap.MysColumn.verify(message.MysteryColumns[i]);
                    if (error)
                        return "MysteryColumns." + error;
                }
            }
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.RoundInfo)
                return object;
            let message = new $root.jlap.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlap.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlap.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlap.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.MysteryColumns) {
                if (!Array.isArray(object.MysteryColumns))
                    throw TypeError(".jlap.RoundInfo.MysteryColumns: array expected");
                message.MysteryColumns = [];
                for (let i = 0; i < object.MysteryColumns.length; ++i) {
                    if (typeof object.MysteryColumns[i] !== "object")
                        throw TypeError(".jlap.RoundInfo.MysteryColumns: object expected");
                    message.MysteryColumns[i] = $root.jlap.MysColumn.fromObject(object.MysteryColumns[i]);
                }
            }
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.RoundInfo
         * @static
         * @param {jlap.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageData = [];
                object.MysteryColumns = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.ScatterCount = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlap.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.MysteryColumns && message.MysteryColumns.length) {
                object.MysteryColumns = [];
                for (let j = 0; j < message.MysteryColumns.length; ++j)
                    object.MysteryColumns[j] = $root.jlap.MysColumn.toObject(message.MysteryColumns[j], options);
            }
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlap.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlap.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.RoundInfo";
        };

        return RoundInfo;
    })();

    jlap.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlap
         * @interface ISpinAck
         * @property {Array.<jlap.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlap
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlap.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlap.IRoundInfo>} RoundQueue
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlap.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlap.SpinAck
         * @static
         * @param {jlap.ISpinAck=} [properties] Properties to set
         * @returns {jlap.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlap.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlap.SpinAck
         * @static
         * @param {jlap.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlap.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.Bet);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlap.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.SpinAck
         * @static
         * @param {jlap.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlap.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.Bet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlap.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlap.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.SpinAck)
                return object;
            let message = new $root.jlap.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlap.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlap.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlap.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.SpinAck
         * @static
         * @param {jlap.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlap.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlap.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlap.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.SpinAck";
        };

        return SpinAck;
    })();

    jlap.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlap
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlap
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlap.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlap.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlap.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlap.GameInfoData
         * @static
         * @param {jlap.IGameInfoData=} [properties] Properties to set
         * @returns {jlap.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlap.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlap.GameInfoData
         * @static
         * @param {jlap.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlap.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlap.GameInfoData
         * @static
         * @param {jlap.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlap.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlap.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlap.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlap.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlap.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlap.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlap.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlap.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlap.GameInfoData)
                return object;
            let message = new $root.jlap.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlap.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlap.GameInfoData
         * @static
         * @param {jlap.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlap.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlap.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlap.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlap;
})();

export const jlasd = $root.jlasd = (() => {

    /**
     * Namespace jlasd.
     * @exports jlasd
     * @namespace
     */
    const jlasd = {};

    /**
     * Symbol enum.
     * @name jlasd.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} SymbolMax=10 SymbolMax value
     */
    jlasd.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "SymbolMax"] = 10;
        return values;
    })();

    jlasd.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlasd
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlasd
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlasd.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlasd.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlasd.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlasd.Block
         * @static
         * @param {jlasd.IBlock=} [properties] Properties to set
         * @returns {jlasd.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlasd.Block.verify|verify} messages.
         * @function encode
         * @memberof jlasd.Block
         * @static
         * @param {jlasd.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlasd.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.Block
         * @static
         * @param {jlasd.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlasd.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.Block)
                return object;
            let message = new $root.jlasd.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.Block
         * @static
         * @param {jlasd.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlasd.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlasd.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.Block";
        };

        return Block;
    })();

    jlasd.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlasd
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [row] Cell row
         * @property {number|null} [column] Cell column
         * @property {number|null} [isGold] Cell isGold
         */

        /**
         * Constructs a new Cell.
         * @memberof jlasd
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlasd.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlasd.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell row.
         * @member {number} row
         * @memberof jlasd.Cell
         * @instance
         */
        Cell.prototype.row = 0;

        /**
         * Cell column.
         * @member {number} column
         * @memberof jlasd.Cell
         * @instance
         */
        Cell.prototype.column = 0;

        /**
         * Cell isGold.
         * @member {number} isGold
         * @memberof jlasd.Cell
         * @instance
         */
        Cell.prototype.isGold = 0;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlasd.Cell
         * @static
         * @param {jlasd.ICell=} [properties] Properties to set
         * @returns {jlasd.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlasd.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlasd.Cell
         * @static
         * @param {jlasd.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.column);
            if (message.isGold != null && Object.hasOwnProperty.call(message, "isGold"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.isGold);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlasd.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.Cell
         * @static
         * @param {jlasd.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.row = reader.uint32();
                        break;
                    }
                case 3: {
                        message.column = reader.uint32();
                        break;
                    }
                case 4: {
                        message.isGold = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlasd.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                if (!$util.isInteger(message.isGold))
                    return "isGold: integer expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.Cell)
                return object;
            let message = new $root.jlasd.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            if (object.isGold != null)
                message.isGold = object.isGold >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.Cell
         * @static
         * @param {jlasd.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.row = 0;
                object.column = 0;
                object.isGold = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                object.isGold = message.isGold;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlasd.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlasd.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.Cell";
        };

        return Cell;
    })();

    jlasd.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlasd
         * @interface IAwardReel
         * @property {number|null} [symbol] AwardReel symbol
         * @property {Array.<jlasd.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlasd
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlasd.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel symbol.
         * @member {number} symbol
         * @memberof jlasd.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlasd.IBlock>} block
         * @memberof jlasd.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlasd.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlasd.AwardReel
         * @static
         * @param {jlasd.IAwardReel=} [properties] Properties to set
         * @returns {jlasd.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlasd.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlasd.AwardReel
         * @static
         * @param {jlasd.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlasd.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlasd.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.AwardReel
         * @static
         * @param {jlasd.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlasd.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlasd.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlasd.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.AwardReel)
                return object;
            let message = new $root.jlasd.AwardReel();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlasd.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlasd.AwardReel.block: object expected");
                    message.block[i] = $root.jlasd.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.AwardReel
         * @static
         * @param {jlasd.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.symbol = 0;
                object.win = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlasd.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlasd.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlasd.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.AwardReel";
        };

        return AwardReel;
    })();

    jlasd.Combo = (function() {

        /**
         * Properties of a Combo.
         * @memberof jlasd
         * @interface ICombo
         * @property {Array.<jlasd.ICell>|null} [change] Combo change
         * @property {Array.<jlasd.IAwardReel>|null} [award] Combo award
         * @property {number|null} [win] Combo win
         * @property {number|null} [comboBonus] Combo comboBonus
         * @property {Array.<jlasd.ICell>|null} [extraChange] Combo extraChange
         * @property {Array.<jlasd.IBlock>|null} [goldReplace] Combo goldReplace
         * @property {Array.<number>|null} [bigGoldCount] Combo bigGoldCount
         */

        /**
         * Constructs a new Combo.
         * @memberof jlasd
         * @classdesc Represents a Combo.
         * @implements ICombo
         * @constructor
         * @param {jlasd.ICombo=} [properties] Properties to set
         */
        function Combo(properties) {
            this.change = [];
            this.award = [];
            this.extraChange = [];
            this.goldReplace = [];
            this.bigGoldCount = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Combo change.
         * @member {Array.<jlasd.ICell>} change
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.change = $util.emptyArray;

        /**
         * Combo award.
         * @member {Array.<jlasd.IAwardReel>} award
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.award = $util.emptyArray;

        /**
         * Combo win.
         * @member {number} win
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.win = 0;

        /**
         * Combo comboBonus.
         * @member {number} comboBonus
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.comboBonus = 0;

        /**
         * Combo extraChange.
         * @member {Array.<jlasd.ICell>} extraChange
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.extraChange = $util.emptyArray;

        /**
         * Combo goldReplace.
         * @member {Array.<jlasd.IBlock>} goldReplace
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.goldReplace = $util.emptyArray;

        /**
         * Combo bigGoldCount.
         * @member {Array.<number>} bigGoldCount
         * @memberof jlasd.Combo
         * @instance
         */
        Combo.prototype.bigGoldCount = $util.emptyArray;

        /**
         * Creates a new Combo instance using the specified properties.
         * @function create
         * @memberof jlasd.Combo
         * @static
         * @param {jlasd.ICombo=} [properties] Properties to set
         * @returns {jlasd.Combo} Combo instance
         */
        Combo.create = function create(properties) {
            return new Combo(properties);
        };

        /**
         * Encodes the specified Combo message. Does not implicitly {@link jlasd.Combo.verify|verify} messages.
         * @function encode
         * @memberof jlasd.Combo
         * @static
         * @param {jlasd.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.change != null && message.change.length)
                for (let i = 0; i < message.change.length; ++i)
                    $root.jlasd.Cell.encode(message.change[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlasd.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.comboBonus != null && Object.hasOwnProperty.call(message, "comboBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.comboBonus);
            if (message.extraChange != null && message.extraChange.length)
                for (let i = 0; i < message.extraChange.length; ++i)
                    $root.jlasd.Cell.encode(message.extraChange[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.goldReplace != null && message.goldReplace.length)
                for (let i = 0; i < message.goldReplace.length; ++i)
                    $root.jlasd.Block.encode(message.goldReplace[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.bigGoldCount != null && message.bigGoldCount.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.bigGoldCount.length; ++i)
                    writer.int32(message.bigGoldCount[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Combo message, length delimited. Does not implicitly {@link jlasd.Combo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.Combo
         * @static
         * @param {jlasd.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Combo message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.Combo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.change && message.change.length))
                            message.change = [];
                        message.change.push($root.jlasd.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlasd.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.comboBonus = reader.uint32();
                        break;
                    }
                case 5: {
                        if (!(message.extraChange && message.extraChange.length))
                            message.extraChange = [];
                        message.extraChange.push($root.jlasd.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.goldReplace && message.goldReplace.length))
                            message.goldReplace = [];
                        message.goldReplace.push($root.jlasd.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.bigGoldCount && message.bigGoldCount.length))
                            message.bigGoldCount = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.bigGoldCount.push(reader.int32());
                        } else
                            message.bigGoldCount.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Combo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Combo message.
         * @function verify
         * @memberof jlasd.Combo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Combo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.change != null && message.hasOwnProperty("change")) {
                if (!Array.isArray(message.change))
                    return "change: array expected";
                for (let i = 0; i < message.change.length; ++i) {
                    let error = $root.jlasd.Cell.verify(message.change[i]);
                    if (error)
                        return "change." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlasd.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.comboBonus != null && message.hasOwnProperty("comboBonus"))
                if (!$util.isInteger(message.comboBonus))
                    return "comboBonus: integer expected";
            if (message.extraChange != null && message.hasOwnProperty("extraChange")) {
                if (!Array.isArray(message.extraChange))
                    return "extraChange: array expected";
                for (let i = 0; i < message.extraChange.length; ++i) {
                    let error = $root.jlasd.Cell.verify(message.extraChange[i]);
                    if (error)
                        return "extraChange." + error;
                }
            }
            if (message.goldReplace != null && message.hasOwnProperty("goldReplace")) {
                if (!Array.isArray(message.goldReplace))
                    return "goldReplace: array expected";
                for (let i = 0; i < message.goldReplace.length; ++i) {
                    let error = $root.jlasd.Block.verify(message.goldReplace[i]);
                    if (error)
                        return "goldReplace." + error;
                }
            }
            if (message.bigGoldCount != null && message.hasOwnProperty("bigGoldCount")) {
                if (!Array.isArray(message.bigGoldCount))
                    return "bigGoldCount: array expected";
                for (let i = 0; i < message.bigGoldCount.length; ++i)
                    if (!$util.isInteger(message.bigGoldCount[i]))
                        return "bigGoldCount: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Combo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.Combo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.Combo} Combo
         */
        Combo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.Combo)
                return object;
            let message = new $root.jlasd.Combo();
            if (object.change) {
                if (!Array.isArray(object.change))
                    throw TypeError(".jlasd.Combo.change: array expected");
                message.change = [];
                for (let i = 0; i < object.change.length; ++i) {
                    if (typeof object.change[i] !== "object")
                        throw TypeError(".jlasd.Combo.change: object expected");
                    message.change[i] = $root.jlasd.Cell.fromObject(object.change[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlasd.Combo.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlasd.Combo.award: object expected");
                    message.award[i] = $root.jlasd.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.comboBonus != null)
                message.comboBonus = object.comboBonus >>> 0;
            if (object.extraChange) {
                if (!Array.isArray(object.extraChange))
                    throw TypeError(".jlasd.Combo.extraChange: array expected");
                message.extraChange = [];
                for (let i = 0; i < object.extraChange.length; ++i) {
                    if (typeof object.extraChange[i] !== "object")
                        throw TypeError(".jlasd.Combo.extraChange: object expected");
                    message.extraChange[i] = $root.jlasd.Cell.fromObject(object.extraChange[i]);
                }
            }
            if (object.goldReplace) {
                if (!Array.isArray(object.goldReplace))
                    throw TypeError(".jlasd.Combo.goldReplace: array expected");
                message.goldReplace = [];
                for (let i = 0; i < object.goldReplace.length; ++i) {
                    if (typeof object.goldReplace[i] !== "object")
                        throw TypeError(".jlasd.Combo.goldReplace: object expected");
                    message.goldReplace[i] = $root.jlasd.Block.fromObject(object.goldReplace[i]);
                }
            }
            if (object.bigGoldCount) {
                if (!Array.isArray(object.bigGoldCount))
                    throw TypeError(".jlasd.Combo.bigGoldCount: array expected");
                message.bigGoldCount = [];
                for (let i = 0; i < object.bigGoldCount.length; ++i)
                    message.bigGoldCount[i] = object.bigGoldCount[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Combo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.Combo
         * @static
         * @param {jlasd.Combo} message Combo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Combo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change = [];
                object.award = [];
                object.extraChange = [];
                object.goldReplace = [];
                object.bigGoldCount = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.comboBonus = 0;
            }
            if (message.change && message.change.length) {
                object.change = [];
                for (let j = 0; j < message.change.length; ++j)
                    object.change[j] = $root.jlasd.Cell.toObject(message.change[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlasd.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.comboBonus != null && message.hasOwnProperty("comboBonus"))
                object.comboBonus = message.comboBonus;
            if (message.extraChange && message.extraChange.length) {
                object.extraChange = [];
                for (let j = 0; j < message.extraChange.length; ++j)
                    object.extraChange[j] = $root.jlasd.Cell.toObject(message.extraChange[j], options);
            }
            if (message.goldReplace && message.goldReplace.length) {
                object.goldReplace = [];
                for (let j = 0; j < message.goldReplace.length; ++j)
                    object.goldReplace[j] = $root.jlasd.Block.toObject(message.goldReplace[j], options);
            }
            if (message.bigGoldCount && message.bigGoldCount.length) {
                object.bigGoldCount = [];
                for (let j = 0; j < message.bigGoldCount.length; ++j)
                    object.bigGoldCount[j] = message.bigGoldCount[j];
            }
            return object;
        };

        /**
         * Converts this Combo to JSON.
         * @function toJSON
         * @memberof jlasd.Combo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Combo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Combo
         * @function getTypeUrl
         * @memberof jlasd.Combo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Combo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.Combo";
        };

        return Combo;
    })();

    jlasd.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlasd
         * @interface IPlate
         * @property {Array.<jlasd.IColumnData>|null} [column] Plate column
         * @property {Array.<jlasd.ICombo>|null} [combo] Plate combo
         * @property {number|null} [win] Plate win
         * @property {boolean|null} [isBouns] Plate isBouns
         * @property {number|null} [totalFreeSpin] Plate totalFreeSpin
         */

        /**
         * Constructs a new Plate.
         * @memberof jlasd
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlasd.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.combo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlasd.IColumnData>} column
         * @memberof jlasd.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate combo.
         * @member {Array.<jlasd.ICombo>} combo
         * @memberof jlasd.Plate
         * @instance
         */
        Plate.prototype.combo = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlasd.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate isBouns.
         * @member {boolean} isBouns
         * @memberof jlasd.Plate
         * @instance
         */
        Plate.prototype.isBouns = false;

        /**
         * Plate totalFreeSpin.
         * @member {number} totalFreeSpin
         * @memberof jlasd.Plate
         * @instance
         */
        Plate.prototype.totalFreeSpin = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlasd.Plate
         * @static
         * @param {jlasd.IPlate=} [properties] Properties to set
         * @returns {jlasd.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlasd.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlasd.Plate
         * @static
         * @param {jlasd.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlasd.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.combo != null && message.combo.length)
                for (let i = 0; i < message.combo.length; ++i)
                    $root.jlasd.Combo.encode(message.combo[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.isBouns != null && Object.hasOwnProperty.call(message, "isBouns"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isBouns);
            if (message.totalFreeSpin != null && Object.hasOwnProperty.call(message, "totalFreeSpin"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.totalFreeSpin);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlasd.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.Plate
         * @static
         * @param {jlasd.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlasd.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.combo && message.combo.length))
                            message.combo = [];
                        message.combo.push($root.jlasd.Combo.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.isBouns = reader.bool();
                        break;
                    }
                case 5: {
                        message.totalFreeSpin = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlasd.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlasd.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.combo != null && message.hasOwnProperty("combo")) {
                if (!Array.isArray(message.combo))
                    return "combo: array expected";
                for (let i = 0; i < message.combo.length; ++i) {
                    let error = $root.jlasd.Combo.verify(message.combo[i]);
                    if (error)
                        return "combo." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.isBouns != null && message.hasOwnProperty("isBouns"))
                if (typeof message.isBouns !== "boolean")
                    return "isBouns: boolean expected";
            if (message.totalFreeSpin != null && message.hasOwnProperty("totalFreeSpin"))
                if (!$util.isInteger(message.totalFreeSpin))
                    return "totalFreeSpin: integer expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.Plate)
                return object;
            let message = new $root.jlasd.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlasd.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlasd.Plate.column: object expected");
                    message.column[i] = $root.jlasd.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.combo) {
                if (!Array.isArray(object.combo))
                    throw TypeError(".jlasd.Plate.combo: array expected");
                message.combo = [];
                for (let i = 0; i < object.combo.length; ++i) {
                    if (typeof object.combo[i] !== "object")
                        throw TypeError(".jlasd.Plate.combo: object expected");
                    message.combo[i] = $root.jlasd.Combo.fromObject(object.combo[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.isBouns != null)
                message.isBouns = Boolean(object.isBouns);
            if (object.totalFreeSpin != null)
                message.totalFreeSpin = object.totalFreeSpin >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.Plate
         * @static
         * @param {jlasd.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.combo = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.isBouns = false;
                object.totalFreeSpin = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlasd.ColumnData.toObject(message.column[j], options);
            }
            if (message.combo && message.combo.length) {
                object.combo = [];
                for (let j = 0; j < message.combo.length; ++j)
                    object.combo[j] = $root.jlasd.Combo.toObject(message.combo[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.isBouns != null && message.hasOwnProperty("isBouns"))
                object.isBouns = message.isBouns;
            if (message.totalFreeSpin != null && message.hasOwnProperty("totalFreeSpin"))
                object.totalFreeSpin = message.totalFreeSpin;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlasd.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlasd.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.Plate";
        };

        return Plate;
    })();

    jlasd.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlasd
         * @interface IAllPlate
         * @property {Array.<jlasd.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlasd
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlasd.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlasd.IPlate>} plate
         * @memberof jlasd.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlasd.AllPlate
         * @static
         * @param {jlasd.IAllPlate=} [properties] Properties to set
         * @returns {jlasd.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlasd.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlasd.AllPlate
         * @static
         * @param {jlasd.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlasd.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlasd.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.AllPlate
         * @static
         * @param {jlasd.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlasd.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlasd.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlasd.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.AllPlate)
                return object;
            let message = new $root.jlasd.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlasd.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlasd.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlasd.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.AllPlate
         * @static
         * @param {jlasd.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlasd.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlasd.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlasd.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.AllPlate";
        };

        return AllPlate;
    })();

    jlasd.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlasd
         * @interface IColumnData
         * @property {Array.<number>|null} [row] ColumnData row
         * @property {Array.<number>|null} [isGold] ColumnData isGold
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlasd
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlasd.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            this.isGold = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<number>} row
         * @memberof jlasd.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * ColumnData isGold.
         * @member {Array.<number>} isGold
         * @memberof jlasd.ColumnData
         * @instance
         */
        ColumnData.prototype.isGold = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlasd.ColumnData
         * @static
         * @param {jlasd.IColumnData=} [properties] Properties to set
         * @returns {jlasd.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlasd.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlasd.ColumnData
         * @static
         * @param {jlasd.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            if (message.isGold != null && message.isGold.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.isGold.length; ++i)
                    writer.uint32(message.isGold[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlasd.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlasd.ColumnData
         * @static
         * @param {jlasd.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlasd.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlasd.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlasd.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.isGold && message.isGold.length))
                            message.isGold = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.isGold.push(reader.uint32());
                        } else
                            message.isGold.push(reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlasd.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlasd.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlasd.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            if (message.isGold != null && message.hasOwnProperty("isGold")) {
                if (!Array.isArray(message.isGold))
                    return "isGold: array expected";
                for (let i = 0; i < message.isGold.length; ++i)
                    if (!$util.isInteger(message.isGold[i]))
                        return "isGold: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlasd.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlasd.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlasd.ColumnData)
                return object;
            let message = new $root.jlasd.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlasd.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            if (object.isGold) {
                if (!Array.isArray(object.isGold))
                    throw TypeError(".jlasd.ColumnData.isGold: array expected");
                message.isGold = [];
                for (let i = 0; i < object.isGold.length; ++i)
                    message.isGold[i] = object.isGold[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlasd.ColumnData
         * @static
         * @param {jlasd.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.row = [];
                object.isGold = [];
            }
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            if (message.isGold && message.isGold.length) {
                object.isGold = [];
                for (let j = 0; j < message.isGold.length; ++j)
                    object.isGold[j] = message.isGold[j];
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlasd.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlasd.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlasd.ColumnData";
        };

        return ColumnData;
    })();

    return jlasd;
})();

export const jlbase = $root.jlbase = (() => {

    /**
     * Namespace jlbase.
     * @exports jlbase
     * @namespace
     */
    const jlbase = {};

    /**
     * StatusCode enum.
     * @name jlbase.StatusCode
     * @enum {number}
     * @property {number} STATUS_OK=0 STATUS_OK value
     * @property {number} STATUS_FAIL=1 STATUS_FAIL value
     */
    jlbase.StatusCode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "STATUS_OK"] = 0;
        values[valuesById[1] = "STATUS_FAIL"] = 1;
        return values;
    })();

    jlbase.DataPacket = (function() {

        /**
         * Properties of a DataPacket.
         * @memberof jlbase
         * @interface IDataPacket
         * @property {jlbase.StatusCode|null} [status] DataPacket status
         * @property {Uint8Array|null} [error] DataPacket error
         * @property {number|Long|null} [key] DataPacket key
         * @property {Uint8Array|null} [data] DataPacket data
         */

        /**
         * Constructs a new DataPacket.
         * @memberof jlbase
         * @classdesc Represents a DataPacket.
         * @implements IDataPacket
         * @constructor
         * @param {jlbase.IDataPacket=} [properties] Properties to set
         */
        function DataPacket(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DataPacket status.
         * @member {jlbase.StatusCode} status
         * @memberof jlbase.DataPacket
         * @instance
         */
        DataPacket.prototype.status = 0;

        /**
         * DataPacket error.
         * @member {Uint8Array} error
         * @memberof jlbase.DataPacket
         * @instance
         */
        DataPacket.prototype.error = $util.newBuffer([]);

        /**
         * DataPacket key.
         * @member {number|Long} key
         * @memberof jlbase.DataPacket
         * @instance
         */
        DataPacket.prototype.key = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * DataPacket data.
         * @member {Uint8Array} data
         * @memberof jlbase.DataPacket
         * @instance
         */
        DataPacket.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new DataPacket instance using the specified properties.
         * @function create
         * @memberof jlbase.DataPacket
         * @static
         * @param {jlbase.IDataPacket=} [properties] Properties to set
         * @returns {jlbase.DataPacket} DataPacket instance
         */
        DataPacket.create = function create(properties) {
            return new DataPacket(properties);
        };

        /**
         * Encodes the specified DataPacket message. Does not implicitly {@link jlbase.DataPacket.verify|verify} messages.
         * @function encode
         * @memberof jlbase.DataPacket
         * @static
         * @param {jlbase.IDataPacket} message DataPacket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataPacket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.error);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.key);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.data);
            return writer;
        };

        /**
         * Encodes the specified DataPacket message, length delimited. Does not implicitly {@link jlbase.DataPacket.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbase.DataPacket
         * @static
         * @param {jlbase.IDataPacket} message DataPacket message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataPacket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DataPacket message from the specified reader or buffer.
         * @function decode
         * @memberof jlbase.DataPacket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbase.DataPacket} DataPacket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataPacket.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbase.DataPacket();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        message.error = reader.bytes();
                        break;
                    }
                case 3: {
                        message.key = reader.uint64();
                        break;
                    }
                case 4: {
                        message.data = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DataPacket message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbase.DataPacket
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbase.DataPacket} DataPacket
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataPacket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DataPacket message.
         * @function verify
         * @memberof jlbase.DataPacket
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DataPacket.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!(message.error && typeof message.error.length === "number" || $util.isString(message.error)))
                    return "error: buffer expected";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isInteger(message.key) && !(message.key && $util.isInteger(message.key.low) && $util.isInteger(message.key.high)))
                    return "key: integer|Long expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        /**
         * Creates a DataPacket message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbase.DataPacket
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbase.DataPacket} DataPacket
         */
        DataPacket.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbase.DataPacket)
                return object;
            let message = new $root.jlbase.DataPacket();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "STATUS_OK":
            case 0:
                message.status = 0;
                break;
            case "STATUS_FAIL":
            case 1:
                message.status = 1;
                break;
            }
            if (object.error != null)
                if (typeof object.error === "string")
                    $util.base64.decode(object.error, message.error = $util.newBuffer($util.base64.length(object.error)), 0);
                else if (object.error.length >= 0)
                    message.error = object.error;
            if (object.key != null)
                if ($util.Long)
                    (message.key = $util.Long.fromValue(object.key)).unsigned = true;
                else if (typeof object.key === "string")
                    message.key = parseInt(object.key, 10);
                else if (typeof object.key === "number")
                    message.key = object.key;
                else if (typeof object.key === "object")
                    message.key = new $util.LongBits(object.key.low >>> 0, object.key.high >>> 0).toNumber(true);
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a DataPacket message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbase.DataPacket
         * @static
         * @param {jlbase.DataPacket} message DataPacket
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DataPacket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.status = options.enums === String ? "STATUS_OK" : 0;
                if (options.bytes === String)
                    object.error = "";
                else {
                    object.error = [];
                    if (options.bytes !== Array)
                        object.error = $util.newBuffer(object.error);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.key = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.key = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.jlbase.StatusCode[message.status] === undefined ? message.status : $root.jlbase.StatusCode[message.status] : message.status;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = options.bytes === String ? $util.base64.encode(message.error, 0, message.error.length) : options.bytes === Array ? Array.prototype.slice.call(message.error) : message.error;
            if (message.key != null && message.hasOwnProperty("key"))
                if (typeof message.key === "number")
                    object.key = options.longs === String ? String(message.key) : message.key;
                else
                    object.key = options.longs === String ? $util.Long.prototype.toString.call(message.key) : options.longs === Number ? new $util.LongBits(message.key.low >>> 0, message.key.high >>> 0).toNumber(true) : message.key;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this DataPacket to JSON.
         * @function toJSON
         * @memberof jlbase.DataPacket
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DataPacket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DataPacket
         * @function getTypeUrl
         * @memberof jlbase.DataPacket
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DataPacket.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbase.DataPacket";
        };

        return DataPacket;
    })();

    jlbase.DataBatch = (function() {

        /**
         * Properties of a DataBatch.
         * @memberof jlbase
         * @interface IDataBatch
         * @property {jlbase.StatusCode|null} [status] DataBatch status
         * @property {Array.<jlbase.IDataPacket>|null} [packets] DataBatch packets
         */

        /**
         * Constructs a new DataBatch.
         * @memberof jlbase
         * @classdesc Represents a DataBatch.
         * @implements IDataBatch
         * @constructor
         * @param {jlbase.IDataBatch=} [properties] Properties to set
         */
        function DataBatch(properties) {
            this.packets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DataBatch status.
         * @member {jlbase.StatusCode} status
         * @memberof jlbase.DataBatch
         * @instance
         */
        DataBatch.prototype.status = 0;

        /**
         * DataBatch packets.
         * @member {Array.<jlbase.IDataPacket>} packets
         * @memberof jlbase.DataBatch
         * @instance
         */
        DataBatch.prototype.packets = $util.emptyArray;

        /**
         * Creates a new DataBatch instance using the specified properties.
         * @function create
         * @memberof jlbase.DataBatch
         * @static
         * @param {jlbase.IDataBatch=} [properties] Properties to set
         * @returns {jlbase.DataBatch} DataBatch instance
         */
        DataBatch.create = function create(properties) {
            return new DataBatch(properties);
        };

        /**
         * Encodes the specified DataBatch message. Does not implicitly {@link jlbase.DataBatch.verify|verify} messages.
         * @function encode
         * @memberof jlbase.DataBatch
         * @static
         * @param {jlbase.IDataBatch} message DataBatch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.packets != null && message.packets.length)
                for (let i = 0; i < message.packets.length; ++i)
                    $root.jlbase.DataPacket.encode(message.packets[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified DataBatch message, length delimited. Does not implicitly {@link jlbase.DataBatch.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbase.DataBatch
         * @static
         * @param {jlbase.IDataBatch} message DataBatch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DataBatch message from the specified reader or buffer.
         * @function decode
         * @memberof jlbase.DataBatch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbase.DataBatch} DataBatch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataBatch.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbase.DataBatch();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.packets && message.packets.length))
                            message.packets = [];
                        message.packets.push($root.jlbase.DataPacket.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DataBatch message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbase.DataBatch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbase.DataBatch} DataBatch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DataBatch message.
         * @function verify
         * @memberof jlbase.DataBatch
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DataBatch.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.packets != null && message.hasOwnProperty("packets")) {
                if (!Array.isArray(message.packets))
                    return "packets: array expected";
                for (let i = 0; i < message.packets.length; ++i) {
                    let error = $root.jlbase.DataPacket.verify(message.packets[i]);
                    if (error)
                        return "packets." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DataBatch message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbase.DataBatch
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbase.DataBatch} DataBatch
         */
        DataBatch.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbase.DataBatch)
                return object;
            let message = new $root.jlbase.DataBatch();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "STATUS_OK":
            case 0:
                message.status = 0;
                break;
            case "STATUS_FAIL":
            case 1:
                message.status = 1;
                break;
            }
            if (object.packets) {
                if (!Array.isArray(object.packets))
                    throw TypeError(".jlbase.DataBatch.packets: array expected");
                message.packets = [];
                for (let i = 0; i < object.packets.length; ++i) {
                    if (typeof object.packets[i] !== "object")
                        throw TypeError(".jlbase.DataBatch.packets: object expected");
                    message.packets[i] = $root.jlbase.DataPacket.fromObject(object.packets[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a DataBatch message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbase.DataBatch
         * @static
         * @param {jlbase.DataBatch} message DataBatch
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DataBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.packets = [];
            if (options.defaults)
                object.status = options.enums === String ? "STATUS_OK" : 0;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.jlbase.StatusCode[message.status] === undefined ? message.status : $root.jlbase.StatusCode[message.status] : message.status;
            if (message.packets && message.packets.length) {
                object.packets = [];
                for (let j = 0; j < message.packets.length; ++j)
                    object.packets[j] = $root.jlbase.DataPacket.toObject(message.packets[j], options);
            }
            return object;
        };

        /**
         * Converts this DataBatch to JSON.
         * @function toJSON
         * @memberof jlbase.DataBatch
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DataBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DataBatch
         * @function getTypeUrl
         * @memberof jlbase.DataBatch
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DataBatch.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbase.DataBatch";
        };

        return DataBatch;
    })();

    jlbase.DataBatchZip = (function() {

        /**
         * Properties of a DataBatchZip.
         * @memberof jlbase
         * @interface IDataBatchZip
         * @property {jlbase.StatusCode|null} [status] DataBatchZip status
         * @property {Object.<string,jlbase.IDataBatch>|null} [data] DataBatchZip data
         */

        /**
         * Constructs a new DataBatchZip.
         * @memberof jlbase
         * @classdesc Represents a DataBatchZip.
         * @implements IDataBatchZip
         * @constructor
         * @param {jlbase.IDataBatchZip=} [properties] Properties to set
         */
        function DataBatchZip(properties) {
            this.data = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DataBatchZip status.
         * @member {jlbase.StatusCode} status
         * @memberof jlbase.DataBatchZip
         * @instance
         */
        DataBatchZip.prototype.status = 0;

        /**
         * DataBatchZip data.
         * @member {Object.<string,jlbase.IDataBatch>} data
         * @memberof jlbase.DataBatchZip
         * @instance
         */
        DataBatchZip.prototype.data = $util.emptyObject;

        /**
         * Creates a new DataBatchZip instance using the specified properties.
         * @function create
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {jlbase.IDataBatchZip=} [properties] Properties to set
         * @returns {jlbase.DataBatchZip} DataBatchZip instance
         */
        DataBatchZip.create = function create(properties) {
            return new DataBatchZip(properties);
        };

        /**
         * Encodes the specified DataBatchZip message. Does not implicitly {@link jlbase.DataBatchZip.verify|verify} messages.
         * @function encode
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {jlbase.IDataBatchZip} message DataBatchZip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataBatchZip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                for (let keys = Object.keys(message.data), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.jlbase.DataBatch.encode(message.data[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified DataBatchZip message, length delimited. Does not implicitly {@link jlbase.DataBatchZip.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {jlbase.IDataBatchZip} message DataBatchZip message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataBatchZip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DataBatchZip message from the specified reader or buffer.
         * @function decode
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbase.DataBatchZip} DataBatchZip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataBatchZip.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbase.DataBatchZip(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        if (message.data === $util.emptyObject)
                            message.data = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = $root.jlbase.DataBatch.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.data[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DataBatchZip message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbase.DataBatchZip} DataBatchZip
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataBatchZip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DataBatchZip message.
         * @function verify
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DataBatchZip.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.data != null && message.hasOwnProperty("data")) {
                if (!$util.isObject(message.data))
                    return "data: object expected";
                let key = Object.keys(message.data);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.jlbase.DataBatch.verify(message.data[key[i]]);
                    if (error)
                        return "data." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DataBatchZip message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbase.DataBatchZip} DataBatchZip
         */
        DataBatchZip.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbase.DataBatchZip)
                return object;
            let message = new $root.jlbase.DataBatchZip();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "STATUS_OK":
            case 0:
                message.status = 0;
                break;
            case "STATUS_FAIL":
            case 1:
                message.status = 1;
                break;
            }
            if (object.data) {
                if (typeof object.data !== "object")
                    throw TypeError(".jlbase.DataBatchZip.data: object expected");
                message.data = {};
                for (let keys = Object.keys(object.data), i = 0; i < keys.length; ++i) {
                    if (typeof object.data[keys[i]] !== "object")
                        throw TypeError(".jlbase.DataBatchZip.data: object expected");
                    message.data[keys[i]] = $root.jlbase.DataBatch.fromObject(object.data[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a DataBatchZip message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {jlbase.DataBatchZip} message DataBatchZip
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DataBatchZip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.objects || options.defaults)
                object.data = {};
            if (options.defaults)
                object.status = options.enums === String ? "STATUS_OK" : 0;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.jlbase.StatusCode[message.status] === undefined ? message.status : $root.jlbase.StatusCode[message.status] : message.status;
            let keys2;
            if (message.data && (keys2 = Object.keys(message.data)).length) {
                object.data = {};
                for (let j = 0; j < keys2.length; ++j)
                    object.data[keys2[j]] = $root.jlbase.DataBatch.toObject(message.data[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this DataBatchZip to JSON.
         * @function toJSON
         * @memberof jlbase.DataBatchZip
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DataBatchZip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DataBatchZip
         * @function getTypeUrl
         * @memberof jlbase.DataBatchZip
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DataBatchZip.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbase.DataBatchZip";
        };

        return DataBatchZip;
    })();

    jlbase.UserInfo = (function() {

        /**
         * Properties of a UserInfo.
         * @memberof jlbase
         * @interface IUserInfo
         * @property {string|null} [gt] UserInfo gt
         * @property {string|null} [uid] UserInfo uid
         * @property {string|null} [token] UserInfo token
         */

        /**
         * Constructs a new UserInfo.
         * @memberof jlbase
         * @classdesc Represents a UserInfo.
         * @implements IUserInfo
         * @constructor
         * @param {jlbase.IUserInfo=} [properties] Properties to set
         */
        function UserInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserInfo gt.
         * @member {string} gt
         * @memberof jlbase.UserInfo
         * @instance
         */
        UserInfo.prototype.gt = "";

        /**
         * UserInfo uid.
         * @member {string} uid
         * @memberof jlbase.UserInfo
         * @instance
         */
        UserInfo.prototype.uid = "";

        /**
         * UserInfo token.
         * @member {string} token
         * @memberof jlbase.UserInfo
         * @instance
         */
        UserInfo.prototype.token = "";

        /**
         * Creates a new UserInfo instance using the specified properties.
         * @function create
         * @memberof jlbase.UserInfo
         * @static
         * @param {jlbase.IUserInfo=} [properties] Properties to set
         * @returns {jlbase.UserInfo} UserInfo instance
         */
        UserInfo.create = function create(properties) {
            return new UserInfo(properties);
        };

        /**
         * Encodes the specified UserInfo message. Does not implicitly {@link jlbase.UserInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbase.UserInfo
         * @static
         * @param {jlbase.IUserInfo} message UserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gt != null && Object.hasOwnProperty.call(message, "gt"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.gt);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.uid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
            return writer;
        };

        /**
         * Encodes the specified UserInfo message, length delimited. Does not implicitly {@link jlbase.UserInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbase.UserInfo
         * @static
         * @param {jlbase.IUserInfo} message UserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbase.UserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbase.UserInfo} UserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbase.UserInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.gt = reader.string();
                        break;
                    }
                case 2: {
                        message.uid = reader.string();
                        break;
                    }
                case 3: {
                        message.token = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbase.UserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbase.UserInfo} UserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserInfo message.
         * @function verify
         * @memberof jlbase.UserInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.gt != null && message.hasOwnProperty("gt"))
                if (!$util.isString(message.gt))
                    return "gt: string expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        /**
         * Creates a UserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbase.UserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbase.UserInfo} UserInfo
         */
        UserInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbase.UserInfo)
                return object;
            let message = new $root.jlbase.UserInfo();
            if (object.gt != null)
                message.gt = String(object.gt);
            if (object.uid != null)
                message.uid = String(object.uid);
            if (object.token != null)
                message.token = String(object.token);
            return message;
        };

        /**
         * Creates a plain object from a UserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbase.UserInfo
         * @static
         * @param {jlbase.UserInfo} message UserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.gt = "";
                object.uid = "";
                object.token = "";
            }
            if (message.gt != null && message.hasOwnProperty("gt"))
                object.gt = message.gt;
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        /**
         * Converts this UserInfo to JSON.
         * @function toJSON
         * @memberof jlbase.UserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserInfo
         * @function getTypeUrl
         * @memberof jlbase.UserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbase.UserInfo";
        };

        return UserInfo;
    })();

    jlbase.UserInfoList = (function() {

        /**
         * Properties of a UserInfoList.
         * @memberof jlbase
         * @interface IUserInfoList
         * @property {Array.<jlbase.IUserInfo>|null} [users] UserInfoList users
         */

        /**
         * Constructs a new UserInfoList.
         * @memberof jlbase
         * @classdesc Represents a UserInfoList.
         * @implements IUserInfoList
         * @constructor
         * @param {jlbase.IUserInfoList=} [properties] Properties to set
         */
        function UserInfoList(properties) {
            this.users = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserInfoList users.
         * @member {Array.<jlbase.IUserInfo>} users
         * @memberof jlbase.UserInfoList
         * @instance
         */
        UserInfoList.prototype.users = $util.emptyArray;

        /**
         * Creates a new UserInfoList instance using the specified properties.
         * @function create
         * @memberof jlbase.UserInfoList
         * @static
         * @param {jlbase.IUserInfoList=} [properties] Properties to set
         * @returns {jlbase.UserInfoList} UserInfoList instance
         */
        UserInfoList.create = function create(properties) {
            return new UserInfoList(properties);
        };

        /**
         * Encodes the specified UserInfoList message. Does not implicitly {@link jlbase.UserInfoList.verify|verify} messages.
         * @function encode
         * @memberof jlbase.UserInfoList
         * @static
         * @param {jlbase.IUserInfoList} message UserInfoList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserInfoList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.users != null && message.users.length)
                for (let i = 0; i < message.users.length; ++i)
                    $root.jlbase.UserInfo.encode(message.users[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified UserInfoList message, length delimited. Does not implicitly {@link jlbase.UserInfoList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbase.UserInfoList
         * @static
         * @param {jlbase.IUserInfoList} message UserInfoList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserInfoList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a UserInfoList message from the specified reader or buffer.
         * @function decode
         * @memberof jlbase.UserInfoList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbase.UserInfoList} UserInfoList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserInfoList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbase.UserInfoList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.users && message.users.length))
                            message.users = [];
                        message.users.push($root.jlbase.UserInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserInfoList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbase.UserInfoList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbase.UserInfoList} UserInfoList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserInfoList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserInfoList message.
         * @function verify
         * @memberof jlbase.UserInfoList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserInfoList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.users != null && message.hasOwnProperty("users")) {
                if (!Array.isArray(message.users))
                    return "users: array expected";
                for (let i = 0; i < message.users.length; ++i) {
                    let error = $root.jlbase.UserInfo.verify(message.users[i]);
                    if (error)
                        return "users." + error;
                }
            }
            return null;
        };

        /**
         * Creates a UserInfoList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbase.UserInfoList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbase.UserInfoList} UserInfoList
         */
        UserInfoList.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbase.UserInfoList)
                return object;
            let message = new $root.jlbase.UserInfoList();
            if (object.users) {
                if (!Array.isArray(object.users))
                    throw TypeError(".jlbase.UserInfoList.users: array expected");
                message.users = [];
                for (let i = 0; i < object.users.length; ++i) {
                    if (typeof object.users[i] !== "object")
                        throw TypeError(".jlbase.UserInfoList.users: object expected");
                    message.users[i] = $root.jlbase.UserInfo.fromObject(object.users[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a UserInfoList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbase.UserInfoList
         * @static
         * @param {jlbase.UserInfoList} message UserInfoList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserInfoList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.users = [];
            if (message.users && message.users.length) {
                object.users = [];
                for (let j = 0; j < message.users.length; ++j)
                    object.users[j] = $root.jlbase.UserInfo.toObject(message.users[j], options);
            }
            return object;
        };

        /**
         * Converts this UserInfoList to JSON.
         * @function toJSON
         * @memberof jlbase.UserInfoList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserInfoList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserInfoList
         * @function getTypeUrl
         * @memberof jlbase.UserInfoList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserInfoList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbase.UserInfoList";
        };

        return UserInfoList;
    })();

    return jlbase;
})();

export const jlbb = $root.jlbb = (() => {

    /**
     * Namespace jlbb.
     * @exports jlbb
     * @namespace
     */
    const jlbb = {};

    jlbb.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbb
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlbb
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbb.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlbb.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbb.Column
         * @static
         * @param {jlbb.IColumn=} [properties] Properties to set
         * @returns {jlbb.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbb.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbb.Column
         * @static
         * @param {jlbb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbb.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.Column
         * @static
         * @param {jlbb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbb.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.Column)
                return object;
            let message = new $root.jlbb.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlbb.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.Column
         * @static
         * @param {jlbb.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbb.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbb.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.Column";
        };

        return Column;
    })();

    jlbb.ColumnF = (function() {

        /**
         * Properties of a ColumnF.
         * @memberof jlbb
         * @interface IColumnF
         * @property {Array.<number>|null} [RowF] ColumnF RowF
         */

        /**
         * Constructs a new ColumnF.
         * @memberof jlbb
         * @classdesc Represents a ColumnF.
         * @implements IColumnF
         * @constructor
         * @param {jlbb.IColumnF=} [properties] Properties to set
         */
        function ColumnF(properties) {
            this.RowF = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnF RowF.
         * @member {Array.<number>} RowF
         * @memberof jlbb.ColumnF
         * @instance
         */
        ColumnF.prototype.RowF = $util.emptyArray;

        /**
         * Creates a new ColumnF instance using the specified properties.
         * @function create
         * @memberof jlbb.ColumnF
         * @static
         * @param {jlbb.IColumnF=} [properties] Properties to set
         * @returns {jlbb.ColumnF} ColumnF instance
         */
        ColumnF.create = function create(properties) {
            return new ColumnF(properties);
        };

        /**
         * Encodes the specified ColumnF message. Does not implicitly {@link jlbb.ColumnF.verify|verify} messages.
         * @function encode
         * @memberof jlbb.ColumnF
         * @static
         * @param {jlbb.IColumnF} message ColumnF message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnF.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RowF != null && message.RowF.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.RowF.length; ++i)
                    writer.double(message.RowF[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnF message, length delimited. Does not implicitly {@link jlbb.ColumnF.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.ColumnF
         * @static
         * @param {jlbb.IColumnF} message ColumnF message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnF.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnF message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.ColumnF
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.ColumnF} ColumnF
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnF.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.ColumnF();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RowF && message.RowF.length))
                            message.RowF = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RowF.push(reader.double());
                        } else
                            message.RowF.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnF message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.ColumnF
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.ColumnF} ColumnF
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnF.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnF message.
         * @function verify
         * @memberof jlbb.ColumnF
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnF.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RowF != null && message.hasOwnProperty("RowF")) {
                if (!Array.isArray(message.RowF))
                    return "RowF: array expected";
                for (let i = 0; i < message.RowF.length; ++i)
                    if (typeof message.RowF[i] !== "number")
                        return "RowF: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnF message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.ColumnF
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.ColumnF} ColumnF
         */
        ColumnF.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.ColumnF)
                return object;
            let message = new $root.jlbb.ColumnF();
            if (object.RowF) {
                if (!Array.isArray(object.RowF))
                    throw TypeError(".jlbb.ColumnF.RowF: array expected");
                message.RowF = [];
                for (let i = 0; i < object.RowF.length; ++i)
                    message.RowF[i] = Number(object.RowF[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnF message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.ColumnF
         * @static
         * @param {jlbb.ColumnF} message ColumnF
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnF.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RowF = [];
            if (message.RowF && message.RowF.length) {
                object.RowF = [];
                for (let j = 0; j < message.RowF.length; ++j)
                    object.RowF[j] = options.json && !isFinite(message.RowF[j]) ? String(message.RowF[j]) : message.RowF[j];
            }
            return object;
        };

        /**
         * Converts this ColumnF to JSON.
         * @function toJSON
         * @memberof jlbb.ColumnF
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnF.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnF
         * @function getTypeUrl
         * @memberof jlbb.ColumnF
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnF.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.ColumnF";
        };

        return ColumnF;
    })();

    jlbb.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbb
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbb
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbb.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbb.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbb.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlbb.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlbb.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbb.AwardData
         * @static
         * @param {jlbb.IAwardData=} [properties] Properties to set
         * @returns {jlbb.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbb.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbb.AwardData
         * @static
         * @param {jlbb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbb.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.AwardData
         * @static
         * @param {jlbb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbb.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.AwardData)
                return object;
            let message = new $root.jlbb.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.AwardData
         * @static
         * @param {jlbb.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbb.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbb.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.AwardData";
        };

        return AwardData;
    })();

    jlbb.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlbb
         * @interface ISinglePlate
         * @property {Array.<jlbb.IColumn>|null} [Symbol] SinglePlate Symbol
         * @property {Array.<jlbb.IColumnF>|null} [Bonus] SinglePlate Bonus
         * @property {Array.<jlbb.IAwardData>|null} [AwardDataVec] SinglePlate AwardDataVec
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         * @property {number|null} [BonusCount] SinglePlate BonusCount
         * @property {number|null} [BonusWin] SinglePlate BonusWin
         * @property {number|null} [BonusRemain] SinglePlate BonusRemain
         * @property {number|null} [BonusRow] SinglePlate BonusRow
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {number|null} [NowMoney] SinglePlate NowMoney
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlbb
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlbb.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.Symbol = [];
            this.Bonus = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate Symbol.
         * @member {Array.<jlbb.IColumn>} Symbol
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Symbol = $util.emptyArray;

        /**
         * SinglePlate Bonus.
         * @member {Array.<jlbb.IColumnF>} Bonus
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Bonus = $util.emptyArray;

        /**
         * SinglePlate AwardDataVec.
         * @member {Array.<jlbb.IAwardData>} AwardDataVec
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * SinglePlate BonusCount.
         * @member {number} BonusCount
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusCount = 0;

        /**
         * SinglePlate BonusWin.
         * @member {number} BonusWin
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusWin = 0;

        /**
         * SinglePlate BonusRemain.
         * @member {number} BonusRemain
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusRemain = 0;

        /**
         * SinglePlate BonusRow.
         * @member {number} BonusRow
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusRow = 0;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate NowMoney.
         * @member {number} NowMoney
         * @memberof jlbb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.NowMoney = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlbb.SinglePlate
         * @static
         * @param {jlbb.ISinglePlate=} [properties] Properties to set
         * @returns {jlbb.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlbb.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlbb.SinglePlate
         * @static
         * @param {jlbb.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlbb.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Bonus != null && message.Bonus.length)
                for (let i = 0; i < message.Bonus.length; ++i)
                    $root.jlbb.ColumnF.encode(message.Bonus[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlbb.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.SingleWin);
            if (message.BonusCount != null && Object.hasOwnProperty.call(message, "BonusCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.BonusCount);
            if (message.BonusWin != null && Object.hasOwnProperty.call(message, "BonusWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.BonusWin);
            if (message.BonusRemain != null && Object.hasOwnProperty.call(message, "BonusRemain"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.BonusRemain);
            if (message.BonusRow != null && Object.hasOwnProperty.call(message, "BonusRow"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.BonusRow);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AwardTypeFlag);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.NowMoney);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlbb.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.SinglePlate
         * @static
         * @param {jlbb.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlbb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Bonus && message.Bonus.length))
                            message.Bonus = [];
                        message.Bonus.push($root.jlbb.ColumnF.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlbb.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 5: {
                        message.BonusCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.BonusWin = reader.double();
                        break;
                    }
                case 7: {
                        message.BonusRemain = reader.int32();
                        break;
                    }
                case 8: {
                        message.BonusRow = reader.int32();
                        break;
                    }
                case 9: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 10: {
                        message.NowMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlbb.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlbb.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Bonus != null && message.hasOwnProperty("Bonus")) {
                if (!Array.isArray(message.Bonus))
                    return "Bonus: array expected";
                for (let i = 0; i < message.Bonus.length; ++i) {
                    let error = $root.jlbb.ColumnF.verify(message.Bonus[i]);
                    if (error)
                        return "Bonus." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlbb.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.BonusCount != null && message.hasOwnProperty("BonusCount"))
                if (!$util.isInteger(message.BonusCount))
                    return "BonusCount: integer expected";
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                if (typeof message.BonusWin !== "number")
                    return "BonusWin: number expected";
            if (message.BonusRemain != null && message.hasOwnProperty("BonusRemain"))
                if (!$util.isInteger(message.BonusRemain))
                    return "BonusRemain: integer expected";
            if (message.BonusRow != null && message.hasOwnProperty("BonusRow"))
                if (!$util.isInteger(message.BonusRow))
                    return "BonusRow: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.SinglePlate)
                return object;
            let message = new $root.jlbb.SinglePlate();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlbb.SinglePlate.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlbb.SinglePlate.Symbol: object expected");
                    message.Symbol[i] = $root.jlbb.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Bonus) {
                if (!Array.isArray(object.Bonus))
                    throw TypeError(".jlbb.SinglePlate.Bonus: array expected");
                message.Bonus = [];
                for (let i = 0; i < object.Bonus.length; ++i) {
                    if (typeof object.Bonus[i] !== "object")
                        throw TypeError(".jlbb.SinglePlate.Bonus: object expected");
                    message.Bonus[i] = $root.jlbb.ColumnF.fromObject(object.Bonus[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlbb.SinglePlate.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlbb.SinglePlate.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlbb.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.BonusCount != null)
                message.BonusCount = object.BonusCount | 0;
            if (object.BonusWin != null)
                message.BonusWin = Number(object.BonusWin);
            if (object.BonusRemain != null)
                message.BonusRemain = object.BonusRemain | 0;
            if (object.BonusRow != null)
                message.BonusRow = object.BonusRow | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.SinglePlate
         * @static
         * @param {jlbb.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Bonus = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.BonusCount = 0;
                object.BonusWin = 0;
                object.BonusRemain = 0;
                object.BonusRow = 0;
                object.AwardTypeFlag = 0;
                object.NowMoney = 0;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlbb.Column.toObject(message.Symbol[j], options);
            }
            if (message.Bonus && message.Bonus.length) {
                object.Bonus = [];
                for (let j = 0; j < message.Bonus.length; ++j)
                    object.Bonus[j] = $root.jlbb.ColumnF.toObject(message.Bonus[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlbb.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.BonusCount != null && message.hasOwnProperty("BonusCount"))
                object.BonusCount = message.BonusCount;
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                object.BonusWin = options.json && !isFinite(message.BonusWin) ? String(message.BonusWin) : message.BonusWin;
            if (message.BonusRemain != null && message.hasOwnProperty("BonusRemain"))
                object.BonusRemain = message.BonusRemain;
            if (message.BonusRow != null && message.hasOwnProperty("BonusRow"))
                object.BonusRow = message.BonusRow;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlbb.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlbb.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.SinglePlate";
        };

        return SinglePlate;
    })();

    jlbb.BonusData = (function() {

        /**
         * Properties of a BonusData.
         * @memberof jlbb
         * @interface IBonusData
         * @property {Array.<jlbb.ISinglePlate>|null} [BonusQueue] BonusData BonusQueue
         * @property {number|null} [BonusTotalWin] BonusData BonusTotalWin
         */

        /**
         * Constructs a new BonusData.
         * @memberof jlbb
         * @classdesc Represents a BonusData.
         * @implements IBonusData
         * @constructor
         * @param {jlbb.IBonusData=} [properties] Properties to set
         */
        function BonusData(properties) {
            this.BonusQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusData BonusQueue.
         * @member {Array.<jlbb.ISinglePlate>} BonusQueue
         * @memberof jlbb.BonusData
         * @instance
         */
        BonusData.prototype.BonusQueue = $util.emptyArray;

        /**
         * BonusData BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlbb.BonusData
         * @instance
         */
        BonusData.prototype.BonusTotalWin = 0;

        /**
         * Creates a new BonusData instance using the specified properties.
         * @function create
         * @memberof jlbb.BonusData
         * @static
         * @param {jlbb.IBonusData=} [properties] Properties to set
         * @returns {jlbb.BonusData} BonusData instance
         */
        BonusData.create = function create(properties) {
            return new BonusData(properties);
        };

        /**
         * Encodes the specified BonusData message. Does not implicitly {@link jlbb.BonusData.verify|verify} messages.
         * @function encode
         * @memberof jlbb.BonusData
         * @static
         * @param {jlbb.IBonusData} message BonusData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BonusQueue != null && message.BonusQueue.length)
                for (let i = 0; i < message.BonusQueue.length; ++i)
                    $root.jlbb.SinglePlate.encode(message.BonusQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusTotalWin);
            return writer;
        };

        /**
         * Encodes the specified BonusData message, length delimited. Does not implicitly {@link jlbb.BonusData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.BonusData
         * @static
         * @param {jlbb.IBonusData} message BonusData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.BonusData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.BonusData} BonusData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.BonusData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BonusQueue && message.BonusQueue.length))
                            message.BonusQueue = [];
                        message.BonusQueue.push($root.jlbb.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.BonusData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.BonusData} BonusData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusData message.
         * @function verify
         * @memberof jlbb.BonusData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BonusQueue != null && message.hasOwnProperty("BonusQueue")) {
                if (!Array.isArray(message.BonusQueue))
                    return "BonusQueue: array expected";
                for (let i = 0; i < message.BonusQueue.length; ++i) {
                    let error = $root.jlbb.SinglePlate.verify(message.BonusQueue[i]);
                    if (error)
                        return "BonusQueue." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            return null;
        };

        /**
         * Creates a BonusData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.BonusData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.BonusData} BonusData
         */
        BonusData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.BonusData)
                return object;
            let message = new $root.jlbb.BonusData();
            if (object.BonusQueue) {
                if (!Array.isArray(object.BonusQueue))
                    throw TypeError(".jlbb.BonusData.BonusQueue: array expected");
                message.BonusQueue = [];
                for (let i = 0; i < object.BonusQueue.length; ++i) {
                    if (typeof object.BonusQueue[i] !== "object")
                        throw TypeError(".jlbb.BonusData.BonusQueue: object expected");
                    message.BonusQueue[i] = $root.jlbb.SinglePlate.fromObject(object.BonusQueue[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a BonusData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.BonusData
         * @static
         * @param {jlbb.BonusData} message BonusData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BonusQueue = [];
            if (options.defaults)
                object.BonusTotalWin = 0;
            if (message.BonusQueue && message.BonusQueue.length) {
                object.BonusQueue = [];
                for (let j = 0; j < message.BonusQueue.length; ++j)
                    object.BonusQueue[j] = $root.jlbb.SinglePlate.toObject(message.BonusQueue[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            return object;
        };

        /**
         * Converts this BonusData to JSON.
         * @function toJSON
         * @memberof jlbb.BonusData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusData
         * @function getTypeUrl
         * @memberof jlbb.BonusData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.BonusData";
        };

        return BonusData;
    })();

    jlbb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbb
         * @interface ISpinAck
         * @property {jlbb.ISinglePlate|null} [MainGame] SpinAck MainGame
         * @property {jlbb.IBonusData|null} [BonusGame] SpinAck BonusGame
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainGame.
         * @member {jlbb.ISinglePlate|null|undefined} MainGame
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.MainGame = null;

        /**
         * SpinAck BonusGame.
         * @member {jlbb.IBonusData|null|undefined} BonusGame
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusGame = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlbb.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbb.SpinAck
         * @static
         * @param {jlbb.ISpinAck=} [properties] Properties to set
         * @returns {jlbb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbb.SpinAck
         * @static
         * @param {jlbb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainGame != null && Object.hasOwnProperty.call(message, "MainGame"))
                $root.jlbb.SinglePlate.encode(message.MainGame, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusGame != null && Object.hasOwnProperty.call(message, "BonusGame"))
                $root.jlbb.BonusData.encode(message.BonusGame, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.SpinAck
         * @static
         * @param {jlbb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainGame = $root.jlbb.SinglePlate.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.BonusGame = $root.jlbb.BonusData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainGame != null && message.hasOwnProperty("MainGame")) {
                let error = $root.jlbb.SinglePlate.verify(message.MainGame);
                if (error)
                    return "MainGame." + error;
            }
            if (message.BonusGame != null && message.hasOwnProperty("BonusGame")) {
                let error = $root.jlbb.BonusData.verify(message.BonusGame);
                if (error)
                    return "BonusGame." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.SpinAck)
                return object;
            let message = new $root.jlbb.SpinAck();
            if (object.MainGame != null) {
                if (typeof object.MainGame !== "object")
                    throw TypeError(".jlbb.SpinAck.MainGame: object expected");
                message.MainGame = $root.jlbb.SinglePlate.fromObject(object.MainGame);
            }
            if (object.BonusGame != null) {
                if (typeof object.BonusGame !== "object")
                    throw TypeError(".jlbb.SpinAck.BonusGame: object expected");
                message.BonusGame = $root.jlbb.BonusData.fromObject(object.BonusGame);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.SpinAck
         * @static
         * @param {jlbb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainGame = null;
                object.BonusGame = null;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.MainGame != null && message.hasOwnProperty("MainGame"))
                object.MainGame = $root.jlbb.SinglePlate.toObject(message.MainGame, options);
            if (message.BonusGame != null && message.hasOwnProperty("BonusGame"))
                object.BonusGame = $root.jlbb.BonusData.toObject(message.BonusGame, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.SpinAck";
        };

        return SpinAck;
    })();

    jlbb.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlbb
         * @interface IGameInfoAck
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlbb
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlbb.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {jlbb.IGameInfoAck=} [properties] Properties to set
         * @returns {jlbb.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlbb.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {jlbb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlbb.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {jlbb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbb.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbb.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbb.GameInfoAck)
                return object;
            return new $root.jlbb.GameInfoAck();
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {jlbb.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlbb.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlbb.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbb.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlbb;
})();

export const jlbbc = $root.jlbbc = (() => {

    /**
     * Namespace jlbbc.
     * @exports jlbbc
     * @namespace
     */
    const jlbbc = {};

    jlbbc.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbbc
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlbbc
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbbc.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlbbc.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbbc.Column
         * @static
         * @param {jlbbc.IColumn=} [properties] Properties to set
         * @returns {jlbbc.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbbc.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.Column
         * @static
         * @param {jlbbc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbbc.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.Column
         * @static
         * @param {jlbbc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbbc.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.Column)
                return object;
            let message = new $root.jlbbc.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlbbc.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.Column
         * @static
         * @param {jlbbc.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbbc.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbbc.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.Column";
        };

        return Column;
    })();

    jlbbc.Award = (function() {

        /**
         * Properties of an Award.
         * @memberof jlbbc
         * @interface IAward
         * @property {number|null} [AwardLink] Award AwardLink
         * @property {number|null} [AwardSymbol] Award AwardSymbol
         * @property {number|null} [LineIndex] Award LineIndex
         * @property {boolean|null} [HasWild] Award HasWild
         * @property {number|null} [Odds] Award Odds
         */

        /**
         * Constructs a new Award.
         * @memberof jlbbc
         * @classdesc Represents an Award.
         * @implements IAward
         * @constructor
         * @param {jlbbc.IAward=} [properties] Properties to set
         */
        function Award(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Award AwardLink.
         * @member {number} AwardLink
         * @memberof jlbbc.Award
         * @instance
         */
        Award.prototype.AwardLink = 0;

        /**
         * Award AwardSymbol.
         * @member {number} AwardSymbol
         * @memberof jlbbc.Award
         * @instance
         */
        Award.prototype.AwardSymbol = 0;

        /**
         * Award LineIndex.
         * @member {number} LineIndex
         * @memberof jlbbc.Award
         * @instance
         */
        Award.prototype.LineIndex = 0;

        /**
         * Award HasWild.
         * @member {boolean} HasWild
         * @memberof jlbbc.Award
         * @instance
         */
        Award.prototype.HasWild = false;

        /**
         * Award Odds.
         * @member {number} Odds
         * @memberof jlbbc.Award
         * @instance
         */
        Award.prototype.Odds = 0;

        /**
         * Creates a new Award instance using the specified properties.
         * @function create
         * @memberof jlbbc.Award
         * @static
         * @param {jlbbc.IAward=} [properties] Properties to set
         * @returns {jlbbc.Award} Award instance
         */
        Award.create = function create(properties) {
            return new Award(properties);
        };

        /**
         * Encodes the specified Award message. Does not implicitly {@link jlbbc.Award.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.Award
         * @static
         * @param {jlbbc.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardLink != null && Object.hasOwnProperty.call(message, "AwardLink"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardLink);
            if (message.AwardSymbol != null && Object.hasOwnProperty.call(message, "AwardSymbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardSymbol);
            if (message.LineIndex != null && Object.hasOwnProperty.call(message, "LineIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineIndex);
            if (message.HasWild != null && Object.hasOwnProperty.call(message, "HasWild"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.HasWild);
            if (message.Odds != null && Object.hasOwnProperty.call(message, "Odds"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.Odds);
            return writer;
        };

        /**
         * Encodes the specified Award message, length delimited. Does not implicitly {@link jlbbc.Award.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.Award
         * @static
         * @param {jlbbc.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Award message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.Award();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardLink = reader.int32();
                        break;
                    }
                case 2: {
                        message.AwardSymbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineIndex = reader.int32();
                        break;
                    }
                case 4: {
                        message.HasWild = reader.bool();
                        break;
                    }
                case 5: {
                        message.Odds = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Award message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Award message.
         * @function verify
         * @memberof jlbbc.Award
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Award.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardLink != null && message.hasOwnProperty("AwardLink"))
                if (!$util.isInteger(message.AwardLink))
                    return "AwardLink: integer expected";
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                if (!$util.isInteger(message.AwardSymbol))
                    return "AwardSymbol: integer expected";
            if (message.LineIndex != null && message.hasOwnProperty("LineIndex"))
                if (!$util.isInteger(message.LineIndex))
                    return "LineIndex: integer expected";
            if (message.HasWild != null && message.hasOwnProperty("HasWild"))
                if (typeof message.HasWild !== "boolean")
                    return "HasWild: boolean expected";
            if (message.Odds != null && message.hasOwnProperty("Odds"))
                if (!$util.isInteger(message.Odds))
                    return "Odds: integer expected";
            return null;
        };

        /**
         * Creates an Award message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.Award
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.Award} Award
         */
        Award.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.Award)
                return object;
            let message = new $root.jlbbc.Award();
            if (object.AwardLink != null)
                message.AwardLink = object.AwardLink | 0;
            if (object.AwardSymbol != null)
                message.AwardSymbol = object.AwardSymbol | 0;
            if (object.LineIndex != null)
                message.LineIndex = object.LineIndex | 0;
            if (object.HasWild != null)
                message.HasWild = Boolean(object.HasWild);
            if (object.Odds != null)
                message.Odds = object.Odds | 0;
            return message;
        };

        /**
         * Creates a plain object from an Award message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.Award
         * @static
         * @param {jlbbc.Award} message Award
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Award.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.AwardLink = 0;
                object.AwardSymbol = 0;
                object.LineIndex = 0;
                object.HasWild = false;
                object.Odds = 0;
            }
            if (message.AwardLink != null && message.hasOwnProperty("AwardLink"))
                object.AwardLink = message.AwardLink;
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                object.AwardSymbol = message.AwardSymbol;
            if (message.LineIndex != null && message.hasOwnProperty("LineIndex"))
                object.LineIndex = message.LineIndex;
            if (message.HasWild != null && message.hasOwnProperty("HasWild"))
                object.HasWild = message.HasWild;
            if (message.Odds != null && message.hasOwnProperty("Odds"))
                object.Odds = message.Odds;
            return object;
        };

        /**
         * Converts this Award to JSON.
         * @function toJSON
         * @memberof jlbbc.Award
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Award.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Award
         * @function getTypeUrl
         * @memberof jlbbc.Award
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Award.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.Award";
        };

        return Award;
    })();

    jlbbc.FreeGamePlateInfo = (function() {

        /**
         * Properties of a FreeGamePlateInfo.
         * @memberof jlbbc
         * @interface IFreeGamePlateInfo
         * @property {Array.<jlbbc.IColumn>|null} [PlateT] FreeGamePlateInfo PlateT
         * @property {Array.<number>|null} [EventPlate] FreeGamePlateInfo EventPlate
         * @property {Array.<jlbbc.IAward>|null} [AwardInfos] FreeGamePlateInfo AwardInfos
         * @property {number|null} [FullWildCount] FreeGamePlateInfo FullWildCount
         * @property {number|null} [AwardPrize] FreeGamePlateInfo AwardPrize
         * @property {string|null} [ShowIndex] FreeGamePlateInfo ShowIndex
         * @property {number|null} [NowMoney] FreeGamePlateInfo NowMoney
         * @property {number|Long|null} [LogIndex] FreeGamePlateInfo LogIndex
         * @property {number|null} [LineBet] FreeGamePlateInfo LineBet
         */

        /**
         * Constructs a new FreeGamePlateInfo.
         * @memberof jlbbc
         * @classdesc Represents a FreeGamePlateInfo.
         * @implements IFreeGamePlateInfo
         * @constructor
         * @param {jlbbc.IFreeGamePlateInfo=} [properties] Properties to set
         */
        function FreeGamePlateInfo(properties) {
            this.PlateT = [];
            this.EventPlate = [];
            this.AwardInfos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeGamePlateInfo PlateT.
         * @member {Array.<jlbbc.IColumn>} PlateT
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.PlateT = $util.emptyArray;

        /**
         * FreeGamePlateInfo EventPlate.
         * @member {Array.<number>} EventPlate
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.EventPlate = $util.emptyArray;

        /**
         * FreeGamePlateInfo AwardInfos.
         * @member {Array.<jlbbc.IAward>} AwardInfos
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.AwardInfos = $util.emptyArray;

        /**
         * FreeGamePlateInfo FullWildCount.
         * @member {number} FullWildCount
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.FullWildCount = 0;

        /**
         * FreeGamePlateInfo AwardPrize.
         * @member {number} AwardPrize
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.AwardPrize = 0;

        /**
         * FreeGamePlateInfo ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.ShowIndex = "";

        /**
         * FreeGamePlateInfo NowMoney.
         * @member {number} NowMoney
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.NowMoney = 0;

        /**
         * FreeGamePlateInfo LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * FreeGamePlateInfo LineBet.
         * @member {number} LineBet
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.LineBet = 0;

        /**
         * Creates a new FreeGamePlateInfo instance using the specified properties.
         * @function create
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {jlbbc.IFreeGamePlateInfo=} [properties] Properties to set
         * @returns {jlbbc.FreeGamePlateInfo} FreeGamePlateInfo instance
         */
        FreeGamePlateInfo.create = function create(properties) {
            return new FreeGamePlateInfo(properties);
        };

        /**
         * Encodes the specified FreeGamePlateInfo message. Does not implicitly {@link jlbbc.FreeGamePlateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {jlbbc.IFreeGamePlateInfo} message FreeGamePlateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGamePlateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateT != null && message.PlateT.length)
                for (let i = 0; i < message.PlateT.length; ++i)
                    $root.jlbbc.Column.encode(message.PlateT[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.EventPlate != null && message.EventPlate.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.EventPlate.length; ++i)
                    writer.int32(message.EventPlate[i]);
                writer.ldelim();
            }
            if (message.AwardInfos != null && message.AwardInfos.length)
                for (let i = 0; i < message.AwardInfos.length; ++i)
                    $root.jlbbc.Award.encode(message.AwardInfos[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FullWildCount != null && Object.hasOwnProperty.call(message, "FullWildCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FullWildCount);
            if (message.AwardPrize != null && Object.hasOwnProperty.call(message, "AwardPrize"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.AwardPrize);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.LogIndex);
            if (message.LineBet != null && Object.hasOwnProperty.call(message, "LineBet"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.LineBet);
            return writer;
        };

        /**
         * Encodes the specified FreeGamePlateInfo message, length delimited. Does not implicitly {@link jlbbc.FreeGamePlateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {jlbbc.IFreeGamePlateInfo} message FreeGamePlateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGamePlateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeGamePlateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.FreeGamePlateInfo} FreeGamePlateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGamePlateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.FreeGamePlateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateT && message.PlateT.length))
                            message.PlateT = [];
                        message.PlateT.push($root.jlbbc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.EventPlate && message.EventPlate.length))
                            message.EventPlate = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.EventPlate.push(reader.int32());
                        } else
                            message.EventPlate.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.AwardInfos && message.AwardInfos.length))
                            message.AwardInfos = [];
                        message.AwardInfos.push($root.jlbbc.Award.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.FullWildCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.AwardPrize = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 9: {
                        message.LineBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeGamePlateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.FreeGamePlateInfo} FreeGamePlateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGamePlateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeGamePlateInfo message.
         * @function verify
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeGamePlateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateT != null && message.hasOwnProperty("PlateT")) {
                if (!Array.isArray(message.PlateT))
                    return "PlateT: array expected";
                for (let i = 0; i < message.PlateT.length; ++i) {
                    let error = $root.jlbbc.Column.verify(message.PlateT[i]);
                    if (error)
                        return "PlateT." + error;
                }
            }
            if (message.EventPlate != null && message.hasOwnProperty("EventPlate")) {
                if (!Array.isArray(message.EventPlate))
                    return "EventPlate: array expected";
                for (let i = 0; i < message.EventPlate.length; ++i)
                    if (!$util.isInteger(message.EventPlate[i]))
                        return "EventPlate: integer[] expected";
            }
            if (message.AwardInfos != null && message.hasOwnProperty("AwardInfos")) {
                if (!Array.isArray(message.AwardInfos))
                    return "AwardInfos: array expected";
                for (let i = 0; i < message.AwardInfos.length; ++i) {
                    let error = $root.jlbbc.Award.verify(message.AwardInfos[i]);
                    if (error)
                        return "AwardInfos." + error;
                }
            }
            if (message.FullWildCount != null && message.hasOwnProperty("FullWildCount"))
                if (!$util.isInteger(message.FullWildCount))
                    return "FullWildCount: integer expected";
            if (message.AwardPrize != null && message.hasOwnProperty("AwardPrize"))
                if (typeof message.AwardPrize !== "number")
                    return "AwardPrize: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.LineBet != null && message.hasOwnProperty("LineBet"))
                if (typeof message.LineBet !== "number")
                    return "LineBet: number expected";
            return null;
        };

        /**
         * Creates a FreeGamePlateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.FreeGamePlateInfo} FreeGamePlateInfo
         */
        FreeGamePlateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.FreeGamePlateInfo)
                return object;
            let message = new $root.jlbbc.FreeGamePlateInfo();
            if (object.PlateT) {
                if (!Array.isArray(object.PlateT))
                    throw TypeError(".jlbbc.FreeGamePlateInfo.PlateT: array expected");
                message.PlateT = [];
                for (let i = 0; i < object.PlateT.length; ++i) {
                    if (typeof object.PlateT[i] !== "object")
                        throw TypeError(".jlbbc.FreeGamePlateInfo.PlateT: object expected");
                    message.PlateT[i] = $root.jlbbc.Column.fromObject(object.PlateT[i]);
                }
            }
            if (object.EventPlate) {
                if (!Array.isArray(object.EventPlate))
                    throw TypeError(".jlbbc.FreeGamePlateInfo.EventPlate: array expected");
                message.EventPlate = [];
                for (let i = 0; i < object.EventPlate.length; ++i)
                    message.EventPlate[i] = object.EventPlate[i] | 0;
            }
            if (object.AwardInfos) {
                if (!Array.isArray(object.AwardInfos))
                    throw TypeError(".jlbbc.FreeGamePlateInfo.AwardInfos: array expected");
                message.AwardInfos = [];
                for (let i = 0; i < object.AwardInfos.length; ++i) {
                    if (typeof object.AwardInfos[i] !== "object")
                        throw TypeError(".jlbbc.FreeGamePlateInfo.AwardInfos: object expected");
                    message.AwardInfos[i] = $root.jlbbc.Award.fromObject(object.AwardInfos[i]);
                }
            }
            if (object.FullWildCount != null)
                message.FullWildCount = object.FullWildCount | 0;
            if (object.AwardPrize != null)
                message.AwardPrize = Number(object.AwardPrize);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.LineBet != null)
                message.LineBet = Number(object.LineBet);
            return message;
        };

        /**
         * Creates a plain object from a FreeGamePlateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {jlbbc.FreeGamePlateInfo} message FreeGamePlateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeGamePlateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateT = [];
                object.EventPlate = [];
                object.AwardInfos = [];
            }
            if (options.defaults) {
                object.FullWildCount = 0;
                object.AwardPrize = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.LineBet = 0;
            }
            if (message.PlateT && message.PlateT.length) {
                object.PlateT = [];
                for (let j = 0; j < message.PlateT.length; ++j)
                    object.PlateT[j] = $root.jlbbc.Column.toObject(message.PlateT[j], options);
            }
            if (message.EventPlate && message.EventPlate.length) {
                object.EventPlate = [];
                for (let j = 0; j < message.EventPlate.length; ++j)
                    object.EventPlate[j] = message.EventPlate[j];
            }
            if (message.AwardInfos && message.AwardInfos.length) {
                object.AwardInfos = [];
                for (let j = 0; j < message.AwardInfos.length; ++j)
                    object.AwardInfos[j] = $root.jlbbc.Award.toObject(message.AwardInfos[j], options);
            }
            if (message.FullWildCount != null && message.hasOwnProperty("FullWildCount"))
                object.FullWildCount = message.FullWildCount;
            if (message.AwardPrize != null && message.hasOwnProperty("AwardPrize"))
                object.AwardPrize = options.json && !isFinite(message.AwardPrize) ? String(message.AwardPrize) : message.AwardPrize;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.LineBet != null && message.hasOwnProperty("LineBet"))
                object.LineBet = options.json && !isFinite(message.LineBet) ? String(message.LineBet) : message.LineBet;
            return object;
        };

        /**
         * Converts this FreeGamePlateInfo to JSON.
         * @function toJSON
         * @memberof jlbbc.FreeGamePlateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeGamePlateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeGamePlateInfo
         * @function getTypeUrl
         * @memberof jlbbc.FreeGamePlateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeGamePlateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.FreeGamePlateInfo";
        };

        return FreeGamePlateInfo;
    })();

    jlbbc.FreeGameData = (function() {

        /**
         * Properties of a FreeGameData.
         * @memberof jlbbc
         * @interface IFreeGameData
         * @property {Array.<jlbbc.IFreeGamePlateInfo>|null} [FreeGamePlateInfo] FreeGameData FreeGamePlateInfo
         * @property {number|null} [TotalPrize] FreeGameData TotalPrize
         * @property {number|null} [TotalFullWildCount] FreeGameData TotalFullWildCount
         * @property {number|null} [Times] FreeGameData Times
         * @property {number|null} [MultiX] FreeGameData MultiX
         */

        /**
         * Constructs a new FreeGameData.
         * @memberof jlbbc
         * @classdesc Represents a FreeGameData.
         * @implements IFreeGameData
         * @constructor
         * @param {jlbbc.IFreeGameData=} [properties] Properties to set
         */
        function FreeGameData(properties) {
            this.FreeGamePlateInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeGameData FreeGamePlateInfo.
         * @member {Array.<jlbbc.IFreeGamePlateInfo>} FreeGamePlateInfo
         * @memberof jlbbc.FreeGameData
         * @instance
         */
        FreeGameData.prototype.FreeGamePlateInfo = $util.emptyArray;

        /**
         * FreeGameData TotalPrize.
         * @member {number} TotalPrize
         * @memberof jlbbc.FreeGameData
         * @instance
         */
        FreeGameData.prototype.TotalPrize = 0;

        /**
         * FreeGameData TotalFullWildCount.
         * @member {number} TotalFullWildCount
         * @memberof jlbbc.FreeGameData
         * @instance
         */
        FreeGameData.prototype.TotalFullWildCount = 0;

        /**
         * FreeGameData Times.
         * @member {number} Times
         * @memberof jlbbc.FreeGameData
         * @instance
         */
        FreeGameData.prototype.Times = 0;

        /**
         * FreeGameData MultiX.
         * @member {number} MultiX
         * @memberof jlbbc.FreeGameData
         * @instance
         */
        FreeGameData.prototype.MultiX = 0;

        /**
         * Creates a new FreeGameData instance using the specified properties.
         * @function create
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {jlbbc.IFreeGameData=} [properties] Properties to set
         * @returns {jlbbc.FreeGameData} FreeGameData instance
         */
        FreeGameData.create = function create(properties) {
            return new FreeGameData(properties);
        };

        /**
         * Encodes the specified FreeGameData message. Does not implicitly {@link jlbbc.FreeGameData.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {jlbbc.IFreeGameData} message FreeGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeGamePlateInfo != null && message.FreeGamePlateInfo.length)
                for (let i = 0; i < message.FreeGamePlateInfo.length; ++i)
                    $root.jlbbc.FreeGamePlateInfo.encode(message.FreeGamePlateInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalPrize != null && Object.hasOwnProperty.call(message, "TotalPrize"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalPrize);
            if (message.TotalFullWildCount != null && Object.hasOwnProperty.call(message, "TotalFullWildCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.TotalFullWildCount);
            if (message.Times != null && Object.hasOwnProperty.call(message, "Times"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Times);
            if (message.MultiX != null && Object.hasOwnProperty.call(message, "MultiX"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.MultiX);
            return writer;
        };

        /**
         * Encodes the specified FreeGameData message, length delimited. Does not implicitly {@link jlbbc.FreeGameData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {jlbbc.IFreeGameData} message FreeGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeGameData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.FreeGameData} FreeGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.FreeGameData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeGamePlateInfo && message.FreeGamePlateInfo.length))
                            message.FreeGamePlateInfo = [];
                        message.FreeGamePlateInfo.push($root.jlbbc.FreeGamePlateInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalPrize = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalFullWildCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.Times = reader.int32();
                        break;
                    }
                case 5: {
                        message.MultiX = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeGameData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.FreeGameData} FreeGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeGameData message.
         * @function verify
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeGameData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeGamePlateInfo != null && message.hasOwnProperty("FreeGamePlateInfo")) {
                if (!Array.isArray(message.FreeGamePlateInfo))
                    return "FreeGamePlateInfo: array expected";
                for (let i = 0; i < message.FreeGamePlateInfo.length; ++i) {
                    let error = $root.jlbbc.FreeGamePlateInfo.verify(message.FreeGamePlateInfo[i]);
                    if (error)
                        return "FreeGamePlateInfo." + error;
                }
            }
            if (message.TotalPrize != null && message.hasOwnProperty("TotalPrize"))
                if (typeof message.TotalPrize !== "number")
                    return "TotalPrize: number expected";
            if (message.TotalFullWildCount != null && message.hasOwnProperty("TotalFullWildCount"))
                if (!$util.isInteger(message.TotalFullWildCount))
                    return "TotalFullWildCount: integer expected";
            if (message.Times != null && message.hasOwnProperty("Times"))
                if (!$util.isInteger(message.Times))
                    return "Times: integer expected";
            if (message.MultiX != null && message.hasOwnProperty("MultiX"))
                if (!$util.isInteger(message.MultiX))
                    return "MultiX: integer expected";
            return null;
        };

        /**
         * Creates a FreeGameData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.FreeGameData} FreeGameData
         */
        FreeGameData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.FreeGameData)
                return object;
            let message = new $root.jlbbc.FreeGameData();
            if (object.FreeGamePlateInfo) {
                if (!Array.isArray(object.FreeGamePlateInfo))
                    throw TypeError(".jlbbc.FreeGameData.FreeGamePlateInfo: array expected");
                message.FreeGamePlateInfo = [];
                for (let i = 0; i < object.FreeGamePlateInfo.length; ++i) {
                    if (typeof object.FreeGamePlateInfo[i] !== "object")
                        throw TypeError(".jlbbc.FreeGameData.FreeGamePlateInfo: object expected");
                    message.FreeGamePlateInfo[i] = $root.jlbbc.FreeGamePlateInfo.fromObject(object.FreeGamePlateInfo[i]);
                }
            }
            if (object.TotalPrize != null)
                message.TotalPrize = Number(object.TotalPrize);
            if (object.TotalFullWildCount != null)
                message.TotalFullWildCount = object.TotalFullWildCount | 0;
            if (object.Times != null)
                message.Times = object.Times | 0;
            if (object.MultiX != null)
                message.MultiX = object.MultiX | 0;
            return message;
        };

        /**
         * Creates a plain object from a FreeGameData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {jlbbc.FreeGameData} message FreeGameData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeGameData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeGamePlateInfo = [];
            if (options.defaults) {
                object.TotalPrize = 0;
                object.TotalFullWildCount = 0;
                object.Times = 0;
                object.MultiX = 0;
            }
            if (message.FreeGamePlateInfo && message.FreeGamePlateInfo.length) {
                object.FreeGamePlateInfo = [];
                for (let j = 0; j < message.FreeGamePlateInfo.length; ++j)
                    object.FreeGamePlateInfo[j] = $root.jlbbc.FreeGamePlateInfo.toObject(message.FreeGamePlateInfo[j], options);
            }
            if (message.TotalPrize != null && message.hasOwnProperty("TotalPrize"))
                object.TotalPrize = options.json && !isFinite(message.TotalPrize) ? String(message.TotalPrize) : message.TotalPrize;
            if (message.TotalFullWildCount != null && message.hasOwnProperty("TotalFullWildCount"))
                object.TotalFullWildCount = message.TotalFullWildCount;
            if (message.Times != null && message.hasOwnProperty("Times"))
                object.Times = message.Times;
            if (message.MultiX != null && message.hasOwnProperty("MultiX"))
                object.MultiX = message.MultiX;
            return object;
        };

        /**
         * Converts this FreeGameData to JSON.
         * @function toJSON
         * @memberof jlbbc.FreeGameData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeGameData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeGameData
         * @function getTypeUrl
         * @memberof jlbbc.FreeGameData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeGameData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.FreeGameData";
        };

        return FreeGameData;
    })();

    jlbbc.PlateInfoBao = (function() {

        /**
         * Properties of a PlateInfoBao.
         * @memberof jlbbc
         * @interface IPlateInfoBao
         * @property {Array.<jlbbc.IColumn>|null} [BaoPlateT] PlateInfoBao BaoPlateT
         * @property {Array.<number>|null} [BaoEventPlate] PlateInfoBao BaoEventPlate
         * @property {Array.<jlbbc.IAward>|null} [AwardInfos] PlateInfoBao AwardInfos
         * @property {number|null} [Scatters] PlateInfoBao Scatters
         * @property {number|null} [TotalWild] PlateInfoBao TotalWild
         * @property {number|null} [TotalOdds] PlateInfoBao TotalOdds
         * @property {number|null} [MainWin] PlateInfoBao MainWin
         * @property {number|null} [FGWin] PlateInfoBao FGWin
         * @property {jlbbc.IFreeGameData|null} [FreeGamePackage] PlateInfoBao FreeGamePackage
         * @property {number|null} [NowMoney] PlateInfoBao NowMoney
         * @property {number|null} [LineBet] PlateInfoBao LineBet
         */

        /**
         * Constructs a new PlateInfoBao.
         * @memberof jlbbc
         * @classdesc Represents a PlateInfoBao.
         * @implements IPlateInfoBao
         * @constructor
         * @param {jlbbc.IPlateInfoBao=} [properties] Properties to set
         */
        function PlateInfoBao(properties) {
            this.BaoPlateT = [];
            this.BaoEventPlate = [];
            this.AwardInfos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlateInfoBao BaoPlateT.
         * @member {Array.<jlbbc.IColumn>} BaoPlateT
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.BaoPlateT = $util.emptyArray;

        /**
         * PlateInfoBao BaoEventPlate.
         * @member {Array.<number>} BaoEventPlate
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.BaoEventPlate = $util.emptyArray;

        /**
         * PlateInfoBao AwardInfos.
         * @member {Array.<jlbbc.IAward>} AwardInfos
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.AwardInfos = $util.emptyArray;

        /**
         * PlateInfoBao Scatters.
         * @member {number} Scatters
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.Scatters = 0;

        /**
         * PlateInfoBao TotalWild.
         * @member {number} TotalWild
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.TotalWild = 0;

        /**
         * PlateInfoBao TotalOdds.
         * @member {number} TotalOdds
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.TotalOdds = 0;

        /**
         * PlateInfoBao MainWin.
         * @member {number} MainWin
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.MainWin = 0;

        /**
         * PlateInfoBao FGWin.
         * @member {number} FGWin
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.FGWin = 0;

        /**
         * PlateInfoBao FreeGamePackage.
         * @member {jlbbc.IFreeGameData|null|undefined} FreeGamePackage
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.FreeGamePackage = null;

        /**
         * PlateInfoBao NowMoney.
         * @member {number} NowMoney
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.NowMoney = 0;

        /**
         * PlateInfoBao LineBet.
         * @member {number} LineBet
         * @memberof jlbbc.PlateInfoBao
         * @instance
         */
        PlateInfoBao.prototype.LineBet = 0;

        /**
         * Creates a new PlateInfoBao instance using the specified properties.
         * @function create
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {jlbbc.IPlateInfoBao=} [properties] Properties to set
         * @returns {jlbbc.PlateInfoBao} PlateInfoBao instance
         */
        PlateInfoBao.create = function create(properties) {
            return new PlateInfoBao(properties);
        };

        /**
         * Encodes the specified PlateInfoBao message. Does not implicitly {@link jlbbc.PlateInfoBao.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {jlbbc.IPlateInfoBao} message PlateInfoBao message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateInfoBao.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaoPlateT != null && message.BaoPlateT.length)
                for (let i = 0; i < message.BaoPlateT.length; ++i)
                    $root.jlbbc.Column.encode(message.BaoPlateT[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BaoEventPlate != null && message.BaoEventPlate.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.BaoEventPlate.length; ++i)
                    writer.int32(message.BaoEventPlate[i]);
                writer.ldelim();
            }
            if (message.AwardInfos != null && message.AwardInfos.length)
                for (let i = 0; i < message.AwardInfos.length; ++i)
                    $root.jlbbc.Award.encode(message.AwardInfos[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.Scatters != null && Object.hasOwnProperty.call(message, "Scatters"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Scatters);
            if (message.TotalWild != null && Object.hasOwnProperty.call(message, "TotalWild"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.TotalWild);
            if (message.TotalOdds != null && Object.hasOwnProperty.call(message, "TotalOdds"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.TotalOdds);
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.MainWin);
            if (message.FGWin != null && Object.hasOwnProperty.call(message, "FGWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.FGWin);
            if (message.FreeGamePackage != null && Object.hasOwnProperty.call(message, "FreeGamePackage"))
                $root.jlbbc.FreeGameData.encode(message.FreeGamePackage, writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.NowMoney);
            if (message.LineBet != null && Object.hasOwnProperty.call(message, "LineBet"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.LineBet);
            return writer;
        };

        /**
         * Encodes the specified PlateInfoBao message, length delimited. Does not implicitly {@link jlbbc.PlateInfoBao.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {jlbbc.IPlateInfoBao} message PlateInfoBao message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateInfoBao.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlateInfoBao message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.PlateInfoBao} PlateInfoBao
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateInfoBao.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.PlateInfoBao();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BaoPlateT && message.BaoPlateT.length))
                            message.BaoPlateT = [];
                        message.BaoPlateT.push($root.jlbbc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.BaoEventPlate && message.BaoEventPlate.length))
                            message.BaoEventPlate = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BaoEventPlate.push(reader.int32());
                        } else
                            message.BaoEventPlate.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.AwardInfos && message.AwardInfos.length))
                            message.AwardInfos = [];
                        message.AwardInfos.push($root.jlbbc.Award.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.Scatters = reader.int32();
                        break;
                    }
                case 5: {
                        message.TotalWild = reader.int32();
                        break;
                    }
                case 6: {
                        message.TotalOdds = reader.int32();
                        break;
                    }
                case 7: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 8: {
                        message.FGWin = reader.double();
                        break;
                    }
                case 9: {
                        message.FreeGamePackage = $root.jlbbc.FreeGameData.decode(reader, reader.uint32());
                        break;
                    }
                case 10: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 11: {
                        message.LineBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlateInfoBao message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.PlateInfoBao} PlateInfoBao
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateInfoBao.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlateInfoBao message.
         * @function verify
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlateInfoBao.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaoPlateT != null && message.hasOwnProperty("BaoPlateT")) {
                if (!Array.isArray(message.BaoPlateT))
                    return "BaoPlateT: array expected";
                for (let i = 0; i < message.BaoPlateT.length; ++i) {
                    let error = $root.jlbbc.Column.verify(message.BaoPlateT[i]);
                    if (error)
                        return "BaoPlateT." + error;
                }
            }
            if (message.BaoEventPlate != null && message.hasOwnProperty("BaoEventPlate")) {
                if (!Array.isArray(message.BaoEventPlate))
                    return "BaoEventPlate: array expected";
                for (let i = 0; i < message.BaoEventPlate.length; ++i)
                    if (!$util.isInteger(message.BaoEventPlate[i]))
                        return "BaoEventPlate: integer[] expected";
            }
            if (message.AwardInfos != null && message.hasOwnProperty("AwardInfos")) {
                if (!Array.isArray(message.AwardInfos))
                    return "AwardInfos: array expected";
                for (let i = 0; i < message.AwardInfos.length; ++i) {
                    let error = $root.jlbbc.Award.verify(message.AwardInfos[i]);
                    if (error)
                        return "AwardInfos." + error;
                }
            }
            if (message.Scatters != null && message.hasOwnProperty("Scatters"))
                if (!$util.isInteger(message.Scatters))
                    return "Scatters: integer expected";
            if (message.TotalWild != null && message.hasOwnProperty("TotalWild"))
                if (!$util.isInteger(message.TotalWild))
                    return "TotalWild: integer expected";
            if (message.TotalOdds != null && message.hasOwnProperty("TotalOdds"))
                if (!$util.isInteger(message.TotalOdds))
                    return "TotalOdds: integer expected";
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.FGWin != null && message.hasOwnProperty("FGWin"))
                if (typeof message.FGWin !== "number")
                    return "FGWin: number expected";
            if (message.FreeGamePackage != null && message.hasOwnProperty("FreeGamePackage")) {
                let error = $root.jlbbc.FreeGameData.verify(message.FreeGamePackage);
                if (error)
                    return "FreeGamePackage." + error;
            }
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.LineBet != null && message.hasOwnProperty("LineBet"))
                if (typeof message.LineBet !== "number")
                    return "LineBet: number expected";
            return null;
        };

        /**
         * Creates a PlateInfoBao message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.PlateInfoBao} PlateInfoBao
         */
        PlateInfoBao.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.PlateInfoBao)
                return object;
            let message = new $root.jlbbc.PlateInfoBao();
            if (object.BaoPlateT) {
                if (!Array.isArray(object.BaoPlateT))
                    throw TypeError(".jlbbc.PlateInfoBao.BaoPlateT: array expected");
                message.BaoPlateT = [];
                for (let i = 0; i < object.BaoPlateT.length; ++i) {
                    if (typeof object.BaoPlateT[i] !== "object")
                        throw TypeError(".jlbbc.PlateInfoBao.BaoPlateT: object expected");
                    message.BaoPlateT[i] = $root.jlbbc.Column.fromObject(object.BaoPlateT[i]);
                }
            }
            if (object.BaoEventPlate) {
                if (!Array.isArray(object.BaoEventPlate))
                    throw TypeError(".jlbbc.PlateInfoBao.BaoEventPlate: array expected");
                message.BaoEventPlate = [];
                for (let i = 0; i < object.BaoEventPlate.length; ++i)
                    message.BaoEventPlate[i] = object.BaoEventPlate[i] | 0;
            }
            if (object.AwardInfos) {
                if (!Array.isArray(object.AwardInfos))
                    throw TypeError(".jlbbc.PlateInfoBao.AwardInfos: array expected");
                message.AwardInfos = [];
                for (let i = 0; i < object.AwardInfos.length; ++i) {
                    if (typeof object.AwardInfos[i] !== "object")
                        throw TypeError(".jlbbc.PlateInfoBao.AwardInfos: object expected");
                    message.AwardInfos[i] = $root.jlbbc.Award.fromObject(object.AwardInfos[i]);
                }
            }
            if (object.Scatters != null)
                message.Scatters = object.Scatters | 0;
            if (object.TotalWild != null)
                message.TotalWild = object.TotalWild | 0;
            if (object.TotalOdds != null)
                message.TotalOdds = object.TotalOdds | 0;
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.FGWin != null)
                message.FGWin = Number(object.FGWin);
            if (object.FreeGamePackage != null) {
                if (typeof object.FreeGamePackage !== "object")
                    throw TypeError(".jlbbc.PlateInfoBao.FreeGamePackage: object expected");
                message.FreeGamePackage = $root.jlbbc.FreeGameData.fromObject(object.FreeGamePackage);
            }
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.LineBet != null)
                message.LineBet = Number(object.LineBet);
            return message;
        };

        /**
         * Creates a plain object from a PlateInfoBao message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {jlbbc.PlateInfoBao} message PlateInfoBao
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlateInfoBao.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.BaoPlateT = [];
                object.BaoEventPlate = [];
                object.AwardInfos = [];
            }
            if (options.defaults) {
                object.Scatters = 0;
                object.TotalWild = 0;
                object.TotalOdds = 0;
                object.MainWin = 0;
                object.FGWin = 0;
                object.FreeGamePackage = null;
                object.NowMoney = 0;
                object.LineBet = 0;
            }
            if (message.BaoPlateT && message.BaoPlateT.length) {
                object.BaoPlateT = [];
                for (let j = 0; j < message.BaoPlateT.length; ++j)
                    object.BaoPlateT[j] = $root.jlbbc.Column.toObject(message.BaoPlateT[j], options);
            }
            if (message.BaoEventPlate && message.BaoEventPlate.length) {
                object.BaoEventPlate = [];
                for (let j = 0; j < message.BaoEventPlate.length; ++j)
                    object.BaoEventPlate[j] = message.BaoEventPlate[j];
            }
            if (message.AwardInfos && message.AwardInfos.length) {
                object.AwardInfos = [];
                for (let j = 0; j < message.AwardInfos.length; ++j)
                    object.AwardInfos[j] = $root.jlbbc.Award.toObject(message.AwardInfos[j], options);
            }
            if (message.Scatters != null && message.hasOwnProperty("Scatters"))
                object.Scatters = message.Scatters;
            if (message.TotalWild != null && message.hasOwnProperty("TotalWild"))
                object.TotalWild = message.TotalWild;
            if (message.TotalOdds != null && message.hasOwnProperty("TotalOdds"))
                object.TotalOdds = message.TotalOdds;
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.FGWin != null && message.hasOwnProperty("FGWin"))
                object.FGWin = options.json && !isFinite(message.FGWin) ? String(message.FGWin) : message.FGWin;
            if (message.FreeGamePackage != null && message.hasOwnProperty("FreeGamePackage"))
                object.FreeGamePackage = $root.jlbbc.FreeGameData.toObject(message.FreeGamePackage, options);
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.LineBet != null && message.hasOwnProperty("LineBet"))
                object.LineBet = options.json && !isFinite(message.LineBet) ? String(message.LineBet) : message.LineBet;
            return object;
        };

        /**
         * Converts this PlateInfoBao to JSON.
         * @function toJSON
         * @memberof jlbbc.PlateInfoBao
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlateInfoBao.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlateInfoBao
         * @function getTypeUrl
         * @memberof jlbbc.PlateInfoBao
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlateInfoBao.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.PlateInfoBao";
        };

        return PlateInfoBao;
    })();

    jlbbc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbbc
         * @interface ISpinAck
         * @property {jlbbc.IPlateInfoBao|null} [PlateInfo] SpinAck PlateInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AckType] SpinAck AckType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbbc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbbc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateInfo.
         * @member {jlbbc.IPlateInfoBao|null|undefined} PlateInfo
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateInfo = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlbbc.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbbc.SpinAck
         * @static
         * @param {jlbbc.ISpinAck=} [properties] Properties to set
         * @returns {jlbbc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbbc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.SpinAck
         * @static
         * @param {jlbbc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateInfo != null && Object.hasOwnProperty.call(message, "PlateInfo"))
                $root.jlbbc.PlateInfoBao.encode(message.PlateInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AckType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbbc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.SpinAck
         * @static
         * @param {jlbbc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlateInfo = $root.jlbbc.PlateInfoBao.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbbc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo")) {
                let error = $root.jlbbc.PlateInfoBao.verify(message.PlateInfo);
                if (error)
                    return "PlateInfo." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.SpinAck)
                return object;
            let message = new $root.jlbbc.SpinAck();
            if (object.PlateInfo != null) {
                if (typeof object.PlateInfo !== "object")
                    throw TypeError(".jlbbc.SpinAck.PlateInfo: object expected");
                message.PlateInfo = $root.jlbbc.PlateInfoBao.fromObject(object.PlateInfo);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.SpinAck
         * @static
         * @param {jlbbc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.PlateInfo = null;
                object.TotalWin = 0;
                object.AckType = 0;
                object.ShowIndex = "";
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo"))
                object.PlateInfo = $root.jlbbc.PlateInfoBao.toObject(message.PlateInfo, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbbc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbbc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.SpinAck";
        };

        return SpinAck;
    })();

    jlbbc.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlbbc
         * @interface IGameInfoAck
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlbbc
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlbbc.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {jlbbc.IGameInfoAck=} [properties] Properties to set
         * @returns {jlbbc.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlbbc.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {jlbbc.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlbbc.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {jlbbc.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbc.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbc.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbc.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbc.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbc.GameInfoAck)
                return object;
            return new $root.jlbbc.GameInfoAck();
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {jlbbc.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlbbc.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlbbc.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbc.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlbbc;
})();

export const jlbbsh = $root.jlbbsh = (() => {

    /**
     * Namespace jlbbsh.
     * @exports jlbbsh
     * @namespace
     */
    const jlbbsh = {};

    /**
     * Symbol enum.
     * @name jlbbsh.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     * @property {number} BonusWild=11 BonusWild value
     */
    jlbbsh.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        values[valuesById[11] = "BonusWild"] = 11;
        return values;
    })();

    jlbbsh.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlbbsh
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlbbsh
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlbbsh.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlbbsh.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlbbsh.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlbbsh.Block
         * @static
         * @param {jlbbsh.IBlock=} [properties] Properties to set
         * @returns {jlbbsh.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlbbsh.Block.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.Block
         * @static
         * @param {jlbbsh.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlbbsh.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.Block
         * @static
         * @param {jlbbsh.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlbbsh.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.Block)
                return object;
            let message = new $root.jlbbsh.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.Block
         * @static
         * @param {jlbbsh.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlbbsh.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlbbsh.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.Block";
        };

        return Block;
    })();

    jlbbsh.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlbbsh
         * @interface IAllPlate
         * @property {Array.<jlbbsh.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlbbsh
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlbbsh.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlbbsh.IPlate>} plate
         * @memberof jlbbsh.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {jlbbsh.IAllPlate=} [properties] Properties to set
         * @returns {jlbbsh.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlbbsh.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {jlbbsh.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlbbsh.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlbbsh.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {jlbbsh.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlbbsh.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlbbsh.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.AllPlate)
                return object;
            let message = new $root.jlbbsh.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlbbsh.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlbbsh.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlbbsh.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {jlbbsh.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlbbsh.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlbbsh.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlbbsh.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.AllPlate";
        };

        return AllPlate;
    })();

    jlbbsh.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlbbsh
         * @interface IAwardReel
         * @property {number|null} [line] AwardReel line
         * @property {Array.<jlbbsh.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlbbsh
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlbbsh.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel line.
         * @member {number} line
         * @memberof jlbbsh.AwardReel
         * @instance
         */
        AwardReel.prototype.line = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlbbsh.IBlock>} block
         * @memberof jlbbsh.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlbbsh.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {jlbbsh.IAwardReel=} [properties] Properties to set
         * @returns {jlbbsh.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlbbsh.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {jlbbsh.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.line);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlbbsh.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlbbsh.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {jlbbsh.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.line = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlbbsh.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.line != null && message.hasOwnProperty("line"))
                if (!$util.isInteger(message.line))
                    return "line: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlbbsh.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.AwardReel)
                return object;
            let message = new $root.jlbbsh.AwardReel();
            if (object.line != null)
                message.line = object.line >>> 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlbbsh.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlbbsh.AwardReel.block: object expected");
                    message.block[i] = $root.jlbbsh.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {jlbbsh.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.line = 0;
                object.win = 0;
            }
            if (message.line != null && message.hasOwnProperty("line"))
                object.line = message.line;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlbbsh.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlbbsh.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlbbsh.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.AwardReel";
        };

        return AwardReel;
    })();

    jlbbsh.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlbbsh
         * @interface IPlate
         * @property {Array.<jlbbsh.IColumnData>|null} [column] Plate column
         * @property {Array.<jlbbsh.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         * @property {number|null} [ballCount] Plate ballCount
         * @property {number|null} [bonusWin] Plate bonusWin
         * @property {jlbbsh.IBubbleInfo|null} [bubbleInfo] Plate bubbleInfo
         */

        /**
         * Constructs a new Plate.
         * @memberof jlbbsh
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlbbsh.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlbbsh.IColumnData>} column
         * @memberof jlbbsh.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jlbbsh.IAwardReel>} award
         * @memberof jlbbsh.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlbbsh.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate ballCount.
         * @member {number} ballCount
         * @memberof jlbbsh.Plate
         * @instance
         */
        Plate.prototype.ballCount = 0;

        /**
         * Plate bonusWin.
         * @member {number} bonusWin
         * @memberof jlbbsh.Plate
         * @instance
         */
        Plate.prototype.bonusWin = 0;

        /**
         * Plate bubbleInfo.
         * @member {jlbbsh.IBubbleInfo|null|undefined} bubbleInfo
         * @memberof jlbbsh.Plate
         * @instance
         */
        Plate.prototype.bubbleInfo = null;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlbbsh.Plate
         * @static
         * @param {jlbbsh.IPlate=} [properties] Properties to set
         * @returns {jlbbsh.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlbbsh.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.Plate
         * @static
         * @param {jlbbsh.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlbbsh.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlbbsh.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.ballCount != null && Object.hasOwnProperty.call(message, "ballCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.ballCount);
            if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
            if (message.bubbleInfo != null && Object.hasOwnProperty.call(message, "bubbleInfo"))
                $root.jlbbsh.BubbleInfo.encode(message.bubbleInfo, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlbbsh.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.Plate
         * @static
         * @param {jlbbsh.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlbbsh.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlbbsh.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.ballCount = reader.uint32();
                        break;
                    }
                case 5: {
                        message.bonusWin = reader.double();
                        break;
                    }
                case 6: {
                        message.bubbleInfo = $root.jlbbsh.BubbleInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlbbsh.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlbbsh.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlbbsh.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                if (!$util.isInteger(message.ballCount))
                    return "ballCount: integer expected";
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                if (typeof message.bonusWin !== "number")
                    return "bonusWin: number expected";
            if (message.bubbleInfo != null && message.hasOwnProperty("bubbleInfo")) {
                let error = $root.jlbbsh.BubbleInfo.verify(message.bubbleInfo);
                if (error)
                    return "bubbleInfo." + error;
            }
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.Plate)
                return object;
            let message = new $root.jlbbsh.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlbbsh.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlbbsh.Plate.column: object expected");
                    message.column[i] = $root.jlbbsh.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlbbsh.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlbbsh.Plate.award: object expected");
                    message.award[i] = $root.jlbbsh.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.ballCount != null)
                message.ballCount = object.ballCount >>> 0;
            if (object.bonusWin != null)
                message.bonusWin = Number(object.bonusWin);
            if (object.bubbleInfo != null) {
                if (typeof object.bubbleInfo !== "object")
                    throw TypeError(".jlbbsh.Plate.bubbleInfo: object expected");
                message.bubbleInfo = $root.jlbbsh.BubbleInfo.fromObject(object.bubbleInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.Plate
         * @static
         * @param {jlbbsh.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.ballCount = 0;
                object.bonusWin = 0;
                object.bubbleInfo = null;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlbbsh.ColumnData.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlbbsh.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                object.ballCount = message.ballCount;
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
            if (message.bubbleInfo != null && message.hasOwnProperty("bubbleInfo"))
                object.bubbleInfo = $root.jlbbsh.BubbleInfo.toObject(message.bubbleInfo, options);
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlbbsh.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlbbsh.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.Plate";
        };

        return Plate;
    })();

    jlbbsh.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlbbsh
         * @interface IColumnData
         * @property {Array.<jlbbsh.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlbbsh
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlbbsh.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jlbbsh.ICell>} row
         * @memberof jlbbsh.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {jlbbsh.IColumnData=} [properties] Properties to set
         * @returns {jlbbsh.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlbbsh.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {jlbbsh.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jlbbsh.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlbbsh.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {jlbbsh.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jlbbsh.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jlbbsh.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.ColumnData)
                return object;
            let message = new $root.jlbbsh.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlbbsh.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jlbbsh.ColumnData.row: object expected");
                    message.row[i] = $root.jlbbsh.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {jlbbsh.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jlbbsh.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlbbsh.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlbbsh.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.ColumnData";
        };

        return ColumnData;
    })();

    jlbbsh.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlbbsh
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [value] Cell value
         * @property {boolean|null} [isConnect] Cell isConnect
         */

        /**
         * Constructs a new Cell.
         * @memberof jlbbsh
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlbbsh.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlbbsh.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell value.
         * @member {number} value
         * @memberof jlbbsh.Cell
         * @instance
         */
        Cell.prototype.value = 0;

        /**
         * Cell isConnect.
         * @member {boolean} isConnect
         * @memberof jlbbsh.Cell
         * @instance
         */
        Cell.prototype.isConnect = false;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlbbsh.Cell
         * @static
         * @param {jlbbsh.ICell=} [properties] Properties to set
         * @returns {jlbbsh.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlbbsh.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.Cell
         * @static
         * @param {jlbbsh.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isConnect);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlbbsh.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.Cell
         * @static
         * @param {jlbbsh.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.value = reader.double();
                        break;
                    }
                case 3: {
                        message.isConnect = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlbbsh.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.Cell)
                return object;
            let message = new $root.jlbbsh.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.value != null)
                message.value = Number(object.value);
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.Cell
         * @static
         * @param {jlbbsh.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.value = 0;
                object.isConnect = false;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlbbsh.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlbbsh.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.Cell";
        };

        return Cell;
    })();

    jlbbsh.BubbleDetail = (function() {

        /**
         * Properties of a BubbleDetail.
         * @memberof jlbbsh
         * @interface IBubbleDetail
         * @property {number|null} [row] BubbleDetail row
         * @property {number|null} [column] BubbleDetail column
         * @property {number|null} [size] BubbleDetail size
         * @property {number|null} [value] BubbleDetail value
         * @property {boolean|null} [isConnect] BubbleDetail isConnect
         * @property {number|null} [bet] BubbleDetail bet
         * @property {number|null} [x] BubbleDetail x
         */

        /**
         * Constructs a new BubbleDetail.
         * @memberof jlbbsh
         * @classdesc Represents a BubbleDetail.
         * @implements IBubbleDetail
         * @constructor
         * @param {jlbbsh.IBubbleDetail=} [properties] Properties to set
         */
        function BubbleDetail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BubbleDetail row.
         * @member {number} row
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.row = 0;

        /**
         * BubbleDetail column.
         * @member {number} column
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.column = 0;

        /**
         * BubbleDetail size.
         * @member {number} size
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.size = 0;

        /**
         * BubbleDetail value.
         * @member {number} value
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.value = 0;

        /**
         * BubbleDetail isConnect.
         * @member {boolean} isConnect
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.isConnect = false;

        /**
         * BubbleDetail bet.
         * @member {number} bet
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.bet = 0;

        /**
         * BubbleDetail x.
         * @member {number} x
         * @memberof jlbbsh.BubbleDetail
         * @instance
         */
        BubbleDetail.prototype.x = 0;

        /**
         * Creates a new BubbleDetail instance using the specified properties.
         * @function create
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {jlbbsh.IBubbleDetail=} [properties] Properties to set
         * @returns {jlbbsh.BubbleDetail} BubbleDetail instance
         */
        BubbleDetail.create = function create(properties) {
            return new BubbleDetail(properties);
        };

        /**
         * Encodes the specified BubbleDetail message. Does not implicitly {@link jlbbsh.BubbleDetail.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {jlbbsh.IBubbleDetail} message BubbleDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BubbleDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            if (message.size != null && Object.hasOwnProperty.call(message, "size"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.size);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.value);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.isConnect);
            if (message.bet != null && Object.hasOwnProperty.call(message, "bet"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.bet);
            if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.x);
            return writer;
        };

        /**
         * Encodes the specified BubbleDetail message, length delimited. Does not implicitly {@link jlbbsh.BubbleDetail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {jlbbsh.IBubbleDetail} message BubbleDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BubbleDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BubbleDetail message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.BubbleDetail} BubbleDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BubbleDetail.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.BubbleDetail();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.int32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                case 3: {
                        message.size = reader.uint32();
                        break;
                    }
                case 4: {
                        message.value = reader.double();
                        break;
                    }
                case 5: {
                        message.isConnect = reader.bool();
                        break;
                    }
                case 6: {
                        message.bet = reader.double();
                        break;
                    }
                case 7: {
                        message.x = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BubbleDetail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.BubbleDetail} BubbleDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BubbleDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BubbleDetail message.
         * @function verify
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BubbleDetail.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            if (message.size != null && message.hasOwnProperty("size"))
                if (!$util.isInteger(message.size))
                    return "size: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            if (message.bet != null && message.hasOwnProperty("bet"))
                if (typeof message.bet !== "number")
                    return "bet: number expected";
            if (message.x != null && message.hasOwnProperty("x"))
                if (!$util.isInteger(message.x))
                    return "x: integer expected";
            return null;
        };

        /**
         * Creates a BubbleDetail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.BubbleDetail} BubbleDetail
         */
        BubbleDetail.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.BubbleDetail)
                return object;
            let message = new $root.jlbbsh.BubbleDetail();
            if (object.row != null)
                message.row = object.row | 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            if (object.size != null)
                message.size = object.size >>> 0;
            if (object.value != null)
                message.value = Number(object.value);
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            if (object.bet != null)
                message.bet = Number(object.bet);
            if (object.x != null)
                message.x = object.x >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a BubbleDetail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {jlbbsh.BubbleDetail} message BubbleDetail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BubbleDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
                object.size = 0;
                object.value = 0;
                object.isConnect = false;
                object.bet = 0;
                object.x = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            if (message.size != null && message.hasOwnProperty("size"))
                object.size = message.size;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            if (message.bet != null && message.hasOwnProperty("bet"))
                object.bet = options.json && !isFinite(message.bet) ? String(message.bet) : message.bet;
            if (message.x != null && message.hasOwnProperty("x"))
                object.x = message.x;
            return object;
        };

        /**
         * Converts this BubbleDetail to JSON.
         * @function toJSON
         * @memberof jlbbsh.BubbleDetail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BubbleDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BubbleDetail
         * @function getTypeUrl
         * @memberof jlbbsh.BubbleDetail
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BubbleDetail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.BubbleDetail";
        };

        return BubbleDetail;
    })();

    jlbbsh.BubbleInfo = (function() {

        /**
         * Properties of a BubbleInfo.
         * @memberof jlbbsh
         * @interface IBubbleInfo
         * @property {Array.<jlbbsh.IBubbleDetail>|null} [bubble] BubbleInfo bubble
         */

        /**
         * Constructs a new BubbleInfo.
         * @memberof jlbbsh
         * @classdesc Represents a BubbleInfo.
         * @implements IBubbleInfo
         * @constructor
         * @param {jlbbsh.IBubbleInfo=} [properties] Properties to set
         */
        function BubbleInfo(properties) {
            this.bubble = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BubbleInfo bubble.
         * @member {Array.<jlbbsh.IBubbleDetail>} bubble
         * @memberof jlbbsh.BubbleInfo
         * @instance
         */
        BubbleInfo.prototype.bubble = $util.emptyArray;

        /**
         * Creates a new BubbleInfo instance using the specified properties.
         * @function create
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {jlbbsh.IBubbleInfo=} [properties] Properties to set
         * @returns {jlbbsh.BubbleInfo} BubbleInfo instance
         */
        BubbleInfo.create = function create(properties) {
            return new BubbleInfo(properties);
        };

        /**
         * Encodes the specified BubbleInfo message. Does not implicitly {@link jlbbsh.BubbleInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {jlbbsh.IBubbleInfo} message BubbleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BubbleInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bubble != null && message.bubble.length)
                for (let i = 0; i < message.bubble.length; ++i)
                    $root.jlbbsh.BubbleDetail.encode(message.bubble[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BubbleInfo message, length delimited. Does not implicitly {@link jlbbsh.BubbleInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {jlbbsh.IBubbleInfo} message BubbleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BubbleInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BubbleInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbbsh.BubbleInfo} BubbleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BubbleInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbbsh.BubbleInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.bubble && message.bubble.length))
                            message.bubble = [];
                        message.bubble.push($root.jlbbsh.BubbleDetail.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BubbleInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbbsh.BubbleInfo} BubbleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BubbleInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BubbleInfo message.
         * @function verify
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BubbleInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bubble != null && message.hasOwnProperty("bubble")) {
                if (!Array.isArray(message.bubble))
                    return "bubble: array expected";
                for (let i = 0; i < message.bubble.length; ++i) {
                    let error = $root.jlbbsh.BubbleDetail.verify(message.bubble[i]);
                    if (error)
                        return "bubble." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BubbleInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbbsh.BubbleInfo} BubbleInfo
         */
        BubbleInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbbsh.BubbleInfo)
                return object;
            let message = new $root.jlbbsh.BubbleInfo();
            if (object.bubble) {
                if (!Array.isArray(object.bubble))
                    throw TypeError(".jlbbsh.BubbleInfo.bubble: array expected");
                message.bubble = [];
                for (let i = 0; i < object.bubble.length; ++i) {
                    if (typeof object.bubble[i] !== "object")
                        throw TypeError(".jlbbsh.BubbleInfo.bubble: object expected");
                    message.bubble[i] = $root.jlbbsh.BubbleDetail.fromObject(object.bubble[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BubbleInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {jlbbsh.BubbleInfo} message BubbleInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BubbleInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.bubble = [];
            if (message.bubble && message.bubble.length) {
                object.bubble = [];
                for (let j = 0; j < message.bubble.length; ++j)
                    object.bubble[j] = $root.jlbbsh.BubbleDetail.toObject(message.bubble[j], options);
            }
            return object;
        };

        /**
         * Converts this BubbleInfo to JSON.
         * @function toJSON
         * @memberof jlbbsh.BubbleInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BubbleInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BubbleInfo
         * @function getTypeUrl
         * @memberof jlbbsh.BubbleInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BubbleInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbbsh.BubbleInfo";
        };

        return BubbleInfo;
    })();

    return jlbbsh;
})();

export const jlbf = $root.jlbf = (() => {

    /**
     * Namespace jlbf.
     * @exports jlbf
     * @namespace
     */
    const jlbf = {};

    jlbf.ColumnB = (function() {

        /**
         * Properties of a ColumnB.
         * @memberof jlbf
         * @interface IColumnB
         * @property {Array.<boolean>|null} [RowB] ColumnB RowB
         */

        /**
         * Constructs a new ColumnB.
         * @memberof jlbf
         * @classdesc Represents a ColumnB.
         * @implements IColumnB
         * @constructor
         * @param {jlbf.IColumnB=} [properties] Properties to set
         */
        function ColumnB(properties) {
            this.RowB = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnB RowB.
         * @member {Array.<boolean>} RowB
         * @memberof jlbf.ColumnB
         * @instance
         */
        ColumnB.prototype.RowB = $util.emptyArray;

        /**
         * Creates a new ColumnB instance using the specified properties.
         * @function create
         * @memberof jlbf.ColumnB
         * @static
         * @param {jlbf.IColumnB=} [properties] Properties to set
         * @returns {jlbf.ColumnB} ColumnB instance
         */
        ColumnB.create = function create(properties) {
            return new ColumnB(properties);
        };

        /**
         * Encodes the specified ColumnB message. Does not implicitly {@link jlbf.ColumnB.verify|verify} messages.
         * @function encode
         * @memberof jlbf.ColumnB
         * @static
         * @param {jlbf.IColumnB} message ColumnB message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnB.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RowB != null && message.RowB.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.RowB.length; ++i)
                    writer.bool(message.RowB[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnB message, length delimited. Does not implicitly {@link jlbf.ColumnB.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf.ColumnB
         * @static
         * @param {jlbf.IColumnB} message ColumnB message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnB.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnB message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf.ColumnB
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf.ColumnB} ColumnB
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnB.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf.ColumnB();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RowB && message.RowB.length))
                            message.RowB = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RowB.push(reader.bool());
                        } else
                            message.RowB.push(reader.bool());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnB message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf.ColumnB
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf.ColumnB} ColumnB
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnB.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnB message.
         * @function verify
         * @memberof jlbf.ColumnB
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnB.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RowB != null && message.hasOwnProperty("RowB")) {
                if (!Array.isArray(message.RowB))
                    return "RowB: array expected";
                for (let i = 0; i < message.RowB.length; ++i)
                    if (typeof message.RowB[i] !== "boolean")
                        return "RowB: boolean[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnB message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf.ColumnB
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf.ColumnB} ColumnB
         */
        ColumnB.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf.ColumnB)
                return object;
            let message = new $root.jlbf.ColumnB();
            if (object.RowB) {
                if (!Array.isArray(object.RowB))
                    throw TypeError(".jlbf.ColumnB.RowB: array expected");
                message.RowB = [];
                for (let i = 0; i < object.RowB.length; ++i)
                    message.RowB[i] = Boolean(object.RowB[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnB message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf.ColumnB
         * @static
         * @param {jlbf.ColumnB} message ColumnB
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnB.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RowB = [];
            if (message.RowB && message.RowB.length) {
                object.RowB = [];
                for (let j = 0; j < message.RowB.length; ++j)
                    object.RowB[j] = message.RowB[j];
            }
            return object;
        };

        /**
         * Converts this ColumnB to JSON.
         * @function toJSON
         * @memberof jlbf.ColumnB
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnB.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnB
         * @function getTypeUrl
         * @memberof jlbf.ColumnB
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnB.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf.ColumnB";
        };

        return ColumnB;
    })();

    jlbf.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbf
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlbf
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbf.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlbf.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbf.Column
         * @static
         * @param {jlbf.IColumn=} [properties] Properties to set
         * @returns {jlbf.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbf.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbf.Column
         * @static
         * @param {jlbf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbf.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf.Column
         * @static
         * @param {jlbf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbf.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf.Column)
                return object;
            let message = new $root.jlbf.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlbf.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf.Column
         * @static
         * @param {jlbf.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbf.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbf.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf.Column";
        };

        return Column;
    })();

    jlbf.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbf
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbf
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbf.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbf.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbf.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlbf.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlbf.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbf.AwardData
         * @static
         * @param {jlbf.IAwardData=} [properties] Properties to set
         * @returns {jlbf.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbf.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbf.AwardData
         * @static
         * @param {jlbf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbf.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf.AwardData
         * @static
         * @param {jlbf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbf.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf.AwardData)
                return object;
            let message = new $root.jlbf.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf.AwardData
         * @static
         * @param {jlbf.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbf.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbf.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf.AwardData";
        };

        return AwardData;
    })();

    jlbf.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlbf
         * @interface IRoundInfo
         * @property {Array.<jlbf.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlbf.IColumnB>|null} [WildCover] RoundInfo WildCover
         * @property {Array.<number>|null} [WildExpandColumn] RoundInfo WildExpandColumn
         * @property {Array.<jlbf.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<jlbf.IColumn>|null} [PlateSymbolReal] RoundInfo PlateSymbolReal
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FGRound] RoundInfo FGRound
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlbf
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlbf.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.WildCover = [];
            this.WildExpandColumn = [];
            this.AwardDataVec = [];
            this.PlateSymbolReal = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlbf.IColumn>} PlateSymbol
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo WildCover.
         * @member {Array.<jlbf.IColumnB>} WildCover
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildCover = $util.emptyArray;

        /**
         * RoundInfo WildExpandColumn.
         * @member {Array.<number>} WildExpandColumn
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildExpandColumn = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlbf.IAwardData>} AwardDataVec
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo PlateSymbolReal.
         * @member {Array.<jlbf.IColumn>} PlateSymbolReal
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolReal = $util.emptyArray;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FGRound.
         * @member {number} FGRound
         * @memberof jlbf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGRound = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlbf.RoundInfo
         * @static
         * @param {jlbf.IRoundInfo=} [properties] Properties to set
         * @returns {jlbf.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlbf.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbf.RoundInfo
         * @static
         * @param {jlbf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlbf.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.WildCover != null && message.WildCover.length)
                for (let i = 0; i < message.WildCover.length; ++i)
                    $root.jlbf.ColumnB.encode(message.WildCover[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.WildExpandColumn != null && message.WildExpandColumn.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.WildExpandColumn.length; ++i)
                    writer.int32(message.WildExpandColumn[i]);
                writer.ldelim();
            }
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlbf.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RoundWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Mult);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            if (message.PlateSymbolReal != null && message.PlateSymbolReal.length)
                for (let i = 0; i < message.PlateSymbolReal.length; ++i)
                    $root.jlbf.Column.encode(message.PlateSymbolReal[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.FreeNowRound);
            if (message.FGRound != null && Object.hasOwnProperty.call(message, "FGRound"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.FGRound);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlbf.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf.RoundInfo
         * @static
         * @param {jlbf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlbf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.WildCover && message.WildCover.length))
                            message.WildCover = [];
                        message.WildCover.push($root.jlbf.ColumnB.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.WildExpandColumn && message.WildExpandColumn.length))
                            message.WildExpandColumn = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WildExpandColumn.push(reader.int32());
                        } else
                            message.WildExpandColumn.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlbf.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 8: {
                        message.Mult = reader.double();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 10: {
                        if (!(message.PlateSymbolReal && message.PlateSymbolReal.length))
                            message.PlateSymbolReal = [];
                        message.PlateSymbolReal.push($root.jlbf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.FGRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlbf.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlbf.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.WildCover != null && message.hasOwnProperty("WildCover")) {
                if (!Array.isArray(message.WildCover))
                    return "WildCover: array expected";
                for (let i = 0; i < message.WildCover.length; ++i) {
                    let error = $root.jlbf.ColumnB.verify(message.WildCover[i]);
                    if (error)
                        return "WildCover." + error;
                }
            }
            if (message.WildExpandColumn != null && message.hasOwnProperty("WildExpandColumn")) {
                if (!Array.isArray(message.WildExpandColumn))
                    return "WildExpandColumn: array expected";
                for (let i = 0; i < message.WildExpandColumn.length; ++i)
                    if (!$util.isInteger(message.WildExpandColumn[i]))
                        return "WildExpandColumn: integer[] expected";
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlbf.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.PlateSymbolReal != null && message.hasOwnProperty("PlateSymbolReal")) {
                if (!Array.isArray(message.PlateSymbolReal))
                    return "PlateSymbolReal: array expected";
                for (let i = 0; i < message.PlateSymbolReal.length; ++i) {
                    let error = $root.jlbf.Column.verify(message.PlateSymbolReal[i]);
                    if (error)
                        return "PlateSymbolReal." + error;
                }
            }
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FGRound != null && message.hasOwnProperty("FGRound"))
                if (!$util.isInteger(message.FGRound))
                    return "FGRound: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf.RoundInfo)
                return object;
            let message = new $root.jlbf.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlbf.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlbf.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlbf.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.WildCover) {
                if (!Array.isArray(object.WildCover))
                    throw TypeError(".jlbf.RoundInfo.WildCover: array expected");
                message.WildCover = [];
                for (let i = 0; i < object.WildCover.length; ++i) {
                    if (typeof object.WildCover[i] !== "object")
                        throw TypeError(".jlbf.RoundInfo.WildCover: object expected");
                    message.WildCover[i] = $root.jlbf.ColumnB.fromObject(object.WildCover[i]);
                }
            }
            if (object.WildExpandColumn) {
                if (!Array.isArray(object.WildExpandColumn))
                    throw TypeError(".jlbf.RoundInfo.WildExpandColumn: array expected");
                message.WildExpandColumn = [];
                for (let i = 0; i < object.WildExpandColumn.length; ++i)
                    message.WildExpandColumn[i] = object.WildExpandColumn[i] | 0;
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlbf.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlbf.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlbf.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.PlateSymbolReal) {
                if (!Array.isArray(object.PlateSymbolReal))
                    throw TypeError(".jlbf.RoundInfo.PlateSymbolReal: array expected");
                message.PlateSymbolReal = [];
                for (let i = 0; i < object.PlateSymbolReal.length; ++i) {
                    if (typeof object.PlateSymbolReal[i] !== "object")
                        throw TypeError(".jlbf.RoundInfo.PlateSymbolReal: object expected");
                    message.PlateSymbolReal[i] = $root.jlbf.Column.fromObject(object.PlateSymbolReal[i]);
                }
            }
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FGRound != null)
                message.FGRound = object.FGRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf.RoundInfo
         * @static
         * @param {jlbf.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.WildCover = [];
                object.WildExpandColumn = [];
                object.AwardDataVec = [];
                object.PlateSymbolReal = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.Mult = 0;
                object.MaxFlag = false;
                object.FreeNowRound = 0;
                object.FGRound = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlbf.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.WildCover && message.WildCover.length) {
                object.WildCover = [];
                for (let j = 0; j < message.WildCover.length; ++j)
                    object.WildCover[j] = $root.jlbf.ColumnB.toObject(message.WildCover[j], options);
            }
            if (message.WildExpandColumn && message.WildExpandColumn.length) {
                object.WildExpandColumn = [];
                for (let j = 0; j < message.WildExpandColumn.length; ++j)
                    object.WildExpandColumn[j] = message.WildExpandColumn[j];
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlbf.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.PlateSymbolReal && message.PlateSymbolReal.length) {
                object.PlateSymbolReal = [];
                for (let j = 0; j < message.PlateSymbolReal.length; ++j)
                    object.PlateSymbolReal[j] = $root.jlbf.Column.toObject(message.PlateSymbolReal[j], options);
            }
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FGRound != null && message.hasOwnProperty("FGRound"))
                object.FGRound = message.FGRound;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlbf.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlbf.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf.RoundInfo";
        };

        return RoundInfo;
    })();

    jlbf.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbf
         * @interface ISpinAck
         * @property {Array.<jlbf.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbf
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbf.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlbf.IRoundInfo>} RoundQueue
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbf.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbf.SpinAck
         * @static
         * @param {jlbf.ISpinAck=} [properties] Properties to set
         * @returns {jlbf.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbf.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbf.SpinAck
         * @static
         * @param {jlbf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlbf.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbf.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf.SpinAck
         * @static
         * @param {jlbf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlbf.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbf.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlbf.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf.SpinAck)
                return object;
            let message = new $root.jlbf.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlbf.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlbf.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlbf.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf.SpinAck
         * @static
         * @param {jlbf.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlbf.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbf.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbf.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf.SpinAck";
        };

        return SpinAck;
    })();

    jlbf.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlbf
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlbf
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlbf.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlbf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlbf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlbf.GameInfoData
         * @static
         * @param {jlbf.IGameInfoData=} [properties] Properties to set
         * @returns {jlbf.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlbf.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlbf.GameInfoData
         * @static
         * @param {jlbf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlbf.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf.GameInfoData
         * @static
         * @param {jlbf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlbf.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf.GameInfoData)
                return object;
            let message = new $root.jlbf.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlbf.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf.GameInfoData
         * @static
         * @param {jlbf.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlbf.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlbf.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlbf;
})();

export const jlbf3 = $root.jlbf3 = (() => {

    /**
     * Namespace jlbf3.
     * @exports jlbf3
     * @namespace
     */
    const jlbf3 = {};

    jlbf3.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbf3
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlbf3
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbf3.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlbf3.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbf3.Column
         * @static
         * @param {jlbf3.IColumn=} [properties] Properties to set
         * @returns {jlbf3.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbf3.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.Column
         * @static
         * @param {jlbf3.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbf3.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.Column
         * @static
         * @param {jlbf3.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbf3.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.Column)
                return object;
            let message = new $root.jlbf3.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlbf3.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.Column
         * @static
         * @param {jlbf3.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbf3.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbf3.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.Column";
        };

        return Column;
    })();

    jlbf3.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlbf3
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlbf3
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlbf3.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlbf3.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {jlbf3.IColumnFloat=} [properties] Properties to set
         * @returns {jlbf3.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlbf3.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {jlbf3.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlbf3.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {jlbf3.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.ColumnFloat)
                return object;
            let message = new $root.jlbf3.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlbf3.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {jlbf3.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlbf3.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlbf3.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlbf3.PlateSymbolData = (function() {

        /**
         * Properties of a PlateSymbolData.
         * @memberof jlbf3
         * @interface IPlateSymbolData
         * @property {Array.<jlbf3.IColumn>|null} [PSD] PlateSymbolData PSD
         */

        /**
         * Constructs a new PlateSymbolData.
         * @memberof jlbf3
         * @classdesc Represents a PlateSymbolData.
         * @implements IPlateSymbolData
         * @constructor
         * @param {jlbf3.IPlateSymbolData=} [properties] Properties to set
         */
        function PlateSymbolData(properties) {
            this.PSD = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlateSymbolData PSD.
         * @member {Array.<jlbf3.IColumn>} PSD
         * @memberof jlbf3.PlateSymbolData
         * @instance
         */
        PlateSymbolData.prototype.PSD = $util.emptyArray;

        /**
         * Creates a new PlateSymbolData instance using the specified properties.
         * @function create
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {jlbf3.IPlateSymbolData=} [properties] Properties to set
         * @returns {jlbf3.PlateSymbolData} PlateSymbolData instance
         */
        PlateSymbolData.create = function create(properties) {
            return new PlateSymbolData(properties);
        };

        /**
         * Encodes the specified PlateSymbolData message. Does not implicitly {@link jlbf3.PlateSymbolData.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {jlbf3.IPlateSymbolData} message PlateSymbolData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateSymbolData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PSD != null && message.PSD.length)
                for (let i = 0; i < message.PSD.length; ++i)
                    $root.jlbf3.Column.encode(message.PSD[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PlateSymbolData message, length delimited. Does not implicitly {@link jlbf3.PlateSymbolData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {jlbf3.IPlateSymbolData} message PlateSymbolData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateSymbolData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlateSymbolData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.PlateSymbolData} PlateSymbolData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateSymbolData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.PlateSymbolData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PSD && message.PSD.length))
                            message.PSD = [];
                        message.PSD.push($root.jlbf3.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlateSymbolData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.PlateSymbolData} PlateSymbolData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateSymbolData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlateSymbolData message.
         * @function verify
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlateSymbolData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PSD != null && message.hasOwnProperty("PSD")) {
                if (!Array.isArray(message.PSD))
                    return "PSD: array expected";
                for (let i = 0; i < message.PSD.length; ++i) {
                    let error = $root.jlbf3.Column.verify(message.PSD[i]);
                    if (error)
                        return "PSD." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PlateSymbolData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.PlateSymbolData} PlateSymbolData
         */
        PlateSymbolData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.PlateSymbolData)
                return object;
            let message = new $root.jlbf3.PlateSymbolData();
            if (object.PSD) {
                if (!Array.isArray(object.PSD))
                    throw TypeError(".jlbf3.PlateSymbolData.PSD: array expected");
                message.PSD = [];
                for (let i = 0; i < object.PSD.length; ++i) {
                    if (typeof object.PSD[i] !== "object")
                        throw TypeError(".jlbf3.PlateSymbolData.PSD: object expected");
                    message.PSD[i] = $root.jlbf3.Column.fromObject(object.PSD[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PlateSymbolData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {jlbf3.PlateSymbolData} message PlateSymbolData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlateSymbolData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PSD = [];
            if (message.PSD && message.PSD.length) {
                object.PSD = [];
                for (let j = 0; j < message.PSD.length; ++j)
                    object.PSD[j] = $root.jlbf3.Column.toObject(message.PSD[j], options);
            }
            return object;
        };

        /**
         * Converts this PlateSymbolData to JSON.
         * @function toJSON
         * @memberof jlbf3.PlateSymbolData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlateSymbolData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlateSymbolData
         * @function getTypeUrl
         * @memberof jlbf3.PlateSymbolData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlateSymbolData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.PlateSymbolData";
        };

        return PlateSymbolData;
    })();

    jlbf3.PlateNumData = (function() {

        /**
         * Properties of a PlateNumData.
         * @memberof jlbf3
         * @interface IPlateNumData
         * @property {Array.<jlbf3.IColumnFloat>|null} [PND] PlateNumData PND
         */

        /**
         * Constructs a new PlateNumData.
         * @memberof jlbf3
         * @classdesc Represents a PlateNumData.
         * @implements IPlateNumData
         * @constructor
         * @param {jlbf3.IPlateNumData=} [properties] Properties to set
         */
        function PlateNumData(properties) {
            this.PND = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlateNumData PND.
         * @member {Array.<jlbf3.IColumnFloat>} PND
         * @memberof jlbf3.PlateNumData
         * @instance
         */
        PlateNumData.prototype.PND = $util.emptyArray;

        /**
         * Creates a new PlateNumData instance using the specified properties.
         * @function create
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {jlbf3.IPlateNumData=} [properties] Properties to set
         * @returns {jlbf3.PlateNumData} PlateNumData instance
         */
        PlateNumData.create = function create(properties) {
            return new PlateNumData(properties);
        };

        /**
         * Encodes the specified PlateNumData message. Does not implicitly {@link jlbf3.PlateNumData.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {jlbf3.IPlateNumData} message PlateNumData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateNumData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PND != null && message.PND.length)
                for (let i = 0; i < message.PND.length; ++i)
                    $root.jlbf3.ColumnFloat.encode(message.PND[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PlateNumData message, length delimited. Does not implicitly {@link jlbf3.PlateNumData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {jlbf3.IPlateNumData} message PlateNumData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateNumData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlateNumData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.PlateNumData} PlateNumData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateNumData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.PlateNumData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PND && message.PND.length))
                            message.PND = [];
                        message.PND.push($root.jlbf3.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlateNumData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.PlateNumData} PlateNumData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateNumData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlateNumData message.
         * @function verify
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlateNumData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PND != null && message.hasOwnProperty("PND")) {
                if (!Array.isArray(message.PND))
                    return "PND: array expected";
                for (let i = 0; i < message.PND.length; ++i) {
                    let error = $root.jlbf3.ColumnFloat.verify(message.PND[i]);
                    if (error)
                        return "PND." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PlateNumData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.PlateNumData} PlateNumData
         */
        PlateNumData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.PlateNumData)
                return object;
            let message = new $root.jlbf3.PlateNumData();
            if (object.PND) {
                if (!Array.isArray(object.PND))
                    throw TypeError(".jlbf3.PlateNumData.PND: array expected");
                message.PND = [];
                for (let i = 0; i < object.PND.length; ++i) {
                    if (typeof object.PND[i] !== "object")
                        throw TypeError(".jlbf3.PlateNumData.PND: object expected");
                    message.PND[i] = $root.jlbf3.ColumnFloat.fromObject(object.PND[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PlateNumData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {jlbf3.PlateNumData} message PlateNumData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlateNumData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PND = [];
            if (message.PND && message.PND.length) {
                object.PND = [];
                for (let j = 0; j < message.PND.length; ++j)
                    object.PND[j] = $root.jlbf3.ColumnFloat.toObject(message.PND[j], options);
            }
            return object;
        };

        /**
         * Converts this PlateNumData to JSON.
         * @function toJSON
         * @memberof jlbf3.PlateNumData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlateNumData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlateNumData
         * @function getTypeUrl
         * @memberof jlbf3.PlateNumData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlateNumData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.PlateNumData";
        };

        return PlateNumData;
    })();

    jlbf3.AwardDataVecData = (function() {

        /**
         * Properties of an AwardDataVecData.
         * @memberof jlbf3
         * @interface IAwardDataVecData
         * @property {Array.<jlbf3.IAwardData>|null} [ADV] AwardDataVecData ADV
         */

        /**
         * Constructs a new AwardDataVecData.
         * @memberof jlbf3
         * @classdesc Represents an AwardDataVecData.
         * @implements IAwardDataVecData
         * @constructor
         * @param {jlbf3.IAwardDataVecData=} [properties] Properties to set
         */
        function AwardDataVecData(properties) {
            this.ADV = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardDataVecData ADV.
         * @member {Array.<jlbf3.IAwardData>} ADV
         * @memberof jlbf3.AwardDataVecData
         * @instance
         */
        AwardDataVecData.prototype.ADV = $util.emptyArray;

        /**
         * Creates a new AwardDataVecData instance using the specified properties.
         * @function create
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {jlbf3.IAwardDataVecData=} [properties] Properties to set
         * @returns {jlbf3.AwardDataVecData} AwardDataVecData instance
         */
        AwardDataVecData.create = function create(properties) {
            return new AwardDataVecData(properties);
        };

        /**
         * Encodes the specified AwardDataVecData message. Does not implicitly {@link jlbf3.AwardDataVecData.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {jlbf3.IAwardDataVecData} message AwardDataVecData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardDataVecData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ADV != null && message.ADV.length)
                for (let i = 0; i < message.ADV.length; ++i)
                    $root.jlbf3.AwardData.encode(message.ADV[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardDataVecData message, length delimited. Does not implicitly {@link jlbf3.AwardDataVecData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {jlbf3.IAwardDataVecData} message AwardDataVecData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardDataVecData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardDataVecData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.AwardDataVecData} AwardDataVecData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardDataVecData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.AwardDataVecData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ADV && message.ADV.length))
                            message.ADV = [];
                        message.ADV.push($root.jlbf3.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardDataVecData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.AwardDataVecData} AwardDataVecData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardDataVecData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardDataVecData message.
         * @function verify
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardDataVecData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ADV != null && message.hasOwnProperty("ADV")) {
                if (!Array.isArray(message.ADV))
                    return "ADV: array expected";
                for (let i = 0; i < message.ADV.length; ++i) {
                    let error = $root.jlbf3.AwardData.verify(message.ADV[i]);
                    if (error)
                        return "ADV." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardDataVecData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.AwardDataVecData} AwardDataVecData
         */
        AwardDataVecData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.AwardDataVecData)
                return object;
            let message = new $root.jlbf3.AwardDataVecData();
            if (object.ADV) {
                if (!Array.isArray(object.ADV))
                    throw TypeError(".jlbf3.AwardDataVecData.ADV: array expected");
                message.ADV = [];
                for (let i = 0; i < object.ADV.length; ++i) {
                    if (typeof object.ADV[i] !== "object")
                        throw TypeError(".jlbf3.AwardDataVecData.ADV: object expected");
                    message.ADV[i] = $root.jlbf3.AwardData.fromObject(object.ADV[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardDataVecData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {jlbf3.AwardDataVecData} message AwardDataVecData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardDataVecData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ADV = [];
            if (message.ADV && message.ADV.length) {
                object.ADV = [];
                for (let j = 0; j < message.ADV.length; ++j)
                    object.ADV[j] = $root.jlbf3.AwardData.toObject(message.ADV[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardDataVecData to JSON.
         * @function toJSON
         * @memberof jlbf3.AwardDataVecData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardDataVecData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardDataVecData
         * @function getTypeUrl
         * @memberof jlbf3.AwardDataVecData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardDataVecData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.AwardDataVecData";
        };

        return AwardDataVecData;
    })();

    jlbf3.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbf3
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Mult] AwardData Mult
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbf3
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbf3.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbf3.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbf3.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlbf3.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlbf3.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlbf3.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbf3.AwardData
         * @static
         * @param {jlbf3.IAwardData=} [properties] Properties to set
         * @returns {jlbf3.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbf3.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.AwardData
         * @static
         * @param {jlbf3.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbf3.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.AwardData
         * @static
         * @param {jlbf3.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbf3.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.AwardData)
                return object;
            let message = new $root.jlbf3.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.AwardData
         * @static
         * @param {jlbf3.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Mult = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbf3.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbf3.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.AwardData";
        };

        return AwardData;
    })();

    jlbf3.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlbf3
         * @interface IRoundInfo
         * @property {Array.<jlbf3.IPlateSymbolData>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlbf3.IPlateNumData>|null} [PlateNum] RoundInfo PlateNum
         * @property {Array.<jlbf3.IAwardDataVecData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [Collected] RoundInfo Collected
         * @property {number|null} [GoldLevel] RoundInfo GoldLevel
         * @property {Array.<jlbf3.IColumn>|null} [GoldPos] RoundInfo GoldPos
         * @property {Array.<number>|null} [CollectWin] RoundInfo CollectWin
         * @property {Array.<number>|null} [LineWin] RoundInfo LineWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [AddRounds] RoundInfo AddRounds
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<number>|null} [AddCrackLv] RoundInfo AddCrackLv
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlbf3
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlbf3.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateNum = [];
            this.AwardDataVec = [];
            this.GoldPos = [];
            this.CollectWin = [];
            this.LineWin = [];
            this.AddCrackLv = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlbf3.IPlateSymbolData>} PlateSymbol
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateNum.
         * @member {Array.<jlbf3.IPlateNumData>} PlateNum
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateNum = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlbf3.IAwardDataVecData>} AwardDataVec
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo Collected.
         * @member {number} Collected
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Collected = 0;

        /**
         * RoundInfo GoldLevel.
         * @member {number} GoldLevel
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GoldLevel = 0;

        /**
         * RoundInfo GoldPos.
         * @member {Array.<jlbf3.IColumn>} GoldPos
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GoldPos = $util.emptyArray;

        /**
         * RoundInfo CollectWin.
         * @member {Array.<number>} CollectWin
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CollectWin = $util.emptyArray;

        /**
         * RoundInfo LineWin.
         * @member {Array.<number>} LineWin
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LineWin = $util.emptyArray;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo AddRounds.
         * @member {number} AddRounds
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRounds = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AddCrackLv.
         * @member {Array.<number>} AddCrackLv
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddCrackLv = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbf3.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {jlbf3.IRoundInfo=} [properties] Properties to set
         * @returns {jlbf3.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlbf3.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {jlbf3.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlbf3.PlateSymbolData.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateNum != null && message.PlateNum.length)
                for (let i = 0; i < message.PlateNum.length; ++i)
                    $root.jlbf3.PlateNumData.encode(message.PlateNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlbf3.AwardDataVecData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.Collected != null && Object.hasOwnProperty.call(message, "Collected"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Collected);
            if (message.GoldLevel != null && Object.hasOwnProperty.call(message, "GoldLevel"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.GoldLevel);
            if (message.GoldPos != null && message.GoldPos.length)
                for (let i = 0; i < message.GoldPos.length; ++i)
                    $root.jlbf3.Column.encode(message.GoldPos[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.CollectWin != null && message.CollectWin.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.CollectWin.length; ++i)
                    writer.double(message.CollectWin[i]);
                writer.ldelim();
            }
            if (message.LineWin != null && message.LineWin.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.LineWin.length; ++i)
                    writer.double(message.LineWin[i]);
                writer.ldelim();
            }
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.FreeTotalRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.FreeNowRound);
            if (message.AddRounds != null && Object.hasOwnProperty.call(message, "AddRounds"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.AddRounds);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.AwardTypeFlag);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 14, wireType 1 =*/113).double(message.RoundWin);
            if (message.AddCrackLv != null && message.AddCrackLv.length) {
                writer.uint32(/* id 15, wireType 2 =*/122).fork();
                for (let i = 0; i < message.AddCrackLv.length; ++i)
                    writer.int32(message.AddCrackLv[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlbf3.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {jlbf3.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlbf3.PlateSymbolData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateNum && message.PlateNum.length))
                            message.PlateNum = [];
                        message.PlateNum.push($root.jlbf3.PlateNumData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlbf3.AwardDataVecData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.Collected = reader.int32();
                        break;
                    }
                case 5: {
                        message.GoldLevel = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.GoldPos && message.GoldPos.length))
                            message.GoldPos = [];
                        message.GoldPos.push($root.jlbf3.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.CollectWin && message.CollectWin.length))
                            message.CollectWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CollectWin.push(reader.double());
                        } else
                            message.CollectWin.push(reader.double());
                        break;
                    }
                case 8: {
                        if (!(message.LineWin && message.LineWin.length))
                            message.LineWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.LineWin.push(reader.double());
                        } else
                            message.LineWin.push(reader.double());
                        break;
                    }
                case 9: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.AddRounds = reader.int32();
                        break;
                    }
                case 13: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 14: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 15: {
                        if (!(message.AddCrackLv && message.AddCrackLv.length))
                            message.AddCrackLv = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AddCrackLv.push(reader.int32());
                        } else
                            message.AddCrackLv.push(reader.int32());
                        break;
                    }
                case 16: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlbf3.PlateSymbolData.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateNum != null && message.hasOwnProperty("PlateNum")) {
                if (!Array.isArray(message.PlateNum))
                    return "PlateNum: array expected";
                for (let i = 0; i < message.PlateNum.length; ++i) {
                    let error = $root.jlbf3.PlateNumData.verify(message.PlateNum[i]);
                    if (error)
                        return "PlateNum." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlbf3.AwardDataVecData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.Collected != null && message.hasOwnProperty("Collected"))
                if (!$util.isInteger(message.Collected))
                    return "Collected: integer expected";
            if (message.GoldLevel != null && message.hasOwnProperty("GoldLevel"))
                if (!$util.isInteger(message.GoldLevel))
                    return "GoldLevel: integer expected";
            if (message.GoldPos != null && message.hasOwnProperty("GoldPos")) {
                if (!Array.isArray(message.GoldPos))
                    return "GoldPos: array expected";
                for (let i = 0; i < message.GoldPos.length; ++i) {
                    let error = $root.jlbf3.Column.verify(message.GoldPos[i]);
                    if (error)
                        return "GoldPos." + error;
                }
            }
            if (message.CollectWin != null && message.hasOwnProperty("CollectWin")) {
                if (!Array.isArray(message.CollectWin))
                    return "CollectWin: array expected";
                for (let i = 0; i < message.CollectWin.length; ++i)
                    if (typeof message.CollectWin[i] !== "number")
                        return "CollectWin: number[] expected";
            }
            if (message.LineWin != null && message.hasOwnProperty("LineWin")) {
                if (!Array.isArray(message.LineWin))
                    return "LineWin: array expected";
                for (let i = 0; i < message.LineWin.length; ++i)
                    if (typeof message.LineWin[i] !== "number")
                        return "LineWin: number[] expected";
            }
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                if (!$util.isInteger(message.AddRounds))
                    return "AddRounds: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AddCrackLv != null && message.hasOwnProperty("AddCrackLv")) {
                if (!Array.isArray(message.AddCrackLv))
                    return "AddCrackLv: array expected";
                for (let i = 0; i < message.AddCrackLv.length; ++i)
                    if (!$util.isInteger(message.AddCrackLv[i]))
                        return "AddCrackLv: integer[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.RoundInfo)
                return object;
            let message = new $root.jlbf3.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlbf3.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlbf3.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlbf3.PlateSymbolData.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateNum) {
                if (!Array.isArray(object.PlateNum))
                    throw TypeError(".jlbf3.RoundInfo.PlateNum: array expected");
                message.PlateNum = [];
                for (let i = 0; i < object.PlateNum.length; ++i) {
                    if (typeof object.PlateNum[i] !== "object")
                        throw TypeError(".jlbf3.RoundInfo.PlateNum: object expected");
                    message.PlateNum[i] = $root.jlbf3.PlateNumData.fromObject(object.PlateNum[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlbf3.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlbf3.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlbf3.AwardDataVecData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.Collected != null)
                message.Collected = object.Collected | 0;
            if (object.GoldLevel != null)
                message.GoldLevel = object.GoldLevel | 0;
            if (object.GoldPos) {
                if (!Array.isArray(object.GoldPos))
                    throw TypeError(".jlbf3.RoundInfo.GoldPos: array expected");
                message.GoldPos = [];
                for (let i = 0; i < object.GoldPos.length; ++i) {
                    if (typeof object.GoldPos[i] !== "object")
                        throw TypeError(".jlbf3.RoundInfo.GoldPos: object expected");
                    message.GoldPos[i] = $root.jlbf3.Column.fromObject(object.GoldPos[i]);
                }
            }
            if (object.CollectWin) {
                if (!Array.isArray(object.CollectWin))
                    throw TypeError(".jlbf3.RoundInfo.CollectWin: array expected");
                message.CollectWin = [];
                for (let i = 0; i < object.CollectWin.length; ++i)
                    message.CollectWin[i] = Number(object.CollectWin[i]);
            }
            if (object.LineWin) {
                if (!Array.isArray(object.LineWin))
                    throw TypeError(".jlbf3.RoundInfo.LineWin: array expected");
                message.LineWin = [];
                for (let i = 0; i < object.LineWin.length; ++i)
                    message.LineWin[i] = Number(object.LineWin[i]);
            }
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.AddRounds != null)
                message.AddRounds = object.AddRounds | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AddCrackLv) {
                if (!Array.isArray(object.AddCrackLv))
                    throw TypeError(".jlbf3.RoundInfo.AddCrackLv: array expected");
                message.AddCrackLv = [];
                for (let i = 0; i < object.AddCrackLv.length; ++i)
                    message.AddCrackLv[i] = object.AddCrackLv[i] | 0;
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {jlbf3.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateNum = [];
                object.AwardDataVec = [];
                object.GoldPos = [];
                object.CollectWin = [];
                object.LineWin = [];
                object.AddCrackLv = [];
            }
            if (options.defaults) {
                object.Collected = 0;
                object.GoldLevel = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
                object.FreeNowRound = 0;
                object.AddRounds = 0;
                object.AwardTypeFlag = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlbf3.PlateSymbolData.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateNum && message.PlateNum.length) {
                object.PlateNum = [];
                for (let j = 0; j < message.PlateNum.length; ++j)
                    object.PlateNum[j] = $root.jlbf3.PlateNumData.toObject(message.PlateNum[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlbf3.AwardDataVecData.toObject(message.AwardDataVec[j], options);
            }
            if (message.Collected != null && message.hasOwnProperty("Collected"))
                object.Collected = message.Collected;
            if (message.GoldLevel != null && message.hasOwnProperty("GoldLevel"))
                object.GoldLevel = message.GoldLevel;
            if (message.GoldPos && message.GoldPos.length) {
                object.GoldPos = [];
                for (let j = 0; j < message.GoldPos.length; ++j)
                    object.GoldPos[j] = $root.jlbf3.Column.toObject(message.GoldPos[j], options);
            }
            if (message.CollectWin && message.CollectWin.length) {
                object.CollectWin = [];
                for (let j = 0; j < message.CollectWin.length; ++j)
                    object.CollectWin[j] = options.json && !isFinite(message.CollectWin[j]) ? String(message.CollectWin[j]) : message.CollectWin[j];
            }
            if (message.LineWin && message.LineWin.length) {
                object.LineWin = [];
                for (let j = 0; j < message.LineWin.length; ++j)
                    object.LineWin[j] = options.json && !isFinite(message.LineWin[j]) ? String(message.LineWin[j]) : message.LineWin[j];
            }
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                object.AddRounds = message.AddRounds;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AddCrackLv && message.AddCrackLv.length) {
                object.AddCrackLv = [];
                for (let j = 0; j < message.AddCrackLv.length; ++j)
                    object.AddCrackLv[j] = message.AddCrackLv[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlbf3.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlbf3.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.RoundInfo";
        };

        return RoundInfo;
    })();

    jlbf3.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbf3
         * @interface ISpinAck
         * @property {Array.<jlbf3.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [GameType] SpinAck GameType
         * @property {Array.<number>|null} [CrackLevels] SpinAck CrackLevels
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbf3
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbf3.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlbf3.IRoundInfo>} RoundQueue
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck GameType.
         * @member {number} GameType
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.GameType = 0;

        /**
         * SpinAck CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jlbf3.SpinAck
         * @instance
         */
        SpinAck.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbf3.SpinAck
         * @static
         * @param {jlbf3.ISpinAck=} [properties] Properties to set
         * @returns {jlbf3.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbf3.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.SpinAck
         * @static
         * @param {jlbf3.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlbf3.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Bet);
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RTP);
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.GameType);
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbf3.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.SpinAck
         * @static
         * @param {jlbf3.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlbf3.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Bet = reader.double();
                        break;
                    }
                case 3: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.RTP = reader.double();
                        break;
                    }
                case 6: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 7: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbf3.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlbf3.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.SpinAck)
                return object;
            let message = new $root.jlbf3.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlbf3.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlbf3.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlbf3.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jlbf3.SpinAck.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.SpinAck
         * @static
         * @param {jlbf3.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.CrackLevels = [];
            }
            if (options.defaults) {
                object.Bet = 0;
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.GameType = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlbf3.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbf3.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbf3.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.SpinAck";
        };

        return SpinAck;
    })();

    jlbf3.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlbf3
         * @interface IStateInfo
         * @property {Array.<number>|null} [CrackLevels] StateInfo CrackLevels
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlbf3
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlbf3.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jlbf3.StateInfo
         * @instance
         */
        StateInfo.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlbf3.StateInfo
         * @static
         * @param {jlbf3.IStateInfo=} [properties] Properties to set
         * @returns {jlbf3.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlbf3.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.StateInfo
         * @static
         * @param {jlbf3.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlbf3.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.StateInfo
         * @static
         * @param {jlbf3.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlbf3.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.StateInfo)
                return object;
            let message = new $root.jlbf3.StateInfo();
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jlbf3.StateInfo.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.StateInfo
         * @static
         * @param {jlbf3.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CrackLevels = [];
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlbf3.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlbf3.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.StateInfo";
        };

        return StateInfo;
    })();

    jlbf3.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlbf3
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [CrackLevels] GameInfoData CrackLevels
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlbf3
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlbf3.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.CrackLevels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlbf3.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlbf3.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData CrackLevels.
         * @member {Array.<number>} CrackLevels
         * @memberof jlbf3.GameInfoData
         * @instance
         */
        GameInfoData.prototype.CrackLevels = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {jlbf3.IGameInfoData=} [properties] Properties to set
         * @returns {jlbf3.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlbf3.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {jlbf3.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.CrackLevels != null && message.CrackLevels.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    writer.int32(message.CrackLevels[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlbf3.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {jlbf3.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbf3.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbf3.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.CrackLevels && message.CrackLevels.length))
                            message.CrackLevels = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CrackLevels.push(reader.int32());
                        } else
                            message.CrackLevels.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbf3.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.CrackLevels != null && message.hasOwnProperty("CrackLevels")) {
                if (!Array.isArray(message.CrackLevels))
                    return "CrackLevels: array expected";
                for (let i = 0; i < message.CrackLevels.length; ++i)
                    if (!$util.isInteger(message.CrackLevels[i]))
                        return "CrackLevels: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbf3.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbf3.GameInfoData)
                return object;
            let message = new $root.jlbf3.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlbf3.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.CrackLevels) {
                if (!Array.isArray(object.CrackLevels))
                    throw TypeError(".jlbf3.GameInfoData.CrackLevels: array expected");
                message.CrackLevels = [];
                for (let i = 0; i < object.CrackLevels.length; ++i)
                    message.CrackLevels[i] = object.CrackLevels[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {jlbf3.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.CrackLevels = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.CrackLevels && message.CrackLevels.length) {
                object.CrackLevels = [];
                for (let j = 0; j < message.CrackLevels.length; ++j)
                    object.CrackLevels[j] = message.CrackLevels[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlbf3.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlbf3.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbf3.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlbf3;
})();

export const jlbh = $root.jlbh = (() => {

    /**
     * Namespace jlbh.
     * @exports jlbh
     * @namespace
     */
    const jlbh = {};

    jlbh.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbh
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlbh
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbh.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlbh.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbh.Column
         * @static
         * @param {jlbh.IColumn=} [properties] Properties to set
         * @returns {jlbh.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbh.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbh.Column
         * @static
         * @param {jlbh.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbh.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.Column
         * @static
         * @param {jlbh.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbh.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.Column)
                return object;
            let message = new $root.jlbh.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlbh.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.Column
         * @static
         * @param {jlbh.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbh.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbh.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.Column";
        };

        return Column;
    })();

    jlbh.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbh
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [Pos] AwardData Pos
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbh
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbh.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbh.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbh.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlbh.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlbh.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Pos.
         * @member {Array.<number>} Pos
         * @memberof jlbh.AwardData
         * @instance
         */
        AwardData.prototype.Pos = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbh.AwardData
         * @static
         * @param {jlbh.IAwardData=} [properties] Properties to set
         * @returns {jlbh.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbh.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbh.AwardData
         * @static
         * @param {jlbh.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Pos != null && message.Pos.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.Pos.length; ++i)
                    writer.int32(message.Pos[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbh.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.AwardData
         * @static
         * @param {jlbh.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.Pos && message.Pos.length))
                            message.Pos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pos.push(reader.int32());
                        } else
                            message.Pos.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbh.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Pos != null && message.hasOwnProperty("Pos")) {
                if (!Array.isArray(message.Pos))
                    return "Pos: array expected";
                for (let i = 0; i < message.Pos.length; ++i)
                    if (!$util.isInteger(message.Pos[i]))
                        return "Pos: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.AwardData)
                return object;
            let message = new $root.jlbh.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Pos) {
                if (!Array.isArray(object.Pos))
                    throw TypeError(".jlbh.AwardData.Pos: array expected");
                message.Pos = [];
                for (let i = 0; i < object.Pos.length; ++i)
                    message.Pos[i] = object.Pos[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.AwardData
         * @static
         * @param {jlbh.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Pos = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Pos && message.Pos.length) {
                object.Pos = [];
                for (let j = 0; j < message.Pos.length; ++j)
                    object.Pos[j] = message.Pos[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbh.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbh.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.AwardData";
        };

        return AwardData;
    })();

    jlbh.GoldToWild = (function() {

        /**
         * Properties of a GoldToWild.
         * @memberof jlbh
         * @interface IGoldToWild
         * @property {number|null} [Pos] GoldToWild Pos
         * @property {number|null} [Wild] GoldToWild Wild
         */

        /**
         * Constructs a new GoldToWild.
         * @memberof jlbh
         * @classdesc Represents a GoldToWild.
         * @implements IGoldToWild
         * @constructor
         * @param {jlbh.IGoldToWild=} [properties] Properties to set
         */
        function GoldToWild(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GoldToWild Pos.
         * @member {number} Pos
         * @memberof jlbh.GoldToWild
         * @instance
         */
        GoldToWild.prototype.Pos = 0;

        /**
         * GoldToWild Wild.
         * @member {number} Wild
         * @memberof jlbh.GoldToWild
         * @instance
         */
        GoldToWild.prototype.Wild = 0;

        /**
         * Creates a new GoldToWild instance using the specified properties.
         * @function create
         * @memberof jlbh.GoldToWild
         * @static
         * @param {jlbh.IGoldToWild=} [properties] Properties to set
         * @returns {jlbh.GoldToWild} GoldToWild instance
         */
        GoldToWild.create = function create(properties) {
            return new GoldToWild(properties);
        };

        /**
         * Encodes the specified GoldToWild message. Does not implicitly {@link jlbh.GoldToWild.verify|verify} messages.
         * @function encode
         * @memberof jlbh.GoldToWild
         * @static
         * @param {jlbh.IGoldToWild} message GoldToWild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GoldToWild.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Wild != null && Object.hasOwnProperty.call(message, "Wild"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Wild);
            return writer;
        };

        /**
         * Encodes the specified GoldToWild message, length delimited. Does not implicitly {@link jlbh.GoldToWild.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.GoldToWild
         * @static
         * @param {jlbh.IGoldToWild} message GoldToWild message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GoldToWild.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GoldToWild message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.GoldToWild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.GoldToWild} GoldToWild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GoldToWild.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.GoldToWild();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Wild = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GoldToWild message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.GoldToWild
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.GoldToWild} GoldToWild
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GoldToWild.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GoldToWild message.
         * @function verify
         * @memberof jlbh.GoldToWild
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GoldToWild.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Wild != null && message.hasOwnProperty("Wild"))
                if (!$util.isInteger(message.Wild))
                    return "Wild: integer expected";
            return null;
        };

        /**
         * Creates a GoldToWild message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.GoldToWild
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.GoldToWild} GoldToWild
         */
        GoldToWild.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.GoldToWild)
                return object;
            let message = new $root.jlbh.GoldToWild();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Wild != null)
                message.Wild = object.Wild | 0;
            return message;
        };

        /**
         * Creates a plain object from a GoldToWild message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.GoldToWild
         * @static
         * @param {jlbh.GoldToWild} message GoldToWild
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GoldToWild.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Wild = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Wild != null && message.hasOwnProperty("Wild"))
                object.Wild = message.Wild;
            return object;
        };

        /**
         * Converts this GoldToWild to JSON.
         * @function toJSON
         * @memberof jlbh.GoldToWild
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GoldToWild.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GoldToWild
         * @function getTypeUrl
         * @memberof jlbh.GoldToWild
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GoldToWild.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.GoldToWild";
        };

        return GoldToWild;
    })();

    jlbh.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlbh
         * @interface ISinglePlate
         * @property {Array.<jlbh.IColumn>|null} [Symbol] SinglePlate Symbol
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         * @property {number|null} [SingleMul] SinglePlate SingleMul
         * @property {Array.<jlbh.IAwardData>|null} [AwardDataVec] SinglePlate AwardDataVec
         * @property {boolean|null} [IsSpecial] SinglePlate IsSpecial
         * @property {Array.<jlbh.IColumn>|null} [SpecialSymbol] SinglePlate SpecialSymbol
         * @property {Array.<jlbh.IGoldToWild>|null} [EliminateGold] SinglePlate EliminateGold
         * @property {boolean|null} [MaxFlag] SinglePlate MaxFlag
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlbh
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlbh.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.Symbol = [];
            this.AwardDataVec = [];
            this.SpecialSymbol = [];
            this.EliminateGold = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate Symbol.
         * @member {Array.<jlbh.IColumn>} Symbol
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Symbol = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * SinglePlate SingleMul.
         * @member {number} SingleMul
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleMul = 0;

        /**
         * SinglePlate AwardDataVec.
         * @member {Array.<jlbh.IAwardData>} AwardDataVec
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SinglePlate IsSpecial.
         * @member {boolean} IsSpecial
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.IsSpecial = false;

        /**
         * SinglePlate SpecialSymbol.
         * @member {Array.<jlbh.IColumn>} SpecialSymbol
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SpecialSymbol = $util.emptyArray;

        /**
         * SinglePlate EliminateGold.
         * @member {Array.<jlbh.IGoldToWild>} EliminateGold
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.EliminateGold = $util.emptyArray;

        /**
         * SinglePlate MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.MaxFlag = false;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlbh.SinglePlate
         * @static
         * @param {jlbh.ISinglePlate=} [properties] Properties to set
         * @returns {jlbh.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlbh.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlbh.SinglePlate
         * @static
         * @param {jlbh.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlbh.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.SingleWin);
            if (message.SingleMul != null && Object.hasOwnProperty.call(message, "SingleMul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.SingleMul);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlbh.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.IsSpecial != null && Object.hasOwnProperty.call(message, "IsSpecial"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.IsSpecial);
            if (message.SpecialSymbol != null && message.SpecialSymbol.length)
                for (let i = 0; i < message.SpecialSymbol.length; ++i)
                    $root.jlbh.Column.encode(message.SpecialSymbol[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.EliminateGold != null && message.EliminateGold.length)
                for (let i = 0; i < message.EliminateGold.length; ++i)
                    $root.jlbh.GoldToWild.encode(message.EliminateGold[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlbh.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.SinglePlate
         * @static
         * @param {jlbh.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlbh.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 3: {
                        message.SingleMul = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlbh.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.IsSpecial = reader.bool();
                        break;
                    }
                case 6: {
                        if (!(message.SpecialSymbol && message.SpecialSymbol.length))
                            message.SpecialSymbol = [];
                        message.SpecialSymbol.push($root.jlbh.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.EliminateGold && message.EliminateGold.length))
                            message.EliminateGold = [];
                        message.EliminateGold.push($root.jlbh.GoldToWild.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlbh.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlbh.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.SingleMul != null && message.hasOwnProperty("SingleMul"))
                if (typeof message.SingleMul !== "number")
                    return "SingleMul: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlbh.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.IsSpecial != null && message.hasOwnProperty("IsSpecial"))
                if (typeof message.IsSpecial !== "boolean")
                    return "IsSpecial: boolean expected";
            if (message.SpecialSymbol != null && message.hasOwnProperty("SpecialSymbol")) {
                if (!Array.isArray(message.SpecialSymbol))
                    return "SpecialSymbol: array expected";
                for (let i = 0; i < message.SpecialSymbol.length; ++i) {
                    let error = $root.jlbh.Column.verify(message.SpecialSymbol[i]);
                    if (error)
                        return "SpecialSymbol." + error;
                }
            }
            if (message.EliminateGold != null && message.hasOwnProperty("EliminateGold")) {
                if (!Array.isArray(message.EliminateGold))
                    return "EliminateGold: array expected";
                for (let i = 0; i < message.EliminateGold.length; ++i) {
                    let error = $root.jlbh.GoldToWild.verify(message.EliminateGold[i]);
                    if (error)
                        return "EliminateGold." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.SinglePlate)
                return object;
            let message = new $root.jlbh.SinglePlate();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlbh.SinglePlate.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlbh.SinglePlate.Symbol: object expected");
                    message.Symbol[i] = $root.jlbh.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.SingleMul != null)
                message.SingleMul = Number(object.SingleMul);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlbh.SinglePlate.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlbh.SinglePlate.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlbh.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.IsSpecial != null)
                message.IsSpecial = Boolean(object.IsSpecial);
            if (object.SpecialSymbol) {
                if (!Array.isArray(object.SpecialSymbol))
                    throw TypeError(".jlbh.SinglePlate.SpecialSymbol: array expected");
                message.SpecialSymbol = [];
                for (let i = 0; i < object.SpecialSymbol.length; ++i) {
                    if (typeof object.SpecialSymbol[i] !== "object")
                        throw TypeError(".jlbh.SinglePlate.SpecialSymbol: object expected");
                    message.SpecialSymbol[i] = $root.jlbh.Column.fromObject(object.SpecialSymbol[i]);
                }
            }
            if (object.EliminateGold) {
                if (!Array.isArray(object.EliminateGold))
                    throw TypeError(".jlbh.SinglePlate.EliminateGold: array expected");
                message.EliminateGold = [];
                for (let i = 0; i < object.EliminateGold.length; ++i) {
                    if (typeof object.EliminateGold[i] !== "object")
                        throw TypeError(".jlbh.SinglePlate.EliminateGold: object expected");
                    message.EliminateGold[i] = $root.jlbh.GoldToWild.fromObject(object.EliminateGold[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.SinglePlate
         * @static
         * @param {jlbh.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.AwardDataVec = [];
                object.SpecialSymbol = [];
                object.EliminateGold = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.SingleMul = 0;
                object.IsSpecial = false;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlbh.Column.toObject(message.Symbol[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.SingleMul != null && message.hasOwnProperty("SingleMul"))
                object.SingleMul = options.json && !isFinite(message.SingleMul) ? String(message.SingleMul) : message.SingleMul;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlbh.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.IsSpecial != null && message.hasOwnProperty("IsSpecial"))
                object.IsSpecial = message.IsSpecial;
            if (message.SpecialSymbol && message.SpecialSymbol.length) {
                object.SpecialSymbol = [];
                for (let j = 0; j < message.SpecialSymbol.length; ++j)
                    object.SpecialSymbol[j] = $root.jlbh.Column.toObject(message.SpecialSymbol[j], options);
            }
            if (message.EliminateGold && message.EliminateGold.length) {
                object.EliminateGold = [];
                for (let j = 0; j < message.EliminateGold.length; ++j)
                    object.EliminateGold[j] = $root.jlbh.GoldToWild.toObject(message.EliminateGold[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlbh.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlbh.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.SinglePlate";
        };

        return SinglePlate;
    })();

    jlbh.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlbh
         * @interface IRoundInfo
         * @property {Array.<jlbh.ISinglePlate>|null} [PlateQueue] RoundInfo PlateQueue
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [RoundMulThis] RoundInfo RoundMulThis
         * @property {number|null} [RoundMulNext] RoundInfo RoundMulNext
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlbh
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlbh.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateQueue.
         * @member {Array.<jlbh.ISinglePlate>} PlateQueue
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateQueue = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo RoundMulThis.
         * @member {number} RoundMulThis
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundMulThis = 0;

        /**
         * RoundInfo RoundMulNext.
         * @member {number} RoundMulNext
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundMulNext = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbh.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlbh.RoundInfo
         * @static
         * @param {jlbh.IRoundInfo=} [properties] Properties to set
         * @returns {jlbh.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlbh.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbh.RoundInfo
         * @static
         * @param {jlbh.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateQueue != null && message.PlateQueue.length)
                for (let i = 0; i < message.PlateQueue.length; ++i)
                    $root.jlbh.SinglePlate.encode(message.PlateQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.RoundMulThis != null && Object.hasOwnProperty.call(message, "RoundMulThis"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundMulThis);
            if (message.RoundMulNext != null && Object.hasOwnProperty.call(message, "RoundMulNext"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RoundMulNext);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ScatterCount);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlbh.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.RoundInfo
         * @static
         * @param {jlbh.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateQueue && message.PlateQueue.length))
                            message.PlateQueue = [];
                        message.PlateQueue.push($root.jlbh.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.RoundMulThis = reader.double();
                        break;
                    }
                case 4: {
                        message.RoundMulNext = reader.double();
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlbh.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateQueue != null && message.hasOwnProperty("PlateQueue")) {
                if (!Array.isArray(message.PlateQueue))
                    return "PlateQueue: array expected";
                for (let i = 0; i < message.PlateQueue.length; ++i) {
                    let error = $root.jlbh.SinglePlate.verify(message.PlateQueue[i]);
                    if (error)
                        return "PlateQueue." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.RoundMulThis != null && message.hasOwnProperty("RoundMulThis"))
                if (typeof message.RoundMulThis !== "number")
                    return "RoundMulThis: number expected";
            if (message.RoundMulNext != null && message.hasOwnProperty("RoundMulNext"))
                if (typeof message.RoundMulNext !== "number")
                    return "RoundMulNext: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.RoundInfo)
                return object;
            let message = new $root.jlbh.RoundInfo();
            if (object.PlateQueue) {
                if (!Array.isArray(object.PlateQueue))
                    throw TypeError(".jlbh.RoundInfo.PlateQueue: array expected");
                message.PlateQueue = [];
                for (let i = 0; i < object.PlateQueue.length; ++i) {
                    if (typeof object.PlateQueue[i] !== "object")
                        throw TypeError(".jlbh.RoundInfo.PlateQueue: object expected");
                    message.PlateQueue[i] = $root.jlbh.SinglePlate.fromObject(object.PlateQueue[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.RoundMulThis != null)
                message.RoundMulThis = Number(object.RoundMulThis);
            if (object.RoundMulNext != null)
                message.RoundMulNext = Number(object.RoundMulNext);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.RoundInfo
         * @static
         * @param {jlbh.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateQueue = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.RoundMulThis = 0;
                object.RoundMulNext = 0;
                object.AwardTypeFlag = 0;
                object.ScatterCount = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
            }
            if (message.PlateQueue && message.PlateQueue.length) {
                object.PlateQueue = [];
                for (let j = 0; j < message.PlateQueue.length; ++j)
                    object.PlateQueue[j] = $root.jlbh.SinglePlate.toObject(message.PlateQueue[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.RoundMulThis != null && message.hasOwnProperty("RoundMulThis"))
                object.RoundMulThis = options.json && !isFinite(message.RoundMulThis) ? String(message.RoundMulThis) : message.RoundMulThis;
            if (message.RoundMulNext != null && message.hasOwnProperty("RoundMulNext"))
                object.RoundMulNext = options.json && !isFinite(message.RoundMulNext) ? String(message.RoundMulNext) : message.RoundMulNext;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlbh.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlbh.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.RoundInfo";
        };

        return RoundInfo;
    })();

    jlbh.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jlbh
         * @interface IFreeData
         * @property {Array.<jlbh.IRoundInfo>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jlbh
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jlbh.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jlbh.IRoundInfo>} FreeQueue
         * @memberof jlbh.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlbh.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jlbh.FreeData
         * @static
         * @param {jlbh.IFreeData=} [properties] Properties to set
         * @returns {jlbh.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jlbh.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jlbh.FreeData
         * @static
         * @param {jlbh.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jlbh.RoundInfo.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jlbh.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.FreeData
         * @static
         * @param {jlbh.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jlbh.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jlbh.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jlbh.RoundInfo.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.FreeData)
                return object;
            let message = new $root.jlbh.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jlbh.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jlbh.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jlbh.RoundInfo.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.FreeData
         * @static
         * @param {jlbh.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults)
                object.FreeTotalWin = 0;
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jlbh.RoundInfo.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jlbh.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jlbh.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.FreeData";
        };

        return FreeData;
    })();

    jlbh.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbh
         * @interface ISpinAck
         * @property {jlbh.IRoundInfo|null} [MainGame] SpinAck MainGame
         * @property {jlbh.IFreeData|null} [FreeGame] SpinAck FreeGame
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbh
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbh.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainGame.
         * @member {jlbh.IRoundInfo|null|undefined} MainGame
         * @memberof jlbh.SpinAck
         * @instance
         */
        SpinAck.prototype.MainGame = null;

        /**
         * SpinAck FreeGame.
         * @member {jlbh.IFreeData|null|undefined} FreeGame
         * @memberof jlbh.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeGame = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbh.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbh.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlbh.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlbh.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbh.SpinAck
         * @static
         * @param {jlbh.ISpinAck=} [properties] Properties to set
         * @returns {jlbh.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbh.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbh.SpinAck
         * @static
         * @param {jlbh.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainGame != null && Object.hasOwnProperty.call(message, "MainGame"))
                $root.jlbh.RoundInfo.encode(message.MainGame, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeGame != null && Object.hasOwnProperty.call(message, "FreeGame"))
                $root.jlbh.FreeData.encode(message.FreeGame, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbh.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbh.SpinAck
         * @static
         * @param {jlbh.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbh.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbh.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbh.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainGame = $root.jlbh.RoundInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeGame = $root.jlbh.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.RTP = reader.double();
                        break;
                    }
                case 5: {
                        message.LID = reader.int32();
                        break;
                    }
                case 6: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbh.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbh.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbh.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainGame != null && message.hasOwnProperty("MainGame")) {
                let error = $root.jlbh.RoundInfo.verify(message.MainGame);
                if (error)
                    return "MainGame." + error;
            }
            if (message.FreeGame != null && message.hasOwnProperty("FreeGame")) {
                let error = $root.jlbh.FreeData.verify(message.FreeGame);
                if (error)
                    return "FreeGame." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbh.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbh.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbh.SpinAck)
                return object;
            let message = new $root.jlbh.SpinAck();
            if (object.MainGame != null) {
                if (typeof object.MainGame !== "object")
                    throw TypeError(".jlbh.SpinAck.MainGame: object expected");
                message.MainGame = $root.jlbh.RoundInfo.fromObject(object.MainGame);
            }
            if (object.FreeGame != null) {
                if (typeof object.FreeGame !== "object")
                    throw TypeError(".jlbh.SpinAck.FreeGame: object expected");
                message.FreeGame = $root.jlbh.FreeData.fromObject(object.FreeGame);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbh.SpinAck
         * @static
         * @param {jlbh.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainGame = null;
                object.FreeGame = null;
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.MainGame != null && message.hasOwnProperty("MainGame"))
                object.MainGame = $root.jlbh.RoundInfo.toObject(message.MainGame, options);
            if (message.FreeGame != null && message.hasOwnProperty("FreeGame"))
                object.FreeGame = $root.jlbh.FreeData.toObject(message.FreeGame, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbh.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbh.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbh.SpinAck";
        };

        return SpinAck;
    })();

    return jlbh;
})();

export const jlbk = $root.jlbk = (() => {

    /**
     * Namespace jlbk.
     * @exports jlbk
     * @namespace
     */
    const jlbk = {};

    jlbk.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbk
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlbk
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbk.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlbk.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbk.Column
         * @static
         * @param {jlbk.IColumn=} [properties] Properties to set
         * @returns {jlbk.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbk.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbk.Column
         * @static
         * @param {jlbk.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbk.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.Column
         * @static
         * @param {jlbk.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbk.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.Column)
                return object;
            let message = new $root.jlbk.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlbk.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.Column
         * @static
         * @param {jlbk.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbk.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbk.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.Column";
        };

        return Column;
    })();

    jlbk.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbk
         * @interface IAwardData
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Money] AwardData Money
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbk
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbk.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlbk.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbk.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbk.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Money.
         * @member {number} Money
         * @memberof jlbk.AwardData
         * @instance
         */
        AwardData.prototype.Money = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbk.AwardData
         * @static
         * @param {jlbk.IAwardData=} [properties] Properties to set
         * @returns {jlbk.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbk.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbk.AwardData
         * @static
         * @param {jlbk.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Count);
            if (message.Money != null && Object.hasOwnProperty.call(message, "Money"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Money);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbk.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.AwardData
         * @static
         * @param {jlbk.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Index = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Count = reader.int32();
                        break;
                    }
                case 4: {
                        message.Money = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbk.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Money != null && message.hasOwnProperty("Money"))
                if (typeof message.Money !== "number")
                    return "Money: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.AwardData)
                return object;
            let message = new $root.jlbk.AwardData();
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Money != null)
                message.Money = Number(object.Money);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.AwardData
         * @static
         * @param {jlbk.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Index = 0;
                object.Symbol = 0;
                object.Count = 0;
                object.Money = 0;
            }
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Money != null && message.hasOwnProperty("Money"))
                object.Money = options.json && !isFinite(message.Money) ? String(message.Money) : message.Money;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbk.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbk.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.AwardData";
        };

        return AwardData;
    })();

    jlbk.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlbk
         * @interface ISinglePlate
         * @property {Array.<jlbk.IColumn>|null} [PlateSymbol] SinglePlate PlateSymbol
         * @property {Array.<jlbk.IAwardData>|null} [AwardDataList] SinglePlate AwardDataList
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         * @property {number|null} [SingleMul] SinglePlate SingleMul
         * @property {Array.<number>|null} [NextRow] SinglePlate NextRow
         * @property {boolean|null} [MaxFlag] SinglePlate MaxFlag
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlbk
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlbk.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.PlateSymbol = [];
            this.AwardDataList = [];
            this.NextRow = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate PlateSymbol.
         * @member {Array.<jlbk.IColumn>} PlateSymbol
         * @memberof jlbk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SinglePlate AwardDataList.
         * @member {Array.<jlbk.IAwardData>} AwardDataList
         * @memberof jlbk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataList = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jlbk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * SinglePlate SingleMul.
         * @member {number} SingleMul
         * @memberof jlbk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleMul = 0;

        /**
         * SinglePlate NextRow.
         * @member {Array.<number>} NextRow
         * @memberof jlbk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.NextRow = $util.emptyArray;

        /**
         * SinglePlate MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.MaxFlag = false;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlbk.SinglePlate
         * @static
         * @param {jlbk.ISinglePlate=} [properties] Properties to set
         * @returns {jlbk.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlbk.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlbk.SinglePlate
         * @static
         * @param {jlbk.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlbk.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlbk.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.SingleWin);
            if (message.SingleMul != null && Object.hasOwnProperty.call(message, "SingleMul"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.SingleMul);
            if (message.NextRow != null && message.NextRow.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.NextRow.length; ++i)
                    writer.int32(message.NextRow[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlbk.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.SinglePlate
         * @static
         * @param {jlbk.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlbk.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlbk.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 4: {
                        message.SingleMul = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.NextRow && message.NextRow.length))
                            message.NextRow = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NextRow.push(reader.int32());
                        } else
                            message.NextRow.push(reader.int32());
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlbk.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlbk.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlbk.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.SingleMul != null && message.hasOwnProperty("SingleMul"))
                if (typeof message.SingleMul !== "number")
                    return "SingleMul: number expected";
            if (message.NextRow != null && message.hasOwnProperty("NextRow")) {
                if (!Array.isArray(message.NextRow))
                    return "NextRow: array expected";
                for (let i = 0; i < message.NextRow.length; ++i)
                    if (!$util.isInteger(message.NextRow[i]))
                        return "NextRow: integer[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.SinglePlate)
                return object;
            let message = new $root.jlbk.SinglePlate();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlbk.SinglePlate.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlbk.SinglePlate.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlbk.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlbk.SinglePlate.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlbk.SinglePlate.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlbk.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.SingleMul != null)
                message.SingleMul = Number(object.SingleMul);
            if (object.NextRow) {
                if (!Array.isArray(object.NextRow))
                    throw TypeError(".jlbk.SinglePlate.NextRow: array expected");
                message.NextRow = [];
                for (let i = 0; i < object.NextRow.length; ++i)
                    message.NextRow[i] = object.NextRow[i] | 0;
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.SinglePlate
         * @static
         * @param {jlbk.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataList = [];
                object.NextRow = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.SingleMul = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlbk.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlbk.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.SingleMul != null && message.hasOwnProperty("SingleMul"))
                object.SingleMul = options.json && !isFinite(message.SingleMul) ? String(message.SingleMul) : message.SingleMul;
            if (message.NextRow && message.NextRow.length) {
                object.NextRow = [];
                for (let j = 0; j < message.NextRow.length; ++j)
                    object.NextRow[j] = message.NextRow[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlbk.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlbk.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.SinglePlate";
        };

        return SinglePlate;
    })();

    jlbk.MainData = (function() {

        /**
         * Properties of a MainData.
         * @memberof jlbk
         * @interface IMainData
         * @property {Array.<jlbk.ISinglePlate>|null} [PlateQueue] MainData PlateQueue
         * @property {number|null} [MainWin] MainData MainWin
         * @property {number|null} [ScatterCount] MainData ScatterCount
         * @property {number|null} [ScatterWin] MainData ScatterWin
         * @property {boolean|null} [MaxFlag] MainData MaxFlag
         */

        /**
         * Constructs a new MainData.
         * @memberof jlbk
         * @classdesc Represents a MainData.
         * @implements IMainData
         * @constructor
         * @param {jlbk.IMainData=} [properties] Properties to set
         */
        function MainData(properties) {
            this.PlateQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MainData PlateQueue.
         * @member {Array.<jlbk.ISinglePlate>} PlateQueue
         * @memberof jlbk.MainData
         * @instance
         */
        MainData.prototype.PlateQueue = $util.emptyArray;

        /**
         * MainData MainWin.
         * @member {number} MainWin
         * @memberof jlbk.MainData
         * @instance
         */
        MainData.prototype.MainWin = 0;

        /**
         * MainData ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlbk.MainData
         * @instance
         */
        MainData.prototype.ScatterCount = 0;

        /**
         * MainData ScatterWin.
         * @member {number} ScatterWin
         * @memberof jlbk.MainData
         * @instance
         */
        MainData.prototype.ScatterWin = 0;

        /**
         * MainData MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbk.MainData
         * @instance
         */
        MainData.prototype.MaxFlag = false;

        /**
         * Creates a new MainData instance using the specified properties.
         * @function create
         * @memberof jlbk.MainData
         * @static
         * @param {jlbk.IMainData=} [properties] Properties to set
         * @returns {jlbk.MainData} MainData instance
         */
        MainData.create = function create(properties) {
            return new MainData(properties);
        };

        /**
         * Encodes the specified MainData message. Does not implicitly {@link jlbk.MainData.verify|verify} messages.
         * @function encode
         * @memberof jlbk.MainData
         * @static
         * @param {jlbk.IMainData} message MainData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MainData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateQueue != null && message.PlateQueue.length)
                for (let i = 0; i < message.PlateQueue.length; ++i)
                    $root.jlbk.SinglePlate.encode(message.PlateQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.MainWin);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ScatterCount);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.ScatterWin);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified MainData message, length delimited. Does not implicitly {@link jlbk.MainData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.MainData
         * @static
         * @param {jlbk.IMainData} message MainData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MainData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MainData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.MainData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.MainData} MainData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MainData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.MainData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateQueue && message.PlateQueue.length))
                            message.PlateQueue = [];
                        message.PlateQueue.push($root.jlbk.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 5: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MainData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.MainData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.MainData} MainData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MainData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MainData message.
         * @function verify
         * @memberof jlbk.MainData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MainData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateQueue != null && message.hasOwnProperty("PlateQueue")) {
                if (!Array.isArray(message.PlateQueue))
                    return "PlateQueue: array expected";
                for (let i = 0; i < message.PlateQueue.length; ++i) {
                    let error = $root.jlbk.SinglePlate.verify(message.PlateQueue[i]);
                    if (error)
                        return "PlateQueue." + error;
                }
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a MainData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.MainData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.MainData} MainData
         */
        MainData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.MainData)
                return object;
            let message = new $root.jlbk.MainData();
            if (object.PlateQueue) {
                if (!Array.isArray(object.PlateQueue))
                    throw TypeError(".jlbk.MainData.PlateQueue: array expected");
                message.PlateQueue = [];
                for (let i = 0; i < object.PlateQueue.length; ++i) {
                    if (typeof object.PlateQueue[i] !== "object")
                        throw TypeError(".jlbk.MainData.PlateQueue: object expected");
                    message.PlateQueue[i] = $root.jlbk.SinglePlate.fromObject(object.PlateQueue[i]);
                }
            }
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a MainData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.MainData
         * @static
         * @param {jlbk.MainData} message MainData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MainData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateQueue = [];
            if (options.defaults) {
                object.MainWin = 0;
                object.ScatterCount = 0;
                object.ScatterWin = 0;
                object.MaxFlag = false;
            }
            if (message.PlateQueue && message.PlateQueue.length) {
                object.PlateQueue = [];
                for (let j = 0; j < message.PlateQueue.length; ++j)
                    object.PlateQueue[j] = $root.jlbk.SinglePlate.toObject(message.PlateQueue[j], options);
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this MainData to JSON.
         * @function toJSON
         * @memberof jlbk.MainData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MainData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MainData
         * @function getTypeUrl
         * @memberof jlbk.MainData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MainData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.MainData";
        };

        return MainData;
    })();

    jlbk.SingleFree = (function() {

        /**
         * Properties of a SingleFree.
         * @memberof jlbk
         * @interface ISingleFree
         * @property {Array.<jlbk.ISinglePlate>|null} [FreePlateQueue] SingleFree FreePlateQueue
         * @property {number|null} [FreeWin] SingleFree FreeWin
         * @property {number|null} [FreePlus] SingleFree FreePlus
         * @property {number|null} [AwardType] SingleFree AwardType
         * @property {number|null} [FreeNowRound] SingleFree FreeNowRound
         * @property {number|null} [FreeTotalRound] SingleFree FreeTotalRound
         * @property {boolean|null} [MaxFlag] SingleFree MaxFlag
         */

        /**
         * Constructs a new SingleFree.
         * @memberof jlbk
         * @classdesc Represents a SingleFree.
         * @implements ISingleFree
         * @constructor
         * @param {jlbk.ISingleFree=} [properties] Properties to set
         */
        function SingleFree(properties) {
            this.FreePlateQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleFree FreePlateQueue.
         * @member {Array.<jlbk.ISinglePlate>} FreePlateQueue
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreePlateQueue = $util.emptyArray;

        /**
         * SingleFree FreeWin.
         * @member {number} FreeWin
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreeWin = 0;

        /**
         * SingleFree FreePlus.
         * @member {number} FreePlus
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreePlus = 0;

        /**
         * SingleFree AwardType.
         * @member {number} AwardType
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.AwardType = 0;

        /**
         * SingleFree FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreeNowRound = 0;

        /**
         * SingleFree FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreeTotalRound = 0;

        /**
         * SingleFree MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbk.SingleFree
         * @instance
         */
        SingleFree.prototype.MaxFlag = false;

        /**
         * Creates a new SingleFree instance using the specified properties.
         * @function create
         * @memberof jlbk.SingleFree
         * @static
         * @param {jlbk.ISingleFree=} [properties] Properties to set
         * @returns {jlbk.SingleFree} SingleFree instance
         */
        SingleFree.create = function create(properties) {
            return new SingleFree(properties);
        };

        /**
         * Encodes the specified SingleFree message. Does not implicitly {@link jlbk.SingleFree.verify|verify} messages.
         * @function encode
         * @memberof jlbk.SingleFree
         * @static
         * @param {jlbk.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreePlateQueue != null && message.FreePlateQueue.length)
                for (let i = 0; i < message.FreePlateQueue.length; ++i)
                    $root.jlbk.SinglePlate.encode(message.FreePlateQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeWin != null && Object.hasOwnProperty.call(message, "FreeWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeWin);
            if (message.FreePlus != null && Object.hasOwnProperty.call(message, "FreePlus"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.FreePlus);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardType);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SingleFree message, length delimited. Does not implicitly {@link jlbk.SingleFree.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.SingleFree
         * @static
         * @param {jlbk.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.SingleFree();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreePlateQueue && message.FreePlateQueue.length))
                            message.FreePlateQueue = [];
                        message.FreePlateQueue.push($root.jlbk.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeWin = reader.double();
                        break;
                    }
                case 3: {
                        message.FreePlus = reader.int32();
                        break;
                    }
                case 4: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleFree message.
         * @function verify
         * @memberof jlbk.SingleFree
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleFree.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreePlateQueue != null && message.hasOwnProperty("FreePlateQueue")) {
                if (!Array.isArray(message.FreePlateQueue))
                    return "FreePlateQueue: array expected";
                for (let i = 0; i < message.FreePlateQueue.length; ++i) {
                    let error = $root.jlbk.SinglePlate.verify(message.FreePlateQueue[i]);
                    if (error)
                        return "FreePlateQueue." + error;
                }
            }
            if (message.FreeWin != null && message.hasOwnProperty("FreeWin"))
                if (typeof message.FreeWin !== "number")
                    return "FreeWin: number expected";
            if (message.FreePlus != null && message.hasOwnProperty("FreePlus"))
                if (!$util.isInteger(message.FreePlus))
                    return "FreePlus: integer expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SingleFree message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.SingleFree
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.SingleFree} SingleFree
         */
        SingleFree.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.SingleFree)
                return object;
            let message = new $root.jlbk.SingleFree();
            if (object.FreePlateQueue) {
                if (!Array.isArray(object.FreePlateQueue))
                    throw TypeError(".jlbk.SingleFree.FreePlateQueue: array expected");
                message.FreePlateQueue = [];
                for (let i = 0; i < object.FreePlateQueue.length; ++i) {
                    if (typeof object.FreePlateQueue[i] !== "object")
                        throw TypeError(".jlbk.SingleFree.FreePlateQueue: object expected");
                    message.FreePlateQueue[i] = $root.jlbk.SinglePlate.fromObject(object.FreePlateQueue[i]);
                }
            }
            if (object.FreeWin != null)
                message.FreeWin = Number(object.FreeWin);
            if (object.FreePlus != null)
                message.FreePlus = object.FreePlus | 0;
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SingleFree message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.SingleFree
         * @static
         * @param {jlbk.SingleFree} message SingleFree
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleFree.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreePlateQueue = [];
            if (options.defaults) {
                object.FreeWin = 0;
                object.FreePlus = 0;
                object.AwardType = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
            }
            if (message.FreePlateQueue && message.FreePlateQueue.length) {
                object.FreePlateQueue = [];
                for (let j = 0; j < message.FreePlateQueue.length; ++j)
                    object.FreePlateQueue[j] = $root.jlbk.SinglePlate.toObject(message.FreePlateQueue[j], options);
            }
            if (message.FreeWin != null && message.hasOwnProperty("FreeWin"))
                object.FreeWin = options.json && !isFinite(message.FreeWin) ? String(message.FreeWin) : message.FreeWin;
            if (message.FreePlus != null && message.hasOwnProperty("FreePlus"))
                object.FreePlus = message.FreePlus;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SingleFree to JSON.
         * @function toJSON
         * @memberof jlbk.SingleFree
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleFree.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleFree
         * @function getTypeUrl
         * @memberof jlbk.SingleFree
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleFree.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.SingleFree";
        };

        return SingleFree;
    })();

    jlbk.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jlbk
         * @interface IFreeData
         * @property {Array.<jlbk.ISingleFree>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jlbk
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jlbk.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jlbk.ISingleFree>} FreeQueue
         * @memberof jlbk.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlbk.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jlbk.FreeData
         * @static
         * @param {jlbk.IFreeData=} [properties] Properties to set
         * @returns {jlbk.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jlbk.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jlbk.FreeData
         * @static
         * @param {jlbk.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jlbk.SingleFree.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jlbk.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.FreeData
         * @static
         * @param {jlbk.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jlbk.SingleFree.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jlbk.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jlbk.SingleFree.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.FreeData)
                return object;
            let message = new $root.jlbk.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jlbk.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jlbk.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jlbk.SingleFree.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.FreeData
         * @static
         * @param {jlbk.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults)
                object.FreeTotalWin = 0;
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jlbk.SingleFree.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jlbk.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jlbk.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.FreeData";
        };

        return FreeData;
    })();

    jlbk.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbk
         * @interface ISpinAck
         * @property {jlbk.IMainData|null} [MainInfo] SpinAck MainInfo
         * @property {jlbk.IFreeData|null} [FreeInfo] SpinAck FreeInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbk
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbk.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainInfo.
         * @member {jlbk.IMainData|null|undefined} MainInfo
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.MainInfo = null;

        /**
         * SpinAck FreeInfo.
         * @member {jlbk.IFreeData|null|undefined} FreeInfo
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeInfo = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbk.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbk.SpinAck
         * @static
         * @param {jlbk.ISpinAck=} [properties] Properties to set
         * @returns {jlbk.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbk.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbk.SpinAck
         * @static
         * @param {jlbk.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainInfo != null && Object.hasOwnProperty.call(message, "MainInfo"))
                $root.jlbk.MainData.encode(message.MainInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeInfo != null && Object.hasOwnProperty.call(message, "FreeInfo"))
                $root.jlbk.FreeData.encode(message.FreeInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardType);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbk.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbk.SpinAck
         * @static
         * @param {jlbk.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbk.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbk.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbk.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainInfo = $root.jlbk.MainData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeInfo = $root.jlbk.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbk.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbk.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbk.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainInfo != null && message.hasOwnProperty("MainInfo")) {
                let error = $root.jlbk.MainData.verify(message.MainInfo);
                if (error)
                    return "MainInfo." + error;
            }
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo")) {
                let error = $root.jlbk.FreeData.verify(message.FreeInfo);
                if (error)
                    return "FreeInfo." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbk.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbk.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbk.SpinAck)
                return object;
            let message = new $root.jlbk.SpinAck();
            if (object.MainInfo != null) {
                if (typeof object.MainInfo !== "object")
                    throw TypeError(".jlbk.SpinAck.MainInfo: object expected");
                message.MainInfo = $root.jlbk.MainData.fromObject(object.MainInfo);
            }
            if (object.FreeInfo != null) {
                if (typeof object.FreeInfo !== "object")
                    throw TypeError(".jlbk.SpinAck.FreeInfo: object expected");
                message.FreeInfo = $root.jlbk.FreeData.fromObject(object.FreeInfo);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbk.SpinAck
         * @static
         * @param {jlbk.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainInfo = null;
                object.FreeInfo = null;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.MainInfo != null && message.hasOwnProperty("MainInfo"))
                object.MainInfo = $root.jlbk.MainData.toObject(message.MainInfo, options);
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo"))
                object.FreeInfo = $root.jlbk.FreeData.toObject(message.FreeInfo, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbk.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbk.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbk.SpinAck";
        };

        return SpinAck;
    })();

    return jlbk;
})();

export const jlbog = $root.jlbog = (() => {

    /**
     * Namespace jlbog.
     * @exports jlbog
     * @namespace
     */
    const jlbog = {};

    jlbog.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbog
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlbog
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbog.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlbog.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbog.Column
         * @static
         * @param {jlbog.IColumn=} [properties] Properties to set
         * @returns {jlbog.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbog.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbog.Column
         * @static
         * @param {jlbog.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbog.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbog.Column
         * @static
         * @param {jlbog.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbog.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbog.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbog.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbog.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbog.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbog.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbog.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbog.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbog.Column)
                return object;
            let message = new $root.jlbog.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlbog.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbog.Column
         * @static
         * @param {jlbog.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbog.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbog.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbog.Column";
        };

        return Column;
    })();

    jlbog.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbog
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbog
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbog.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbog.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbog.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlbog.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlbog.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbog.AwardData
         * @static
         * @param {jlbog.IAwardData=} [properties] Properties to set
         * @returns {jlbog.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbog.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbog.AwardData
         * @static
         * @param {jlbog.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbog.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbog.AwardData
         * @static
         * @param {jlbog.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbog.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbog.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbog.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbog.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbog.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbog.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbog.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbog.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbog.AwardData)
                return object;
            let message = new $root.jlbog.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbog.AwardData
         * @static
         * @param {jlbog.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbog.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbog.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbog.AwardData";
        };

        return AwardData;
    })();

    jlbog.ExpandInfo = (function() {

        /**
         * Properties of an ExpandInfo.
         * @memberof jlbog
         * @interface IExpandInfo
         * @property {number|null} [Symbol] ExpandInfo Symbol
         * @property {Array.<boolean>|null} [ExpandColumn] ExpandInfo ExpandColumn
         * @property {number|null} [Win] ExpandInfo Win
         * @property {number|null} [ColumnHit] ExpandInfo ColumnHit
         */

        /**
         * Constructs a new ExpandInfo.
         * @memberof jlbog
         * @classdesc Represents an ExpandInfo.
         * @implements IExpandInfo
         * @constructor
         * @param {jlbog.IExpandInfo=} [properties] Properties to set
         */
        function ExpandInfo(properties) {
            this.ExpandColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExpandInfo Symbol.
         * @member {number} Symbol
         * @memberof jlbog.ExpandInfo
         * @instance
         */
        ExpandInfo.prototype.Symbol = 0;

        /**
         * ExpandInfo ExpandColumn.
         * @member {Array.<boolean>} ExpandColumn
         * @memberof jlbog.ExpandInfo
         * @instance
         */
        ExpandInfo.prototype.ExpandColumn = $util.emptyArray;

        /**
         * ExpandInfo Win.
         * @member {number} Win
         * @memberof jlbog.ExpandInfo
         * @instance
         */
        ExpandInfo.prototype.Win = 0;

        /**
         * ExpandInfo ColumnHit.
         * @member {number} ColumnHit
         * @memberof jlbog.ExpandInfo
         * @instance
         */
        ExpandInfo.prototype.ColumnHit = 0;

        /**
         * Creates a new ExpandInfo instance using the specified properties.
         * @function create
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {jlbog.IExpandInfo=} [properties] Properties to set
         * @returns {jlbog.ExpandInfo} ExpandInfo instance
         */
        ExpandInfo.create = function create(properties) {
            return new ExpandInfo(properties);
        };

        /**
         * Encodes the specified ExpandInfo message. Does not implicitly {@link jlbog.ExpandInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {jlbog.IExpandInfo} message ExpandInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExpandInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.ExpandColumn != null && message.ExpandColumn.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.ExpandColumn.length; ++i)
                    writer.bool(message.ExpandColumn[i]);
                writer.ldelim();
            }
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.ColumnHit != null && Object.hasOwnProperty.call(message, "ColumnHit"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ColumnHit);
            return writer;
        };

        /**
         * Encodes the specified ExpandInfo message, length delimited. Does not implicitly {@link jlbog.ExpandInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {jlbog.IExpandInfo} message ExpandInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExpandInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ExpandInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbog.ExpandInfo} ExpandInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExpandInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbog.ExpandInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.ExpandColumn && message.ExpandColumn.length))
                            message.ExpandColumn = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ExpandColumn.push(reader.bool());
                        } else
                            message.ExpandColumn.push(reader.bool());
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.ColumnHit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExpandInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbog.ExpandInfo} ExpandInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExpandInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExpandInfo message.
         * @function verify
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExpandInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.ExpandColumn != null && message.hasOwnProperty("ExpandColumn")) {
                if (!Array.isArray(message.ExpandColumn))
                    return "ExpandColumn: array expected";
                for (let i = 0; i < message.ExpandColumn.length; ++i)
                    if (typeof message.ExpandColumn[i] !== "boolean")
                        return "ExpandColumn: boolean[] expected";
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.ColumnHit != null && message.hasOwnProperty("ColumnHit"))
                if (!$util.isInteger(message.ColumnHit))
                    return "ColumnHit: integer expected";
            return null;
        };

        /**
         * Creates an ExpandInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbog.ExpandInfo} ExpandInfo
         */
        ExpandInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbog.ExpandInfo)
                return object;
            let message = new $root.jlbog.ExpandInfo();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.ExpandColumn) {
                if (!Array.isArray(object.ExpandColumn))
                    throw TypeError(".jlbog.ExpandInfo.ExpandColumn: array expected");
                message.ExpandColumn = [];
                for (let i = 0; i < object.ExpandColumn.length; ++i)
                    message.ExpandColumn[i] = Boolean(object.ExpandColumn[i]);
            }
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.ColumnHit != null)
                message.ColumnHit = object.ColumnHit | 0;
            return message;
        };

        /**
         * Creates a plain object from an ExpandInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {jlbog.ExpandInfo} message ExpandInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExpandInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ExpandColumn = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Win = 0;
                object.ColumnHit = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.ExpandColumn && message.ExpandColumn.length) {
                object.ExpandColumn = [];
                for (let j = 0; j < message.ExpandColumn.length; ++j)
                    object.ExpandColumn[j] = message.ExpandColumn[j];
            }
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.ColumnHit != null && message.hasOwnProperty("ColumnHit"))
                object.ColumnHit = message.ColumnHit;
            return object;
        };

        /**
         * Converts this ExpandInfo to JSON.
         * @function toJSON
         * @memberof jlbog.ExpandInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExpandInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExpandInfo
         * @function getTypeUrl
         * @memberof jlbog.ExpandInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExpandInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbog.ExpandInfo";
        };

        return ExpandInfo;
    })();

    jlbog.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlbog
         * @interface IRoundInfo
         * @property {Array.<jlbog.IColumn>|null} [PlateSymbolClient] RoundInfo PlateSymbolClient
         * @property {Array.<boolean>|null} [WildExpandColumn] RoundInfo WildExpandColumn
         * @property {Array.<jlbog.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {Array.<jlbog.IExpandInfo>|null} [FreeExpandInfo] RoundInfo FreeExpandInfo
         * @property {number|null} [ScatterWin] RoundInfo ScatterWin
         * @property {number|null} [LineWin] RoundInfo LineWin
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<jlbog.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlbog
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlbog.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbolClient = [];
            this.WildExpandColumn = [];
            this.AwardDataVec = [];
            this.FreeExpandInfo = [];
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbolClient.
         * @member {Array.<jlbog.IColumn>} PlateSymbolClient
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolClient = $util.emptyArray;

        /**
         * RoundInfo WildExpandColumn.
         * @member {Array.<boolean>} WildExpandColumn
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildExpandColumn = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlbog.IAwardData>} AwardDataVec
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeExpandInfo.
         * @member {Array.<jlbog.IExpandInfo>} FreeExpandInfo
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeExpandInfo = $util.emptyArray;

        /**
         * RoundInfo ScatterWin.
         * @member {number} ScatterWin
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterWin = 0;

        /**
         * RoundInfo LineWin.
         * @member {number} LineWin
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LineWin = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlbog.IColumn>} PlateSymbol
         * @memberof jlbog.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlbog.RoundInfo
         * @static
         * @param {jlbog.IRoundInfo=} [properties] Properties to set
         * @returns {jlbog.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlbog.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbog.RoundInfo
         * @static
         * @param {jlbog.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbolClient != null && message.PlateSymbolClient.length)
                for (let i = 0; i < message.PlateSymbolClient.length; ++i)
                    $root.jlbog.Column.encode(message.PlateSymbolClient[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.WildExpandColumn != null && message.WildExpandColumn.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.WildExpandColumn.length; ++i)
                    writer.bool(message.WildExpandColumn[i]);
                writer.ldelim();
            }
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlbog.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.FreeExpandInfo != null && message.FreeExpandInfo.length)
                for (let i = 0; i < message.FreeExpandInfo.length; ++i)
                    $root.jlbog.ExpandInfo.encode(message.FreeExpandInfo[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.ScatterWin);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.LineWin);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RoundWin);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.MaxFlag);
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlbog.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlbog.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbog.RoundInfo
         * @static
         * @param {jlbog.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbog.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbog.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbog.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbolClient && message.PlateSymbolClient.length))
                            message.PlateSymbolClient = [];
                        message.PlateSymbolClient.push($root.jlbog.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.WildExpandColumn && message.WildExpandColumn.length))
                            message.WildExpandColumn = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WildExpandColumn.push(reader.bool());
                        } else
                            message.WildExpandColumn.push(reader.bool());
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlbog.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.FreeExpandInfo && message.FreeExpandInfo.length))
                            message.FreeExpandInfo = [];
                        message.FreeExpandInfo.push($root.jlbog.ExpandInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 8: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 9: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 10: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 11: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlbog.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbog.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbog.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlbog.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbolClient != null && message.hasOwnProperty("PlateSymbolClient")) {
                if (!Array.isArray(message.PlateSymbolClient))
                    return "PlateSymbolClient: array expected";
                for (let i = 0; i < message.PlateSymbolClient.length; ++i) {
                    let error = $root.jlbog.Column.verify(message.PlateSymbolClient[i]);
                    if (error)
                        return "PlateSymbolClient." + error;
                }
            }
            if (message.WildExpandColumn != null && message.hasOwnProperty("WildExpandColumn")) {
                if (!Array.isArray(message.WildExpandColumn))
                    return "WildExpandColumn: array expected";
                for (let i = 0; i < message.WildExpandColumn.length; ++i)
                    if (typeof message.WildExpandColumn[i] !== "boolean")
                        return "WildExpandColumn: boolean[] expected";
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlbog.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeExpandInfo != null && message.hasOwnProperty("FreeExpandInfo")) {
                if (!Array.isArray(message.FreeExpandInfo))
                    return "FreeExpandInfo: array expected";
                for (let i = 0; i < message.FreeExpandInfo.length; ++i) {
                    let error = $root.jlbog.ExpandInfo.verify(message.FreeExpandInfo[i]);
                    if (error)
                        return "FreeExpandInfo." + error;
                }
            }
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlbog.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbog.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbog.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbog.RoundInfo)
                return object;
            let message = new $root.jlbog.RoundInfo();
            if (object.PlateSymbolClient) {
                if (!Array.isArray(object.PlateSymbolClient))
                    throw TypeError(".jlbog.RoundInfo.PlateSymbolClient: array expected");
                message.PlateSymbolClient = [];
                for (let i = 0; i < object.PlateSymbolClient.length; ++i) {
                    if (typeof object.PlateSymbolClient[i] !== "object")
                        throw TypeError(".jlbog.RoundInfo.PlateSymbolClient: object expected");
                    message.PlateSymbolClient[i] = $root.jlbog.Column.fromObject(object.PlateSymbolClient[i]);
                }
            }
            if (object.WildExpandColumn) {
                if (!Array.isArray(object.WildExpandColumn))
                    throw TypeError(".jlbog.RoundInfo.WildExpandColumn: array expected");
                message.WildExpandColumn = [];
                for (let i = 0; i < object.WildExpandColumn.length; ++i)
                    message.WildExpandColumn[i] = Boolean(object.WildExpandColumn[i]);
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlbog.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlbog.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlbog.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeExpandInfo) {
                if (!Array.isArray(object.FreeExpandInfo))
                    throw TypeError(".jlbog.RoundInfo.FreeExpandInfo: array expected");
                message.FreeExpandInfo = [];
                for (let i = 0; i < object.FreeExpandInfo.length; ++i) {
                    if (typeof object.FreeExpandInfo[i] !== "object")
                        throw TypeError(".jlbog.RoundInfo.FreeExpandInfo: object expected");
                    message.FreeExpandInfo[i] = $root.jlbog.ExpandInfo.fromObject(object.FreeExpandInfo[i]);
                }
            }
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlbog.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlbog.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlbog.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbog.RoundInfo
         * @static
         * @param {jlbog.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbolClient = [];
                object.WildExpandColumn = [];
                object.AwardDataVec = [];
                object.FreeExpandInfo = [];
                object.PlateSymbol = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.ScatterWin = 0;
                object.LineWin = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbolClient && message.PlateSymbolClient.length) {
                object.PlateSymbolClient = [];
                for (let j = 0; j < message.PlateSymbolClient.length; ++j)
                    object.PlateSymbolClient[j] = $root.jlbog.Column.toObject(message.PlateSymbolClient[j], options);
            }
            if (message.WildExpandColumn && message.WildExpandColumn.length) {
                object.WildExpandColumn = [];
                for (let j = 0; j < message.WildExpandColumn.length; ++j)
                    object.WildExpandColumn[j] = message.WildExpandColumn[j];
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlbog.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeExpandInfo && message.FreeExpandInfo.length) {
                object.FreeExpandInfo = [];
                for (let j = 0; j < message.FreeExpandInfo.length; ++j)
                    object.FreeExpandInfo[j] = $root.jlbog.ExpandInfo.toObject(message.FreeExpandInfo[j], options);
            }
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlbog.Column.toObject(message.PlateSymbol[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlbog.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlbog.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbog.RoundInfo";
        };

        return RoundInfo;
    })();

    jlbog.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbog
         * @interface ISpinAck
         * @property {Array.<jlbog.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {Array.<number>|null} [FreeFrameSymbol] SpinAck FreeFrameSymbol
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbog
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbog.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.FreeFrameSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlbog.IRoundInfo>} RoundQueue
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck FreeFrameSymbol.
         * @member {Array.<number>} FreeFrameSymbol
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeFrameSymbol = $util.emptyArray;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlbog.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbog.SpinAck
         * @static
         * @param {jlbog.ISpinAck=} [properties] Properties to set
         * @returns {jlbog.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbog.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbog.SpinAck
         * @static
         * @param {jlbog.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlbog.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.FreeFrameSymbol != null && message.FreeFrameSymbol.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.FreeFrameSymbol.length; ++i)
                    writer.int32(message.FreeFrameSymbol[i]);
                writer.ldelim();
            }
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbog.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbog.SpinAck
         * @static
         * @param {jlbog.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbog.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbog.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbog.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlbog.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.FreeFrameSymbol && message.FreeFrameSymbol.length))
                            message.FreeFrameSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FreeFrameSymbol.push(reader.int32());
                        } else
                            message.FreeFrameSymbol.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbog.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbog.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbog.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlbog.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.FreeFrameSymbol != null && message.hasOwnProperty("FreeFrameSymbol")) {
                if (!Array.isArray(message.FreeFrameSymbol))
                    return "FreeFrameSymbol: array expected";
                for (let i = 0; i < message.FreeFrameSymbol.length; ++i)
                    if (!$util.isInteger(message.FreeFrameSymbol[i]))
                        return "FreeFrameSymbol: integer[] expected";
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbog.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbog.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbog.SpinAck)
                return object;
            let message = new $root.jlbog.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlbog.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlbog.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlbog.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.FreeFrameSymbol) {
                if (!Array.isArray(object.FreeFrameSymbol))
                    throw TypeError(".jlbog.SpinAck.FreeFrameSymbol: array expected");
                message.FreeFrameSymbol = [];
                for (let i = 0; i < object.FreeFrameSymbol.length; ++i)
                    message.FreeFrameSymbol[i] = object.FreeFrameSymbol[i] | 0;
            }
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbog.SpinAck
         * @static
         * @param {jlbog.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.FreeFrameSymbol = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlbog.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.FreeFrameSymbol && message.FreeFrameSymbol.length) {
                object.FreeFrameSymbol = [];
                for (let j = 0; j < message.FreeFrameSymbol.length; ++j)
                    object.FreeFrameSymbol[j] = message.FreeFrameSymbol[j];
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbog.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbog.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbog.SpinAck";
        };

        return SpinAck;
    })();

    jlbog.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlbog
         * @interface IGameInfoData
         * @property {number|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlbog
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlbog.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {number} ShowExtra
         * @memberof jlbog.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = 0;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlbog.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlbog.GameInfoData
         * @static
         * @param {jlbog.IGameInfoData=} [properties] Properties to set
         * @returns {jlbog.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlbog.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlbog.GameInfoData
         * @static
         * @param {jlbog.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlbog.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbog.GameInfoData
         * @static
         * @param {jlbog.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbog.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbog.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbog.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.double();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbog.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbog.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlbog.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "number")
                    return "ShowExtra: number expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbog.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbog.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbog.GameInfoData)
                return object;
            let message = new $root.jlbog.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Number(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlbog.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbog.GameInfoData
         * @static
         * @param {jlbog.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = 0;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = options.json && !isFinite(message.ShowExtra) ? String(message.ShowExtra) : message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlbog.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlbog.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbog.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlbog;
})();

export const jlbs2 = $root.jlbs2 = (() => {

    /**
     * Namespace jlbs2.
     * @exports jlbs2
     * @namespace
     */
    const jlbs2 = {};

    jlbs2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlbs2
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlbs2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlbs2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlbs2.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlbs2.Column
         * @static
         * @param {jlbs2.IColumn=} [properties] Properties to set
         * @returns {jlbs2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlbs2.Column.verify|verify} messages.
         * @function encode
         * @memberof jlbs2.Column
         * @static
         * @param {jlbs2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlbs2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbs2.Column
         * @static
         * @param {jlbs2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlbs2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbs2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbs2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbs2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbs2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlbs2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbs2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbs2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbs2.Column)
                return object;
            let message = new $root.jlbs2.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlbs2.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbs2.Column
         * @static
         * @param {jlbs2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlbs2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlbs2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbs2.Column";
        };

        return Column;
    })();

    jlbs2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlbs2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Mult] AwardData Mult
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlbs2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlbs2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlbs2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlbs2.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlbs2.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlbs2.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlbs2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlbs2.AwardData
         * @static
         * @param {jlbs2.IAwardData=} [properties] Properties to set
         * @returns {jlbs2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlbs2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlbs2.AwardData
         * @static
         * @param {jlbs2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlbs2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbs2.AwardData
         * @static
         * @param {jlbs2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbs2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbs2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbs2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbs2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbs2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlbs2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbs2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbs2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbs2.AwardData)
                return object;
            let message = new $root.jlbs2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbs2.AwardData
         * @static
         * @param {jlbs2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Mult = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlbs2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlbs2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbs2.AwardData";
        };

        return AwardData;
    })();

    jlbs2.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlbs2
         * @interface IRoundInfo
         * @property {Array.<jlbs2.IColumn>|null} [PlateSymbolExtend] RoundInfo PlateSymbolExtend
         * @property {Array.<jlbs2.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<number>|null} [ColumnLength] RoundInfo ColumnLength
         * @property {Array.<boolean>|null} [ArrowTag] RoundInfo ArrowTag
         * @property {Array.<boolean>|null} [GoldArrowTag] RoundInfo GoldArrowTag
         * @property {number|null} [HitStampCol] RoundInfo HitStampCol
         * @property {Array.<number>|null} [StampedeSymbols] RoundInfo StampedeSymbols
         * @property {Array.<jlbs2.IColumn>|null} [PlateSymbolLog] RoundInfo PlateSymbolLog
         * @property {Array.<number>|null} [LastLength] RoundInfo LastLength
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlbs2
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlbs2.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbolExtend = [];
            this.AwardDataVec = [];
            this.ColumnLength = [];
            this.ArrowTag = [];
            this.GoldArrowTag = [];
            this.StampedeSymbols = [];
            this.PlateSymbolLog = [];
            this.LastLength = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbolExtend.
         * @member {Array.<jlbs2.IColumn>} PlateSymbolExtend
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolExtend = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlbs2.IAwardData>} AwardDataVec
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo ColumnLength.
         * @member {Array.<number>} ColumnLength
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ColumnLength = $util.emptyArray;

        /**
         * RoundInfo ArrowTag.
         * @member {Array.<boolean>} ArrowTag
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ArrowTag = $util.emptyArray;

        /**
         * RoundInfo GoldArrowTag.
         * @member {Array.<boolean>} GoldArrowTag
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GoldArrowTag = $util.emptyArray;

        /**
         * RoundInfo HitStampCol.
         * @member {number} HitStampCol
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.HitStampCol = 0;

        /**
         * RoundInfo StampedeSymbols.
         * @member {Array.<number>} StampedeSymbols
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.StampedeSymbols = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolLog.
         * @member {Array.<jlbs2.IColumn>} PlateSymbolLog
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo LastLength.
         * @member {Array.<number>} LastLength
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LastLength = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlbs2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {jlbs2.IRoundInfo=} [properties] Properties to set
         * @returns {jlbs2.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlbs2.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {jlbs2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbolExtend != null && message.PlateSymbolExtend.length)
                for (let i = 0; i < message.PlateSymbolExtend.length; ++i)
                    $root.jlbs2.Column.encode(message.PlateSymbolExtend[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlbs2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RoundWin);
            if (message.ColumnLength != null && message.ColumnLength.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.ColumnLength.length; ++i)
                    writer.int32(message.ColumnLength[i]);
                writer.ldelim();
            }
            if (message.ArrowTag != null && message.ArrowTag.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.ArrowTag.length; ++i)
                    writer.bool(message.ArrowTag[i]);
                writer.ldelim();
            }
            if (message.GoldArrowTag != null && message.GoldArrowTag.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.GoldArrowTag.length; ++i)
                    writer.bool(message.GoldArrowTag[i]);
                writer.ldelim();
            }
            if (message.HitStampCol != null && Object.hasOwnProperty.call(message, "HitStampCol"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.HitStampCol);
            if (message.StampedeSymbols != null && message.StampedeSymbols.length) {
                writer.uint32(/* id 12, wireType 2 =*/98).fork();
                for (let i = 0; i < message.StampedeSymbols.length; ++i)
                    writer.int32(message.StampedeSymbols[i]);
                writer.ldelim();
            }
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlbs2.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.LastLength != null && message.LastLength.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (let i = 0; i < message.LastLength.length; ++i)
                    writer.int32(message.LastLength[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlbs2.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {jlbs2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbs2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbs2.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbolExtend && message.PlateSymbolExtend.length))
                            message.PlateSymbolExtend = [];
                        message.PlateSymbolExtend.push($root.jlbs2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlbs2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 8: {
                        if (!(message.ColumnLength && message.ColumnLength.length))
                            message.ColumnLength = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ColumnLength.push(reader.int32());
                        } else
                            message.ColumnLength.push(reader.int32());
                        break;
                    }
                case 9: {
                        if (!(message.ArrowTag && message.ArrowTag.length))
                            message.ArrowTag = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ArrowTag.push(reader.bool());
                        } else
                            message.ArrowTag.push(reader.bool());
                        break;
                    }
                case 10: {
                        if (!(message.GoldArrowTag && message.GoldArrowTag.length))
                            message.GoldArrowTag = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.GoldArrowTag.push(reader.bool());
                        } else
                            message.GoldArrowTag.push(reader.bool());
                        break;
                    }
                case 11: {
                        message.HitStampCol = reader.int32();
                        break;
                    }
                case 12: {
                        if (!(message.StampedeSymbols && message.StampedeSymbols.length))
                            message.StampedeSymbols = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.StampedeSymbols.push(reader.int32());
                        } else
                            message.StampedeSymbols.push(reader.int32());
                        break;
                    }
                case 13: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlbs2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 14: {
                        if (!(message.LastLength && message.LastLength.length))
                            message.LastLength = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.LastLength.push(reader.int32());
                        } else
                            message.LastLength.push(reader.int32());
                        break;
                    }
                case 15: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbs2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbolExtend != null && message.hasOwnProperty("PlateSymbolExtend")) {
                if (!Array.isArray(message.PlateSymbolExtend))
                    return "PlateSymbolExtend: array expected";
                for (let i = 0; i < message.PlateSymbolExtend.length; ++i) {
                    let error = $root.jlbs2.Column.verify(message.PlateSymbolExtend[i]);
                    if (error)
                        return "PlateSymbolExtend." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlbs2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.ColumnLength != null && message.hasOwnProperty("ColumnLength")) {
                if (!Array.isArray(message.ColumnLength))
                    return "ColumnLength: array expected";
                for (let i = 0; i < message.ColumnLength.length; ++i)
                    if (!$util.isInteger(message.ColumnLength[i]))
                        return "ColumnLength: integer[] expected";
            }
            if (message.ArrowTag != null && message.hasOwnProperty("ArrowTag")) {
                if (!Array.isArray(message.ArrowTag))
                    return "ArrowTag: array expected";
                for (let i = 0; i < message.ArrowTag.length; ++i)
                    if (typeof message.ArrowTag[i] !== "boolean")
                        return "ArrowTag: boolean[] expected";
            }
            if (message.GoldArrowTag != null && message.hasOwnProperty("GoldArrowTag")) {
                if (!Array.isArray(message.GoldArrowTag))
                    return "GoldArrowTag: array expected";
                for (let i = 0; i < message.GoldArrowTag.length; ++i)
                    if (typeof message.GoldArrowTag[i] !== "boolean")
                        return "GoldArrowTag: boolean[] expected";
            }
            if (message.HitStampCol != null && message.hasOwnProperty("HitStampCol"))
                if (!$util.isInteger(message.HitStampCol))
                    return "HitStampCol: integer expected";
            if (message.StampedeSymbols != null && message.hasOwnProperty("StampedeSymbols")) {
                if (!Array.isArray(message.StampedeSymbols))
                    return "StampedeSymbols: array expected";
                for (let i = 0; i < message.StampedeSymbols.length; ++i)
                    if (!$util.isInteger(message.StampedeSymbols[i]))
                        return "StampedeSymbols: integer[] expected";
            }
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlbs2.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            if (message.LastLength != null && message.hasOwnProperty("LastLength")) {
                if (!Array.isArray(message.LastLength))
                    return "LastLength: array expected";
                for (let i = 0; i < message.LastLength.length; ++i)
                    if (!$util.isInteger(message.LastLength[i]))
                        return "LastLength: integer[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbs2.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbs2.RoundInfo)
                return object;
            let message = new $root.jlbs2.RoundInfo();
            if (object.PlateSymbolExtend) {
                if (!Array.isArray(object.PlateSymbolExtend))
                    throw TypeError(".jlbs2.RoundInfo.PlateSymbolExtend: array expected");
                message.PlateSymbolExtend = [];
                for (let i = 0; i < object.PlateSymbolExtend.length; ++i) {
                    if (typeof object.PlateSymbolExtend[i] !== "object")
                        throw TypeError(".jlbs2.RoundInfo.PlateSymbolExtend: object expected");
                    message.PlateSymbolExtend[i] = $root.jlbs2.Column.fromObject(object.PlateSymbolExtend[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlbs2.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlbs2.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlbs2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.ColumnLength) {
                if (!Array.isArray(object.ColumnLength))
                    throw TypeError(".jlbs2.RoundInfo.ColumnLength: array expected");
                message.ColumnLength = [];
                for (let i = 0; i < object.ColumnLength.length; ++i)
                    message.ColumnLength[i] = object.ColumnLength[i] | 0;
            }
            if (object.ArrowTag) {
                if (!Array.isArray(object.ArrowTag))
                    throw TypeError(".jlbs2.RoundInfo.ArrowTag: array expected");
                message.ArrowTag = [];
                for (let i = 0; i < object.ArrowTag.length; ++i)
                    message.ArrowTag[i] = Boolean(object.ArrowTag[i]);
            }
            if (object.GoldArrowTag) {
                if (!Array.isArray(object.GoldArrowTag))
                    throw TypeError(".jlbs2.RoundInfo.GoldArrowTag: array expected");
                message.GoldArrowTag = [];
                for (let i = 0; i < object.GoldArrowTag.length; ++i)
                    message.GoldArrowTag[i] = Boolean(object.GoldArrowTag[i]);
            }
            if (object.HitStampCol != null)
                message.HitStampCol = object.HitStampCol | 0;
            if (object.StampedeSymbols) {
                if (!Array.isArray(object.StampedeSymbols))
                    throw TypeError(".jlbs2.RoundInfo.StampedeSymbols: array expected");
                message.StampedeSymbols = [];
                for (let i = 0; i < object.StampedeSymbols.length; ++i)
                    message.StampedeSymbols[i] = object.StampedeSymbols[i] | 0;
            }
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlbs2.RoundInfo.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlbs2.RoundInfo.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlbs2.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            if (object.LastLength) {
                if (!Array.isArray(object.LastLength))
                    throw TypeError(".jlbs2.RoundInfo.LastLength: array expected");
                message.LastLength = [];
                for (let i = 0; i < object.LastLength.length; ++i)
                    message.LastLength[i] = object.LastLength[i] | 0;
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {jlbs2.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbolExtend = [];
                object.AwardDataVec = [];
                object.ColumnLength = [];
                object.ArrowTag = [];
                object.GoldArrowTag = [];
                object.StampedeSymbols = [];
                object.PlateSymbolLog = [];
                object.LastLength = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.RoundWin = 0;
                object.HitStampCol = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbolExtend && message.PlateSymbolExtend.length) {
                object.PlateSymbolExtend = [];
                for (let j = 0; j < message.PlateSymbolExtend.length; ++j)
                    object.PlateSymbolExtend[j] = $root.jlbs2.Column.toObject(message.PlateSymbolExtend[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlbs2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.ColumnLength && message.ColumnLength.length) {
                object.ColumnLength = [];
                for (let j = 0; j < message.ColumnLength.length; ++j)
                    object.ColumnLength[j] = message.ColumnLength[j];
            }
            if (message.ArrowTag && message.ArrowTag.length) {
                object.ArrowTag = [];
                for (let j = 0; j < message.ArrowTag.length; ++j)
                    object.ArrowTag[j] = message.ArrowTag[j];
            }
            if (message.GoldArrowTag && message.GoldArrowTag.length) {
                object.GoldArrowTag = [];
                for (let j = 0; j < message.GoldArrowTag.length; ++j)
                    object.GoldArrowTag[j] = message.GoldArrowTag[j];
            }
            if (message.HitStampCol != null && message.hasOwnProperty("HitStampCol"))
                object.HitStampCol = message.HitStampCol;
            if (message.StampedeSymbols && message.StampedeSymbols.length) {
                object.StampedeSymbols = [];
                for (let j = 0; j < message.StampedeSymbols.length; ++j)
                    object.StampedeSymbols[j] = message.StampedeSymbols[j];
            }
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlbs2.Column.toObject(message.PlateSymbolLog[j], options);
            }
            if (message.LastLength && message.LastLength.length) {
                object.LastLength = [];
                for (let j = 0; j < message.LastLength.length; ++j)
                    object.LastLength[j] = message.LastLength[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlbs2.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlbs2.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbs2.RoundInfo";
        };

        return RoundInfo;
    })();

    jlbs2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlbs2
         * @interface ISpinAck
         * @property {Array.<jlbs2.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {Array.<boolean>|null} [HitFGX3X5Col] SpinAck HitFGX3X5Col
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlbs2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlbs2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.HitFGX3X5Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlbs2.IRoundInfo>} RoundQueue
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck HitFGX3X5Col.
         * @member {Array.<boolean>} HitFGX3X5Col
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.HitFGX3X5Col = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlbs2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlbs2.SpinAck
         * @static
         * @param {jlbs2.ISpinAck=} [properties] Properties to set
         * @returns {jlbs2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlbs2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlbs2.SpinAck
         * @static
         * @param {jlbs2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlbs2.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.HitFGX3X5Col != null && message.HitFGX3X5Col.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.HitFGX3X5Col.length; ++i)
                    writer.bool(message.HitFGX3X5Col[i]);
                writer.ldelim();
            }
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlbs2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbs2.SpinAck
         * @static
         * @param {jlbs2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlbs2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbs2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbs2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlbs2.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.HitFGX3X5Col && message.HitFGX3X5Col.length))
                            message.HitFGX3X5Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.HitFGX3X5Col.push(reader.bool());
                        } else
                            message.HitFGX3X5Col.push(reader.bool());
                        break;
                    }
                case 3: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbs2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbs2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlbs2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlbs2.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.HitFGX3X5Col != null && message.hasOwnProperty("HitFGX3X5Col")) {
                if (!Array.isArray(message.HitFGX3X5Col))
                    return "HitFGX3X5Col: array expected";
                for (let i = 0; i < message.HitFGX3X5Col.length; ++i)
                    if (typeof message.HitFGX3X5Col[i] !== "boolean")
                        return "HitFGX3X5Col: boolean[] expected";
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbs2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbs2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbs2.SpinAck)
                return object;
            let message = new $root.jlbs2.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlbs2.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlbs2.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlbs2.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.HitFGX3X5Col) {
                if (!Array.isArray(object.HitFGX3X5Col))
                    throw TypeError(".jlbs2.SpinAck.HitFGX3X5Col: array expected");
                message.HitFGX3X5Col = [];
                for (let i = 0; i < object.HitFGX3X5Col.length; ++i)
                    message.HitFGX3X5Col[i] = Boolean(object.HitFGX3X5Col[i]);
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbs2.SpinAck
         * @static
         * @param {jlbs2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.HitFGX3X5Col = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlbs2.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.HitFGX3X5Col && message.HitFGX3X5Col.length) {
                object.HitFGX3X5Col = [];
                for (let j = 0; j < message.HitFGX3X5Col.length; ++j)
                    object.HitFGX3X5Col[j] = message.HitFGX3X5Col[j];
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlbs2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlbs2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbs2.SpinAck";
        };

        return SpinAck;
    })();

    jlbs2.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlbs2
         * @interface IStateInfo
         * @property {Array.<number>|null} [ColumnShowLength] StateInfo ColumnShowLength
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlbs2
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlbs2.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.ColumnShowLength = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo ColumnShowLength.
         * @member {Array.<number>} ColumnShowLength
         * @memberof jlbs2.StateInfo
         * @instance
         */
        StateInfo.prototype.ColumnShowLength = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlbs2.StateInfo
         * @static
         * @param {jlbs2.IStateInfo=} [properties] Properties to set
         * @returns {jlbs2.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlbs2.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlbs2.StateInfo
         * @static
         * @param {jlbs2.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ColumnShowLength != null && message.ColumnShowLength.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.ColumnShowLength.length; ++i)
                    writer.int32(message.ColumnShowLength[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlbs2.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbs2.StateInfo
         * @static
         * @param {jlbs2.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlbs2.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbs2.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbs2.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ColumnShowLength && message.ColumnShowLength.length))
                            message.ColumnShowLength = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ColumnShowLength.push(reader.int32());
                        } else
                            message.ColumnShowLength.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbs2.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbs2.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlbs2.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ColumnShowLength != null && message.hasOwnProperty("ColumnShowLength")) {
                if (!Array.isArray(message.ColumnShowLength))
                    return "ColumnShowLength: array expected";
                for (let i = 0; i < message.ColumnShowLength.length; ++i)
                    if (!$util.isInteger(message.ColumnShowLength[i]))
                        return "ColumnShowLength: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbs2.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbs2.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbs2.StateInfo)
                return object;
            let message = new $root.jlbs2.StateInfo();
            if (object.ColumnShowLength) {
                if (!Array.isArray(object.ColumnShowLength))
                    throw TypeError(".jlbs2.StateInfo.ColumnShowLength: array expected");
                message.ColumnShowLength = [];
                for (let i = 0; i < object.ColumnShowLength.length; ++i)
                    message.ColumnShowLength[i] = object.ColumnShowLength[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbs2.StateInfo
         * @static
         * @param {jlbs2.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ColumnShowLength = [];
            if (message.ColumnShowLength && message.ColumnShowLength.length) {
                object.ColumnShowLength = [];
                for (let j = 0; j < message.ColumnShowLength.length; ++j)
                    object.ColumnShowLength[j] = message.ColumnShowLength[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlbs2.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlbs2.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbs2.StateInfo";
        };

        return StateInfo;
    })();

    jlbs2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlbs2
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [Length] GameInfoData Length
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlbs2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlbs2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.Length = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlbs2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlbs2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData Length.
         * @member {Array.<number>} Length
         * @memberof jlbs2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Length = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {jlbs2.IGameInfoData=} [properties] Properties to set
         * @returns {jlbs2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlbs2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {jlbs2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.Length != null && message.Length.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.Length.length; ++i)
                    writer.int32(message.Length[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlbs2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {jlbs2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlbs2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlbs2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.Length && message.Length.length))
                            message.Length = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Length.push(reader.int32());
                        } else
                            message.Length.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlbs2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.Length != null && message.hasOwnProperty("Length")) {
                if (!Array.isArray(message.Length))
                    return "Length: array expected";
                for (let i = 0; i < message.Length.length; ++i)
                    if (!$util.isInteger(message.Length[i]))
                        return "Length: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlbs2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlbs2.GameInfoData)
                return object;
            let message = new $root.jlbs2.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlbs2.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.Length) {
                if (!Array.isArray(object.Length))
                    throw TypeError(".jlbs2.GameInfoData.Length: array expected");
                message.Length = [];
                for (let i = 0; i < object.Length.length; ++i)
                    message.Length[i] = object.Length[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {jlbs2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.Length = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.Length && message.Length.length) {
                object.Length = [];
                for (let j = 0; j < message.Length.length; ++j)
                    object.Length[j] = message.Length[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlbs2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlbs2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlbs2.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlbs2;
})();

export const jlc777 = $root.jlc777 = (() => {

    /**
     * Namespace jlc777.
     * @exports jlc777
     * @namespace
     */
    const jlc777 = {};

    jlc777.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlc777
         * @interface IGameInfoData
         * @property {Array.<number>|null} [OddList] GameInfoData OddList
         * @property {number|null} [OddFactor] GameInfoData OddFactor
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlc777
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlc777.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.OddList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData OddList.
         * @member {Array.<number>} OddList
         * @memberof jlc777.GameInfoData
         * @instance
         */
        GameInfoData.prototype.OddList = $util.emptyArray;

        /**
         * GameInfoData OddFactor.
         * @member {number} OddFactor
         * @memberof jlc777.GameInfoData
         * @instance
         */
        GameInfoData.prototype.OddFactor = 0;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlc777.GameInfoData
         * @static
         * @param {jlc777.IGameInfoData=} [properties] Properties to set
         * @returns {jlc777.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlc777.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlc777.GameInfoData
         * @static
         * @param {jlc777.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.OddList != null && message.OddList.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.OddList.length; ++i)
                    writer.double(message.OddList[i]);
                writer.ldelim();
            }
            if (message.OddFactor != null && Object.hasOwnProperty.call(message, "OddFactor"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.OddFactor);
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlc777.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlc777.GameInfoData
         * @static
         * @param {jlc777.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlc777.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlc777.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlc777.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 7: {
                        if (!(message.OddList && message.OddList.length))
                            message.OddList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.OddList.push(reader.double());
                        } else
                            message.OddList.push(reader.double());
                        break;
                    }
                case 8: {
                        message.OddFactor = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlc777.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlc777.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlc777.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.OddList != null && message.hasOwnProperty("OddList")) {
                if (!Array.isArray(message.OddList))
                    return "OddList: array expected";
                for (let i = 0; i < message.OddList.length; ++i)
                    if (typeof message.OddList[i] !== "number")
                        return "OddList: number[] expected";
            }
            if (message.OddFactor != null && message.hasOwnProperty("OddFactor"))
                if (typeof message.OddFactor !== "number")
                    return "OddFactor: number expected";
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlc777.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlc777.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlc777.GameInfoData)
                return object;
            let message = new $root.jlc777.GameInfoData();
            if (object.OddList) {
                if (!Array.isArray(object.OddList))
                    throw TypeError(".jlc777.GameInfoData.OddList: array expected");
                message.OddList = [];
                for (let i = 0; i < object.OddList.length; ++i)
                    message.OddList[i] = Number(object.OddList[i]);
            }
            if (object.OddFactor != null)
                message.OddFactor = Number(object.OddFactor);
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlc777.GameInfoData
         * @static
         * @param {jlc777.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.OddList = [];
            if (options.defaults)
                object.OddFactor = 0;
            if (message.OddList && message.OddList.length) {
                object.OddList = [];
                for (let j = 0; j < message.OddList.length; ++j)
                    object.OddList[j] = options.json && !isFinite(message.OddList[j]) ? String(message.OddList[j]) : message.OddList[j];
            }
            if (message.OddFactor != null && message.hasOwnProperty("OddFactor"))
                object.OddFactor = options.json && !isFinite(message.OddFactor) ? String(message.OddFactor) : message.OddFactor;
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlc777.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlc777.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlc777.GameInfoData";
        };

        return GameInfoData;
    })();

    jlc777.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlc777
         * @interface ISpinAck
         * @property {number|null} [Award] SpinAck Award
         * @property {Array.<number>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {number|null} [RespinTimes] SpinAck RespinTimes
         * @property {number|null} [Mult] SpinAck Mult
         * @property {number|null} [PlateWin] SpinAck PlateWin
         * @property {number|null} [MultWin] SpinAck MultWin
         * @property {number|null} [ExtraWin] SpinAck ExtraWin
         * @property {number|null} [RespinWin] SpinAck RespinWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlc777
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlc777.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck Award.
         * @member {number} Award
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.Award = 0;

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<number>} PlateSymbol
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck RespinTimes.
         * @member {number} RespinTimes
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.RespinTimes = 0;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck PlateWin.
         * @member {number} PlateWin
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateWin = 0;

        /**
         * SpinAck MultWin.
         * @member {number} MultWin
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.MultWin = 0;

        /**
         * SpinAck ExtraWin.
         * @member {number} ExtraWin
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.ExtraWin = 0;

        /**
         * SpinAck RespinWin.
         * @member {number} RespinWin
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.RespinWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlc777.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlc777.SpinAck
         * @static
         * @param {jlc777.ISpinAck=} [properties] Properties to set
         * @returns {jlc777.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlc777.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlc777.SpinAck
         * @static
         * @param {jlc777.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Award != null && Object.hasOwnProperty.call(message, "Award"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Award);
            if (message.PlateSymbol != null && message.PlateSymbol.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.int32(message.PlateSymbol[i]);
                writer.ldelim();
            }
            if (message.RespinTimes != null && Object.hasOwnProperty.call(message, "RespinTimes"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.RespinTimes);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.PlateWin != null && Object.hasOwnProperty.call(message, "PlateWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.PlateWin);
            if (message.MultWin != null && Object.hasOwnProperty.call(message, "MultWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.MultWin);
            if (message.ExtraWin != null && Object.hasOwnProperty.call(message, "ExtraWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.ExtraWin);
            if (message.RespinWin != null && Object.hasOwnProperty.call(message, "RespinWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RespinWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.AckType);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlc777.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlc777.SpinAck
         * @static
         * @param {jlc777.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlc777.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlc777.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlc777.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Award = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateSymbol.push(reader.int32());
                        } else
                            message.PlateSymbol.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.RespinTimes = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.PlateWin = reader.double();
                        break;
                    }
                case 6: {
                        message.MultWin = reader.double();
                        break;
                    }
                case 7: {
                        message.ExtraWin = reader.double();
                        break;
                    }
                case 8: {
                        message.RespinWin = reader.double();
                        break;
                    }
                case 9: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 10: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 11: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 12: {
                        message.AckType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlc777.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlc777.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlc777.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Award != null && message.hasOwnProperty("Award"))
                if (!$util.isInteger(message.Award))
                    return "Award: integer expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isInteger(message.PlateSymbol[i]))
                        return "PlateSymbol: integer[] expected";
            }
            if (message.RespinTimes != null && message.hasOwnProperty("RespinTimes"))
                if (!$util.isInteger(message.RespinTimes))
                    return "RespinTimes: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                if (typeof message.PlateWin !== "number")
                    return "PlateWin: number expected";
            if (message.MultWin != null && message.hasOwnProperty("MultWin"))
                if (typeof message.MultWin !== "number")
                    return "MultWin: number expected";
            if (message.ExtraWin != null && message.hasOwnProperty("ExtraWin"))
                if (typeof message.ExtraWin !== "number")
                    return "ExtraWin: number expected";
            if (message.RespinWin != null && message.hasOwnProperty("RespinWin"))
                if (typeof message.RespinWin !== "number")
                    return "RespinWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlc777.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlc777.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlc777.SpinAck)
                return object;
            let message = new $root.jlc777.SpinAck();
            if (object.Award != null)
                message.Award = object.Award | 0;
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlc777.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = object.PlateSymbol[i] | 0;
            }
            if (object.RespinTimes != null)
                message.RespinTimes = object.RespinTimes | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.PlateWin != null)
                message.PlateWin = Number(object.PlateWin);
            if (object.MultWin != null)
                message.MultWin = Number(object.MultWin);
            if (object.ExtraWin != null)
                message.ExtraWin = Number(object.ExtraWin);
            if (object.RespinWin != null)
                message.RespinWin = Number(object.RespinWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlc777.SpinAck
         * @static
         * @param {jlc777.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateSymbol = [];
            if (options.defaults) {
                object.Award = 0;
                object.RespinTimes = 0;
                object.Mult = 0;
                object.PlateWin = 0;
                object.MultWin = 0;
                object.ExtraWin = 0;
                object.RespinWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                object.Award = message.Award;
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.RespinTimes != null && message.hasOwnProperty("RespinTimes"))
                object.RespinTimes = message.RespinTimes;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                object.PlateWin = options.json && !isFinite(message.PlateWin) ? String(message.PlateWin) : message.PlateWin;
            if (message.MultWin != null && message.hasOwnProperty("MultWin"))
                object.MultWin = options.json && !isFinite(message.MultWin) ? String(message.MultWin) : message.MultWin;
            if (message.ExtraWin != null && message.hasOwnProperty("ExtraWin"))
                object.ExtraWin = options.json && !isFinite(message.ExtraWin) ? String(message.ExtraWin) : message.ExtraWin;
            if (message.RespinWin != null && message.hasOwnProperty("RespinWin"))
                object.RespinWin = options.json && !isFinite(message.RespinWin) ? String(message.RespinWin) : message.RespinWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlc777.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlc777.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlc777.SpinAck";
        };

        return SpinAck;
    })();

    return jlc777;
})();

export const jlcb = $root.jlcb = (() => {

    /**
     * Namespace jlcb.
     * @exports jlcb
     * @namespace
     */
    const jlcb = {};

    jlcb.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcb
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Tag] AwardData Tag
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Count] AwardData Count
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcb
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcb.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlcb.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Tag.
         * @member {number} Tag
         * @memberof jlcb.AwardData
         * @instance
         */
        AwardData.prototype.Tag = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlcb.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlcb.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcb.AwardData
         * @static
         * @param {jlcb.IAwardData=} [properties] Properties to set
         * @returns {jlcb.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcb.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcb.AwardData
         * @static
         * @param {jlcb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Tag != null && Object.hasOwnProperty.call(message, "Tag"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Tag);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Count);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcb.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.AwardData
         * @static
         * @param {jlcb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Tag = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.Count = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcb.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Tag != null && message.hasOwnProperty("Tag"))
                if (!$util.isInteger(message.Tag))
                    return "Tag: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.AwardData)
                return object;
            let message = new $root.jlcb.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Tag != null)
                message.Tag = object.Tag | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Count != null)
                message.Count = object.Count | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.AwardData
         * @static
         * @param {jlcb.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Tag = 0;
                object.Win = 0;
                object.Count = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Tag != null && message.hasOwnProperty("Tag"))
                object.Tag = message.Tag;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcb.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcb.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.AwardData";
        };

        return AwardData;
    })();

    jlcb.SingleGem = (function() {

        /**
         * Properties of a SingleGem.
         * @memberof jlcb
         * @interface ISingleGem
         * @property {Array.<string>|null} [PlateSymbol] SingleGem PlateSymbol
         * @property {Array.<string>|null} [PlateTag] SingleGem PlateTag
         * @property {Array.<jlcb.IAwardData>|null} [AwardInfos] SingleGem AwardInfos
         * @property {number|null} [SingleWin] SingleGem SingleWin
         * @property {boolean|null} [HasUp] SingleGem HasUp
         */

        /**
         * Constructs a new SingleGem.
         * @memberof jlcb
         * @classdesc Represents a SingleGem.
         * @implements ISingleGem
         * @constructor
         * @param {jlcb.ISingleGem=} [properties] Properties to set
         */
        function SingleGem(properties) {
            this.PlateSymbol = [];
            this.PlateTag = [];
            this.AwardInfos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleGem PlateSymbol.
         * @member {Array.<string>} PlateSymbol
         * @memberof jlcb.SingleGem
         * @instance
         */
        SingleGem.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SingleGem PlateTag.
         * @member {Array.<string>} PlateTag
         * @memberof jlcb.SingleGem
         * @instance
         */
        SingleGem.prototype.PlateTag = $util.emptyArray;

        /**
         * SingleGem AwardInfos.
         * @member {Array.<jlcb.IAwardData>} AwardInfos
         * @memberof jlcb.SingleGem
         * @instance
         */
        SingleGem.prototype.AwardInfos = $util.emptyArray;

        /**
         * SingleGem SingleWin.
         * @member {number} SingleWin
         * @memberof jlcb.SingleGem
         * @instance
         */
        SingleGem.prototype.SingleWin = 0;

        /**
         * SingleGem HasUp.
         * @member {boolean} HasUp
         * @memberof jlcb.SingleGem
         * @instance
         */
        SingleGem.prototype.HasUp = false;

        /**
         * Creates a new SingleGem instance using the specified properties.
         * @function create
         * @memberof jlcb.SingleGem
         * @static
         * @param {jlcb.ISingleGem=} [properties] Properties to set
         * @returns {jlcb.SingleGem} SingleGem instance
         */
        SingleGem.create = function create(properties) {
            return new SingleGem(properties);
        };

        /**
         * Encodes the specified SingleGem message. Does not implicitly {@link jlcb.SingleGem.verify|verify} messages.
         * @function encode
         * @memberof jlcb.SingleGem
         * @static
         * @param {jlcb.ISingleGem} message SingleGem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleGem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.PlateSymbol[i]);
            if (message.PlateTag != null && message.PlateTag.length)
                for (let i = 0; i < message.PlateTag.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.PlateTag[i]);
            if (message.AwardInfos != null && message.AwardInfos.length)
                for (let i = 0; i < message.AwardInfos.length; ++i)
                    $root.jlcb.AwardData.encode(message.AwardInfos[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.SingleWin);
            if (message.HasUp != null && Object.hasOwnProperty.call(message, "HasUp"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.HasUp);
            return writer;
        };

        /**
         * Encodes the specified SingleGem message, length delimited. Does not implicitly {@link jlcb.SingleGem.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.SingleGem
         * @static
         * @param {jlcb.ISingleGem} message SingleGem message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleGem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleGem message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.SingleGem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.SingleGem} SingleGem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleGem.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.SingleGem();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push(reader.string());
                        break;
                    }
                case 2: {
                        if (!(message.PlateTag && message.PlateTag.length))
                            message.PlateTag = [];
                        message.PlateTag.push(reader.string());
                        break;
                    }
                case 3: {
                        if (!(message.AwardInfos && message.AwardInfos.length))
                            message.AwardInfos = [];
                        message.AwardInfos.push($root.jlcb.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 5: {
                        message.HasUp = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleGem message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.SingleGem
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.SingleGem} SingleGem
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleGem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleGem message.
         * @function verify
         * @memberof jlcb.SingleGem
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleGem.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isString(message.PlateSymbol[i]))
                        return "PlateSymbol: string[] expected";
            }
            if (message.PlateTag != null && message.hasOwnProperty("PlateTag")) {
                if (!Array.isArray(message.PlateTag))
                    return "PlateTag: array expected";
                for (let i = 0; i < message.PlateTag.length; ++i)
                    if (!$util.isString(message.PlateTag[i]))
                        return "PlateTag: string[] expected";
            }
            if (message.AwardInfos != null && message.hasOwnProperty("AwardInfos")) {
                if (!Array.isArray(message.AwardInfos))
                    return "AwardInfos: array expected";
                for (let i = 0; i < message.AwardInfos.length; ++i) {
                    let error = $root.jlcb.AwardData.verify(message.AwardInfos[i]);
                    if (error)
                        return "AwardInfos." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.HasUp != null && message.hasOwnProperty("HasUp"))
                if (typeof message.HasUp !== "boolean")
                    return "HasUp: boolean expected";
            return null;
        };

        /**
         * Creates a SingleGem message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.SingleGem
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.SingleGem} SingleGem
         */
        SingleGem.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.SingleGem)
                return object;
            let message = new $root.jlcb.SingleGem();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlcb.SingleGem.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = String(object.PlateSymbol[i]);
            }
            if (object.PlateTag) {
                if (!Array.isArray(object.PlateTag))
                    throw TypeError(".jlcb.SingleGem.PlateTag: array expected");
                message.PlateTag = [];
                for (let i = 0; i < object.PlateTag.length; ++i)
                    message.PlateTag[i] = String(object.PlateTag[i]);
            }
            if (object.AwardInfos) {
                if (!Array.isArray(object.AwardInfos))
                    throw TypeError(".jlcb.SingleGem.AwardInfos: array expected");
                message.AwardInfos = [];
                for (let i = 0; i < object.AwardInfos.length; ++i) {
                    if (typeof object.AwardInfos[i] !== "object")
                        throw TypeError(".jlcb.SingleGem.AwardInfos: object expected");
                    message.AwardInfos[i] = $root.jlcb.AwardData.fromObject(object.AwardInfos[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.HasUp != null)
                message.HasUp = Boolean(object.HasUp);
            return message;
        };

        /**
         * Creates a plain object from a SingleGem message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.SingleGem
         * @static
         * @param {jlcb.SingleGem} message SingleGem
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleGem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateTag = [];
                object.AwardInfos = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.HasUp = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.PlateTag && message.PlateTag.length) {
                object.PlateTag = [];
                for (let j = 0; j < message.PlateTag.length; ++j)
                    object.PlateTag[j] = message.PlateTag[j];
            }
            if (message.AwardInfos && message.AwardInfos.length) {
                object.AwardInfos = [];
                for (let j = 0; j < message.AwardInfos.length; ++j)
                    object.AwardInfos[j] = $root.jlcb.AwardData.toObject(message.AwardInfos[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.HasUp != null && message.hasOwnProperty("HasUp"))
                object.HasUp = message.HasUp;
            return object;
        };

        /**
         * Converts this SingleGem to JSON.
         * @function toJSON
         * @memberof jlcb.SingleGem
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleGem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleGem
         * @function getTypeUrl
         * @memberof jlcb.SingleGem
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleGem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.SingleGem";
        };

        return SingleGem;
    })();

    jlcb.GemData = (function() {

        /**
         * Properties of a GemData.
         * @memberof jlcb
         * @interface IGemData
         * @property {Array.<jlcb.ISingleGem>|null} [PlateQueue] GemData PlateQueue
         * @property {number|null} [Count] GemData Count
         * @property {number|null} [GemWin] GemData GemWin
         */

        /**
         * Constructs a new GemData.
         * @memberof jlcb
         * @classdesc Represents a GemData.
         * @implements IGemData
         * @constructor
         * @param {jlcb.IGemData=} [properties] Properties to set
         */
        function GemData(properties) {
            this.PlateQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GemData PlateQueue.
         * @member {Array.<jlcb.ISingleGem>} PlateQueue
         * @memberof jlcb.GemData
         * @instance
         */
        GemData.prototype.PlateQueue = $util.emptyArray;

        /**
         * GemData Count.
         * @member {number} Count
         * @memberof jlcb.GemData
         * @instance
         */
        GemData.prototype.Count = 0;

        /**
         * GemData GemWin.
         * @member {number} GemWin
         * @memberof jlcb.GemData
         * @instance
         */
        GemData.prototype.GemWin = 0;

        /**
         * Creates a new GemData instance using the specified properties.
         * @function create
         * @memberof jlcb.GemData
         * @static
         * @param {jlcb.IGemData=} [properties] Properties to set
         * @returns {jlcb.GemData} GemData instance
         */
        GemData.create = function create(properties) {
            return new GemData(properties);
        };

        /**
         * Encodes the specified GemData message. Does not implicitly {@link jlcb.GemData.verify|verify} messages.
         * @function encode
         * @memberof jlcb.GemData
         * @static
         * @param {jlcb.IGemData} message GemData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GemData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateQueue != null && message.PlateQueue.length)
                for (let i = 0; i < message.PlateQueue.length; ++i)
                    $root.jlcb.SingleGem.encode(message.PlateQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.GemWin != null && Object.hasOwnProperty.call(message, "GemWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.GemWin);
            return writer;
        };

        /**
         * Encodes the specified GemData message, length delimited. Does not implicitly {@link jlcb.GemData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.GemData
         * @static
         * @param {jlcb.IGemData} message GemData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GemData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GemData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.GemData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.GemData} GemData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GemData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.GemData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateQueue && message.PlateQueue.length))
                            message.PlateQueue = [];
                        message.PlateQueue.push($root.jlcb.SingleGem.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.GemWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GemData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.GemData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.GemData} GemData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GemData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GemData message.
         * @function verify
         * @memberof jlcb.GemData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GemData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateQueue != null && message.hasOwnProperty("PlateQueue")) {
                if (!Array.isArray(message.PlateQueue))
                    return "PlateQueue: array expected";
                for (let i = 0; i < message.PlateQueue.length; ++i) {
                    let error = $root.jlcb.SingleGem.verify(message.PlateQueue[i]);
                    if (error)
                        return "PlateQueue." + error;
                }
            }
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.GemWin != null && message.hasOwnProperty("GemWin"))
                if (typeof message.GemWin !== "number")
                    return "GemWin: number expected";
            return null;
        };

        /**
         * Creates a GemData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.GemData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.GemData} GemData
         */
        GemData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.GemData)
                return object;
            let message = new $root.jlcb.GemData();
            if (object.PlateQueue) {
                if (!Array.isArray(object.PlateQueue))
                    throw TypeError(".jlcb.GemData.PlateQueue: array expected");
                message.PlateQueue = [];
                for (let i = 0; i < object.PlateQueue.length; ++i) {
                    if (typeof object.PlateQueue[i] !== "object")
                        throw TypeError(".jlcb.GemData.PlateQueue: object expected");
                    message.PlateQueue[i] = $root.jlcb.SingleGem.fromObject(object.PlateQueue[i]);
                }
            }
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.GemWin != null)
                message.GemWin = Number(object.GemWin);
            return message;
        };

        /**
         * Creates a plain object from a GemData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.GemData
         * @static
         * @param {jlcb.GemData} message GemData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GemData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateQueue = [];
            if (options.defaults) {
                object.Count = 0;
                object.GemWin = 0;
            }
            if (message.PlateQueue && message.PlateQueue.length) {
                object.PlateQueue = [];
                for (let j = 0; j < message.PlateQueue.length; ++j)
                    object.PlateQueue[j] = $root.jlcb.SingleGem.toObject(message.PlateQueue[j], options);
            }
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.GemWin != null && message.hasOwnProperty("GemWin"))
                object.GemWin = options.json && !isFinite(message.GemWin) ? String(message.GemWin) : message.GemWin;
            return object;
        };

        /**
         * Converts this GemData to JSON.
         * @function toJSON
         * @memberof jlcb.GemData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GemData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GemData
         * @function getTypeUrl
         * @memberof jlcb.GemData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GemData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.GemData";
        };

        return GemData;
    })();

    jlcb.SingleFree = (function() {

        /**
         * Properties of a SingleFree.
         * @memberof jlcb
         * @interface ISingleFree
         * @property {jlcb.IGemData|null} [FreeGemInfo] SingleFree FreeGemInfo
         * @property {number|null} [FreeSingleWin] SingleFree FreeSingleWin
         * @property {number|null} [AwardType] SingleFree AwardType
         * @property {number|null} [NowMoney] SingleFree NowMoney
         */

        /**
         * Constructs a new SingleFree.
         * @memberof jlcb
         * @classdesc Represents a SingleFree.
         * @implements ISingleFree
         * @constructor
         * @param {jlcb.ISingleFree=} [properties] Properties to set
         */
        function SingleFree(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleFree FreeGemInfo.
         * @member {jlcb.IGemData|null|undefined} FreeGemInfo
         * @memberof jlcb.SingleFree
         * @instance
         */
        SingleFree.prototype.FreeGemInfo = null;

        /**
         * SingleFree FreeSingleWin.
         * @member {number} FreeSingleWin
         * @memberof jlcb.SingleFree
         * @instance
         */
        SingleFree.prototype.FreeSingleWin = 0;

        /**
         * SingleFree AwardType.
         * @member {number} AwardType
         * @memberof jlcb.SingleFree
         * @instance
         */
        SingleFree.prototype.AwardType = 0;

        /**
         * SingleFree NowMoney.
         * @member {number} NowMoney
         * @memberof jlcb.SingleFree
         * @instance
         */
        SingleFree.prototype.NowMoney = 0;

        /**
         * Creates a new SingleFree instance using the specified properties.
         * @function create
         * @memberof jlcb.SingleFree
         * @static
         * @param {jlcb.ISingleFree=} [properties] Properties to set
         * @returns {jlcb.SingleFree} SingleFree instance
         */
        SingleFree.create = function create(properties) {
            return new SingleFree(properties);
        };

        /**
         * Encodes the specified SingleFree message. Does not implicitly {@link jlcb.SingleFree.verify|verify} messages.
         * @function encode
         * @memberof jlcb.SingleFree
         * @static
         * @param {jlcb.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeGemInfo != null && Object.hasOwnProperty.call(message, "FreeGemInfo"))
                $root.jlcb.GemData.encode(message.FreeGemInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeSingleWin != null && Object.hasOwnProperty.call(message, "FreeSingleWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeSingleWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardType);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.NowMoney);
            return writer;
        };

        /**
         * Encodes the specified SingleFree message, length delimited. Does not implicitly {@link jlcb.SingleFree.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.SingleFree
         * @static
         * @param {jlcb.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.SingleFree();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.FreeGemInfo = $root.jlcb.GemData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeSingleWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 4: {
                        message.NowMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleFree message.
         * @function verify
         * @memberof jlcb.SingleFree
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleFree.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeGemInfo != null && message.hasOwnProperty("FreeGemInfo")) {
                let error = $root.jlcb.GemData.verify(message.FreeGemInfo);
                if (error)
                    return "FreeGemInfo." + error;
            }
            if (message.FreeSingleWin != null && message.hasOwnProperty("FreeSingleWin"))
                if (typeof message.FreeSingleWin !== "number")
                    return "FreeSingleWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            return null;
        };

        /**
         * Creates a SingleFree message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.SingleFree
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.SingleFree} SingleFree
         */
        SingleFree.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.SingleFree)
                return object;
            let message = new $root.jlcb.SingleFree();
            if (object.FreeGemInfo != null) {
                if (typeof object.FreeGemInfo !== "object")
                    throw TypeError(".jlcb.SingleFree.FreeGemInfo: object expected");
                message.FreeGemInfo = $root.jlcb.GemData.fromObject(object.FreeGemInfo);
            }
            if (object.FreeSingleWin != null)
                message.FreeSingleWin = Number(object.FreeSingleWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            return message;
        };

        /**
         * Creates a plain object from a SingleFree message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.SingleFree
         * @static
         * @param {jlcb.SingleFree} message SingleFree
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleFree.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.FreeGemInfo = null;
                object.FreeSingleWin = 0;
                object.AwardType = 0;
                object.NowMoney = 0;
            }
            if (message.FreeGemInfo != null && message.hasOwnProperty("FreeGemInfo"))
                object.FreeGemInfo = $root.jlcb.GemData.toObject(message.FreeGemInfo, options);
            if (message.FreeSingleWin != null && message.hasOwnProperty("FreeSingleWin"))
                object.FreeSingleWin = options.json && !isFinite(message.FreeSingleWin) ? String(message.FreeSingleWin) : message.FreeSingleWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            return object;
        };

        /**
         * Converts this SingleFree to JSON.
         * @function toJSON
         * @memberof jlcb.SingleFree
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleFree.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleFree
         * @function getTypeUrl
         * @memberof jlcb.SingleFree
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleFree.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.SingleFree";
        };

        return SingleFree;
    })();

    jlcb.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jlcb
         * @interface IFreeData
         * @property {Array.<jlcb.ISingleFree>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeMul] FreeData FreeMul
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jlcb
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jlcb.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jlcb.ISingleFree>} FreeQueue
         * @memberof jlcb.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeMul.
         * @member {number} FreeMul
         * @memberof jlcb.FreeData
         * @instance
         */
        FreeData.prototype.FreeMul = 0;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlcb.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jlcb.FreeData
         * @static
         * @param {jlcb.IFreeData=} [properties] Properties to set
         * @returns {jlcb.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jlcb.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jlcb.FreeData
         * @static
         * @param {jlcb.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jlcb.SingleFree.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeMul != null && Object.hasOwnProperty.call(message, "FreeMul"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeMul);
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jlcb.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.FreeData
         * @static
         * @param {jlcb.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jlcb.SingleFree.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeMul = reader.double();
                        break;
                    }
                case 3: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jlcb.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jlcb.SingleFree.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeMul != null && message.hasOwnProperty("FreeMul"))
                if (typeof message.FreeMul !== "number")
                    return "FreeMul: number expected";
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.FreeData)
                return object;
            let message = new $root.jlcb.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jlcb.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jlcb.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jlcb.SingleFree.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeMul != null)
                message.FreeMul = Number(object.FreeMul);
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.FreeData
         * @static
         * @param {jlcb.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults) {
                object.FreeMul = 0;
                object.FreeTotalWin = 0;
            }
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jlcb.SingleFree.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeMul != null && message.hasOwnProperty("FreeMul"))
                object.FreeMul = options.json && !isFinite(message.FreeMul) ? String(message.FreeMul) : message.FreeMul;
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jlcb.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jlcb.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.FreeData";
        };

        return FreeData;
    })();

    jlcb.SingleBonus = (function() {

        /**
         * Properties of a SingleBonus.
         * @memberof jlcb
         * @interface ISingleBonus
         * @property {jlcb.IGemData|null} [BonusGem] SingleBonus BonusGem
         * @property {number|null} [BonusSymbol] SingleBonus BonusSymbol
         * @property {number|null} [BonusSymbolCount] SingleBonus BonusSymbolCount
         */

        /**
         * Constructs a new SingleBonus.
         * @memberof jlcb
         * @classdesc Represents a SingleBonus.
         * @implements ISingleBonus
         * @constructor
         * @param {jlcb.ISingleBonus=} [properties] Properties to set
         */
        function SingleBonus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleBonus BonusGem.
         * @member {jlcb.IGemData|null|undefined} BonusGem
         * @memberof jlcb.SingleBonus
         * @instance
         */
        SingleBonus.prototype.BonusGem = null;

        /**
         * SingleBonus BonusSymbol.
         * @member {number} BonusSymbol
         * @memberof jlcb.SingleBonus
         * @instance
         */
        SingleBonus.prototype.BonusSymbol = 0;

        /**
         * SingleBonus BonusSymbolCount.
         * @member {number} BonusSymbolCount
         * @memberof jlcb.SingleBonus
         * @instance
         */
        SingleBonus.prototype.BonusSymbolCount = 0;

        /**
         * Creates a new SingleBonus instance using the specified properties.
         * @function create
         * @memberof jlcb.SingleBonus
         * @static
         * @param {jlcb.ISingleBonus=} [properties] Properties to set
         * @returns {jlcb.SingleBonus} SingleBonus instance
         */
        SingleBonus.create = function create(properties) {
            return new SingleBonus(properties);
        };

        /**
         * Encodes the specified SingleBonus message. Does not implicitly {@link jlcb.SingleBonus.verify|verify} messages.
         * @function encode
         * @memberof jlcb.SingleBonus
         * @static
         * @param {jlcb.ISingleBonus} message SingleBonus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleBonus.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BonusGem != null && Object.hasOwnProperty.call(message, "BonusGem"))
                $root.jlcb.GemData.encode(message.BonusGem, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusSymbol != null && Object.hasOwnProperty.call(message, "BonusSymbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.BonusSymbol);
            if (message.BonusSymbolCount != null && Object.hasOwnProperty.call(message, "BonusSymbolCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.BonusSymbolCount);
            return writer;
        };

        /**
         * Encodes the specified SingleBonus message, length delimited. Does not implicitly {@link jlcb.SingleBonus.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.SingleBonus
         * @static
         * @param {jlcb.ISingleBonus} message SingleBonus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleBonus.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleBonus message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.SingleBonus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.SingleBonus} SingleBonus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleBonus.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.SingleBonus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BonusGem = $root.jlcb.GemData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.BonusSymbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.BonusSymbolCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleBonus message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.SingleBonus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.SingleBonus} SingleBonus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleBonus.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleBonus message.
         * @function verify
         * @memberof jlcb.SingleBonus
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleBonus.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BonusGem != null && message.hasOwnProperty("BonusGem")) {
                let error = $root.jlcb.GemData.verify(message.BonusGem);
                if (error)
                    return "BonusGem." + error;
            }
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                if (!$util.isInteger(message.BonusSymbol))
                    return "BonusSymbol: integer expected";
            if (message.BonusSymbolCount != null && message.hasOwnProperty("BonusSymbolCount"))
                if (!$util.isInteger(message.BonusSymbolCount))
                    return "BonusSymbolCount: integer expected";
            return null;
        };

        /**
         * Creates a SingleBonus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.SingleBonus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.SingleBonus} SingleBonus
         */
        SingleBonus.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.SingleBonus)
                return object;
            let message = new $root.jlcb.SingleBonus();
            if (object.BonusGem != null) {
                if (typeof object.BonusGem !== "object")
                    throw TypeError(".jlcb.SingleBonus.BonusGem: object expected");
                message.BonusGem = $root.jlcb.GemData.fromObject(object.BonusGem);
            }
            if (object.BonusSymbol != null)
                message.BonusSymbol = object.BonusSymbol | 0;
            if (object.BonusSymbolCount != null)
                message.BonusSymbolCount = object.BonusSymbolCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a SingleBonus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.SingleBonus
         * @static
         * @param {jlcb.SingleBonus} message SingleBonus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleBonus.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BonusGem = null;
                object.BonusSymbol = 0;
                object.BonusSymbolCount = 0;
            }
            if (message.BonusGem != null && message.hasOwnProperty("BonusGem"))
                object.BonusGem = $root.jlcb.GemData.toObject(message.BonusGem, options);
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                object.BonusSymbol = message.BonusSymbol;
            if (message.BonusSymbolCount != null && message.hasOwnProperty("BonusSymbolCount"))
                object.BonusSymbolCount = message.BonusSymbolCount;
            return object;
        };

        /**
         * Converts this SingleBonus to JSON.
         * @function toJSON
         * @memberof jlcb.SingleBonus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleBonus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleBonus
         * @function getTypeUrl
         * @memberof jlcb.SingleBonus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleBonus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.SingleBonus";
        };

        return SingleBonus;
    })();

    jlcb.BonusData = (function() {

        /**
         * Properties of a BonusData.
         * @memberof jlcb
         * @interface IBonusData
         * @property {Array.<jlcb.ISingleBonus>|null} [BonusQueue] BonusData BonusQueue
         * @property {number|null} [BonusCount] BonusData BonusCount
         * @property {number|null} [BonusTotalWin] BonusData BonusTotalWin
         */

        /**
         * Constructs a new BonusData.
         * @memberof jlcb
         * @classdesc Represents a BonusData.
         * @implements IBonusData
         * @constructor
         * @param {jlcb.IBonusData=} [properties] Properties to set
         */
        function BonusData(properties) {
            this.BonusQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusData BonusQueue.
         * @member {Array.<jlcb.ISingleBonus>} BonusQueue
         * @memberof jlcb.BonusData
         * @instance
         */
        BonusData.prototype.BonusQueue = $util.emptyArray;

        /**
         * BonusData BonusCount.
         * @member {number} BonusCount
         * @memberof jlcb.BonusData
         * @instance
         */
        BonusData.prototype.BonusCount = 0;

        /**
         * BonusData BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlcb.BonusData
         * @instance
         */
        BonusData.prototype.BonusTotalWin = 0;

        /**
         * Creates a new BonusData instance using the specified properties.
         * @function create
         * @memberof jlcb.BonusData
         * @static
         * @param {jlcb.IBonusData=} [properties] Properties to set
         * @returns {jlcb.BonusData} BonusData instance
         */
        BonusData.create = function create(properties) {
            return new BonusData(properties);
        };

        /**
         * Encodes the specified BonusData message. Does not implicitly {@link jlcb.BonusData.verify|verify} messages.
         * @function encode
         * @memberof jlcb.BonusData
         * @static
         * @param {jlcb.IBonusData} message BonusData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BonusQueue != null && message.BonusQueue.length)
                for (let i = 0; i < message.BonusQueue.length; ++i)
                    $root.jlcb.SingleBonus.encode(message.BonusQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusCount != null && Object.hasOwnProperty.call(message, "BonusCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.BonusCount);
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.BonusTotalWin);
            return writer;
        };

        /**
         * Encodes the specified BonusData message, length delimited. Does not implicitly {@link jlcb.BonusData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.BonusData
         * @static
         * @param {jlcb.IBonusData} message BonusData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.BonusData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.BonusData} BonusData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.BonusData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BonusQueue && message.BonusQueue.length))
                            message.BonusQueue = [];
                        message.BonusQueue.push($root.jlcb.SingleBonus.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusCount = reader.int32();
                        break;
                    }
                case 3: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.BonusData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.BonusData} BonusData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusData message.
         * @function verify
         * @memberof jlcb.BonusData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BonusQueue != null && message.hasOwnProperty("BonusQueue")) {
                if (!Array.isArray(message.BonusQueue))
                    return "BonusQueue: array expected";
                for (let i = 0; i < message.BonusQueue.length; ++i) {
                    let error = $root.jlcb.SingleBonus.verify(message.BonusQueue[i]);
                    if (error)
                        return "BonusQueue." + error;
                }
            }
            if (message.BonusCount != null && message.hasOwnProperty("BonusCount"))
                if (!$util.isInteger(message.BonusCount))
                    return "BonusCount: integer expected";
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            return null;
        };

        /**
         * Creates a BonusData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.BonusData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.BonusData} BonusData
         */
        BonusData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.BonusData)
                return object;
            let message = new $root.jlcb.BonusData();
            if (object.BonusQueue) {
                if (!Array.isArray(object.BonusQueue))
                    throw TypeError(".jlcb.BonusData.BonusQueue: array expected");
                message.BonusQueue = [];
                for (let i = 0; i < object.BonusQueue.length; ++i) {
                    if (typeof object.BonusQueue[i] !== "object")
                        throw TypeError(".jlcb.BonusData.BonusQueue: object expected");
                    message.BonusQueue[i] = $root.jlcb.SingleBonus.fromObject(object.BonusQueue[i]);
                }
            }
            if (object.BonusCount != null)
                message.BonusCount = object.BonusCount | 0;
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a BonusData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.BonusData
         * @static
         * @param {jlcb.BonusData} message BonusData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BonusQueue = [];
            if (options.defaults) {
                object.BonusCount = 0;
                object.BonusTotalWin = 0;
            }
            if (message.BonusQueue && message.BonusQueue.length) {
                object.BonusQueue = [];
                for (let j = 0; j < message.BonusQueue.length; ++j)
                    object.BonusQueue[j] = $root.jlcb.SingleBonus.toObject(message.BonusQueue[j], options);
            }
            if (message.BonusCount != null && message.hasOwnProperty("BonusCount"))
                object.BonusCount = message.BonusCount;
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            return object;
        };

        /**
         * Converts this BonusData to JSON.
         * @function toJSON
         * @memberof jlcb.BonusData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusData
         * @function getTypeUrl
         * @memberof jlcb.BonusData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.BonusData";
        };

        return BonusData;
    })();

    jlcb.BetMulData = (function() {

        /**
         * Properties of a BetMulData.
         * @memberof jlcb
         * @interface IBetMulData
         * @property {number|null} [BetMul] BetMulData BetMul
         * @property {number|null} [BetMulTotalWin] BetMulData BetMulTotalWin
         */

        /**
         * Constructs a new BetMulData.
         * @memberof jlcb
         * @classdesc Represents a BetMulData.
         * @implements IBetMulData
         * @constructor
         * @param {jlcb.IBetMulData=} [properties] Properties to set
         */
        function BetMulData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BetMulData BetMul.
         * @member {number} BetMul
         * @memberof jlcb.BetMulData
         * @instance
         */
        BetMulData.prototype.BetMul = 0;

        /**
         * BetMulData BetMulTotalWin.
         * @member {number} BetMulTotalWin
         * @memberof jlcb.BetMulData
         * @instance
         */
        BetMulData.prototype.BetMulTotalWin = 0;

        /**
         * Creates a new BetMulData instance using the specified properties.
         * @function create
         * @memberof jlcb.BetMulData
         * @static
         * @param {jlcb.IBetMulData=} [properties] Properties to set
         * @returns {jlcb.BetMulData} BetMulData instance
         */
        BetMulData.create = function create(properties) {
            return new BetMulData(properties);
        };

        /**
         * Encodes the specified BetMulData message. Does not implicitly {@link jlcb.BetMulData.verify|verify} messages.
         * @function encode
         * @memberof jlcb.BetMulData
         * @static
         * @param {jlcb.IBetMulData} message BetMulData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetMulData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BetMul != null && Object.hasOwnProperty.call(message, "BetMul"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BetMul);
            if (message.BetMulTotalWin != null && Object.hasOwnProperty.call(message, "BetMulTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BetMulTotalWin);
            return writer;
        };

        /**
         * Encodes the specified BetMulData message, length delimited. Does not implicitly {@link jlcb.BetMulData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.BetMulData
         * @static
         * @param {jlcb.IBetMulData} message BetMulData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetMulData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BetMulData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.BetMulData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.BetMulData} BetMulData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetMulData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.BetMulData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BetMul = reader.double();
                        break;
                    }
                case 2: {
                        message.BetMulTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BetMulData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.BetMulData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.BetMulData} BetMulData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetMulData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BetMulData message.
         * @function verify
         * @memberof jlcb.BetMulData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BetMulData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BetMul != null && message.hasOwnProperty("BetMul"))
                if (typeof message.BetMul !== "number")
                    return "BetMul: number expected";
            if (message.BetMulTotalWin != null && message.hasOwnProperty("BetMulTotalWin"))
                if (typeof message.BetMulTotalWin !== "number")
                    return "BetMulTotalWin: number expected";
            return null;
        };

        /**
         * Creates a BetMulData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.BetMulData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.BetMulData} BetMulData
         */
        BetMulData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.BetMulData)
                return object;
            let message = new $root.jlcb.BetMulData();
            if (object.BetMul != null)
                message.BetMul = Number(object.BetMul);
            if (object.BetMulTotalWin != null)
                message.BetMulTotalWin = Number(object.BetMulTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a BetMulData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.BetMulData
         * @static
         * @param {jlcb.BetMulData} message BetMulData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BetMulData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BetMul = 0;
                object.BetMulTotalWin = 0;
            }
            if (message.BetMul != null && message.hasOwnProperty("BetMul"))
                object.BetMul = options.json && !isFinite(message.BetMul) ? String(message.BetMul) : message.BetMul;
            if (message.BetMulTotalWin != null && message.hasOwnProperty("BetMulTotalWin"))
                object.BetMulTotalWin = options.json && !isFinite(message.BetMulTotalWin) ? String(message.BetMulTotalWin) : message.BetMulTotalWin;
            return object;
        };

        /**
         * Converts this BetMulData to JSON.
         * @function toJSON
         * @memberof jlcb.BetMulData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BetMulData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BetMulData
         * @function getTypeUrl
         * @memberof jlcb.BetMulData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BetMulData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.BetMulData";
        };

        return BetMulData;
    })();

    jlcb.JPData = (function() {

        /**
         * Properties of a JPData.
         * @memberof jlcb
         * @interface IJPData
         * @property {number|null} [JPType] JPData JPType
         * @property {number|null} [JPTotalWin] JPData JPTotalWin
         */

        /**
         * Constructs a new JPData.
         * @memberof jlcb
         * @classdesc Represents a JPData.
         * @implements IJPData
         * @constructor
         * @param {jlcb.IJPData=} [properties] Properties to set
         */
        function JPData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPData JPType.
         * @member {number} JPType
         * @memberof jlcb.JPData
         * @instance
         */
        JPData.prototype.JPType = 0;

        /**
         * JPData JPTotalWin.
         * @member {number} JPTotalWin
         * @memberof jlcb.JPData
         * @instance
         */
        JPData.prototype.JPTotalWin = 0;

        /**
         * Creates a new JPData instance using the specified properties.
         * @function create
         * @memberof jlcb.JPData
         * @static
         * @param {jlcb.IJPData=} [properties] Properties to set
         * @returns {jlcb.JPData} JPData instance
         */
        JPData.create = function create(properties) {
            return new JPData(properties);
        };

        /**
         * Encodes the specified JPData message. Does not implicitly {@link jlcb.JPData.verify|verify} messages.
         * @function encode
         * @memberof jlcb.JPData
         * @static
         * @param {jlcb.IJPData} message JPData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.JPType != null && Object.hasOwnProperty.call(message, "JPType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.JPType);
            if (message.JPTotalWin != null && Object.hasOwnProperty.call(message, "JPTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.JPTotalWin);
            return writer;
        };

        /**
         * Encodes the specified JPData message, length delimited. Does not implicitly {@link jlcb.JPData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.JPData
         * @static
         * @param {jlcb.IJPData} message JPData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.JPData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.JPData} JPData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.JPData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.JPType = reader.int32();
                        break;
                    }
                case 2: {
                        message.JPTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.JPData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.JPData} JPData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPData message.
         * @function verify
         * @memberof jlcb.JPData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.JPType != null && message.hasOwnProperty("JPType"))
                if (!$util.isInteger(message.JPType))
                    return "JPType: integer expected";
            if (message.JPTotalWin != null && message.hasOwnProperty("JPTotalWin"))
                if (typeof message.JPTotalWin !== "number")
                    return "JPTotalWin: number expected";
            return null;
        };

        /**
         * Creates a JPData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.JPData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.JPData} JPData
         */
        JPData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.JPData)
                return object;
            let message = new $root.jlcb.JPData();
            if (object.JPType != null)
                message.JPType = object.JPType | 0;
            if (object.JPTotalWin != null)
                message.JPTotalWin = Number(object.JPTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a JPData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.JPData
         * @static
         * @param {jlcb.JPData} message JPData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.JPType = 0;
                object.JPTotalWin = 0;
            }
            if (message.JPType != null && message.hasOwnProperty("JPType"))
                object.JPType = message.JPType;
            if (message.JPTotalWin != null && message.hasOwnProperty("JPTotalWin"))
                object.JPTotalWin = options.json && !isFinite(message.JPTotalWin) ? String(message.JPTotalWin) : message.JPTotalWin;
            return object;
        };

        /**
         * Converts this JPData to JSON.
         * @function toJSON
         * @memberof jlcb.JPData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPData
         * @function getTypeUrl
         * @memberof jlcb.JPData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.JPData";
        };

        return JPData;
    })();

    jlcb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcb
         * @interface ISpinAck
         * @property {jlcb.IGemData|null} [GemInfo] SpinAck GemInfo
         * @property {boolean|null} [ChangePlate] SpinAck ChangePlate
         * @property {number|null} [ChangePlateSize] SpinAck ChangePlateSize
         * @property {boolean|null} [GetWheel] SpinAck GetWheel
         * @property {number|null} [WheelType] SpinAck WheelType
         * @property {jlcb.IFreeData|null} [FreeInfo] SpinAck FreeInfo
         * @property {jlcb.IBonusData|null} [BonusInfo] SpinAck BonusInfo
         * @property {jlcb.IBetMulData|null} [BetMulInfo] SpinAck BetMulInfo
         * @property {jlcb.IJPData|null} [JPWinInfo] SpinAck JPWinInfo
         * @property {number|null} [Upcount] SpinAck Upcount
         * @property {number|null} [TotalUpcount] SpinAck TotalUpcount
         * @property {number|null} [Mainwin] SpinAck Mainwin
         * @property {number|null} [Wheelwin] SpinAck Wheelwin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {jlcb.IJPInfo|null} [JPInfo] SpinAck JPInfo
         * @property {jlcb.IJPInfo|null} [JPBeforeInfo] SpinAck JPBeforeInfo
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck GemInfo.
         * @member {jlcb.IGemData|null|undefined} GemInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.GemInfo = null;

        /**
         * SpinAck ChangePlate.
         * @member {boolean} ChangePlate
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.ChangePlate = false;

        /**
         * SpinAck ChangePlateSize.
         * @member {number} ChangePlateSize
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.ChangePlateSize = 0;

        /**
         * SpinAck GetWheel.
         * @member {boolean} GetWheel
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.GetWheel = false;

        /**
         * SpinAck WheelType.
         * @member {number} WheelType
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelType = 0;

        /**
         * SpinAck FreeInfo.
         * @member {jlcb.IFreeData|null|undefined} FreeInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeInfo = null;

        /**
         * SpinAck BonusInfo.
         * @member {jlcb.IBonusData|null|undefined} BonusInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusInfo = null;

        /**
         * SpinAck BetMulInfo.
         * @member {jlcb.IBetMulData|null|undefined} BetMulInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.BetMulInfo = null;

        /**
         * SpinAck JPWinInfo.
         * @member {jlcb.IJPData|null|undefined} JPWinInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.JPWinInfo = null;

        /**
         * SpinAck Upcount.
         * @member {number} Upcount
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.Upcount = 0;

        /**
         * SpinAck TotalUpcount.
         * @member {number} TotalUpcount
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalUpcount = 0;

        /**
         * SpinAck Mainwin.
         * @member {number} Mainwin
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.Mainwin = 0;

        /**
         * SpinAck Wheelwin.
         * @member {number} Wheelwin
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.Wheelwin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck JPInfo.
         * @member {jlcb.IJPInfo|null|undefined} JPInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.JPInfo = null;

        /**
         * SpinAck JPBeforeInfo.
         * @member {jlcb.IJPInfo|null|undefined} JPBeforeInfo
         * @memberof jlcb.SpinAck
         * @instance
         */
        SpinAck.prototype.JPBeforeInfo = null;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcb.SpinAck
         * @static
         * @param {jlcb.ISpinAck=} [properties] Properties to set
         * @returns {jlcb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcb.SpinAck
         * @static
         * @param {jlcb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.GemInfo != null && Object.hasOwnProperty.call(message, "GemInfo"))
                $root.jlcb.GemData.encode(message.GemInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ChangePlate != null && Object.hasOwnProperty.call(message, "ChangePlate"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.ChangePlate);
            if (message.ChangePlateSize != null && Object.hasOwnProperty.call(message, "ChangePlateSize"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ChangePlateSize);
            if (message.GetWheel != null && Object.hasOwnProperty.call(message, "GetWheel"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.GetWheel);
            if (message.WheelType != null && Object.hasOwnProperty.call(message, "WheelType"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.WheelType);
            if (message.FreeInfo != null && Object.hasOwnProperty.call(message, "FreeInfo"))
                $root.jlcb.FreeData.encode(message.FreeInfo, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.BonusInfo != null && Object.hasOwnProperty.call(message, "BonusInfo"))
                $root.jlcb.BonusData.encode(message.BonusInfo, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.BetMulInfo != null && Object.hasOwnProperty.call(message, "BetMulInfo"))
                $root.jlcb.BetMulData.encode(message.BetMulInfo, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.JPWinInfo != null && Object.hasOwnProperty.call(message, "JPWinInfo"))
                $root.jlcb.JPData.encode(message.JPWinInfo, writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.Upcount != null && Object.hasOwnProperty.call(message, "Upcount"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.Upcount);
            if (message.TotalUpcount != null && Object.hasOwnProperty.call(message, "TotalUpcount"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.TotalUpcount);
            if (message.Mainwin != null && Object.hasOwnProperty.call(message, "Mainwin"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.Mainwin);
            if (message.Wheelwin != null && Object.hasOwnProperty.call(message, "Wheelwin"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.Wheelwin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 14, wireType 1 =*/113).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 17, wireType 1 =*/137).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 19, wireType 1 =*/153).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.RID);
            if (message.JPInfo != null && Object.hasOwnProperty.call(message, "JPInfo"))
                $root.jlcb.JPInfo.encode(message.JPInfo, writer.uint32(/* id 22, wireType 2 =*/178).fork()).ldelim();
            if (message.JPBeforeInfo != null && Object.hasOwnProperty.call(message, "JPBeforeInfo"))
                $root.jlcb.JPInfo.encode(message.JPBeforeInfo, writer.uint32(/* id 23, wireType 2 =*/186).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.SpinAck
         * @static
         * @param {jlcb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GemInfo = $root.jlcb.GemData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.ChangePlate = reader.bool();
                        break;
                    }
                case 3: {
                        message.ChangePlateSize = reader.int32();
                        break;
                    }
                case 4: {
                        message.GetWheel = reader.bool();
                        break;
                    }
                case 5: {
                        message.WheelType = reader.double();
                        break;
                    }
                case 6: {
                        message.FreeInfo = $root.jlcb.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 7: {
                        message.BonusInfo = $root.jlcb.BonusData.decode(reader, reader.uint32());
                        break;
                    }
                case 8: {
                        message.BetMulInfo = $root.jlcb.BetMulData.decode(reader, reader.uint32());
                        break;
                    }
                case 9: {
                        message.JPWinInfo = $root.jlcb.JPData.decode(reader, reader.uint32());
                        break;
                    }
                case 10: {
                        message.Upcount = reader.int32();
                        break;
                    }
                case 11: {
                        message.TotalUpcount = reader.int32();
                        break;
                    }
                case 12: {
                        message.Mainwin = reader.double();
                        break;
                    }
                case 13: {
                        message.Wheelwin = reader.double();
                        break;
                    }
                case 14: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 15: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 16: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 17: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 18: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 19: {
                        message.RTP = reader.double();
                        break;
                    }
                case 20: {
                        message.LID = reader.int32();
                        break;
                    }
                case 21: {
                        message.RID = reader.int32();
                        break;
                    }
                case 22: {
                        message.JPInfo = $root.jlcb.JPInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 23: {
                        message.JPBeforeInfo = $root.jlcb.JPInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.GemInfo != null && message.hasOwnProperty("GemInfo")) {
                let error = $root.jlcb.GemData.verify(message.GemInfo);
                if (error)
                    return "GemInfo." + error;
            }
            if (message.ChangePlate != null && message.hasOwnProperty("ChangePlate"))
                if (typeof message.ChangePlate !== "boolean")
                    return "ChangePlate: boolean expected";
            if (message.ChangePlateSize != null && message.hasOwnProperty("ChangePlateSize"))
                if (!$util.isInteger(message.ChangePlateSize))
                    return "ChangePlateSize: integer expected";
            if (message.GetWheel != null && message.hasOwnProperty("GetWheel"))
                if (typeof message.GetWheel !== "boolean")
                    return "GetWheel: boolean expected";
            if (message.WheelType != null && message.hasOwnProperty("WheelType"))
                if (typeof message.WheelType !== "number")
                    return "WheelType: number expected";
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo")) {
                let error = $root.jlcb.FreeData.verify(message.FreeInfo);
                if (error)
                    return "FreeInfo." + error;
            }
            if (message.BonusInfo != null && message.hasOwnProperty("BonusInfo")) {
                let error = $root.jlcb.BonusData.verify(message.BonusInfo);
                if (error)
                    return "BonusInfo." + error;
            }
            if (message.BetMulInfo != null && message.hasOwnProperty("BetMulInfo")) {
                let error = $root.jlcb.BetMulData.verify(message.BetMulInfo);
                if (error)
                    return "BetMulInfo." + error;
            }
            if (message.JPWinInfo != null && message.hasOwnProperty("JPWinInfo")) {
                let error = $root.jlcb.JPData.verify(message.JPWinInfo);
                if (error)
                    return "JPWinInfo." + error;
            }
            if (message.Upcount != null && message.hasOwnProperty("Upcount"))
                if (!$util.isInteger(message.Upcount))
                    return "Upcount: integer expected";
            if (message.TotalUpcount != null && message.hasOwnProperty("TotalUpcount"))
                if (!$util.isInteger(message.TotalUpcount))
                    return "TotalUpcount: integer expected";
            if (message.Mainwin != null && message.hasOwnProperty("Mainwin"))
                if (typeof message.Mainwin !== "number")
                    return "Mainwin: number expected";
            if (message.Wheelwin != null && message.hasOwnProperty("Wheelwin"))
                if (typeof message.Wheelwin !== "number")
                    return "Wheelwin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.JPInfo != null && message.hasOwnProperty("JPInfo")) {
                let error = $root.jlcb.JPInfo.verify(message.JPInfo);
                if (error)
                    return "JPInfo." + error;
            }
            if (message.JPBeforeInfo != null && message.hasOwnProperty("JPBeforeInfo")) {
                let error = $root.jlcb.JPInfo.verify(message.JPBeforeInfo);
                if (error)
                    return "JPBeforeInfo." + error;
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.SpinAck)
                return object;
            let message = new $root.jlcb.SpinAck();
            if (object.GemInfo != null) {
                if (typeof object.GemInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.GemInfo: object expected");
                message.GemInfo = $root.jlcb.GemData.fromObject(object.GemInfo);
            }
            if (object.ChangePlate != null)
                message.ChangePlate = Boolean(object.ChangePlate);
            if (object.ChangePlateSize != null)
                message.ChangePlateSize = object.ChangePlateSize | 0;
            if (object.GetWheel != null)
                message.GetWheel = Boolean(object.GetWheel);
            if (object.WheelType != null)
                message.WheelType = Number(object.WheelType);
            if (object.FreeInfo != null) {
                if (typeof object.FreeInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.FreeInfo: object expected");
                message.FreeInfo = $root.jlcb.FreeData.fromObject(object.FreeInfo);
            }
            if (object.BonusInfo != null) {
                if (typeof object.BonusInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.BonusInfo: object expected");
                message.BonusInfo = $root.jlcb.BonusData.fromObject(object.BonusInfo);
            }
            if (object.BetMulInfo != null) {
                if (typeof object.BetMulInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.BetMulInfo: object expected");
                message.BetMulInfo = $root.jlcb.BetMulData.fromObject(object.BetMulInfo);
            }
            if (object.JPWinInfo != null) {
                if (typeof object.JPWinInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.JPWinInfo: object expected");
                message.JPWinInfo = $root.jlcb.JPData.fromObject(object.JPWinInfo);
            }
            if (object.Upcount != null)
                message.Upcount = object.Upcount | 0;
            if (object.TotalUpcount != null)
                message.TotalUpcount = object.TotalUpcount | 0;
            if (object.Mainwin != null)
                message.Mainwin = Number(object.Mainwin);
            if (object.Wheelwin != null)
                message.Wheelwin = Number(object.Wheelwin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.JPInfo != null) {
                if (typeof object.JPInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.JPInfo: object expected");
                message.JPInfo = $root.jlcb.JPInfo.fromObject(object.JPInfo);
            }
            if (object.JPBeforeInfo != null) {
                if (typeof object.JPBeforeInfo !== "object")
                    throw TypeError(".jlcb.SpinAck.JPBeforeInfo: object expected");
                message.JPBeforeInfo = $root.jlcb.JPInfo.fromObject(object.JPBeforeInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.SpinAck
         * @static
         * @param {jlcb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.GemInfo = null;
                object.ChangePlate = false;
                object.ChangePlateSize = 0;
                object.GetWheel = false;
                object.WheelType = 0;
                object.FreeInfo = null;
                object.BonusInfo = null;
                object.BetMulInfo = null;
                object.JPWinInfo = null;
                object.Upcount = 0;
                object.TotalUpcount = 0;
                object.Mainwin = 0;
                object.Wheelwin = 0;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.JPInfo = null;
                object.JPBeforeInfo = null;
            }
            if (message.GemInfo != null && message.hasOwnProperty("GemInfo"))
                object.GemInfo = $root.jlcb.GemData.toObject(message.GemInfo, options);
            if (message.ChangePlate != null && message.hasOwnProperty("ChangePlate"))
                object.ChangePlate = message.ChangePlate;
            if (message.ChangePlateSize != null && message.hasOwnProperty("ChangePlateSize"))
                object.ChangePlateSize = message.ChangePlateSize;
            if (message.GetWheel != null && message.hasOwnProperty("GetWheel"))
                object.GetWheel = message.GetWheel;
            if (message.WheelType != null && message.hasOwnProperty("WheelType"))
                object.WheelType = options.json && !isFinite(message.WheelType) ? String(message.WheelType) : message.WheelType;
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo"))
                object.FreeInfo = $root.jlcb.FreeData.toObject(message.FreeInfo, options);
            if (message.BonusInfo != null && message.hasOwnProperty("BonusInfo"))
                object.BonusInfo = $root.jlcb.BonusData.toObject(message.BonusInfo, options);
            if (message.BetMulInfo != null && message.hasOwnProperty("BetMulInfo"))
                object.BetMulInfo = $root.jlcb.BetMulData.toObject(message.BetMulInfo, options);
            if (message.JPWinInfo != null && message.hasOwnProperty("JPWinInfo"))
                object.JPWinInfo = $root.jlcb.JPData.toObject(message.JPWinInfo, options);
            if (message.Upcount != null && message.hasOwnProperty("Upcount"))
                object.Upcount = message.Upcount;
            if (message.TotalUpcount != null && message.hasOwnProperty("TotalUpcount"))
                object.TotalUpcount = message.TotalUpcount;
            if (message.Mainwin != null && message.hasOwnProperty("Mainwin"))
                object.Mainwin = options.json && !isFinite(message.Mainwin) ? String(message.Mainwin) : message.Mainwin;
            if (message.Wheelwin != null && message.hasOwnProperty("Wheelwin"))
                object.Wheelwin = options.json && !isFinite(message.Wheelwin) ? String(message.Wheelwin) : message.Wheelwin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.JPInfo != null && message.hasOwnProperty("JPInfo"))
                object.JPInfo = $root.jlcb.JPInfo.toObject(message.JPInfo, options);
            if (message.JPBeforeInfo != null && message.hasOwnProperty("JPBeforeInfo"))
                object.JPBeforeInfo = $root.jlcb.JPInfo.toObject(message.JPBeforeInfo, options);
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.SpinAck";
        };

        return SpinAck;
    })();

    jlcb.JpReq = (function() {

        /**
         * Properties of a JpReq.
         * @memberof jlcb
         * @interface IJpReq
         * @property {number|null} [currency] JpReq currency
         */

        /**
         * Constructs a new JpReq.
         * @memberof jlcb
         * @classdesc Represents a JpReq.
         * @implements IJpReq
         * @constructor
         * @param {jlcb.IJpReq=} [properties] Properties to set
         */
        function JpReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JpReq currency.
         * @member {number} currency
         * @memberof jlcb.JpReq
         * @instance
         */
        JpReq.prototype.currency = 0;

        /**
         * Creates a new JpReq instance using the specified properties.
         * @function create
         * @memberof jlcb.JpReq
         * @static
         * @param {jlcb.IJpReq=} [properties] Properties to set
         * @returns {jlcb.JpReq} JpReq instance
         */
        JpReq.create = function create(properties) {
            return new JpReq(properties);
        };

        /**
         * Encodes the specified JpReq message. Does not implicitly {@link jlcb.JpReq.verify|verify} messages.
         * @function encode
         * @memberof jlcb.JpReq
         * @static
         * @param {jlcb.IJpReq} message JpReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.currency);
            return writer;
        };

        /**
         * Encodes the specified JpReq message, length delimited. Does not implicitly {@link jlcb.JpReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.JpReq
         * @static
         * @param {jlcb.IJpReq} message JpReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JpReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.JpReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.JpReq} JpReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.JpReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.currency = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JpReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.JpReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.JpReq} JpReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JpReq message.
         * @function verify
         * @memberof jlcb.JpReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JpReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.currency != null && message.hasOwnProperty("currency"))
                if (!$util.isInteger(message.currency))
                    return "currency: integer expected";
            return null;
        };

        /**
         * Creates a JpReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.JpReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.JpReq} JpReq
         */
        JpReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.JpReq)
                return object;
            let message = new $root.jlcb.JpReq();
            if (object.currency != null)
                message.currency = object.currency | 0;
            return message;
        };

        /**
         * Creates a plain object from a JpReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.JpReq
         * @static
         * @param {jlcb.JpReq} message JpReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JpReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.currency = 0;
            if (message.currency != null && message.hasOwnProperty("currency"))
                object.currency = message.currency;
            return object;
        };

        /**
         * Converts this JpReq to JSON.
         * @function toJSON
         * @memberof jlcb.JpReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JpReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JpReq
         * @function getTypeUrl
         * @memberof jlcb.JpReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JpReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.JpReq";
        };

        return JpReq;
    })();

    jlcb.JPInfo = (function() {

        /**
         * Properties of a JPInfo.
         * @memberof jlcb
         * @interface IJPInfo
         * @property {number|null} [Self] JPInfo Self
         * @property {Array.<number>|null} [Fronts] JPInfo Fronts
         */

        /**
         * Constructs a new JPInfo.
         * @memberof jlcb
         * @classdesc Represents a JPInfo.
         * @implements IJPInfo
         * @constructor
         * @param {jlcb.IJPInfo=} [properties] Properties to set
         */
        function JPInfo(properties) {
            this.Fronts = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPInfo Self.
         * @member {number} Self
         * @memberof jlcb.JPInfo
         * @instance
         */
        JPInfo.prototype.Self = 0;

        /**
         * JPInfo Fronts.
         * @member {Array.<number>} Fronts
         * @memberof jlcb.JPInfo
         * @instance
         */
        JPInfo.prototype.Fronts = $util.emptyArray;

        /**
         * Creates a new JPInfo instance using the specified properties.
         * @function create
         * @memberof jlcb.JPInfo
         * @static
         * @param {jlcb.IJPInfo=} [properties] Properties to set
         * @returns {jlcb.JPInfo} JPInfo instance
         */
        JPInfo.create = function create(properties) {
            return new JPInfo(properties);
        };

        /**
         * Encodes the specified JPInfo message. Does not implicitly {@link jlcb.JPInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcb.JPInfo
         * @static
         * @param {jlcb.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Self != null && Object.hasOwnProperty.call(message, "Self"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Self);
            if (message.Fronts != null && message.Fronts.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Fronts.length; ++i)
                    writer.double(message.Fronts[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified JPInfo message, length delimited. Does not implicitly {@link jlcb.JPInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.JPInfo
         * @static
         * @param {jlcb.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.JPInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Self = reader.double();
                        break;
                    }
                case 2: {
                        if (!(message.Fronts && message.Fronts.length))
                            message.Fronts = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Fronts.push(reader.double());
                        } else
                            message.Fronts.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPInfo message.
         * @function verify
         * @memberof jlcb.JPInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Self != null && message.hasOwnProperty("Self"))
                if (typeof message.Self !== "number")
                    return "Self: number expected";
            if (message.Fronts != null && message.hasOwnProperty("Fronts")) {
                if (!Array.isArray(message.Fronts))
                    return "Fronts: array expected";
                for (let i = 0; i < message.Fronts.length; ++i)
                    if (typeof message.Fronts[i] !== "number")
                        return "Fronts: number[] expected";
            }
            return null;
        };

        /**
         * Creates a JPInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.JPInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.JPInfo} JPInfo
         */
        JPInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.JPInfo)
                return object;
            let message = new $root.jlcb.JPInfo();
            if (object.Self != null)
                message.Self = Number(object.Self);
            if (object.Fronts) {
                if (!Array.isArray(object.Fronts))
                    throw TypeError(".jlcb.JPInfo.Fronts: array expected");
                message.Fronts = [];
                for (let i = 0; i < object.Fronts.length; ++i)
                    message.Fronts[i] = Number(object.Fronts[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a JPInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.JPInfo
         * @static
         * @param {jlcb.JPInfo} message JPInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Fronts = [];
            if (options.defaults)
                object.Self = 0;
            if (message.Self != null && message.hasOwnProperty("Self"))
                object.Self = options.json && !isFinite(message.Self) ? String(message.Self) : message.Self;
            if (message.Fronts && message.Fronts.length) {
                object.Fronts = [];
                for (let j = 0; j < message.Fronts.length; ++j)
                    object.Fronts[j] = options.json && !isFinite(message.Fronts[j]) ? String(message.Fronts[j]) : message.Fronts[j];
            }
            return object;
        };

        /**
         * Converts this JPInfo to JSON.
         * @function toJSON
         * @memberof jlcb.JPInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPInfo
         * @function getTypeUrl
         * @memberof jlcb.JPInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.JPInfo";
        };

        return JPInfo;
    })();

    jlcb.ChangeBetReq = (function() {

        /**
         * Properties of a ChangeBetReq.
         * @memberof jlcb
         * @interface IChangeBetReq
         * @property {number|null} [bet] ChangeBetReq bet
         */

        /**
         * Constructs a new ChangeBetReq.
         * @memberof jlcb
         * @classdesc Represents a ChangeBetReq.
         * @implements IChangeBetReq
         * @constructor
         * @param {jlcb.IChangeBetReq=} [properties] Properties to set
         */
        function ChangeBetReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeBetReq bet.
         * @member {number} bet
         * @memberof jlcb.ChangeBetReq
         * @instance
         */
        ChangeBetReq.prototype.bet = 0;

        /**
         * Creates a new ChangeBetReq instance using the specified properties.
         * @function create
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {jlcb.IChangeBetReq=} [properties] Properties to set
         * @returns {jlcb.ChangeBetReq} ChangeBetReq instance
         */
        ChangeBetReq.create = function create(properties) {
            return new ChangeBetReq(properties);
        };

        /**
         * Encodes the specified ChangeBetReq message. Does not implicitly {@link jlcb.ChangeBetReq.verify|verify} messages.
         * @function encode
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {jlcb.IChangeBetReq} message ChangeBetReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeBetReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bet != null && Object.hasOwnProperty.call(message, "bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.bet);
            return writer;
        };

        /**
         * Encodes the specified ChangeBetReq message, length delimited. Does not implicitly {@link jlcb.ChangeBetReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {jlcb.IChangeBetReq} message ChangeBetReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeBetReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChangeBetReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.ChangeBetReq} ChangeBetReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeBetReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.ChangeBetReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChangeBetReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.ChangeBetReq} ChangeBetReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeBetReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChangeBetReq message.
         * @function verify
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChangeBetReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bet != null && message.hasOwnProperty("bet"))
                if (typeof message.bet !== "number")
                    return "bet: number expected";
            return null;
        };

        /**
         * Creates a ChangeBetReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.ChangeBetReq} ChangeBetReq
         */
        ChangeBetReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.ChangeBetReq)
                return object;
            let message = new $root.jlcb.ChangeBetReq();
            if (object.bet != null)
                message.bet = Number(object.bet);
            return message;
        };

        /**
         * Creates a plain object from a ChangeBetReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {jlcb.ChangeBetReq} message ChangeBetReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeBetReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.bet = 0;
            if (message.bet != null && message.hasOwnProperty("bet"))
                object.bet = options.json && !isFinite(message.bet) ? String(message.bet) : message.bet;
            return object;
        };

        /**
         * Converts this ChangeBetReq to JSON.
         * @function toJSON
         * @memberof jlcb.ChangeBetReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeBetReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeBetReq
         * @function getTypeUrl
         * @memberof jlcb.ChangeBetReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeBetReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.ChangeBetReq";
        };

        return ChangeBetReq;
    })();

    jlcb.ChangeBetAck = (function() {

        /**
         * Properties of a ChangeBetAck.
         * @memberof jlcb
         * @interface IChangeBetAck
         * @property {number|null} [NewBet] ChangeBetAck NewBet
         * @property {number|null} [UpCount] ChangeBetAck UpCount
         */

        /**
         * Constructs a new ChangeBetAck.
         * @memberof jlcb
         * @classdesc Represents a ChangeBetAck.
         * @implements IChangeBetAck
         * @constructor
         * @param {jlcb.IChangeBetAck=} [properties] Properties to set
         */
        function ChangeBetAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeBetAck NewBet.
         * @member {number} NewBet
         * @memberof jlcb.ChangeBetAck
         * @instance
         */
        ChangeBetAck.prototype.NewBet = 0;

        /**
         * ChangeBetAck UpCount.
         * @member {number} UpCount
         * @memberof jlcb.ChangeBetAck
         * @instance
         */
        ChangeBetAck.prototype.UpCount = 0;

        /**
         * Creates a new ChangeBetAck instance using the specified properties.
         * @function create
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {jlcb.IChangeBetAck=} [properties] Properties to set
         * @returns {jlcb.ChangeBetAck} ChangeBetAck instance
         */
        ChangeBetAck.create = function create(properties) {
            return new ChangeBetAck(properties);
        };

        /**
         * Encodes the specified ChangeBetAck message. Does not implicitly {@link jlcb.ChangeBetAck.verify|verify} messages.
         * @function encode
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {jlcb.IChangeBetAck} message ChangeBetAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeBetAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.NewBet != null && Object.hasOwnProperty.call(message, "NewBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.NewBet);
            if (message.UpCount != null && Object.hasOwnProperty.call(message, "UpCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.UpCount);
            return writer;
        };

        /**
         * Encodes the specified ChangeBetAck message, length delimited. Does not implicitly {@link jlcb.ChangeBetAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {jlcb.IChangeBetAck} message ChangeBetAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeBetAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChangeBetAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.ChangeBetAck} ChangeBetAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeBetAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.ChangeBetAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.NewBet = reader.double();
                        break;
                    }
                case 2: {
                        message.UpCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChangeBetAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.ChangeBetAck} ChangeBetAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeBetAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChangeBetAck message.
         * @function verify
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChangeBetAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.NewBet != null && message.hasOwnProperty("NewBet"))
                if (typeof message.NewBet !== "number")
                    return "NewBet: number expected";
            if (message.UpCount != null && message.hasOwnProperty("UpCount"))
                if (!$util.isInteger(message.UpCount))
                    return "UpCount: integer expected";
            return null;
        };

        /**
         * Creates a ChangeBetAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.ChangeBetAck} ChangeBetAck
         */
        ChangeBetAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.ChangeBetAck)
                return object;
            let message = new $root.jlcb.ChangeBetAck();
            if (object.NewBet != null)
                message.NewBet = Number(object.NewBet);
            if (object.UpCount != null)
                message.UpCount = object.UpCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a ChangeBetAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {jlcb.ChangeBetAck} message ChangeBetAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeBetAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.NewBet = 0;
                object.UpCount = 0;
            }
            if (message.NewBet != null && message.hasOwnProperty("NewBet"))
                object.NewBet = options.json && !isFinite(message.NewBet) ? String(message.NewBet) : message.NewBet;
            if (message.UpCount != null && message.hasOwnProperty("UpCount"))
                object.UpCount = message.UpCount;
            return object;
        };

        /**
         * Converts this ChangeBetAck to JSON.
         * @function toJSON
         * @memberof jlcb.ChangeBetAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeBetAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeBetAck
         * @function getTypeUrl
         * @memberof jlcb.ChangeBetAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeBetAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.ChangeBetAck";
        };

        return ChangeBetAck;
    })();

    jlcb.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlcb
         * @interface IGameInfoAck
         * @property {number|null} [LockBet] GameInfoAck LockBet
         * @property {number|null} [Nowsize] GameInfoAck Nowsize
         * @property {number|null} [Upcount] GameInfoAck Upcount
         * @property {number|null} [JpBetMin] GameInfoAck JpBetMin
         * @property {number|null} [JpBetBase] GameInfoAck JpBetBase
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlcb
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlcb.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck LockBet.
         * @member {number} LockBet
         * @memberof jlcb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.LockBet = 0;

        /**
         * GameInfoAck Nowsize.
         * @member {number} Nowsize
         * @memberof jlcb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Nowsize = 0;

        /**
         * GameInfoAck Upcount.
         * @member {number} Upcount
         * @memberof jlcb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Upcount = 0;

        /**
         * GameInfoAck JpBetMin.
         * @member {number} JpBetMin
         * @memberof jlcb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.JpBetMin = 0;

        /**
         * GameInfoAck JpBetBase.
         * @member {number} JpBetBase
         * @memberof jlcb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.JpBetBase = 0;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {jlcb.IGameInfoAck=} [properties] Properties to set
         * @returns {jlcb.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlcb.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {jlcb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LockBet != null && Object.hasOwnProperty.call(message, "LockBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.LockBet);
            if (message.Nowsize != null && Object.hasOwnProperty.call(message, "Nowsize"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Nowsize);
            if (message.Upcount != null && Object.hasOwnProperty.call(message, "Upcount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Upcount);
            if (message.JpBetMin != null && Object.hasOwnProperty.call(message, "JpBetMin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.JpBetMin);
            if (message.JpBetBase != null && Object.hasOwnProperty.call(message, "JpBetBase"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.JpBetBase);
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlcb.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {jlcb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LockBet = reader.double();
                        break;
                    }
                case 2: {
                        message.Nowsize = reader.int32();
                        break;
                    }
                case 3: {
                        message.Upcount = reader.int32();
                        break;
                    }
                case 4: {
                        message.JpBetMin = reader.double();
                        break;
                    }
                case 5: {
                        message.JpBetBase = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LockBet != null && message.hasOwnProperty("LockBet"))
                if (typeof message.LockBet !== "number")
                    return "LockBet: number expected";
            if (message.Nowsize != null && message.hasOwnProperty("Nowsize"))
                if (!$util.isInteger(message.Nowsize))
                    return "Nowsize: integer expected";
            if (message.Upcount != null && message.hasOwnProperty("Upcount"))
                if (!$util.isInteger(message.Upcount))
                    return "Upcount: integer expected";
            if (message.JpBetMin != null && message.hasOwnProperty("JpBetMin"))
                if (typeof message.JpBetMin !== "number")
                    return "JpBetMin: number expected";
            if (message.JpBetBase != null && message.hasOwnProperty("JpBetBase"))
                if (typeof message.JpBetBase !== "number")
                    return "JpBetBase: number expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.GameInfoAck)
                return object;
            let message = new $root.jlcb.GameInfoAck();
            if (object.LockBet != null)
                message.LockBet = Number(object.LockBet);
            if (object.Nowsize != null)
                message.Nowsize = object.Nowsize | 0;
            if (object.Upcount != null)
                message.Upcount = object.Upcount | 0;
            if (object.JpBetMin != null)
                message.JpBetMin = Number(object.JpBetMin);
            if (object.JpBetBase != null)
                message.JpBetBase = Number(object.JpBetBase);
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {jlcb.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.LockBet = 0;
                object.Nowsize = 0;
                object.Upcount = 0;
                object.JpBetMin = 0;
                object.JpBetBase = 0;
            }
            if (message.LockBet != null && message.hasOwnProperty("LockBet"))
                object.LockBet = options.json && !isFinite(message.LockBet) ? String(message.LockBet) : message.LockBet;
            if (message.Nowsize != null && message.hasOwnProperty("Nowsize"))
                object.Nowsize = message.Nowsize;
            if (message.Upcount != null && message.hasOwnProperty("Upcount"))
                object.Upcount = message.Upcount;
            if (message.JpBetMin != null && message.hasOwnProperty("JpBetMin"))
                object.JpBetMin = options.json && !isFinite(message.JpBetMin) ? String(message.JpBetMin) : message.JpBetMin;
            if (message.JpBetBase != null && message.hasOwnProperty("JpBetBase"))
                object.JpBetBase = options.json && !isFinite(message.JpBetBase) ? String(message.JpBetBase) : message.JpBetBase;
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlcb.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlcb.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jlcb.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlcb
         * @interface IStateInfo
         * @property {number|null} [NowSize] StateInfo NowSize
         * @property {number|null} [NowSizeType] StateInfo NowSizeType
         * @property {number|null} [NowLockBet] StateInfo NowLockBet
         * @property {number|null} [UpCount] StateInfo UpCount
         * @property {number|null} [NowSpinUpCount] StateInfo NowSpinUpCount
         * @property {number|null} [SelfJP] StateInfo SelfJP
         * @property {string|null} [Upmap] StateInfo Upmap
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlcb
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlcb.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo NowSize.
         * @member {number} NowSize
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.NowSize = 0;

        /**
         * StateInfo NowSizeType.
         * @member {number} NowSizeType
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.NowSizeType = 0;

        /**
         * StateInfo NowLockBet.
         * @member {number} NowLockBet
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.NowLockBet = 0;

        /**
         * StateInfo UpCount.
         * @member {number} UpCount
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.UpCount = 0;

        /**
         * StateInfo NowSpinUpCount.
         * @member {number} NowSpinUpCount
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.NowSpinUpCount = 0;

        /**
         * StateInfo SelfJP.
         * @member {number} SelfJP
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.SelfJP = 0;

        /**
         * StateInfo Upmap.
         * @member {string} Upmap
         * @memberof jlcb.StateInfo
         * @instance
         */
        StateInfo.prototype.Upmap = "";

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlcb.StateInfo
         * @static
         * @param {jlcb.IStateInfo=} [properties] Properties to set
         * @returns {jlcb.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlcb.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcb.StateInfo
         * @static
         * @param {jlcb.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.NowSize != null && Object.hasOwnProperty.call(message, "NowSize"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.NowSize);
            if (message.NowSizeType != null && Object.hasOwnProperty.call(message, "NowSizeType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.NowSizeType);
            if (message.NowLockBet != null && Object.hasOwnProperty.call(message, "NowLockBet"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.NowLockBet);
            if (message.UpCount != null && Object.hasOwnProperty.call(message, "UpCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.UpCount);
            if (message.NowSpinUpCount != null && Object.hasOwnProperty.call(message, "NowSpinUpCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.NowSpinUpCount);
            if (message.SelfJP != null && Object.hasOwnProperty.call(message, "SelfJP"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.SelfJP);
            if (message.Upmap != null && Object.hasOwnProperty.call(message, "Upmap"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.Upmap);
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlcb.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcb.StateInfo
         * @static
         * @param {jlcb.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcb.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcb.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcb.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.NowSize = reader.int32();
                        break;
                    }
                case 2: {
                        message.NowSizeType = reader.int32();
                        break;
                    }
                case 3: {
                        message.NowLockBet = reader.double();
                        break;
                    }
                case 4: {
                        message.UpCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.NowSpinUpCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.SelfJP = reader.double();
                        break;
                    }
                case 7: {
                        message.Upmap = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcb.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcb.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlcb.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.NowSize != null && message.hasOwnProperty("NowSize"))
                if (!$util.isInteger(message.NowSize))
                    return "NowSize: integer expected";
            if (message.NowSizeType != null && message.hasOwnProperty("NowSizeType"))
                if (!$util.isInteger(message.NowSizeType))
                    return "NowSizeType: integer expected";
            if (message.NowLockBet != null && message.hasOwnProperty("NowLockBet"))
                if (typeof message.NowLockBet !== "number")
                    return "NowLockBet: number expected";
            if (message.UpCount != null && message.hasOwnProperty("UpCount"))
                if (!$util.isInteger(message.UpCount))
                    return "UpCount: integer expected";
            if (message.NowSpinUpCount != null && message.hasOwnProperty("NowSpinUpCount"))
                if (!$util.isInteger(message.NowSpinUpCount))
                    return "NowSpinUpCount: integer expected";
            if (message.SelfJP != null && message.hasOwnProperty("SelfJP"))
                if (typeof message.SelfJP !== "number")
                    return "SelfJP: number expected";
            if (message.Upmap != null && message.hasOwnProperty("Upmap"))
                if (!$util.isString(message.Upmap))
                    return "Upmap: string expected";
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcb.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcb.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcb.StateInfo)
                return object;
            let message = new $root.jlcb.StateInfo();
            if (object.NowSize != null)
                message.NowSize = object.NowSize | 0;
            if (object.NowSizeType != null)
                message.NowSizeType = object.NowSizeType | 0;
            if (object.NowLockBet != null)
                message.NowLockBet = Number(object.NowLockBet);
            if (object.UpCount != null)
                message.UpCount = object.UpCount | 0;
            if (object.NowSpinUpCount != null)
                message.NowSpinUpCount = object.NowSpinUpCount | 0;
            if (object.SelfJP != null)
                message.SelfJP = Number(object.SelfJP);
            if (object.Upmap != null)
                message.Upmap = String(object.Upmap);
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcb.StateInfo
         * @static
         * @param {jlcb.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.NowSize = 0;
                object.NowSizeType = 0;
                object.NowLockBet = 0;
                object.UpCount = 0;
                object.NowSpinUpCount = 0;
                object.SelfJP = 0;
                object.Upmap = "";
            }
            if (message.NowSize != null && message.hasOwnProperty("NowSize"))
                object.NowSize = message.NowSize;
            if (message.NowSizeType != null && message.hasOwnProperty("NowSizeType"))
                object.NowSizeType = message.NowSizeType;
            if (message.NowLockBet != null && message.hasOwnProperty("NowLockBet"))
                object.NowLockBet = options.json && !isFinite(message.NowLockBet) ? String(message.NowLockBet) : message.NowLockBet;
            if (message.UpCount != null && message.hasOwnProperty("UpCount"))
                object.UpCount = message.UpCount;
            if (message.NowSpinUpCount != null && message.hasOwnProperty("NowSpinUpCount"))
                object.NowSpinUpCount = message.NowSpinUpCount;
            if (message.SelfJP != null && message.hasOwnProperty("SelfJP"))
                object.SelfJP = options.json && !isFinite(message.SelfJP) ? String(message.SelfJP) : message.SelfJP;
            if (message.Upmap != null && message.hasOwnProperty("Upmap"))
                object.Upmap = message.Upmap;
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlcb.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlcb.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcb.StateInfo";
        };

        return StateInfo;
    })();

    return jlcb;
})();

export const jlcc = $root.jlcc = (() => {

    /**
     * Namespace jlcc.
     * @exports jlcc
     * @namespace
     */
    const jlcc = {};

    jlcc.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlcc
         * @interface IColumn
         * @property {Array.<string>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlcc
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlcc.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<string>} Col
         * @memberof jlcc.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlcc.Column
         * @static
         * @param {jlcc.IColumn=} [properties] Properties to set
         * @returns {jlcc.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlcc.Column.verify|verify} messages.
         * @function encode
         * @memberof jlcc.Column
         * @static
         * @param {jlcc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.Col[i]);
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlcc.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.Column
         * @static
         * @param {jlcc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlcc.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isString(message.Col[i]))
                        return "Col: string[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.Column)
                return object;
            let message = new $root.jlcc.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlcc.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = String(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.Column
         * @static
         * @param {jlcc.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlcc.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlcc.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.Column";
        };

        return Column;
    })();

    jlcc.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcc
         * @interface IAwardData
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcc
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcc.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlcc.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlcc.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcc.AwardData
         * @static
         * @param {jlcc.IAwardData=} [properties] Properties to set
         * @returns {jlcc.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcc.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcc.AwardData
         * @static
         * @param {jlcc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Index);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcc.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.AwardData
         * @static
         * @param {jlcc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Index = reader.int32();
                        break;
                    }
                case 2: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcc.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.AwardData)
                return object;
            let message = new $root.jlcc.AwardData();
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.AwardData
         * @static
         * @param {jlcc.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Index = 0;
                object.Win = 0;
            }
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcc.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcc.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.AwardData";
        };

        return AwardData;
    })();

    jlcc.SpecialData = (function() {

        /**
         * Properties of a SpecialData.
         * @memberof jlcc
         * @interface ISpecialData
         * @property {number|null} [Pos] SpecialData Pos
         * @property {number|null} [SPType] SpecialData SPType
         * @property {number|null} [Origin] SpecialData Origin
         * @property {number|null} [Final] SpecialData Final
         */

        /**
         * Constructs a new SpecialData.
         * @memberof jlcc
         * @classdesc Represents a SpecialData.
         * @implements ISpecialData
         * @constructor
         * @param {jlcc.ISpecialData=} [properties] Properties to set
         */
        function SpecialData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpecialData Pos.
         * @member {number} Pos
         * @memberof jlcc.SpecialData
         * @instance
         */
        SpecialData.prototype.Pos = 0;

        /**
         * SpecialData SPType.
         * @member {number} SPType
         * @memberof jlcc.SpecialData
         * @instance
         */
        SpecialData.prototype.SPType = 0;

        /**
         * SpecialData Origin.
         * @member {number} Origin
         * @memberof jlcc.SpecialData
         * @instance
         */
        SpecialData.prototype.Origin = 0;

        /**
         * SpecialData Final.
         * @member {number} Final
         * @memberof jlcc.SpecialData
         * @instance
         */
        SpecialData.prototype.Final = 0;

        /**
         * Creates a new SpecialData instance using the specified properties.
         * @function create
         * @memberof jlcc.SpecialData
         * @static
         * @param {jlcc.ISpecialData=} [properties] Properties to set
         * @returns {jlcc.SpecialData} SpecialData instance
         */
        SpecialData.create = function create(properties) {
            return new SpecialData(properties);
        };

        /**
         * Encodes the specified SpecialData message. Does not implicitly {@link jlcc.SpecialData.verify|verify} messages.
         * @function encode
         * @memberof jlcc.SpecialData
         * @static
         * @param {jlcc.ISpecialData} message SpecialData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpecialData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.SPType != null && Object.hasOwnProperty.call(message, "SPType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SPType);
            if (message.Origin != null && Object.hasOwnProperty.call(message, "Origin"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Origin);
            if (message.Final != null && Object.hasOwnProperty.call(message, "Final"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Final);
            return writer;
        };

        /**
         * Encodes the specified SpecialData message, length delimited. Does not implicitly {@link jlcc.SpecialData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.SpecialData
         * @static
         * @param {jlcc.ISpecialData} message SpecialData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpecialData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpecialData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.SpecialData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.SpecialData} SpecialData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpecialData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.SpecialData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.SPType = reader.int32();
                        break;
                    }
                case 3: {
                        message.Origin = reader.int32();
                        break;
                    }
                case 4: {
                        message.Final = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpecialData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.SpecialData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.SpecialData} SpecialData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpecialData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpecialData message.
         * @function verify
         * @memberof jlcc.SpecialData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpecialData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.SPType != null && message.hasOwnProperty("SPType"))
                if (!$util.isInteger(message.SPType))
                    return "SPType: integer expected";
            if (message.Origin != null && message.hasOwnProperty("Origin"))
                if (!$util.isInteger(message.Origin))
                    return "Origin: integer expected";
            if (message.Final != null && message.hasOwnProperty("Final"))
                if (!$util.isInteger(message.Final))
                    return "Final: integer expected";
            return null;
        };

        /**
         * Creates a SpecialData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.SpecialData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.SpecialData} SpecialData
         */
        SpecialData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.SpecialData)
                return object;
            let message = new $root.jlcc.SpecialData();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.SPType != null)
                message.SPType = object.SPType | 0;
            if (object.Origin != null)
                message.Origin = object.Origin | 0;
            if (object.Final != null)
                message.Final = object.Final | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpecialData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.SpecialData
         * @static
         * @param {jlcc.SpecialData} message SpecialData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpecialData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.SPType = 0;
                object.Origin = 0;
                object.Final = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.SPType != null && message.hasOwnProperty("SPType"))
                object.SPType = message.SPType;
            if (message.Origin != null && message.hasOwnProperty("Origin"))
                object.Origin = message.Origin;
            if (message.Final != null && message.hasOwnProperty("Final"))
                object.Final = message.Final;
            return object;
        };

        /**
         * Converts this SpecialData to JSON.
         * @function toJSON
         * @memberof jlcc.SpecialData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpecialData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpecialData
         * @function getTypeUrl
         * @memberof jlcc.SpecialData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpecialData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.SpecialData";
        };

        return SpecialData;
    })();

    jlcc.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlcc
         * @interface IRoundInfo
         * @property {Array.<jlcc.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [Bet] RoundInfo Bet
         * @property {Array.<jlcc.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {Array.<jlcc.ISpecialData>|null} [SpeicalVec] RoundInfo SpeicalVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlcc
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlcc.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.AwardDataVec = [];
            this.SpeicalVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jlcc.IColumn>} Symbol
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo Bet.
         * @member {number} Bet
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Bet = 0;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlcc.IAwardData>} AwardDataVec
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo SpeicalVec.
         * @member {Array.<jlcc.ISpecialData>} SpeicalVec
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.SpeicalVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlcc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlcc.RoundInfo
         * @static
         * @param {jlcc.IRoundInfo=} [properties] Properties to set
         * @returns {jlcc.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlcc.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcc.RoundInfo
         * @static
         * @param {jlcc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlcc.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Bet);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlcc.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.SpeicalVec != null && message.SpeicalVec.length)
                for (let i = 0; i < message.SpeicalVec.length; ++i)
                    $root.jlcc.SpecialData.encode(message.SpeicalVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlcc.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.RoundInfo
         * @static
         * @param {jlcc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlcc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.Bet = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlcc.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.SpeicalVec && message.SpeicalVec.length))
                            message.SpeicalVec = [];
                        message.SpeicalVec.push($root.jlcc.SpecialData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlcc.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlcc.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlcc.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.SpeicalVec != null && message.hasOwnProperty("SpeicalVec")) {
                if (!Array.isArray(message.SpeicalVec))
                    return "SpeicalVec: array expected";
                for (let i = 0; i < message.SpeicalVec.length; ++i) {
                    let error = $root.jlcc.SpecialData.verify(message.SpeicalVec[i]);
                    if (error)
                        return "SpeicalVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.RoundInfo)
                return object;
            let message = new $root.jlcc.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlcc.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlcc.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jlcc.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlcc.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlcc.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlcc.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.SpeicalVec) {
                if (!Array.isArray(object.SpeicalVec))
                    throw TypeError(".jlcc.RoundInfo.SpeicalVec: array expected");
                message.SpeicalVec = [];
                for (let i = 0; i < object.SpeicalVec.length; ++i) {
                    if (typeof object.SpeicalVec[i] !== "object")
                        throw TypeError(".jlcc.RoundInfo.SpeicalVec: object expected");
                    message.SpeicalVec[i] = $root.jlcc.SpecialData.fromObject(object.SpeicalVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.RoundInfo
         * @static
         * @param {jlcc.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.AwardDataVec = [];
                object.SpeicalVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.Bet = 0;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlcc.Column.toObject(message.Symbol[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlcc.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.SpeicalVec && message.SpeicalVec.length) {
                object.SpeicalVec = [];
                for (let j = 0; j < message.SpeicalVec.length; ++j)
                    object.SpeicalVec[j] = $root.jlcc.SpecialData.toObject(message.SpeicalVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlcc.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlcc.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.RoundInfo";
        };

        return RoundInfo;
    })();

    jlcc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcc
         * @interface ISpinAck
         * @property {Array.<jlcc.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {number|null} [AckType] SpinAck AckType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlcc.IRoundInfo>} RoundQueue
         * @memberof jlcc.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlcc.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlcc.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlcc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcc.SpinAck
         * @static
         * @param {jlcc.ISpinAck=} [properties] Properties to set
         * @returns {jlcc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcc.SpinAck
         * @static
         * @param {jlcc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlcc.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.RID);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.SpinAck
         * @static
         * @param {jlcc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlcc.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.RTP = reader.double();
                        break;
                    }
                case 4: {
                        message.LID = reader.int32();
                        break;
                    }
                case 5: {
                        message.RID = reader.int32();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlcc.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.SpinAck)
                return object;
            let message = new $root.jlcc.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlcc.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlcc.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlcc.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.SpinAck
         * @static
         * @param {jlcc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.AckType = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlcc.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.SpinAck";
        };

        return SpinAck;
    })();

    jlcc.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlcc
         * @interface IGameInfoData
         * @property {number|null} [LevelUp] GameInfoData LevelUp
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlcc
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlcc.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData LevelUp.
         * @member {number} LevelUp
         * @memberof jlcc.GameInfoData
         * @instance
         */
        GameInfoData.prototype.LevelUp = 0;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlcc.GameInfoData
         * @static
         * @param {jlcc.IGameInfoData=} [properties] Properties to set
         * @returns {jlcc.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlcc.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlcc.GameInfoData
         * @static
         * @param {jlcc.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LevelUp != null && Object.hasOwnProperty.call(message, "LevelUp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.LevelUp);
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlcc.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.GameInfoData
         * @static
         * @param {jlcc.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LevelUp = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlcc.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LevelUp != null && message.hasOwnProperty("LevelUp"))
                if (!$util.isInteger(message.LevelUp))
                    return "LevelUp: integer expected";
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.GameInfoData)
                return object;
            let message = new $root.jlcc.GameInfoData();
            if (object.LevelUp != null)
                message.LevelUp = object.LevelUp | 0;
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.GameInfoData
         * @static
         * @param {jlcc.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.LevelUp = 0;
            if (message.LevelUp != null && message.hasOwnProperty("LevelUp"))
                object.LevelUp = message.LevelUp;
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlcc.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlcc.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.GameInfoData";
        };

        return GameInfoData;
    })();

    jlcc.LevelUpInfo = (function() {

        /**
         * Properties of a LevelUpInfo.
         * @memberof jlcc
         * @interface ILevelUpInfo
         * @property {number|null} [LevelUp] LevelUpInfo LevelUp
         */

        /**
         * Constructs a new LevelUpInfo.
         * @memberof jlcc
         * @classdesc Represents a LevelUpInfo.
         * @implements ILevelUpInfo
         * @constructor
         * @param {jlcc.ILevelUpInfo=} [properties] Properties to set
         */
        function LevelUpInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LevelUpInfo LevelUp.
         * @member {number} LevelUp
         * @memberof jlcc.LevelUpInfo
         * @instance
         */
        LevelUpInfo.prototype.LevelUp = 0;

        /**
         * Creates a new LevelUpInfo instance using the specified properties.
         * @function create
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {jlcc.ILevelUpInfo=} [properties] Properties to set
         * @returns {jlcc.LevelUpInfo} LevelUpInfo instance
         */
        LevelUpInfo.create = function create(properties) {
            return new LevelUpInfo(properties);
        };

        /**
         * Encodes the specified LevelUpInfo message. Does not implicitly {@link jlcc.LevelUpInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {jlcc.ILevelUpInfo} message LevelUpInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LevelUpInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LevelUp != null && Object.hasOwnProperty.call(message, "LevelUp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.LevelUp);
            return writer;
        };

        /**
         * Encodes the specified LevelUpInfo message, length delimited. Does not implicitly {@link jlcc.LevelUpInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {jlcc.ILevelUpInfo} message LevelUpInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LevelUpInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LevelUpInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcc.LevelUpInfo} LevelUpInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LevelUpInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcc.LevelUpInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LevelUp = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LevelUpInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcc.LevelUpInfo} LevelUpInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LevelUpInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LevelUpInfo message.
         * @function verify
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LevelUpInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LevelUp != null && message.hasOwnProperty("LevelUp"))
                if (!$util.isInteger(message.LevelUp))
                    return "LevelUp: integer expected";
            return null;
        };

        /**
         * Creates a LevelUpInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcc.LevelUpInfo} LevelUpInfo
         */
        LevelUpInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcc.LevelUpInfo)
                return object;
            let message = new $root.jlcc.LevelUpInfo();
            if (object.LevelUp != null)
                message.LevelUp = object.LevelUp | 0;
            return message;
        };

        /**
         * Creates a plain object from a LevelUpInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {jlcc.LevelUpInfo} message LevelUpInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LevelUpInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.LevelUp = 0;
            if (message.LevelUp != null && message.hasOwnProperty("LevelUp"))
                object.LevelUp = message.LevelUp;
            return object;
        };

        /**
         * Converts this LevelUpInfo to JSON.
         * @function toJSON
         * @memberof jlcc.LevelUpInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LevelUpInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LevelUpInfo
         * @function getTypeUrl
         * @memberof jlcc.LevelUpInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LevelUpInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcc.LevelUpInfo";
        };

        return LevelUpInfo;
    })();

    return jlcc;
})();

export const jlcfff = $root.jlcfff = (() => {

    /**
     * Namespace jlcfff.
     * @exports jlcfff
     * @namespace
     */
    const jlcfff = {};

    jlcfff.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlcfff
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlcfff
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlcfff.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlcfff.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlcfff.Column
         * @static
         * @param {jlcfff.IColumn=} [properties] Properties to set
         * @returns {jlcfff.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlcfff.Column.verify|verify} messages.
         * @function encode
         * @memberof jlcfff.Column
         * @static
         * @param {jlcfff.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlcfff.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcfff.Column
         * @static
         * @param {jlcfff.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlcfff.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcfff.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcfff.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcfff.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcfff.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlcfff.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcfff.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcfff.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcfff.Column)
                return object;
            let message = new $root.jlcfff.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlcfff.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcfff.Column
         * @static
         * @param {jlcfff.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlcfff.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlcfff.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcfff.Column";
        };

        return Column;
    })();

    jlcfff.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcfff
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Plate] AwardData Plate
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcfff
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcfff.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlcfff.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Plate.
         * @member {number} Plate
         * @memberof jlcfff.AwardData
         * @instance
         */
        AwardData.prototype.Plate = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlcfff.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlcfff.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcfff.AwardData
         * @static
         * @param {jlcfff.IAwardData=} [properties] Properties to set
         * @returns {jlcfff.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcfff.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcfff.AwardData
         * @static
         * @param {jlcfff.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Plate != null && Object.hasOwnProperty.call(message, "Plate"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Plate);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcfff.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcfff.AwardData
         * @static
         * @param {jlcfff.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcfff.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcfff.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcfff.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Plate = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcfff.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcfff.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcfff.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Plate != null && message.hasOwnProperty("Plate"))
                if (!$util.isInteger(message.Plate))
                    return "Plate: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcfff.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcfff.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcfff.AwardData)
                return object;
            let message = new $root.jlcfff.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Plate != null)
                message.Plate = object.Plate | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcfff.AwardData
         * @static
         * @param {jlcfff.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Plate = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Plate != null && message.hasOwnProperty("Plate"))
                object.Plate = message.Plate;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcfff.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcfff.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcfff.AwardData";
        };

        return AwardData;
    })();

    jlcfff.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlcfff
         * @interface ISinglePlate
         * @property {Array.<jlcfff.IColumn>|null} [PlateSymbol] SinglePlate PlateSymbol
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {Array.<jlcfff.IAwardData>|null} [AwardDataVec] SinglePlate AwardDataVec
         * @property {Array.<number>|null} [PlateWin] SinglePlate PlateWin
         * @property {number|null} [BothWin] SinglePlate BothWin
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlcfff
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlcfff.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.PlateWin = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate PlateSymbol.
         * @member {Array.<jlcfff.IColumn>} PlateSymbol
         * @memberof jlcfff.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlcfff.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate AwardDataVec.
         * @member {Array.<jlcfff.IAwardData>} AwardDataVec
         * @memberof jlcfff.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SinglePlate PlateWin.
         * @member {Array.<number>} PlateWin
         * @memberof jlcfff.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateWin = $util.emptyArray;

        /**
         * SinglePlate BothWin.
         * @member {number} BothWin
         * @memberof jlcfff.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BothWin = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {jlcfff.ISinglePlate=} [properties] Properties to set
         * @returns {jlcfff.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlcfff.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {jlcfff.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlcfff.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardTypeFlag);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlcfff.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.PlateWin != null && message.PlateWin.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.PlateWin.length; ++i)
                    writer.double(message.PlateWin[i]);
                writer.ldelim();
            }
            if (message.BothWin != null && Object.hasOwnProperty.call(message, "BothWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.BothWin);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlcfff.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {jlcfff.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcfff.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcfff.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlcfff.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlcfff.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.PlateWin && message.PlateWin.length))
                            message.PlateWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateWin.push(reader.double());
                        } else
                            message.PlateWin.push(reader.double());
                        break;
                    }
                case 5: {
                        message.BothWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcfff.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlcfff.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlcfff.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin")) {
                if (!Array.isArray(message.PlateWin))
                    return "PlateWin: array expected";
                for (let i = 0; i < message.PlateWin.length; ++i)
                    if (typeof message.PlateWin[i] !== "number")
                        return "PlateWin: number[] expected";
            }
            if (message.BothWin != null && message.hasOwnProperty("BothWin"))
                if (typeof message.BothWin !== "number")
                    return "BothWin: number expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcfff.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcfff.SinglePlate)
                return object;
            let message = new $root.jlcfff.SinglePlate();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlcfff.SinglePlate.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlcfff.SinglePlate.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlcfff.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlcfff.SinglePlate.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlcfff.SinglePlate.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlcfff.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.PlateWin) {
                if (!Array.isArray(object.PlateWin))
                    throw TypeError(".jlcfff.SinglePlate.PlateWin: array expected");
                message.PlateWin = [];
                for (let i = 0; i < object.PlateWin.length; ++i)
                    message.PlateWin[i] = Number(object.PlateWin[i]);
            }
            if (object.BothWin != null)
                message.BothWin = Number(object.BothWin);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {jlcfff.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.PlateWin = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.BothWin = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlcfff.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlcfff.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.PlateWin && message.PlateWin.length) {
                object.PlateWin = [];
                for (let j = 0; j < message.PlateWin.length; ++j)
                    object.PlateWin[j] = options.json && !isFinite(message.PlateWin[j]) ? String(message.PlateWin[j]) : message.PlateWin[j];
            }
            if (message.BothWin != null && message.hasOwnProperty("BothWin"))
                object.BothWin = options.json && !isFinite(message.BothWin) ? String(message.BothWin) : message.BothWin;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlcfff.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlcfff.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcfff.SinglePlate";
        };

        return SinglePlate;
    })();

    jlcfff.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcfff
         * @interface ISpinAck
         * @property {Array.<jlcfff.ISinglePlate>|null} [AckQueue] SpinAck AckQueue
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcfff
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcfff.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.AckQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck AckQueue.
         * @member {Array.<jlcfff.ISinglePlate>} AckQueue
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.AckQueue = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlcfff.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcfff.SpinAck
         * @static
         * @param {jlcfff.ISpinAck=} [properties] Properties to set
         * @returns {jlcfff.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcfff.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcfff.SpinAck
         * @static
         * @param {jlcfff.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AckQueue != null && message.AckQueue.length)
                for (let i = 0; i < message.AckQueue.length; ++i)
                    $root.jlcfff.SinglePlate.encode(message.AckQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcfff.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcfff.SpinAck
         * @static
         * @param {jlcfff.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcfff.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcfff.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcfff.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AckQueue && message.AckQueue.length))
                            message.AckQueue = [];
                        message.AckQueue.push($root.jlcfff.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 4: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 5: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcfff.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcfff.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcfff.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AckQueue != null && message.hasOwnProperty("AckQueue")) {
                if (!Array.isArray(message.AckQueue))
                    return "AckQueue: array expected";
                for (let i = 0; i < message.AckQueue.length; ++i) {
                    let error = $root.jlcfff.SinglePlate.verify(message.AckQueue[i]);
                    if (error)
                        return "AckQueue." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcfff.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcfff.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcfff.SpinAck)
                return object;
            let message = new $root.jlcfff.SpinAck();
            if (object.AckQueue) {
                if (!Array.isArray(object.AckQueue))
                    throw TypeError(".jlcfff.SpinAck.AckQueue: array expected");
                message.AckQueue = [];
                for (let i = 0; i < object.AckQueue.length; ++i) {
                    if (typeof object.AckQueue[i] !== "object")
                        throw TypeError(".jlcfff.SpinAck.AckQueue: object expected");
                    message.AckQueue[i] = $root.jlcfff.SinglePlate.fromObject(object.AckQueue[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcfff.SpinAck
         * @static
         * @param {jlcfff.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AckQueue = [];
            if (options.defaults) {
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.AckQueue && message.AckQueue.length) {
                object.AckQueue = [];
                for (let j = 0; j < message.AckQueue.length; ++j)
                    object.AckQueue[j] = $root.jlcfff.SinglePlate.toObject(message.AckQueue[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcfff.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcfff.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcfff.SpinAck";
        };

        return SpinAck;
    })();

    jlcfff.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlcfff
         * @interface IGameInfoAck
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlcfff
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlcfff.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {jlcfff.IGameInfoAck=} [properties] Properties to set
         * @returns {jlcfff.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlcfff.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {jlcfff.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlcfff.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {jlcfff.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcfff.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcfff.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcfff.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcfff.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcfff.GameInfoAck)
                return object;
            return new $root.jlcfff.GameInfoAck();
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {jlcfff.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlcfff.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlcfff.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcfff.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlcfff;
})();

export const jlck18 = $root.jlck18 = (() => {

    /**
     * Namespace jlck18.
     * @exports jlck18
     * @namespace
     */
    const jlck18 = {};

    jlck18.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlck18
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlck18
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlck18.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlck18.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlck18.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlck18.Grid
         * @static
         * @param {jlck18.IGrid=} [properties] Properties to set
         * @returns {jlck18.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlck18.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlck18.Grid
         * @static
         * @param {jlck18.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlck18.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.Grid
         * @static
         * @param {jlck18.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlck18.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.Grid)
                return object;
            let message = new $root.jlck18.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.Grid
         * @static
         * @param {jlck18.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlck18.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlck18.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.Grid";
        };

        return Grid;
    })();

    jlck18.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlck18
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlck18
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlck18.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlck18.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlck18.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlck18.MSymbol
         * @static
         * @param {jlck18.IMSymbol=} [properties] Properties to set
         * @returns {jlck18.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlck18.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlck18.MSymbol
         * @static
         * @param {jlck18.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlck18.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.MSymbol
         * @static
         * @param {jlck18.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlck18.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.MSymbol)
                return object;
            let message = new $root.jlck18.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.MSymbol
         * @static
         * @param {jlck18.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlck18.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlck18.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.MSymbol";
        };

        return MSymbol;
    })();

    jlck18.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlck18
         * @interface IMColumn
         * @property {Array.<jlck18.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlck18
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlck18.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlck18.IMSymbol>} MColumn
         * @memberof jlck18.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlck18.MColumn
         * @static
         * @param {jlck18.IMColumn=} [properties] Properties to set
         * @returns {jlck18.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlck18.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlck18.MColumn
         * @static
         * @param {jlck18.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlck18.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlck18.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.MColumn
         * @static
         * @param {jlck18.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlck18.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlck18.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlck18.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.MColumn)
                return object;
            let message = new $root.jlck18.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlck18.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlck18.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlck18.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.MColumn
         * @static
         * @param {jlck18.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlck18.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlck18.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlck18.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.MColumn";
        };

        return MColumn;
    })();

    jlck18.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlck18
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlck18.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlck18
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlck18.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlck18.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlck18.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlck18.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlck18.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlck18.IGrid>} GridVec
         * @memberof jlck18.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlck18.AwardData
         * @static
         * @param {jlck18.IAwardData=} [properties] Properties to set
         * @returns {jlck18.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlck18.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlck18.AwardData
         * @static
         * @param {jlck18.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlck18.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlck18.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.AwardData
         * @static
         * @param {jlck18.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlck18.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlck18.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlck18.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.AwardData)
                return object;
            let message = new $root.jlck18.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlck18.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlck18.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlck18.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.AwardData
         * @static
         * @param {jlck18.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlck18.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlck18.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlck18.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.AwardData";
        };

        return AwardData;
    })();

    jlck18.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlck18
         * @interface IComboStageInfo
         * @property {Array.<jlck18.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlck18.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlck18.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlck18
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlck18.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlck18.IMColumn>} ComboStageSymbol
         * @memberof jlck18.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlck18.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlck18.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlck18.IAwardData>} AwardDataVec
         * @memberof jlck18.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlck18.IMColumn>} FillSymbols
         * @memberof jlck18.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {jlck18.IComboStageInfo=} [properties] Properties to set
         * @returns {jlck18.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlck18.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {jlck18.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlck18.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlck18.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlck18.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlck18.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {jlck18.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlck18.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlck18.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlck18.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlck18.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlck18.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlck18.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.ComboStageInfo)
                return object;
            let message = new $root.jlck18.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlck18.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlck18.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlck18.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlck18.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlck18.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlck18.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlck18.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlck18.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlck18.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {jlck18.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlck18.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlck18.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlck18.MColumn.toObject(message.FillSymbols[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlck18.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlck18.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlck18.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlck18
         * @interface IRoundInfo
         * @property {Array.<jlck18.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [EnterChest] RoundInfo EnterChest
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlck18
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlck18.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlck18.IComboStageInfo>} ComboStageData
         * @memberof jlck18.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlck18.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlck18.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlck18.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo EnterChest.
         * @member {number} EnterChest
         * @memberof jlck18.RoundInfo
         * @instance
         */
        RoundInfo.prototype.EnterChest = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlck18.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlck18.RoundInfo
         * @static
         * @param {jlck18.IRoundInfo=} [properties] Properties to set
         * @returns {jlck18.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlck18.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlck18.RoundInfo
         * @static
         * @param {jlck18.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlck18.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.EnterChest != null && Object.hasOwnProperty.call(message, "EnterChest"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.EnterChest);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlck18.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.RoundInfo
         * @static
         * @param {jlck18.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlck18.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.EnterChest = reader.double();
                        break;
                    }
                case 6: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlck18.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlck18.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.EnterChest != null && message.hasOwnProperty("EnterChest"))
                if (typeof message.EnterChest !== "number")
                    return "EnterChest: number expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.RoundInfo)
                return object;
            let message = new $root.jlck18.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlck18.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlck18.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlck18.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.EnterChest != null)
                message.EnterChest = Number(object.EnterChest);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.RoundInfo
         * @static
         * @param {jlck18.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.EnterChest = 0;
                object.ScatterCount = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlck18.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.EnterChest != null && message.hasOwnProperty("EnterChest"))
                object.EnterChest = options.json && !isFinite(message.EnterChest) ? String(message.EnterChest) : message.EnterChest;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlck18.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlck18.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.RoundInfo";
        };

        return RoundInfo;
    })();

    jlck18.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlck18
         * @interface ISpinAck
         * @property {Array.<jlck18.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlck18
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlck18.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlck18.IRoundInfo>} RoundQueue
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlck18.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlck18.SpinAck
         * @static
         * @param {jlck18.ISpinAck=} [properties] Properties to set
         * @returns {jlck18.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlck18.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlck18.SpinAck
         * @static
         * @param {jlck18.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlck18.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlck18.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.SpinAck
         * @static
         * @param {jlck18.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlck18.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                case 11: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlck18.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlck18.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.SpinAck)
                return object;
            let message = new $root.jlck18.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlck18.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlck18.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlck18.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.SpinAck
         * @static
         * @param {jlck18.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlck18.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlck18.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlck18.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.SpinAck";
        };

        return SpinAck;
    })();

    jlck18.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlck18
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlck18
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlck18.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlck18.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {jlck18.IGameInfoAck=} [properties] Properties to set
         * @returns {jlck18.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlck18.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {jlck18.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlck18.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {jlck18.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlck18.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlck18.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlck18.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlck18.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlck18.GameInfoAck)
                return object;
            let message = new $root.jlck18.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlck18.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {jlck18.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlck18.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlck18.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlck18.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlck18;
})();

export const jlcol = $root.jlcol = (() => {

    /**
     * Namespace jlcol.
     * @exports jlcol
     * @namespace
     */
    const jlcol = {};

    jlcol.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlcol
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlcol
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlcol.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlcol.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlcol.Column
         * @static
         * @param {jlcol.IColumn=} [properties] Properties to set
         * @returns {jlcol.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlcol.Column.verify|verify} messages.
         * @function encode
         * @memberof jlcol.Column
         * @static
         * @param {jlcol.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlcol.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcol.Column
         * @static
         * @param {jlcol.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlcol.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcol.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcol.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcol.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcol.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlcol.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcol.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcol.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcol.Column)
                return object;
            let message = new $root.jlcol.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlcol.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcol.Column
         * @static
         * @param {jlcol.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlcol.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlcol.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcol.Column";
        };

        return Column;
    })();

    jlcol.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlcol
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlcol
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlcol.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlcol.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {jlcol.IColumnFloat=} [properties] Properties to set
         * @returns {jlcol.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlcol.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {jlcol.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlcol.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {jlcol.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcol.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcol.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcol.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcol.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcol.ColumnFloat)
                return object;
            let message = new $root.jlcol.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlcol.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {jlcol.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlcol.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlcol.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcol.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlcol.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcol
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcol
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcol.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlcol.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlcol.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlcol.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlcol.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcol.AwardData
         * @static
         * @param {jlcol.IAwardData=} [properties] Properties to set
         * @returns {jlcol.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcol.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcol.AwardData
         * @static
         * @param {jlcol.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 54, wireType 1 =*/433).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcol.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcol.AwardData
         * @static
         * @param {jlcol.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcol.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcol.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcol.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 54: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcol.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcol.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcol.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcol.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcol.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcol.AwardData)
                return object;
            let message = new $root.jlcol.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcol.AwardData
         * @static
         * @param {jlcol.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcol.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcol.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcol.AwardData";
        };

        return AwardData;
    })();

    jlcol.Pot = (function() {

        /**
         * Properties of a Pot.
         * @memberof jlcol
         * @interface IPot
         * @property {number|null} [Symbol] Pot Symbol
         * @property {number|null} [Value] Pot Value
         * @property {number|null} [Pos] Pot Pos
         */

        /**
         * Constructs a new Pot.
         * @memberof jlcol
         * @classdesc Represents a Pot.
         * @implements IPot
         * @constructor
         * @param {jlcol.IPot=} [properties] Properties to set
         */
        function Pot(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Pot Symbol.
         * @member {number} Symbol
         * @memberof jlcol.Pot
         * @instance
         */
        Pot.prototype.Symbol = 0;

        /**
         * Pot Value.
         * @member {number} Value
         * @memberof jlcol.Pot
         * @instance
         */
        Pot.prototype.Value = 0;

        /**
         * Pot Pos.
         * @member {number} Pos
         * @memberof jlcol.Pot
         * @instance
         */
        Pot.prototype.Pos = 0;

        /**
         * Creates a new Pot instance using the specified properties.
         * @function create
         * @memberof jlcol.Pot
         * @static
         * @param {jlcol.IPot=} [properties] Properties to set
         * @returns {jlcol.Pot} Pot instance
         */
        Pot.create = function create(properties) {
            return new Pot(properties);
        };

        /**
         * Encodes the specified Pot message. Does not implicitly {@link jlcol.Pot.verify|verify} messages.
         * @function encode
         * @memberof jlcol.Pot
         * @static
         * @param {jlcol.IPot} message Pot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Pot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Value);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Pos);
            return writer;
        };

        /**
         * Encodes the specified Pot message, length delimited. Does not implicitly {@link jlcol.Pot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcol.Pot
         * @static
         * @param {jlcol.IPot} message Pot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Pot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Pot message from the specified reader or buffer.
         * @function decode
         * @memberof jlcol.Pot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcol.Pot} Pot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Pot.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcol.Pot();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Value = reader.double();
                        break;
                    }
                case 3: {
                        message.Pos = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Pot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcol.Pot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcol.Pot} Pot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Pot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Pot message.
         * @function verify
         * @memberof jlcol.Pot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Pot.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Value != null && message.hasOwnProperty("Value"))
                if (typeof message.Value !== "number")
                    return "Value: number expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            return null;
        };

        /**
         * Creates a Pot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcol.Pot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcol.Pot} Pot
         */
        Pot.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcol.Pot)
                return object;
            let message = new $root.jlcol.Pot();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Value != null)
                message.Value = Number(object.Value);
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            return message;
        };

        /**
         * Creates a plain object from a Pot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcol.Pot
         * @static
         * @param {jlcol.Pot} message Pot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Pot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Value = 0;
                object.Pos = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Value != null && message.hasOwnProperty("Value"))
                object.Value = options.json && !isFinite(message.Value) ? String(message.Value) : message.Value;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            return object;
        };

        /**
         * Converts this Pot to JSON.
         * @function toJSON
         * @memberof jlcol.Pot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Pot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Pot
         * @function getTypeUrl
         * @memberof jlcol.Pot
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Pot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcol.Pot";
        };

        return Pot;
    })();

    jlcol.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcol
         * @interface ISpinAck
         * @property {Array.<jlcol.IColumn>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlcol.IColumn>|null} [PlateSymbolPot] SpinAck PlateSymbolPot
         * @property {Array.<jlcol.IColumnFloat>|null} [PlateNum] SpinAck PlateNum
         * @property {Array.<jlcol.IColumnFloat>|null} [PlateNumPot] SpinAck PlateNumPot
         * @property {Array.<jlcol.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [LineWin] SpinAck LineWin
         * @property {number|null} [CollectWin] SpinAck CollectWin
         * @property {number|null} [PotWin] SpinAck PotWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {Array.<jlcol.IPot>|null} [PotList] SpinAck PotList
         * @property {boolean|null} [MaxFlag] SpinAck MaxFlag
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {number|null} [ScatterWin] SpinAck ScatterWin
         * @property {jlcol.IColumn|null} [FakerR5] SpinAck FakerR5
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcol
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcol.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolPot = [];
            this.PlateNum = [];
            this.PlateNumPot = [];
            this.AwardDataVec = [];
            this.PotList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlcol.IColumn>} PlateSymbol
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck PlateSymbolPot.
         * @member {Array.<jlcol.IColumn>} PlateSymbolPot
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbolPot = $util.emptyArray;

        /**
         * SpinAck PlateNum.
         * @member {Array.<jlcol.IColumnFloat>} PlateNum
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateNum = $util.emptyArray;

        /**
         * SpinAck PlateNumPot.
         * @member {Array.<jlcol.IColumnFloat>} PlateNumPot
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateNumPot = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlcol.IAwardData>} AwardDataVec
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck LineWin.
         * @member {number} LineWin
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.LineWin = 0;

        /**
         * SpinAck CollectWin.
         * @member {number} CollectWin
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.CollectWin = 0;

        /**
         * SpinAck PotWin.
         * @member {number} PotWin
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.PotWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck PotList.
         * @member {Array.<jlcol.IPot>} PotList
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.PotList = $util.emptyArray;

        /**
         * SpinAck MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.MaxFlag = false;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck ScatterWin.
         * @member {number} ScatterWin
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.ScatterWin = 0;

        /**
         * SpinAck FakerR5.
         * @member {jlcol.IColumn|null|undefined} FakerR5
         * @memberof jlcol.SpinAck
         * @instance
         */
        SpinAck.prototype.FakerR5 = null;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcol.SpinAck
         * @static
         * @param {jlcol.ISpinAck=} [properties] Properties to set
         * @returns {jlcol.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcol.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcol.SpinAck
         * @static
         * @param {jlcol.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlcol.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolPot != null && message.PlateSymbolPot.length)
                for (let i = 0; i < message.PlateSymbolPot.length; ++i)
                    $root.jlcol.Column.encode(message.PlateSymbolPot[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.PlateNum != null && message.PlateNum.length)
                for (let i = 0; i < message.PlateNum.length; ++i)
                    $root.jlcol.ColumnFloat.encode(message.PlateNum[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.PlateNumPot != null && message.PlateNumPot.length)
                for (let i = 0; i < message.PlateNumPot.length; ++i)
                    $root.jlcol.ColumnFloat.encode(message.PlateNumPot[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlcol.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.LineWin);
            if (message.CollectWin != null && Object.hasOwnProperty.call(message, "CollectWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.CollectWin);
            if (message.PotWin != null && Object.hasOwnProperty.call(message, "PotWin"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.PotWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.TotalWin);
            if (message.PotList != null && message.PotList.length)
                for (let i = 0; i < message.PotList.length; ++i)
                    $root.jlcol.Pot.encode(message.PotList[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.RID);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 16, wireType 1 =*/129).double(message.ScatterWin);
            if (message.FakerR5 != null && Object.hasOwnProperty.call(message, "FakerR5"))
                $root.jlcol.Column.encode(message.FakerR5, writer.uint32(/* id 17, wireType 2 =*/138).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcol.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcol.SpinAck
         * @static
         * @param {jlcol.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcol.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcol.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcol.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlcol.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolPot && message.PlateSymbolPot.length))
                            message.PlateSymbolPot = [];
                        message.PlateSymbolPot.push($root.jlcol.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.PlateNum && message.PlateNum.length))
                            message.PlateNum = [];
                        message.PlateNum.push($root.jlcol.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.PlateNumPot && message.PlateNumPot.length))
                            message.PlateNumPot = [];
                        message.PlateNumPot.push($root.jlcol.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlcol.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 8: {
                        message.CollectWin = reader.double();
                        break;
                    }
                case 9: {
                        message.PotWin = reader.double();
                        break;
                    }
                case 10: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 11: {
                        if (!(message.PotList && message.PotList.length))
                            message.PotList = [];
                        message.PotList.push($root.jlcol.Pot.decode(reader, reader.uint32()));
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 13: {
                        message.RTP = reader.double();
                        break;
                    }
                case 14: {
                        message.LID = reader.int32();
                        break;
                    }
                case 15: {
                        message.RID = reader.int32();
                        break;
                    }
                case 16: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 17: {
                        message.FakerR5 = $root.jlcol.Column.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcol.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcol.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcol.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlcol.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolPot != null && message.hasOwnProperty("PlateSymbolPot")) {
                if (!Array.isArray(message.PlateSymbolPot))
                    return "PlateSymbolPot: array expected";
                for (let i = 0; i < message.PlateSymbolPot.length; ++i) {
                    let error = $root.jlcol.Column.verify(message.PlateSymbolPot[i]);
                    if (error)
                        return "PlateSymbolPot." + error;
                }
            }
            if (message.PlateNum != null && message.hasOwnProperty("PlateNum")) {
                if (!Array.isArray(message.PlateNum))
                    return "PlateNum: array expected";
                for (let i = 0; i < message.PlateNum.length; ++i) {
                    let error = $root.jlcol.ColumnFloat.verify(message.PlateNum[i]);
                    if (error)
                        return "PlateNum." + error;
                }
            }
            if (message.PlateNumPot != null && message.hasOwnProperty("PlateNumPot")) {
                if (!Array.isArray(message.PlateNumPot))
                    return "PlateNumPot: array expected";
                for (let i = 0; i < message.PlateNumPot.length; ++i) {
                    let error = $root.jlcol.ColumnFloat.verify(message.PlateNumPot[i]);
                    if (error)
                        return "PlateNumPot." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlcol.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.CollectWin != null && message.hasOwnProperty("CollectWin"))
                if (typeof message.CollectWin !== "number")
                    return "CollectWin: number expected";
            if (message.PotWin != null && message.hasOwnProperty("PotWin"))
                if (typeof message.PotWin !== "number")
                    return "PotWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.PotList != null && message.hasOwnProperty("PotList")) {
                if (!Array.isArray(message.PotList))
                    return "PotList: array expected";
                for (let i = 0; i < message.PotList.length; ++i) {
                    let error = $root.jlcol.Pot.verify(message.PotList[i]);
                    if (error)
                        return "PotList." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.FakerR5 != null && message.hasOwnProperty("FakerR5")) {
                let error = $root.jlcol.Column.verify(message.FakerR5);
                if (error)
                    return "FakerR5." + error;
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcol.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcol.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcol.SpinAck)
                return object;
            let message = new $root.jlcol.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlcol.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlcol.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlcol.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolPot) {
                if (!Array.isArray(object.PlateSymbolPot))
                    throw TypeError(".jlcol.SpinAck.PlateSymbolPot: array expected");
                message.PlateSymbolPot = [];
                for (let i = 0; i < object.PlateSymbolPot.length; ++i) {
                    if (typeof object.PlateSymbolPot[i] !== "object")
                        throw TypeError(".jlcol.SpinAck.PlateSymbolPot: object expected");
                    message.PlateSymbolPot[i] = $root.jlcol.Column.fromObject(object.PlateSymbolPot[i]);
                }
            }
            if (object.PlateNum) {
                if (!Array.isArray(object.PlateNum))
                    throw TypeError(".jlcol.SpinAck.PlateNum: array expected");
                message.PlateNum = [];
                for (let i = 0; i < object.PlateNum.length; ++i) {
                    if (typeof object.PlateNum[i] !== "object")
                        throw TypeError(".jlcol.SpinAck.PlateNum: object expected");
                    message.PlateNum[i] = $root.jlcol.ColumnFloat.fromObject(object.PlateNum[i]);
                }
            }
            if (object.PlateNumPot) {
                if (!Array.isArray(object.PlateNumPot))
                    throw TypeError(".jlcol.SpinAck.PlateNumPot: array expected");
                message.PlateNumPot = [];
                for (let i = 0; i < object.PlateNumPot.length; ++i) {
                    if (typeof object.PlateNumPot[i] !== "object")
                        throw TypeError(".jlcol.SpinAck.PlateNumPot: object expected");
                    message.PlateNumPot[i] = $root.jlcol.ColumnFloat.fromObject(object.PlateNumPot[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlcol.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlcol.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlcol.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.CollectWin != null)
                message.CollectWin = Number(object.CollectWin);
            if (object.PotWin != null)
                message.PotWin = Number(object.PotWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.PotList) {
                if (!Array.isArray(object.PotList))
                    throw TypeError(".jlcol.SpinAck.PotList: array expected");
                message.PotList = [];
                for (let i = 0; i < object.PotList.length; ++i) {
                    if (typeof object.PotList[i] !== "object")
                        throw TypeError(".jlcol.SpinAck.PotList: object expected");
                    message.PotList[i] = $root.jlcol.Pot.fromObject(object.PotList[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.FakerR5 != null) {
                if (typeof object.FakerR5 !== "object")
                    throw TypeError(".jlcol.SpinAck.FakerR5: object expected");
                message.FakerR5 = $root.jlcol.Column.fromObject(object.FakerR5);
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcol.SpinAck
         * @static
         * @param {jlcol.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolPot = [];
                object.PlateNum = [];
                object.PlateNumPot = [];
                object.AwardDataVec = [];
                object.PotList = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.LineWin = 0;
                object.CollectWin = 0;
                object.PotWin = 0;
                object.TotalWin = 0;
                object.MaxFlag = false;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.ScatterWin = 0;
                object.FakerR5 = null;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlcol.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolPot && message.PlateSymbolPot.length) {
                object.PlateSymbolPot = [];
                for (let j = 0; j < message.PlateSymbolPot.length; ++j)
                    object.PlateSymbolPot[j] = $root.jlcol.Column.toObject(message.PlateSymbolPot[j], options);
            }
            if (message.PlateNum && message.PlateNum.length) {
                object.PlateNum = [];
                for (let j = 0; j < message.PlateNum.length; ++j)
                    object.PlateNum[j] = $root.jlcol.ColumnFloat.toObject(message.PlateNum[j], options);
            }
            if (message.PlateNumPot && message.PlateNumPot.length) {
                object.PlateNumPot = [];
                for (let j = 0; j < message.PlateNumPot.length; ++j)
                    object.PlateNumPot[j] = $root.jlcol.ColumnFloat.toObject(message.PlateNumPot[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlcol.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.CollectWin != null && message.hasOwnProperty("CollectWin"))
                object.CollectWin = options.json && !isFinite(message.CollectWin) ? String(message.CollectWin) : message.CollectWin;
            if (message.PotWin != null && message.hasOwnProperty("PotWin"))
                object.PotWin = options.json && !isFinite(message.PotWin) ? String(message.PotWin) : message.PotWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.PotList && message.PotList.length) {
                object.PotList = [];
                for (let j = 0; j < message.PotList.length; ++j)
                    object.PotList[j] = $root.jlcol.Pot.toObject(message.PotList[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.FakerR5 != null && message.hasOwnProperty("FakerR5"))
                object.FakerR5 = $root.jlcol.Column.toObject(message.FakerR5, options);
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcol.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcol.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcol.SpinAck";
        };

        return SpinAck;
    })();

    return jlcol;
})();

export const jlcs = $root.jlcs = (() => {

    /**
     * Namespace jlcs.
     * @exports jlcs
     * @namespace
     */
    const jlcs = {};

    jlcs.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlcs
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlcs
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlcs.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlcs.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlcs.Column
         * @static
         * @param {jlcs.IColumn=} [properties] Properties to set
         * @returns {jlcs.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlcs.Column.verify|verify} messages.
         * @function encode
         * @memberof jlcs.Column
         * @static
         * @param {jlcs.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlcs.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs.Column
         * @static
         * @param {jlcs.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlcs.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs.Column)
                return object;
            let message = new $root.jlcs.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlcs.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs.Column
         * @static
         * @param {jlcs.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlcs.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlcs.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs.Column";
        };

        return Column;
    })();

    jlcs.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcs
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcs
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcs.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlcs.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlcs.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlcs.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlcs.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcs.AwardData
         * @static
         * @param {jlcs.IAwardData=} [properties] Properties to set
         * @returns {jlcs.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcs.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcs.AwardData
         * @static
         * @param {jlcs.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcs.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs.AwardData
         * @static
         * @param {jlcs.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcs.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs.AwardData)
                return object;
            let message = new $root.jlcs.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs.AwardData
         * @static
         * @param {jlcs.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcs.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcs.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs.AwardData";
        };

        return AwardData;
    })();

    jlcs.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlcs
         * @interface IRoundInfo
         * @property {Array.<jlcs.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<number>|null} [WildColumnMult] RoundInfo WildColumnMult
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {Array.<jlcs.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [DisplayID] RoundInfo DisplayID
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlcs
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlcs.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.WildColumnMult = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlcs.IColumn>} PlateSymbol
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo WildColumnMult.
         * @member {Array.<number>} WildColumnMult
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildColumnMult = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlcs.IAwardData>} AwardDataVec
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo DisplayID.
         * @member {number} DisplayID
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.DisplayID = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlcs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlcs.RoundInfo
         * @static
         * @param {jlcs.IRoundInfo=} [properties] Properties to set
         * @returns {jlcs.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlcs.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcs.RoundInfo
         * @static
         * @param {jlcs.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlcs.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.WildColumnMult != null && message.WildColumnMult.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.WildColumnMult.length; ++i)
                    writer.int32(message.WildColumnMult[i]);
                writer.ldelim();
            }
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlcs.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.DisplayID != null && Object.hasOwnProperty.call(message, "DisplayID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.DisplayID);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlcs.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs.RoundInfo
         * @static
         * @param {jlcs.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlcs.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.WildColumnMult && message.WildColumnMult.length))
                            message.WildColumnMult = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WildColumnMult.push(reader.int32());
                        } else
                            message.WildColumnMult.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlcs.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.DisplayID = reader.int32();
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlcs.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlcs.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.WildColumnMult != null && message.hasOwnProperty("WildColumnMult")) {
                if (!Array.isArray(message.WildColumnMult))
                    return "WildColumnMult: array expected";
                for (let i = 0; i < message.WildColumnMult.length; ++i)
                    if (!$util.isInteger(message.WildColumnMult[i]))
                        return "WildColumnMult: integer[] expected";
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlcs.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.DisplayID != null && message.hasOwnProperty("DisplayID"))
                if (!$util.isInteger(message.DisplayID))
                    return "DisplayID: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs.RoundInfo)
                return object;
            let message = new $root.jlcs.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlcs.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlcs.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlcs.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.WildColumnMult) {
                if (!Array.isArray(object.WildColumnMult))
                    throw TypeError(".jlcs.RoundInfo.WildColumnMult: array expected");
                message.WildColumnMult = [];
                for (let i = 0; i < object.WildColumnMult.length; ++i)
                    message.WildColumnMult[i] = object.WildColumnMult[i] | 0;
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlcs.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlcs.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlcs.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.DisplayID != null)
                message.DisplayID = object.DisplayID | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs.RoundInfo
         * @static
         * @param {jlcs.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.WildColumnMult = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.DisplayID = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlcs.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.WildColumnMult && message.WildColumnMult.length) {
                object.WildColumnMult = [];
                for (let j = 0; j < message.WildColumnMult.length; ++j)
                    object.WildColumnMult[j] = message.WildColumnMult[j];
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlcs.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.DisplayID != null && message.hasOwnProperty("DisplayID"))
                object.DisplayID = message.DisplayID;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlcs.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlcs.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs.RoundInfo";
        };

        return RoundInfo;
    })();

    jlcs.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcs
         * @interface ISpinAck
         * @property {Array.<jlcs.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcs
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcs.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlcs.IRoundInfo>} RoundQueue
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {number} Extra
         * @memberof jlcs.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcs.SpinAck
         * @static
         * @param {jlcs.ISpinAck=} [properties] Properties to set
         * @returns {jlcs.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcs.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcs.SpinAck
         * @static
         * @param {jlcs.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlcs.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcs.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs.SpinAck
         * @static
         * @param {jlcs.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlcs.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 4: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 5: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 6: {
                        message.RTP = reader.double();
                        break;
                    }
                case 7: {
                        message.Extra = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcs.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlcs.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs.SpinAck)
                return object;
            let message = new $root.jlcs.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlcs.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlcs.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlcs.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs.SpinAck
         * @static
         * @param {jlcs.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlcs.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcs.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcs.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs.SpinAck";
        };

        return SpinAck;
    })();

    jlcs.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlcs
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlcs
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlcs.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlcs.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlcs.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlcs.GameInfoData
         * @static
         * @param {jlcs.IGameInfoData=} [properties] Properties to set
         * @returns {jlcs.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlcs.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlcs.GameInfoData
         * @static
         * @param {jlcs.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlcs.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs.GameInfoData
         * @static
         * @param {jlcs.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlcs.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs.GameInfoData)
                return object;
            let message = new $root.jlcs.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlcs.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs.GameInfoData
         * @static
         * @param {jlcs.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlcs.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlcs.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlcs;
})();

export const jlcs75 = $root.jlcs75 = (() => {

    /**
     * Namespace jlcs75.
     * @exports jlcs75
     * @namespace
     */
    const jlcs75 = {};

    jlcs75.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlcs75
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlcs75
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlcs75.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlcs75.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlcs75.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlcs75.Grid
         * @static
         * @param {jlcs75.IGrid=} [properties] Properties to set
         * @returns {jlcs75.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlcs75.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.Grid
         * @static
         * @param {jlcs75.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlcs75.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.Grid
         * @static
         * @param {jlcs75.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlcs75.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.Grid)
                return object;
            let message = new $root.jlcs75.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.Grid
         * @static
         * @param {jlcs75.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlcs75.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlcs75.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.Grid";
        };

        return Grid;
    })();

    jlcs75.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlcs75
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlcs75
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlcs75.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlcs75.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlcs75.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlcs75.MSymbol
         * @static
         * @param {jlcs75.IMSymbol=} [properties] Properties to set
         * @returns {jlcs75.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlcs75.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.MSymbol
         * @static
         * @param {jlcs75.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlcs75.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.MSymbol
         * @static
         * @param {jlcs75.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlcs75.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.MSymbol)
                return object;
            let message = new $root.jlcs75.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.MSymbol
         * @static
         * @param {jlcs75.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlcs75.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlcs75.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.MSymbol";
        };

        return MSymbol;
    })();

    jlcs75.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlcs75
         * @interface IMColumn
         * @property {Array.<jlcs75.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlcs75
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlcs75.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlcs75.IMSymbol>} MColumn
         * @memberof jlcs75.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlcs75.MColumn
         * @static
         * @param {jlcs75.IMColumn=} [properties] Properties to set
         * @returns {jlcs75.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlcs75.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.MColumn
         * @static
         * @param {jlcs75.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlcs75.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlcs75.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.MColumn
         * @static
         * @param {jlcs75.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlcs75.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlcs75.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlcs75.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.MColumn)
                return object;
            let message = new $root.jlcs75.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlcs75.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlcs75.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlcs75.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.MColumn
         * @static
         * @param {jlcs75.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlcs75.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlcs75.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlcs75.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.MColumn";
        };

        return MColumn;
    })();

    jlcs75.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcs75
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlcs75.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcs75
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcs75.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlcs75.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlcs75.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlcs75.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlcs75.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlcs75.IGrid>} GridVec
         * @memberof jlcs75.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcs75.AwardData
         * @static
         * @param {jlcs75.IAwardData=} [properties] Properties to set
         * @returns {jlcs75.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcs75.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.AwardData
         * @static
         * @param {jlcs75.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlcs75.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcs75.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.AwardData
         * @static
         * @param {jlcs75.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlcs75.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcs75.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlcs75.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.AwardData)
                return object;
            let message = new $root.jlcs75.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlcs75.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlcs75.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlcs75.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.AwardData
         * @static
         * @param {jlcs75.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlcs75.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcs75.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcs75.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.AwardData";
        };

        return AwardData;
    })();

    jlcs75.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlcs75
         * @interface IComboStageInfo
         * @property {Array.<jlcs75.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlcs75.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlcs75.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlcs75
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlcs75.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlcs75.IMColumn>} ComboStageSymbol
         * @memberof jlcs75.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlcs75.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlcs75.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlcs75.IAwardData>} AwardDataVec
         * @memberof jlcs75.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlcs75.IMColumn>} FillSymbols
         * @memberof jlcs75.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {jlcs75.IComboStageInfo=} [properties] Properties to set
         * @returns {jlcs75.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlcs75.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {jlcs75.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlcs75.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlcs75.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlcs75.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlcs75.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {jlcs75.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlcs75.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlcs75.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlcs75.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlcs75.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlcs75.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlcs75.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.ComboStageInfo)
                return object;
            let message = new $root.jlcs75.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlcs75.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlcs75.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlcs75.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlcs75.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlcs75.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlcs75.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlcs75.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlcs75.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlcs75.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {jlcs75.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlcs75.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlcs75.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlcs75.MColumn.toObject(message.FillSymbols[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlcs75.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlcs75.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlcs75.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlcs75
         * @interface IRoundInfo
         * @property {Array.<jlcs75.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [EnterChest] RoundInfo EnterChest
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlcs75
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlcs75.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlcs75.IComboStageInfo>} ComboStageData
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo EnterChest.
         * @member {number} EnterChest
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.EnterChest = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlcs75.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {jlcs75.IRoundInfo=} [properties] Properties to set
         * @returns {jlcs75.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlcs75.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {jlcs75.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlcs75.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.EnterChest != null && Object.hasOwnProperty.call(message, "EnterChest"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.EnterChest);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlcs75.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {jlcs75.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlcs75.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.EnterChest = reader.double();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 7: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlcs75.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.EnterChest != null && message.hasOwnProperty("EnterChest"))
                if (typeof message.EnterChest !== "number")
                    return "EnterChest: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.RoundInfo)
                return object;
            let message = new $root.jlcs75.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlcs75.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlcs75.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlcs75.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.EnterChest != null)
                message.EnterChest = Number(object.EnterChest);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {jlcs75.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.EnterChest = 0;
                object.MaxFlag = false;
                object.ScatterCount = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlcs75.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.EnterChest != null && message.hasOwnProperty("EnterChest"))
                object.EnterChest = options.json && !isFinite(message.EnterChest) ? String(message.EnterChest) : message.EnterChest;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlcs75.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlcs75.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.RoundInfo";
        };

        return RoundInfo;
    })();

    jlcs75.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcs75
         * @interface ISpinAck
         * @property {Array.<jlcs75.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcs75
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcs75.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlcs75.IRoundInfo>} RoundQueue
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlcs75.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcs75.SpinAck
         * @static
         * @param {jlcs75.ISpinAck=} [properties] Properties to set
         * @returns {jlcs75.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcs75.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.SpinAck
         * @static
         * @param {jlcs75.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlcs75.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcs75.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.SpinAck
         * @static
         * @param {jlcs75.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlcs75.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                case 11: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcs75.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlcs75.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.SpinAck)
                return object;
            let message = new $root.jlcs75.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlcs75.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlcs75.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlcs75.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.SpinAck
         * @static
         * @param {jlcs75.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlcs75.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcs75.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcs75.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.SpinAck";
        };

        return SpinAck;
    })();

    jlcs75.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlcs75
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlcs75
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlcs75.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlcs75.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {jlcs75.IGameInfoAck=} [properties] Properties to set
         * @returns {jlcs75.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlcs75.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {jlcs75.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlcs75.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {jlcs75.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcs75.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcs75.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcs75.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcs75.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcs75.GameInfoAck)
                return object;
            let message = new $root.jlcs75.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlcs75.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {jlcs75.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlcs75.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlcs75.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcs75.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlcs75;
})();

export const jlcsh = $root.jlcsh = (() => {

    /**
     * Namespace jlcsh.
     * @exports jlcsh
     * @namespace
     */
    const jlcsh = {};

    jlcsh.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlcsh
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlcsh
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlcsh.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlcsh.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlcsh.Column
         * @static
         * @param {jlcsh.IColumn=} [properties] Properties to set
         * @returns {jlcsh.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlcsh.Column.verify|verify} messages.
         * @function encode
         * @memberof jlcsh.Column
         * @static
         * @param {jlcsh.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlcsh.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcsh.Column
         * @static
         * @param {jlcsh.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlcsh.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcsh.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcsh.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcsh.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcsh.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlcsh.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcsh.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcsh.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcsh.Column)
                return object;
            let message = new $root.jlcsh.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlcsh.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcsh.Column
         * @static
         * @param {jlcsh.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlcsh.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlcsh.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcsh.Column";
        };

        return Column;
    })();

    jlcsh.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlcsh
         * @interface IGrid
         * @property {number|null} [Column] Grid Column
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlcsh
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlcsh.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Column.
         * @member {number} Column
         * @memberof jlcsh.Grid
         * @instance
         */
        Grid.prototype.Column = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlcsh.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlcsh.Grid
         * @static
         * @param {jlcsh.IGrid=} [properties] Properties to set
         * @returns {jlcsh.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlcsh.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlcsh.Grid
         * @static
         * @param {jlcsh.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && Object.hasOwnProperty.call(message, "Column"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Column);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlcsh.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcsh.Grid
         * @static
         * @param {jlcsh.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlcsh.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcsh.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcsh.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Column = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcsh.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcsh.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlcsh.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column"))
                if (!$util.isInteger(message.Column))
                    return "Column: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcsh.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcsh.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcsh.Grid)
                return object;
            let message = new $root.jlcsh.Grid();
            if (object.Column != null)
                message.Column = object.Column | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcsh.Grid
         * @static
         * @param {jlcsh.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Column = 0;
                object.Row = 0;
            }
            if (message.Column != null && message.hasOwnProperty("Column"))
                object.Column = message.Column;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlcsh.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlcsh.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcsh.Grid";
        };

        return Grid;
    })();

    jlcsh.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlcsh
         * @interface IAwardData
         * @property {number|null} [AwardSymbol] AwardData AwardSymbol
         * @property {Array.<jlcsh.IGrid>|null} [AwardGridVec] AwardData AwardGridVec
         * @property {number|null} [AwardMoney] AwardData AwardMoney
         * @property {number|null} [SymbolCount] AwardData SymbolCount
         * @property {number|null} [Lines] AwardData Lines
         * @property {boolean|null} [ContainWild] AwardData ContainWild
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlcsh
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlcsh.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.AwardGridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData AwardSymbol.
         * @member {number} AwardSymbol
         * @memberof jlcsh.AwardData
         * @instance
         */
        AwardData.prototype.AwardSymbol = 0;

        /**
         * AwardData AwardGridVec.
         * @member {Array.<jlcsh.IGrid>} AwardGridVec
         * @memberof jlcsh.AwardData
         * @instance
         */
        AwardData.prototype.AwardGridVec = $util.emptyArray;

        /**
         * AwardData AwardMoney.
         * @member {number} AwardMoney
         * @memberof jlcsh.AwardData
         * @instance
         */
        AwardData.prototype.AwardMoney = 0;

        /**
         * AwardData SymbolCount.
         * @member {number} SymbolCount
         * @memberof jlcsh.AwardData
         * @instance
         */
        AwardData.prototype.SymbolCount = 0;

        /**
         * AwardData Lines.
         * @member {number} Lines
         * @memberof jlcsh.AwardData
         * @instance
         */
        AwardData.prototype.Lines = 0;

        /**
         * AwardData ContainWild.
         * @member {boolean} ContainWild
         * @memberof jlcsh.AwardData
         * @instance
         */
        AwardData.prototype.ContainWild = false;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlcsh.AwardData
         * @static
         * @param {jlcsh.IAwardData=} [properties] Properties to set
         * @returns {jlcsh.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlcsh.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlcsh.AwardData
         * @static
         * @param {jlcsh.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardSymbol != null && Object.hasOwnProperty.call(message, "AwardSymbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardSymbol);
            if (message.AwardGridVec != null && message.AwardGridVec.length)
                for (let i = 0; i < message.AwardGridVec.length; ++i)
                    $root.jlcsh.Grid.encode(message.AwardGridVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardMoney != null && Object.hasOwnProperty.call(message, "AwardMoney"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.AwardMoney);
            if (message.SymbolCount != null && Object.hasOwnProperty.call(message, "SymbolCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.SymbolCount);
            if (message.Lines != null && Object.hasOwnProperty.call(message, "Lines"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.Lines);
            if (message.ContainWild != null && Object.hasOwnProperty.call(message, "ContainWild"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.ContainWild);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlcsh.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcsh.AwardData
         * @static
         * @param {jlcsh.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlcsh.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcsh.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcsh.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardSymbol = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.AwardGridVec && message.AwardGridVec.length))
                            message.AwardGridVec = [];
                        message.AwardGridVec.push($root.jlcsh.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardMoney = reader.double();
                        break;
                    }
                case 4: {
                        message.SymbolCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.Lines = reader.int32();
                        break;
                    }
                case 6: {
                        message.ContainWild = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcsh.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcsh.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlcsh.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                if (!$util.isInteger(message.AwardSymbol))
                    return "AwardSymbol: integer expected";
            if (message.AwardGridVec != null && message.hasOwnProperty("AwardGridVec")) {
                if (!Array.isArray(message.AwardGridVec))
                    return "AwardGridVec: array expected";
                for (let i = 0; i < message.AwardGridVec.length; ++i) {
                    let error = $root.jlcsh.Grid.verify(message.AwardGridVec[i]);
                    if (error)
                        return "AwardGridVec." + error;
                }
            }
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                if (typeof message.AwardMoney !== "number")
                    return "AwardMoney: number expected";
            if (message.SymbolCount != null && message.hasOwnProperty("SymbolCount"))
                if (!$util.isInteger(message.SymbolCount))
                    return "SymbolCount: integer expected";
            if (message.Lines != null && message.hasOwnProperty("Lines"))
                if (!$util.isInteger(message.Lines))
                    return "Lines: integer expected";
            if (message.ContainWild != null && message.hasOwnProperty("ContainWild"))
                if (typeof message.ContainWild !== "boolean")
                    return "ContainWild: boolean expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcsh.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcsh.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcsh.AwardData)
                return object;
            let message = new $root.jlcsh.AwardData();
            if (object.AwardSymbol != null)
                message.AwardSymbol = object.AwardSymbol | 0;
            if (object.AwardGridVec) {
                if (!Array.isArray(object.AwardGridVec))
                    throw TypeError(".jlcsh.AwardData.AwardGridVec: array expected");
                message.AwardGridVec = [];
                for (let i = 0; i < object.AwardGridVec.length; ++i) {
                    if (typeof object.AwardGridVec[i] !== "object")
                        throw TypeError(".jlcsh.AwardData.AwardGridVec: object expected");
                    message.AwardGridVec[i] = $root.jlcsh.Grid.fromObject(object.AwardGridVec[i]);
                }
            }
            if (object.AwardMoney != null)
                message.AwardMoney = Number(object.AwardMoney);
            if (object.SymbolCount != null)
                message.SymbolCount = object.SymbolCount | 0;
            if (object.Lines != null)
                message.Lines = object.Lines | 0;
            if (object.ContainWild != null)
                message.ContainWild = Boolean(object.ContainWild);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcsh.AwardData
         * @static
         * @param {jlcsh.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AwardGridVec = [];
            if (options.defaults) {
                object.AwardSymbol = 0;
                object.AwardMoney = 0;
                object.SymbolCount = 0;
                object.Lines = 0;
                object.ContainWild = false;
            }
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                object.AwardSymbol = message.AwardSymbol;
            if (message.AwardGridVec && message.AwardGridVec.length) {
                object.AwardGridVec = [];
                for (let j = 0; j < message.AwardGridVec.length; ++j)
                    object.AwardGridVec[j] = $root.jlcsh.Grid.toObject(message.AwardGridVec[j], options);
            }
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                object.AwardMoney = options.json && !isFinite(message.AwardMoney) ? String(message.AwardMoney) : message.AwardMoney;
            if (message.SymbolCount != null && message.hasOwnProperty("SymbolCount"))
                object.SymbolCount = message.SymbolCount;
            if (message.Lines != null && message.hasOwnProperty("Lines"))
                object.Lines = message.Lines;
            if (message.ContainWild != null && message.hasOwnProperty("ContainWild"))
                object.ContainWild = message.ContainWild;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlcsh.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlcsh.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcsh.AwardData";
        };

        return AwardData;
    })();

    jlcsh.AwardDetail = (function() {

        /**
         * Properties of an AwardDetail.
         * @memberof jlcsh
         * @interface IAwardDetail
         * @property {Array.<jlcsh.IAwardData>|null} [AwardSet] AwardDetail AwardSet
         * @property {number|null} [AwardMoney] AwardDetail AwardMoney
         */

        /**
         * Constructs a new AwardDetail.
         * @memberof jlcsh
         * @classdesc Represents an AwardDetail.
         * @implements IAwardDetail
         * @constructor
         * @param {jlcsh.IAwardDetail=} [properties] Properties to set
         */
        function AwardDetail(properties) {
            this.AwardSet = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardDetail AwardSet.
         * @member {Array.<jlcsh.IAwardData>} AwardSet
         * @memberof jlcsh.AwardDetail
         * @instance
         */
        AwardDetail.prototype.AwardSet = $util.emptyArray;

        /**
         * AwardDetail AwardMoney.
         * @member {number} AwardMoney
         * @memberof jlcsh.AwardDetail
         * @instance
         */
        AwardDetail.prototype.AwardMoney = 0;

        /**
         * Creates a new AwardDetail instance using the specified properties.
         * @function create
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {jlcsh.IAwardDetail=} [properties] Properties to set
         * @returns {jlcsh.AwardDetail} AwardDetail instance
         */
        AwardDetail.create = function create(properties) {
            return new AwardDetail(properties);
        };

        /**
         * Encodes the specified AwardDetail message. Does not implicitly {@link jlcsh.AwardDetail.verify|verify} messages.
         * @function encode
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {jlcsh.IAwardDetail} message AwardDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardSet != null && message.AwardSet.length)
                for (let i = 0; i < message.AwardSet.length; ++i)
                    $root.jlcsh.AwardData.encode(message.AwardSet[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardMoney != null && Object.hasOwnProperty.call(message, "AwardMoney"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.AwardMoney);
            return writer;
        };

        /**
         * Encodes the specified AwardDetail message, length delimited. Does not implicitly {@link jlcsh.AwardDetail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {jlcsh.IAwardDetail} message AwardDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardDetail message from the specified reader or buffer.
         * @function decode
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcsh.AwardDetail} AwardDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardDetail.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcsh.AwardDetail();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AwardSet && message.AwardSet.length))
                            message.AwardSet = [];
                        message.AwardSet.push($root.jlcsh.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardDetail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcsh.AwardDetail} AwardDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardDetail message.
         * @function verify
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardDetail.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardSet != null && message.hasOwnProperty("AwardSet")) {
                if (!Array.isArray(message.AwardSet))
                    return "AwardSet: array expected";
                for (let i = 0; i < message.AwardSet.length; ++i) {
                    let error = $root.jlcsh.AwardData.verify(message.AwardSet[i]);
                    if (error)
                        return "AwardSet." + error;
                }
            }
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                if (typeof message.AwardMoney !== "number")
                    return "AwardMoney: number expected";
            return null;
        };

        /**
         * Creates an AwardDetail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcsh.AwardDetail} AwardDetail
         */
        AwardDetail.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcsh.AwardDetail)
                return object;
            let message = new $root.jlcsh.AwardDetail();
            if (object.AwardSet) {
                if (!Array.isArray(object.AwardSet))
                    throw TypeError(".jlcsh.AwardDetail.AwardSet: array expected");
                message.AwardSet = [];
                for (let i = 0; i < object.AwardSet.length; ++i) {
                    if (typeof object.AwardSet[i] !== "object")
                        throw TypeError(".jlcsh.AwardDetail.AwardSet: object expected");
                    message.AwardSet[i] = $root.jlcsh.AwardData.fromObject(object.AwardSet[i]);
                }
            }
            if (object.AwardMoney != null)
                message.AwardMoney = Number(object.AwardMoney);
            return message;
        };

        /**
         * Creates a plain object from an AwardDetail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {jlcsh.AwardDetail} message AwardDetail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AwardSet = [];
            if (options.defaults)
                object.AwardMoney = 0;
            if (message.AwardSet && message.AwardSet.length) {
                object.AwardSet = [];
                for (let j = 0; j < message.AwardSet.length; ++j)
                    object.AwardSet[j] = $root.jlcsh.AwardData.toObject(message.AwardSet[j], options);
            }
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                object.AwardMoney = options.json && !isFinite(message.AwardMoney) ? String(message.AwardMoney) : message.AwardMoney;
            return object;
        };

        /**
         * Converts this AwardDetail to JSON.
         * @function toJSON
         * @memberof jlcsh.AwardDetail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardDetail
         * @function getTypeUrl
         * @memberof jlcsh.AwardDetail
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardDetail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcsh.AwardDetail";
        };

        return AwardDetail;
    })();

    jlcsh.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlcsh
         * @interface ISinglePlate
         * @property {Array.<jlcsh.IAwardDetail>|null} [AwardVec] SinglePlate AwardVec
         * @property {Array.<jlcsh.IColumn>|null} [PlateVec] SinglePlate PlateVec
         * @property {Array.<jlcsh.IColumn>|null} [ColumnSymbol] SinglePlate ColumnSymbol
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {number|null} [RemainRound] SinglePlate RemainRound
         * @property {number|null} [ExtraRound] SinglePlate ExtraRound
         * @property {number|null} [ExtraOdds] SinglePlate ExtraOdds
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlcsh
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlcsh.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.AwardVec = [];
            this.PlateVec = [];
            this.ColumnSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate AwardVec.
         * @member {Array.<jlcsh.IAwardDetail>} AwardVec
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardVec = $util.emptyArray;

        /**
         * SinglePlate PlateVec.
         * @member {Array.<jlcsh.IColumn>} PlateVec
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateVec = $util.emptyArray;

        /**
         * SinglePlate ColumnSymbol.
         * @member {Array.<jlcsh.IColumn>} ColumnSymbol
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.ColumnSymbol = $util.emptyArray;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate RemainRound.
         * @member {number} RemainRound
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.RemainRound = 0;

        /**
         * SinglePlate ExtraRound.
         * @member {number} ExtraRound
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.ExtraRound = 0;

        /**
         * SinglePlate ExtraOdds.
         * @member {number} ExtraOdds
         * @memberof jlcsh.SinglePlate
         * @instance
         */
        SinglePlate.prototype.ExtraOdds = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {jlcsh.ISinglePlate=} [properties] Properties to set
         * @returns {jlcsh.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlcsh.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {jlcsh.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardVec != null && message.AwardVec.length)
                for (let i = 0; i < message.AwardVec.length; ++i)
                    $root.jlcsh.AwardDetail.encode(message.AwardVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateVec != null && message.PlateVec.length)
                for (let i = 0; i < message.PlateVec.length; ++i)
                    $root.jlcsh.Column.encode(message.PlateVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ColumnSymbol != null && message.ColumnSymbol.length)
                for (let i = 0; i < message.ColumnSymbol.length; ++i)
                    $root.jlcsh.Column.encode(message.ColumnSymbol[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.RemainRound != null && Object.hasOwnProperty.call(message, "RemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.RemainRound);
            if (message.ExtraRound != null && Object.hasOwnProperty.call(message, "ExtraRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.ExtraRound);
            if (message.ExtraOdds != null && Object.hasOwnProperty.call(message, "ExtraOdds"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.ExtraOdds);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlcsh.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {jlcsh.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcsh.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcsh.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AwardVec && message.AwardVec.length))
                            message.AwardVec = [];
                        message.AwardVec.push($root.jlcsh.AwardDetail.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateVec && message.PlateVec.length))
                            message.PlateVec = [];
                        message.PlateVec.push($root.jlcsh.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.ColumnSymbol && message.ColumnSymbol.length))
                            message.ColumnSymbol = [];
                        message.ColumnSymbol.push($root.jlcsh.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.RemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.ExtraRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.ExtraOdds = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcsh.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardVec != null && message.hasOwnProperty("AwardVec")) {
                if (!Array.isArray(message.AwardVec))
                    return "AwardVec: array expected";
                for (let i = 0; i < message.AwardVec.length; ++i) {
                    let error = $root.jlcsh.AwardDetail.verify(message.AwardVec[i]);
                    if (error)
                        return "AwardVec." + error;
                }
            }
            if (message.PlateVec != null && message.hasOwnProperty("PlateVec")) {
                if (!Array.isArray(message.PlateVec))
                    return "PlateVec: array expected";
                for (let i = 0; i < message.PlateVec.length; ++i) {
                    let error = $root.jlcsh.Column.verify(message.PlateVec[i]);
                    if (error)
                        return "PlateVec." + error;
                }
            }
            if (message.ColumnSymbol != null && message.hasOwnProperty("ColumnSymbol")) {
                if (!Array.isArray(message.ColumnSymbol))
                    return "ColumnSymbol: array expected";
                for (let i = 0; i < message.ColumnSymbol.length; ++i) {
                    let error = $root.jlcsh.Column.verify(message.ColumnSymbol[i]);
                    if (error)
                        return "ColumnSymbol." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RemainRound != null && message.hasOwnProperty("RemainRound"))
                if (!$util.isInteger(message.RemainRound))
                    return "RemainRound: integer expected";
            if (message.ExtraRound != null && message.hasOwnProperty("ExtraRound"))
                if (!$util.isInteger(message.ExtraRound))
                    return "ExtraRound: integer expected";
            if (message.ExtraOdds != null && message.hasOwnProperty("ExtraOdds"))
                if (!$util.isInteger(message.ExtraOdds))
                    return "ExtraOdds: integer expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcsh.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcsh.SinglePlate)
                return object;
            let message = new $root.jlcsh.SinglePlate();
            if (object.AwardVec) {
                if (!Array.isArray(object.AwardVec))
                    throw TypeError(".jlcsh.SinglePlate.AwardVec: array expected");
                message.AwardVec = [];
                for (let i = 0; i < object.AwardVec.length; ++i) {
                    if (typeof object.AwardVec[i] !== "object")
                        throw TypeError(".jlcsh.SinglePlate.AwardVec: object expected");
                    message.AwardVec[i] = $root.jlcsh.AwardDetail.fromObject(object.AwardVec[i]);
                }
            }
            if (object.PlateVec) {
                if (!Array.isArray(object.PlateVec))
                    throw TypeError(".jlcsh.SinglePlate.PlateVec: array expected");
                message.PlateVec = [];
                for (let i = 0; i < object.PlateVec.length; ++i) {
                    if (typeof object.PlateVec[i] !== "object")
                        throw TypeError(".jlcsh.SinglePlate.PlateVec: object expected");
                    message.PlateVec[i] = $root.jlcsh.Column.fromObject(object.PlateVec[i]);
                }
            }
            if (object.ColumnSymbol) {
                if (!Array.isArray(object.ColumnSymbol))
                    throw TypeError(".jlcsh.SinglePlate.ColumnSymbol: array expected");
                message.ColumnSymbol = [];
                for (let i = 0; i < object.ColumnSymbol.length; ++i) {
                    if (typeof object.ColumnSymbol[i] !== "object")
                        throw TypeError(".jlcsh.SinglePlate.ColumnSymbol: object expected");
                    message.ColumnSymbol[i] = $root.jlcsh.Column.fromObject(object.ColumnSymbol[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RemainRound != null)
                message.RemainRound = object.RemainRound | 0;
            if (object.ExtraRound != null)
                message.ExtraRound = object.ExtraRound | 0;
            if (object.ExtraOdds != null)
                message.ExtraOdds = object.ExtraOdds | 0;
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {jlcsh.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.AwardVec = [];
                object.PlateVec = [];
                object.ColumnSymbol = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RemainRound = 0;
                object.ExtraRound = 0;
                object.ExtraOdds = 0;
            }
            if (message.AwardVec && message.AwardVec.length) {
                object.AwardVec = [];
                for (let j = 0; j < message.AwardVec.length; ++j)
                    object.AwardVec[j] = $root.jlcsh.AwardDetail.toObject(message.AwardVec[j], options);
            }
            if (message.PlateVec && message.PlateVec.length) {
                object.PlateVec = [];
                for (let j = 0; j < message.PlateVec.length; ++j)
                    object.PlateVec[j] = $root.jlcsh.Column.toObject(message.PlateVec[j], options);
            }
            if (message.ColumnSymbol && message.ColumnSymbol.length) {
                object.ColumnSymbol = [];
                for (let j = 0; j < message.ColumnSymbol.length; ++j)
                    object.ColumnSymbol[j] = $root.jlcsh.Column.toObject(message.ColumnSymbol[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RemainRound != null && message.hasOwnProperty("RemainRound"))
                object.RemainRound = message.RemainRound;
            if (message.ExtraRound != null && message.hasOwnProperty("ExtraRound"))
                object.ExtraRound = message.ExtraRound;
            if (message.ExtraOdds != null && message.hasOwnProperty("ExtraOdds"))
                object.ExtraOdds = message.ExtraOdds;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlcsh.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlcsh.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcsh.SinglePlate";
        };

        return SinglePlate;
    })();

    jlcsh.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlcsh
         * @interface ISpinAck
         * @property {Array.<jlcsh.ISinglePlate>|null} [AckVec] SpinAck AckVec
         * @property {number|null} [MGWin] SpinAck MGWin
         * @property {number|null} [FGWin] SpinAck FGWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlcsh
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlcsh.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.AckVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck AckVec.
         * @member {Array.<jlcsh.ISinglePlate>} AckVec
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.AckVec = $util.emptyArray;

        /**
         * SpinAck MGWin.
         * @member {number} MGWin
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.MGWin = 0;

        /**
         * SpinAck FGWin.
         * @member {number} FGWin
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.FGWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlcsh.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlcsh.SpinAck
         * @static
         * @param {jlcsh.ISpinAck=} [properties] Properties to set
         * @returns {jlcsh.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlcsh.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlcsh.SpinAck
         * @static
         * @param {jlcsh.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AckVec != null && message.AckVec.length)
                for (let i = 0; i < message.AckVec.length; ++i)
                    $root.jlcsh.SinglePlate.encode(message.AckVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.MGWin != null && Object.hasOwnProperty.call(message, "MGWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.MGWin);
            if (message.FGWin != null && Object.hasOwnProperty.call(message, "FGWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FGWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlcsh.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlcsh.SpinAck
         * @static
         * @param {jlcsh.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlcsh.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlcsh.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlcsh.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AckVec && message.AckVec.length))
                            message.AckVec = [];
                        message.AckVec.push($root.jlcsh.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.MGWin = reader.double();
                        break;
                    }
                case 3: {
                        message.FGWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlcsh.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlcsh.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlcsh.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AckVec != null && message.hasOwnProperty("AckVec")) {
                if (!Array.isArray(message.AckVec))
                    return "AckVec: array expected";
                for (let i = 0; i < message.AckVec.length; ++i) {
                    let error = $root.jlcsh.SinglePlate.verify(message.AckVec[i]);
                    if (error)
                        return "AckVec." + error;
                }
            }
            if (message.MGWin != null && message.hasOwnProperty("MGWin"))
                if (typeof message.MGWin !== "number")
                    return "MGWin: number expected";
            if (message.FGWin != null && message.hasOwnProperty("FGWin"))
                if (typeof message.FGWin !== "number")
                    return "FGWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlcsh.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlcsh.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlcsh.SpinAck)
                return object;
            let message = new $root.jlcsh.SpinAck();
            if (object.AckVec) {
                if (!Array.isArray(object.AckVec))
                    throw TypeError(".jlcsh.SpinAck.AckVec: array expected");
                message.AckVec = [];
                for (let i = 0; i < object.AckVec.length; ++i) {
                    if (typeof object.AckVec[i] !== "object")
                        throw TypeError(".jlcsh.SpinAck.AckVec: object expected");
                    message.AckVec[i] = $root.jlcsh.SinglePlate.fromObject(object.AckVec[i]);
                }
            }
            if (object.MGWin != null)
                message.MGWin = Number(object.MGWin);
            if (object.FGWin != null)
                message.FGWin = Number(object.FGWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlcsh.SpinAck
         * @static
         * @param {jlcsh.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AckVec = [];
            if (options.defaults) {
                object.MGWin = 0;
                object.FGWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.AckVec && message.AckVec.length) {
                object.AckVec = [];
                for (let j = 0; j < message.AckVec.length; ++j)
                    object.AckVec[j] = $root.jlcsh.SinglePlate.toObject(message.AckVec[j], options);
            }
            if (message.MGWin != null && message.hasOwnProperty("MGWin"))
                object.MGWin = options.json && !isFinite(message.MGWin) ? String(message.MGWin) : message.MGWin;
            if (message.FGWin != null && message.hasOwnProperty("FGWin"))
                object.FGWin = options.json && !isFinite(message.FGWin) ? String(message.FGWin) : message.FGWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlcsh.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlcsh.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlcsh.SpinAck";
        };

        return SpinAck;
    })();

    return jlcsh;
})();

export const jlct = $root.jlct = (() => {

    /**
     * Namespace jlct.
     * @exports jlct
     * @namespace
     */
    const jlct = {};

    jlct.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlct
         * @interface ISpinAck
         * @property {number|null} [Award] SpinAck Award
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {Array.<number>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlct
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlct.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck Award.
         * @member {number} Award
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.Award = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<number>} PlateSymbol
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlct.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlct.SpinAck
         * @static
         * @param {jlct.ISpinAck=} [properties] Properties to set
         * @returns {jlct.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlct.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlct.SpinAck
         * @static
         * @param {jlct.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Award != null && Object.hasOwnProperty.call(message, "Award"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Award);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.PlateSymbol != null && message.PlateSymbol.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.int32(message.PlateSymbol[i]);
                writer.ldelim();
            }
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlct.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlct.SpinAck
         * @static
         * @param {jlct.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlct.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlct.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlct.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Award = reader.int32();
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateSymbol.push(reader.int32());
                        } else
                            message.PlateSymbol.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlct.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlct.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlct.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Award != null && message.hasOwnProperty("Award"))
                if (!$util.isInteger(message.Award))
                    return "Award: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isInteger(message.PlateSymbol[i]))
                        return "PlateSymbol: integer[] expected";
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlct.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlct.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlct.SpinAck)
                return object;
            let message = new $root.jlct.SpinAck();
            if (object.Award != null)
                message.Award = object.Award | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlct.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = object.PlateSymbol[i] | 0;
            }
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlct.SpinAck
         * @static
         * @param {jlct.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateSymbol = [];
            if (options.defaults) {
                object.Award = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                object.Award = message.Award;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlct.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlct.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlct.SpinAck";
        };

        return SpinAck;
    })();

    return jlct;
})();

export const jldb = $root.jldb = (() => {

    /**
     * Namespace jldb.
     * @exports jldb
     * @namespace
     */
    const jldb = {};

    jldb.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jldb
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jldb
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jldb.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jldb.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jldb.Column
         * @static
         * @param {jldb.IColumn=} [properties] Properties to set
         * @returns {jldb.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jldb.Column.verify|verify} messages.
         * @function encode
         * @memberof jldb.Column
         * @static
         * @param {jldb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jldb.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.Column
         * @static
         * @param {jldb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jldb.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.Column)
                return object;
            let message = new $root.jldb.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jldb.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.Column
         * @static
         * @param {jldb.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jldb.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jldb.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.Column";
        };

        return Column;
    })();

    jldb.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jldb
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jldb
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jldb.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jldb.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jldb.ColumnFloat
         * @static
         * @param {jldb.IColumnFloat=} [properties] Properties to set
         * @returns {jldb.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jldb.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jldb.ColumnFloat
         * @static
         * @param {jldb.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jldb.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.ColumnFloat
         * @static
         * @param {jldb.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jldb.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.ColumnFloat)
                return object;
            let message = new $root.jldb.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jldb.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.ColumnFloat
         * @static
         * @param {jldb.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jldb.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jldb.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jldb.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jldb
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jldb
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jldb.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jldb.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jldb.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jldb.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jldb.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jldb.AwardData
         * @static
         * @param {jldb.IAwardData=} [properties] Properties to set
         * @returns {jldb.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jldb.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jldb.AwardData
         * @static
         * @param {jldb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jldb.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.AwardData
         * @static
         * @param {jldb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jldb.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.AwardData)
                return object;
            let message = new $root.jldb.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.AwardData
         * @static
         * @param {jldb.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jldb.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jldb.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.AwardData";
        };

        return AwardData;
    })();

    jldb.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jldb
         * @interface IRoundInfo
         * @property {Array.<jldb.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jldb.IColumnFloat>|null} [PlateSymbolNum] RoundInfo PlateSymbolNum
         * @property {Array.<jldb.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [RoundTotalMult] RoundInfo RoundTotalMult
         * @property {jldb.IColumnFloat|null} [PlateSymbolNumLog] RoundInfo PlateSymbolNumLog
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jldb
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jldb.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jldb.IColumn>} PlateSymbol
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNum.
         * @member {Array.<jldb.IColumnFloat>} PlateSymbolNum
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jldb.IAwardData>} AwardDataVec
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo RoundTotalMult.
         * @member {number} RoundTotalMult
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundTotalMult = 0;

        /**
         * RoundInfo PlateSymbolNumLog.
         * @member {jldb.IColumnFloat|null|undefined} PlateSymbolNumLog
         * @memberof jldb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNumLog = null;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jldb.RoundInfo
         * @static
         * @param {jldb.IRoundInfo=} [properties] Properties to set
         * @returns {jldb.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jldb.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jldb.RoundInfo
         * @static
         * @param {jldb.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jldb.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jldb.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jldb.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.RoundTotalMult != null && Object.hasOwnProperty.call(message, "RoundTotalMult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RoundTotalMult);
            if (message.PlateSymbolNumLog != null && Object.hasOwnProperty.call(message, "PlateSymbolNumLog"))
                $root.jldb.ColumnFloat.encode(message.PlateSymbolNumLog, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jldb.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.RoundInfo
         * @static
         * @param {jldb.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jldb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jldb.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jldb.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.RoundTotalMult = reader.double();
                        break;
                    }
                case 8: {
                        message.PlateSymbolNumLog = $root.jldb.ColumnFloat.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jldb.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jldb.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jldb.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jldb.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.RoundTotalMult != null && message.hasOwnProperty("RoundTotalMult"))
                if (typeof message.RoundTotalMult !== "number")
                    return "RoundTotalMult: number expected";
            if (message.PlateSymbolNumLog != null && message.hasOwnProperty("PlateSymbolNumLog")) {
                let error = $root.jldb.ColumnFloat.verify(message.PlateSymbolNumLog);
                if (error)
                    return "PlateSymbolNumLog." + error;
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.RoundInfo)
                return object;
            let message = new $root.jldb.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jldb.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jldb.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jldb.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jldb.RoundInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jldb.RoundInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jldb.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jldb.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jldb.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jldb.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.RoundTotalMult != null)
                message.RoundTotalMult = Number(object.RoundTotalMult);
            if (object.PlateSymbolNumLog != null) {
                if (typeof object.PlateSymbolNumLog !== "object")
                    throw TypeError(".jldb.RoundInfo.PlateSymbolNumLog: object expected");
                message.PlateSymbolNumLog = $root.jldb.ColumnFloat.fromObject(object.PlateSymbolNumLog);
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.RoundInfo
         * @static
         * @param {jldb.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.RoundTotalMult = 0;
                object.PlateSymbolNumLog = null;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jldb.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jldb.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jldb.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.RoundTotalMult != null && message.hasOwnProperty("RoundTotalMult"))
                object.RoundTotalMult = options.json && !isFinite(message.RoundTotalMult) ? String(message.RoundTotalMult) : message.RoundTotalMult;
            if (message.PlateSymbolNumLog != null && message.hasOwnProperty("PlateSymbolNumLog"))
                object.PlateSymbolNumLog = $root.jldb.ColumnFloat.toObject(message.PlateSymbolNumLog, options);
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jldb.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jldb.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.RoundInfo";
        };

        return RoundInfo;
    })();

    jldb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jldb
         * @interface ISpinAck
         * @property {Array.<jldb.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jldb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jldb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jldb.IRoundInfo>} RoundQueue
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jldb.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jldb.SpinAck
         * @static
         * @param {jldb.ISpinAck=} [properties] Properties to set
         * @returns {jldb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jldb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jldb.SpinAck
         * @static
         * @param {jldb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jldb.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jldb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.SpinAck
         * @static
         * @param {jldb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jldb.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jldb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jldb.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.SpinAck)
                return object;
            let message = new $root.jldb.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jldb.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jldb.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jldb.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.SpinAck
         * @static
         * @param {jldb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jldb.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jldb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jldb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.SpinAck";
        };

        return SpinAck;
    })();

    jldb.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jldb
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         * @property {Array.<jldb.IColumn>|null} [FGUpgrade] GameInfoAck FGUpgrade
         * @property {number|null} [ApiType] GameInfoAck ApiType
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jldb
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jldb.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            this.FGUpgrade = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jldb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoAck FGUpgrade.
         * @member {Array.<jldb.IColumn>} FGUpgrade
         * @memberof jldb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.FGUpgrade = $util.emptyArray;

        /**
         * GameInfoAck ApiType.
         * @member {number} ApiType
         * @memberof jldb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.ApiType = 0;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jldb.GameInfoAck
         * @static
         * @param {jldb.IGameInfoAck=} [properties] Properties to set
         * @returns {jldb.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jldb.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jldb.GameInfoAck
         * @static
         * @param {jldb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.FGUpgrade != null && message.FGUpgrade.length)
                for (let i = 0; i < message.FGUpgrade.length; ++i)
                    $root.jldb.Column.encode(message.FGUpgrade[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.ApiType != null && Object.hasOwnProperty.call(message, "ApiType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.ApiType);
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jldb.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.GameInfoAck
         * @static
         * @param {jldb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 6: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 9: {
                        if (!(message.FGUpgrade && message.FGUpgrade.length))
                            message.FGUpgrade = [];
                        message.FGUpgrade.push($root.jldb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        message.ApiType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jldb.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.FGUpgrade != null && message.hasOwnProperty("FGUpgrade")) {
                if (!Array.isArray(message.FGUpgrade))
                    return "FGUpgrade: array expected";
                for (let i = 0; i < message.FGUpgrade.length; ++i) {
                    let error = $root.jldb.Column.verify(message.FGUpgrade[i]);
                    if (error)
                        return "FGUpgrade." + error;
                }
            }
            if (message.ApiType != null && message.hasOwnProperty("ApiType"))
                if (!$util.isInteger(message.ApiType))
                    return "ApiType: integer expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.GameInfoAck)
                return object;
            let message = new $root.jldb.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jldb.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.FGUpgrade) {
                if (!Array.isArray(object.FGUpgrade))
                    throw TypeError(".jldb.GameInfoAck.FGUpgrade: array expected");
                message.FGUpgrade = [];
                for (let i = 0; i < object.FGUpgrade.length; ++i) {
                    if (typeof object.FGUpgrade[i] !== "object")
                        throw TypeError(".jldb.GameInfoAck.FGUpgrade: object expected");
                    message.FGUpgrade[i] = $root.jldb.Column.fromObject(object.FGUpgrade[i]);
                }
            }
            if (object.ApiType != null)
                message.ApiType = object.ApiType | 0;
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.GameInfoAck
         * @static
         * @param {jldb.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.FGUpgrade = [];
            }
            if (options.defaults)
                object.ApiType = 0;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.FGUpgrade && message.FGUpgrade.length) {
                object.FGUpgrade = [];
                for (let j = 0; j < message.FGUpgrade.length; ++j)
                    object.FGUpgrade[j] = $root.jldb.Column.toObject(message.FGUpgrade[j], options);
            }
            if (message.ApiType != null && message.hasOwnProperty("ApiType"))
                object.ApiType = message.ApiType;
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jldb.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jldb.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jldb.SpinEndAck = (function() {

        /**
         * Properties of a SpinEndAck.
         * @memberof jldb
         * @interface ISpinEndAck
         * @property {number|null} [AckType] SpinEndAck AckType
         * @property {number|null} [NowRound] SpinEndAck NowRound
         */

        /**
         * Constructs a new SpinEndAck.
         * @memberof jldb
         * @classdesc Represents a SpinEndAck.
         * @implements ISpinEndAck
         * @constructor
         * @param {jldb.ISpinEndAck=} [properties] Properties to set
         */
        function SpinEndAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinEndAck AckType.
         * @member {number} AckType
         * @memberof jldb.SpinEndAck
         * @instance
         */
        SpinEndAck.prototype.AckType = 0;

        /**
         * SpinEndAck NowRound.
         * @member {number} NowRound
         * @memberof jldb.SpinEndAck
         * @instance
         */
        SpinEndAck.prototype.NowRound = 0;

        /**
         * Creates a new SpinEndAck instance using the specified properties.
         * @function create
         * @memberof jldb.SpinEndAck
         * @static
         * @param {jldb.ISpinEndAck=} [properties] Properties to set
         * @returns {jldb.SpinEndAck} SpinEndAck instance
         */
        SpinEndAck.create = function create(properties) {
            return new SpinEndAck(properties);
        };

        /**
         * Encodes the specified SpinEndAck message. Does not implicitly {@link jldb.SpinEndAck.verify|verify} messages.
         * @function encode
         * @memberof jldb.SpinEndAck
         * @static
         * @param {jldb.ISpinEndAck} message SpinEndAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinEndAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AckType);
            if (message.NowRound != null && Object.hasOwnProperty.call(message, "NowRound"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.NowRound);
            return writer;
        };

        /**
         * Encodes the specified SpinEndAck message, length delimited. Does not implicitly {@link jldb.SpinEndAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldb.SpinEndAck
         * @static
         * @param {jldb.ISpinEndAck} message SpinEndAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinEndAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinEndAck message from the specified reader or buffer.
         * @function decode
         * @memberof jldb.SpinEndAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldb.SpinEndAck} SpinEndAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinEndAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldb.SpinEndAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 2: {
                        message.NowRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinEndAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldb.SpinEndAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldb.SpinEndAck} SpinEndAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinEndAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinEndAck message.
         * @function verify
         * @memberof jldb.SpinEndAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinEndAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.NowRound != null && message.hasOwnProperty("NowRound"))
                if (!$util.isInteger(message.NowRound))
                    return "NowRound: integer expected";
            return null;
        };

        /**
         * Creates a SpinEndAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldb.SpinEndAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldb.SpinEndAck} SpinEndAck
         */
        SpinEndAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jldb.SpinEndAck)
                return object;
            let message = new $root.jldb.SpinEndAck();
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.NowRound != null)
                message.NowRound = object.NowRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinEndAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldb.SpinEndAck
         * @static
         * @param {jldb.SpinEndAck} message SpinEndAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinEndAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.AckType = 0;
                object.NowRound = 0;
            }
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.NowRound != null && message.hasOwnProperty("NowRound"))
                object.NowRound = message.NowRound;
            return object;
        };

        /**
         * Converts this SpinEndAck to JSON.
         * @function toJSON
         * @memberof jldb.SpinEndAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinEndAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinEndAck
         * @function getTypeUrl
         * @memberof jldb.SpinEndAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinEndAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldb.SpinEndAck";
        };

        return SpinEndAck;
    })();

    return jldb;
})();

export const jldf1 = $root.jldf1 = (() => {

    /**
     * Namespace jldf1.
     * @exports jldf1
     * @namespace
     */
    const jldf1 = {};

    jldf1.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jldf1
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jldf1
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jldf1.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jldf1.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jldf1.Column
         * @static
         * @param {jldf1.IColumn=} [properties] Properties to set
         * @returns {jldf1.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jldf1.Column.verify|verify} messages.
         * @function encode
         * @memberof jldf1.Column
         * @static
         * @param {jldf1.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jldf1.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf1.Column
         * @static
         * @param {jldf1.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jldf1.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf1.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf1.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf1.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf1.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jldf1.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf1.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf1.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf1.Column)
                return object;
            let message = new $root.jldf1.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jldf1.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf1.Column
         * @static
         * @param {jldf1.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jldf1.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jldf1.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf1.Column";
        };

        return Column;
    })();

    jldf1.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jldf1
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         */

        /**
         * Constructs a new AwardData.
         * @memberof jldf1
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jldf1.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jldf1.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jldf1.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jldf1.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jldf1.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jldf1.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jldf1.AwardData
         * @static
         * @param {jldf1.IAwardData=} [properties] Properties to set
         * @returns {jldf1.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jldf1.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jldf1.AwardData
         * @static
         * @param {jldf1.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mult);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jldf1.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf1.AwardData
         * @static
         * @param {jldf1.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jldf1.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf1.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf1.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        message.Mult = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf1.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf1.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jldf1.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf1.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf1.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf1.AwardData)
                return object;
            let message = new $root.jldf1.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf1.AwardData
         * @static
         * @param {jldf1.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jldf1.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jldf1.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf1.AwardData";
        };

        return AwardData;
    })();

    jldf1.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jldf1
         * @interface IRoundInfo
         * @property {Array.<jldf1.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jldf1.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeType] RoundInfo FreeType
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jldf1
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jldf1.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jldf1.IColumn>} PlateSymbol
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jldf1.IAwardData>} AwardDataVec
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeType.
         * @member {number} FreeType
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeType = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jldf1.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jldf1.RoundInfo
         * @static
         * @param {jldf1.IRoundInfo=} [properties] Properties to set
         * @returns {jldf1.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jldf1.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jldf1.RoundInfo
         * @static
         * @param {jldf1.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jldf1.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jldf1.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeType != null && Object.hasOwnProperty.call(message, "FreeType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeType);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Mult);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RoundWin);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeTotalRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FreeNowRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jldf1.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf1.RoundInfo
         * @static
         * @param {jldf1.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jldf1.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf1.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf1.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jldf1.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jldf1.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeType = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.Mult = reader.double();
                        break;
                    }
                case 7: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 8: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf1.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf1.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jldf1.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jldf1.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jldf1.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                if (!$util.isInteger(message.FreeType))
                    return "FreeType: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf1.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf1.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf1.RoundInfo)
                return object;
            let message = new $root.jldf1.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jldf1.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jldf1.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jldf1.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jldf1.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jldf1.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jldf1.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeType != null)
                message.FreeType = object.FreeType | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf1.RoundInfo
         * @static
         * @param {jldf1.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeType = 0;
                object.FreeRemainRound = 0;
                object.Mult = 0;
                object.RoundWin = 0;
                object.FreeTotalRound = 0;
                object.FreeNowRound = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jldf1.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jldf1.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                object.FreeType = message.FreeType;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jldf1.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jldf1.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf1.RoundInfo";
        };

        return RoundInfo;
    })();

    jldf1.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jldf1
         * @interface ISpinAck
         * @property {Array.<jldf1.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jldf1
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jldf1.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jldf1.IRoundInfo>} RoundQueue
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jldf1.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jldf1.SpinAck
         * @static
         * @param {jldf1.ISpinAck=} [properties] Properties to set
         * @returns {jldf1.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jldf1.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jldf1.SpinAck
         * @static
         * @param {jldf1.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jldf1.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Extra);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.Bet);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jldf1.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf1.SpinAck
         * @static
         * @param {jldf1.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jldf1.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf1.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf1.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jldf1.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.Bet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf1.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf1.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jldf1.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jldf1.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf1.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf1.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf1.SpinAck)
                return object;
            let message = new $root.jldf1.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jldf1.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jldf1.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jldf1.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf1.SpinAck
         * @static
         * @param {jldf1.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.Extra = false;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jldf1.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jldf1.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jldf1.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf1.SpinAck";
        };

        return SpinAck;
    })();

    jldf1.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jldf1
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [JPOdds] GameInfoData JPOdds
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jldf1
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jldf1.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.JPOdds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jldf1.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jldf1.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<number>} JPOdds
         * @memberof jldf1.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jldf1.GameInfoData
         * @static
         * @param {jldf1.IGameInfoData=} [properties] Properties to set
         * @returns {jldf1.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jldf1.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jldf1.GameInfoData
         * @static
         * @param {jldf1.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.JPOdds != null && message.JPOdds.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.JPOdds.length; ++i)
                    writer.double(message.JPOdds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jldf1.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf1.GameInfoData
         * @static
         * @param {jldf1.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jldf1.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf1.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf1.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPOdds.push(reader.double());
                        } else
                            message.JPOdds.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf1.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf1.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jldf1.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i)
                    if (typeof message.JPOdds[i] !== "number")
                        return "JPOdds: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf1.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf1.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf1.GameInfoData)
                return object;
            let message = new $root.jldf1.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jldf1.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jldf1.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i)
                    message.JPOdds[i] = Number(object.JPOdds[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf1.GameInfoData
         * @static
         * @param {jldf1.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.JPOdds = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = options.json && !isFinite(message.JPOdds[j]) ? String(message.JPOdds[j]) : message.JPOdds[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jldf1.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jldf1.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf1.GameInfoData";
        };

        return GameInfoData;
    })();

    return jldf1;
})();

export const jldf2 = $root.jldf2 = (() => {

    /**
     * Namespace jldf2.
     * @exports jldf2
     * @namespace
     */
    const jldf2 = {};

    jldf2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jldf2
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jldf2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jldf2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jldf2.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jldf2.Column
         * @static
         * @param {jldf2.IColumn=} [properties] Properties to set
         * @returns {jldf2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jldf2.Column.verify|verify} messages.
         * @function encode
         * @memberof jldf2.Column
         * @static
         * @param {jldf2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jldf2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf2.Column
         * @static
         * @param {jldf2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jldf2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jldf2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf2.Column)
                return object;
            let message = new $root.jldf2.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jldf2.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf2.Column
         * @static
         * @param {jldf2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jldf2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jldf2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf2.Column";
        };

        return Column;
    })();

    jldf2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jldf2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jldf2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jldf2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jldf2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jldf2.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jldf2.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jldf2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jldf2.AwardData
         * @static
         * @param {jldf2.IAwardData=} [properties] Properties to set
         * @returns {jldf2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jldf2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jldf2.AwardData
         * @static
         * @param {jldf2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Index);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jldf2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf2.AwardData
         * @static
         * @param {jldf2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jldf2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Index = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jldf2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf2.AwardData)
                return object;
            let message = new $root.jldf2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf2.AwardData
         * @static
         * @param {jldf2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Index = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jldf2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jldf2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf2.AwardData";
        };

        return AwardData;
    })();

    jldf2.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jldf2
         * @interface ISinglePlate
         * @property {Array.<jldf2.IColumn>|null} [Symbol] SinglePlate Symbol
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         * @property {Array.<jldf2.IAwardData>|null} [AwardDataVec] SinglePlate AwardDataVec
         * @property {boolean|null} [IsBonus] SinglePlate IsBonus
         * @property {number|null} [BonusWin] SinglePlate BonusWin
         * @property {number|null} [GetFreeRound] SinglePlate GetFreeRound
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {Array.<number>|null} [JPWin] SinglePlate JPWin
         * @property {Array.<jldf2.IAwardData>|null} [BonusDataVec] SinglePlate BonusDataVec
         * @property {number|null} [FreeNowRound] SinglePlate FreeNowRound
         * @property {number|null} [FreeTotalRound] SinglePlate FreeTotalRound
         * @property {boolean|null} [MaxFlag] SinglePlate MaxFlag
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jldf2
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jldf2.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.Symbol = [];
            this.AwardDataVec = [];
            this.JPWin = [];
            this.BonusDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate Symbol.
         * @member {Array.<jldf2.IColumn>} Symbol
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Symbol = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * SinglePlate AwardDataVec.
         * @member {Array.<jldf2.IAwardData>} AwardDataVec
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SinglePlate IsBonus.
         * @member {boolean} IsBonus
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.IsBonus = false;

        /**
         * SinglePlate BonusWin.
         * @member {number} BonusWin
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusWin = 0;

        /**
         * SinglePlate GetFreeRound.
         * @member {number} GetFreeRound
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.GetFreeRound = 0;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate JPWin.
         * @member {Array.<number>} JPWin
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.JPWin = $util.emptyArray;

        /**
         * SinglePlate BonusDataVec.
         * @member {Array.<jldf2.IAwardData>} BonusDataVec
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusDataVec = $util.emptyArray;

        /**
         * SinglePlate FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeNowRound = 0;

        /**
         * SinglePlate FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeTotalRound = 0;

        /**
         * SinglePlate MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jldf2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.MaxFlag = false;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jldf2.SinglePlate
         * @static
         * @param {jldf2.ISinglePlate=} [properties] Properties to set
         * @returns {jldf2.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jldf2.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jldf2.SinglePlate
         * @static
         * @param {jldf2.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jldf2.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.SingleWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jldf2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.IsBonus != null && Object.hasOwnProperty.call(message, "IsBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.IsBonus);
            if (message.BonusWin != null && Object.hasOwnProperty.call(message, "BonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.BonusWin);
            if (message.GetFreeRound != null && Object.hasOwnProperty.call(message, "GetFreeRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.GetFreeRound);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AwardTypeFlag);
            if (message.JPWin != null && message.JPWin.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.JPWin.length; ++i)
                    writer.double(message.JPWin[i]);
                writer.ldelim();
            }
            if (message.BonusDataVec != null && message.BonusDataVec.length)
                for (let i = 0; i < message.BonusDataVec.length; ++i)
                    $root.jldf2.AwardData.encode(message.BonusDataVec[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jldf2.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf2.SinglePlate
         * @static
         * @param {jldf2.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jldf2.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf2.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf2.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jldf2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jldf2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.IsBonus = reader.bool();
                        break;
                    }
                case 5: {
                        message.BonusWin = reader.double();
                        break;
                    }
                case 6: {
                        message.GetFreeRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 8: {
                        if (!(message.JPWin && message.JPWin.length))
                            message.JPWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPWin.push(reader.double());
                        } else
                            message.JPWin.push(reader.double());
                        break;
                    }
                case 9: {
                        if (!(message.BonusDataVec && message.BonusDataVec.length))
                            message.BonusDataVec = [];
                        message.BonusDataVec.push($root.jldf2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf2.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf2.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jldf2.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jldf2.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jldf2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                if (typeof message.IsBonus !== "boolean")
                    return "IsBonus: boolean expected";
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                if (typeof message.BonusWin !== "number")
                    return "BonusWin: number expected";
            if (message.GetFreeRound != null && message.hasOwnProperty("GetFreeRound"))
                if (!$util.isInteger(message.GetFreeRound))
                    return "GetFreeRound: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.JPWin != null && message.hasOwnProperty("JPWin")) {
                if (!Array.isArray(message.JPWin))
                    return "JPWin: array expected";
                for (let i = 0; i < message.JPWin.length; ++i)
                    if (typeof message.JPWin[i] !== "number")
                        return "JPWin: number[] expected";
            }
            if (message.BonusDataVec != null && message.hasOwnProperty("BonusDataVec")) {
                if (!Array.isArray(message.BonusDataVec))
                    return "BonusDataVec: array expected";
                for (let i = 0; i < message.BonusDataVec.length; ++i) {
                    let error = $root.jldf2.AwardData.verify(message.BonusDataVec[i]);
                    if (error)
                        return "BonusDataVec." + error;
                }
            }
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf2.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf2.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf2.SinglePlate)
                return object;
            let message = new $root.jldf2.SinglePlate();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jldf2.SinglePlate.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jldf2.SinglePlate.Symbol: object expected");
                    message.Symbol[i] = $root.jldf2.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jldf2.SinglePlate.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jldf2.SinglePlate.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jldf2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.IsBonus != null)
                message.IsBonus = Boolean(object.IsBonus);
            if (object.BonusWin != null)
                message.BonusWin = Number(object.BonusWin);
            if (object.GetFreeRound != null)
                message.GetFreeRound = object.GetFreeRound | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.JPWin) {
                if (!Array.isArray(object.JPWin))
                    throw TypeError(".jldf2.SinglePlate.JPWin: array expected");
                message.JPWin = [];
                for (let i = 0; i < object.JPWin.length; ++i)
                    message.JPWin[i] = Number(object.JPWin[i]);
            }
            if (object.BonusDataVec) {
                if (!Array.isArray(object.BonusDataVec))
                    throw TypeError(".jldf2.SinglePlate.BonusDataVec: array expected");
                message.BonusDataVec = [];
                for (let i = 0; i < object.BonusDataVec.length; ++i) {
                    if (typeof object.BonusDataVec[i] !== "object")
                        throw TypeError(".jldf2.SinglePlate.BonusDataVec: object expected");
                    message.BonusDataVec[i] = $root.jldf2.AwardData.fromObject(object.BonusDataVec[i]);
                }
            }
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf2.SinglePlate
         * @static
         * @param {jldf2.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.AwardDataVec = [];
                object.JPWin = [];
                object.BonusDataVec = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.IsBonus = false;
                object.BonusWin = 0;
                object.GetFreeRound = 0;
                object.AwardTypeFlag = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jldf2.Column.toObject(message.Symbol[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jldf2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                object.IsBonus = message.IsBonus;
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                object.BonusWin = options.json && !isFinite(message.BonusWin) ? String(message.BonusWin) : message.BonusWin;
            if (message.GetFreeRound != null && message.hasOwnProperty("GetFreeRound"))
                object.GetFreeRound = message.GetFreeRound;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.JPWin && message.JPWin.length) {
                object.JPWin = [];
                for (let j = 0; j < message.JPWin.length; ++j)
                    object.JPWin[j] = options.json && !isFinite(message.JPWin[j]) ? String(message.JPWin[j]) : message.JPWin[j];
            }
            if (message.BonusDataVec && message.BonusDataVec.length) {
                object.BonusDataVec = [];
                for (let j = 0; j < message.BonusDataVec.length; ++j)
                    object.BonusDataVec[j] = $root.jldf2.AwardData.toObject(message.BonusDataVec[j], options);
            }
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jldf2.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jldf2.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf2.SinglePlate";
        };

        return SinglePlate;
    })();

    jldf2.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jldf2
         * @interface IFreeData
         * @property {Array.<jldf2.ISinglePlate>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jldf2
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jldf2.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jldf2.ISinglePlate>} FreeQueue
         * @memberof jldf2.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jldf2.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jldf2.FreeData
         * @static
         * @param {jldf2.IFreeData=} [properties] Properties to set
         * @returns {jldf2.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jldf2.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jldf2.FreeData
         * @static
         * @param {jldf2.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jldf2.SinglePlate.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jldf2.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf2.FreeData
         * @static
         * @param {jldf2.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jldf2.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf2.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf2.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jldf2.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf2.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf2.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jldf2.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jldf2.SinglePlate.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf2.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf2.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf2.FreeData)
                return object;
            let message = new $root.jldf2.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jldf2.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jldf2.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jldf2.SinglePlate.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf2.FreeData
         * @static
         * @param {jldf2.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults)
                object.FreeTotalWin = 0;
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jldf2.SinglePlate.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jldf2.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jldf2.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf2.FreeData";
        };

        return FreeData;
    })();

    jldf2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jldf2
         * @interface ISpinAck
         * @property {jldf2.ISinglePlate|null} [MainGame] SpinAck MainGame
         * @property {jldf2.IFreeData|null} [FreeGame] SpinAck FreeGame
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jldf2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jldf2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainGame.
         * @member {jldf2.ISinglePlate|null|undefined} MainGame
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.MainGame = null;

        /**
         * SpinAck FreeGame.
         * @member {jldf2.IFreeData|null|undefined} FreeGame
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeGame = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jldf2.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jldf2.SpinAck
         * @static
         * @param {jldf2.ISpinAck=} [properties] Properties to set
         * @returns {jldf2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jldf2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jldf2.SpinAck
         * @static
         * @param {jldf2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainGame != null && Object.hasOwnProperty.call(message, "MainGame"))
                $root.jldf2.SinglePlate.encode(message.MainGame, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeGame != null && Object.hasOwnProperty.call(message, "FreeGame"))
                $root.jldf2.FreeData.encode(message.FreeGame, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jldf2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf2.SpinAck
         * @static
         * @param {jldf2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jldf2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainGame = $root.jldf2.SinglePlate.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeGame = $root.jldf2.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jldf2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainGame != null && message.hasOwnProperty("MainGame")) {
                let error = $root.jldf2.SinglePlate.verify(message.MainGame);
                if (error)
                    return "MainGame." + error;
            }
            if (message.FreeGame != null && message.hasOwnProperty("FreeGame")) {
                let error = $root.jldf2.FreeData.verify(message.FreeGame);
                if (error)
                    return "FreeGame." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf2.SpinAck)
                return object;
            let message = new $root.jldf2.SpinAck();
            if (object.MainGame != null) {
                if (typeof object.MainGame !== "object")
                    throw TypeError(".jldf2.SpinAck.MainGame: object expected");
                message.MainGame = $root.jldf2.SinglePlate.fromObject(object.MainGame);
            }
            if (object.FreeGame != null) {
                if (typeof object.FreeGame !== "object")
                    throw TypeError(".jldf2.SpinAck.FreeGame: object expected");
                message.FreeGame = $root.jldf2.FreeData.fromObject(object.FreeGame);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf2.SpinAck
         * @static
         * @param {jldf2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainGame = null;
                object.FreeGame = null;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.MainGame != null && message.hasOwnProperty("MainGame"))
                object.MainGame = $root.jldf2.SinglePlate.toObject(message.MainGame, options);
            if (message.FreeGame != null && message.hasOwnProperty("FreeGame"))
                object.FreeGame = $root.jldf2.FreeData.toObject(message.FreeGame, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jldf2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jldf2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf2.SpinAck";
        };

        return SpinAck;
    })();

    jldf2.PigInfo = (function() {

        /**
         * Properties of a PigInfo.
         * @memberof jldf2
         * @interface IPigInfo
         * @property {number|null} [Left] PigInfo Left
         * @property {number|null} [Right] PigInfo Right
         */

        /**
         * Constructs a new PigInfo.
         * @memberof jldf2
         * @classdesc Represents a PigInfo.
         * @implements IPigInfo
         * @constructor
         * @param {jldf2.IPigInfo=} [properties] Properties to set
         */
        function PigInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PigInfo Left.
         * @member {number} Left
         * @memberof jldf2.PigInfo
         * @instance
         */
        PigInfo.prototype.Left = 0;

        /**
         * PigInfo Right.
         * @member {number} Right
         * @memberof jldf2.PigInfo
         * @instance
         */
        PigInfo.prototype.Right = 0;

        /**
         * Creates a new PigInfo instance using the specified properties.
         * @function create
         * @memberof jldf2.PigInfo
         * @static
         * @param {jldf2.IPigInfo=} [properties] Properties to set
         * @returns {jldf2.PigInfo} PigInfo instance
         */
        PigInfo.create = function create(properties) {
            return new PigInfo(properties);
        };

        /**
         * Encodes the specified PigInfo message. Does not implicitly {@link jldf2.PigInfo.verify|verify} messages.
         * @function encode
         * @memberof jldf2.PigInfo
         * @static
         * @param {jldf2.IPigInfo} message PigInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PigInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Left != null && Object.hasOwnProperty.call(message, "Left"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Left);
            if (message.Right != null && Object.hasOwnProperty.call(message, "Right"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Right);
            return writer;
        };

        /**
         * Encodes the specified PigInfo message, length delimited. Does not implicitly {@link jldf2.PigInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldf2.PigInfo
         * @static
         * @param {jldf2.IPigInfo} message PigInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PigInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PigInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jldf2.PigInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldf2.PigInfo} PigInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PigInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldf2.PigInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Left = reader.int32();
                        break;
                    }
                case 2: {
                        message.Right = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PigInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldf2.PigInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldf2.PigInfo} PigInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PigInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PigInfo message.
         * @function verify
         * @memberof jldf2.PigInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PigInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Left != null && message.hasOwnProperty("Left"))
                if (!$util.isInteger(message.Left))
                    return "Left: integer expected";
            if (message.Right != null && message.hasOwnProperty("Right"))
                if (!$util.isInteger(message.Right))
                    return "Right: integer expected";
            return null;
        };

        /**
         * Creates a PigInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldf2.PigInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldf2.PigInfo} PigInfo
         */
        PigInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jldf2.PigInfo)
                return object;
            let message = new $root.jldf2.PigInfo();
            if (object.Left != null)
                message.Left = object.Left | 0;
            if (object.Right != null)
                message.Right = object.Right | 0;
            return message;
        };

        /**
         * Creates a plain object from a PigInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldf2.PigInfo
         * @static
         * @param {jldf2.PigInfo} message PigInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PigInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Left = 0;
                object.Right = 0;
            }
            if (message.Left != null && message.hasOwnProperty("Left"))
                object.Left = message.Left;
            if (message.Right != null && message.hasOwnProperty("Right"))
                object.Right = message.Right;
            return object;
        };

        /**
         * Converts this PigInfo to JSON.
         * @function toJSON
         * @memberof jldf2.PigInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PigInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PigInfo
         * @function getTypeUrl
         * @memberof jldf2.PigInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PigInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldf2.PigInfo";
        };

        return PigInfo;
    })();

    return jldf2;
})();

export const jldft = $root.jldft = (() => {

    /**
     * Namespace jldft.
     * @exports jldft
     * @namespace
     */
    const jldft = {};

    jldft.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jldft
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jldft
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jldft.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jldft.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jldft.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jldft.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jldft.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jldft.AwardData
         * @static
         * @param {jldft.IAwardData=} [properties] Properties to set
         * @returns {jldft.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jldft.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jldft.AwardData
         * @static
         * @param {jldft.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Index);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jldft.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldft.AwardData
         * @static
         * @param {jldft.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jldft.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldft.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldft.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Index = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldft.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldft.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jldft.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldft.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldft.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldft.AwardData)
                return object;
            let message = new $root.jldft.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldft.AwardData
         * @static
         * @param {jldft.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Index = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jldft.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jldft.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldft.AwardData";
        };

        return AwardData;
    })();

    jldft.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jldft
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jldft
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jldft.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jldft.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jldft.Column
         * @static
         * @param {jldft.IColumn=} [properties] Properties to set
         * @returns {jldft.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jldft.Column.verify|verify} messages.
         * @function encode
         * @memberof jldft.Column
         * @static
         * @param {jldft.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jldft.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldft.Column
         * @static
         * @param {jldft.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jldft.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldft.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldft.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldft.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldft.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jldft.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldft.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldft.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jldft.Column)
                return object;
            let message = new $root.jldft.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jldft.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldft.Column
         * @static
         * @param {jldft.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jldft.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jldft.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldft.Column";
        };

        return Column;
    })();

    jldft.PigInfo = (function() {

        /**
         * Properties of a PigInfo.
         * @memberof jldft
         * @interface IPigInfo
         * @property {number|null} [Left] PigInfo Left
         * @property {number|null} [Right] PigInfo Right
         */

        /**
         * Constructs a new PigInfo.
         * @memberof jldft
         * @classdesc Represents a PigInfo.
         * @implements IPigInfo
         * @constructor
         * @param {jldft.IPigInfo=} [properties] Properties to set
         */
        function PigInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PigInfo Left.
         * @member {number} Left
         * @memberof jldft.PigInfo
         * @instance
         */
        PigInfo.prototype.Left = 0;

        /**
         * PigInfo Right.
         * @member {number} Right
         * @memberof jldft.PigInfo
         * @instance
         */
        PigInfo.prototype.Right = 0;

        /**
         * Creates a new PigInfo instance using the specified properties.
         * @function create
         * @memberof jldft.PigInfo
         * @static
         * @param {jldft.IPigInfo=} [properties] Properties to set
         * @returns {jldft.PigInfo} PigInfo instance
         */
        PigInfo.create = function create(properties) {
            return new PigInfo(properties);
        };

        /**
         * Encodes the specified PigInfo message. Does not implicitly {@link jldft.PigInfo.verify|verify} messages.
         * @function encode
         * @memberof jldft.PigInfo
         * @static
         * @param {jldft.IPigInfo} message PigInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PigInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Left != null && Object.hasOwnProperty.call(message, "Left"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Left);
            if (message.Right != null && Object.hasOwnProperty.call(message, "Right"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Right);
            return writer;
        };

        /**
         * Encodes the specified PigInfo message, length delimited. Does not implicitly {@link jldft.PigInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldft.PigInfo
         * @static
         * @param {jldft.IPigInfo} message PigInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PigInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PigInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jldft.PigInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldft.PigInfo} PigInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PigInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldft.PigInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Left = reader.int32();
                        break;
                    }
                case 2: {
                        message.Right = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PigInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldft.PigInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldft.PigInfo} PigInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PigInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PigInfo message.
         * @function verify
         * @memberof jldft.PigInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PigInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Left != null && message.hasOwnProperty("Left"))
                if (!$util.isInteger(message.Left))
                    return "Left: integer expected";
            if (message.Right != null && message.hasOwnProperty("Right"))
                if (!$util.isInteger(message.Right))
                    return "Right: integer expected";
            return null;
        };

        /**
         * Creates a PigInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldft.PigInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldft.PigInfo} PigInfo
         */
        PigInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jldft.PigInfo)
                return object;
            let message = new $root.jldft.PigInfo();
            if (object.Left != null)
                message.Left = object.Left | 0;
            if (object.Right != null)
                message.Right = object.Right | 0;
            return message;
        };

        /**
         * Creates a plain object from a PigInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldft.PigInfo
         * @static
         * @param {jldft.PigInfo} message PigInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PigInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Left = 0;
                object.Right = 0;
            }
            if (message.Left != null && message.hasOwnProperty("Left"))
                object.Left = message.Left;
            if (message.Right != null && message.hasOwnProperty("Right"))
                object.Right = message.Right;
            return object;
        };

        /**
         * Converts this PigInfo to JSON.
         * @function toJSON
         * @memberof jldft.PigInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PigInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PigInfo
         * @function getTypeUrl
         * @memberof jldft.PigInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PigInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldft.PigInfo";
        };

        return PigInfo;
    })();

    jldft.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jldft
         * @interface IRoundInfo
         * @property {Array.<jldft.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<jldft.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {boolean|null} [IsBonus] RoundInfo IsBonus
         * @property {number|null} [BonusWin] RoundInfo BonusWin
         * @property {number|null} [BonusMul] RoundInfo BonusMul
         * @property {Array.<jldft.IAwardData>|null} [BonusDataVec] RoundInfo BonusDataVec
         * @property {number|null} [GetFreeRound] RoundInfo GetFreeRound
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jldft
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jldft.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.AwardDataVec = [];
            this.BonusDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jldft.IColumn>} Symbol
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jldft.IAwardData>} AwardDataVec
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo IsBonus.
         * @member {boolean} IsBonus
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.IsBonus = false;

        /**
         * RoundInfo BonusWin.
         * @member {number} BonusWin
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusWin = 0;

        /**
         * RoundInfo BonusMul.
         * @member {number} BonusMul
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusMul = 0;

        /**
         * RoundInfo BonusDataVec.
         * @member {Array.<jldft.IAwardData>} BonusDataVec
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusDataVec = $util.emptyArray;

        /**
         * RoundInfo GetFreeRound.
         * @member {number} GetFreeRound
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetFreeRound = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jldft.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jldft.RoundInfo
         * @static
         * @param {jldft.IRoundInfo=} [properties] Properties to set
         * @returns {jldft.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jldft.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jldft.RoundInfo
         * @static
         * @param {jldft.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jldft.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jldft.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.IsBonus != null && Object.hasOwnProperty.call(message, "IsBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.IsBonus);
            if (message.BonusWin != null && Object.hasOwnProperty.call(message, "BonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.BonusWin);
            if (message.BonusDataVec != null && message.BonusDataVec.length)
                for (let i = 0; i < message.BonusDataVec.length; ++i)
                    $root.jldft.AwardData.encode(message.BonusDataVec[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.GetFreeRound != null && Object.hasOwnProperty.call(message, "GetFreeRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.GetFreeRound);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AwardTypeFlag);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.MaxFlag);
            if (message.BonusMul != null && Object.hasOwnProperty.call(message, "BonusMul"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.BonusMul);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jldft.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldft.RoundInfo
         * @static
         * @param {jldft.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jldft.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldft.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldft.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jldft.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jldft.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.IsBonus = reader.bool();
                        break;
                    }
                case 5: {
                        message.BonusWin = reader.double();
                        break;
                    }
                case 12: {
                        message.BonusMul = reader.double();
                        break;
                    }
                case 6: {
                        if (!(message.BonusDataVec && message.BonusDataVec.length))
                            message.BonusDataVec = [];
                        message.BonusDataVec.push($root.jldft.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        message.GetFreeRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 9: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldft.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldft.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jldft.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jldft.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jldft.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                if (typeof message.IsBonus !== "boolean")
                    return "IsBonus: boolean expected";
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                if (typeof message.BonusWin !== "number")
                    return "BonusWin: number expected";
            if (message.BonusMul != null && message.hasOwnProperty("BonusMul"))
                if (typeof message.BonusMul !== "number")
                    return "BonusMul: number expected";
            if (message.BonusDataVec != null && message.hasOwnProperty("BonusDataVec")) {
                if (!Array.isArray(message.BonusDataVec))
                    return "BonusDataVec: array expected";
                for (let i = 0; i < message.BonusDataVec.length; ++i) {
                    let error = $root.jldft.AwardData.verify(message.BonusDataVec[i]);
                    if (error)
                        return "BonusDataVec." + error;
                }
            }
            if (message.GetFreeRound != null && message.hasOwnProperty("GetFreeRound"))
                if (!$util.isInteger(message.GetFreeRound))
                    return "GetFreeRound: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldft.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldft.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jldft.RoundInfo)
                return object;
            let message = new $root.jldft.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jldft.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jldft.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jldft.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jldft.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jldft.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jldft.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.IsBonus != null)
                message.IsBonus = Boolean(object.IsBonus);
            if (object.BonusWin != null)
                message.BonusWin = Number(object.BonusWin);
            if (object.BonusMul != null)
                message.BonusMul = Number(object.BonusMul);
            if (object.BonusDataVec) {
                if (!Array.isArray(object.BonusDataVec))
                    throw TypeError(".jldft.RoundInfo.BonusDataVec: array expected");
                message.BonusDataVec = [];
                for (let i = 0; i < object.BonusDataVec.length; ++i) {
                    if (typeof object.BonusDataVec[i] !== "object")
                        throw TypeError(".jldft.RoundInfo.BonusDataVec: object expected");
                    message.BonusDataVec[i] = $root.jldft.AwardData.fromObject(object.BonusDataVec[i]);
                }
            }
            if (object.GetFreeRound != null)
                message.GetFreeRound = object.GetFreeRound | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldft.RoundInfo
         * @static
         * @param {jldft.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.AwardDataVec = [];
                object.BonusDataVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.IsBonus = false;
                object.BonusWin = 0;
                object.GetFreeRound = 0;
                object.AwardTypeFlag = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
                object.BonusMul = 0;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jldft.Column.toObject(message.Symbol[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jldft.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                object.IsBonus = message.IsBonus;
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                object.BonusWin = options.json && !isFinite(message.BonusWin) ? String(message.BonusWin) : message.BonusWin;
            if (message.BonusDataVec && message.BonusDataVec.length) {
                object.BonusDataVec = [];
                for (let j = 0; j < message.BonusDataVec.length; ++j)
                    object.BonusDataVec[j] = $root.jldft.AwardData.toObject(message.BonusDataVec[j], options);
            }
            if (message.GetFreeRound != null && message.hasOwnProperty("GetFreeRound"))
                object.GetFreeRound = message.GetFreeRound;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.BonusMul != null && message.hasOwnProperty("BonusMul"))
                object.BonusMul = options.json && !isFinite(message.BonusMul) ? String(message.BonusMul) : message.BonusMul;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jldft.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jldft.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldft.RoundInfo";
        };

        return RoundInfo;
    })();

    jldft.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jldft
         * @interface ISpinAck
         * @property {Array.<jldft.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jldft
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jldft.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jldft.IRoundInfo>} RoundQueue
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jldft.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jldft.SpinAck
         * @static
         * @param {jldft.ISpinAck=} [properties] Properties to set
         * @returns {jldft.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jldft.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jldft.SpinAck
         * @static
         * @param {jldft.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jldft.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jldft.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldft.SpinAck
         * @static
         * @param {jldft.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jldft.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldft.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldft.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jldft.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 5: {
                        message.RTP = reader.double();
                        break;
                    }
                case 6: {
                        message.LID = reader.int32();
                        break;
                    }
                case 7: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldft.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldft.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jldft.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jldft.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldft.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldft.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jldft.SpinAck)
                return object;
            let message = new $root.jldft.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jldft.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jldft.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jldft.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldft.SpinAck
         * @static
         * @param {jldft.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jldft.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jldft.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jldft.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldft.SpinAck";
        };

        return SpinAck;
    })();

    return jldft;
})();

export const jldp = $root.jldp = (() => {

    /**
     * Namespace jldp.
     * @exports jldp
     * @namespace
     */
    const jldp = {};

    /**
     * Symbol enum.
     * @name jldp.Symbol
     * @enum {number}
     * @property {number} Scatter=0 Scatter value
     * @property {number} Symbol1=1 Symbol1 value
     * @property {number} Symbol2=2 Symbol2 value
     * @property {number} Symbol3=3 Symbol3 value
     * @property {number} Symbol4=4 Symbol4 value
     * @property {number} Symbol5=5 Symbol5 value
     * @property {number} Symbol6=6 Symbol6 value
     * @property {number} Symbol7=7 Symbol7 value
     * @property {number} Wild=8 Wild value
     */
    jldp.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Scatter"] = 0;
        values[valuesById[1] = "Symbol1"] = 1;
        values[valuesById[2] = "Symbol2"] = 2;
        values[valuesById[3] = "Symbol3"] = 3;
        values[valuesById[4] = "Symbol4"] = 4;
        values[valuesById[5] = "Symbol5"] = 5;
        values[valuesById[6] = "Symbol6"] = 6;
        values[valuesById[7] = "Symbol7"] = 7;
        values[valuesById[8] = "Wild"] = 8;
        return values;
    })();

    jldp.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jldp
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jldp
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jldp.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jldp.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jldp.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jldp.Block
         * @static
         * @param {jldp.IBlock=} [properties] Properties to set
         * @returns {jldp.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jldp.Block.verify|verify} messages.
         * @function encode
         * @memberof jldp.Block
         * @static
         * @param {jldp.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jldp.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldp.Block
         * @static
         * @param {jldp.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jldp.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldp.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldp.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldp.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldp.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jldp.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldp.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldp.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jldp.Block)
                return object;
            let message = new $root.jldp.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldp.Block
         * @static
         * @param {jldp.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jldp.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jldp.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldp.Block";
        };

        return Block;
    })();

    jldp.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jldp
         * @interface IAwardReel
         * @property {number|null} [line] AwardReel line
         * @property {Array.<jldp.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         * @property {number|null} [dir] AwardReel dir
         * @property {number|null} [symbol] AwardReel symbol
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jldp
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jldp.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel line.
         * @member {number} line
         * @memberof jldp.AwardReel
         * @instance
         */
        AwardReel.prototype.line = 0;

        /**
         * AwardReel block.
         * @member {Array.<jldp.IBlock>} block
         * @memberof jldp.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jldp.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * AwardReel dir.
         * @member {number} dir
         * @memberof jldp.AwardReel
         * @instance
         */
        AwardReel.prototype.dir = 0;

        /**
         * AwardReel symbol.
         * @member {number} symbol
         * @memberof jldp.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jldp.AwardReel
         * @static
         * @param {jldp.IAwardReel=} [properties] Properties to set
         * @returns {jldp.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jldp.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jldp.AwardReel
         * @static
         * @param {jldp.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.line);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jldp.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.dir);
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.symbol);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jldp.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldp.AwardReel
         * @static
         * @param {jldp.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jldp.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldp.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldp.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.line = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jldp.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.dir = reader.uint32();
                        break;
                    }
                case 5: {
                        message.symbol = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldp.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldp.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jldp.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.line != null && message.hasOwnProperty("line"))
                if (!$util.isInteger(message.line))
                    return "line: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jldp.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.dir != null && message.hasOwnProperty("dir"))
                if (!$util.isInteger(message.dir))
                    return "dir: integer expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldp.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldp.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jldp.AwardReel)
                return object;
            let message = new $root.jldp.AwardReel();
            if (object.line != null)
                message.line = object.line >>> 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jldp.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jldp.AwardReel.block: object expected");
                    message.block[i] = $root.jldp.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.dir != null)
                message.dir = object.dir >>> 0;
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldp.AwardReel
         * @static
         * @param {jldp.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.line = 0;
                object.win = 0;
                object.dir = 0;
                object.symbol = 0;
            }
            if (message.line != null && message.hasOwnProperty("line"))
                object.line = message.line;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jldp.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.dir != null && message.hasOwnProperty("dir"))
                object.dir = message.dir;
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jldp.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jldp.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldp.AwardReel";
        };

        return AwardReel;
    })();

    jldp.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jldp
         * @interface IPlate
         * @property {Array.<jldp.IRow>|null} [column] Plate column
         * @property {Array.<jldp.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         * @property {boolean|null} [isBonus] Plate isBonus
         * @property {number|null} [diamondBonus] Plate diamondBonus
         * @property {number|null} [diamondCount] Plate diamondCount
         * @property {number|null} [diamondStamp] Plate diamondStamp
         * @property {number|null} [awardStamp] Plate awardStamp
         */

        /**
         * Constructs a new Plate.
         * @memberof jldp
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jldp.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jldp.IRow>} column
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jldp.IAwardReel>} award
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate isBonus.
         * @member {boolean} isBonus
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.isBonus = false;

        /**
         * Plate diamondBonus.
         * @member {number} diamondBonus
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.diamondBonus = 0;

        /**
         * Plate diamondCount.
         * @member {number} diamondCount
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.diamondCount = 0;

        /**
         * Plate diamondStamp.
         * @member {number} diamondStamp
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.diamondStamp = 0;

        /**
         * Plate awardStamp.
         * @member {number} awardStamp
         * @memberof jldp.Plate
         * @instance
         */
        Plate.prototype.awardStamp = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jldp.Plate
         * @static
         * @param {jldp.IPlate=} [properties] Properties to set
         * @returns {jldp.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jldp.Plate.verify|verify} messages.
         * @function encode
         * @memberof jldp.Plate
         * @static
         * @param {jldp.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jldp.Row.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jldp.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.isBonus != null && Object.hasOwnProperty.call(message, "isBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isBonus);
            if (message.diamondBonus != null && Object.hasOwnProperty.call(message, "diamondBonus"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.diamondBonus);
            if (message.diamondCount != null && Object.hasOwnProperty.call(message, "diamondCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.diamondCount);
            if (message.diamondStamp != null && Object.hasOwnProperty.call(message, "diamondStamp"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.diamondStamp);
            if (message.awardStamp != null && Object.hasOwnProperty.call(message, "awardStamp"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.awardStamp);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jldp.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldp.Plate
         * @static
         * @param {jldp.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jldp.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldp.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldp.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jldp.Row.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jldp.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.isBonus = reader.bool();
                        break;
                    }
                case 5: {
                        message.diamondBonus = reader.double();
                        break;
                    }
                case 6: {
                        message.diamondCount = reader.uint32();
                        break;
                    }
                case 7: {
                        message.diamondStamp = reader.uint32();
                        break;
                    }
                case 8: {
                        message.awardStamp = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldp.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldp.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jldp.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jldp.Row.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jldp.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                if (typeof message.isBonus !== "boolean")
                    return "isBonus: boolean expected";
            if (message.diamondBonus != null && message.hasOwnProperty("diamondBonus"))
                if (typeof message.diamondBonus !== "number")
                    return "diamondBonus: number expected";
            if (message.diamondCount != null && message.hasOwnProperty("diamondCount"))
                if (!$util.isInteger(message.diamondCount))
                    return "diamondCount: integer expected";
            if (message.diamondStamp != null && message.hasOwnProperty("diamondStamp"))
                if (!$util.isInteger(message.diamondStamp))
                    return "diamondStamp: integer expected";
            if (message.awardStamp != null && message.hasOwnProperty("awardStamp"))
                if (!$util.isInteger(message.awardStamp))
                    return "awardStamp: integer expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldp.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldp.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jldp.Plate)
                return object;
            let message = new $root.jldp.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jldp.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jldp.Plate.column: object expected");
                    message.column[i] = $root.jldp.Row.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jldp.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jldp.Plate.award: object expected");
                    message.award[i] = $root.jldp.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.isBonus != null)
                message.isBonus = Boolean(object.isBonus);
            if (object.diamondBonus != null)
                message.diamondBonus = Number(object.diamondBonus);
            if (object.diamondCount != null)
                message.diamondCount = object.diamondCount >>> 0;
            if (object.diamondStamp != null)
                message.diamondStamp = object.diamondStamp >>> 0;
            if (object.awardStamp != null)
                message.awardStamp = object.awardStamp >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldp.Plate
         * @static
         * @param {jldp.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.isBonus = false;
                object.diamondBonus = 0;
                object.diamondCount = 0;
                object.diamondStamp = 0;
                object.awardStamp = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jldp.Row.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jldp.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                object.isBonus = message.isBonus;
            if (message.diamondBonus != null && message.hasOwnProperty("diamondBonus"))
                object.diamondBonus = options.json && !isFinite(message.diamondBonus) ? String(message.diamondBonus) : message.diamondBonus;
            if (message.diamondCount != null && message.hasOwnProperty("diamondCount"))
                object.diamondCount = message.diamondCount;
            if (message.diamondStamp != null && message.hasOwnProperty("diamondStamp"))
                object.diamondStamp = message.diamondStamp;
            if (message.awardStamp != null && message.hasOwnProperty("awardStamp"))
                object.awardStamp = message.awardStamp;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jldp.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jldp.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldp.Plate";
        };

        return Plate;
    })();

    jldp.Row = (function() {

        /**
         * Properties of a Row.
         * @memberof jldp
         * @interface IRow
         * @property {Array.<number>|null} [row] Row row
         */

        /**
         * Constructs a new Row.
         * @memberof jldp
         * @classdesc Represents a Row.
         * @implements IRow
         * @constructor
         * @param {jldp.IRow=} [properties] Properties to set
         */
        function Row(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Row row.
         * @member {Array.<number>} row
         * @memberof jldp.Row
         * @instance
         */
        Row.prototype.row = $util.emptyArray;

        /**
         * Creates a new Row instance using the specified properties.
         * @function create
         * @memberof jldp.Row
         * @static
         * @param {jldp.IRow=} [properties] Properties to set
         * @returns {jldp.Row} Row instance
         */
        Row.create = function create(properties) {
            return new Row(properties);
        };

        /**
         * Encodes the specified Row message. Does not implicitly {@link jldp.Row.verify|verify} messages.
         * @function encode
         * @memberof jldp.Row
         * @static
         * @param {jldp.IRow} message Row message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Row.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Row message, length delimited. Does not implicitly {@link jldp.Row.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldp.Row
         * @static
         * @param {jldp.IRow} message Row message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Row.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Row message from the specified reader or buffer.
         * @function decode
         * @memberof jldp.Row
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldp.Row} Row
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Row.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldp.Row();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Row message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldp.Row
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldp.Row} Row
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Row.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Row message.
         * @function verify
         * @memberof jldp.Row
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Row.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Row message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldp.Row
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldp.Row} Row
         */
        Row.fromObject = function fromObject(object) {
            if (object instanceof $root.jldp.Row)
                return object;
            let message = new $root.jldp.Row();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jldp.Row.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Row message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldp.Row
         * @static
         * @param {jldp.Row} message Row
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Row.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            return object;
        };

        /**
         * Converts this Row to JSON.
         * @function toJSON
         * @memberof jldp.Row
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Row.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Row
         * @function getTypeUrl
         * @memberof jldp.Row
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Row.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldp.Row";
        };

        return Row;
    })();

    jldp.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jldp
         * @interface IAllPlate
         * @property {Array.<jldp.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jldp
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jldp.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jldp.IPlate>} plate
         * @memberof jldp.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jldp.AllPlate
         * @static
         * @param {jldp.IAllPlate=} [properties] Properties to set
         * @returns {jldp.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jldp.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jldp.AllPlate
         * @static
         * @param {jldp.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jldp.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jldp.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldp.AllPlate
         * @static
         * @param {jldp.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jldp.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldp.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldp.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jldp.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldp.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldp.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jldp.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jldp.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldp.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldp.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jldp.AllPlate)
                return object;
            let message = new $root.jldp.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jldp.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jldp.AllPlate.plate: object expected");
                    message.plate[i] = $root.jldp.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldp.AllPlate
         * @static
         * @param {jldp.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jldp.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jldp.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jldp.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldp.AllPlate";
        };

        return AllPlate;
    })();

    jldp.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jldp
         * @interface IColumnData
         * @property {Array.<number>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jldp
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jldp.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<number>} row
         * @memberof jldp.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jldp.ColumnData
         * @static
         * @param {jldp.IColumnData=} [properties] Properties to set
         * @returns {jldp.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jldp.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jldp.ColumnData
         * @static
         * @param {jldp.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jldp.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jldp.ColumnData
         * @static
         * @param {jldp.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jldp.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jldp.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jldp.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jldp.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jldp.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jldp.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jldp.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jldp.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jldp.ColumnData)
                return object;
            let message = new $root.jldp.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jldp.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jldp.ColumnData
         * @static
         * @param {jldp.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jldp.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jldp.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jldp.ColumnData";
        };

        return ColumnData;
    })();

    return jldp;
})();

export const jleb = $root.jleb = (() => {

    /**
     * Namespace jleb.
     * @exports jleb
     * @namespace
     */
    const jleb = {};

    jleb.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jleb
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jleb
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jleb.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jleb.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jleb.Column
         * @static
         * @param {jleb.IColumn=} [properties] Properties to set
         * @returns {jleb.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jleb.Column.verify|verify} messages.
         * @function encode
         * @memberof jleb.Column
         * @static
         * @param {jleb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jleb.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.Column
         * @static
         * @param {jleb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jleb.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.Column)
                return object;
            let message = new $root.jleb.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jleb.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.Column
         * @static
         * @param {jleb.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jleb.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jleb.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.Column";
        };

        return Column;
    })();

    jleb.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jleb
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jleb
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jleb.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jleb.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jleb.ColumnFloat
         * @static
         * @param {jleb.IColumnFloat=} [properties] Properties to set
         * @returns {jleb.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jleb.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jleb.ColumnFloat
         * @static
         * @param {jleb.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jleb.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.ColumnFloat
         * @static
         * @param {jleb.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jleb.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.ColumnFloat)
                return object;
            let message = new $root.jleb.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jleb.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.ColumnFloat
         * @static
         * @param {jleb.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jleb.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jleb.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jleb.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jleb
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jleb
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jleb.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jleb.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jleb.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jleb.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jleb.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jleb.AwardData
         * @static
         * @param {jleb.IAwardData=} [properties] Properties to set
         * @returns {jleb.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jleb.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jleb.AwardData
         * @static
         * @param {jleb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jleb.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.AwardData
         * @static
         * @param {jleb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jleb.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.AwardData)
                return object;
            let message = new $root.jleb.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.AwardData
         * @static
         * @param {jleb.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jleb.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jleb.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.AwardData";
        };

        return AwardData;
    })();

    jleb.CoinValue = (function() {

        /**
         * Properties of a CoinValue.
         * @memberof jleb
         * @interface ICoinValue
         * @property {number|null} [CoinState] CoinValue CoinState
         * @property {number|null} [Value] CoinValue Value
         * @property {number|null} [Position] CoinValue Position
         * @property {number|null} [Expand] CoinValue Expand
         */

        /**
         * Constructs a new CoinValue.
         * @memberof jleb
         * @classdesc Represents a CoinValue.
         * @implements ICoinValue
         * @constructor
         * @param {jleb.ICoinValue=} [properties] Properties to set
         */
        function CoinValue(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CoinValue CoinState.
         * @member {number} CoinState
         * @memberof jleb.CoinValue
         * @instance
         */
        CoinValue.prototype.CoinState = 0;

        /**
         * CoinValue Value.
         * @member {number} Value
         * @memberof jleb.CoinValue
         * @instance
         */
        CoinValue.prototype.Value = 0;

        /**
         * CoinValue Position.
         * @member {number} Position
         * @memberof jleb.CoinValue
         * @instance
         */
        CoinValue.prototype.Position = 0;

        /**
         * CoinValue Expand.
         * @member {number} Expand
         * @memberof jleb.CoinValue
         * @instance
         */
        CoinValue.prototype.Expand = 0;

        /**
         * Creates a new CoinValue instance using the specified properties.
         * @function create
         * @memberof jleb.CoinValue
         * @static
         * @param {jleb.ICoinValue=} [properties] Properties to set
         * @returns {jleb.CoinValue} CoinValue instance
         */
        CoinValue.create = function create(properties) {
            return new CoinValue(properties);
        };

        /**
         * Encodes the specified CoinValue message. Does not implicitly {@link jleb.CoinValue.verify|verify} messages.
         * @function encode
         * @memberof jleb.CoinValue
         * @static
         * @param {jleb.ICoinValue} message CoinValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoinValue.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CoinState != null && Object.hasOwnProperty.call(message, "CoinState"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.CoinState);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Value);
            if (message.Position != null && Object.hasOwnProperty.call(message, "Position"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Position);
            if (message.Expand != null && Object.hasOwnProperty.call(message, "Expand"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Expand);
            return writer;
        };

        /**
         * Encodes the specified CoinValue message, length delimited. Does not implicitly {@link jleb.CoinValue.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.CoinValue
         * @static
         * @param {jleb.ICoinValue} message CoinValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoinValue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CoinValue message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.CoinValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.CoinValue} CoinValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoinValue.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.CoinValue();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.CoinState = reader.int32();
                        break;
                    }
                case 2: {
                        message.Value = reader.double();
                        break;
                    }
                case 3: {
                        message.Position = reader.int32();
                        break;
                    }
                case 4: {
                        message.Expand = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CoinValue message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.CoinValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.CoinValue} CoinValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoinValue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CoinValue message.
         * @function verify
         * @memberof jleb.CoinValue
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CoinValue.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CoinState != null && message.hasOwnProperty("CoinState"))
                if (!$util.isInteger(message.CoinState))
                    return "CoinState: integer expected";
            if (message.Value != null && message.hasOwnProperty("Value"))
                if (typeof message.Value !== "number")
                    return "Value: number expected";
            if (message.Position != null && message.hasOwnProperty("Position"))
                if (!$util.isInteger(message.Position))
                    return "Position: integer expected";
            if (message.Expand != null && message.hasOwnProperty("Expand"))
                if (!$util.isInteger(message.Expand))
                    return "Expand: integer expected";
            return null;
        };

        /**
         * Creates a CoinValue message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.CoinValue
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.CoinValue} CoinValue
         */
        CoinValue.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.CoinValue)
                return object;
            let message = new $root.jleb.CoinValue();
            if (object.CoinState != null)
                message.CoinState = object.CoinState | 0;
            if (object.Value != null)
                message.Value = Number(object.Value);
            if (object.Position != null)
                message.Position = object.Position | 0;
            if (object.Expand != null)
                message.Expand = object.Expand | 0;
            return message;
        };

        /**
         * Creates a plain object from a CoinValue message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.CoinValue
         * @static
         * @param {jleb.CoinValue} message CoinValue
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CoinValue.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.CoinState = 0;
                object.Value = 0;
                object.Position = 0;
                object.Expand = 0;
            }
            if (message.CoinState != null && message.hasOwnProperty("CoinState"))
                object.CoinState = message.CoinState;
            if (message.Value != null && message.hasOwnProperty("Value"))
                object.Value = options.json && !isFinite(message.Value) ? String(message.Value) : message.Value;
            if (message.Position != null && message.hasOwnProperty("Position"))
                object.Position = message.Position;
            if (message.Expand != null && message.hasOwnProperty("Expand"))
                object.Expand = message.Expand;
            return object;
        };

        /**
         * Converts this CoinValue to JSON.
         * @function toJSON
         * @memberof jleb.CoinValue
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CoinValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CoinValue
         * @function getTypeUrl
         * @memberof jleb.CoinValue
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CoinValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.CoinValue";
        };

        return CoinValue;
    })();

    jleb.BonusAward = (function() {

        /**
         * Properties of a BonusAward.
         * @memberof jleb
         * @interface IBonusAward
         * @property {number|null} [JPIndex] BonusAward JPIndex
         * @property {number|null} [Value] BonusAward Value
         */

        /**
         * Constructs a new BonusAward.
         * @memberof jleb
         * @classdesc Represents a BonusAward.
         * @implements IBonusAward
         * @constructor
         * @param {jleb.IBonusAward=} [properties] Properties to set
         */
        function BonusAward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusAward JPIndex.
         * @member {number} JPIndex
         * @memberof jleb.BonusAward
         * @instance
         */
        BonusAward.prototype.JPIndex = 0;

        /**
         * BonusAward Value.
         * @member {number} Value
         * @memberof jleb.BonusAward
         * @instance
         */
        BonusAward.prototype.Value = 0;

        /**
         * Creates a new BonusAward instance using the specified properties.
         * @function create
         * @memberof jleb.BonusAward
         * @static
         * @param {jleb.IBonusAward=} [properties] Properties to set
         * @returns {jleb.BonusAward} BonusAward instance
         */
        BonusAward.create = function create(properties) {
            return new BonusAward(properties);
        };

        /**
         * Encodes the specified BonusAward message. Does not implicitly {@link jleb.BonusAward.verify|verify} messages.
         * @function encode
         * @memberof jleb.BonusAward
         * @static
         * @param {jleb.IBonusAward} message BonusAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.JPIndex != null && Object.hasOwnProperty.call(message, "JPIndex"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.JPIndex);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Value);
            return writer;
        };

        /**
         * Encodes the specified BonusAward message, length delimited. Does not implicitly {@link jleb.BonusAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.BonusAward
         * @static
         * @param {jleb.IBonusAward} message BonusAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusAward message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.BonusAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.BonusAward} BonusAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.BonusAward();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.JPIndex = reader.double();
                        break;
                    }
                case 2: {
                        message.Value = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.BonusAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.BonusAward} BonusAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusAward message.
         * @function verify
         * @memberof jleb.BonusAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.JPIndex != null && message.hasOwnProperty("JPIndex"))
                if (typeof message.JPIndex !== "number")
                    return "JPIndex: number expected";
            if (message.Value != null && message.hasOwnProperty("Value"))
                if (typeof message.Value !== "number")
                    return "Value: number expected";
            return null;
        };

        /**
         * Creates a BonusAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.BonusAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.BonusAward} BonusAward
         */
        BonusAward.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.BonusAward)
                return object;
            let message = new $root.jleb.BonusAward();
            if (object.JPIndex != null)
                message.JPIndex = Number(object.JPIndex);
            if (object.Value != null)
                message.Value = Number(object.Value);
            return message;
        };

        /**
         * Creates a plain object from a BonusAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.BonusAward
         * @static
         * @param {jleb.BonusAward} message BonusAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.JPIndex = 0;
                object.Value = 0;
            }
            if (message.JPIndex != null && message.hasOwnProperty("JPIndex"))
                object.JPIndex = options.json && !isFinite(message.JPIndex) ? String(message.JPIndex) : message.JPIndex;
            if (message.Value != null && message.hasOwnProperty("Value"))
                object.Value = options.json && !isFinite(message.Value) ? String(message.Value) : message.Value;
            return object;
        };

        /**
         * Converts this BonusAward to JSON.
         * @function toJSON
         * @memberof jleb.BonusAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusAward
         * @function getTypeUrl
         * @memberof jleb.BonusAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.BonusAward";
        };

        return BonusAward;
    })();

    jleb.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jleb
         * @interface IRoundInfo
         * @property {Array.<jleb.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jleb.ICoinValue>|null} [CoinChange] RoundInfo CoinChange
         * @property {Array.<jleb.ICoinValue>|null} [FreeCoin] RoundInfo FreeCoin
         * @property {Array.<jleb.ICoinValue>|null} [BingoInit] RoundInfo BingoInit
         * @property {Array.<jleb.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {Array.<jleb.IBonusAward>|null} [Bonus] RoundInfo Bonus
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [WildMult] RoundInfo WildMult
         * @property {number|null} [BingoWin] RoundInfo BingoWin
         * @property {number|null} [ScatterWin] RoundInfo ScatterWin
         * @property {number|null} [LineWin] RoundInfo LineWin
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [JPWin] RoundInfo JPWin
         * @property {Array.<jleb.IBingoGridColumn>|null} [BingoPlate] RoundInfo BingoPlate
         * @property {Array.<jleb.IBingoGridColumn>|null} [BingoPlateMax] RoundInfo BingoPlateMax
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jleb
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jleb.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.CoinChange = [];
            this.FreeCoin = [];
            this.BingoInit = [];
            this.AwardDataVec = [];
            this.Bonus = [];
            this.BingoPlate = [];
            this.BingoPlateMax = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jleb.IColumn>} PlateSymbol
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo CoinChange.
         * @member {Array.<jleb.ICoinValue>} CoinChange
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CoinChange = $util.emptyArray;

        /**
         * RoundInfo FreeCoin.
         * @member {Array.<jleb.ICoinValue>} FreeCoin
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeCoin = $util.emptyArray;

        /**
         * RoundInfo BingoInit.
         * @member {Array.<jleb.ICoinValue>} BingoInit
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BingoInit = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jleb.IAwardData>} AwardDataVec
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo Bonus.
         * @member {Array.<jleb.IBonusAward>} Bonus
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Bonus = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo WildMult.
         * @member {number} WildMult
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildMult = 0;

        /**
         * RoundInfo BingoWin.
         * @member {number} BingoWin
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BingoWin = 0;

        /**
         * RoundInfo ScatterWin.
         * @member {number} ScatterWin
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterWin = 0;

        /**
         * RoundInfo LineWin.
         * @member {number} LineWin
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LineWin = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo JPWin.
         * @member {number} JPWin
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.JPWin = 0;

        /**
         * RoundInfo BingoPlate.
         * @member {Array.<jleb.IBingoGridColumn>} BingoPlate
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BingoPlate = $util.emptyArray;

        /**
         * RoundInfo BingoPlateMax.
         * @member {Array.<jleb.IBingoGridColumn>} BingoPlateMax
         * @memberof jleb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BingoPlateMax = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jleb.RoundInfo
         * @static
         * @param {jleb.IRoundInfo=} [properties] Properties to set
         * @returns {jleb.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jleb.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jleb.RoundInfo
         * @static
         * @param {jleb.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jleb.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.CoinChange != null && message.CoinChange.length)
                for (let i = 0; i < message.CoinChange.length; ++i)
                    $root.jleb.CoinValue.encode(message.CoinChange[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreeCoin != null && message.FreeCoin.length)
                for (let i = 0; i < message.FreeCoin.length; ++i)
                    $root.jleb.CoinValue.encode(message.FreeCoin[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.BingoInit != null && message.BingoInit.length)
                for (let i = 0; i < message.BingoInit.length; ++i)
                    $root.jleb.CoinValue.encode(message.BingoInit[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jleb.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.Bonus != null && message.Bonus.length)
                for (let i = 0; i < message.Bonus.length; ++i)
                    $root.jleb.BonusAward.encode(message.Bonus[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeRemainRound);
            if (message.WildMult != null && Object.hasOwnProperty.call(message, "WildMult"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.WildMult);
            if (message.BingoWin != null && Object.hasOwnProperty.call(message, "BingoWin"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.BingoWin);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.ScatterWin);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.LineWin);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.RoundWin);
            if (message.JPWin != null && Object.hasOwnProperty.call(message, "JPWin"))
                writer.uint32(/* id 14, wireType 1 =*/113).double(message.JPWin);
            if (message.BingoPlate != null && message.BingoPlate.length)
                for (let i = 0; i < message.BingoPlate.length; ++i)
                    $root.jleb.BingoGridColumn.encode(message.BingoPlate[i], writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.BingoPlateMax != null && message.BingoPlateMax.length)
                for (let i = 0; i < message.BingoPlateMax.length; ++i)
                    $root.jleb.BingoGridColumn.encode(message.BingoPlateMax[i], writer.uint32(/* id 16, wireType 2 =*/130).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jleb.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.RoundInfo
         * @static
         * @param {jleb.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jleb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.CoinChange && message.CoinChange.length))
                            message.CoinChange = [];
                        message.CoinChange.push($root.jleb.CoinValue.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FreeCoin && message.FreeCoin.length))
                            message.FreeCoin = [];
                        message.FreeCoin.push($root.jleb.CoinValue.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.BingoInit && message.BingoInit.length))
                            message.BingoInit = [];
                        message.BingoInit.push($root.jleb.CoinValue.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jleb.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.Bonus && message.Bonus.length))
                            message.Bonus = [];
                        message.Bonus.push($root.jleb.BonusAward.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.WildMult = reader.double();
                        break;
                    }
                case 10: {
                        message.BingoWin = reader.double();
                        break;
                    }
                case 11: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 12: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 13: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 14: {
                        message.JPWin = reader.double();
                        break;
                    }
                case 15: {
                        if (!(message.BingoPlate && message.BingoPlate.length))
                            message.BingoPlate = [];
                        message.BingoPlate.push($root.jleb.BingoGridColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 16: {
                        if (!(message.BingoPlateMax && message.BingoPlateMax.length))
                            message.BingoPlateMax = [];
                        message.BingoPlateMax.push($root.jleb.BingoGridColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jleb.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jleb.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.CoinChange != null && message.hasOwnProperty("CoinChange")) {
                if (!Array.isArray(message.CoinChange))
                    return "CoinChange: array expected";
                for (let i = 0; i < message.CoinChange.length; ++i) {
                    let error = $root.jleb.CoinValue.verify(message.CoinChange[i]);
                    if (error)
                        return "CoinChange." + error;
                }
            }
            if (message.FreeCoin != null && message.hasOwnProperty("FreeCoin")) {
                if (!Array.isArray(message.FreeCoin))
                    return "FreeCoin: array expected";
                for (let i = 0; i < message.FreeCoin.length; ++i) {
                    let error = $root.jleb.CoinValue.verify(message.FreeCoin[i]);
                    if (error)
                        return "FreeCoin." + error;
                }
            }
            if (message.BingoInit != null && message.hasOwnProperty("BingoInit")) {
                if (!Array.isArray(message.BingoInit))
                    return "BingoInit: array expected";
                for (let i = 0; i < message.BingoInit.length; ++i) {
                    let error = $root.jleb.CoinValue.verify(message.BingoInit[i]);
                    if (error)
                        return "BingoInit." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jleb.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.Bonus != null && message.hasOwnProperty("Bonus")) {
                if (!Array.isArray(message.Bonus))
                    return "Bonus: array expected";
                for (let i = 0; i < message.Bonus.length; ++i) {
                    let error = $root.jleb.BonusAward.verify(message.Bonus[i]);
                    if (error)
                        return "Bonus." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.WildMult != null && message.hasOwnProperty("WildMult"))
                if (typeof message.WildMult !== "number")
                    return "WildMult: number expected";
            if (message.BingoWin != null && message.hasOwnProperty("BingoWin"))
                if (typeof message.BingoWin !== "number")
                    return "BingoWin: number expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                if (typeof message.JPWin !== "number")
                    return "JPWin: number expected";
            if (message.BingoPlate != null && message.hasOwnProperty("BingoPlate")) {
                if (!Array.isArray(message.BingoPlate))
                    return "BingoPlate: array expected";
                for (let i = 0; i < message.BingoPlate.length; ++i) {
                    let error = $root.jleb.BingoGridColumn.verify(message.BingoPlate[i]);
                    if (error)
                        return "BingoPlate." + error;
                }
            }
            if (message.BingoPlateMax != null && message.hasOwnProperty("BingoPlateMax")) {
                if (!Array.isArray(message.BingoPlateMax))
                    return "BingoPlateMax: array expected";
                for (let i = 0; i < message.BingoPlateMax.length; ++i) {
                    let error = $root.jleb.BingoGridColumn.verify(message.BingoPlateMax[i]);
                    if (error)
                        return "BingoPlateMax." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.RoundInfo)
                return object;
            let message = new $root.jleb.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jleb.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jleb.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.CoinChange) {
                if (!Array.isArray(object.CoinChange))
                    throw TypeError(".jleb.RoundInfo.CoinChange: array expected");
                message.CoinChange = [];
                for (let i = 0; i < object.CoinChange.length; ++i) {
                    if (typeof object.CoinChange[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.CoinChange: object expected");
                    message.CoinChange[i] = $root.jleb.CoinValue.fromObject(object.CoinChange[i]);
                }
            }
            if (object.FreeCoin) {
                if (!Array.isArray(object.FreeCoin))
                    throw TypeError(".jleb.RoundInfo.FreeCoin: array expected");
                message.FreeCoin = [];
                for (let i = 0; i < object.FreeCoin.length; ++i) {
                    if (typeof object.FreeCoin[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.FreeCoin: object expected");
                    message.FreeCoin[i] = $root.jleb.CoinValue.fromObject(object.FreeCoin[i]);
                }
            }
            if (object.BingoInit) {
                if (!Array.isArray(object.BingoInit))
                    throw TypeError(".jleb.RoundInfo.BingoInit: array expected");
                message.BingoInit = [];
                for (let i = 0; i < object.BingoInit.length; ++i) {
                    if (typeof object.BingoInit[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.BingoInit: object expected");
                    message.BingoInit[i] = $root.jleb.CoinValue.fromObject(object.BingoInit[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jleb.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jleb.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.Bonus) {
                if (!Array.isArray(object.Bonus))
                    throw TypeError(".jleb.RoundInfo.Bonus: array expected");
                message.Bonus = [];
                for (let i = 0; i < object.Bonus.length; ++i) {
                    if (typeof object.Bonus[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.Bonus: object expected");
                    message.Bonus[i] = $root.jleb.BonusAward.fromObject(object.Bonus[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.WildMult != null)
                message.WildMult = Number(object.WildMult);
            if (object.BingoWin != null)
                message.BingoWin = Number(object.BingoWin);
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.JPWin != null)
                message.JPWin = Number(object.JPWin);
            if (object.BingoPlate) {
                if (!Array.isArray(object.BingoPlate))
                    throw TypeError(".jleb.RoundInfo.BingoPlate: array expected");
                message.BingoPlate = [];
                for (let i = 0; i < object.BingoPlate.length; ++i) {
                    if (typeof object.BingoPlate[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.BingoPlate: object expected");
                    message.BingoPlate[i] = $root.jleb.BingoGridColumn.fromObject(object.BingoPlate[i]);
                }
            }
            if (object.BingoPlateMax) {
                if (!Array.isArray(object.BingoPlateMax))
                    throw TypeError(".jleb.RoundInfo.BingoPlateMax: array expected");
                message.BingoPlateMax = [];
                for (let i = 0; i < object.BingoPlateMax.length; ++i) {
                    if (typeof object.BingoPlateMax[i] !== "object")
                        throw TypeError(".jleb.RoundInfo.BingoPlateMax: object expected");
                    message.BingoPlateMax[i] = $root.jleb.BingoGridColumn.fromObject(object.BingoPlateMax[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.RoundInfo
         * @static
         * @param {jleb.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.CoinChange = [];
                object.FreeCoin = [];
                object.BingoInit = [];
                object.AwardDataVec = [];
                object.Bonus = [];
                object.BingoPlate = [];
                object.BingoPlateMax = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.WildMult = 0;
                object.BingoWin = 0;
                object.ScatterWin = 0;
                object.LineWin = 0;
                object.RoundWin = 0;
                object.JPWin = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jleb.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.CoinChange && message.CoinChange.length) {
                object.CoinChange = [];
                for (let j = 0; j < message.CoinChange.length; ++j)
                    object.CoinChange[j] = $root.jleb.CoinValue.toObject(message.CoinChange[j], options);
            }
            if (message.FreeCoin && message.FreeCoin.length) {
                object.FreeCoin = [];
                for (let j = 0; j < message.FreeCoin.length; ++j)
                    object.FreeCoin[j] = $root.jleb.CoinValue.toObject(message.FreeCoin[j], options);
            }
            if (message.BingoInit && message.BingoInit.length) {
                object.BingoInit = [];
                for (let j = 0; j < message.BingoInit.length; ++j)
                    object.BingoInit[j] = $root.jleb.CoinValue.toObject(message.BingoInit[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jleb.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.Bonus && message.Bonus.length) {
                object.Bonus = [];
                for (let j = 0; j < message.Bonus.length; ++j)
                    object.Bonus[j] = $root.jleb.BonusAward.toObject(message.Bonus[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.WildMult != null && message.hasOwnProperty("WildMult"))
                object.WildMult = options.json && !isFinite(message.WildMult) ? String(message.WildMult) : message.WildMult;
            if (message.BingoWin != null && message.hasOwnProperty("BingoWin"))
                object.BingoWin = options.json && !isFinite(message.BingoWin) ? String(message.BingoWin) : message.BingoWin;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                object.JPWin = options.json && !isFinite(message.JPWin) ? String(message.JPWin) : message.JPWin;
            if (message.BingoPlate && message.BingoPlate.length) {
                object.BingoPlate = [];
                for (let j = 0; j < message.BingoPlate.length; ++j)
                    object.BingoPlate[j] = $root.jleb.BingoGridColumn.toObject(message.BingoPlate[j], options);
            }
            if (message.BingoPlateMax && message.BingoPlateMax.length) {
                object.BingoPlateMax = [];
                for (let j = 0; j < message.BingoPlateMax.length; ++j)
                    object.BingoPlateMax[j] = $root.jleb.BingoGridColumn.toObject(message.BingoPlateMax[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jleb.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jleb.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.RoundInfo";
        };

        return RoundInfo;
    })();

    jleb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jleb
         * @interface ISpinAck
         * @property {Array.<jleb.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [FeatureBet] SpinAck FeatureBet
         * @property {number|null} [FeaturePos] SpinAck FeaturePos
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {Array.<jleb.IBingoSet>|null} [BingoSetVec] SpinAck BingoSetVec
         * @property {Array.<jleb.IBingoSetClient>|null} [LastInfo] SpinAck LastInfo
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jleb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jleb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.BingoSetVec = [];
            this.LastInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jleb.IRoundInfo>} RoundQueue
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck FeatureBet.
         * @member {number} FeatureBet
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.FeatureBet = 0;

        /**
         * SpinAck FeaturePos.
         * @member {number} FeaturePos
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.FeaturePos = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck BingoSetVec.
         * @member {Array.<jleb.IBingoSet>} BingoSetVec
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.BingoSetVec = $util.emptyArray;

        /**
         * SpinAck LastInfo.
         * @member {Array.<jleb.IBingoSetClient>} LastInfo
         * @memberof jleb.SpinAck
         * @instance
         */
        SpinAck.prototype.LastInfo = $util.emptyArray;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jleb.SpinAck
         * @static
         * @param {jleb.ISpinAck=} [properties] Properties to set
         * @returns {jleb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jleb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jleb.SpinAck
         * @static
         * @param {jleb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jleb.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.FeatureBet != null && Object.hasOwnProperty.call(message, "FeatureBet"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.FeatureBet);
            if (message.FeaturePos != null && Object.hasOwnProperty.call(message, "FeaturePos"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FeaturePos);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.BingoSetVec != null && message.BingoSetVec.length)
                for (let i = 0; i < message.BingoSetVec.length; ++i)
                    $root.jleb.BingoSet.encode(message.BingoSetVec[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.LastInfo != null && message.LastInfo.length)
                for (let i = 0; i < message.LastInfo.length; ++i)
                    $root.jleb.BingoSetClient.encode(message.LastInfo[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jleb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.SpinAck
         * @static
         * @param {jleb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jleb.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.FeatureBet = reader.double();
                        break;
                    }
                case 5: {
                        message.FeaturePos = reader.int32();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        if (!(message.BingoSetVec && message.BingoSetVec.length))
                            message.BingoSetVec = [];
                        message.BingoSetVec.push($root.jleb.BingoSet.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        if (!(message.LastInfo && message.LastInfo.length))
                            message.LastInfo = [];
                        message.LastInfo.push($root.jleb.BingoSetClient.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jleb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jleb.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.FeatureBet != null && message.hasOwnProperty("FeatureBet"))
                if (typeof message.FeatureBet !== "number")
                    return "FeatureBet: number expected";
            if (message.FeaturePos != null && message.hasOwnProperty("FeaturePos"))
                if (!$util.isInteger(message.FeaturePos))
                    return "FeaturePos: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.BingoSetVec != null && message.hasOwnProperty("BingoSetVec")) {
                if (!Array.isArray(message.BingoSetVec))
                    return "BingoSetVec: array expected";
                for (let i = 0; i < message.BingoSetVec.length; ++i) {
                    let error = $root.jleb.BingoSet.verify(message.BingoSetVec[i]);
                    if (error)
                        return "BingoSetVec." + error;
                }
            }
            if (message.LastInfo != null && message.hasOwnProperty("LastInfo")) {
                if (!Array.isArray(message.LastInfo))
                    return "LastInfo: array expected";
                for (let i = 0; i < message.LastInfo.length; ++i) {
                    let error = $root.jleb.BingoSetClient.verify(message.LastInfo[i]);
                    if (error)
                        return "LastInfo." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.SpinAck)
                return object;
            let message = new $root.jleb.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jleb.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jleb.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jleb.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.FeatureBet != null)
                message.FeatureBet = Number(object.FeatureBet);
            if (object.FeaturePos != null)
                message.FeaturePos = object.FeaturePos | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.BingoSetVec) {
                if (!Array.isArray(object.BingoSetVec))
                    throw TypeError(".jleb.SpinAck.BingoSetVec: array expected");
                message.BingoSetVec = [];
                for (let i = 0; i < object.BingoSetVec.length; ++i) {
                    if (typeof object.BingoSetVec[i] !== "object")
                        throw TypeError(".jleb.SpinAck.BingoSetVec: object expected");
                    message.BingoSetVec[i] = $root.jleb.BingoSet.fromObject(object.BingoSetVec[i]);
                }
            }
            if (object.LastInfo) {
                if (!Array.isArray(object.LastInfo))
                    throw TypeError(".jleb.SpinAck.LastInfo: array expected");
                message.LastInfo = [];
                for (let i = 0; i < object.LastInfo.length; ++i) {
                    if (typeof object.LastInfo[i] !== "object")
                        throw TypeError(".jleb.SpinAck.LastInfo: object expected");
                    message.LastInfo[i] = $root.jleb.BingoSetClient.fromObject(object.LastInfo[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.SpinAck
         * @static
         * @param {jleb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.BingoSetVec = [];
                object.LastInfo = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.FeatureBet = 0;
                object.FeaturePos = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jleb.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.FeatureBet != null && message.hasOwnProperty("FeatureBet"))
                object.FeatureBet = options.json && !isFinite(message.FeatureBet) ? String(message.FeatureBet) : message.FeatureBet;
            if (message.FeaturePos != null && message.hasOwnProperty("FeaturePos"))
                object.FeaturePos = message.FeaturePos;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.BingoSetVec && message.BingoSetVec.length) {
                object.BingoSetVec = [];
                for (let j = 0; j < message.BingoSetVec.length; ++j)
                    object.BingoSetVec[j] = $root.jleb.BingoSet.toObject(message.BingoSetVec[j], options);
            }
            if (message.LastInfo && message.LastInfo.length) {
                object.LastInfo = [];
                for (let j = 0; j < message.LastInfo.length; ++j)
                    object.LastInfo[j] = $root.jleb.BingoSetClient.toObject(message.LastInfo[j], options);
            }
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jleb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jleb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.SpinAck";
        };

        return SpinAck;
    })();

    jleb.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jleb
         * @interface IStateInfo
         * @property {Array.<jleb.IBingoSet>|null} [BingoSetVec] StateInfo BingoSetVec
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jleb
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jleb.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.BingoSetVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo BingoSetVec.
         * @member {Array.<jleb.IBingoSet>} BingoSetVec
         * @memberof jleb.StateInfo
         * @instance
         */
        StateInfo.prototype.BingoSetVec = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jleb.StateInfo
         * @static
         * @param {jleb.IStateInfo=} [properties] Properties to set
         * @returns {jleb.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jleb.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jleb.StateInfo
         * @static
         * @param {jleb.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BingoSetVec != null && message.BingoSetVec.length)
                for (let i = 0; i < message.BingoSetVec.length; ++i)
                    $root.jleb.BingoSet.encode(message.BingoSetVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jleb.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.StateInfo
         * @static
         * @param {jleb.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BingoSetVec && message.BingoSetVec.length))
                            message.BingoSetVec = [];
                        message.BingoSetVec.push($root.jleb.BingoSet.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jleb.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BingoSetVec != null && message.hasOwnProperty("BingoSetVec")) {
                if (!Array.isArray(message.BingoSetVec))
                    return "BingoSetVec: array expected";
                for (let i = 0; i < message.BingoSetVec.length; ++i) {
                    let error = $root.jleb.BingoSet.verify(message.BingoSetVec[i]);
                    if (error)
                        return "BingoSetVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.StateInfo)
                return object;
            let message = new $root.jleb.StateInfo();
            if (object.BingoSetVec) {
                if (!Array.isArray(object.BingoSetVec))
                    throw TypeError(".jleb.StateInfo.BingoSetVec: array expected");
                message.BingoSetVec = [];
                for (let i = 0; i < object.BingoSetVec.length; ++i) {
                    if (typeof object.BingoSetVec[i] !== "object")
                        throw TypeError(".jleb.StateInfo.BingoSetVec: object expected");
                    message.BingoSetVec[i] = $root.jleb.BingoSet.fromObject(object.BingoSetVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.StateInfo
         * @static
         * @param {jleb.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BingoSetVec = [];
            if (message.BingoSetVec && message.BingoSetVec.length) {
                object.BingoSetVec = [];
                for (let j = 0; j < message.BingoSetVec.length; ++j)
                    object.BingoSetVec[j] = $root.jleb.BingoSet.toObject(message.BingoSetVec[j], options);
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jleb.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jleb.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.StateInfo";
        };

        return StateInfo;
    })();

    jleb.BingoSet = (function() {

        /**
         * Properties of a BingoSet.
         * @memberof jleb
         * @interface IBingoSet
         * @property {number|null} [Bet] BingoSet Bet
         * @property {boolean|null} [IsNeedSave] BingoSet IsNeedSave
         * @property {Array.<jleb.IBingoGridColumn>|null} [BingoPlate] BingoSet BingoPlate
         * @property {Array.<jleb.IBingoGridColumn>|null} [BingoPlateMax] BingoSet BingoPlateMax
         */

        /**
         * Constructs a new BingoSet.
         * @memberof jleb
         * @classdesc Represents a BingoSet.
         * @implements IBingoSet
         * @constructor
         * @param {jleb.IBingoSet=} [properties] Properties to set
         */
        function BingoSet(properties) {
            this.BingoPlate = [];
            this.BingoPlateMax = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BingoSet Bet.
         * @member {number} Bet
         * @memberof jleb.BingoSet
         * @instance
         */
        BingoSet.prototype.Bet = 0;

        /**
         * BingoSet IsNeedSave.
         * @member {boolean} IsNeedSave
         * @memberof jleb.BingoSet
         * @instance
         */
        BingoSet.prototype.IsNeedSave = false;

        /**
         * BingoSet BingoPlate.
         * @member {Array.<jleb.IBingoGridColumn>} BingoPlate
         * @memberof jleb.BingoSet
         * @instance
         */
        BingoSet.prototype.BingoPlate = $util.emptyArray;

        /**
         * BingoSet BingoPlateMax.
         * @member {Array.<jleb.IBingoGridColumn>} BingoPlateMax
         * @memberof jleb.BingoSet
         * @instance
         */
        BingoSet.prototype.BingoPlateMax = $util.emptyArray;

        /**
         * Creates a new BingoSet instance using the specified properties.
         * @function create
         * @memberof jleb.BingoSet
         * @static
         * @param {jleb.IBingoSet=} [properties] Properties to set
         * @returns {jleb.BingoSet} BingoSet instance
         */
        BingoSet.create = function create(properties) {
            return new BingoSet(properties);
        };

        /**
         * Encodes the specified BingoSet message. Does not implicitly {@link jleb.BingoSet.verify|verify} messages.
         * @function encode
         * @memberof jleb.BingoSet
         * @static
         * @param {jleb.IBingoSet} message BingoSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoSet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Bet);
            if (message.IsNeedSave != null && Object.hasOwnProperty.call(message, "IsNeedSave"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.IsNeedSave);
            if (message.BingoPlate != null && message.BingoPlate.length)
                for (let i = 0; i < message.BingoPlate.length; ++i)
                    $root.jleb.BingoGridColumn.encode(message.BingoPlate[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.BingoPlateMax != null && message.BingoPlateMax.length)
                for (let i = 0; i < message.BingoPlateMax.length; ++i)
                    $root.jleb.BingoGridColumn.encode(message.BingoPlateMax[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BingoSet message, length delimited. Does not implicitly {@link jleb.BingoSet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.BingoSet
         * @static
         * @param {jleb.IBingoSet} message BingoSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoSet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BingoSet message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.BingoSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.BingoSet} BingoSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoSet.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.BingoSet();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Bet = reader.double();
                        break;
                    }
                case 2: {
                        message.IsNeedSave = reader.bool();
                        break;
                    }
                case 3: {
                        if (!(message.BingoPlate && message.BingoPlate.length))
                            message.BingoPlate = [];
                        message.BingoPlate.push($root.jleb.BingoGridColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.BingoPlateMax && message.BingoPlateMax.length))
                            message.BingoPlateMax = [];
                        message.BingoPlateMax.push($root.jleb.BingoGridColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BingoSet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.BingoSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.BingoSet} BingoSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoSet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BingoSet message.
         * @function verify
         * @memberof jleb.BingoSet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BingoSet.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.IsNeedSave != null && message.hasOwnProperty("IsNeedSave"))
                if (typeof message.IsNeedSave !== "boolean")
                    return "IsNeedSave: boolean expected";
            if (message.BingoPlate != null && message.hasOwnProperty("BingoPlate")) {
                if (!Array.isArray(message.BingoPlate))
                    return "BingoPlate: array expected";
                for (let i = 0; i < message.BingoPlate.length; ++i) {
                    let error = $root.jleb.BingoGridColumn.verify(message.BingoPlate[i]);
                    if (error)
                        return "BingoPlate." + error;
                }
            }
            if (message.BingoPlateMax != null && message.hasOwnProperty("BingoPlateMax")) {
                if (!Array.isArray(message.BingoPlateMax))
                    return "BingoPlateMax: array expected";
                for (let i = 0; i < message.BingoPlateMax.length; ++i) {
                    let error = $root.jleb.BingoGridColumn.verify(message.BingoPlateMax[i]);
                    if (error)
                        return "BingoPlateMax." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BingoSet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.BingoSet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.BingoSet} BingoSet
         */
        BingoSet.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.BingoSet)
                return object;
            let message = new $root.jleb.BingoSet();
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.IsNeedSave != null)
                message.IsNeedSave = Boolean(object.IsNeedSave);
            if (object.BingoPlate) {
                if (!Array.isArray(object.BingoPlate))
                    throw TypeError(".jleb.BingoSet.BingoPlate: array expected");
                message.BingoPlate = [];
                for (let i = 0; i < object.BingoPlate.length; ++i) {
                    if (typeof object.BingoPlate[i] !== "object")
                        throw TypeError(".jleb.BingoSet.BingoPlate: object expected");
                    message.BingoPlate[i] = $root.jleb.BingoGridColumn.fromObject(object.BingoPlate[i]);
                }
            }
            if (object.BingoPlateMax) {
                if (!Array.isArray(object.BingoPlateMax))
                    throw TypeError(".jleb.BingoSet.BingoPlateMax: array expected");
                message.BingoPlateMax = [];
                for (let i = 0; i < object.BingoPlateMax.length; ++i) {
                    if (typeof object.BingoPlateMax[i] !== "object")
                        throw TypeError(".jleb.BingoSet.BingoPlateMax: object expected");
                    message.BingoPlateMax[i] = $root.jleb.BingoGridColumn.fromObject(object.BingoPlateMax[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BingoSet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.BingoSet
         * @static
         * @param {jleb.BingoSet} message BingoSet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BingoSet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.BingoPlate = [];
                object.BingoPlateMax = [];
            }
            if (options.defaults) {
                object.Bet = 0;
                object.IsNeedSave = false;
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.IsNeedSave != null && message.hasOwnProperty("IsNeedSave"))
                object.IsNeedSave = message.IsNeedSave;
            if (message.BingoPlate && message.BingoPlate.length) {
                object.BingoPlate = [];
                for (let j = 0; j < message.BingoPlate.length; ++j)
                    object.BingoPlate[j] = $root.jleb.BingoGridColumn.toObject(message.BingoPlate[j], options);
            }
            if (message.BingoPlateMax && message.BingoPlateMax.length) {
                object.BingoPlateMax = [];
                for (let j = 0; j < message.BingoPlateMax.length; ++j)
                    object.BingoPlateMax[j] = $root.jleb.BingoGridColumn.toObject(message.BingoPlateMax[j], options);
            }
            return object;
        };

        /**
         * Converts this BingoSet to JSON.
         * @function toJSON
         * @memberof jleb.BingoSet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BingoSet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BingoSet
         * @function getTypeUrl
         * @memberof jleb.BingoSet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BingoSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.BingoSet";
        };

        return BingoSet;
    })();

    jleb.BingoGridColumn = (function() {

        /**
         * Properties of a BingoGridColumn.
         * @memberof jleb
         * @interface IBingoGridColumn
         * @property {Array.<jleb.IBingoGrid>|null} [BGC] BingoGridColumn BGC
         */

        /**
         * Constructs a new BingoGridColumn.
         * @memberof jleb
         * @classdesc Represents a BingoGridColumn.
         * @implements IBingoGridColumn
         * @constructor
         * @param {jleb.IBingoGridColumn=} [properties] Properties to set
         */
        function BingoGridColumn(properties) {
            this.BGC = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BingoGridColumn BGC.
         * @member {Array.<jleb.IBingoGrid>} BGC
         * @memberof jleb.BingoGridColumn
         * @instance
         */
        BingoGridColumn.prototype.BGC = $util.emptyArray;

        /**
         * Creates a new BingoGridColumn instance using the specified properties.
         * @function create
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {jleb.IBingoGridColumn=} [properties] Properties to set
         * @returns {jleb.BingoGridColumn} BingoGridColumn instance
         */
        BingoGridColumn.create = function create(properties) {
            return new BingoGridColumn(properties);
        };

        /**
         * Encodes the specified BingoGridColumn message. Does not implicitly {@link jleb.BingoGridColumn.verify|verify} messages.
         * @function encode
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {jleb.IBingoGridColumn} message BingoGridColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoGridColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BGC != null && message.BGC.length)
                for (let i = 0; i < message.BGC.length; ++i)
                    $root.jleb.BingoGrid.encode(message.BGC[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BingoGridColumn message, length delimited. Does not implicitly {@link jleb.BingoGridColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {jleb.IBingoGridColumn} message BingoGridColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoGridColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BingoGridColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.BingoGridColumn} BingoGridColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoGridColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.BingoGridColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BGC && message.BGC.length))
                            message.BGC = [];
                        message.BGC.push($root.jleb.BingoGrid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BingoGridColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.BingoGridColumn} BingoGridColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoGridColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BingoGridColumn message.
         * @function verify
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BingoGridColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BGC != null && message.hasOwnProperty("BGC")) {
                if (!Array.isArray(message.BGC))
                    return "BGC: array expected";
                for (let i = 0; i < message.BGC.length; ++i) {
                    let error = $root.jleb.BingoGrid.verify(message.BGC[i]);
                    if (error)
                        return "BGC." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BingoGridColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.BingoGridColumn} BingoGridColumn
         */
        BingoGridColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.BingoGridColumn)
                return object;
            let message = new $root.jleb.BingoGridColumn();
            if (object.BGC) {
                if (!Array.isArray(object.BGC))
                    throw TypeError(".jleb.BingoGridColumn.BGC: array expected");
                message.BGC = [];
                for (let i = 0; i < object.BGC.length; ++i) {
                    if (typeof object.BGC[i] !== "object")
                        throw TypeError(".jleb.BingoGridColumn.BGC: object expected");
                    message.BGC[i] = $root.jleb.BingoGrid.fromObject(object.BGC[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BingoGridColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {jleb.BingoGridColumn} message BingoGridColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BingoGridColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BGC = [];
            if (message.BGC && message.BGC.length) {
                object.BGC = [];
                for (let j = 0; j < message.BGC.length; ++j)
                    object.BGC[j] = $root.jleb.BingoGrid.toObject(message.BGC[j], options);
            }
            return object;
        };

        /**
         * Converts this BingoGridColumn to JSON.
         * @function toJSON
         * @memberof jleb.BingoGridColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BingoGridColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BingoGridColumn
         * @function getTypeUrl
         * @memberof jleb.BingoGridColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BingoGridColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.BingoGridColumn";
        };

        return BingoGridColumn;
    })();

    jleb.BingoGrid = (function() {

        /**
         * Properties of a BingoGrid.
         * @memberof jleb
         * @interface IBingoGrid
         * @property {number|null} [CoinState] BingoGrid CoinState
         * @property {number|null} [Value] BingoGrid Value
         */

        /**
         * Constructs a new BingoGrid.
         * @memberof jleb
         * @classdesc Represents a BingoGrid.
         * @implements IBingoGrid
         * @constructor
         * @param {jleb.IBingoGrid=} [properties] Properties to set
         */
        function BingoGrid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BingoGrid CoinState.
         * @member {number} CoinState
         * @memberof jleb.BingoGrid
         * @instance
         */
        BingoGrid.prototype.CoinState = 0;

        /**
         * BingoGrid Value.
         * @member {number} Value
         * @memberof jleb.BingoGrid
         * @instance
         */
        BingoGrid.prototype.Value = 0;

        /**
         * Creates a new BingoGrid instance using the specified properties.
         * @function create
         * @memberof jleb.BingoGrid
         * @static
         * @param {jleb.IBingoGrid=} [properties] Properties to set
         * @returns {jleb.BingoGrid} BingoGrid instance
         */
        BingoGrid.create = function create(properties) {
            return new BingoGrid(properties);
        };

        /**
         * Encodes the specified BingoGrid message. Does not implicitly {@link jleb.BingoGrid.verify|verify} messages.
         * @function encode
         * @memberof jleb.BingoGrid
         * @static
         * @param {jleb.IBingoGrid} message BingoGrid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoGrid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.CoinState != null && Object.hasOwnProperty.call(message, "CoinState"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.CoinState);
            if (message.Value != null && Object.hasOwnProperty.call(message, "Value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Value);
            return writer;
        };

        /**
         * Encodes the specified BingoGrid message, length delimited. Does not implicitly {@link jleb.BingoGrid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.BingoGrid
         * @static
         * @param {jleb.IBingoGrid} message BingoGrid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoGrid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BingoGrid message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.BingoGrid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.BingoGrid} BingoGrid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoGrid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.BingoGrid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.CoinState = reader.int32();
                        break;
                    }
                case 2: {
                        message.Value = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BingoGrid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.BingoGrid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.BingoGrid} BingoGrid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoGrid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BingoGrid message.
         * @function verify
         * @memberof jleb.BingoGrid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BingoGrid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.CoinState != null && message.hasOwnProperty("CoinState"))
                if (!$util.isInteger(message.CoinState))
                    return "CoinState: integer expected";
            if (message.Value != null && message.hasOwnProperty("Value"))
                if (typeof message.Value !== "number")
                    return "Value: number expected";
            return null;
        };

        /**
         * Creates a BingoGrid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.BingoGrid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.BingoGrid} BingoGrid
         */
        BingoGrid.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.BingoGrid)
                return object;
            let message = new $root.jleb.BingoGrid();
            if (object.CoinState != null)
                message.CoinState = object.CoinState | 0;
            if (object.Value != null)
                message.Value = Number(object.Value);
            return message;
        };

        /**
         * Creates a plain object from a BingoGrid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.BingoGrid
         * @static
         * @param {jleb.BingoGrid} message BingoGrid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BingoGrid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.CoinState = 0;
                object.Value = 0;
            }
            if (message.CoinState != null && message.hasOwnProperty("CoinState"))
                object.CoinState = message.CoinState;
            if (message.Value != null && message.hasOwnProperty("Value"))
                object.Value = options.json && !isFinite(message.Value) ? String(message.Value) : message.Value;
            return object;
        };

        /**
         * Converts this BingoGrid to JSON.
         * @function toJSON
         * @memberof jleb.BingoGrid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BingoGrid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BingoGrid
         * @function getTypeUrl
         * @memberof jleb.BingoGrid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BingoGrid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.BingoGrid";
        };

        return BingoGrid;
    })();

    jleb.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jleb
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<jleb.IBingoSetClient>|null} [BingoSetVec] GameInfoData BingoSetVec
         * @property {number|null} [FeatureLevel] GameInfoData FeatureLevel
         * @property {Array.<jleb.IColumnFloat>|null} [JPOdds] GameInfoData JPOdds
         * @property {Array.<number>|null} [BonusList] GameInfoData BonusList
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jleb
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jleb.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.BingoSetVec = [];
            this.JPOdds = [];
            this.BonusList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jleb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jleb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData BingoSetVec.
         * @member {Array.<jleb.IBingoSetClient>} BingoSetVec
         * @memberof jleb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BingoSetVec = $util.emptyArray;

        /**
         * GameInfoData FeatureLevel.
         * @member {number} FeatureLevel
         * @memberof jleb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.FeatureLevel = 0;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<jleb.IColumnFloat>} JPOdds
         * @memberof jleb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * GameInfoData BonusList.
         * @member {Array.<number>} BonusList
         * @memberof jleb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BonusList = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jleb.GameInfoData
         * @static
         * @param {jleb.IGameInfoData=} [properties] Properties to set
         * @returns {jleb.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jleb.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jleb.GameInfoData
         * @static
         * @param {jleb.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.BingoSetVec != null && message.BingoSetVec.length)
                for (let i = 0; i < message.BingoSetVec.length; ++i)
                    $root.jleb.BingoSetClient.encode(message.BingoSetVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FeatureLevel != null && Object.hasOwnProperty.call(message, "FeatureLevel"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FeatureLevel);
            if (message.JPOdds != null && message.JPOdds.length)
                for (let i = 0; i < message.JPOdds.length; ++i)
                    $root.jleb.ColumnFloat.encode(message.JPOdds[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.BonusList != null && message.BonusList.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.BonusList.length; ++i)
                    writer.double(message.BonusList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jleb.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.GameInfoData
         * @static
         * @param {jleb.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.BingoSetVec && message.BingoSetVec.length))
                            message.BingoSetVec = [];
                        message.BingoSetVec.push($root.jleb.BingoSetClient.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.FeatureLevel = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        message.JPOdds.push($root.jleb.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.BonusList && message.BonusList.length))
                            message.BonusList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BonusList.push(reader.double());
                        } else
                            message.BonusList.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jleb.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.BingoSetVec != null && message.hasOwnProperty("BingoSetVec")) {
                if (!Array.isArray(message.BingoSetVec))
                    return "BingoSetVec: array expected";
                for (let i = 0; i < message.BingoSetVec.length; ++i) {
                    let error = $root.jleb.BingoSetClient.verify(message.BingoSetVec[i]);
                    if (error)
                        return "BingoSetVec." + error;
                }
            }
            if (message.FeatureLevel != null && message.hasOwnProperty("FeatureLevel"))
                if (!$util.isInteger(message.FeatureLevel))
                    return "FeatureLevel: integer expected";
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i) {
                    let error = $root.jleb.ColumnFloat.verify(message.JPOdds[i]);
                    if (error)
                        return "JPOdds." + error;
                }
            }
            if (message.BonusList != null && message.hasOwnProperty("BonusList")) {
                if (!Array.isArray(message.BonusList))
                    return "BonusList: array expected";
                for (let i = 0; i < message.BonusList.length; ++i)
                    if (typeof message.BonusList[i] !== "number")
                        return "BonusList: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.GameInfoData)
                return object;
            let message = new $root.jleb.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jleb.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.BingoSetVec) {
                if (!Array.isArray(object.BingoSetVec))
                    throw TypeError(".jleb.GameInfoData.BingoSetVec: array expected");
                message.BingoSetVec = [];
                for (let i = 0; i < object.BingoSetVec.length; ++i) {
                    if (typeof object.BingoSetVec[i] !== "object")
                        throw TypeError(".jleb.GameInfoData.BingoSetVec: object expected");
                    message.BingoSetVec[i] = $root.jleb.BingoSetClient.fromObject(object.BingoSetVec[i]);
                }
            }
            if (object.FeatureLevel != null)
                message.FeatureLevel = object.FeatureLevel | 0;
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jleb.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i) {
                    if (typeof object.JPOdds[i] !== "object")
                        throw TypeError(".jleb.GameInfoData.JPOdds: object expected");
                    message.JPOdds[i] = $root.jleb.ColumnFloat.fromObject(object.JPOdds[i]);
                }
            }
            if (object.BonusList) {
                if (!Array.isArray(object.BonusList))
                    throw TypeError(".jleb.GameInfoData.BonusList: array expected");
                message.BonusList = [];
                for (let i = 0; i < object.BonusList.length; ++i)
                    message.BonusList[i] = Number(object.BonusList[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.GameInfoData
         * @static
         * @param {jleb.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.BingoSetVec = [];
                object.JPOdds = [];
                object.BonusList = [];
            }
            if (options.defaults) {
                object.ShowExtra = false;
                object.FeatureLevel = 0;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.BingoSetVec && message.BingoSetVec.length) {
                object.BingoSetVec = [];
                for (let j = 0; j < message.BingoSetVec.length; ++j)
                    object.BingoSetVec[j] = $root.jleb.BingoSetClient.toObject(message.BingoSetVec[j], options);
            }
            if (message.FeatureLevel != null && message.hasOwnProperty("FeatureLevel"))
                object.FeatureLevel = message.FeatureLevel;
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = $root.jleb.ColumnFloat.toObject(message.JPOdds[j], options);
            }
            if (message.BonusList && message.BonusList.length) {
                object.BonusList = [];
                for (let j = 0; j < message.BonusList.length; ++j)
                    object.BonusList[j] = options.json && !isFinite(message.BonusList[j]) ? String(message.BonusList[j]) : message.BonusList[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jleb.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jleb.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.GameInfoData";
        };

        return GameInfoData;
    })();

    jleb.BingoSetClient = (function() {

        /**
         * Properties of a BingoSetClient.
         * @memberof jleb
         * @interface IBingoSetClient
         * @property {number|null} [Bet] BingoSetClient Bet
         * @property {Array.<jleb.ICoinValue>|null} [Coins] BingoSetClient Coins
         */

        /**
         * Constructs a new BingoSetClient.
         * @memberof jleb
         * @classdesc Represents a BingoSetClient.
         * @implements IBingoSetClient
         * @constructor
         * @param {jleb.IBingoSetClient=} [properties] Properties to set
         */
        function BingoSetClient(properties) {
            this.Coins = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BingoSetClient Bet.
         * @member {number} Bet
         * @memberof jleb.BingoSetClient
         * @instance
         */
        BingoSetClient.prototype.Bet = 0;

        /**
         * BingoSetClient Coins.
         * @member {Array.<jleb.ICoinValue>} Coins
         * @memberof jleb.BingoSetClient
         * @instance
         */
        BingoSetClient.prototype.Coins = $util.emptyArray;

        /**
         * Creates a new BingoSetClient instance using the specified properties.
         * @function create
         * @memberof jleb.BingoSetClient
         * @static
         * @param {jleb.IBingoSetClient=} [properties] Properties to set
         * @returns {jleb.BingoSetClient} BingoSetClient instance
         */
        BingoSetClient.create = function create(properties) {
            return new BingoSetClient(properties);
        };

        /**
         * Encodes the specified BingoSetClient message. Does not implicitly {@link jleb.BingoSetClient.verify|verify} messages.
         * @function encode
         * @memberof jleb.BingoSetClient
         * @static
         * @param {jleb.IBingoSetClient} message BingoSetClient message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoSetClient.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Bet);
            if (message.Coins != null && message.Coins.length)
                for (let i = 0; i < message.Coins.length; ++i)
                    $root.jleb.CoinValue.encode(message.Coins[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BingoSetClient message, length delimited. Does not implicitly {@link jleb.BingoSetClient.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleb.BingoSetClient
         * @static
         * @param {jleb.IBingoSetClient} message BingoSetClient message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BingoSetClient.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BingoSetClient message from the specified reader or buffer.
         * @function decode
         * @memberof jleb.BingoSetClient
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleb.BingoSetClient} BingoSetClient
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoSetClient.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleb.BingoSetClient();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Bet = reader.double();
                        break;
                    }
                case 2: {
                        if (!(message.Coins && message.Coins.length))
                            message.Coins = [];
                        message.Coins.push($root.jleb.CoinValue.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BingoSetClient message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleb.BingoSetClient
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleb.BingoSetClient} BingoSetClient
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BingoSetClient.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BingoSetClient message.
         * @function verify
         * @memberof jleb.BingoSetClient
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BingoSetClient.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Coins != null && message.hasOwnProperty("Coins")) {
                if (!Array.isArray(message.Coins))
                    return "Coins: array expected";
                for (let i = 0; i < message.Coins.length; ++i) {
                    let error = $root.jleb.CoinValue.verify(message.Coins[i]);
                    if (error)
                        return "Coins." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BingoSetClient message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleb.BingoSetClient
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleb.BingoSetClient} BingoSetClient
         */
        BingoSetClient.fromObject = function fromObject(object) {
            if (object instanceof $root.jleb.BingoSetClient)
                return object;
            let message = new $root.jleb.BingoSetClient();
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Coins) {
                if (!Array.isArray(object.Coins))
                    throw TypeError(".jleb.BingoSetClient.Coins: array expected");
                message.Coins = [];
                for (let i = 0; i < object.Coins.length; ++i) {
                    if (typeof object.Coins[i] !== "object")
                        throw TypeError(".jleb.BingoSetClient.Coins: object expected");
                    message.Coins[i] = $root.jleb.CoinValue.fromObject(object.Coins[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BingoSetClient message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleb.BingoSetClient
         * @static
         * @param {jleb.BingoSetClient} message BingoSetClient
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BingoSetClient.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Coins = [];
            if (options.defaults)
                object.Bet = 0;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Coins && message.Coins.length) {
                object.Coins = [];
                for (let j = 0; j < message.Coins.length; ++j)
                    object.Coins[j] = $root.jleb.CoinValue.toObject(message.Coins[j], options);
            }
            return object;
        };

        /**
         * Converts this BingoSetClient to JSON.
         * @function toJSON
         * @memberof jleb.BingoSetClient
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BingoSetClient.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BingoSetClient
         * @function getTypeUrl
         * @memberof jleb.BingoSetClient
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BingoSetClient.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleb.BingoSetClient";
        };

        return BingoSetClient;
    })();

    return jleb;
})();

export const jleg = $root.jleg = (() => {

    /**
     * Namespace jleg.
     * @exports jleg
     * @namespace
     */
    const jleg = {};

    jleg.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jleg
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jleg
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jleg.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jleg.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jleg.Column
         * @static
         * @param {jleg.IColumn=} [properties] Properties to set
         * @returns {jleg.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jleg.Column.verify|verify} messages.
         * @function encode
         * @memberof jleg.Column
         * @static
         * @param {jleg.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jleg.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleg.Column
         * @static
         * @param {jleg.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jleg.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleg.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleg.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleg.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleg.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jleg.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleg.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleg.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jleg.Column)
                return object;
            let message = new $root.jleg.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jleg.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleg.Column
         * @static
         * @param {jleg.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jleg.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jleg.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleg.Column";
        };

        return Column;
    })();

    jleg.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jleg
         * @interface IAwardData
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jleg
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jleg.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jleg.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jleg.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jleg.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jleg.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jleg.AwardData
         * @static
         * @param {jleg.IAwardData=} [properties] Properties to set
         * @returns {jleg.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jleg.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jleg.AwardData
         * @static
         * @param {jleg.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jleg.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleg.AwardData
         * @static
         * @param {jleg.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jleg.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleg.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleg.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Index = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Count = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleg.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleg.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jleg.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleg.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleg.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jleg.AwardData)
                return object;
            let message = new $root.jleg.AwardData();
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleg.AwardData
         * @static
         * @param {jleg.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Index = 0;
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
            }
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jleg.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jleg.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleg.AwardData";
        };

        return AwardData;
    })();

    jleg.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jleg
         * @interface IComboStageInfo
         * @property {Array.<jleg.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jleg.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {boolean|null} [NeedEliminate] ComboStageInfo NeedEliminate
         * @property {Array.<jleg.IColumn>|null} [Eliminate] ComboStageInfo Eliminate
         * @property {Array.<jleg.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<number>|null} [NowUpperMul] ComboStageInfo NowUpperMul
         * @property {Array.<number>|null} [UpperMul] ComboStageInfo UpperMul
         * @property {number|null} [NowMul] ComboStageInfo NowMul
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jleg
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jleg.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.Eliminate = [];
            this.FillSymbols = [];
            this.NowUpperMul = [];
            this.UpperMul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jleg.IColumn>} ComboStageSymbol
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jleg.IAwardData>} AwardDataVec
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo NeedEliminate.
         * @member {boolean} NeedEliminate
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NeedEliminate = false;

        /**
         * ComboStageInfo Eliminate.
         * @member {Array.<jleg.IColumn>} Eliminate
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Eliminate = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jleg.IColumn>} FillSymbols
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo NowUpperMul.
         * @member {Array.<number>} NowUpperMul
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NowUpperMul = $util.emptyArray;

        /**
         * ComboStageInfo UpperMul.
         * @member {Array.<number>} UpperMul
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.UpperMul = $util.emptyArray;

        /**
         * ComboStageInfo NowMul.
         * @member {number} NowMul
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NowMul = 0;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jleg.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {jleg.IComboStageInfo=} [properties] Properties to set
         * @returns {jleg.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jleg.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {jleg.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jleg.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jleg.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.NeedEliminate != null && Object.hasOwnProperty.call(message, "NeedEliminate"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.NeedEliminate);
            if (message.Eliminate != null && message.Eliminate.length)
                for (let i = 0; i < message.Eliminate.length; ++i)
                    $root.jleg.Column.encode(message.Eliminate[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jleg.Column.encode(message.FillSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.UpperMul != null && message.UpperMul.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.UpperMul.length; ++i)
                    writer.double(message.UpperMul[i]);
                writer.ldelim();
            }
            if (message.NowMul != null && Object.hasOwnProperty.call(message, "NowMul"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.NowMul);
            if (message.NowUpperMul != null && message.NowUpperMul.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.NowUpperMul.length; ++i)
                    writer.double(message.NowUpperMul[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jleg.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {jleg.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleg.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleg.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jleg.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jleg.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.NeedEliminate = reader.bool();
                        break;
                    }
                case 5: {
                        if (!(message.Eliminate && message.Eliminate.length))
                            message.Eliminate = [];
                        message.Eliminate.push($root.jleg.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jleg.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 9: {
                        if (!(message.NowUpperMul && message.NowUpperMul.length))
                            message.NowUpperMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NowUpperMul.push(reader.double());
                        } else
                            message.NowUpperMul.push(reader.double());
                        break;
                    }
                case 7: {
                        if (!(message.UpperMul && message.UpperMul.length))
                            message.UpperMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.UpperMul.push(reader.double());
                        } else
                            message.UpperMul.push(reader.double());
                        break;
                    }
                case 8: {
                        message.NowMul = reader.double();
                        break;
                    }
                case 10: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleg.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jleg.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jleg.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                if (typeof message.NeedEliminate !== "boolean")
                    return "NeedEliminate: boolean expected";
            if (message.Eliminate != null && message.hasOwnProperty("Eliminate")) {
                if (!Array.isArray(message.Eliminate))
                    return "Eliminate: array expected";
                for (let i = 0; i < message.Eliminate.length; ++i) {
                    let error = $root.jleg.Column.verify(message.Eliminate[i]);
                    if (error)
                        return "Eliminate." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jleg.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.NowUpperMul != null && message.hasOwnProperty("NowUpperMul")) {
                if (!Array.isArray(message.NowUpperMul))
                    return "NowUpperMul: array expected";
                for (let i = 0; i < message.NowUpperMul.length; ++i)
                    if (typeof message.NowUpperMul[i] !== "number")
                        return "NowUpperMul: number[] expected";
            }
            if (message.UpperMul != null && message.hasOwnProperty("UpperMul")) {
                if (!Array.isArray(message.UpperMul))
                    return "UpperMul: array expected";
                for (let i = 0; i < message.UpperMul.length; ++i)
                    if (typeof message.UpperMul[i] !== "number")
                        return "UpperMul: number[] expected";
            }
            if (message.NowMul != null && message.hasOwnProperty("NowMul"))
                if (typeof message.NowMul !== "number")
                    return "NowMul: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleg.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jleg.ComboStageInfo)
                return object;
            let message = new $root.jleg.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jleg.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jleg.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jleg.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jleg.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jleg.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jleg.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.NeedEliminate != null)
                message.NeedEliminate = Boolean(object.NeedEliminate);
            if (object.Eliminate) {
                if (!Array.isArray(object.Eliminate))
                    throw TypeError(".jleg.ComboStageInfo.Eliminate: array expected");
                message.Eliminate = [];
                for (let i = 0; i < object.Eliminate.length; ++i) {
                    if (typeof object.Eliminate[i] !== "object")
                        throw TypeError(".jleg.ComboStageInfo.Eliminate: object expected");
                    message.Eliminate[i] = $root.jleg.Column.fromObject(object.Eliminate[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jleg.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jleg.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jleg.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.NowUpperMul) {
                if (!Array.isArray(object.NowUpperMul))
                    throw TypeError(".jleg.ComboStageInfo.NowUpperMul: array expected");
                message.NowUpperMul = [];
                for (let i = 0; i < object.NowUpperMul.length; ++i)
                    message.NowUpperMul[i] = Number(object.NowUpperMul[i]);
            }
            if (object.UpperMul) {
                if (!Array.isArray(object.UpperMul))
                    throw TypeError(".jleg.ComboStageInfo.UpperMul: array expected");
                message.UpperMul = [];
                for (let i = 0; i < object.UpperMul.length; ++i)
                    message.UpperMul[i] = Number(object.UpperMul[i]);
            }
            if (object.NowMul != null)
                message.NowMul = Number(object.NowMul);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {jleg.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.Eliminate = [];
                object.FillSymbols = [];
                object.UpperMul = [];
                object.NowUpperMul = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.NeedEliminate = false;
                object.NowMul = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jleg.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jleg.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                object.NeedEliminate = message.NeedEliminate;
            if (message.Eliminate && message.Eliminate.length) {
                object.Eliminate = [];
                for (let j = 0; j < message.Eliminate.length; ++j)
                    object.Eliminate[j] = $root.jleg.Column.toObject(message.Eliminate[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jleg.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.UpperMul && message.UpperMul.length) {
                object.UpperMul = [];
                for (let j = 0; j < message.UpperMul.length; ++j)
                    object.UpperMul[j] = options.json && !isFinite(message.UpperMul[j]) ? String(message.UpperMul[j]) : message.UpperMul[j];
            }
            if (message.NowMul != null && message.hasOwnProperty("NowMul"))
                object.NowMul = options.json && !isFinite(message.NowMul) ? String(message.NowMul) : message.NowMul;
            if (message.NowUpperMul && message.NowUpperMul.length) {
                object.NowUpperMul = [];
                for (let j = 0; j < message.NowUpperMul.length; ++j)
                    object.NowUpperMul[j] = options.json && !isFinite(message.NowUpperMul[j]) ? String(message.NowUpperMul[j]) : message.NowUpperMul[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jleg.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jleg.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleg.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jleg.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jleg
         * @interface IRoundInfo
         * @property {Array.<jleg.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {number|null} [ScatterWin] RoundInfo ScatterWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRounds] RoundInfo AddRounds
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jleg
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jleg.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jleg.IComboStageInfo>} ComboStageData
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo ScatterWin.
         * @member {number} ScatterWin
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRounds.
         * @member {number} AddRounds
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRounds = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jleg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jleg.RoundInfo
         * @static
         * @param {jleg.IRoundInfo=} [properties] Properties to set
         * @returns {jleg.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jleg.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jleg.RoundInfo
         * @static
         * @param {jleg.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jleg.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ScatterCount);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.ScatterWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.AddRounds != null && Object.hasOwnProperty.call(message, "AddRounds"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AddRounds);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jleg.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleg.RoundInfo
         * @static
         * @param {jleg.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jleg.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleg.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleg.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jleg.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AddRounds = reader.int32();
                        break;
                    }
                case 8: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleg.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleg.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jleg.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jleg.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                if (!$util.isInteger(message.AddRounds))
                    return "AddRounds: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleg.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleg.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jleg.RoundInfo)
                return object;
            let message = new $root.jleg.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jleg.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jleg.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jleg.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRounds != null)
                message.AddRounds = object.AddRounds | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleg.RoundInfo
         * @static
         * @param {jleg.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.ScatterCount = 0;
                object.ScatterWin = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
                object.AddRounds = 0;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jleg.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                object.AddRounds = message.AddRounds;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jleg.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jleg.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleg.RoundInfo";
        };

        return RoundInfo;
    })();

    jleg.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jleg
         * @interface ISpinAck
         * @property {Array.<jleg.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jleg
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jleg.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jleg.IRoundInfo>} RoundQueue
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jleg.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jleg.SpinAck
         * @static
         * @param {jleg.ISpinAck=} [properties] Properties to set
         * @returns {jleg.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jleg.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jleg.SpinAck
         * @static
         * @param {jleg.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jleg.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jleg.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jleg.SpinAck
         * @static
         * @param {jleg.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jleg.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jleg.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jleg.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jleg.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jleg.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jleg.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jleg.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jleg.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jleg.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jleg.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jleg.SpinAck)
                return object;
            let message = new $root.jleg.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jleg.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jleg.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jleg.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jleg.SpinAck
         * @static
         * @param {jleg.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jleg.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jleg.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jleg.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jleg.SpinAck";
        };

        return SpinAck;
    })();

    return jleg;
})();

export const jlfc = $root.jlfc = (() => {

    /**
     * Namespace jlfc.
     * @exports jlfc
     * @namespace
     */
    const jlfc = {};

    jlfc.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfc
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfc
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfc.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfc.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfc.Column
         * @static
         * @param {jlfc.IColumn=} [properties] Properties to set
         * @returns {jlfc.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfc.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfc.Column
         * @static
         * @param {jlfc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfc.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc.Column
         * @static
         * @param {jlfc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfc.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc.Column)
                return object;
            let message = new $root.jlfc.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfc.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc.Column
         * @static
         * @param {jlfc.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfc.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfc.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc.Column";
        };

        return Column;
    })();

    jlfc.ColumnM = (function() {

        /**
         * Properties of a ColumnM.
         * @memberof jlfc
         * @interface IColumnM
         * @property {Array.<number>|null} [Col] ColumnM Col
         */

        /**
         * Constructs a new ColumnM.
         * @memberof jlfc
         * @classdesc Represents a ColumnM.
         * @implements IColumnM
         * @constructor
         * @param {jlfc.IColumnM=} [properties] Properties to set
         */
        function ColumnM(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnM Col.
         * @member {Array.<number>} Col
         * @memberof jlfc.ColumnM
         * @instance
         */
        ColumnM.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnM instance using the specified properties.
         * @function create
         * @memberof jlfc.ColumnM
         * @static
         * @param {jlfc.IColumnM=} [properties] Properties to set
         * @returns {jlfc.ColumnM} ColumnM instance
         */
        ColumnM.create = function create(properties) {
            return new ColumnM(properties);
        };

        /**
         * Encodes the specified ColumnM message. Does not implicitly {@link jlfc.ColumnM.verify|verify} messages.
         * @function encode
         * @memberof jlfc.ColumnM
         * @static
         * @param {jlfc.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnM message, length delimited. Does not implicitly {@link jlfc.ColumnM.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc.ColumnM
         * @static
         * @param {jlfc.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc.ColumnM();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnM message.
         * @function verify
         * @memberof jlfc.ColumnM
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnM.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnM message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc.ColumnM
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc.ColumnM} ColumnM
         */
        ColumnM.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc.ColumnM)
                return object;
            let message = new $root.jlfc.ColumnM();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfc.ColumnM.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnM message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc.ColumnM
         * @static
         * @param {jlfc.ColumnM} message ColumnM
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnM.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnM to JSON.
         * @function toJSON
         * @memberof jlfc.ColumnM
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnM.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnM
         * @function getTypeUrl
         * @memberof jlfc.ColumnM
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnM.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc.ColumnM";
        };

        return ColumnM;
    })();

    jlfc.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfc
         * @interface IAwardData
         * @property {number|null} [Atype] AwardData Atype
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Pos] AwardData Pos
         * @property {number|null} [Mul] AwardData Mul
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfc
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfc.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Atype.
         * @member {number} Atype
         * @memberof jlfc.AwardData
         * @instance
         */
        AwardData.prototype.Atype = 0;

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlfc.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfc.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Pos.
         * @member {number} Pos
         * @memberof jlfc.AwardData
         * @instance
         */
        AwardData.prototype.Pos = 0;

        /**
         * AwardData Mul.
         * @member {number} Mul
         * @memberof jlfc.AwardData
         * @instance
         */
        AwardData.prototype.Mul = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfc.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfc.AwardData
         * @static
         * @param {jlfc.IAwardData=} [properties] Properties to set
         * @returns {jlfc.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfc.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfc.AwardData
         * @static
         * @param {jlfc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Atype != null && Object.hasOwnProperty.call(message, "Atype"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Atype);
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Pos);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mul);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfc.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc.AwardData
         * @static
         * @param {jlfc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Atype = reader.int32();
                        break;
                    }
                case 2: {
                        message.Index = reader.int32();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 5: {
                        message.Mul = reader.double();
                        break;
                    }
                case 6: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfc.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                if (!$util.isInteger(message.Atype))
                    return "Atype: integer expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc.AwardData)
                return object;
            let message = new $root.jlfc.AwardData();
            if (object.Atype != null)
                message.Atype = object.Atype | 0;
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc.AwardData
         * @static
         * @param {jlfc.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Atype = 0;
                object.Index = 0;
                object.Symbol = 0;
                object.Pos = 0;
                object.Mul = 0;
                object.Win = 0;
            }
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                object.Atype = message.Atype;
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfc.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfc.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc.AwardData";
        };

        return AwardData;
    })();

    jlfc.PotSymbol = (function() {

        /**
         * Properties of a PotSymbol.
         * @memberof jlfc
         * @interface IPotSymbol
         * @property {number|null} [Pos] PotSymbol Pos
         * @property {number|null} [Symbol] PotSymbol Symbol
         * @property {number|null} [Mul] PotSymbol Mul
         */

        /**
         * Constructs a new PotSymbol.
         * @memberof jlfc
         * @classdesc Represents a PotSymbol.
         * @implements IPotSymbol
         * @constructor
         * @param {jlfc.IPotSymbol=} [properties] Properties to set
         */
        function PotSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PotSymbol Pos.
         * @member {number} Pos
         * @memberof jlfc.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Pos = 0;

        /**
         * PotSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlfc.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Symbol = 0;

        /**
         * PotSymbol Mul.
         * @member {number} Mul
         * @memberof jlfc.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Mul = 0;

        /**
         * Creates a new PotSymbol instance using the specified properties.
         * @function create
         * @memberof jlfc.PotSymbol
         * @static
         * @param {jlfc.IPotSymbol=} [properties] Properties to set
         * @returns {jlfc.PotSymbol} PotSymbol instance
         */
        PotSymbol.create = function create(properties) {
            return new PotSymbol(properties);
        };

        /**
         * Encodes the specified PotSymbol message. Does not implicitly {@link jlfc.PotSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlfc.PotSymbol
         * @static
         * @param {jlfc.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified PotSymbol message, length delimited. Does not implicitly {@link jlfc.PotSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc.PotSymbol
         * @static
         * @param {jlfc.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc.PotSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PotSymbol message.
         * @function verify
         * @memberof jlfc.PotSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PotSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a PotSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc.PotSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc.PotSymbol} PotSymbol
         */
        PotSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc.PotSymbol)
                return object;
            let message = new $root.jlfc.PotSymbol();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a PotSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc.PotSymbol
         * @static
         * @param {jlfc.PotSymbol} message PotSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PotSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this PotSymbol to JSON.
         * @function toJSON
         * @memberof jlfc.PotSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PotSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PotSymbol
         * @function getTypeUrl
         * @memberof jlfc.PotSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PotSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc.PotSymbol";
        };

        return PotSymbol;
    })();

    jlfc.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlfc
         * @interface IRoundInfo
         * @property {Array.<jlfc.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {Array.<jlfc.IColumnM>|null} [Mul] RoundInfo Mul
         * @property {Array.<jlfc.IColumn>|null} [FinalSymbol] RoundInfo FinalSymbol
         * @property {Array.<jlfc.IColumnM>|null} [FinalMul] RoundInfo FinalMul
         * @property {Array.<jlfc.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<jlfc.IPotSymbol>|null} [PotList] RoundInfo PotList
         * @property {number|null} [BonusRemainRound] RoundInfo BonusRemainRound
         * @property {number|null} [BonusTotalRound] RoundInfo BonusTotalRound
         * @property {boolean|null} [GetBonus] RoundInfo GetBonus
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlfc
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlfc.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            this.FinalSymbol = [];
            this.FinalMul = [];
            this.AwardDataVec = [];
            this.PotList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jlfc.IColumn>} Symbol
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo Mul.
         * @member {Array.<jlfc.IColumnM>} Mul
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mul = $util.emptyArray;

        /**
         * RoundInfo FinalSymbol.
         * @member {Array.<jlfc.IColumn>} FinalSymbol
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalSymbol = $util.emptyArray;

        /**
         * RoundInfo FinalMul.
         * @member {Array.<jlfc.IColumnM>} FinalMul
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalMul = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlfc.IAwardData>} AwardDataVec
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo PotList.
         * @member {Array.<jlfc.IPotSymbol>} PotList
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PotList = $util.emptyArray;

        /**
         * RoundInfo BonusRemainRound.
         * @member {number} BonusRemainRound
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusRemainRound = 0;

        /**
         * RoundInfo BonusTotalRound.
         * @member {number} BonusTotalRound
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusTotalRound = 0;

        /**
         * RoundInfo GetBonus.
         * @member {boolean} GetBonus
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetBonus = false;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlfc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlfc.RoundInfo
         * @static
         * @param {jlfc.IRoundInfo=} [properties] Properties to set
         * @returns {jlfc.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlfc.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlfc.RoundInfo
         * @static
         * @param {jlfc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlfc.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length)
                for (let i = 0; i < message.Mul.length; ++i)
                    $root.jlfc.ColumnM.encode(message.Mul[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FinalSymbol != null && message.FinalSymbol.length)
                for (let i = 0; i < message.FinalSymbol.length; ++i)
                    $root.jlfc.Column.encode(message.FinalSymbol[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FinalMul != null && message.FinalMul.length)
                for (let i = 0; i < message.FinalMul.length; ++i)
                    $root.jlfc.ColumnM.encode(message.FinalMul[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfc.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.PotList != null && message.PotList.length)
                for (let i = 0; i < message.PotList.length; ++i)
                    $root.jlfc.PotSymbol.encode(message.PotList[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.BonusRemainRound != null && Object.hasOwnProperty.call(message, "BonusRemainRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.BonusRemainRound);
            if (message.BonusTotalRound != null && Object.hasOwnProperty.call(message, "BonusTotalRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.BonusTotalRound);
            if (message.GetBonus != null && Object.hasOwnProperty.call(message, "GetBonus"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.GetBonus);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlfc.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc.RoundInfo
         * @static
         * @param {jlfc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlfc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        message.Mul.push($root.jlfc.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FinalSymbol && message.FinalSymbol.length))
                            message.FinalSymbol = [];
                        message.FinalSymbol.push($root.jlfc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FinalMul && message.FinalMul.length))
                            message.FinalMul = [];
                        message.FinalMul.push($root.jlfc.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfc.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        if (!(message.PotList && message.PotList.length))
                            message.PotList = [];
                        message.PotList.push($root.jlfc.PotSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.BonusRemainRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.BonusTotalRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.GetBonus = reader.bool();
                        break;
                    }
                case 11: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlfc.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlfc.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i) {
                    let error = $root.jlfc.ColumnM.verify(message.Mul[i]);
                    if (error)
                        return "Mul." + error;
                }
            }
            if (message.FinalSymbol != null && message.hasOwnProperty("FinalSymbol")) {
                if (!Array.isArray(message.FinalSymbol))
                    return "FinalSymbol: array expected";
                for (let i = 0; i < message.FinalSymbol.length; ++i) {
                    let error = $root.jlfc.Column.verify(message.FinalSymbol[i]);
                    if (error)
                        return "FinalSymbol." + error;
                }
            }
            if (message.FinalMul != null && message.hasOwnProperty("FinalMul")) {
                if (!Array.isArray(message.FinalMul))
                    return "FinalMul: array expected";
                for (let i = 0; i < message.FinalMul.length; ++i) {
                    let error = $root.jlfc.ColumnM.verify(message.FinalMul[i]);
                    if (error)
                        return "FinalMul." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfc.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.PotList != null && message.hasOwnProperty("PotList")) {
                if (!Array.isArray(message.PotList))
                    return "PotList: array expected";
                for (let i = 0; i < message.PotList.length; ++i) {
                    let error = $root.jlfc.PotSymbol.verify(message.PotList[i]);
                    if (error)
                        return "PotList." + error;
                }
            }
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                if (!$util.isInteger(message.BonusRemainRound))
                    return "BonusRemainRound: integer expected";
            if (message.BonusTotalRound != null && message.hasOwnProperty("BonusTotalRound"))
                if (!$util.isInteger(message.BonusTotalRound))
                    return "BonusTotalRound: integer expected";
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                if (typeof message.GetBonus !== "boolean")
                    return "GetBonus: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc.RoundInfo)
                return object;
            let message = new $root.jlfc.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlfc.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlfc.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jlfc.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfc.RoundInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i) {
                    if (typeof object.Mul[i] !== "object")
                        throw TypeError(".jlfc.RoundInfo.Mul: object expected");
                    message.Mul[i] = $root.jlfc.ColumnM.fromObject(object.Mul[i]);
                }
            }
            if (object.FinalSymbol) {
                if (!Array.isArray(object.FinalSymbol))
                    throw TypeError(".jlfc.RoundInfo.FinalSymbol: array expected");
                message.FinalSymbol = [];
                for (let i = 0; i < object.FinalSymbol.length; ++i) {
                    if (typeof object.FinalSymbol[i] !== "object")
                        throw TypeError(".jlfc.RoundInfo.FinalSymbol: object expected");
                    message.FinalSymbol[i] = $root.jlfc.Column.fromObject(object.FinalSymbol[i]);
                }
            }
            if (object.FinalMul) {
                if (!Array.isArray(object.FinalMul))
                    throw TypeError(".jlfc.RoundInfo.FinalMul: array expected");
                message.FinalMul = [];
                for (let i = 0; i < object.FinalMul.length; ++i) {
                    if (typeof object.FinalMul[i] !== "object")
                        throw TypeError(".jlfc.RoundInfo.FinalMul: object expected");
                    message.FinalMul[i] = $root.jlfc.ColumnM.fromObject(object.FinalMul[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfc.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfc.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfc.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.PotList) {
                if (!Array.isArray(object.PotList))
                    throw TypeError(".jlfc.RoundInfo.PotList: array expected");
                message.PotList = [];
                for (let i = 0; i < object.PotList.length; ++i) {
                    if (typeof object.PotList[i] !== "object")
                        throw TypeError(".jlfc.RoundInfo.PotList: object expected");
                    message.PotList[i] = $root.jlfc.PotSymbol.fromObject(object.PotList[i]);
                }
            }
            if (object.BonusRemainRound != null)
                message.BonusRemainRound = object.BonusRemainRound | 0;
            if (object.BonusTotalRound != null)
                message.BonusTotalRound = object.BonusTotalRound | 0;
            if (object.GetBonus != null)
                message.GetBonus = Boolean(object.GetBonus);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc.RoundInfo
         * @static
         * @param {jlfc.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
                object.FinalSymbol = [];
                object.FinalMul = [];
                object.AwardDataVec = [];
                object.PotList = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.BonusRemainRound = 0;
                object.BonusTotalRound = 0;
                object.GetBonus = false;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlfc.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = $root.jlfc.ColumnM.toObject(message.Mul[j], options);
            }
            if (message.FinalSymbol && message.FinalSymbol.length) {
                object.FinalSymbol = [];
                for (let j = 0; j < message.FinalSymbol.length; ++j)
                    object.FinalSymbol[j] = $root.jlfc.Column.toObject(message.FinalSymbol[j], options);
            }
            if (message.FinalMul && message.FinalMul.length) {
                object.FinalMul = [];
                for (let j = 0; j < message.FinalMul.length; ++j)
                    object.FinalMul[j] = $root.jlfc.ColumnM.toObject(message.FinalMul[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfc.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.PotList && message.PotList.length) {
                object.PotList = [];
                for (let j = 0; j < message.PotList.length; ++j)
                    object.PotList[j] = $root.jlfc.PotSymbol.toObject(message.PotList[j], options);
            }
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                object.BonusRemainRound = message.BonusRemainRound;
            if (message.BonusTotalRound != null && message.hasOwnProperty("BonusTotalRound"))
                object.BonusTotalRound = message.BonusTotalRound;
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                object.GetBonus = message.GetBonus;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlfc.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlfc.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc.RoundInfo";
        };

        return RoundInfo;
    })();

    jlfc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfc
         * @interface ISpinAck
         * @property {Array.<jlfc.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlfc.IRoundInfo>} RoundQueue
         * @memberof jlfc.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlfc.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlfc.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlfc.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfc.SpinAck
         * @static
         * @param {jlfc.ISpinAck=} [properties] Properties to set
         * @returns {jlfc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfc.SpinAck
         * @static
         * @param {jlfc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlfc.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc.SpinAck
         * @static
         * @param {jlfc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlfc.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.RTP = reader.double();
                        break;
                    }
                case 5: {
                        message.LID = reader.int32();
                        break;
                    }
                case 6: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlfc.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc.SpinAck)
                return object;
            let message = new $root.jlfc.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlfc.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlfc.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlfc.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc.SpinAck
         * @static
         * @param {jlfc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlfc.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc.SpinAck";
        };

        return SpinAck;
    })();

    return jlfc;
})();

export const jlfc2 = $root.jlfc2 = (() => {

    /**
     * Namespace jlfc2.
     * @exports jlfc2
     * @namespace
     */
    const jlfc2 = {};

    jlfc2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfc2
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfc2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfc2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfc2.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfc2.Column
         * @static
         * @param {jlfc2.IColumn=} [properties] Properties to set
         * @returns {jlfc2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfc2.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.Column
         * @static
         * @param {jlfc2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfc2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.Column
         * @static
         * @param {jlfc2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfc2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.Column)
                return object;
            let message = new $root.jlfc2.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfc2.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.Column
         * @static
         * @param {jlfc2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfc2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfc2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.Column";
        };

        return Column;
    })();

    jlfc2.ColumnM = (function() {

        /**
         * Properties of a ColumnM.
         * @memberof jlfc2
         * @interface IColumnM
         * @property {Array.<number>|null} [Col] ColumnM Col
         */

        /**
         * Constructs a new ColumnM.
         * @memberof jlfc2
         * @classdesc Represents a ColumnM.
         * @implements IColumnM
         * @constructor
         * @param {jlfc2.IColumnM=} [properties] Properties to set
         */
        function ColumnM(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnM Col.
         * @member {Array.<number>} Col
         * @memberof jlfc2.ColumnM
         * @instance
         */
        ColumnM.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnM instance using the specified properties.
         * @function create
         * @memberof jlfc2.ColumnM
         * @static
         * @param {jlfc2.IColumnM=} [properties] Properties to set
         * @returns {jlfc2.ColumnM} ColumnM instance
         */
        ColumnM.create = function create(properties) {
            return new ColumnM(properties);
        };

        /**
         * Encodes the specified ColumnM message. Does not implicitly {@link jlfc2.ColumnM.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.ColumnM
         * @static
         * @param {jlfc2.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnM message, length delimited. Does not implicitly {@link jlfc2.ColumnM.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.ColumnM
         * @static
         * @param {jlfc2.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.ColumnM();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnM message.
         * @function verify
         * @memberof jlfc2.ColumnM
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnM.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnM message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.ColumnM
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.ColumnM} ColumnM
         */
        ColumnM.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.ColumnM)
                return object;
            let message = new $root.jlfc2.ColumnM();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfc2.ColumnM.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnM message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.ColumnM
         * @static
         * @param {jlfc2.ColumnM} message ColumnM
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnM.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnM to JSON.
         * @function toJSON
         * @memberof jlfc2.ColumnM
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnM.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnM
         * @function getTypeUrl
         * @memberof jlfc2.ColumnM
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnM.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.ColumnM";
        };

        return ColumnM;
    })();

    jlfc2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfc2
         * @interface IAwardData
         * @property {number|null} [Atype] AwardData Atype
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Pos] AwardData Pos
         * @property {number|null} [Mul] AwardData Mul
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfc2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfc2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Atype.
         * @member {number} Atype
         * @memberof jlfc2.AwardData
         * @instance
         */
        AwardData.prototype.Atype = 0;

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlfc2.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfc2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Pos.
         * @member {number} Pos
         * @memberof jlfc2.AwardData
         * @instance
         */
        AwardData.prototype.Pos = 0;

        /**
         * AwardData Mul.
         * @member {number} Mul
         * @memberof jlfc2.AwardData
         * @instance
         */
        AwardData.prototype.Mul = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfc2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfc2.AwardData
         * @static
         * @param {jlfc2.IAwardData=} [properties] Properties to set
         * @returns {jlfc2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfc2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.AwardData
         * @static
         * @param {jlfc2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Atype != null && Object.hasOwnProperty.call(message, "Atype"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Atype);
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Pos);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mul);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfc2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.AwardData
         * @static
         * @param {jlfc2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Atype = reader.int32();
                        break;
                    }
                case 2: {
                        message.Index = reader.int32();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 5: {
                        message.Mul = reader.double();
                        break;
                    }
                case 6: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfc2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                if (!$util.isInteger(message.Atype))
                    return "Atype: integer expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.AwardData)
                return object;
            let message = new $root.jlfc2.AwardData();
            if (object.Atype != null)
                message.Atype = object.Atype | 0;
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.AwardData
         * @static
         * @param {jlfc2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Atype = 0;
                object.Index = 0;
                object.Symbol = 0;
                object.Pos = 0;
                object.Mul = 0;
                object.Win = 0;
            }
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                object.Atype = message.Atype;
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfc2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfc2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.AwardData";
        };

        return AwardData;
    })();

    jlfc2.PotSymbol = (function() {

        /**
         * Properties of a PotSymbol.
         * @memberof jlfc2
         * @interface IPotSymbol
         * @property {number|null} [Pos] PotSymbol Pos
         * @property {number|null} [Symbol] PotSymbol Symbol
         * @property {number|null} [Mul] PotSymbol Mul
         * @property {number|null} [ScatterMultiplier] PotSymbol ScatterMultiplier
         */

        /**
         * Constructs a new PotSymbol.
         * @memberof jlfc2
         * @classdesc Represents a PotSymbol.
         * @implements IPotSymbol
         * @constructor
         * @param {jlfc2.IPotSymbol=} [properties] Properties to set
         */
        function PotSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PotSymbol Pos.
         * @member {number} Pos
         * @memberof jlfc2.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Pos = 0;

        /**
         * PotSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlfc2.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Symbol = 0;

        /**
         * PotSymbol Mul.
         * @member {number} Mul
         * @memberof jlfc2.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Mul = 0;

        /**
         * PotSymbol ScatterMultiplier.
         * @member {number} ScatterMultiplier
         * @memberof jlfc2.PotSymbol
         * @instance
         */
        PotSymbol.prototype.ScatterMultiplier = 0;

        /**
         * Creates a new PotSymbol instance using the specified properties.
         * @function create
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {jlfc2.IPotSymbol=} [properties] Properties to set
         * @returns {jlfc2.PotSymbol} PotSymbol instance
         */
        PotSymbol.create = function create(properties) {
            return new PotSymbol(properties);
        };

        /**
         * Encodes the specified PotSymbol message. Does not implicitly {@link jlfc2.PotSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {jlfc2.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            if (message.ScatterMultiplier != null && Object.hasOwnProperty.call(message, "ScatterMultiplier"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ScatterMultiplier);
            return writer;
        };

        /**
         * Encodes the specified PotSymbol message, length delimited. Does not implicitly {@link jlfc2.PotSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {jlfc2.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.PotSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                case 4: {
                        message.ScatterMultiplier = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PotSymbol message.
         * @function verify
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PotSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.ScatterMultiplier != null && message.hasOwnProperty("ScatterMultiplier"))
                if (!$util.isInteger(message.ScatterMultiplier))
                    return "ScatterMultiplier: integer expected";
            return null;
        };

        /**
         * Creates a PotSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.PotSymbol} PotSymbol
         */
        PotSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.PotSymbol)
                return object;
            let message = new $root.jlfc2.PotSymbol();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.ScatterMultiplier != null)
                message.ScatterMultiplier = object.ScatterMultiplier | 0;
            return message;
        };

        /**
         * Creates a plain object from a PotSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {jlfc2.PotSymbol} message PotSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PotSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
                object.ScatterMultiplier = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.ScatterMultiplier != null && message.hasOwnProperty("ScatterMultiplier"))
                object.ScatterMultiplier = message.ScatterMultiplier;
            return object;
        };

        /**
         * Converts this PotSymbol to JSON.
         * @function toJSON
         * @memberof jlfc2.PotSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PotSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PotSymbol
         * @function getTypeUrl
         * @memberof jlfc2.PotSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PotSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.PotSymbol";
        };

        return PotSymbol;
    })();

    jlfc2.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlfc2
         * @interface IRoundInfo
         * @property {Array.<jlfc2.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {Array.<jlfc2.IColumnM>|null} [Mul] RoundInfo Mul
         * @property {Array.<jlfc2.IColumn>|null} [FinalSymbol] RoundInfo FinalSymbol
         * @property {Array.<jlfc2.IColumnM>|null} [FinalMul] RoundInfo FinalMul
         * @property {Array.<jlfc2.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<jlfc2.IPotSymbol>|null} [PotList] RoundInfo PotList
         * @property {number|null} [BonusRemainRound] RoundInfo BonusRemainRound
         * @property {number|null} [BonusTotalRound] RoundInfo BonusTotalRound
         * @property {boolean|null} [GetBonus] RoundInfo GetBonus
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [PlateChangeFlag] RoundInfo PlateChangeFlag
         * @property {Array.<number>|null} [ScatterMultiplier] RoundInfo ScatterMultiplier
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlfc2
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlfc2.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            this.FinalSymbol = [];
            this.FinalMul = [];
            this.AwardDataVec = [];
            this.PotList = [];
            this.ScatterMultiplier = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jlfc2.IColumn>} Symbol
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo Mul.
         * @member {Array.<jlfc2.IColumnM>} Mul
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mul = $util.emptyArray;

        /**
         * RoundInfo FinalSymbol.
         * @member {Array.<jlfc2.IColumn>} FinalSymbol
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalSymbol = $util.emptyArray;

        /**
         * RoundInfo FinalMul.
         * @member {Array.<jlfc2.IColumnM>} FinalMul
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalMul = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlfc2.IAwardData>} AwardDataVec
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo PotList.
         * @member {Array.<jlfc2.IPotSymbol>} PotList
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PotList = $util.emptyArray;

        /**
         * RoundInfo BonusRemainRound.
         * @member {number} BonusRemainRound
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusRemainRound = 0;

        /**
         * RoundInfo BonusTotalRound.
         * @member {number} BonusTotalRound
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusTotalRound = 0;

        /**
         * RoundInfo GetBonus.
         * @member {boolean} GetBonus
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetBonus = false;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo PlateChangeFlag.
         * @member {number} PlateChangeFlag
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateChangeFlag = 0;

        /**
         * RoundInfo ScatterMultiplier.
         * @member {Array.<number>} ScatterMultiplier
         * @memberof jlfc2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterMultiplier = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {jlfc2.IRoundInfo=} [properties] Properties to set
         * @returns {jlfc2.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlfc2.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {jlfc2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlfc2.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length)
                for (let i = 0; i < message.Mul.length; ++i)
                    $root.jlfc2.ColumnM.encode(message.Mul[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FinalSymbol != null && message.FinalSymbol.length)
                for (let i = 0; i < message.FinalSymbol.length; ++i)
                    $root.jlfc2.Column.encode(message.FinalSymbol[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FinalMul != null && message.FinalMul.length)
                for (let i = 0; i < message.FinalMul.length; ++i)
                    $root.jlfc2.ColumnM.encode(message.FinalMul[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfc2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.PotList != null && message.PotList.length)
                for (let i = 0; i < message.PotList.length; ++i)
                    $root.jlfc2.PotSymbol.encode(message.PotList[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.BonusRemainRound != null && Object.hasOwnProperty.call(message, "BonusRemainRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.BonusRemainRound);
            if (message.BonusTotalRound != null && Object.hasOwnProperty.call(message, "BonusTotalRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.BonusTotalRound);
            if (message.GetBonus != null && Object.hasOwnProperty.call(message, "GetBonus"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.GetBonus);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            if (message.PlateChangeFlag != null && Object.hasOwnProperty.call(message, "PlateChangeFlag"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.PlateChangeFlag);
            if (message.ScatterMultiplier != null && message.ScatterMultiplier.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (let i = 0; i < message.ScatterMultiplier.length; ++i)
                    writer.int32(message.ScatterMultiplier[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlfc2.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {jlfc2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlfc2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        message.Mul.push($root.jlfc2.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FinalSymbol && message.FinalSymbol.length))
                            message.FinalSymbol = [];
                        message.FinalSymbol.push($root.jlfc2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FinalMul && message.FinalMul.length))
                            message.FinalMul = [];
                        message.FinalMul.push($root.jlfc2.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfc2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        if (!(message.PotList && message.PotList.length))
                            message.PotList = [];
                        message.PotList.push($root.jlfc2.PotSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.BonusRemainRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.BonusTotalRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.GetBonus = reader.bool();
                        break;
                    }
                case 11: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 13: {
                        message.PlateChangeFlag = reader.int32();
                        break;
                    }
                case 14: {
                        if (!(message.ScatterMultiplier && message.ScatterMultiplier.length))
                            message.ScatterMultiplier = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ScatterMultiplier.push(reader.int32());
                        } else
                            message.ScatterMultiplier.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlfc2.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i) {
                    let error = $root.jlfc2.ColumnM.verify(message.Mul[i]);
                    if (error)
                        return "Mul." + error;
                }
            }
            if (message.FinalSymbol != null && message.hasOwnProperty("FinalSymbol")) {
                if (!Array.isArray(message.FinalSymbol))
                    return "FinalSymbol: array expected";
                for (let i = 0; i < message.FinalSymbol.length; ++i) {
                    let error = $root.jlfc2.Column.verify(message.FinalSymbol[i]);
                    if (error)
                        return "FinalSymbol." + error;
                }
            }
            if (message.FinalMul != null && message.hasOwnProperty("FinalMul")) {
                if (!Array.isArray(message.FinalMul))
                    return "FinalMul: array expected";
                for (let i = 0; i < message.FinalMul.length; ++i) {
                    let error = $root.jlfc2.ColumnM.verify(message.FinalMul[i]);
                    if (error)
                        return "FinalMul." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfc2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.PotList != null && message.hasOwnProperty("PotList")) {
                if (!Array.isArray(message.PotList))
                    return "PotList: array expected";
                for (let i = 0; i < message.PotList.length; ++i) {
                    let error = $root.jlfc2.PotSymbol.verify(message.PotList[i]);
                    if (error)
                        return "PotList." + error;
                }
            }
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                if (!$util.isInteger(message.BonusRemainRound))
                    return "BonusRemainRound: integer expected";
            if (message.BonusTotalRound != null && message.hasOwnProperty("BonusTotalRound"))
                if (!$util.isInteger(message.BonusTotalRound))
                    return "BonusTotalRound: integer expected";
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                if (typeof message.GetBonus !== "boolean")
                    return "GetBonus: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.PlateChangeFlag != null && message.hasOwnProperty("PlateChangeFlag"))
                if (!$util.isInteger(message.PlateChangeFlag))
                    return "PlateChangeFlag: integer expected";
            if (message.ScatterMultiplier != null && message.hasOwnProperty("ScatterMultiplier")) {
                if (!Array.isArray(message.ScatterMultiplier))
                    return "ScatterMultiplier: array expected";
                for (let i = 0; i < message.ScatterMultiplier.length; ++i)
                    if (!$util.isInteger(message.ScatterMultiplier[i]))
                        return "ScatterMultiplier: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.RoundInfo)
                return object;
            let message = new $root.jlfc2.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlfc2.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlfc2.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jlfc2.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfc2.RoundInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i) {
                    if (typeof object.Mul[i] !== "object")
                        throw TypeError(".jlfc2.RoundInfo.Mul: object expected");
                    message.Mul[i] = $root.jlfc2.ColumnM.fromObject(object.Mul[i]);
                }
            }
            if (object.FinalSymbol) {
                if (!Array.isArray(object.FinalSymbol))
                    throw TypeError(".jlfc2.RoundInfo.FinalSymbol: array expected");
                message.FinalSymbol = [];
                for (let i = 0; i < object.FinalSymbol.length; ++i) {
                    if (typeof object.FinalSymbol[i] !== "object")
                        throw TypeError(".jlfc2.RoundInfo.FinalSymbol: object expected");
                    message.FinalSymbol[i] = $root.jlfc2.Column.fromObject(object.FinalSymbol[i]);
                }
            }
            if (object.FinalMul) {
                if (!Array.isArray(object.FinalMul))
                    throw TypeError(".jlfc2.RoundInfo.FinalMul: array expected");
                message.FinalMul = [];
                for (let i = 0; i < object.FinalMul.length; ++i) {
                    if (typeof object.FinalMul[i] !== "object")
                        throw TypeError(".jlfc2.RoundInfo.FinalMul: object expected");
                    message.FinalMul[i] = $root.jlfc2.ColumnM.fromObject(object.FinalMul[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfc2.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfc2.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfc2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.PotList) {
                if (!Array.isArray(object.PotList))
                    throw TypeError(".jlfc2.RoundInfo.PotList: array expected");
                message.PotList = [];
                for (let i = 0; i < object.PotList.length; ++i) {
                    if (typeof object.PotList[i] !== "object")
                        throw TypeError(".jlfc2.RoundInfo.PotList: object expected");
                    message.PotList[i] = $root.jlfc2.PotSymbol.fromObject(object.PotList[i]);
                }
            }
            if (object.BonusRemainRound != null)
                message.BonusRemainRound = object.BonusRemainRound | 0;
            if (object.BonusTotalRound != null)
                message.BonusTotalRound = object.BonusTotalRound | 0;
            if (object.GetBonus != null)
                message.GetBonus = Boolean(object.GetBonus);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.PlateChangeFlag != null)
                message.PlateChangeFlag = object.PlateChangeFlag | 0;
            if (object.ScatterMultiplier) {
                if (!Array.isArray(object.ScatterMultiplier))
                    throw TypeError(".jlfc2.RoundInfo.ScatterMultiplier: array expected");
                message.ScatterMultiplier = [];
                for (let i = 0; i < object.ScatterMultiplier.length; ++i)
                    message.ScatterMultiplier[i] = object.ScatterMultiplier[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {jlfc2.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
                object.FinalSymbol = [];
                object.FinalMul = [];
                object.AwardDataVec = [];
                object.PotList = [];
                object.ScatterMultiplier = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.BonusRemainRound = 0;
                object.BonusTotalRound = 0;
                object.GetBonus = false;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
                object.PlateChangeFlag = 0;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlfc2.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = $root.jlfc2.ColumnM.toObject(message.Mul[j], options);
            }
            if (message.FinalSymbol && message.FinalSymbol.length) {
                object.FinalSymbol = [];
                for (let j = 0; j < message.FinalSymbol.length; ++j)
                    object.FinalSymbol[j] = $root.jlfc2.Column.toObject(message.FinalSymbol[j], options);
            }
            if (message.FinalMul && message.FinalMul.length) {
                object.FinalMul = [];
                for (let j = 0; j < message.FinalMul.length; ++j)
                    object.FinalMul[j] = $root.jlfc2.ColumnM.toObject(message.FinalMul[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfc2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.PotList && message.PotList.length) {
                object.PotList = [];
                for (let j = 0; j < message.PotList.length; ++j)
                    object.PotList[j] = $root.jlfc2.PotSymbol.toObject(message.PotList[j], options);
            }
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                object.BonusRemainRound = message.BonusRemainRound;
            if (message.BonusTotalRound != null && message.hasOwnProperty("BonusTotalRound"))
                object.BonusTotalRound = message.BonusTotalRound;
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                object.GetBonus = message.GetBonus;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.PlateChangeFlag != null && message.hasOwnProperty("PlateChangeFlag"))
                object.PlateChangeFlag = message.PlateChangeFlag;
            if (message.ScatterMultiplier && message.ScatterMultiplier.length) {
                object.ScatterMultiplier = [];
                for (let j = 0; j < message.ScatterMultiplier.length; ++j)
                    object.ScatterMultiplier[j] = message.ScatterMultiplier[j];
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlfc2.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlfc2.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.RoundInfo";
        };

        return RoundInfo;
    })();

    jlfc2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfc2
         * @interface ISpinAck
         * @property {Array.<jlfc2.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfc2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfc2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlfc2.IRoundInfo>} RoundQueue
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {number} Extra
         * @memberof jlfc2.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfc2.SpinAck
         * @static
         * @param {jlfc2.ISpinAck=} [properties] Properties to set
         * @returns {jlfc2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfc2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.SpinAck
         * @static
         * @param {jlfc2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlfc2.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.RID);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfc2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.SpinAck
         * @static
         * @param {jlfc2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlfc2.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.RTP = reader.double();
                        break;
                    }
                case 5: {
                        message.LID = reader.int32();
                        break;
                    }
                case 6: {
                        message.RID = reader.int32();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.Extra = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfc2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlfc2.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.SpinAck)
                return object;
            let message = new $root.jlfc2.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlfc2.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlfc2.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlfc2.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.SpinAck
         * @static
         * @param {jlfc2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.AckType = 0;
                object.Extra = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlfc2.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfc2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfc2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.SpinAck";
        };

        return SpinAck;
    })();

    jlfc2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlfc2
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlfc2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlfc2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlfc2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlfc2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {jlfc2.IGameInfoData=} [properties] Properties to set
         * @returns {jlfc2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlfc2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {jlfc2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlfc2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {jlfc2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfc2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfc2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfc2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfc2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfc2.GameInfoData)
                return object;
            let message = new $root.jlfc2.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfc2.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {jlfc2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlfc2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlfc2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfc2.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlfc2;
})();

export const jlfff = $root.jlfff = (() => {

    /**
     * Namespace jlfff.
     * @exports jlfff
     * @namespace
     */
    const jlfff = {};

    jlfff.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfff
         * @interface ISpinAck
         * @property {Array.<number>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {number|null} [Award] SpinAck Award
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {number|null} [Money] SpinAck Money
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfff
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfff.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<number>} PlateSymbol
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck Award.
         * @member {number} Award
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.Award = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck Money.
         * @member {number} Money
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.Money = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfff.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfff.SpinAck
         * @static
         * @param {jlfff.ISpinAck=} [properties] Properties to set
         * @returns {jlfff.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfff.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfff.SpinAck
         * @static
         * @param {jlfff.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.int32(message.PlateSymbol[i]);
                writer.ldelim();
            }
            if (message.Award != null && Object.hasOwnProperty.call(message, "Award"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Award);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardType);
            if (message.Money != null && Object.hasOwnProperty.call(message, "Money"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Money);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfff.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfff.SpinAck
         * @static
         * @param {jlfff.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfff.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfff.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfff.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateSymbol.push(reader.int32());
                        } else
                            message.PlateSymbol.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.Award = reader.int32();
                        break;
                    }
                case 3: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 4: {
                        message.Money = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfff.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfff.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfff.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isInteger(message.PlateSymbol[i]))
                        return "PlateSymbol: integer[] expected";
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                if (!$util.isInteger(message.Award))
                    return "Award: integer expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.Money != null && message.hasOwnProperty("Money"))
                if (typeof message.Money !== "number")
                    return "Money: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfff.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfff.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfff.SpinAck)
                return object;
            let message = new $root.jlfff.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfff.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = object.PlateSymbol[i] | 0;
            }
            if (object.Award != null)
                message.Award = object.Award | 0;
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.Money != null)
                message.Money = Number(object.Money);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfff.SpinAck
         * @static
         * @param {jlfff.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateSymbol = [];
            if (options.defaults) {
                object.Award = 0;
                object.AwardType = 0;
                object.Money = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                object.Award = message.Award;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.Money != null && message.hasOwnProperty("Money"))
                object.Money = options.json && !isFinite(message.Money) ? String(message.Money) : message.Money;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfff.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfff.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfff.SpinAck";
        };

        return SpinAck;
    })();

    return jlfff;
})();

export const jlfg = $root.jlfg = (() => {

    /**
     * Namespace jlfg.
     * @exports jlfg
     * @namespace
     */
    const jlfg = {};

    jlfg.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfg
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfg
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfg.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfg.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfg.Column
         * @static
         * @param {jlfg.IColumn=} [properties] Properties to set
         * @returns {jlfg.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfg.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfg.Column
         * @static
         * @param {jlfg.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfg.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg.Column
         * @static
         * @param {jlfg.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfg.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg.Column)
                return object;
            let message = new $root.jlfg.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfg.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg.Column
         * @static
         * @param {jlfg.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfg.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfg.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg.Column";
        };

        return Column;
    })();

    jlfg.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfg
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfg
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfg.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfg.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlfg.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfg.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfg.AwardData
         * @static
         * @param {jlfg.IAwardData=} [properties] Properties to set
         * @returns {jlfg.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfg.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfg.AwardData
         * @static
         * @param {jlfg.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfg.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg.AwardData
         * @static
         * @param {jlfg.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfg.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg.AwardData)
                return object;
            let message = new $root.jlfg.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg.AwardData
         * @static
         * @param {jlfg.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfg.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfg.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg.AwardData";
        };

        return AwardData;
    })();

    jlfg.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfg
         * @interface ISpinAck
         * @property {Array.<jlfg.IColumn>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlfg.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [Mult] SpinAck Mult
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfg
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfg.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlfg.IColumn>} PlateSymbol
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlfg.IAwardData>} AwardDataVec
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfg.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfg.SpinAck
         * @static
         * @param {jlfg.ISpinAck=} [properties] Properties to set
         * @returns {jlfg.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfg.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfg.SpinAck
         * @static
         * @param {jlfg.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlfg.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfg.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfg.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg.SpinAck
         * @static
         * @param {jlfg.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlfg.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfg.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 10: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfg.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlfg.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfg.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg.SpinAck)
                return object;
            let message = new $root.jlfg.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfg.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlfg.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlfg.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfg.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfg.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfg.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg.SpinAck
         * @static
         * @param {jlfg.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.Mult = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.RTP = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlfg.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfg.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfg.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfg.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg.SpinAck";
        };

        return SpinAck;
    })();

    jlfg.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlfg
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlfg
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlfg.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlfg.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlfg.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlfg.GameInfoData
         * @static
         * @param {jlfg.IGameInfoData=} [properties] Properties to set
         * @returns {jlfg.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlfg.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlfg.GameInfoData
         * @static
         * @param {jlfg.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlfg.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg.GameInfoData
         * @static
         * @param {jlfg.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlfg.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg.GameInfoData)
                return object;
            let message = new $root.jlfg.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfg.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg.GameInfoData
         * @static
         * @param {jlfg.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlfg.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlfg.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlfg;
})();

export const jlfg2 = $root.jlfg2 = (() => {

    /**
     * Namespace jlfg2.
     * @exports jlfg2
     * @namespace
     */
    const jlfg2 = {};

    jlfg2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfg2
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfg2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfg2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfg2.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfg2.Column
         * @static
         * @param {jlfg2.IColumn=} [properties] Properties to set
         * @returns {jlfg2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfg2.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfg2.Column
         * @static
         * @param {jlfg2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfg2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg2.Column
         * @static
         * @param {jlfg2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfg2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg2.Column)
                return object;
            let message = new $root.jlfg2.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfg2.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg2.Column
         * @static
         * @param {jlfg2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfg2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfg2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg2.Column";
        };

        return Column;
    })();

    jlfg2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfg2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfg2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfg2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfg2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlfg2.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfg2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfg2.AwardData
         * @static
         * @param {jlfg2.IAwardData=} [properties] Properties to set
         * @returns {jlfg2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfg2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfg2.AwardData
         * @static
         * @param {jlfg2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfg2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg2.AwardData
         * @static
         * @param {jlfg2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfg2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg2.AwardData)
                return object;
            let message = new $root.jlfg2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg2.AwardData
         * @static
         * @param {jlfg2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfg2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfg2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg2.AwardData";
        };

        return AwardData;
    })();

    jlfg2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfg2
         * @interface ISpinAck
         * @property {Array.<jlfg2.IColumn>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlfg2.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [LineMult] SpinAck LineMult
         * @property {number|null} [LineWin] SpinAck LineWin
         * @property {number|null} [WheelOdd] SpinAck WheelOdd
         * @property {number|null} [WheelMult] SpinAck WheelMult
         * @property {number|null} [WheelWin] SpinAck WheelWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {Array.<number>|null} [MultShowArray] SpinAck MultShowArray
         * @property {Array.<number>|null} [LightOnPos] SpinAck LightOnPos
         * @property {number|null} [Bet] SpinAck Bet
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {boolean|null} [IsBonus] SpinAck IsBonus
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfg2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfg2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.MultShowArray = [];
            this.LightOnPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlfg2.IColumn>} PlateSymbol
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlfg2.IAwardData>} AwardDataVec
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck LineMult.
         * @member {number} LineMult
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.LineMult = 0;

        /**
         * SpinAck LineWin.
         * @member {number} LineWin
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.LineWin = 0;

        /**
         * SpinAck WheelOdd.
         * @member {number} WheelOdd
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelOdd = 0;

        /**
         * SpinAck WheelMult.
         * @member {number} WheelMult
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelMult = 0;

        /**
         * SpinAck WheelWin.
         * @member {number} WheelWin
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck MultShowArray.
         * @member {Array.<number>} MultShowArray
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.MultShowArray = $util.emptyArray;

        /**
         * SpinAck LightOnPos.
         * @member {Array.<number>} LightOnPos
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.LightOnPos = $util.emptyArray;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck IsBonus.
         * @member {boolean} IsBonus
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.IsBonus = false;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfg2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfg2.SpinAck
         * @static
         * @param {jlfg2.ISpinAck=} [properties] Properties to set
         * @returns {jlfg2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfg2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfg2.SpinAck
         * @static
         * @param {jlfg2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlfg2.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfg2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.LineMult != null && Object.hasOwnProperty.call(message, "LineMult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.LineMult);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.LineWin);
            if (message.WheelOdd != null && Object.hasOwnProperty.call(message, "WheelOdd"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.WheelOdd);
            if (message.WheelMult != null && Object.hasOwnProperty.call(message, "WheelMult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.WheelMult);
            if (message.WheelWin != null && Object.hasOwnProperty.call(message, "WheelWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.WheelWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.TotalWin);
            if (message.MultShowArray != null && message.MultShowArray.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.MultShowArray.length; ++i)
                    writer.double(message.MultShowArray[i]);
                writer.ldelim();
            }
            if (message.LightOnPos != null && message.LightOnPos.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.LightOnPos.length; ++i)
                    writer.int32(message.LightOnPos[i]);
                writer.ldelim();
            }
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.Bet);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.Extra);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 15, wireType 1 =*/121).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.AckType);
            if (message.IsBonus != null && Object.hasOwnProperty.call(message, "IsBonus"))
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.IsBonus);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfg2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg2.SpinAck
         * @static
         * @param {jlfg2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlfg2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfg2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.LineMult = reader.double();
                        break;
                    }
                case 5: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 6: {
                        message.WheelOdd = reader.double();
                        break;
                    }
                case 7: {
                        message.WheelMult = reader.double();
                        break;
                    }
                case 8: {
                        message.WheelWin = reader.double();
                        break;
                    }
                case 9: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 10: {
                        if (!(message.MultShowArray && message.MultShowArray.length))
                            message.MultShowArray = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.MultShowArray.push(reader.double());
                        } else
                            message.MultShowArray.push(reader.double());
                        break;
                    }
                case 11: {
                        if (!(message.LightOnPos && message.LightOnPos.length))
                            message.LightOnPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.LightOnPos.push(reader.int32());
                        } else
                            message.LightOnPos.push(reader.int32());
                        break;
                    }
                case 12: {
                        message.Bet = reader.double();
                        break;
                    }
                case 13: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 17: {
                        message.IsBonus = reader.bool();
                        break;
                    }
                case 14: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 15: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 16: {
                        message.AckType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfg2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlfg2.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfg2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.LineMult != null && message.hasOwnProperty("LineMult"))
                if (typeof message.LineMult !== "number")
                    return "LineMult: number expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.WheelOdd != null && message.hasOwnProperty("WheelOdd"))
                if (typeof message.WheelOdd !== "number")
                    return "WheelOdd: number expected";
            if (message.WheelMult != null && message.hasOwnProperty("WheelMult"))
                if (typeof message.WheelMult !== "number")
                    return "WheelMult: number expected";
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                if (typeof message.WheelWin !== "number")
                    return "WheelWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.MultShowArray != null && message.hasOwnProperty("MultShowArray")) {
                if (!Array.isArray(message.MultShowArray))
                    return "MultShowArray: array expected";
                for (let i = 0; i < message.MultShowArray.length; ++i)
                    if (typeof message.MultShowArray[i] !== "number")
                        return "MultShowArray: number[] expected";
            }
            if (message.LightOnPos != null && message.hasOwnProperty("LightOnPos")) {
                if (!Array.isArray(message.LightOnPos))
                    return "LightOnPos: array expected";
                for (let i = 0; i < message.LightOnPos.length; ++i)
                    if (!$util.isInteger(message.LightOnPos[i]))
                        return "LightOnPos: integer[] expected";
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                if (typeof message.IsBonus !== "boolean")
                    return "IsBonus: boolean expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg2.SpinAck)
                return object;
            let message = new $root.jlfg2.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfg2.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlfg2.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlfg2.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfg2.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfg2.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfg2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.LineMult != null)
                message.LineMult = Number(object.LineMult);
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.WheelOdd != null)
                message.WheelOdd = Number(object.WheelOdd);
            if (object.WheelMult != null)
                message.WheelMult = Number(object.WheelMult);
            if (object.WheelWin != null)
                message.WheelWin = Number(object.WheelWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.MultShowArray) {
                if (!Array.isArray(object.MultShowArray))
                    throw TypeError(".jlfg2.SpinAck.MultShowArray: array expected");
                message.MultShowArray = [];
                for (let i = 0; i < object.MultShowArray.length; ++i)
                    message.MultShowArray[i] = Number(object.MultShowArray[i]);
            }
            if (object.LightOnPos) {
                if (!Array.isArray(object.LightOnPos))
                    throw TypeError(".jlfg2.SpinAck.LightOnPos: array expected");
                message.LightOnPos = [];
                for (let i = 0; i < object.LightOnPos.length; ++i)
                    message.LightOnPos[i] = object.LightOnPos[i] | 0;
            }
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.IsBonus != null)
                message.IsBonus = Boolean(object.IsBonus);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg2.SpinAck
         * @static
         * @param {jlfg2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.MultShowArray = [];
                object.LightOnPos = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.LineMult = 0;
                object.LineWin = 0;
                object.WheelOdd = 0;
                object.WheelMult = 0;
                object.WheelWin = 0;
                object.TotalWin = 0;
                object.Bet = 0;
                object.Extra = false;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.IsBonus = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlfg2.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfg2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.LineMult != null && message.hasOwnProperty("LineMult"))
                object.LineMult = options.json && !isFinite(message.LineMult) ? String(message.LineMult) : message.LineMult;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.WheelOdd != null && message.hasOwnProperty("WheelOdd"))
                object.WheelOdd = options.json && !isFinite(message.WheelOdd) ? String(message.WheelOdd) : message.WheelOdd;
            if (message.WheelMult != null && message.hasOwnProperty("WheelMult"))
                object.WheelMult = options.json && !isFinite(message.WheelMult) ? String(message.WheelMult) : message.WheelMult;
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                object.WheelWin = options.json && !isFinite(message.WheelWin) ? String(message.WheelWin) : message.WheelWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.MultShowArray && message.MultShowArray.length) {
                object.MultShowArray = [];
                for (let j = 0; j < message.MultShowArray.length; ++j)
                    object.MultShowArray[j] = options.json && !isFinite(message.MultShowArray[j]) ? String(message.MultShowArray[j]) : message.MultShowArray[j];
            }
            if (message.LightOnPos && message.LightOnPos.length) {
                object.LightOnPos = [];
                for (let j = 0; j < message.LightOnPos.length; ++j)
                    object.LightOnPos[j] = message.LightOnPos[j];
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                object.IsBonus = message.IsBonus;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfg2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfg2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg2.SpinAck";
        };

        return SpinAck;
    })();

    jlfg2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlfg2
         * @interface IGameInfoData
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [WheelOdds] GameInfoData WheelOdds
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlfg2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlfg2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.WheelOdds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlfg2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData WheelOdds.
         * @member {Array.<number>} WheelOdds
         * @memberof jlfg2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.WheelOdds = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {jlfg2.IGameInfoData=} [properties] Properties to set
         * @returns {jlfg2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlfg2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {jlfg2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.WheelOdds != null && message.WheelOdds.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.WheelOdds.length; ++i)
                    writer.double(message.WheelOdds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlfg2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {jlfg2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 2: {
                        if (!(message.WheelOdds && message.WheelOdds.length))
                            message.WheelOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WheelOdds.push(reader.double());
                        } else
                            message.WheelOdds.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.WheelOdds != null && message.hasOwnProperty("WheelOdds")) {
                if (!Array.isArray(message.WheelOdds))
                    return "WheelOdds: array expected";
                for (let i = 0; i < message.WheelOdds.length; ++i)
                    if (typeof message.WheelOdds[i] !== "number")
                        return "WheelOdds: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg2.GameInfoData)
                return object;
            let message = new $root.jlfg2.GameInfoData();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfg2.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.WheelOdds) {
                if (!Array.isArray(object.WheelOdds))
                    throw TypeError(".jlfg2.GameInfoData.WheelOdds: array expected");
                message.WheelOdds = [];
                for (let i = 0; i < object.WheelOdds.length; ++i)
                    message.WheelOdds[i] = Number(object.WheelOdds[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {jlfg2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.WheelOdds = [];
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.WheelOdds && message.WheelOdds.length) {
                object.WheelOdds = [];
                for (let j = 0; j < message.WheelOdds.length; ++j)
                    object.WheelOdds[j] = options.json && !isFinite(message.WheelOdds[j]) ? String(message.WheelOdds[j]) : message.WheelOdds[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlfg2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlfg2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg2.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlfg2;
})();

export const jlfg3 = $root.jlfg3 = (() => {

    /**
     * Namespace jlfg3.
     * @exports jlfg3
     * @namespace
     */
    const jlfg3 = {};

    jlfg3.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfg3
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfg3
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfg3.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfg3.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfg3.Column
         * @static
         * @param {jlfg3.IColumn=} [properties] Properties to set
         * @returns {jlfg3.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfg3.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfg3.Column
         * @static
         * @param {jlfg3.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfg3.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg3.Column
         * @static
         * @param {jlfg3.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg3.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg3.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg3.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg3.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg3.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfg3.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg3.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg3.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg3.Column)
                return object;
            let message = new $root.jlfg3.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfg3.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg3.Column
         * @static
         * @param {jlfg3.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfg3.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfg3.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg3.Column";
        };

        return Column;
    })();

    jlfg3.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfg3
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfg3
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfg3.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfg3.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlfg3.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfg3.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlfg3.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfg3.AwardData
         * @static
         * @param {jlfg3.IAwardData=} [properties] Properties to set
         * @returns {jlfg3.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfg3.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfg3.AwardData
         * @static
         * @param {jlfg3.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfg3.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg3.AwardData
         * @static
         * @param {jlfg3.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg3.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg3.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg3.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg3.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg3.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfg3.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg3.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg3.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg3.AwardData)
                return object;
            let message = new $root.jlfg3.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg3.AwardData
         * @static
         * @param {jlfg3.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfg3.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfg3.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg3.AwardData";
        };

        return AwardData;
    })();

    jlfg3.SplitInfo = (function() {

        /**
         * Properties of a SplitInfo.
         * @memberof jlfg3
         * @interface ISplitInfo
         * @property {number|null} [Pos] SplitInfo Pos
         * @property {number|null} [Level] SplitInfo Level
         */

        /**
         * Constructs a new SplitInfo.
         * @memberof jlfg3
         * @classdesc Represents a SplitInfo.
         * @implements ISplitInfo
         * @constructor
         * @param {jlfg3.ISplitInfo=} [properties] Properties to set
         */
        function SplitInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SplitInfo Pos.
         * @member {number} Pos
         * @memberof jlfg3.SplitInfo
         * @instance
         */
        SplitInfo.prototype.Pos = 0;

        /**
         * SplitInfo Level.
         * @member {number} Level
         * @memberof jlfg3.SplitInfo
         * @instance
         */
        SplitInfo.prototype.Level = 0;

        /**
         * Creates a new SplitInfo instance using the specified properties.
         * @function create
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {jlfg3.ISplitInfo=} [properties] Properties to set
         * @returns {jlfg3.SplitInfo} SplitInfo instance
         */
        SplitInfo.create = function create(properties) {
            return new SplitInfo(properties);
        };

        /**
         * Encodes the specified SplitInfo message. Does not implicitly {@link jlfg3.SplitInfo.verify|verify} messages.
         * @function encode
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {jlfg3.ISplitInfo} message SplitInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SplitInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Level);
            return writer;
        };

        /**
         * Encodes the specified SplitInfo message, length delimited. Does not implicitly {@link jlfg3.SplitInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {jlfg3.ISplitInfo} message SplitInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SplitInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SplitInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg3.SplitInfo} SplitInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SplitInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg3.SplitInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SplitInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg3.SplitInfo} SplitInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SplitInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SplitInfo message.
         * @function verify
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SplitInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Level != null && message.hasOwnProperty("Level"))
                if (!$util.isInteger(message.Level))
                    return "Level: integer expected";
            return null;
        };

        /**
         * Creates a SplitInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg3.SplitInfo} SplitInfo
         */
        SplitInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg3.SplitInfo)
                return object;
            let message = new $root.jlfg3.SplitInfo();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Level != null)
                message.Level = object.Level | 0;
            return message;
        };

        /**
         * Creates a plain object from a SplitInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {jlfg3.SplitInfo} message SplitInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SplitInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Level = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Level != null && message.hasOwnProperty("Level"))
                object.Level = message.Level;
            return object;
        };

        /**
         * Converts this SplitInfo to JSON.
         * @function toJSON
         * @memberof jlfg3.SplitInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SplitInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SplitInfo
         * @function getTypeUrl
         * @memberof jlfg3.SplitInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SplitInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg3.SplitInfo";
        };

        return SplitInfo;
    })();

    jlfg3.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfg3
         * @interface ISpinAck
         * @property {Array.<jlfg3.IColumn>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlfg3.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {Array.<jlfg3.ISplitInfo>|null} [Split] SpinAck Split
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [ReelMult] SpinAck ReelMult
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {Array.<jlfg3.IAwardData>|null} [PreAwardDataVec] SpinAck PreAwardDataVec
         * @property {number|null} [PreTotalWin] SpinAck PreTotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {Array.<jlfg3.IColumn>|null} [PlateSymbolLog] SpinAck PlateSymbolLog
         * @property {boolean|null} [IsBonus] SpinAck IsBonus
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfg3
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfg3.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.Split = [];
            this.PreAwardDataVec = [];
            this.PlateSymbolLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlfg3.IColumn>} PlateSymbol
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlfg3.IAwardData>} AwardDataVec
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck Split.
         * @member {Array.<jlfg3.ISplitInfo>} Split
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.Split = $util.emptyArray;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck ReelMult.
         * @member {number} ReelMult
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.ReelMult = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck PreAwardDataVec.
         * @member {Array.<jlfg3.IAwardData>} PreAwardDataVec
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.PreAwardDataVec = $util.emptyArray;

        /**
         * SpinAck PreTotalWin.
         * @member {number} PreTotalWin
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.PreTotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck PlateSymbolLog.
         * @member {Array.<jlfg3.IColumn>} PlateSymbolLog
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * SpinAck IsBonus.
         * @member {boolean} IsBonus
         * @memberof jlfg3.SpinAck
         * @instance
         */
        SpinAck.prototype.IsBonus = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfg3.SpinAck
         * @static
         * @param {jlfg3.ISpinAck=} [properties] Properties to set
         * @returns {jlfg3.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfg3.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfg3.SpinAck
         * @static
         * @param {jlfg3.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlfg3.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfg3.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.Split != null && message.Split.length)
                for (let i = 0; i < message.Split.length; ++i)
                    $root.jlfg3.SplitInfo.encode(message.Split[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.ReelMult != null && Object.hasOwnProperty.call(message, "ReelMult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.ReelMult);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.Extra);
            if (message.PreAwardDataVec != null && message.PreAwardDataVec.length)
                for (let i = 0; i < message.PreAwardDataVec.length; ++i)
                    $root.jlfg3.AwardData.encode(message.PreAwardDataVec[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.PreTotalWin != null && Object.hasOwnProperty.call(message, "PreTotalWin"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.PreTotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.RTP);
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlfg3.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 14, wireType 2 =*/114).fork()).ldelim();
            if (message.IsBonus != null && Object.hasOwnProperty.call(message, "IsBonus"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.IsBonus);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfg3.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg3.SpinAck
         * @static
         * @param {jlfg3.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg3.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg3.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg3.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlfg3.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfg3.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.Split && message.Split.length))
                            message.Split = [];
                        message.Split.push($root.jlfg3.SplitInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.ReelMult = reader.double();
                        break;
                    }
                case 6: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 7: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 8: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 9: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 10: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 11: {
                        if (!(message.PreAwardDataVec && message.PreAwardDataVec.length))
                            message.PreAwardDataVec = [];
                        message.PreAwardDataVec.push($root.jlfg3.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 12: {
                        message.PreTotalWin = reader.double();
                        break;
                    }
                case 13: {
                        message.RTP = reader.double();
                        break;
                    }
                case 14: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlfg3.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 15: {
                        message.IsBonus = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg3.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg3.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfg3.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlfg3.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfg3.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.Split != null && message.hasOwnProperty("Split")) {
                if (!Array.isArray(message.Split))
                    return "Split: array expected";
                for (let i = 0; i < message.Split.length; ++i) {
                    let error = $root.jlfg3.SplitInfo.verify(message.Split[i]);
                    if (error)
                        return "Split." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.ReelMult != null && message.hasOwnProperty("ReelMult"))
                if (typeof message.ReelMult !== "number")
                    return "ReelMult: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.PreAwardDataVec != null && message.hasOwnProperty("PreAwardDataVec")) {
                if (!Array.isArray(message.PreAwardDataVec))
                    return "PreAwardDataVec: array expected";
                for (let i = 0; i < message.PreAwardDataVec.length; ++i) {
                    let error = $root.jlfg3.AwardData.verify(message.PreAwardDataVec[i]);
                    if (error)
                        return "PreAwardDataVec." + error;
                }
            }
            if (message.PreTotalWin != null && message.hasOwnProperty("PreTotalWin"))
                if (typeof message.PreTotalWin !== "number")
                    return "PreTotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlfg3.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                if (typeof message.IsBonus !== "boolean")
                    return "IsBonus: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg3.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg3.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg3.SpinAck)
                return object;
            let message = new $root.jlfg3.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfg3.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlfg3.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlfg3.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfg3.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfg3.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfg3.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.Split) {
                if (!Array.isArray(object.Split))
                    throw TypeError(".jlfg3.SpinAck.Split: array expected");
                message.Split = [];
                for (let i = 0; i < object.Split.length; ++i) {
                    if (typeof object.Split[i] !== "object")
                        throw TypeError(".jlfg3.SpinAck.Split: object expected");
                    message.Split[i] = $root.jlfg3.SplitInfo.fromObject(object.Split[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.ReelMult != null)
                message.ReelMult = Number(object.ReelMult);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.PreAwardDataVec) {
                if (!Array.isArray(object.PreAwardDataVec))
                    throw TypeError(".jlfg3.SpinAck.PreAwardDataVec: array expected");
                message.PreAwardDataVec = [];
                for (let i = 0; i < object.PreAwardDataVec.length; ++i) {
                    if (typeof object.PreAwardDataVec[i] !== "object")
                        throw TypeError(".jlfg3.SpinAck.PreAwardDataVec: object expected");
                    message.PreAwardDataVec[i] = $root.jlfg3.AwardData.fromObject(object.PreAwardDataVec[i]);
                }
            }
            if (object.PreTotalWin != null)
                message.PreTotalWin = Number(object.PreTotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlfg3.SpinAck.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlfg3.SpinAck.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlfg3.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            if (object.IsBonus != null)
                message.IsBonus = Boolean(object.IsBonus);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg3.SpinAck
         * @static
         * @param {jlfg3.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.Split = [];
                object.PreAwardDataVec = [];
                object.PlateSymbolLog = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.ReelMult = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.PreTotalWin = 0;
                object.RTP = 0;
                object.IsBonus = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlfg3.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfg3.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.Split && message.Split.length) {
                object.Split = [];
                for (let j = 0; j < message.Split.length; ++j)
                    object.Split[j] = $root.jlfg3.SplitInfo.toObject(message.Split[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.ReelMult != null && message.hasOwnProperty("ReelMult"))
                object.ReelMult = options.json && !isFinite(message.ReelMult) ? String(message.ReelMult) : message.ReelMult;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.PreAwardDataVec && message.PreAwardDataVec.length) {
                object.PreAwardDataVec = [];
                for (let j = 0; j < message.PreAwardDataVec.length; ++j)
                    object.PreAwardDataVec[j] = $root.jlfg3.AwardData.toObject(message.PreAwardDataVec[j], options);
            }
            if (message.PreTotalWin != null && message.hasOwnProperty("PreTotalWin"))
                object.PreTotalWin = options.json && !isFinite(message.PreTotalWin) ? String(message.PreTotalWin) : message.PreTotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlfg3.Column.toObject(message.PlateSymbolLog[j], options);
            }
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                object.IsBonus = message.IsBonus;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfg3.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfg3.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg3.SpinAck";
        };

        return SpinAck;
    })();

    jlfg3.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlfg3
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlfg3
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlfg3.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlfg3.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlfg3.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {jlfg3.IGameInfoData=} [properties] Properties to set
         * @returns {jlfg3.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlfg3.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {jlfg3.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlfg3.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {jlfg3.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg3.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg3.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg3.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg3.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg3.GameInfoData)
                return object;
            let message = new $root.jlfg3.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfg3.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {jlfg3.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlfg3.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlfg3.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg3.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlfg3;
})();

export const jlfg5 = $root.jlfg5 = (() => {

    /**
     * Namespace jlfg5.
     * @exports jlfg5
     * @namespace
     */
    const jlfg5 = {};

    jlfg5.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfg5
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfg5
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfg5.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfg5.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlfg5.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfg5.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfg5.AwardData
         * @static
         * @param {jlfg5.IAwardData=} [properties] Properties to set
         * @returns {jlfg5.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfg5.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfg5.AwardData
         * @static
         * @param {jlfg5.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfg5.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg5.AwardData
         * @static
         * @param {jlfg5.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg5.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg5.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg5.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg5.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg5.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfg5.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg5.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg5.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg5.AwardData)
                return object;
            let message = new $root.jlfg5.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg5.AwardData
         * @static
         * @param {jlfg5.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfg5.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfg5.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg5.AwardData";
        };

        return AwardData;
    })();

    jlfg5.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfg5
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfg5
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfg5.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfg5.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfg5.Column
         * @static
         * @param {jlfg5.IColumn=} [properties] Properties to set
         * @returns {jlfg5.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfg5.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfg5.Column
         * @static
         * @param {jlfg5.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfg5.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg5.Column
         * @static
         * @param {jlfg5.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg5.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg5.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg5.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg5.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg5.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfg5.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg5.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg5.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg5.Column)
                return object;
            let message = new $root.jlfg5.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfg5.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg5.Column
         * @static
         * @param {jlfg5.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfg5.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfg5.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg5.Column";
        };

        return Column;
    })();

    jlfg5.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfg5
         * @interface ISpinAck
         * @property {Array.<jlfg5.IColumn>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlfg5.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [Mult] SpinAck Mult
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         * @property {Array.<number>|null} [NudgeSymbolList] SpinAck NudgeSymbolList
         * @property {Array.<number>|null} [MultList] SpinAck MultList
         * @property {boolean|null} [MaxFlag] SpinAck MaxFlag
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfg5
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfg5.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.NudgeSymbolList = [];
            this.MultList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlfg5.IColumn>} PlateSymbol
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlfg5.IAwardData>} AwardDataVec
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {number} Extra
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck NudgeSymbolList.
         * @member {Array.<number>} NudgeSymbolList
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.NudgeSymbolList = $util.emptyArray;

        /**
         * SpinAck MultList.
         * @member {Array.<number>} MultList
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.MultList = $util.emptyArray;

        /**
         * SpinAck MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.MaxFlag = false;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlfg5.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfg5.SpinAck
         * @static
         * @param {jlfg5.ISpinAck=} [properties] Properties to set
         * @returns {jlfg5.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfg5.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfg5.SpinAck
         * @static
         * @param {jlfg5.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlfg5.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfg5.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.RTP);
            if (message.NudgeSymbolList != null && message.NudgeSymbolList.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.NudgeSymbolList.length; ++i)
                    writer.int32(message.NudgeSymbolList[i]);
                writer.ldelim();
            }
            if (message.MultList != null && message.MultList.length) {
                writer.uint32(/* id 12, wireType 2 =*/98).fork();
                for (let i = 0; i < message.MultList.length; ++i)
                    writer.double(message.MultList[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.MaxFlag);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfg5.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg5.SpinAck
         * @static
         * @param {jlfg5.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg5.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg5.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg5.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlfg5.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfg5.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.Extra = reader.int32();
                        break;
                    }
                case 10: {
                        message.RTP = reader.double();
                        break;
                    }
                case 11: {
                        if (!(message.NudgeSymbolList && message.NudgeSymbolList.length))
                            message.NudgeSymbolList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NudgeSymbolList.push(reader.int32());
                        } else
                            message.NudgeSymbolList.push(reader.int32());
                        break;
                    }
                case 12: {
                        if (!(message.MultList && message.MultList.length))
                            message.MultList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.MultList.push(reader.double());
                        } else
                            message.MultList.push(reader.double());
                        break;
                    }
                case 13: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 14: {
                        message.LID = reader.int32();
                        break;
                    }
                case 15: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg5.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg5.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfg5.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlfg5.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfg5.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.NudgeSymbolList != null && message.hasOwnProperty("NudgeSymbolList")) {
                if (!Array.isArray(message.NudgeSymbolList))
                    return "NudgeSymbolList: array expected";
                for (let i = 0; i < message.NudgeSymbolList.length; ++i)
                    if (!$util.isInteger(message.NudgeSymbolList[i]))
                        return "NudgeSymbolList: integer[] expected";
            }
            if (message.MultList != null && message.hasOwnProperty("MultList")) {
                if (!Array.isArray(message.MultList))
                    return "MultList: array expected";
                for (let i = 0; i < message.MultList.length; ++i)
                    if (typeof message.MultList[i] !== "number")
                        return "MultList: number[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg5.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg5.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg5.SpinAck)
                return object;
            let message = new $root.jlfg5.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfg5.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlfg5.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlfg5.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfg5.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfg5.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfg5.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.NudgeSymbolList) {
                if (!Array.isArray(object.NudgeSymbolList))
                    throw TypeError(".jlfg5.SpinAck.NudgeSymbolList: array expected");
                message.NudgeSymbolList = [];
                for (let i = 0; i < object.NudgeSymbolList.length; ++i)
                    message.NudgeSymbolList[i] = object.NudgeSymbolList[i] | 0;
            }
            if (object.MultList) {
                if (!Array.isArray(object.MultList))
                    throw TypeError(".jlfg5.SpinAck.MultList: array expected");
                message.MultList = [];
                for (let i = 0; i < object.MultList.length; ++i)
                    message.MultList[i] = Number(object.MultList[i]);
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg5.SpinAck
         * @static
         * @param {jlfg5.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.NudgeSymbolList = [];
                object.MultList = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.Mult = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = 0;
                object.RTP = 0;
                object.MaxFlag = false;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlfg5.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfg5.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.NudgeSymbolList && message.NudgeSymbolList.length) {
                object.NudgeSymbolList = [];
                for (let j = 0; j < message.NudgeSymbolList.length; ++j)
                    object.NudgeSymbolList[j] = message.NudgeSymbolList[j];
            }
            if (message.MultList && message.MultList.length) {
                object.MultList = [];
                for (let j = 0; j < message.MultList.length; ++j)
                    object.MultList[j] = options.json && !isFinite(message.MultList[j]) ? String(message.MultList[j]) : message.MultList[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfg5.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfg5.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg5.SpinAck";
        };

        return SpinAck;
    })();

    jlfg5.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlfg5
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlfg5
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlfg5.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlfg5.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlfg5.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {jlfg5.IGameInfoData=} [properties] Properties to set
         * @returns {jlfg5.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlfg5.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {jlfg5.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlfg5.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {jlfg5.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfg5.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfg5.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfg5.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfg5.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfg5.GameInfoData)
                return object;
            let message = new $root.jlfg5.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfg5.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {jlfg5.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlfg5.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlfg5.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfg5.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlfg5;
})();

export const jlfm = $root.jlfm = (() => {

    /**
     * Namespace jlfm.
     * @exports jlfm
     * @namespace
     */
    const jlfm = {};

    jlfm.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfm
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlfm
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfm.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlfm.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfm.Column
         * @static
         * @param {jlfm.IColumn=} [properties] Properties to set
         * @returns {jlfm.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfm.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfm.Column
         * @static
         * @param {jlfm.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfm.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfm.Column
         * @static
         * @param {jlfm.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfm.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfm.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfm.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfm.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfm.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfm.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfm.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfm.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfm.Column)
                return object;
            let message = new $root.jlfm.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlfm.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfm.Column
         * @static
         * @param {jlfm.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfm.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfm.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfm.Column";
        };

        return Column;
    })();

    jlfm.Single = (function() {

        /**
         * Properties of a Single.
         * @memberof jlfm
         * @interface ISingle
         * @property {Array.<jlfm.IColumn>|null} [Col] Single Col
         */

        /**
         * Constructs a new Single.
         * @memberof jlfm
         * @classdesc Represents a Single.
         * @implements ISingle
         * @constructor
         * @param {jlfm.ISingle=} [properties] Properties to set
         */
        function Single(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Single Col.
         * @member {Array.<jlfm.IColumn>} Col
         * @memberof jlfm.Single
         * @instance
         */
        Single.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Single instance using the specified properties.
         * @function create
         * @memberof jlfm.Single
         * @static
         * @param {jlfm.ISingle=} [properties] Properties to set
         * @returns {jlfm.Single} Single instance
         */
        Single.create = function create(properties) {
            return new Single(properties);
        };

        /**
         * Encodes the specified Single message. Does not implicitly {@link jlfm.Single.verify|verify} messages.
         * @function encode
         * @memberof jlfm.Single
         * @static
         * @param {jlfm.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jlfm.Column.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Single message, length delimited. Does not implicitly {@link jlfm.Single.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfm.Single
         * @static
         * @param {jlfm.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Single message from the specified reader or buffer.
         * @function decode
         * @memberof jlfm.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfm.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfm.Single();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jlfm.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Single message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfm.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfm.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Single message.
         * @function verify
         * @memberof jlfm.Single
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Single.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jlfm.Column.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Single message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfm.Single
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfm.Single} Single
         */
        Single.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfm.Single)
                return object;
            let message = new $root.jlfm.Single();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfm.Single.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jlfm.Single.Col: object expected");
                    message.Col[i] = $root.jlfm.Column.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Single message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfm.Single
         * @static
         * @param {jlfm.Single} message Single
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Single.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jlfm.Column.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this Single to JSON.
         * @function toJSON
         * @memberof jlfm.Single
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Single.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Single
         * @function getTypeUrl
         * @memberof jlfm.Single
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Single.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfm.Single";
        };

        return Single;
    })();

    jlfm.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfm
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfm
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfm.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfm.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlfm.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfm.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfm.AwardData
         * @static
         * @param {jlfm.IAwardData=} [properties] Properties to set
         * @returns {jlfm.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfm.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfm.AwardData
         * @static
         * @param {jlfm.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfm.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfm.AwardData
         * @static
         * @param {jlfm.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfm.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfm.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfm.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfm.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfm.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfm.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfm.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfm.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfm.AwardData)
                return object;
            let message = new $root.jlfm.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfm.AwardData
         * @static
         * @param {jlfm.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfm.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfm.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfm.AwardData";
        };

        return AwardData;
    })();

    jlfm.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfm
         * @interface ISpinAck
         * @property {Array.<jlfm.ISingle>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlfm.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [Mult] SpinAck Mult
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfm
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfm.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlfm.ISingle>} PlateSymbol
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlfm.IAwardData>} AwardDataVec
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlfm.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfm.SpinAck
         * @static
         * @param {jlfm.ISpinAck=} [properties] Properties to set
         * @returns {jlfm.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfm.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfm.SpinAck
         * @static
         * @param {jlfm.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlfm.Single.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfm.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfm.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfm.SpinAck
         * @static
         * @param {jlfm.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfm.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfm.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfm.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlfm.Single.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfm.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfm.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfm.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfm.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlfm.Single.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfm.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfm.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfm.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfm.SpinAck)
                return object;
            let message = new $root.jlfm.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfm.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlfm.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlfm.Single.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfm.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfm.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfm.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfm.SpinAck
         * @static
         * @param {jlfm.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.Mult = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlfm.Single.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfm.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfm.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfm.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfm.SpinAck";
        };

        return SpinAck;
    })();

    jlfm.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlfm
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlfm
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlfm.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlfm.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {jlfm.IGameInfoAck=} [properties] Properties to set
         * @returns {jlfm.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlfm.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {jlfm.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlfm.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {jlfm.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfm.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfm.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfm.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfm.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfm.GameInfoAck)
                return object;
            let message = new $root.jlfm.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlfm.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {jlfm.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlfm.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlfm.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfm.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlfm;
})();

export const jlfp = $root.jlfp = (() => {

    /**
     * Namespace jlfp.
     * @exports jlfp
     * @namespace
     */
    const jlfp = {};

    jlfp.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfp
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlfp
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfp.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlfp.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfp.Column
         * @static
         * @param {jlfp.IColumn=} [properties] Properties to set
         * @returns {jlfp.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfp.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfp.Column
         * @static
         * @param {jlfp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfp.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.Column
         * @static
         * @param {jlfp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfp.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.Column)
                return object;
            let message = new $root.jlfp.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlfp.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.Column
         * @static
         * @param {jlfp.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfp.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfp.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.Column";
        };

        return Column;
    })();

    jlfp.ColumnF = (function() {

        /**
         * Properties of a ColumnF.
         * @memberof jlfp
         * @interface IColumnF
         * @property {Array.<number>|null} [RowF] ColumnF RowF
         */

        /**
         * Constructs a new ColumnF.
         * @memberof jlfp
         * @classdesc Represents a ColumnF.
         * @implements IColumnF
         * @constructor
         * @param {jlfp.IColumnF=} [properties] Properties to set
         */
        function ColumnF(properties) {
            this.RowF = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnF RowF.
         * @member {Array.<number>} RowF
         * @memberof jlfp.ColumnF
         * @instance
         */
        ColumnF.prototype.RowF = $util.emptyArray;

        /**
         * Creates a new ColumnF instance using the specified properties.
         * @function create
         * @memberof jlfp.ColumnF
         * @static
         * @param {jlfp.IColumnF=} [properties] Properties to set
         * @returns {jlfp.ColumnF} ColumnF instance
         */
        ColumnF.create = function create(properties) {
            return new ColumnF(properties);
        };

        /**
         * Encodes the specified ColumnF message. Does not implicitly {@link jlfp.ColumnF.verify|verify} messages.
         * @function encode
         * @memberof jlfp.ColumnF
         * @static
         * @param {jlfp.IColumnF} message ColumnF message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnF.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RowF != null && message.RowF.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.RowF.length; ++i)
                    writer.double(message.RowF[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnF message, length delimited. Does not implicitly {@link jlfp.ColumnF.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.ColumnF
         * @static
         * @param {jlfp.IColumnF} message ColumnF message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnF.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnF message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.ColumnF
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.ColumnF} ColumnF
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnF.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.ColumnF();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RowF && message.RowF.length))
                            message.RowF = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RowF.push(reader.double());
                        } else
                            message.RowF.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnF message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.ColumnF
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.ColumnF} ColumnF
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnF.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnF message.
         * @function verify
         * @memberof jlfp.ColumnF
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnF.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RowF != null && message.hasOwnProperty("RowF")) {
                if (!Array.isArray(message.RowF))
                    return "RowF: array expected";
                for (let i = 0; i < message.RowF.length; ++i)
                    if (typeof message.RowF[i] !== "number")
                        return "RowF: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnF message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.ColumnF
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.ColumnF} ColumnF
         */
        ColumnF.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.ColumnF)
                return object;
            let message = new $root.jlfp.ColumnF();
            if (object.RowF) {
                if (!Array.isArray(object.RowF))
                    throw TypeError(".jlfp.ColumnF.RowF: array expected");
                message.RowF = [];
                for (let i = 0; i < object.RowF.length; ++i)
                    message.RowF[i] = Number(object.RowF[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnF message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.ColumnF
         * @static
         * @param {jlfp.ColumnF} message ColumnF
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnF.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RowF = [];
            if (message.RowF && message.RowF.length) {
                object.RowF = [];
                for (let j = 0; j < message.RowF.length; ++j)
                    object.RowF[j] = options.json && !isFinite(message.RowF[j]) ? String(message.RowF[j]) : message.RowF[j];
            }
            return object;
        };

        /**
         * Converts this ColumnF to JSON.
         * @function toJSON
         * @memberof jlfp.ColumnF
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnF.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnF
         * @function getTypeUrl
         * @memberof jlfp.ColumnF
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnF.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.ColumnF";
        };

        return ColumnF;
    })();

    jlfp.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfp
         * @interface IAwardData
         * @property {number|null} [AwardSymbol] AwardData AwardSymbol
         * @property {number|null} [AwardLine] AwardData AwardLine
         * @property {number|null} [AwardCount] AwardData AwardCount
         * @property {number|null} [AwardMoney] AwardData AwardMoney
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfp
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfp.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData AwardSymbol.
         * @member {number} AwardSymbol
         * @memberof jlfp.AwardData
         * @instance
         */
        AwardData.prototype.AwardSymbol = 0;

        /**
         * AwardData AwardLine.
         * @member {number} AwardLine
         * @memberof jlfp.AwardData
         * @instance
         */
        AwardData.prototype.AwardLine = 0;

        /**
         * AwardData AwardCount.
         * @member {number} AwardCount
         * @memberof jlfp.AwardData
         * @instance
         */
        AwardData.prototype.AwardCount = 0;

        /**
         * AwardData AwardMoney.
         * @member {number} AwardMoney
         * @memberof jlfp.AwardData
         * @instance
         */
        AwardData.prototype.AwardMoney = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfp.AwardData
         * @static
         * @param {jlfp.IAwardData=} [properties] Properties to set
         * @returns {jlfp.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfp.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfp.AwardData
         * @static
         * @param {jlfp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardSymbol != null && Object.hasOwnProperty.call(message, "AwardSymbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardSymbol);
            if (message.AwardLine != null && Object.hasOwnProperty.call(message, "AwardLine"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardLine);
            if (message.AwardCount != null && Object.hasOwnProperty.call(message, "AwardCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardCount);
            if (message.AwardMoney != null && Object.hasOwnProperty.call(message, "AwardMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.AwardMoney);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfp.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.AwardData
         * @static
         * @param {jlfp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardSymbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.AwardLine = reader.int32();
                        break;
                    }
                case 3: {
                        message.AwardCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.AwardMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfp.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                if (!$util.isInteger(message.AwardSymbol))
                    return "AwardSymbol: integer expected";
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                if (!$util.isInteger(message.AwardLine))
                    return "AwardLine: integer expected";
            if (message.AwardCount != null && message.hasOwnProperty("AwardCount"))
                if (!$util.isInteger(message.AwardCount))
                    return "AwardCount: integer expected";
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                if (typeof message.AwardMoney !== "number")
                    return "AwardMoney: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.AwardData)
                return object;
            let message = new $root.jlfp.AwardData();
            if (object.AwardSymbol != null)
                message.AwardSymbol = object.AwardSymbol | 0;
            if (object.AwardLine != null)
                message.AwardLine = object.AwardLine | 0;
            if (object.AwardCount != null)
                message.AwardCount = object.AwardCount | 0;
            if (object.AwardMoney != null)
                message.AwardMoney = Number(object.AwardMoney);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.AwardData
         * @static
         * @param {jlfp.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.AwardSymbol = 0;
                object.AwardLine = 0;
                object.AwardCount = 0;
                object.AwardMoney = 0;
            }
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                object.AwardSymbol = message.AwardSymbol;
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                object.AwardLine = message.AwardLine;
            if (message.AwardCount != null && message.hasOwnProperty("AwardCount"))
                object.AwardCount = message.AwardCount;
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                object.AwardMoney = options.json && !isFinite(message.AwardMoney) ? String(message.AwardMoney) : message.AwardMoney;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfp.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfp.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.AwardData";
        };

        return AwardData;
    })();

    jlfp.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlfp
         * @interface ISinglePlate
         * @property {Array.<jlfp.IAwardData>|null} [AwardDataList] SinglePlate AwardDataList
         * @property {Array.<jlfp.IColumn>|null} [PlateSymbol] SinglePlate PlateSymbol
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {number|null} [PlateWin] SinglePlate PlateWin
         * @property {number|null} [LineWin] SinglePlate LineWin
         * @property {number|null} [JPWin] SinglePlate JPWin
         * @property {number|null} [LinePigMult] SinglePlate LinePigMult
         * @property {boolean|null} [IsNewPig] SinglePlate IsNewPig
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlfp
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlfp.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.AwardDataList = [];
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate AwardDataList.
         * @member {Array.<jlfp.IAwardData>} AwardDataList
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataList = $util.emptyArray;

        /**
         * SinglePlate PlateSymbol.
         * @member {Array.<jlfp.IColumn>} PlateSymbol
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate PlateWin.
         * @member {number} PlateWin
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateWin = 0;

        /**
         * SinglePlate LineWin.
         * @member {number} LineWin
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.LineWin = 0;

        /**
         * SinglePlate JPWin.
         * @member {number} JPWin
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.JPWin = 0;

        /**
         * SinglePlate LinePigMult.
         * @member {number} LinePigMult
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.LinePigMult = 0;

        /**
         * SinglePlate IsNewPig.
         * @member {boolean} IsNewPig
         * @memberof jlfp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.IsNewPig = false;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlfp.SinglePlate
         * @static
         * @param {jlfp.ISinglePlate=} [properties] Properties to set
         * @returns {jlfp.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlfp.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlfp.SinglePlate
         * @static
         * @param {jlfp.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlfp.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlfp.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.PlateWin != null && Object.hasOwnProperty.call(message, "PlateWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.PlateWin);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.LineWin);
            if (message.JPWin != null && Object.hasOwnProperty.call(message, "JPWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.JPWin);
            if (message.LinePigMult != null && Object.hasOwnProperty.call(message, "LinePigMult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.LinePigMult);
            if (message.IsNewPig != null && Object.hasOwnProperty.call(message, "IsNewPig"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.IsNewPig);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlfp.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.SinglePlate
         * @static
         * @param {jlfp.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlfp.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlfp.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.PlateWin = reader.double();
                        break;
                    }
                case 5: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 6: {
                        message.JPWin = reader.double();
                        break;
                    }
                case 7: {
                        message.LinePigMult = reader.double();
                        break;
                    }
                case 8: {
                        message.IsNewPig = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlfp.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlfp.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlfp.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                if (typeof message.PlateWin !== "number")
                    return "PlateWin: number expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                if (typeof message.JPWin !== "number")
                    return "JPWin: number expected";
            if (message.LinePigMult != null && message.hasOwnProperty("LinePigMult"))
                if (typeof message.LinePigMult !== "number")
                    return "LinePigMult: number expected";
            if (message.IsNewPig != null && message.hasOwnProperty("IsNewPig"))
                if (typeof message.IsNewPig !== "boolean")
                    return "IsNewPig: boolean expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.SinglePlate)
                return object;
            let message = new $root.jlfp.SinglePlate();
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlfp.SinglePlate.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlfp.SinglePlate.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlfp.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlfp.SinglePlate.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlfp.SinglePlate.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlfp.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.PlateWin != null)
                message.PlateWin = Number(object.PlateWin);
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.JPWin != null)
                message.JPWin = Number(object.JPWin);
            if (object.LinePigMult != null)
                message.LinePigMult = Number(object.LinePigMult);
            if (object.IsNewPig != null)
                message.IsNewPig = Boolean(object.IsNewPig);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.SinglePlate
         * @static
         * @param {jlfp.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.AwardDataList = [];
                object.PlateSymbol = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.PlateWin = 0;
                object.LineWin = 0;
                object.JPWin = 0;
                object.LinePigMult = 0;
                object.IsNewPig = false;
            }
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlfp.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlfp.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                object.PlateWin = options.json && !isFinite(message.PlateWin) ? String(message.PlateWin) : message.PlateWin;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                object.JPWin = options.json && !isFinite(message.JPWin) ? String(message.JPWin) : message.JPWin;
            if (message.LinePigMult != null && message.hasOwnProperty("LinePigMult"))
                object.LinePigMult = options.json && !isFinite(message.LinePigMult) ? String(message.LinePigMult) : message.LinePigMult;
            if (message.IsNewPig != null && message.hasOwnProperty("IsNewPig"))
                object.IsNewPig = message.IsNewPig;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlfp.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlfp.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.SinglePlate";
        };

        return SinglePlate;
    })();

    jlfp.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfp
         * @interface ISpinAck
         * @property {Array.<jlfp.ISinglePlate>|null} [PlateInfo] SpinAck PlateInfo
         * @property {Array.<number>|null} [ExpLen] SpinAck ExpLen
         * @property {Array.<jlfp.IColumnF>|null} [WildPlate] SpinAck WildPlate
         * @property {boolean|null} [HasRespin] SpinAck HasRespin
         * @property {boolean|null} [HasExpand] SpinAck HasExpand
         * @property {jlfp.IJPInfo|null} [JPInfo] SpinAck JPInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfp
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfp.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateInfo = [];
            this.ExpLen = [];
            this.WildPlate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateInfo.
         * @member {Array.<jlfp.ISinglePlate>} PlateInfo
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateInfo = $util.emptyArray;

        /**
         * SpinAck ExpLen.
         * @member {Array.<number>} ExpLen
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.ExpLen = $util.emptyArray;

        /**
         * SpinAck WildPlate.
         * @member {Array.<jlfp.IColumnF>} WildPlate
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.WildPlate = $util.emptyArray;

        /**
         * SpinAck HasRespin.
         * @member {boolean} HasRespin
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.HasRespin = false;

        /**
         * SpinAck HasExpand.
         * @member {boolean} HasExpand
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.HasExpand = false;

        /**
         * SpinAck JPInfo.
         * @member {jlfp.IJPInfo|null|undefined} JPInfo
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.JPInfo = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlfp.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfp.SpinAck
         * @static
         * @param {jlfp.ISpinAck=} [properties] Properties to set
         * @returns {jlfp.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfp.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfp.SpinAck
         * @static
         * @param {jlfp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateInfo != null && message.PlateInfo.length)
                for (let i = 0; i < message.PlateInfo.length; ++i)
                    $root.jlfp.SinglePlate.encode(message.PlateInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ExpLen != null && message.ExpLen.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.ExpLen.length; ++i)
                    writer.int32(message.ExpLen[i]);
                writer.ldelim();
            }
            if (message.WildPlate != null && message.WildPlate.length)
                for (let i = 0; i < message.WildPlate.length; ++i)
                    $root.jlfp.ColumnF.encode(message.WildPlate[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.HasRespin != null && Object.hasOwnProperty.call(message, "HasRespin"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.HasRespin);
            if (message.HasExpand != null && Object.hasOwnProperty.call(message, "HasExpand"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.HasExpand);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AckType);
            if (message.JPInfo != null && Object.hasOwnProperty.call(message, "JPInfo"))
                $root.jlfp.JPInfo.encode(message.JPInfo, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfp.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.SpinAck
         * @static
         * @param {jlfp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateInfo && message.PlateInfo.length))
                            message.PlateInfo = [];
                        message.PlateInfo.push($root.jlfp.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.ExpLen && message.ExpLen.length))
                            message.ExpLen = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ExpLen.push(reader.int32());
                        } else
                            message.ExpLen.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.WildPlate && message.WildPlate.length))
                            message.WildPlate = [];
                        message.WildPlate.push($root.jlfp.ColumnF.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.HasRespin = reader.bool();
                        break;
                    }
                case 5: {
                        message.HasExpand = reader.bool();
                        break;
                    }
                case 10: {
                        message.JPInfo = $root.jlfp.JPInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 7: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 8: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 9: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 11: {
                        message.RTP = reader.double();
                        break;
                    }
                case 12: {
                        message.LID = reader.int32();
                        break;
                    }
                case 13: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfp.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo")) {
                if (!Array.isArray(message.PlateInfo))
                    return "PlateInfo: array expected";
                for (let i = 0; i < message.PlateInfo.length; ++i) {
                    let error = $root.jlfp.SinglePlate.verify(message.PlateInfo[i]);
                    if (error)
                        return "PlateInfo." + error;
                }
            }
            if (message.ExpLen != null && message.hasOwnProperty("ExpLen")) {
                if (!Array.isArray(message.ExpLen))
                    return "ExpLen: array expected";
                for (let i = 0; i < message.ExpLen.length; ++i)
                    if (!$util.isInteger(message.ExpLen[i]))
                        return "ExpLen: integer[] expected";
            }
            if (message.WildPlate != null && message.hasOwnProperty("WildPlate")) {
                if (!Array.isArray(message.WildPlate))
                    return "WildPlate: array expected";
                for (let i = 0; i < message.WildPlate.length; ++i) {
                    let error = $root.jlfp.ColumnF.verify(message.WildPlate[i]);
                    if (error)
                        return "WildPlate." + error;
                }
            }
            if (message.HasRespin != null && message.hasOwnProperty("HasRespin"))
                if (typeof message.HasRespin !== "boolean")
                    return "HasRespin: boolean expected";
            if (message.HasExpand != null && message.hasOwnProperty("HasExpand"))
                if (typeof message.HasExpand !== "boolean")
                    return "HasExpand: boolean expected";
            if (message.JPInfo != null && message.hasOwnProperty("JPInfo")) {
                let error = $root.jlfp.JPInfo.verify(message.JPInfo);
                if (error)
                    return "JPInfo." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.SpinAck)
                return object;
            let message = new $root.jlfp.SpinAck();
            if (object.PlateInfo) {
                if (!Array.isArray(object.PlateInfo))
                    throw TypeError(".jlfp.SpinAck.PlateInfo: array expected");
                message.PlateInfo = [];
                for (let i = 0; i < object.PlateInfo.length; ++i) {
                    if (typeof object.PlateInfo[i] !== "object")
                        throw TypeError(".jlfp.SpinAck.PlateInfo: object expected");
                    message.PlateInfo[i] = $root.jlfp.SinglePlate.fromObject(object.PlateInfo[i]);
                }
            }
            if (object.ExpLen) {
                if (!Array.isArray(object.ExpLen))
                    throw TypeError(".jlfp.SpinAck.ExpLen: array expected");
                message.ExpLen = [];
                for (let i = 0; i < object.ExpLen.length; ++i)
                    message.ExpLen[i] = object.ExpLen[i] | 0;
            }
            if (object.WildPlate) {
                if (!Array.isArray(object.WildPlate))
                    throw TypeError(".jlfp.SpinAck.WildPlate: array expected");
                message.WildPlate = [];
                for (let i = 0; i < object.WildPlate.length; ++i) {
                    if (typeof object.WildPlate[i] !== "object")
                        throw TypeError(".jlfp.SpinAck.WildPlate: object expected");
                    message.WildPlate[i] = $root.jlfp.ColumnF.fromObject(object.WildPlate[i]);
                }
            }
            if (object.HasRespin != null)
                message.HasRespin = Boolean(object.HasRespin);
            if (object.HasExpand != null)
                message.HasExpand = Boolean(object.HasExpand);
            if (object.JPInfo != null) {
                if (typeof object.JPInfo !== "object")
                    throw TypeError(".jlfp.SpinAck.JPInfo: object expected");
                message.JPInfo = $root.jlfp.JPInfo.fromObject(object.JPInfo);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.SpinAck
         * @static
         * @param {jlfp.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateInfo = [];
                object.ExpLen = [];
                object.WildPlate = [];
            }
            if (options.defaults) {
                object.HasRespin = false;
                object.HasExpand = false;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.JPInfo = null;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateInfo && message.PlateInfo.length) {
                object.PlateInfo = [];
                for (let j = 0; j < message.PlateInfo.length; ++j)
                    object.PlateInfo[j] = $root.jlfp.SinglePlate.toObject(message.PlateInfo[j], options);
            }
            if (message.ExpLen && message.ExpLen.length) {
                object.ExpLen = [];
                for (let j = 0; j < message.ExpLen.length; ++j)
                    object.ExpLen[j] = message.ExpLen[j];
            }
            if (message.WildPlate && message.WildPlate.length) {
                object.WildPlate = [];
                for (let j = 0; j < message.WildPlate.length; ++j)
                    object.WildPlate[j] = $root.jlfp.ColumnF.toObject(message.WildPlate[j], options);
            }
            if (message.HasRespin != null && message.hasOwnProperty("HasRespin"))
                object.HasRespin = message.HasRespin;
            if (message.HasExpand != null && message.hasOwnProperty("HasExpand"))
                object.HasExpand = message.HasExpand;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.JPInfo != null && message.hasOwnProperty("JPInfo"))
                object.JPInfo = $root.jlfp.JPInfo.toObject(message.JPInfo, options);
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfp.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfp.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.SpinAck";
        };

        return SpinAck;
    })();

    jlfp.JpReq = (function() {

        /**
         * Properties of a JpReq.
         * @memberof jlfp
         * @interface IJpReq
         * @property {number|null} [currency] JpReq currency
         */

        /**
         * Constructs a new JpReq.
         * @memberof jlfp
         * @classdesc Represents a JpReq.
         * @implements IJpReq
         * @constructor
         * @param {jlfp.IJpReq=} [properties] Properties to set
         */
        function JpReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JpReq currency.
         * @member {number} currency
         * @memberof jlfp.JpReq
         * @instance
         */
        JpReq.prototype.currency = 0;

        /**
         * Creates a new JpReq instance using the specified properties.
         * @function create
         * @memberof jlfp.JpReq
         * @static
         * @param {jlfp.IJpReq=} [properties] Properties to set
         * @returns {jlfp.JpReq} JpReq instance
         */
        JpReq.create = function create(properties) {
            return new JpReq(properties);
        };

        /**
         * Encodes the specified JpReq message. Does not implicitly {@link jlfp.JpReq.verify|verify} messages.
         * @function encode
         * @memberof jlfp.JpReq
         * @static
         * @param {jlfp.IJpReq} message JpReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.currency);
            return writer;
        };

        /**
         * Encodes the specified JpReq message, length delimited. Does not implicitly {@link jlfp.JpReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.JpReq
         * @static
         * @param {jlfp.IJpReq} message JpReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JpReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.JpReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.JpReq} JpReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.JpReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.currency = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JpReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.JpReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.JpReq} JpReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JpReq message.
         * @function verify
         * @memberof jlfp.JpReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JpReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.currency != null && message.hasOwnProperty("currency"))
                if (!$util.isInteger(message.currency))
                    return "currency: integer expected";
            return null;
        };

        /**
         * Creates a JpReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.JpReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.JpReq} JpReq
         */
        JpReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.JpReq)
                return object;
            let message = new $root.jlfp.JpReq();
            if (object.currency != null)
                message.currency = object.currency | 0;
            return message;
        };

        /**
         * Creates a plain object from a JpReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.JpReq
         * @static
         * @param {jlfp.JpReq} message JpReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JpReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.currency = 0;
            if (message.currency != null && message.hasOwnProperty("currency"))
                object.currency = message.currency;
            return object;
        };

        /**
         * Converts this JpReq to JSON.
         * @function toJSON
         * @memberof jlfp.JpReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JpReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JpReq
         * @function getTypeUrl
         * @memberof jlfp.JpReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JpReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.JpReq";
        };

        return JpReq;
    })();

    jlfp.JPInfo = (function() {

        /**
         * Properties of a JPInfo.
         * @memberof jlfp
         * @interface IJPInfo
         * @property {Array.<number>|null} [Fronts] JPInfo Fronts
         * @property {Array.<number>|null} [OddPrize] JPInfo OddPrize
         * @property {number|null} [BaseBet] JPInfo BaseBet
         */

        /**
         * Constructs a new JPInfo.
         * @memberof jlfp
         * @classdesc Represents a JPInfo.
         * @implements IJPInfo
         * @constructor
         * @param {jlfp.IJPInfo=} [properties] Properties to set
         */
        function JPInfo(properties) {
            this.Fronts = [];
            this.OddPrize = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPInfo Fronts.
         * @member {Array.<number>} Fronts
         * @memberof jlfp.JPInfo
         * @instance
         */
        JPInfo.prototype.Fronts = $util.emptyArray;

        /**
         * JPInfo OddPrize.
         * @member {Array.<number>} OddPrize
         * @memberof jlfp.JPInfo
         * @instance
         */
        JPInfo.prototype.OddPrize = $util.emptyArray;

        /**
         * JPInfo BaseBet.
         * @member {number} BaseBet
         * @memberof jlfp.JPInfo
         * @instance
         */
        JPInfo.prototype.BaseBet = 0;

        /**
         * Creates a new JPInfo instance using the specified properties.
         * @function create
         * @memberof jlfp.JPInfo
         * @static
         * @param {jlfp.IJPInfo=} [properties] Properties to set
         * @returns {jlfp.JPInfo} JPInfo instance
         */
        JPInfo.create = function create(properties) {
            return new JPInfo(properties);
        };

        /**
         * Encodes the specified JPInfo message. Does not implicitly {@link jlfp.JPInfo.verify|verify} messages.
         * @function encode
         * @memberof jlfp.JPInfo
         * @static
         * @param {jlfp.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Fronts != null && message.Fronts.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Fronts.length; ++i)
                    writer.double(message.Fronts[i]);
                writer.ldelim();
            }
            if (message.OddPrize != null && message.OddPrize.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.OddPrize.length; ++i)
                    writer.double(message.OddPrize[i]);
                writer.ldelim();
            }
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.BaseBet);
            return writer;
        };

        /**
         * Encodes the specified JPInfo message, length delimited. Does not implicitly {@link jlfp.JPInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfp.JPInfo
         * @static
         * @param {jlfp.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlfp.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfp.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfp.JPInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Fronts && message.Fronts.length))
                            message.Fronts = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Fronts.push(reader.double());
                        } else
                            message.Fronts.push(reader.double());
                        break;
                    }
                case 2: {
                        if (!(message.OddPrize && message.OddPrize.length))
                            message.OddPrize = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.OddPrize.push(reader.double());
                        } else
                            message.OddPrize.push(reader.double());
                        break;
                    }
                case 3: {
                        message.BaseBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfp.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfp.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPInfo message.
         * @function verify
         * @memberof jlfp.JPInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Fronts != null && message.hasOwnProperty("Fronts")) {
                if (!Array.isArray(message.Fronts))
                    return "Fronts: array expected";
                for (let i = 0; i < message.Fronts.length; ++i)
                    if (typeof message.Fronts[i] !== "number")
                        return "Fronts: number[] expected";
            }
            if (message.OddPrize != null && message.hasOwnProperty("OddPrize")) {
                if (!Array.isArray(message.OddPrize))
                    return "OddPrize: array expected";
                for (let i = 0; i < message.OddPrize.length; ++i)
                    if (typeof message.OddPrize[i] !== "number")
                        return "OddPrize: number[] expected";
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            return null;
        };

        /**
         * Creates a JPInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfp.JPInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfp.JPInfo} JPInfo
         */
        JPInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfp.JPInfo)
                return object;
            let message = new $root.jlfp.JPInfo();
            if (object.Fronts) {
                if (!Array.isArray(object.Fronts))
                    throw TypeError(".jlfp.JPInfo.Fronts: array expected");
                message.Fronts = [];
                for (let i = 0; i < object.Fronts.length; ++i)
                    message.Fronts[i] = Number(object.Fronts[i]);
            }
            if (object.OddPrize) {
                if (!Array.isArray(object.OddPrize))
                    throw TypeError(".jlfp.JPInfo.OddPrize: array expected");
                message.OddPrize = [];
                for (let i = 0; i < object.OddPrize.length; ++i)
                    message.OddPrize[i] = Number(object.OddPrize[i]);
            }
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            return message;
        };

        /**
         * Creates a plain object from a JPInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfp.JPInfo
         * @static
         * @param {jlfp.JPInfo} message JPInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Fronts = [];
                object.OddPrize = [];
            }
            if (options.defaults)
                object.BaseBet = 0;
            if (message.Fronts && message.Fronts.length) {
                object.Fronts = [];
                for (let j = 0; j < message.Fronts.length; ++j)
                    object.Fronts[j] = options.json && !isFinite(message.Fronts[j]) ? String(message.Fronts[j]) : message.Fronts[j];
            }
            if (message.OddPrize && message.OddPrize.length) {
                object.OddPrize = [];
                for (let j = 0; j < message.OddPrize.length; ++j)
                    object.OddPrize[j] = options.json && !isFinite(message.OddPrize[j]) ? String(message.OddPrize[j]) : message.OddPrize[j];
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            return object;
        };

        /**
         * Converts this JPInfo to JSON.
         * @function toJSON
         * @memberof jlfp.JPInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPInfo
         * @function getTypeUrl
         * @memberof jlfp.JPInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfp.JPInfo";
        };

        return JPInfo;
    })();

    return jlfp;
})();

export const jlfs = $root.jlfs = (() => {

    /**
     * Namespace jlfs.
     * @exports jlfs
     * @namespace
     */
    const jlfs = {};

    /**
     * Symbol enum.
     * @name jlfs.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     */
    jlfs.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        return values;
    })();

    jlfs.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlfs
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlfs
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlfs.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlfs.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlfs.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlfs.Block
         * @static
         * @param {jlfs.IBlock=} [properties] Properties to set
         * @returns {jlfs.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlfs.Block.verify|verify} messages.
         * @function encode
         * @memberof jlfs.Block
         * @static
         * @param {jlfs.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlfs.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.Block
         * @static
         * @param {jlfs.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlfs.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.Block)
                return object;
            let message = new $root.jlfs.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.Block
         * @static
         * @param {jlfs.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlfs.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlfs.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.Block";
        };

        return Block;
    })();

    jlfs.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlfs
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [row] Cell row
         * @property {number|null} [column] Cell column
         */

        /**
         * Constructs a new Cell.
         * @memberof jlfs
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlfs.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlfs.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell row.
         * @member {number} row
         * @memberof jlfs.Cell
         * @instance
         */
        Cell.prototype.row = 0;

        /**
         * Cell column.
         * @member {number} column
         * @memberof jlfs.Cell
         * @instance
         */
        Cell.prototype.column = 0;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlfs.Cell
         * @static
         * @param {jlfs.ICell=} [properties] Properties to set
         * @returns {jlfs.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlfs.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlfs.Cell
         * @static
         * @param {jlfs.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlfs.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.Cell
         * @static
         * @param {jlfs.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.row = reader.uint32();
                        break;
                    }
                case 3: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlfs.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.Cell)
                return object;
            let message = new $root.jlfs.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.Cell
         * @static
         * @param {jlfs.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.row = 0;
                object.column = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlfs.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlfs.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.Cell";
        };

        return Cell;
    })();

    jlfs.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlfs
         * @interface IAwardReel
         * @property {number|null} [symbol] AwardReel symbol
         * @property {Array.<jlfs.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         * @property {number|null} [bonus] AwardReel bonus
         * @property {number|null} [maxLen] AwardReel maxLen
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlfs
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlfs.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel symbol.
         * @member {number} symbol
         * @memberof jlfs.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlfs.IBlock>} block
         * @memberof jlfs.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlfs.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * AwardReel bonus.
         * @member {number} bonus
         * @memberof jlfs.AwardReel
         * @instance
         */
        AwardReel.prototype.bonus = 0;

        /**
         * AwardReel maxLen.
         * @member {number} maxLen
         * @memberof jlfs.AwardReel
         * @instance
         */
        AwardReel.prototype.maxLen = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlfs.AwardReel
         * @static
         * @param {jlfs.IAwardReel=} [properties] Properties to set
         * @returns {jlfs.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlfs.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlfs.AwardReel
         * @static
         * @param {jlfs.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlfs.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.bonus != null && Object.hasOwnProperty.call(message, "bonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.bonus);
            if (message.maxLen != null && Object.hasOwnProperty.call(message, "maxLen"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.maxLen);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlfs.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.AwardReel
         * @static
         * @param {jlfs.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlfs.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.bonus = reader.uint32();
                        break;
                    }
                case 5: {
                        message.maxLen = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlfs.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlfs.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                if (!$util.isInteger(message.bonus))
                    return "bonus: integer expected";
            if (message.maxLen != null && message.hasOwnProperty("maxLen"))
                if (!$util.isInteger(message.maxLen))
                    return "maxLen: integer expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.AwardReel)
                return object;
            let message = new $root.jlfs.AwardReel();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlfs.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlfs.AwardReel.block: object expected");
                    message.block[i] = $root.jlfs.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.bonus != null)
                message.bonus = object.bonus >>> 0;
            if (object.maxLen != null)
                message.maxLen = object.maxLen >>> 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.AwardReel
         * @static
         * @param {jlfs.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.symbol = 0;
                object.win = 0;
                object.bonus = 0;
                object.maxLen = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlfs.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                object.bonus = message.bonus;
            if (message.maxLen != null && message.hasOwnProperty("maxLen"))
                object.maxLen = message.maxLen;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlfs.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlfs.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.AwardReel";
        };

        return AwardReel;
    })();

    jlfs.Combo = (function() {

        /**
         * Properties of a Combo.
         * @memberof jlfs
         * @interface ICombo
         * @property {Array.<jlfs.ICell>|null} [change] Combo change
         * @property {Array.<jlfs.IAwardReel>|null} [award] Combo award
         * @property {number|null} [win] Combo win
         */

        /**
         * Constructs a new Combo.
         * @memberof jlfs
         * @classdesc Represents a Combo.
         * @implements ICombo
         * @constructor
         * @param {jlfs.ICombo=} [properties] Properties to set
         */
        function Combo(properties) {
            this.change = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Combo change.
         * @member {Array.<jlfs.ICell>} change
         * @memberof jlfs.Combo
         * @instance
         */
        Combo.prototype.change = $util.emptyArray;

        /**
         * Combo award.
         * @member {Array.<jlfs.IAwardReel>} award
         * @memberof jlfs.Combo
         * @instance
         */
        Combo.prototype.award = $util.emptyArray;

        /**
         * Combo win.
         * @member {number} win
         * @memberof jlfs.Combo
         * @instance
         */
        Combo.prototype.win = 0;

        /**
         * Creates a new Combo instance using the specified properties.
         * @function create
         * @memberof jlfs.Combo
         * @static
         * @param {jlfs.ICombo=} [properties] Properties to set
         * @returns {jlfs.Combo} Combo instance
         */
        Combo.create = function create(properties) {
            return new Combo(properties);
        };

        /**
         * Encodes the specified Combo message. Does not implicitly {@link jlfs.Combo.verify|verify} messages.
         * @function encode
         * @memberof jlfs.Combo
         * @static
         * @param {jlfs.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.change != null && message.change.length)
                for (let i = 0; i < message.change.length; ++i)
                    $root.jlfs.Cell.encode(message.change[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlfs.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified Combo message, length delimited. Does not implicitly {@link jlfs.Combo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.Combo
         * @static
         * @param {jlfs.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Combo message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.Combo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.change && message.change.length))
                            message.change = [];
                        message.change.push($root.jlfs.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlfs.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Combo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Combo message.
         * @function verify
         * @memberof jlfs.Combo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Combo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.change != null && message.hasOwnProperty("change")) {
                if (!Array.isArray(message.change))
                    return "change: array expected";
                for (let i = 0; i < message.change.length; ++i) {
                    let error = $root.jlfs.Cell.verify(message.change[i]);
                    if (error)
                        return "change." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlfs.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates a Combo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.Combo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.Combo} Combo
         */
        Combo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.Combo)
                return object;
            let message = new $root.jlfs.Combo();
            if (object.change) {
                if (!Array.isArray(object.change))
                    throw TypeError(".jlfs.Combo.change: array expected");
                message.change = [];
                for (let i = 0; i < object.change.length; ++i) {
                    if (typeof object.change[i] !== "object")
                        throw TypeError(".jlfs.Combo.change: object expected");
                    message.change[i] = $root.jlfs.Cell.fromObject(object.change[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlfs.Combo.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlfs.Combo.award: object expected");
                    message.award[i] = $root.jlfs.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from a Combo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.Combo
         * @static
         * @param {jlfs.Combo} message Combo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Combo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change = [];
                object.award = [];
            }
            if (options.defaults)
                object.win = 0;
            if (message.change && message.change.length) {
                object.change = [];
                for (let j = 0; j < message.change.length; ++j)
                    object.change[j] = $root.jlfs.Cell.toObject(message.change[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlfs.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this Combo to JSON.
         * @function toJSON
         * @memberof jlfs.Combo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Combo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Combo
         * @function getTypeUrl
         * @memberof jlfs.Combo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Combo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.Combo";
        };

        return Combo;
    })();

    jlfs.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlfs
         * @interface IPlate
         * @property {Array.<jlfs.IColumnData>|null} [column] Plate column
         * @property {Array.<jlfs.ICell>|null} [change] Plate change
         * @property {Array.<jlfs.ICombo>|null} [combo] Plate combo
         * @property {number|null} [win] Plate win
         * @property {number|null} [bonusWin] Plate bonusWin
         * @property {boolean|null} [isBonus] Plate isBonus
         * @property {number|null} [bonusRate] Plate bonusRate
         */

        /**
         * Constructs a new Plate.
         * @memberof jlfs
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlfs.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.change = [];
            this.combo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlfs.IColumnData>} column
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate change.
         * @member {Array.<jlfs.ICell>} change
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.change = $util.emptyArray;

        /**
         * Plate combo.
         * @member {Array.<jlfs.ICombo>} combo
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.combo = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate bonusWin.
         * @member {number} bonusWin
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.bonusWin = 0;

        /**
         * Plate isBonus.
         * @member {boolean} isBonus
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.isBonus = false;

        /**
         * Plate bonusRate.
         * @member {number} bonusRate
         * @memberof jlfs.Plate
         * @instance
         */
        Plate.prototype.bonusRate = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlfs.Plate
         * @static
         * @param {jlfs.IPlate=} [properties] Properties to set
         * @returns {jlfs.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlfs.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlfs.Plate
         * @static
         * @param {jlfs.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlfs.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.change != null && message.change.length)
                for (let i = 0; i < message.change.length; ++i)
                    $root.jlfs.Cell.encode(message.change[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.combo != null && message.combo.length)
                for (let i = 0; i < message.combo.length; ++i)
                    $root.jlfs.Combo.encode(message.combo[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.win);
            if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
            if (message.isBonus != null && Object.hasOwnProperty.call(message, "isBonus"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isBonus);
            if (message.bonusRate != null && Object.hasOwnProperty.call(message, "bonusRate"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.bonusRate);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlfs.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.Plate
         * @static
         * @param {jlfs.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlfs.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.change && message.change.length))
                            message.change = [];
                        message.change.push($root.jlfs.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.combo && message.combo.length))
                            message.combo = [];
                        message.combo.push($root.jlfs.Combo.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.win = reader.double();
                        break;
                    }
                case 5: {
                        message.bonusWin = reader.double();
                        break;
                    }
                case 6: {
                        message.isBonus = reader.bool();
                        break;
                    }
                case 7: {
                        message.bonusRate = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlfs.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlfs.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.change != null && message.hasOwnProperty("change")) {
                if (!Array.isArray(message.change))
                    return "change: array expected";
                for (let i = 0; i < message.change.length; ++i) {
                    let error = $root.jlfs.Cell.verify(message.change[i]);
                    if (error)
                        return "change." + error;
                }
            }
            if (message.combo != null && message.hasOwnProperty("combo")) {
                if (!Array.isArray(message.combo))
                    return "combo: array expected";
                for (let i = 0; i < message.combo.length; ++i) {
                    let error = $root.jlfs.Combo.verify(message.combo[i]);
                    if (error)
                        return "combo." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                if (typeof message.bonusWin !== "number")
                    return "bonusWin: number expected";
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                if (typeof message.isBonus !== "boolean")
                    return "isBonus: boolean expected";
            if (message.bonusRate != null && message.hasOwnProperty("bonusRate"))
                if (!$util.isInteger(message.bonusRate))
                    return "bonusRate: integer expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.Plate)
                return object;
            let message = new $root.jlfs.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlfs.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlfs.Plate.column: object expected");
                    message.column[i] = $root.jlfs.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.change) {
                if (!Array.isArray(object.change))
                    throw TypeError(".jlfs.Plate.change: array expected");
                message.change = [];
                for (let i = 0; i < object.change.length; ++i) {
                    if (typeof object.change[i] !== "object")
                        throw TypeError(".jlfs.Plate.change: object expected");
                    message.change[i] = $root.jlfs.Cell.fromObject(object.change[i]);
                }
            }
            if (object.combo) {
                if (!Array.isArray(object.combo))
                    throw TypeError(".jlfs.Plate.combo: array expected");
                message.combo = [];
                for (let i = 0; i < object.combo.length; ++i) {
                    if (typeof object.combo[i] !== "object")
                        throw TypeError(".jlfs.Plate.combo: object expected");
                    message.combo[i] = $root.jlfs.Combo.fromObject(object.combo[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.bonusWin != null)
                message.bonusWin = Number(object.bonusWin);
            if (object.isBonus != null)
                message.isBonus = Boolean(object.isBonus);
            if (object.bonusRate != null)
                message.bonusRate = object.bonusRate >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.Plate
         * @static
         * @param {jlfs.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.change = [];
                object.combo = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.bonusWin = 0;
                object.isBonus = false;
                object.bonusRate = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlfs.ColumnData.toObject(message.column[j], options);
            }
            if (message.change && message.change.length) {
                object.change = [];
                for (let j = 0; j < message.change.length; ++j)
                    object.change[j] = $root.jlfs.Cell.toObject(message.change[j], options);
            }
            if (message.combo && message.combo.length) {
                object.combo = [];
                for (let j = 0; j < message.combo.length; ++j)
                    object.combo[j] = $root.jlfs.Combo.toObject(message.combo[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                object.isBonus = message.isBonus;
            if (message.bonusRate != null && message.hasOwnProperty("bonusRate"))
                object.bonusRate = message.bonusRate;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlfs.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlfs.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.Plate";
        };

        return Plate;
    })();

    jlfs.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlfs
         * @interface IAllPlate
         * @property {Array.<jlfs.IPlate>|null} [plate] AllPlate plate
         * @property {jlfs.IPlate|null} [finalPlate] AllPlate finalPlate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlfs
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlfs.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlfs.IPlate>} plate
         * @memberof jlfs.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * AllPlate finalPlate.
         * @member {jlfs.IPlate|null|undefined} finalPlate
         * @memberof jlfs.AllPlate
         * @instance
         */
        AllPlate.prototype.finalPlate = null;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlfs.AllPlate
         * @static
         * @param {jlfs.IAllPlate=} [properties] Properties to set
         * @returns {jlfs.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlfs.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlfs.AllPlate
         * @static
         * @param {jlfs.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlfs.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.finalPlate != null && Object.hasOwnProperty.call(message, "finalPlate"))
                $root.jlfs.Plate.encode(message.finalPlate, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlfs.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.AllPlate
         * @static
         * @param {jlfs.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlfs.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.finalPlate = $root.jlfs.Plate.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlfs.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlfs.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            if (message.finalPlate != null && message.hasOwnProperty("finalPlate")) {
                let error = $root.jlfs.Plate.verify(message.finalPlate);
                if (error)
                    return "finalPlate." + error;
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.AllPlate)
                return object;
            let message = new $root.jlfs.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlfs.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlfs.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlfs.Plate.fromObject(object.plate[i]);
                }
            }
            if (object.finalPlate != null) {
                if (typeof object.finalPlate !== "object")
                    throw TypeError(".jlfs.AllPlate.finalPlate: object expected");
                message.finalPlate = $root.jlfs.Plate.fromObject(object.finalPlate);
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.AllPlate
         * @static
         * @param {jlfs.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (options.defaults)
                object.finalPlate = null;
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlfs.Plate.toObject(message.plate[j], options);
            }
            if (message.finalPlate != null && message.hasOwnProperty("finalPlate"))
                object.finalPlate = $root.jlfs.Plate.toObject(message.finalPlate, options);
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlfs.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlfs.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.AllPlate";
        };

        return AllPlate;
    })();

    jlfs.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlfs
         * @interface IColumnData
         * @property {Array.<number>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlfs
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlfs.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<number>} row
         * @memberof jlfs.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlfs.ColumnData
         * @static
         * @param {jlfs.IColumnData=} [properties] Properties to set
         * @returns {jlfs.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlfs.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlfs.ColumnData
         * @static
         * @param {jlfs.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlfs.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfs.ColumnData
         * @static
         * @param {jlfs.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfs.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfs.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfs.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfs.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfs.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlfs.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfs.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfs.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfs.ColumnData)
                return object;
            let message = new $root.jlfs.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlfs.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfs.ColumnData
         * @static
         * @param {jlfs.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlfs.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlfs.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfs.ColumnData";
        };

        return ColumnData;
    })();

    return jlfs;
})();

export const jlfw = $root.jlfw = (() => {

    /**
     * Namespace jlfw.
     * @exports jlfw
     * @namespace
     */
    const jlfw = {};

    jlfw.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlfw
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlfw
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlfw.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlfw.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlfw.Column
         * @static
         * @param {jlfw.IColumn=} [properties] Properties to set
         * @returns {jlfw.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlfw.Column.verify|verify} messages.
         * @function encode
         * @memberof jlfw.Column
         * @static
         * @param {jlfw.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlfw.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfw.Column
         * @static
         * @param {jlfw.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlfw.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfw.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfw.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfw.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfw.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlfw.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfw.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfw.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfw.Column)
                return object;
            let message = new $root.jlfw.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlfw.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfw.Column
         * @static
         * @param {jlfw.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlfw.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlfw.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfw.Column";
        };

        return Column;
    })();

    jlfw.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlfw
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [Pos] AwardData Pos
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlfw
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlfw.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlfw.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlfw.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlfw.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlfw.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Pos.
         * @member {Array.<number>} Pos
         * @memberof jlfw.AwardData
         * @instance
         */
        AwardData.prototype.Pos = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlfw.AwardData
         * @static
         * @param {jlfw.IAwardData=} [properties] Properties to set
         * @returns {jlfw.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlfw.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlfw.AwardData
         * @static
         * @param {jlfw.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Pos != null && message.Pos.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.Pos.length; ++i)
                    writer.int32(message.Pos[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlfw.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfw.AwardData
         * @static
         * @param {jlfw.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlfw.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfw.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfw.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.Pos && message.Pos.length))
                            message.Pos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pos.push(reader.int32());
                        } else
                            message.Pos.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfw.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfw.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlfw.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Pos != null && message.hasOwnProperty("Pos")) {
                if (!Array.isArray(message.Pos))
                    return "Pos: array expected";
                for (let i = 0; i < message.Pos.length; ++i)
                    if (!$util.isInteger(message.Pos[i]))
                        return "Pos: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfw.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfw.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfw.AwardData)
                return object;
            let message = new $root.jlfw.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Pos) {
                if (!Array.isArray(object.Pos))
                    throw TypeError(".jlfw.AwardData.Pos: array expected");
                message.Pos = [];
                for (let i = 0; i < object.Pos.length; ++i)
                    message.Pos[i] = object.Pos[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfw.AwardData
         * @static
         * @param {jlfw.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Pos = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Pos && message.Pos.length) {
                object.Pos = [];
                for (let j = 0; j < message.Pos.length; ++j)
                    object.Pos[j] = message.Pos[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlfw.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlfw.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfw.AwardData";
        };

        return AwardData;
    })();

    jlfw.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlfw
         * @interface IRoundInfo
         * @property {Array.<jlfw.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {Array.<jlfw.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [WheelType] RoundInfo WheelType
         * @property {number|null} [WheelMul] RoundInfo WheelMul
         * @property {number|null} [WildCount] RoundInfo WildCount
         * @property {boolean|null} [GetFree] RoundInfo GetFree
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [FreeWheelType] RoundInfo FreeWheelType
         * @property {Array.<number>|null} [FreeWheelColumn] RoundInfo FreeWheelColumn
         * @property {Array.<jlfw.IColumn>|null} [FreeSymbol] RoundInfo FreeSymbol
         * @property {Array.<jlfw.IAwardData>|null} [FreeAwardDataVec] RoundInfo FreeAwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [ReelType] RoundInfo ReelType
         * @property {boolean|null} [IsMain] RoundInfo IsMain
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [FreeFirstWin] RoundInfo FreeFirstWin
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlfw
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlfw.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.AwardDataVec = [];
            this.FreeWheelColumn = [];
            this.FreeSymbol = [];
            this.FreeAwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jlfw.IColumn>} Symbol
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlfw.IAwardData>} AwardDataVec
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo WheelType.
         * @member {number} WheelType
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WheelType = 0;

        /**
         * RoundInfo WheelMul.
         * @member {number} WheelMul
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WheelMul = 0;

        /**
         * RoundInfo WildCount.
         * @member {number} WildCount
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WildCount = 0;

        /**
         * RoundInfo GetFree.
         * @member {boolean} GetFree
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetFree = false;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo FreeWheelType.
         * @member {number} FreeWheelType
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeWheelType = 0;

        /**
         * RoundInfo FreeWheelColumn.
         * @member {Array.<number>} FreeWheelColumn
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeWheelColumn = $util.emptyArray;

        /**
         * RoundInfo FreeSymbol.
         * @member {Array.<jlfw.IColumn>} FreeSymbol
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeSymbol = $util.emptyArray;

        /**
         * RoundInfo FreeAwardDataVec.
         * @member {Array.<jlfw.IAwardData>} FreeAwardDataVec
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeAwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo ReelType.
         * @member {number} ReelType
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ReelType = 0;

        /**
         * RoundInfo IsMain.
         * @member {boolean} IsMain
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.IsMain = false;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo FreeFirstWin.
         * @member {number} FreeFirstWin
         * @memberof jlfw.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeFirstWin = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlfw.RoundInfo
         * @static
         * @param {jlfw.IRoundInfo=} [properties] Properties to set
         * @returns {jlfw.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlfw.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlfw.RoundInfo
         * @static
         * @param {jlfw.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlfw.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlfw.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundWin);
            if (message.WheelType != null && Object.hasOwnProperty.call(message, "WheelType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.WheelType);
            if (message.WheelMul != null && Object.hasOwnProperty.call(message, "WheelMul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.WheelMul);
            if (message.WildCount != null && Object.hasOwnProperty.call(message, "WildCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.WildCount);
            if (message.GetFree != null && Object.hasOwnProperty.call(message, "GetFree"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.GetFree);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.FreeTotalRound);
            if (message.FreeWheelType != null && Object.hasOwnProperty.call(message, "FreeWheelType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.FreeWheelType);
            if (message.FreeWheelColumn != null && message.FreeWheelColumn.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.FreeWheelColumn.length; ++i)
                    writer.int32(message.FreeWheelColumn[i]);
                writer.ldelim();
            }
            if (message.FreeSymbol != null && message.FreeSymbol.length)
                for (let i = 0; i < message.FreeSymbol.length; ++i)
                    $root.jlfw.Column.encode(message.FreeSymbol[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.FreeAwardDataVec != null && message.FreeAwardDataVec.length)
                for (let i = 0; i < message.FreeAwardDataVec.length; ++i)
                    $root.jlfw.AwardData.encode(message.FreeAwardDataVec[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.AwardTypeFlag);
            if (message.ReelType != null && Object.hasOwnProperty.call(message, "ReelType"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.ReelType);
            if (message.IsMain != null && Object.hasOwnProperty.call(message, "IsMain"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.IsMain);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.MaxFlag);
            if (message.FreeFirstWin != null && Object.hasOwnProperty.call(message, "FreeFirstWin"))
                writer.uint32(/* id 18, wireType 1 =*/145).double(message.FreeFirstWin);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlfw.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfw.RoundInfo
         * @static
         * @param {jlfw.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlfw.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfw.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfw.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlfw.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlfw.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 4: {
                        message.WheelType = reader.int32();
                        break;
                    }
                case 5: {
                        message.WheelMul = reader.double();
                        break;
                    }
                case 6: {
                        message.WildCount = reader.int32();
                        break;
                    }
                case 7: {
                        message.GetFree = reader.bool();
                        break;
                    }
                case 8: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.FreeWheelType = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.FreeWheelColumn && message.FreeWheelColumn.length))
                            message.FreeWheelColumn = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FreeWheelColumn.push(reader.int32());
                        } else
                            message.FreeWheelColumn.push(reader.int32());
                        break;
                    }
                case 12: {
                        if (!(message.FreeSymbol && message.FreeSymbol.length))
                            message.FreeSymbol = [];
                        message.FreeSymbol.push($root.jlfw.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 13: {
                        if (!(message.FreeAwardDataVec && message.FreeAwardDataVec.length))
                            message.FreeAwardDataVec = [];
                        message.FreeAwardDataVec.push($root.jlfw.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 14: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 15: {
                        message.ReelType = reader.int32();
                        break;
                    }
                case 16: {
                        message.IsMain = reader.bool();
                        break;
                    }
                case 17: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 18: {
                        message.FreeFirstWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfw.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfw.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlfw.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlfw.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlfw.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.WheelType != null && message.hasOwnProperty("WheelType"))
                if (!$util.isInteger(message.WheelType))
                    return "WheelType: integer expected";
            if (message.WheelMul != null && message.hasOwnProperty("WheelMul"))
                if (typeof message.WheelMul !== "number")
                    return "WheelMul: number expected";
            if (message.WildCount != null && message.hasOwnProperty("WildCount"))
                if (!$util.isInteger(message.WildCount))
                    return "WildCount: integer expected";
            if (message.GetFree != null && message.hasOwnProperty("GetFree"))
                if (typeof message.GetFree !== "boolean")
                    return "GetFree: boolean expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.FreeWheelType != null && message.hasOwnProperty("FreeWheelType"))
                if (!$util.isInteger(message.FreeWheelType))
                    return "FreeWheelType: integer expected";
            if (message.FreeWheelColumn != null && message.hasOwnProperty("FreeWheelColumn")) {
                if (!Array.isArray(message.FreeWheelColumn))
                    return "FreeWheelColumn: array expected";
                for (let i = 0; i < message.FreeWheelColumn.length; ++i)
                    if (!$util.isInteger(message.FreeWheelColumn[i]))
                        return "FreeWheelColumn: integer[] expected";
            }
            if (message.FreeSymbol != null && message.hasOwnProperty("FreeSymbol")) {
                if (!Array.isArray(message.FreeSymbol))
                    return "FreeSymbol: array expected";
                for (let i = 0; i < message.FreeSymbol.length; ++i) {
                    let error = $root.jlfw.Column.verify(message.FreeSymbol[i]);
                    if (error)
                        return "FreeSymbol." + error;
                }
            }
            if (message.FreeAwardDataVec != null && message.hasOwnProperty("FreeAwardDataVec")) {
                if (!Array.isArray(message.FreeAwardDataVec))
                    return "FreeAwardDataVec: array expected";
                for (let i = 0; i < message.FreeAwardDataVec.length; ++i) {
                    let error = $root.jlfw.AwardData.verify(message.FreeAwardDataVec[i]);
                    if (error)
                        return "FreeAwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.ReelType != null && message.hasOwnProperty("ReelType"))
                if (!$util.isInteger(message.ReelType))
                    return "ReelType: integer expected";
            if (message.IsMain != null && message.hasOwnProperty("IsMain"))
                if (typeof message.IsMain !== "boolean")
                    return "IsMain: boolean expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.FreeFirstWin != null && message.hasOwnProperty("FreeFirstWin"))
                if (typeof message.FreeFirstWin !== "number")
                    return "FreeFirstWin: number expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfw.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfw.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfw.RoundInfo)
                return object;
            let message = new $root.jlfw.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlfw.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlfw.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jlfw.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlfw.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlfw.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlfw.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.WheelType != null)
                message.WheelType = object.WheelType | 0;
            if (object.WheelMul != null)
                message.WheelMul = Number(object.WheelMul);
            if (object.WildCount != null)
                message.WildCount = object.WildCount | 0;
            if (object.GetFree != null)
                message.GetFree = Boolean(object.GetFree);
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.FreeWheelType != null)
                message.FreeWheelType = object.FreeWheelType | 0;
            if (object.FreeWheelColumn) {
                if (!Array.isArray(object.FreeWheelColumn))
                    throw TypeError(".jlfw.RoundInfo.FreeWheelColumn: array expected");
                message.FreeWheelColumn = [];
                for (let i = 0; i < object.FreeWheelColumn.length; ++i)
                    message.FreeWheelColumn[i] = object.FreeWheelColumn[i] | 0;
            }
            if (object.FreeSymbol) {
                if (!Array.isArray(object.FreeSymbol))
                    throw TypeError(".jlfw.RoundInfo.FreeSymbol: array expected");
                message.FreeSymbol = [];
                for (let i = 0; i < object.FreeSymbol.length; ++i) {
                    if (typeof object.FreeSymbol[i] !== "object")
                        throw TypeError(".jlfw.RoundInfo.FreeSymbol: object expected");
                    message.FreeSymbol[i] = $root.jlfw.Column.fromObject(object.FreeSymbol[i]);
                }
            }
            if (object.FreeAwardDataVec) {
                if (!Array.isArray(object.FreeAwardDataVec))
                    throw TypeError(".jlfw.RoundInfo.FreeAwardDataVec: array expected");
                message.FreeAwardDataVec = [];
                for (let i = 0; i < object.FreeAwardDataVec.length; ++i) {
                    if (typeof object.FreeAwardDataVec[i] !== "object")
                        throw TypeError(".jlfw.RoundInfo.FreeAwardDataVec: object expected");
                    message.FreeAwardDataVec[i] = $root.jlfw.AwardData.fromObject(object.FreeAwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.ReelType != null)
                message.ReelType = object.ReelType | 0;
            if (object.IsMain != null)
                message.IsMain = Boolean(object.IsMain);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.FreeFirstWin != null)
                message.FreeFirstWin = Number(object.FreeFirstWin);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfw.RoundInfo
         * @static
         * @param {jlfw.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.AwardDataVec = [];
                object.FreeWheelColumn = [];
                object.FreeSymbol = [];
                object.FreeAwardDataVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.WheelType = 0;
                object.WheelMul = 0;
                object.WildCount = 0;
                object.GetFree = false;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.FreeWheelType = 0;
                object.AwardTypeFlag = 0;
                object.ReelType = 0;
                object.IsMain = false;
                object.MaxFlag = false;
                object.FreeFirstWin = 0;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlfw.Column.toObject(message.Symbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlfw.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.WheelType != null && message.hasOwnProperty("WheelType"))
                object.WheelType = message.WheelType;
            if (message.WheelMul != null && message.hasOwnProperty("WheelMul"))
                object.WheelMul = options.json && !isFinite(message.WheelMul) ? String(message.WheelMul) : message.WheelMul;
            if (message.WildCount != null && message.hasOwnProperty("WildCount"))
                object.WildCount = message.WildCount;
            if (message.GetFree != null && message.hasOwnProperty("GetFree"))
                object.GetFree = message.GetFree;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.FreeWheelType != null && message.hasOwnProperty("FreeWheelType"))
                object.FreeWheelType = message.FreeWheelType;
            if (message.FreeWheelColumn && message.FreeWheelColumn.length) {
                object.FreeWheelColumn = [];
                for (let j = 0; j < message.FreeWheelColumn.length; ++j)
                    object.FreeWheelColumn[j] = message.FreeWheelColumn[j];
            }
            if (message.FreeSymbol && message.FreeSymbol.length) {
                object.FreeSymbol = [];
                for (let j = 0; j < message.FreeSymbol.length; ++j)
                    object.FreeSymbol[j] = $root.jlfw.Column.toObject(message.FreeSymbol[j], options);
            }
            if (message.FreeAwardDataVec && message.FreeAwardDataVec.length) {
                object.FreeAwardDataVec = [];
                for (let j = 0; j < message.FreeAwardDataVec.length; ++j)
                    object.FreeAwardDataVec[j] = $root.jlfw.AwardData.toObject(message.FreeAwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.ReelType != null && message.hasOwnProperty("ReelType"))
                object.ReelType = message.ReelType;
            if (message.IsMain != null && message.hasOwnProperty("IsMain"))
                object.IsMain = message.IsMain;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.FreeFirstWin != null && message.hasOwnProperty("FreeFirstWin"))
                object.FreeFirstWin = options.json && !isFinite(message.FreeFirstWin) ? String(message.FreeFirstWin) : message.FreeFirstWin;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlfw.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlfw.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfw.RoundInfo";
        };

        return RoundInfo;
    })();

    jlfw.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlfw
         * @interface ISpinAck
         * @property {Array.<jlfw.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlfw
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlfw.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlfw.IRoundInfo>} RoundQueue
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlfw.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlfw.SpinAck
         * @static
         * @param {jlfw.ISpinAck=} [properties] Properties to set
         * @returns {jlfw.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlfw.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlfw.SpinAck
         * @static
         * @param {jlfw.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlfw.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlfw.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlfw.SpinAck
         * @static
         * @param {jlfw.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlfw.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlfw.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlfw.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlfw.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 5: {
                        message.RTP = reader.double();
                        break;
                    }
                case 6: {
                        message.LID = reader.int32();
                        break;
                    }
                case 7: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlfw.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlfw.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlfw.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlfw.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlfw.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlfw.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlfw.SpinAck)
                return object;
            let message = new $root.jlfw.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlfw.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlfw.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlfw.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlfw.SpinAck
         * @static
         * @param {jlfw.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlfw.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlfw.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlfw.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlfw.SpinAck";
        };

        return SpinAck;
    })();

    return jlfw;
})();

export const jlgb = $root.jlgb = (() => {

    /**
     * Namespace jlgb.
     * @exports jlgb
     * @namespace
     */
    const jlgb = {};

    jlgb.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlgb
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlgb
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlgb.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlgb.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlgb.Column
         * @static
         * @param {jlgb.IColumn=} [properties] Properties to set
         * @returns {jlgb.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlgb.Column.verify|verify} messages.
         * @function encode
         * @memberof jlgb.Column
         * @static
         * @param {jlgb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlgb.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgb.Column
         * @static
         * @param {jlgb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlgb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgb.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlgb.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgb.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgb.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgb.Column)
                return object;
            let message = new $root.jlgb.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlgb.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgb.Column
         * @static
         * @param {jlgb.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlgb.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlgb.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgb.Column";
        };

        return Column;
    })();

    jlgb.Detail = (function() {

        /**
         * Properties of a Detail.
         * @memberof jlgb
         * @interface IDetail
         * @property {Array.<number>|null} [Detail] Detail Detail
         */

        /**
         * Constructs a new Detail.
         * @memberof jlgb
         * @classdesc Represents a Detail.
         * @implements IDetail
         * @constructor
         * @param {jlgb.IDetail=} [properties] Properties to set
         */
        function Detail(properties) {
            this.Detail = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Detail Detail.
         * @member {Array.<number>} Detail
         * @memberof jlgb.Detail
         * @instance
         */
        Detail.prototype.Detail = $util.emptyArray;

        /**
         * Creates a new Detail instance using the specified properties.
         * @function create
         * @memberof jlgb.Detail
         * @static
         * @param {jlgb.IDetail=} [properties] Properties to set
         * @returns {jlgb.Detail} Detail instance
         */
        Detail.create = function create(properties) {
            return new Detail(properties);
        };

        /**
         * Encodes the specified Detail message. Does not implicitly {@link jlgb.Detail.verify|verify} messages.
         * @function encode
         * @memberof jlgb.Detail
         * @static
         * @param {jlgb.IDetail} message Detail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Detail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Detail != null && message.Detail.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Detail.length; ++i)
                    writer.double(message.Detail[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Detail message, length delimited. Does not implicitly {@link jlgb.Detail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgb.Detail
         * @static
         * @param {jlgb.IDetail} message Detail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Detail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Detail message from the specified reader or buffer.
         * @function decode
         * @memberof jlgb.Detail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgb.Detail} Detail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Detail.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgb.Detail();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Detail && message.Detail.length))
                            message.Detail = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Detail.push(reader.double());
                        } else
                            message.Detail.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Detail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgb.Detail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgb.Detail} Detail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Detail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Detail message.
         * @function verify
         * @memberof jlgb.Detail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Detail.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Detail != null && message.hasOwnProperty("Detail")) {
                if (!Array.isArray(message.Detail))
                    return "Detail: array expected";
                for (let i = 0; i < message.Detail.length; ++i)
                    if (typeof message.Detail[i] !== "number")
                        return "Detail: number[] expected";
            }
            return null;
        };

        /**
         * Creates a Detail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgb.Detail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgb.Detail} Detail
         */
        Detail.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgb.Detail)
                return object;
            let message = new $root.jlgb.Detail();
            if (object.Detail) {
                if (!Array.isArray(object.Detail))
                    throw TypeError(".jlgb.Detail.Detail: array expected");
                message.Detail = [];
                for (let i = 0; i < object.Detail.length; ++i)
                    message.Detail[i] = Number(object.Detail[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Detail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgb.Detail
         * @static
         * @param {jlgb.Detail} message Detail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Detail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Detail = [];
            if (message.Detail && message.Detail.length) {
                object.Detail = [];
                for (let j = 0; j < message.Detail.length; ++j)
                    object.Detail[j] = options.json && !isFinite(message.Detail[j]) ? String(message.Detail[j]) : message.Detail[j];
            }
            return object;
        };

        /**
         * Converts this Detail to JSON.
         * @function toJSON
         * @memberof jlgb.Detail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Detail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Detail
         * @function getTypeUrl
         * @memberof jlgb.Detail
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Detail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgb.Detail";
        };

        return Detail;
    })();

    jlgb.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlgb
         * @interface ISinglePlate
         * @property {Array.<jlgb.IColumn>|null} [PlateSymbol] SinglePlate PlateSymbol
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {number|null} [PlateWin] SinglePlate PlateWin
         * @property {number|null} [LineWin] SinglePlate LineWin
         * @property {number|null} [SymbolWin] SinglePlate SymbolWin
         * @property {number|null} [PoolWin] SinglePlate PoolWin
         * @property {Array.<jlgb.IDetail>|null} [PoolDetail] SinglePlate PoolDetail
         * @property {number|null} [Mult] SinglePlate Mult
         * @property {Array.<number>|null} [Pool] SinglePlate Pool
         * @property {Array.<jlgb.IDetail>|null} [ShowDetail] SinglePlate ShowDetail
         * @property {number|null} [Plus] SinglePlate Plus
         * @property {number|null} [Light] SinglePlate Light
         * @property {number|null} [Line] SinglePlate Line
         * @property {number|null} [FreeRemainRound] SinglePlate FreeRemainRound
         * @property {number|null} [FreeTotalRound] SinglePlate FreeTotalRound
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlgb
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlgb.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.PlateSymbol = [];
            this.PoolDetail = [];
            this.Pool = [];
            this.ShowDetail = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate PlateSymbol.
         * @member {Array.<jlgb.IColumn>} PlateSymbol
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate PlateWin.
         * @member {number} PlateWin
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateWin = 0;

        /**
         * SinglePlate LineWin.
         * @member {number} LineWin
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.LineWin = 0;

        /**
         * SinglePlate SymbolWin.
         * @member {number} SymbolWin
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SymbolWin = 0;

        /**
         * SinglePlate PoolWin.
         * @member {number} PoolWin
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PoolWin = 0;

        /**
         * SinglePlate PoolDetail.
         * @member {Array.<jlgb.IDetail>} PoolDetail
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PoolDetail = $util.emptyArray;

        /**
         * SinglePlate Mult.
         * @member {number} Mult
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Mult = 0;

        /**
         * SinglePlate Pool.
         * @member {Array.<number>} Pool
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Pool = $util.emptyArray;

        /**
         * SinglePlate ShowDetail.
         * @member {Array.<jlgb.IDetail>} ShowDetail
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.ShowDetail = $util.emptyArray;

        /**
         * SinglePlate Plus.
         * @member {number} Plus
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Plus = 0;

        /**
         * SinglePlate Light.
         * @member {number} Light
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Light = 0;

        /**
         * SinglePlate Line.
         * @member {number} Line
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Line = 0;

        /**
         * SinglePlate FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeRemainRound = 0;

        /**
         * SinglePlate FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlgb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.FreeTotalRound = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlgb.SinglePlate
         * @static
         * @param {jlgb.ISinglePlate=} [properties] Properties to set
         * @returns {jlgb.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlgb.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlgb.SinglePlate
         * @static
         * @param {jlgb.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlgb.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardTypeFlag);
            if (message.PlateWin != null && Object.hasOwnProperty.call(message, "PlateWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.PlateWin);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.LineWin);
            if (message.SymbolWin != null && Object.hasOwnProperty.call(message, "SymbolWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.SymbolWin);
            if (message.PoolWin != null && Object.hasOwnProperty.call(message, "PoolWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.PoolWin);
            if (message.PoolDetail != null && message.PoolDetail.length)
                for (let i = 0; i < message.PoolDetail.length; ++i)
                    $root.jlgb.Detail.encode(message.PoolDetail[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Mult);
            if (message.Pool != null && message.Pool.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.Pool.length; ++i)
                    writer.double(message.Pool[i]);
                writer.ldelim();
            }
            if (message.ShowDetail != null && message.ShowDetail.length)
                for (let i = 0; i < message.ShowDetail.length; ++i)
                    $root.jlgb.Detail.encode(message.ShowDetail[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.Plus != null && Object.hasOwnProperty.call(message, "Plus"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.Plus);
            if (message.Light != null && Object.hasOwnProperty.call(message, "Light"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.Light);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.Line);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.FreeTotalRound);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlgb.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgb.SinglePlate
         * @static
         * @param {jlgb.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlgb.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgb.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgb.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlgb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 3: {
                        message.PlateWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 5: {
                        message.SymbolWin = reader.double();
                        break;
                    }
                case 6: {
                        message.PoolWin = reader.double();
                        break;
                    }
                case 7: {
                        if (!(message.PoolDetail && message.PoolDetail.length))
                            message.PoolDetail = [];
                        message.PoolDetail.push($root.jlgb.Detail.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.Mult = reader.double();
                        break;
                    }
                case 9: {
                        if (!(message.Pool && message.Pool.length))
                            message.Pool = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pool.push(reader.double());
                        } else
                            message.Pool.push(reader.double());
                        break;
                    }
                case 10: {
                        if (!(message.ShowDetail && message.ShowDetail.length))
                            message.ShowDetail = [];
                        message.ShowDetail.push($root.jlgb.Detail.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.Plus = reader.int32();
                        break;
                    }
                case 12: {
                        message.Light = reader.int32();
                        break;
                    }
                case 13: {
                        message.Line = reader.int32();
                        break;
                    }
                case 14: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 15: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgb.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgb.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlgb.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlgb.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                if (typeof message.PlateWin !== "number")
                    return "PlateWin: number expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.SymbolWin != null && message.hasOwnProperty("SymbolWin"))
                if (typeof message.SymbolWin !== "number")
                    return "SymbolWin: number expected";
            if (message.PoolWin != null && message.hasOwnProperty("PoolWin"))
                if (typeof message.PoolWin !== "number")
                    return "PoolWin: number expected";
            if (message.PoolDetail != null && message.hasOwnProperty("PoolDetail")) {
                if (!Array.isArray(message.PoolDetail))
                    return "PoolDetail: array expected";
                for (let i = 0; i < message.PoolDetail.length; ++i) {
                    let error = $root.jlgb.Detail.verify(message.PoolDetail[i]);
                    if (error)
                        return "PoolDetail." + error;
                }
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Pool != null && message.hasOwnProperty("Pool")) {
                if (!Array.isArray(message.Pool))
                    return "Pool: array expected";
                for (let i = 0; i < message.Pool.length; ++i)
                    if (typeof message.Pool[i] !== "number")
                        return "Pool: number[] expected";
            }
            if (message.ShowDetail != null && message.hasOwnProperty("ShowDetail")) {
                if (!Array.isArray(message.ShowDetail))
                    return "ShowDetail: array expected";
                for (let i = 0; i < message.ShowDetail.length; ++i) {
                    let error = $root.jlgb.Detail.verify(message.ShowDetail[i]);
                    if (error)
                        return "ShowDetail." + error;
                }
            }
            if (message.Plus != null && message.hasOwnProperty("Plus"))
                if (!$util.isInteger(message.Plus))
                    return "Plus: integer expected";
            if (message.Light != null && message.hasOwnProperty("Light"))
                if (!$util.isInteger(message.Light))
                    return "Light: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgb.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgb.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgb.SinglePlate)
                return object;
            let message = new $root.jlgb.SinglePlate();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlgb.SinglePlate.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlgb.SinglePlate.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlgb.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.PlateWin != null)
                message.PlateWin = Number(object.PlateWin);
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.SymbolWin != null)
                message.SymbolWin = Number(object.SymbolWin);
            if (object.PoolWin != null)
                message.PoolWin = Number(object.PoolWin);
            if (object.PoolDetail) {
                if (!Array.isArray(object.PoolDetail))
                    throw TypeError(".jlgb.SinglePlate.PoolDetail: array expected");
                message.PoolDetail = [];
                for (let i = 0; i < object.PoolDetail.length; ++i) {
                    if (typeof object.PoolDetail[i] !== "object")
                        throw TypeError(".jlgb.SinglePlate.PoolDetail: object expected");
                    message.PoolDetail[i] = $root.jlgb.Detail.fromObject(object.PoolDetail[i]);
                }
            }
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Pool) {
                if (!Array.isArray(object.Pool))
                    throw TypeError(".jlgb.SinglePlate.Pool: array expected");
                message.Pool = [];
                for (let i = 0; i < object.Pool.length; ++i)
                    message.Pool[i] = Number(object.Pool[i]);
            }
            if (object.ShowDetail) {
                if (!Array.isArray(object.ShowDetail))
                    throw TypeError(".jlgb.SinglePlate.ShowDetail: array expected");
                message.ShowDetail = [];
                for (let i = 0; i < object.ShowDetail.length; ++i) {
                    if (typeof object.ShowDetail[i] !== "object")
                        throw TypeError(".jlgb.SinglePlate.ShowDetail: object expected");
                    message.ShowDetail[i] = $root.jlgb.Detail.fromObject(object.ShowDetail[i]);
                }
            }
            if (object.Plus != null)
                message.Plus = object.Plus | 0;
            if (object.Light != null)
                message.Light = object.Light | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgb.SinglePlate
         * @static
         * @param {jlgb.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PoolDetail = [];
                object.Pool = [];
                object.ShowDetail = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.PlateWin = 0;
                object.LineWin = 0;
                object.SymbolWin = 0;
                object.PoolWin = 0;
                object.Mult = 0;
                object.Plus = 0;
                object.Light = 0;
                object.Line = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlgb.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                object.PlateWin = options.json && !isFinite(message.PlateWin) ? String(message.PlateWin) : message.PlateWin;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.SymbolWin != null && message.hasOwnProperty("SymbolWin"))
                object.SymbolWin = options.json && !isFinite(message.SymbolWin) ? String(message.SymbolWin) : message.SymbolWin;
            if (message.PoolWin != null && message.hasOwnProperty("PoolWin"))
                object.PoolWin = options.json && !isFinite(message.PoolWin) ? String(message.PoolWin) : message.PoolWin;
            if (message.PoolDetail && message.PoolDetail.length) {
                object.PoolDetail = [];
                for (let j = 0; j < message.PoolDetail.length; ++j)
                    object.PoolDetail[j] = $root.jlgb.Detail.toObject(message.PoolDetail[j], options);
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Pool && message.Pool.length) {
                object.Pool = [];
                for (let j = 0; j < message.Pool.length; ++j)
                    object.Pool[j] = options.json && !isFinite(message.Pool[j]) ? String(message.Pool[j]) : message.Pool[j];
            }
            if (message.ShowDetail && message.ShowDetail.length) {
                object.ShowDetail = [];
                for (let j = 0; j < message.ShowDetail.length; ++j)
                    object.ShowDetail[j] = $root.jlgb.Detail.toObject(message.ShowDetail[j], options);
            }
            if (message.Plus != null && message.hasOwnProperty("Plus"))
                object.Plus = message.Plus;
            if (message.Light != null && message.hasOwnProperty("Light"))
                object.Light = message.Light;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlgb.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlgb.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgb.SinglePlate";
        };

        return SinglePlate;
    })();

    jlgb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlgb
         * @interface ISpinAck
         * @property {Array.<jlgb.ISinglePlate>|null} [AckQueue] SpinAck AckQueue
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlgb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlgb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.AckQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck AckQueue.
         * @member {Array.<jlgb.ISinglePlate>} AckQueue
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckQueue = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlgb.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlgb.SpinAck
         * @static
         * @param {jlgb.ISpinAck=} [properties] Properties to set
         * @returns {jlgb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlgb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlgb.SpinAck
         * @static
         * @param {jlgb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AckQueue != null && message.AckQueue.length)
                for (let i = 0; i < message.AckQueue.length; ++i)
                    $root.jlgb.SinglePlate.encode(message.AckQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlgb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgb.SpinAck
         * @static
         * @param {jlgb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AckQueue && message.AckQueue.length))
                            message.AckQueue = [];
                        message.AckQueue.push($root.jlgb.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlgb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AckQueue != null && message.hasOwnProperty("AckQueue")) {
                if (!Array.isArray(message.AckQueue))
                    return "AckQueue: array expected";
                for (let i = 0; i < message.AckQueue.length; ++i) {
                    let error = $root.jlgb.SinglePlate.verify(message.AckQueue[i]);
                    if (error)
                        return "AckQueue." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgb.SpinAck)
                return object;
            let message = new $root.jlgb.SpinAck();
            if (object.AckQueue) {
                if (!Array.isArray(object.AckQueue))
                    throw TypeError(".jlgb.SpinAck.AckQueue: array expected");
                message.AckQueue = [];
                for (let i = 0; i < object.AckQueue.length; ++i) {
                    if (typeof object.AckQueue[i] !== "object")
                        throw TypeError(".jlgb.SpinAck.AckQueue: object expected");
                    message.AckQueue[i] = $root.jlgb.SinglePlate.fromObject(object.AckQueue[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgb.SpinAck
         * @static
         * @param {jlgb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AckQueue = [];
            if (options.defaults) {
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.AckQueue && message.AckQueue.length) {
                object.AckQueue = [];
                for (let j = 0; j < message.AckQueue.length; ++j)
                    object.AckQueue[j] = $root.jlgb.SinglePlate.toObject(message.AckQueue[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlgb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlgb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgb.SpinAck";
        };

        return SpinAck;
    })();

    jlgb.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlgb
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [OddList] GameInfoAck OddList
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlgb
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlgb.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.OddList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck OddList.
         * @member {Array.<number>} OddList
         * @memberof jlgb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.OddList = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {jlgb.IGameInfoAck=} [properties] Properties to set
         * @returns {jlgb.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlgb.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {jlgb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.OddList != null && message.OddList.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.OddList.length; ++i)
                    writer.double(message.OddList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlgb.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {jlgb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgb.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.OddList && message.OddList.length))
                            message.OddList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.OddList.push(reader.double());
                        } else
                            message.OddList.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.OddList != null && message.hasOwnProperty("OddList")) {
                if (!Array.isArray(message.OddList))
                    return "OddList: array expected";
                for (let i = 0; i < message.OddList.length; ++i)
                    if (typeof message.OddList[i] !== "number")
                        return "OddList: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgb.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgb.GameInfoAck)
                return object;
            let message = new $root.jlgb.GameInfoAck();
            if (object.OddList) {
                if (!Array.isArray(object.OddList))
                    throw TypeError(".jlgb.GameInfoAck.OddList: array expected");
                message.OddList = [];
                for (let i = 0; i < object.OddList.length; ++i)
                    message.OddList[i] = Number(object.OddList[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {jlgb.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.OddList = [];
            if (message.OddList && message.OddList.length) {
                object.OddList = [];
                for (let j = 0; j < message.OddList.length; ++j)
                    object.OddList[j] = options.json && !isFinite(message.OddList[j]) ? String(message.OddList[j]) : message.OddList[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlgb.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlgb.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgb.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlgb;
})();

export const jlge = $root.jlge = (() => {

    /**
     * Namespace jlge.
     * @exports jlge
     * @namespace
     */
    const jlge = {};

    jlge.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlge
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         * @property {number|null} [Energy] MSymbol Energy
         * @property {boolean|null} [Framed] MSymbol Framed
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlge
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlge.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlge.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlge.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * MSymbol Energy.
         * @member {number} Energy
         * @memberof jlge.MSymbol
         * @instance
         */
        MSymbol.prototype.Energy = 0;

        /**
         * MSymbol Framed.
         * @member {boolean} Framed
         * @memberof jlge.MSymbol
         * @instance
         */
        MSymbol.prototype.Framed = false;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlge.MSymbol
         * @static
         * @param {jlge.IMSymbol=} [properties] Properties to set
         * @returns {jlge.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlge.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlge.MSymbol
         * @static
         * @param {jlge.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            if (message.Energy != null && Object.hasOwnProperty.call(message, "Energy"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Energy);
            if (message.Framed != null && Object.hasOwnProperty.call(message, "Framed"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Framed);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlge.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.MSymbol
         * @static
         * @param {jlge.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                case 3: {
                        message.Energy = reader.int32();
                        break;
                    }
                case 4: {
                        message.Framed = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlge.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            if (message.Energy != null && message.hasOwnProperty("Energy"))
                if (!$util.isInteger(message.Energy))
                    return "Energy: integer expected";
            if (message.Framed != null && message.hasOwnProperty("Framed"))
                if (typeof message.Framed !== "boolean")
                    return "Framed: boolean expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.MSymbol)
                return object;
            let message = new $root.jlge.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            if (object.Energy != null)
                message.Energy = object.Energy | 0;
            if (object.Framed != null)
                message.Framed = Boolean(object.Framed);
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.MSymbol
         * @static
         * @param {jlge.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
                object.Energy = 0;
                object.Framed = false;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            if (message.Energy != null && message.hasOwnProperty("Energy"))
                object.Energy = message.Energy;
            if (message.Framed != null && message.hasOwnProperty("Framed"))
                object.Framed = message.Framed;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlge.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlge.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.MSymbol";
        };

        return MSymbol;
    })();

    jlge.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlge
         * @interface IMColumn
         * @property {Array.<jlge.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlge
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlge.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlge.IMSymbol>} MColumn
         * @memberof jlge.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlge.MColumn
         * @static
         * @param {jlge.IMColumn=} [properties] Properties to set
         * @returns {jlge.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlge.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlge.MColumn
         * @static
         * @param {jlge.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlge.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlge.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.MColumn
         * @static
         * @param {jlge.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlge.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlge.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlge.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.MColumn)
                return object;
            let message = new $root.jlge.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlge.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlge.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlge.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.MColumn
         * @static
         * @param {jlge.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlge.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlge.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlge.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.MColumn";
        };

        return MColumn;
    })();

    jlge.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlge
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlge
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlge.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlge.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlge.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlge.Grid
         * @static
         * @param {jlge.IGrid=} [properties] Properties to set
         * @returns {jlge.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlge.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlge.Grid
         * @static
         * @param {jlge.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlge.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.Grid
         * @static
         * @param {jlge.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlge.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.Grid)
                return object;
            let message = new $root.jlge.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.Grid
         * @static
         * @param {jlge.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlge.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlge.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.Grid";
        };

        return Grid;
    })();

    jlge.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlge
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlge.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlge
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlge.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlge.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlge.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlge.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlge.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlge.IGrid>} GridVec
         * @memberof jlge.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlge.AwardData
         * @static
         * @param {jlge.IAwardData=} [properties] Properties to set
         * @returns {jlge.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlge.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlge.AwardData
         * @static
         * @param {jlge.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlge.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlge.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.AwardData
         * @static
         * @param {jlge.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlge.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlge.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlge.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.AwardData)
                return object;
            let message = new $root.jlge.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlge.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlge.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlge.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.AwardData
         * @static
         * @param {jlge.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlge.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlge.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlge.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.AwardData";
        };

        return AwardData;
    })();

    jlge.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlge
         * @interface IComboStageInfo
         * @property {Array.<jlge.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlge.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlge.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlge.IMColumn>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlge
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlge.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlge.IMColumn>} ComboStageSymbol
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlge.IAwardData>} AwardDataVec
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlge.IMColumn>} FillSymbols
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlge.IMColumn>} ChangeSymbols
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlge.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {jlge.IComboStageInfo=} [properties] Properties to set
         * @returns {jlge.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlge.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {jlge.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlge.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlge.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlge.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlge.MColumn.encode(message.ChangeSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlge.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {jlge.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlge.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlge.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlge.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlge.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlge.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlge.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlge.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlge.MColumn.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.ComboStageInfo)
                return object;
            let message = new $root.jlge.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlge.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlge.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlge.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlge.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlge.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlge.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlge.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlge.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlge.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlge.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlge.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlge.MColumn.fromObject(object.ChangeSymbols[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {jlge.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlge.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlge.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlge.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlge.MColumn.toObject(message.ChangeSymbols[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlge.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlge.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlge.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlge
         * @interface IRoundInfo
         * @property {Array.<jlge.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlge
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlge.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlge.IComboStageInfo>} ComboStageData
         * @memberof jlge.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlge.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlge.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlge.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlge.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlge.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlge.RoundInfo
         * @static
         * @param {jlge.IRoundInfo=} [properties] Properties to set
         * @returns {jlge.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlge.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlge.RoundInfo
         * @static
         * @param {jlge.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlge.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ScatterCount);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlge.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.RoundInfo
         * @static
         * @param {jlge.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlge.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlge.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlge.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.RoundInfo)
                return object;
            let message = new $root.jlge.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlge.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlge.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlge.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.RoundInfo
         * @static
         * @param {jlge.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.ScatterCount = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlge.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlge.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlge.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.RoundInfo";
        };

        return RoundInfo;
    })();

    jlge.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlge
         * @interface ISpinAck
         * @property {Array.<jlge.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlge
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlge.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlge.IRoundInfo>} RoundQueue
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlge.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlge.SpinAck
         * @static
         * @param {jlge.ISpinAck=} [properties] Properties to set
         * @returns {jlge.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlge.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlge.SpinAck
         * @static
         * @param {jlge.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlge.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlge.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.SpinAck
         * @static
         * @param {jlge.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlge.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlge.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlge.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.SpinAck)
                return object;
            let message = new $root.jlge.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlge.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlge.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlge.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.SpinAck
         * @static
         * @param {jlge.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlge.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlge.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlge.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.SpinAck";
        };

        return SpinAck;
    })();

    jlge.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlge
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlge
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlge.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlge.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlge.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlge.GameInfoData
         * @static
         * @param {jlge.IGameInfoData=} [properties] Properties to set
         * @returns {jlge.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlge.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlge.GameInfoData
         * @static
         * @param {jlge.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlge.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge.GameInfoData
         * @static
         * @param {jlge.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlge.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlge.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge.GameInfoData)
                return object;
            let message = new $root.jlge.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlge.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge.GameInfoData
         * @static
         * @param {jlge.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlge.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlge.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlge;
})();

export const jlge2 = $root.jlge2 = (() => {

    /**
     * Namespace jlge2.
     * @exports jlge2
     * @namespace
     */
    const jlge2 = {};

    jlge2.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlge2
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         * @property {number|null} [Energy] MSymbol Energy
         * @property {boolean|null} [Framed] MSymbol Framed
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlge2
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlge2.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlge2.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlge2.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * MSymbol Energy.
         * @member {number} Energy
         * @memberof jlge2.MSymbol
         * @instance
         */
        MSymbol.prototype.Energy = 0;

        /**
         * MSymbol Framed.
         * @member {boolean} Framed
         * @memberof jlge2.MSymbol
         * @instance
         */
        MSymbol.prototype.Framed = false;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlge2.MSymbol
         * @static
         * @param {jlge2.IMSymbol=} [properties] Properties to set
         * @returns {jlge2.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlge2.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlge2.MSymbol
         * @static
         * @param {jlge2.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            if (message.Energy != null && Object.hasOwnProperty.call(message, "Energy"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Energy);
            if (message.Framed != null && Object.hasOwnProperty.call(message, "Framed"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Framed);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlge2.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.MSymbol
         * @static
         * @param {jlge2.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                case 3: {
                        message.Energy = reader.int32();
                        break;
                    }
                case 4: {
                        message.Framed = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlge2.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            if (message.Energy != null && message.hasOwnProperty("Energy"))
                if (!$util.isInteger(message.Energy))
                    return "Energy: integer expected";
            if (message.Framed != null && message.hasOwnProperty("Framed"))
                if (typeof message.Framed !== "boolean")
                    return "Framed: boolean expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.MSymbol)
                return object;
            let message = new $root.jlge2.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            if (object.Energy != null)
                message.Energy = object.Energy | 0;
            if (object.Framed != null)
                message.Framed = Boolean(object.Framed);
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.MSymbol
         * @static
         * @param {jlge2.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
                object.Energy = 0;
                object.Framed = false;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            if (message.Energy != null && message.hasOwnProperty("Energy"))
                object.Energy = message.Energy;
            if (message.Framed != null && message.hasOwnProperty("Framed"))
                object.Framed = message.Framed;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlge2.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlge2.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.MSymbol";
        };

        return MSymbol;
    })();

    jlge2.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlge2
         * @interface IMColumn
         * @property {Array.<jlge2.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlge2
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlge2.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlge2.IMSymbol>} MColumn
         * @memberof jlge2.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlge2.MColumn
         * @static
         * @param {jlge2.IMColumn=} [properties] Properties to set
         * @returns {jlge2.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlge2.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlge2.MColumn
         * @static
         * @param {jlge2.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlge2.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlge2.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.MColumn
         * @static
         * @param {jlge2.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlge2.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlge2.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlge2.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.MColumn)
                return object;
            let message = new $root.jlge2.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlge2.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlge2.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlge2.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.MColumn
         * @static
         * @param {jlge2.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlge2.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlge2.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlge2.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.MColumn";
        };

        return MColumn;
    })();

    jlge2.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlge2
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlge2
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlge2.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlge2.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlge2.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlge2.Grid
         * @static
         * @param {jlge2.IGrid=} [properties] Properties to set
         * @returns {jlge2.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlge2.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlge2.Grid
         * @static
         * @param {jlge2.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlge2.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.Grid
         * @static
         * @param {jlge2.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlge2.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.Grid)
                return object;
            let message = new $root.jlge2.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.Grid
         * @static
         * @param {jlge2.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlge2.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlge2.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.Grid";
        };

        return Grid;
    })();

    jlge2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlge2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlge2.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlge2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlge2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlge2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlge2.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlge2.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlge2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlge2.IGrid>} GridVec
         * @memberof jlge2.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlge2.AwardData
         * @static
         * @param {jlge2.IAwardData=} [properties] Properties to set
         * @returns {jlge2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlge2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlge2.AwardData
         * @static
         * @param {jlge2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlge2.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlge2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.AwardData
         * @static
         * @param {jlge2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlge2.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlge2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlge2.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.AwardData)
                return object;
            let message = new $root.jlge2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlge2.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlge2.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlge2.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.AwardData
         * @static
         * @param {jlge2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlge2.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlge2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlge2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.AwardData";
        };

        return AwardData;
    })();

    jlge2.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlge2
         * @interface IComboStageInfo
         * @property {Array.<jlge2.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {Array.<jlge2.IMColumn>|null} [ComboStageSymbolLog] ComboStageInfo ComboStageSymbolLog
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlge2.IGrid>|null} [BonusEffectedPos] ComboStageInfo BonusEffectedPos
         * @property {Array.<jlge2.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlge2.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlge2.IMColumn>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlge2
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlge2.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.ComboStageSymbolLog = [];
            this.BonusEffectedPos = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlge2.IMColumn>} ComboStageSymbol
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageSymbolLog.
         * @member {Array.<jlge2.IMColumn>} ComboStageSymbolLog
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbolLog = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo BonusEffectedPos.
         * @member {Array.<jlge2.IGrid>} BonusEffectedPos
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.BonusEffectedPos = $util.emptyArray;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlge2.IAwardData>} AwardDataVec
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlge2.IMColumn>} FillSymbols
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlge2.IMColumn>} ChangeSymbols
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlge2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {jlge2.IComboStageInfo=} [properties] Properties to set
         * @returns {jlge2.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlge2.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {jlge2.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlge2.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageSymbolLog != null && message.ComboStageSymbolLog.length)
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i)
                    $root.jlge2.MColumn.encode(message.ComboStageSymbolLog[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.ComboStageMult);
            if (message.BonusEffectedPos != null && message.BonusEffectedPos.length)
                for (let i = 0; i < message.BonusEffectedPos.length; ++i)
                    $root.jlge2.Grid.encode(message.BonusEffectedPos[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlge2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlge2.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlge2.MColumn.encode(message.ChangeSymbols[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlge2.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {jlge2.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlge2.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.ComboStageSymbolLog && message.ComboStageSymbolLog.length))
                            message.ComboStageSymbolLog = [];
                        message.ComboStageSymbolLog.push($root.jlge2.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.BonusEffectedPos && message.BonusEffectedPos.length))
                            message.BonusEffectedPos = [];
                        message.BonusEffectedPos.push($root.jlge2.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlge2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlge2.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlge2.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlge2.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageSymbolLog != null && message.hasOwnProperty("ComboStageSymbolLog")) {
                if (!Array.isArray(message.ComboStageSymbolLog))
                    return "ComboStageSymbolLog: array expected";
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i) {
                    let error = $root.jlge2.MColumn.verify(message.ComboStageSymbolLog[i]);
                    if (error)
                        return "ComboStageSymbolLog." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.BonusEffectedPos != null && message.hasOwnProperty("BonusEffectedPos")) {
                if (!Array.isArray(message.BonusEffectedPos))
                    return "BonusEffectedPos: array expected";
                for (let i = 0; i < message.BonusEffectedPos.length; ++i) {
                    let error = $root.jlge2.Grid.verify(message.BonusEffectedPos[i]);
                    if (error)
                        return "BonusEffectedPos." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlge2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlge2.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlge2.MColumn.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.ComboStageInfo)
                return object;
            let message = new $root.jlge2.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlge2.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlge2.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlge2.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageSymbolLog) {
                if (!Array.isArray(object.ComboStageSymbolLog))
                    throw TypeError(".jlge2.ComboStageInfo.ComboStageSymbolLog: array expected");
                message.ComboStageSymbolLog = [];
                for (let i = 0; i < object.ComboStageSymbolLog.length; ++i) {
                    if (typeof object.ComboStageSymbolLog[i] !== "object")
                        throw TypeError(".jlge2.ComboStageInfo.ComboStageSymbolLog: object expected");
                    message.ComboStageSymbolLog[i] = $root.jlge2.MColumn.fromObject(object.ComboStageSymbolLog[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.BonusEffectedPos) {
                if (!Array.isArray(object.BonusEffectedPos))
                    throw TypeError(".jlge2.ComboStageInfo.BonusEffectedPos: array expected");
                message.BonusEffectedPos = [];
                for (let i = 0; i < object.BonusEffectedPos.length; ++i) {
                    if (typeof object.BonusEffectedPos[i] !== "object")
                        throw TypeError(".jlge2.ComboStageInfo.BonusEffectedPos: object expected");
                    message.BonusEffectedPos[i] = $root.jlge2.Grid.fromObject(object.BonusEffectedPos[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlge2.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlge2.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlge2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlge2.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlge2.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlge2.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlge2.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlge2.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlge2.MColumn.fromObject(object.ChangeSymbols[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {jlge2.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.ComboStageSymbolLog = [];
                object.BonusEffectedPos = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlge2.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageSymbolLog && message.ComboStageSymbolLog.length) {
                object.ComboStageSymbolLog = [];
                for (let j = 0; j < message.ComboStageSymbolLog.length; ++j)
                    object.ComboStageSymbolLog[j] = $root.jlge2.MColumn.toObject(message.ComboStageSymbolLog[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.BonusEffectedPos && message.BonusEffectedPos.length) {
                object.BonusEffectedPos = [];
                for (let j = 0; j < message.BonusEffectedPos.length; ++j)
                    object.BonusEffectedPos[j] = $root.jlge2.Grid.toObject(message.BonusEffectedPos[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlge2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlge2.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlge2.MColumn.toObject(message.ChangeSymbols[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlge2.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlge2.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlge2.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlge2
         * @interface IRoundInfo
         * @property {Array.<jlge2.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {Array.<jlge2.IGrid>|null} [KeepPos] RoundInfo KeepPos
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<jlge2.IGrid>|null} [KeptWildPos] RoundInfo KeptWildPos
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlge2
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlge2.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            this.KeepPos = [];
            this.KeptWildPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlge2.IComboStageInfo>} ComboStageData
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo KeepPos.
         * @member {Array.<jlge2.IGrid>} KeepPos
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.KeepPos = $util.emptyArray;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo KeptWildPos.
         * @member {Array.<jlge2.IGrid>} KeptWildPos
         * @memberof jlge2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.KeptWildPos = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlge2.RoundInfo
         * @static
         * @param {jlge2.IRoundInfo=} [properties] Properties to set
         * @returns {jlge2.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlge2.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlge2.RoundInfo
         * @static
         * @param {jlge2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlge2.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AddRound);
            if (message.KeepPos != null && message.KeepPos.length)
                for (let i = 0; i < message.KeepPos.length; ++i)
                    $root.jlge2.Grid.encode(message.KeepPos[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.ScatterCount);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.MaxFlag);
            if (message.KeptWildPos != null && message.KeptWildPos.length)
                for (let i = 0; i < message.KeptWildPos.length; ++i)
                    $root.jlge2.Grid.encode(message.KeptWildPos[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlge2.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.RoundInfo
         * @static
         * @param {jlge2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlge2.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 8: {
                        if (!(message.KeepPos && message.KeepPos.length))
                            message.KeepPos = [];
                        message.KeepPos.push($root.jlge2.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 9: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 10: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 11: {
                        if (!(message.KeptWildPos && message.KeptWildPos.length))
                            message.KeptWildPos = [];
                        message.KeptWildPos.push($root.jlge2.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlge2.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlge2.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.KeepPos != null && message.hasOwnProperty("KeepPos")) {
                if (!Array.isArray(message.KeepPos))
                    return "KeepPos: array expected";
                for (let i = 0; i < message.KeepPos.length; ++i) {
                    let error = $root.jlge2.Grid.verify(message.KeepPos[i]);
                    if (error)
                        return "KeepPos." + error;
                }
            }
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.KeptWildPos != null && message.hasOwnProperty("KeptWildPos")) {
                if (!Array.isArray(message.KeptWildPos))
                    return "KeptWildPos: array expected";
                for (let i = 0; i < message.KeptWildPos.length; ++i) {
                    let error = $root.jlge2.Grid.verify(message.KeptWildPos[i]);
                    if (error)
                        return "KeptWildPos." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.RoundInfo)
                return object;
            let message = new $root.jlge2.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlge2.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlge2.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlge2.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.KeepPos) {
                if (!Array.isArray(object.KeepPos))
                    throw TypeError(".jlge2.RoundInfo.KeepPos: array expected");
                message.KeepPos = [];
                for (let i = 0; i < object.KeepPos.length; ++i) {
                    if (typeof object.KeepPos[i] !== "object")
                        throw TypeError(".jlge2.RoundInfo.KeepPos: object expected");
                    message.KeepPos[i] = $root.jlge2.Grid.fromObject(object.KeepPos[i]);
                }
            }
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.KeptWildPos) {
                if (!Array.isArray(object.KeptWildPos))
                    throw TypeError(".jlge2.RoundInfo.KeptWildPos: array expected");
                message.KeptWildPos = [];
                for (let i = 0; i < object.KeptWildPos.length; ++i) {
                    if (typeof object.KeptWildPos[i] !== "object")
                        throw TypeError(".jlge2.RoundInfo.KeptWildPos: object expected");
                    message.KeptWildPos[i] = $root.jlge2.Grid.fromObject(object.KeptWildPos[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.RoundInfo
         * @static
         * @param {jlge2.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageData = [];
                object.KeepPos = [];
                object.KeptWildPos = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.AddRound = 0;
                object.ScatterCount = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlge2.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.KeepPos && message.KeepPos.length) {
                object.KeepPos = [];
                for (let j = 0; j < message.KeepPos.length; ++j)
                    object.KeepPos[j] = $root.jlge2.Grid.toObject(message.KeepPos[j], options);
            }
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.KeptWildPos && message.KeptWildPos.length) {
                object.KeptWildPos = [];
                for (let j = 0; j < message.KeptWildPos.length; ++j)
                    object.KeptWildPos[j] = $root.jlge2.Grid.toObject(message.KeptWildPos[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlge2.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlge2.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.RoundInfo";
        };

        return RoundInfo;
    })();

    jlge2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlge2
         * @interface ISpinAck
         * @property {Array.<jlge2.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlge2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlge2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlge2.IRoundInfo>} RoundQueue
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlge2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlge2.SpinAck
         * @static
         * @param {jlge2.ISpinAck=} [properties] Properties to set
         * @returns {jlge2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlge2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlge2.SpinAck
         * @static
         * @param {jlge2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlge2.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlge2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.SpinAck
         * @static
         * @param {jlge2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlge2.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlge2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlge2.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.SpinAck)
                return object;
            let message = new $root.jlge2.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlge2.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlge2.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlge2.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.SpinAck
         * @static
         * @param {jlge2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlge2.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlge2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlge2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.SpinAck";
        };

        return SpinAck;
    })();

    jlge2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlge2
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlge2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlge2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlge2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlge2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlge2.GameInfoData
         * @static
         * @param {jlge2.IGameInfoData=} [properties] Properties to set
         * @returns {jlge2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlge2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlge2.GameInfoData
         * @static
         * @param {jlge2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlge2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlge2.GameInfoData
         * @static
         * @param {jlge2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlge2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlge2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlge2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlge2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlge2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlge2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlge2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlge2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlge2.GameInfoData)
                return object;
            let message = new $root.jlge2.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlge2.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlge2.GameInfoData
         * @static
         * @param {jlge2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlge2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlge2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlge2.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlge2;
})();

export const jlgj = $root.jlgj = (() => {

    /**
     * Namespace jlgj.
     * @exports jlgj
     * @namespace
     */
    const jlgj = {};

    jlgj.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlgj
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlgj
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlgj.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlgj.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlgj.Column
         * @static
         * @param {jlgj.IColumn=} [properties] Properties to set
         * @returns {jlgj.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlgj.Column.verify|verify} messages.
         * @function encode
         * @memberof jlgj.Column
         * @static
         * @param {jlgj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlgj.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgj.Column
         * @static
         * @param {jlgj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlgj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgj.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlgj.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgj.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgj.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgj.Column)
                return object;
            let message = new $root.jlgj.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlgj.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgj.Column
         * @static
         * @param {jlgj.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlgj.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlgj.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgj.Column";
        };

        return Column;
    })();

    jlgj.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlgj
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlgj
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlgj.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlgj.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlgj.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlgj.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlgj.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlgj.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlgj.AwardData
         * @static
         * @param {jlgj.IAwardData=} [properties] Properties to set
         * @returns {jlgj.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlgj.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlgj.AwardData
         * @static
         * @param {jlgj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mult);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlgj.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgj.AwardData
         * @static
         * @param {jlgj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgj.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        message.Mult = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlgj.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgj.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgj.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgj.AwardData)
                return object;
            let message = new $root.jlgj.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgj.AwardData
         * @static
         * @param {jlgj.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlgj.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlgj.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgj.AwardData";
        };

        return AwardData;
    })();

    jlgj.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlgj
         * @interface IRoundInfo
         * @property {Array.<jlgj.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlgj.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RespinColumn] RoundInfo RespinColumn
         * @property {number|null} [ToWildColumn] RoundInfo ToWildColumn
         * @property {number|null} [Wheel] RoundInfo Wheel
         * @property {number|null} [WheelWin] RoundInfo WheelWin
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlgj
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlgj.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlgj.IColumn>} PlateSymbol
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlgj.IAwardData>} AwardDataVec
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RespinColumn.
         * @member {number} RespinColumn
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RespinColumn = 0;

        /**
         * RoundInfo ToWildColumn.
         * @member {number} ToWildColumn
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ToWildColumn = 0;

        /**
         * RoundInfo Wheel.
         * @member {number} Wheel
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Wheel = 0;

        /**
         * RoundInfo WheelWin.
         * @member {number} WheelWin
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WheelWin = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlgj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlgj.RoundInfo
         * @static
         * @param {jlgj.IRoundInfo=} [properties] Properties to set
         * @returns {jlgj.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlgj.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgj.RoundInfo
         * @static
         * @param {jlgj.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlgj.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlgj.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.RespinColumn != null && Object.hasOwnProperty.call(message, "RespinColumn"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.RespinColumn);
            if (message.ToWildColumn != null && Object.hasOwnProperty.call(message, "ToWildColumn"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ToWildColumn);
            if (message.Wheel != null && Object.hasOwnProperty.call(message, "Wheel"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Wheel);
            if (message.WheelWin != null && Object.hasOwnProperty.call(message, "WheelWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.WheelWin);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlgj.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgj.RoundInfo
         * @static
         * @param {jlgj.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgj.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgj.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgj.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlgj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlgj.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.RespinColumn = reader.int32();
                        break;
                    }
                case 5: {
                        message.ToWildColumn = reader.int32();
                        break;
                    }
                case 6: {
                        message.Wheel = reader.double();
                        break;
                    }
                case 7: {
                        message.WheelWin = reader.double();
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgj.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgj.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlgj.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlgj.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlgj.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RespinColumn != null && message.hasOwnProperty("RespinColumn"))
                if (!$util.isInteger(message.RespinColumn))
                    return "RespinColumn: integer expected";
            if (message.ToWildColumn != null && message.hasOwnProperty("ToWildColumn"))
                if (!$util.isInteger(message.ToWildColumn))
                    return "ToWildColumn: integer expected";
            if (message.Wheel != null && message.hasOwnProperty("Wheel"))
                if (typeof message.Wheel !== "number")
                    return "Wheel: number expected";
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                if (typeof message.WheelWin !== "number")
                    return "WheelWin: number expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgj.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgj.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgj.RoundInfo)
                return object;
            let message = new $root.jlgj.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlgj.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlgj.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlgj.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlgj.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlgj.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlgj.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RespinColumn != null)
                message.RespinColumn = object.RespinColumn | 0;
            if (object.ToWildColumn != null)
                message.ToWildColumn = object.ToWildColumn | 0;
            if (object.Wheel != null)
                message.Wheel = Number(object.Wheel);
            if (object.WheelWin != null)
                message.WheelWin = Number(object.WheelWin);
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgj.RoundInfo
         * @static
         * @param {jlgj.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RespinColumn = 0;
                object.ToWildColumn = 0;
                object.Wheel = 0;
                object.WheelWin = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlgj.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlgj.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RespinColumn != null && message.hasOwnProperty("RespinColumn"))
                object.RespinColumn = message.RespinColumn;
            if (message.ToWildColumn != null && message.hasOwnProperty("ToWildColumn"))
                object.ToWildColumn = message.ToWildColumn;
            if (message.Wheel != null && message.hasOwnProperty("Wheel"))
                object.Wheel = options.json && !isFinite(message.Wheel) ? String(message.Wheel) : message.Wheel;
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                object.WheelWin = options.json && !isFinite(message.WheelWin) ? String(message.WheelWin) : message.WheelWin;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlgj.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlgj.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgj.RoundInfo";
        };

        return RoundInfo;
    })();

    jlgj.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlgj
         * @interface ISpinAck
         * @property {Array.<jlgj.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlgj
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlgj.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlgj.IRoundInfo>} RoundQueue
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlgj.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlgj.SpinAck
         * @static
         * @param {jlgj.ISpinAck=} [properties] Properties to set
         * @returns {jlgj.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlgj.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlgj.SpinAck
         * @static
         * @param {jlgj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlgj.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlgj.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgj.SpinAck
         * @static
         * @param {jlgj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgj.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlgj.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 4: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 5: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 6: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlgj.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlgj.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgj.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgj.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgj.SpinAck)
                return object;
            let message = new $root.jlgj.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlgj.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlgj.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlgj.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgj.SpinAck
         * @static
         * @param {jlgj.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlgj.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlgj.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlgj.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgj.SpinAck";
        };

        return SpinAck;
    })();

    jlgj.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlgj
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         * @property {Array.<number>|null} [WheelList] GameInfoAck WheelList
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlgj
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlgj.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            this.WheelList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlgj.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoAck WheelList.
         * @member {Array.<number>} WheelList
         * @memberof jlgj.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.WheelList = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {jlgj.IGameInfoAck=} [properties] Properties to set
         * @returns {jlgj.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlgj.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {jlgj.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.WheelList != null && message.WheelList.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.WheelList.length; ++i)
                    writer.double(message.WheelList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlgj.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {jlgj.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgj.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgj.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 6: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 9: {
                        if (!(message.WheelList && message.WheelList.length))
                            message.WheelList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WheelList.push(reader.double());
                        } else
                            message.WheelList.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgj.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.WheelList != null && message.hasOwnProperty("WheelList")) {
                if (!Array.isArray(message.WheelList))
                    return "WheelList: array expected";
                for (let i = 0; i < message.WheelList.length; ++i)
                    if (typeof message.WheelList[i] !== "number")
                        return "WheelList: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgj.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgj.GameInfoAck)
                return object;
            let message = new $root.jlgj.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlgj.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.WheelList) {
                if (!Array.isArray(object.WheelList))
                    throw TypeError(".jlgj.GameInfoAck.WheelList: array expected");
                message.WheelList = [];
                for (let i = 0; i < object.WheelList.length; ++i)
                    message.WheelList[i] = Number(object.WheelList[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {jlgj.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.WheelList = [];
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.WheelList && message.WheelList.length) {
                object.WheelList = [];
                for (let j = 0; j < message.WheelList.length; ++j)
                    object.WheelList[j] = options.json && !isFinite(message.WheelList[j]) ? String(message.WheelList[j]) : message.WheelList[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlgj.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlgj.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgj.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlgj;
})();

export const jlgom = $root.jlgom = (() => {

    /**
     * Namespace jlgom.
     * @exports jlgom
     * @namespace
     */
    const jlgom = {};

    jlgom.FreeGameSet = (function() {

        /**
         * Properties of a FreeGameSet.
         * @memberof jlgom
         * @interface IFreeGameSet
         * @property {number|null} [LineMode] FreeGameSet LineMode
         * @property {number|null} [FreeGameTimes] FreeGameSet FreeGameTimes
         */

        /**
         * Constructs a new FreeGameSet.
         * @memberof jlgom
         * @classdesc Represents a FreeGameSet.
         * @implements IFreeGameSet
         * @constructor
         * @param {jlgom.IFreeGameSet=} [properties] Properties to set
         */
        function FreeGameSet(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeGameSet LineMode.
         * @member {number} LineMode
         * @memberof jlgom.FreeGameSet
         * @instance
         */
        FreeGameSet.prototype.LineMode = 0;

        /**
         * FreeGameSet FreeGameTimes.
         * @member {number} FreeGameTimes
         * @memberof jlgom.FreeGameSet
         * @instance
         */
        FreeGameSet.prototype.FreeGameTimes = 0;

        /**
         * Creates a new FreeGameSet instance using the specified properties.
         * @function create
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {jlgom.IFreeGameSet=} [properties] Properties to set
         * @returns {jlgom.FreeGameSet} FreeGameSet instance
         */
        FreeGameSet.create = function create(properties) {
            return new FreeGameSet(properties);
        };

        /**
         * Encodes the specified FreeGameSet message. Does not implicitly {@link jlgom.FreeGameSet.verify|verify} messages.
         * @function encode
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {jlgom.IFreeGameSet} message FreeGameSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameSet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LineMode != null && Object.hasOwnProperty.call(message, "LineMode"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.LineMode);
            if (message.FreeGameTimes != null && Object.hasOwnProperty.call(message, "FreeGameTimes"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.FreeGameTimes);
            return writer;
        };

        /**
         * Encodes the specified FreeGameSet message, length delimited. Does not implicitly {@link jlgom.FreeGameSet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {jlgom.IFreeGameSet} message FreeGameSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameSet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeGameSet message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.FreeGameSet} FreeGameSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameSet.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.FreeGameSet();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LineMode = reader.int32();
                        break;
                    }
                case 2: {
                        message.FreeGameTimes = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeGameSet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.FreeGameSet} FreeGameSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameSet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeGameSet message.
         * @function verify
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeGameSet.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LineMode != null && message.hasOwnProperty("LineMode"))
                if (!$util.isInteger(message.LineMode))
                    return "LineMode: integer expected";
            if (message.FreeGameTimes != null && message.hasOwnProperty("FreeGameTimes"))
                if (!$util.isInteger(message.FreeGameTimes))
                    return "FreeGameTimes: integer expected";
            return null;
        };

        /**
         * Creates a FreeGameSet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.FreeGameSet} FreeGameSet
         */
        FreeGameSet.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.FreeGameSet)
                return object;
            let message = new $root.jlgom.FreeGameSet();
            if (object.LineMode != null)
                message.LineMode = object.LineMode | 0;
            if (object.FreeGameTimes != null)
                message.FreeGameTimes = object.FreeGameTimes | 0;
            return message;
        };

        /**
         * Creates a plain object from a FreeGameSet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {jlgom.FreeGameSet} message FreeGameSet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeGameSet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.LineMode = 0;
                object.FreeGameTimes = 0;
            }
            if (message.LineMode != null && message.hasOwnProperty("LineMode"))
                object.LineMode = message.LineMode;
            if (message.FreeGameTimes != null && message.hasOwnProperty("FreeGameTimes"))
                object.FreeGameTimes = message.FreeGameTimes;
            return object;
        };

        /**
         * Converts this FreeGameSet to JSON.
         * @function toJSON
         * @memberof jlgom.FreeGameSet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeGameSet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeGameSet
         * @function getTypeUrl
         * @memberof jlgom.FreeGameSet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeGameSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.FreeGameSet";
        };

        return FreeGameSet;
    })();

    jlgom.Award = (function() {

        /**
         * Properties of an Award.
         * @memberof jlgom
         * @interface IAward
         * @property {number|null} [AwardMode] Award AwardMode
         * @property {number|null} [AwardItem] Award AwardItem
         * @property {number|null} [Side] Award Side
         * @property {number|null} [LineIndex] Award LineIndex
         * @property {number|null} [Odds] Award Odds
         */

        /**
         * Constructs a new Award.
         * @memberof jlgom
         * @classdesc Represents an Award.
         * @implements IAward
         * @constructor
         * @param {jlgom.IAward=} [properties] Properties to set
         */
        function Award(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Award AwardMode.
         * @member {number} AwardMode
         * @memberof jlgom.Award
         * @instance
         */
        Award.prototype.AwardMode = 0;

        /**
         * Award AwardItem.
         * @member {number} AwardItem
         * @memberof jlgom.Award
         * @instance
         */
        Award.prototype.AwardItem = 0;

        /**
         * Award Side.
         * @member {number} Side
         * @memberof jlgom.Award
         * @instance
         */
        Award.prototype.Side = 0;

        /**
         * Award LineIndex.
         * @member {number} LineIndex
         * @memberof jlgom.Award
         * @instance
         */
        Award.prototype.LineIndex = 0;

        /**
         * Award Odds.
         * @member {number} Odds
         * @memberof jlgom.Award
         * @instance
         */
        Award.prototype.Odds = 0;

        /**
         * Creates a new Award instance using the specified properties.
         * @function create
         * @memberof jlgom.Award
         * @static
         * @param {jlgom.IAward=} [properties] Properties to set
         * @returns {jlgom.Award} Award instance
         */
        Award.create = function create(properties) {
            return new Award(properties);
        };

        /**
         * Encodes the specified Award message. Does not implicitly {@link jlgom.Award.verify|verify} messages.
         * @function encode
         * @memberof jlgom.Award
         * @static
         * @param {jlgom.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardMode != null && Object.hasOwnProperty.call(message, "AwardMode"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardMode);
            if (message.AwardItem != null && Object.hasOwnProperty.call(message, "AwardItem"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardItem);
            if (message.Side != null && Object.hasOwnProperty.call(message, "Side"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Side);
            if (message.LineIndex != null && Object.hasOwnProperty.call(message, "LineIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.LineIndex);
            if (message.Odds != null && Object.hasOwnProperty.call(message, "Odds"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Odds);
            return writer;
        };

        /**
         * Encodes the specified Award message, length delimited. Does not implicitly {@link jlgom.Award.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.Award
         * @static
         * @param {jlgom.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Award message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.Award();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardMode = reader.int32();
                        break;
                    }
                case 2: {
                        message.AwardItem = reader.int32();
                        break;
                    }
                case 3: {
                        message.Side = reader.int32();
                        break;
                    }
                case 4: {
                        message.LineIndex = reader.int32();
                        break;
                    }
                case 5: {
                        message.Odds = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Award message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Award message.
         * @function verify
         * @memberof jlgom.Award
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Award.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardMode != null && message.hasOwnProperty("AwardMode"))
                if (!$util.isInteger(message.AwardMode))
                    return "AwardMode: integer expected";
            if (message.AwardItem != null && message.hasOwnProperty("AwardItem"))
                if (!$util.isInteger(message.AwardItem))
                    return "AwardItem: integer expected";
            if (message.Side != null && message.hasOwnProperty("Side"))
                if (!$util.isInteger(message.Side))
                    return "Side: integer expected";
            if (message.LineIndex != null && message.hasOwnProperty("LineIndex"))
                if (!$util.isInteger(message.LineIndex))
                    return "LineIndex: integer expected";
            if (message.Odds != null && message.hasOwnProperty("Odds"))
                if (typeof message.Odds !== "number")
                    return "Odds: number expected";
            return null;
        };

        /**
         * Creates an Award message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.Award
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.Award} Award
         */
        Award.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.Award)
                return object;
            let message = new $root.jlgom.Award();
            if (object.AwardMode != null)
                message.AwardMode = object.AwardMode | 0;
            if (object.AwardItem != null)
                message.AwardItem = object.AwardItem | 0;
            if (object.Side != null)
                message.Side = object.Side | 0;
            if (object.LineIndex != null)
                message.LineIndex = object.LineIndex | 0;
            if (object.Odds != null)
                message.Odds = Number(object.Odds);
            return message;
        };

        /**
         * Creates a plain object from an Award message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.Award
         * @static
         * @param {jlgom.Award} message Award
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Award.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.AwardMode = 0;
                object.AwardItem = 0;
                object.Side = 0;
                object.LineIndex = 0;
                object.Odds = 0;
            }
            if (message.AwardMode != null && message.hasOwnProperty("AwardMode"))
                object.AwardMode = message.AwardMode;
            if (message.AwardItem != null && message.hasOwnProperty("AwardItem"))
                object.AwardItem = message.AwardItem;
            if (message.Side != null && message.hasOwnProperty("Side"))
                object.Side = message.Side;
            if (message.LineIndex != null && message.hasOwnProperty("LineIndex"))
                object.LineIndex = message.LineIndex;
            if (message.Odds != null && message.hasOwnProperty("Odds"))
                object.Odds = options.json && !isFinite(message.Odds) ? String(message.Odds) : message.Odds;
            return object;
        };

        /**
         * Converts this Award to JSON.
         * @function toJSON
         * @memberof jlgom.Award
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Award.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Award
         * @function getTypeUrl
         * @memberof jlgom.Award
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Award.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.Award";
        };

        return Award;
    })();

    jlgom.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlgom
         * @interface IAwardData
         * @property {Array.<jlgom.IAward>|null} [Award] AwardData Award
         * @property {number|null} [AwardPrize] AwardData AwardPrize
         * @property {boolean|null} [IsFreeGame] AwardData IsFreeGame
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlgom
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlgom.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Award.
         * @member {Array.<jlgom.IAward>} Award
         * @memberof jlgom.AwardData
         * @instance
         */
        AwardData.prototype.Award = $util.emptyArray;

        /**
         * AwardData AwardPrize.
         * @member {number} AwardPrize
         * @memberof jlgom.AwardData
         * @instance
         */
        AwardData.prototype.AwardPrize = 0;

        /**
         * AwardData IsFreeGame.
         * @member {boolean} IsFreeGame
         * @memberof jlgom.AwardData
         * @instance
         */
        AwardData.prototype.IsFreeGame = false;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlgom.AwardData
         * @static
         * @param {jlgom.IAwardData=} [properties] Properties to set
         * @returns {jlgom.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlgom.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlgom.AwardData
         * @static
         * @param {jlgom.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Award != null && message.Award.length)
                for (let i = 0; i < message.Award.length; ++i)
                    $root.jlgom.Award.encode(message.Award[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardPrize != null && Object.hasOwnProperty.call(message, "AwardPrize"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.AwardPrize);
            if (message.IsFreeGame != null && Object.hasOwnProperty.call(message, "IsFreeGame"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.IsFreeGame);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlgom.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.AwardData
         * @static
         * @param {jlgom.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Award && message.Award.length))
                            message.Award = [];
                        message.Award.push($root.jlgom.Award.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.AwardPrize = reader.double();
                        break;
                    }
                case 3: {
                        message.IsFreeGame = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlgom.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Award != null && message.hasOwnProperty("Award")) {
                if (!Array.isArray(message.Award))
                    return "Award: array expected";
                for (let i = 0; i < message.Award.length; ++i) {
                    let error = $root.jlgom.Award.verify(message.Award[i]);
                    if (error)
                        return "Award." + error;
                }
            }
            if (message.AwardPrize != null && message.hasOwnProperty("AwardPrize"))
                if (typeof message.AwardPrize !== "number")
                    return "AwardPrize: number expected";
            if (message.IsFreeGame != null && message.hasOwnProperty("IsFreeGame"))
                if (typeof message.IsFreeGame !== "boolean")
                    return "IsFreeGame: boolean expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.AwardData)
                return object;
            let message = new $root.jlgom.AwardData();
            if (object.Award) {
                if (!Array.isArray(object.Award))
                    throw TypeError(".jlgom.AwardData.Award: array expected");
                message.Award = [];
                for (let i = 0; i < object.Award.length; ++i) {
                    if (typeof object.Award[i] !== "object")
                        throw TypeError(".jlgom.AwardData.Award: object expected");
                    message.Award[i] = $root.jlgom.Award.fromObject(object.Award[i]);
                }
            }
            if (object.AwardPrize != null)
                message.AwardPrize = Number(object.AwardPrize);
            if (object.IsFreeGame != null)
                message.IsFreeGame = Boolean(object.IsFreeGame);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.AwardData
         * @static
         * @param {jlgom.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Award = [];
            if (options.defaults) {
                object.AwardPrize = 0;
                object.IsFreeGame = false;
            }
            if (message.Award && message.Award.length) {
                object.Award = [];
                for (let j = 0; j < message.Award.length; ++j)
                    object.Award[j] = $root.jlgom.Award.toObject(message.Award[j], options);
            }
            if (message.AwardPrize != null && message.hasOwnProperty("AwardPrize"))
                object.AwardPrize = options.json && !isFinite(message.AwardPrize) ? String(message.AwardPrize) : message.AwardPrize;
            if (message.IsFreeGame != null && message.hasOwnProperty("IsFreeGame"))
                object.IsFreeGame = message.IsFreeGame;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlgom.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlgom.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.AwardData";
        };

        return AwardData;
    })();

    jlgom.FreeGamePlateInfo = (function() {

        /**
         * Properties of a FreeGamePlateInfo.
         * @memberof jlgom
         * @interface IFreeGamePlateInfo
         * @property {Array.<number>|null} [Plate] FreeGamePlateInfo Plate
         * @property {jlgom.IAwardData|null} [AwardData] FreeGamePlateInfo AwardData
         */

        /**
         * Constructs a new FreeGamePlateInfo.
         * @memberof jlgom
         * @classdesc Represents a FreeGamePlateInfo.
         * @implements IFreeGamePlateInfo
         * @constructor
         * @param {jlgom.IFreeGamePlateInfo=} [properties] Properties to set
         */
        function FreeGamePlateInfo(properties) {
            this.Plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeGamePlateInfo Plate.
         * @member {Array.<number>} Plate
         * @memberof jlgom.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.Plate = $util.emptyArray;

        /**
         * FreeGamePlateInfo AwardData.
         * @member {jlgom.IAwardData|null|undefined} AwardData
         * @memberof jlgom.FreeGamePlateInfo
         * @instance
         */
        FreeGamePlateInfo.prototype.AwardData = null;

        /**
         * Creates a new FreeGamePlateInfo instance using the specified properties.
         * @function create
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {jlgom.IFreeGamePlateInfo=} [properties] Properties to set
         * @returns {jlgom.FreeGamePlateInfo} FreeGamePlateInfo instance
         */
        FreeGamePlateInfo.create = function create(properties) {
            return new FreeGamePlateInfo(properties);
        };

        /**
         * Encodes the specified FreeGamePlateInfo message. Does not implicitly {@link jlgom.FreeGamePlateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {jlgom.IFreeGamePlateInfo} message FreeGamePlateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGamePlateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Plate != null && message.Plate.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Plate.length; ++i)
                    writer.int32(message.Plate[i]);
                writer.ldelim();
            }
            if (message.AwardData != null && Object.hasOwnProperty.call(message, "AwardData"))
                $root.jlgom.AwardData.encode(message.AwardData, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FreeGamePlateInfo message, length delimited. Does not implicitly {@link jlgom.FreeGamePlateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {jlgom.IFreeGamePlateInfo} message FreeGamePlateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGamePlateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeGamePlateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.FreeGamePlateInfo} FreeGamePlateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGamePlateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.FreeGamePlateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Plate && message.Plate.length))
                            message.Plate = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Plate.push(reader.int32());
                        } else
                            message.Plate.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.AwardData = $root.jlgom.AwardData.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeGamePlateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.FreeGamePlateInfo} FreeGamePlateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGamePlateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeGamePlateInfo message.
         * @function verify
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeGamePlateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Plate != null && message.hasOwnProperty("Plate")) {
                if (!Array.isArray(message.Plate))
                    return "Plate: array expected";
                for (let i = 0; i < message.Plate.length; ++i)
                    if (!$util.isInteger(message.Plate[i]))
                        return "Plate: integer[] expected";
            }
            if (message.AwardData != null && message.hasOwnProperty("AwardData")) {
                let error = $root.jlgom.AwardData.verify(message.AwardData);
                if (error)
                    return "AwardData." + error;
            }
            return null;
        };

        /**
         * Creates a FreeGamePlateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.FreeGamePlateInfo} FreeGamePlateInfo
         */
        FreeGamePlateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.FreeGamePlateInfo)
                return object;
            let message = new $root.jlgom.FreeGamePlateInfo();
            if (object.Plate) {
                if (!Array.isArray(object.Plate))
                    throw TypeError(".jlgom.FreeGamePlateInfo.Plate: array expected");
                message.Plate = [];
                for (let i = 0; i < object.Plate.length; ++i)
                    message.Plate[i] = object.Plate[i] | 0;
            }
            if (object.AwardData != null) {
                if (typeof object.AwardData !== "object")
                    throw TypeError(".jlgom.FreeGamePlateInfo.AwardData: object expected");
                message.AwardData = $root.jlgom.AwardData.fromObject(object.AwardData);
            }
            return message;
        };

        /**
         * Creates a plain object from a FreeGamePlateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {jlgom.FreeGamePlateInfo} message FreeGamePlateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeGamePlateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Plate = [];
            if (options.defaults)
                object.AwardData = null;
            if (message.Plate && message.Plate.length) {
                object.Plate = [];
                for (let j = 0; j < message.Plate.length; ++j)
                    object.Plate[j] = message.Plate[j];
            }
            if (message.AwardData != null && message.hasOwnProperty("AwardData"))
                object.AwardData = $root.jlgom.AwardData.toObject(message.AwardData, options);
            return object;
        };

        /**
         * Converts this FreeGamePlateInfo to JSON.
         * @function toJSON
         * @memberof jlgom.FreeGamePlateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeGamePlateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeGamePlateInfo
         * @function getTypeUrl
         * @memberof jlgom.FreeGamePlateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeGamePlateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.FreeGamePlateInfo";
        };

        return FreeGamePlateInfo;
    })();

    jlgom.FreeGameData = (function() {

        /**
         * Properties of a FreeGameData.
         * @memberof jlgom
         * @interface IFreeGameData
         * @property {Array.<jlgom.IFreeGamePlateInfo>|null} [FreeGamePlateInfo] FreeGameData FreeGamePlateInfo
         * @property {number|null} [TotalPrize] FreeGameData TotalPrize
         * @property {number|null} [Times] FreeGameData Times
         * @property {number|null} [BeginTimes] FreeGameData BeginTimes
         * @property {boolean|null} [PackageFinish] FreeGameData PackageFinish
         * @property {string|null} [LogIndex] FreeGameData LogIndex
         * @property {string|null} [ShowIndex] FreeGameData ShowIndex
         */

        /**
         * Constructs a new FreeGameData.
         * @memberof jlgom
         * @classdesc Represents a FreeGameData.
         * @implements IFreeGameData
         * @constructor
         * @param {jlgom.IFreeGameData=} [properties] Properties to set
         */
        function FreeGameData(properties) {
            this.FreeGamePlateInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeGameData FreeGamePlateInfo.
         * @member {Array.<jlgom.IFreeGamePlateInfo>} FreeGamePlateInfo
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.FreeGamePlateInfo = $util.emptyArray;

        /**
         * FreeGameData TotalPrize.
         * @member {number} TotalPrize
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.TotalPrize = 0;

        /**
         * FreeGameData Times.
         * @member {number} Times
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.Times = 0;

        /**
         * FreeGameData BeginTimes.
         * @member {number} BeginTimes
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.BeginTimes = 0;

        /**
         * FreeGameData PackageFinish.
         * @member {boolean} PackageFinish
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.PackageFinish = false;

        /**
         * FreeGameData LogIndex.
         * @member {string} LogIndex
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.LogIndex = "";

        /**
         * FreeGameData ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgom.FreeGameData
         * @instance
         */
        FreeGameData.prototype.ShowIndex = "";

        /**
         * Creates a new FreeGameData instance using the specified properties.
         * @function create
         * @memberof jlgom.FreeGameData
         * @static
         * @param {jlgom.IFreeGameData=} [properties] Properties to set
         * @returns {jlgom.FreeGameData} FreeGameData instance
         */
        FreeGameData.create = function create(properties) {
            return new FreeGameData(properties);
        };

        /**
         * Encodes the specified FreeGameData message. Does not implicitly {@link jlgom.FreeGameData.verify|verify} messages.
         * @function encode
         * @memberof jlgom.FreeGameData
         * @static
         * @param {jlgom.IFreeGameData} message FreeGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeGamePlateInfo != null && message.FreeGamePlateInfo.length)
                for (let i = 0; i < message.FreeGamePlateInfo.length; ++i)
                    $root.jlgom.FreeGamePlateInfo.encode(message.FreeGamePlateInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalPrize != null && Object.hasOwnProperty.call(message, "TotalPrize"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalPrize);
            if (message.Times != null && Object.hasOwnProperty.call(message, "Times"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Times);
            if (message.BeginTimes != null && Object.hasOwnProperty.call(message, "BeginTimes"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.BeginTimes);
            if (message.PackageFinish != null && Object.hasOwnProperty.call(message, "PackageFinish"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.PackageFinish);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.ShowIndex);
            return writer;
        };

        /**
         * Encodes the specified FreeGameData message, length delimited. Does not implicitly {@link jlgom.FreeGameData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.FreeGameData
         * @static
         * @param {jlgom.IFreeGameData} message FreeGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeGameData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.FreeGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.FreeGameData} FreeGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.FreeGameData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeGamePlateInfo && message.FreeGamePlateInfo.length))
                            message.FreeGamePlateInfo = [];
                        message.FreeGamePlateInfo.push($root.jlgom.FreeGamePlateInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalPrize = reader.double();
                        break;
                    }
                case 3: {
                        message.Times = reader.int32();
                        break;
                    }
                case 4: {
                        message.BeginTimes = reader.int32();
                        break;
                    }
                case 5: {
                        message.PackageFinish = reader.bool();
                        break;
                    }
                case 6: {
                        message.LogIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeGameData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.FreeGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.FreeGameData} FreeGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeGameData message.
         * @function verify
         * @memberof jlgom.FreeGameData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeGameData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeGamePlateInfo != null && message.hasOwnProperty("FreeGamePlateInfo")) {
                if (!Array.isArray(message.FreeGamePlateInfo))
                    return "FreeGamePlateInfo: array expected";
                for (let i = 0; i < message.FreeGamePlateInfo.length; ++i) {
                    let error = $root.jlgom.FreeGamePlateInfo.verify(message.FreeGamePlateInfo[i]);
                    if (error)
                        return "FreeGamePlateInfo." + error;
                }
            }
            if (message.TotalPrize != null && message.hasOwnProperty("TotalPrize"))
                if (typeof message.TotalPrize !== "number")
                    return "TotalPrize: number expected";
            if (message.Times != null && message.hasOwnProperty("Times"))
                if (!$util.isInteger(message.Times))
                    return "Times: integer expected";
            if (message.BeginTimes != null && message.hasOwnProperty("BeginTimes"))
                if (!$util.isInteger(message.BeginTimes))
                    return "BeginTimes: integer expected";
            if (message.PackageFinish != null && message.hasOwnProperty("PackageFinish"))
                if (typeof message.PackageFinish !== "boolean")
                    return "PackageFinish: boolean expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isString(message.LogIndex))
                    return "LogIndex: string expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            return null;
        };

        /**
         * Creates a FreeGameData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.FreeGameData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.FreeGameData} FreeGameData
         */
        FreeGameData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.FreeGameData)
                return object;
            let message = new $root.jlgom.FreeGameData();
            if (object.FreeGamePlateInfo) {
                if (!Array.isArray(object.FreeGamePlateInfo))
                    throw TypeError(".jlgom.FreeGameData.FreeGamePlateInfo: array expected");
                message.FreeGamePlateInfo = [];
                for (let i = 0; i < object.FreeGamePlateInfo.length; ++i) {
                    if (typeof object.FreeGamePlateInfo[i] !== "object")
                        throw TypeError(".jlgom.FreeGameData.FreeGamePlateInfo: object expected");
                    message.FreeGamePlateInfo[i] = $root.jlgom.FreeGamePlateInfo.fromObject(object.FreeGamePlateInfo[i]);
                }
            }
            if (object.TotalPrize != null)
                message.TotalPrize = Number(object.TotalPrize);
            if (object.Times != null)
                message.Times = object.Times | 0;
            if (object.BeginTimes != null)
                message.BeginTimes = object.BeginTimes | 0;
            if (object.PackageFinish != null)
                message.PackageFinish = Boolean(object.PackageFinish);
            if (object.LogIndex != null)
                message.LogIndex = String(object.LogIndex);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            return message;
        };

        /**
         * Creates a plain object from a FreeGameData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.FreeGameData
         * @static
         * @param {jlgom.FreeGameData} message FreeGameData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeGameData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeGamePlateInfo = [];
            if (options.defaults) {
                object.TotalPrize = 0;
                object.Times = 0;
                object.BeginTimes = 0;
                object.PackageFinish = false;
                object.LogIndex = "";
                object.ShowIndex = "";
            }
            if (message.FreeGamePlateInfo && message.FreeGamePlateInfo.length) {
                object.FreeGamePlateInfo = [];
                for (let j = 0; j < message.FreeGamePlateInfo.length; ++j)
                    object.FreeGamePlateInfo[j] = $root.jlgom.FreeGamePlateInfo.toObject(message.FreeGamePlateInfo[j], options);
            }
            if (message.TotalPrize != null && message.hasOwnProperty("TotalPrize"))
                object.TotalPrize = options.json && !isFinite(message.TotalPrize) ? String(message.TotalPrize) : message.TotalPrize;
            if (message.Times != null && message.hasOwnProperty("Times"))
                object.Times = message.Times;
            if (message.BeginTimes != null && message.hasOwnProperty("BeginTimes"))
                object.BeginTimes = message.BeginTimes;
            if (message.PackageFinish != null && message.hasOwnProperty("PackageFinish"))
                object.PackageFinish = message.PackageFinish;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                object.LogIndex = message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            return object;
        };

        /**
         * Converts this FreeGameData to JSON.
         * @function toJSON
         * @memberof jlgom.FreeGameData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeGameData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeGameData
         * @function getTypeUrl
         * @memberof jlgom.FreeGameData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeGameData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.FreeGameData";
        };

        return FreeGameData;
    })();

    jlgom.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlgom
         * @interface ISpinAck
         * @property {Array.<number>|null} [Plate] SpinAck Plate
         * @property {jlgom.IAwardData|null} [AwardData] SpinAck AwardData
         * @property {number|null} [MGWin] SpinAck MGWin
         * @property {Array.<jlgom.IFreeGameData>|null} [FreeGamePackage] SpinAck FreeGamePackage
         * @property {boolean|null} [NowMode] SpinAck NowMode
         * @property {boolean|null} [IsHighRateMode] SpinAck IsHighRateMode
         * @property {number|null} [GameType] SpinAck GameType
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlgom
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlgom.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.Plate = [];
            this.FreeGamePackage = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck Plate.
         * @member {Array.<number>} Plate
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.Plate = $util.emptyArray;

        /**
         * SpinAck AwardData.
         * @member {jlgom.IAwardData|null|undefined} AwardData
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardData = null;

        /**
         * SpinAck MGWin.
         * @member {number} MGWin
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.MGWin = 0;

        /**
         * SpinAck FreeGamePackage.
         * @member {Array.<jlgom.IFreeGameData>} FreeGamePackage
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeGamePackage = $util.emptyArray;

        /**
         * SpinAck NowMode.
         * @member {boolean} NowMode
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMode = false;

        /**
         * SpinAck IsHighRateMode.
         * @member {boolean} IsHighRateMode
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.IsHighRateMode = false;

        /**
         * SpinAck GameType.
         * @member {number} GameType
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.GameType = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlgom.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlgom.SpinAck
         * @static
         * @param {jlgom.ISpinAck=} [properties] Properties to set
         * @returns {jlgom.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlgom.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlgom.SpinAck
         * @static
         * @param {jlgom.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Plate != null && message.Plate.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Plate.length; ++i)
                    writer.int32(message.Plate[i]);
                writer.ldelim();
            }
            if (message.AwardData != null && Object.hasOwnProperty.call(message, "AwardData"))
                $root.jlgom.AwardData.encode(message.AwardData, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.MGWin != null && Object.hasOwnProperty.call(message, "MGWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.MGWin);
            if (message.FreeGamePackage != null && message.FreeGamePackage.length)
                for (let i = 0; i < message.FreeGamePackage.length; ++i)
                    $root.jlgom.FreeGameData.encode(message.FreeGamePackage[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.NowMode != null && Object.hasOwnProperty.call(message, "NowMode"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.NowMode);
            if (message.IsHighRateMode != null && Object.hasOwnProperty.call(message, "IsHighRateMode"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.IsHighRateMode);
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.GameType);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlgom.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.SpinAck
         * @static
         * @param {jlgom.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Plate && message.Plate.length))
                            message.Plate = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Plate.push(reader.int32());
                        } else
                            message.Plate.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.AwardData = $root.jlgom.AwardData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.MGWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.FreeGamePackage && message.FreeGamePackage.length))
                            message.FreeGamePackage = [];
                        message.FreeGamePackage.push($root.jlgom.FreeGameData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.NowMode = reader.bool();
                        break;
                    }
                case 6: {
                        message.IsHighRateMode = reader.bool();
                        break;
                    }
                case 7: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 8: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 9: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 10: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 11: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 12: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 13: {
                        message.RTP = reader.double();
                        break;
                    }
                case 14: {
                        message.LID = reader.int32();
                        break;
                    }
                case 15: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlgom.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Plate != null && message.hasOwnProperty("Plate")) {
                if (!Array.isArray(message.Plate))
                    return "Plate: array expected";
                for (let i = 0; i < message.Plate.length; ++i)
                    if (!$util.isInteger(message.Plate[i]))
                        return "Plate: integer[] expected";
            }
            if (message.AwardData != null && message.hasOwnProperty("AwardData")) {
                let error = $root.jlgom.AwardData.verify(message.AwardData);
                if (error)
                    return "AwardData." + error;
            }
            if (message.MGWin != null && message.hasOwnProperty("MGWin"))
                if (typeof message.MGWin !== "number")
                    return "MGWin: number expected";
            if (message.FreeGamePackage != null && message.hasOwnProperty("FreeGamePackage")) {
                if (!Array.isArray(message.FreeGamePackage))
                    return "FreeGamePackage: array expected";
                for (let i = 0; i < message.FreeGamePackage.length; ++i) {
                    let error = $root.jlgom.FreeGameData.verify(message.FreeGamePackage[i]);
                    if (error)
                        return "FreeGamePackage." + error;
                }
            }
            if (message.NowMode != null && message.hasOwnProperty("NowMode"))
                if (typeof message.NowMode !== "boolean")
                    return "NowMode: boolean expected";
            if (message.IsHighRateMode != null && message.hasOwnProperty("IsHighRateMode"))
                if (typeof message.IsHighRateMode !== "boolean")
                    return "IsHighRateMode: boolean expected";
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.SpinAck)
                return object;
            let message = new $root.jlgom.SpinAck();
            if (object.Plate) {
                if (!Array.isArray(object.Plate))
                    throw TypeError(".jlgom.SpinAck.Plate: array expected");
                message.Plate = [];
                for (let i = 0; i < object.Plate.length; ++i)
                    message.Plate[i] = object.Plate[i] | 0;
            }
            if (object.AwardData != null) {
                if (typeof object.AwardData !== "object")
                    throw TypeError(".jlgom.SpinAck.AwardData: object expected");
                message.AwardData = $root.jlgom.AwardData.fromObject(object.AwardData);
            }
            if (object.MGWin != null)
                message.MGWin = Number(object.MGWin);
            if (object.FreeGamePackage) {
                if (!Array.isArray(object.FreeGamePackage))
                    throw TypeError(".jlgom.SpinAck.FreeGamePackage: array expected");
                message.FreeGamePackage = [];
                for (let i = 0; i < object.FreeGamePackage.length; ++i) {
                    if (typeof object.FreeGamePackage[i] !== "object")
                        throw TypeError(".jlgom.SpinAck.FreeGamePackage: object expected");
                    message.FreeGamePackage[i] = $root.jlgom.FreeGameData.fromObject(object.FreeGamePackage[i]);
                }
            }
            if (object.NowMode != null)
                message.NowMode = Boolean(object.NowMode);
            if (object.IsHighRateMode != null)
                message.IsHighRateMode = Boolean(object.IsHighRateMode);
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.SpinAck
         * @static
         * @param {jlgom.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Plate = [];
                object.FreeGamePackage = [];
            }
            if (options.defaults) {
                object.AwardData = null;
                object.MGWin = 0;
                object.NowMode = false;
                object.IsHighRateMode = false;
                object.GameType = 0;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.Plate && message.Plate.length) {
                object.Plate = [];
                for (let j = 0; j < message.Plate.length; ++j)
                    object.Plate[j] = message.Plate[j];
            }
            if (message.AwardData != null && message.hasOwnProperty("AwardData"))
                object.AwardData = $root.jlgom.AwardData.toObject(message.AwardData, options);
            if (message.MGWin != null && message.hasOwnProperty("MGWin"))
                object.MGWin = options.json && !isFinite(message.MGWin) ? String(message.MGWin) : message.MGWin;
            if (message.FreeGamePackage && message.FreeGamePackage.length) {
                object.FreeGamePackage = [];
                for (let j = 0; j < message.FreeGamePackage.length; ++j)
                    object.FreeGamePackage[j] = $root.jlgom.FreeGameData.toObject(message.FreeGamePackage[j], options);
            }
            if (message.NowMode != null && message.hasOwnProperty("NowMode"))
                object.NowMode = message.NowMode;
            if (message.IsHighRateMode != null && message.hasOwnProperty("IsHighRateMode"))
                object.IsHighRateMode = message.IsHighRateMode;
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlgom.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlgom.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.SpinAck";
        };

        return SpinAck;
    })();

    jlgom.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlgom
         * @interface IGameInfoAck
         * @property {boolean|null} [IsHighRateMode] GameInfoAck IsHighRateMode
         * @property {number|null} [HighRateBet] GameInfoAck HighRateBet
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlgom
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlgom.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck IsHighRateMode.
         * @member {boolean} IsHighRateMode
         * @memberof jlgom.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.IsHighRateMode = false;

        /**
         * GameInfoAck HighRateBet.
         * @member {number} HighRateBet
         * @memberof jlgom.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.HighRateBet = 0;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {jlgom.IGameInfoAck=} [properties] Properties to set
         * @returns {jlgom.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlgom.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {jlgom.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.IsHighRateMode != null && Object.hasOwnProperty.call(message, "IsHighRateMode"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.IsHighRateMode);
            if (message.HighRateBet != null && Object.hasOwnProperty.call(message, "HighRateBet"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.HighRateBet);
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlgom.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {jlgom.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.IsHighRateMode = reader.bool();
                        break;
                    }
                case 2: {
                        message.HighRateBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.IsHighRateMode != null && message.hasOwnProperty("IsHighRateMode"))
                if (typeof message.IsHighRateMode !== "boolean")
                    return "IsHighRateMode: boolean expected";
            if (message.HighRateBet != null && message.hasOwnProperty("HighRateBet"))
                if (typeof message.HighRateBet !== "number")
                    return "HighRateBet: number expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.GameInfoAck)
                return object;
            let message = new $root.jlgom.GameInfoAck();
            if (object.IsHighRateMode != null)
                message.IsHighRateMode = Boolean(object.IsHighRateMode);
            if (object.HighRateBet != null)
                message.HighRateBet = Number(object.HighRateBet);
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {jlgom.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.IsHighRateMode = false;
                object.HighRateBet = 0;
            }
            if (message.IsHighRateMode != null && message.hasOwnProperty("IsHighRateMode"))
                object.IsHighRateMode = message.IsHighRateMode;
            if (message.HighRateBet != null && message.hasOwnProperty("HighRateBet"))
                object.HighRateBet = options.json && !isFinite(message.HighRateBet) ? String(message.HighRateBet) : message.HighRateBet;
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlgom.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlgom.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jlgom.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlgom
         * @interface IStateInfo
         * @property {number|null} [PreBet] StateInfo PreBet
         * @property {number|null} [RemainHighRateRound] StateInfo RemainHighRateRound
         * @property {number|null} [Spec1Count] StateInfo Spec1Count
         * @property {number|null} [Spec2Count] StateInfo Spec2Count
         * @property {number|null} [Spec3Count] StateInfo Spec3Count
         * @property {number|null} [Spec4Count] StateInfo Spec4Count
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlgom
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlgom.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo PreBet.
         * @member {number} PreBet
         * @memberof jlgom.StateInfo
         * @instance
         */
        StateInfo.prototype.PreBet = 0;

        /**
         * StateInfo RemainHighRateRound.
         * @member {number} RemainHighRateRound
         * @memberof jlgom.StateInfo
         * @instance
         */
        StateInfo.prototype.RemainHighRateRound = 0;

        /**
         * StateInfo Spec1Count.
         * @member {number} Spec1Count
         * @memberof jlgom.StateInfo
         * @instance
         */
        StateInfo.prototype.Spec1Count = 0;

        /**
         * StateInfo Spec2Count.
         * @member {number} Spec2Count
         * @memberof jlgom.StateInfo
         * @instance
         */
        StateInfo.prototype.Spec2Count = 0;

        /**
         * StateInfo Spec3Count.
         * @member {number} Spec3Count
         * @memberof jlgom.StateInfo
         * @instance
         */
        StateInfo.prototype.Spec3Count = 0;

        /**
         * StateInfo Spec4Count.
         * @member {number} Spec4Count
         * @memberof jlgom.StateInfo
         * @instance
         */
        StateInfo.prototype.Spec4Count = 0;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlgom.StateInfo
         * @static
         * @param {jlgom.IStateInfo=} [properties] Properties to set
         * @returns {jlgom.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlgom.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgom.StateInfo
         * @static
         * @param {jlgom.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PreBet != null && Object.hasOwnProperty.call(message, "PreBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.PreBet);
            if (message.RemainHighRateRound != null && Object.hasOwnProperty.call(message, "RemainHighRateRound"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.RemainHighRateRound);
            if (message.Spec1Count != null && Object.hasOwnProperty.call(message, "Spec1Count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Spec1Count);
            if (message.Spec2Count != null && Object.hasOwnProperty.call(message, "Spec2Count"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Spec2Count);
            if (message.Spec3Count != null && Object.hasOwnProperty.call(message, "Spec3Count"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.Spec3Count);
            if (message.Spec4Count != null && Object.hasOwnProperty.call(message, "Spec4Count"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.Spec4Count);
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlgom.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgom.StateInfo
         * @static
         * @param {jlgom.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgom.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgom.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgom.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PreBet = reader.double();
                        break;
                    }
                case 2: {
                        message.RemainHighRateRound = reader.int32();
                        break;
                    }
                case 3: {
                        message.Spec1Count = reader.int32();
                        break;
                    }
                case 4: {
                        message.Spec2Count = reader.int32();
                        break;
                    }
                case 5: {
                        message.Spec3Count = reader.int32();
                        break;
                    }
                case 6: {
                        message.Spec4Count = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgom.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgom.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlgom.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PreBet != null && message.hasOwnProperty("PreBet"))
                if (typeof message.PreBet !== "number")
                    return "PreBet: number expected";
            if (message.RemainHighRateRound != null && message.hasOwnProperty("RemainHighRateRound"))
                if (!$util.isInteger(message.RemainHighRateRound))
                    return "RemainHighRateRound: integer expected";
            if (message.Spec1Count != null && message.hasOwnProperty("Spec1Count"))
                if (!$util.isInteger(message.Spec1Count))
                    return "Spec1Count: integer expected";
            if (message.Spec2Count != null && message.hasOwnProperty("Spec2Count"))
                if (!$util.isInteger(message.Spec2Count))
                    return "Spec2Count: integer expected";
            if (message.Spec3Count != null && message.hasOwnProperty("Spec3Count"))
                if (!$util.isInteger(message.Spec3Count))
                    return "Spec3Count: integer expected";
            if (message.Spec4Count != null && message.hasOwnProperty("Spec4Count"))
                if (!$util.isInteger(message.Spec4Count))
                    return "Spec4Count: integer expected";
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgom.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgom.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgom.StateInfo)
                return object;
            let message = new $root.jlgom.StateInfo();
            if (object.PreBet != null)
                message.PreBet = Number(object.PreBet);
            if (object.RemainHighRateRound != null)
                message.RemainHighRateRound = object.RemainHighRateRound | 0;
            if (object.Spec1Count != null)
                message.Spec1Count = object.Spec1Count | 0;
            if (object.Spec2Count != null)
                message.Spec2Count = object.Spec2Count | 0;
            if (object.Spec3Count != null)
                message.Spec3Count = object.Spec3Count | 0;
            if (object.Spec4Count != null)
                message.Spec4Count = object.Spec4Count | 0;
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgom.StateInfo
         * @static
         * @param {jlgom.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.PreBet = 0;
                object.RemainHighRateRound = 0;
                object.Spec1Count = 0;
                object.Spec2Count = 0;
                object.Spec3Count = 0;
                object.Spec4Count = 0;
            }
            if (message.PreBet != null && message.hasOwnProperty("PreBet"))
                object.PreBet = options.json && !isFinite(message.PreBet) ? String(message.PreBet) : message.PreBet;
            if (message.RemainHighRateRound != null && message.hasOwnProperty("RemainHighRateRound"))
                object.RemainHighRateRound = message.RemainHighRateRound;
            if (message.Spec1Count != null && message.hasOwnProperty("Spec1Count"))
                object.Spec1Count = message.Spec1Count;
            if (message.Spec2Count != null && message.hasOwnProperty("Spec2Count"))
                object.Spec2Count = message.Spec2Count;
            if (message.Spec3Count != null && message.hasOwnProperty("Spec3Count"))
                object.Spec3Count = message.Spec3Count;
            if (message.Spec4Count != null && message.hasOwnProperty("Spec4Count"))
                object.Spec4Count = message.Spec4Count;
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlgom.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlgom.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgom.StateInfo";
        };

        return StateInfo;
    })();

    return jlgom;
})();

export const jlgq = $root.jlgq = (() => {

    /**
     * Namespace jlgq.
     * @exports jlgq
     * @namespace
     */
    const jlgq = {};

    jlgq.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlgq
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlgq
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlgq.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlgq.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlgq.Column
         * @static
         * @param {jlgq.IColumn=} [properties] Properties to set
         * @returns {jlgq.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlgq.Column.verify|verify} messages.
         * @function encode
         * @memberof jlgq.Column
         * @static
         * @param {jlgq.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlgq.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.Column
         * @static
         * @param {jlgq.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlgq.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.Column)
                return object;
            let message = new $root.jlgq.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlgq.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.Column
         * @static
         * @param {jlgq.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlgq.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlgq.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.Column";
        };

        return Column;
    })();

    jlgq.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlgq
         * @interface IAwardData
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Money] AwardData Money
         * @property {number|null} [LMul] AwardData LMul
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlgq
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlgq.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlgq.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlgq.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlgq.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Money.
         * @member {number} Money
         * @memberof jlgq.AwardData
         * @instance
         */
        AwardData.prototype.Money = 0;

        /**
         * AwardData LMul.
         * @member {number} LMul
         * @memberof jlgq.AwardData
         * @instance
         */
        AwardData.prototype.LMul = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlgq.AwardData
         * @static
         * @param {jlgq.IAwardData=} [properties] Properties to set
         * @returns {jlgq.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlgq.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlgq.AwardData
         * @static
         * @param {jlgq.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Count);
            if (message.Money != null && Object.hasOwnProperty.call(message, "Money"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Money);
            if (message.LMul != null && Object.hasOwnProperty.call(message, "LMul"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.LMul);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlgq.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.AwardData
         * @static
         * @param {jlgq.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Index = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Count = reader.int32();
                        break;
                    }
                case 4: {
                        message.Money = reader.double();
                        break;
                    }
                case 5: {
                        message.LMul = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlgq.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Money != null && message.hasOwnProperty("Money"))
                if (typeof message.Money !== "number")
                    return "Money: number expected";
            if (message.LMul != null && message.hasOwnProperty("LMul"))
                if (!$util.isInteger(message.LMul))
                    return "LMul: integer expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.AwardData)
                return object;
            let message = new $root.jlgq.AwardData();
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Money != null)
                message.Money = Number(object.Money);
            if (object.LMul != null)
                message.LMul = object.LMul | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.AwardData
         * @static
         * @param {jlgq.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Index = 0;
                object.Symbol = 0;
                object.Count = 0;
                object.Money = 0;
                object.LMul = 0;
            }
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Money != null && message.hasOwnProperty("Money"))
                object.Money = options.json && !isFinite(message.Money) ? String(message.Money) : message.Money;
            if (message.LMul != null && message.hasOwnProperty("LMul"))
                object.LMul = message.LMul;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlgq.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlgq.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.AwardData";
        };

        return AwardData;
    })();

    jlgq.WildData = (function() {

        /**
         * Properties of a WildData.
         * @memberof jlgq
         * @interface IWildData
         * @property {number|null} [SPos] WildData SPos
         * @property {Array.<number>|null} [WMul] WildData WMul
         * @property {Array.<number>|null} [WPos] WildData WPos
         */

        /**
         * Constructs a new WildData.
         * @memberof jlgq
         * @classdesc Represents a WildData.
         * @implements IWildData
         * @constructor
         * @param {jlgq.IWildData=} [properties] Properties to set
         */
        function WildData(properties) {
            this.WMul = [];
            this.WPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WildData SPos.
         * @member {number} SPos
         * @memberof jlgq.WildData
         * @instance
         */
        WildData.prototype.SPos = 0;

        /**
         * WildData WMul.
         * @member {Array.<number>} WMul
         * @memberof jlgq.WildData
         * @instance
         */
        WildData.prototype.WMul = $util.emptyArray;

        /**
         * WildData WPos.
         * @member {Array.<number>} WPos
         * @memberof jlgq.WildData
         * @instance
         */
        WildData.prototype.WPos = $util.emptyArray;

        /**
         * Creates a new WildData instance using the specified properties.
         * @function create
         * @memberof jlgq.WildData
         * @static
         * @param {jlgq.IWildData=} [properties] Properties to set
         * @returns {jlgq.WildData} WildData instance
         */
        WildData.create = function create(properties) {
            return new WildData(properties);
        };

        /**
         * Encodes the specified WildData message. Does not implicitly {@link jlgq.WildData.verify|verify} messages.
         * @function encode
         * @memberof jlgq.WildData
         * @static
         * @param {jlgq.IWildData} message WildData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WildData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.SPos != null && Object.hasOwnProperty.call(message, "SPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.SPos);
            if (message.WMul != null && message.WMul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.WMul.length; ++i)
                    writer.int32(message.WMul[i]);
                writer.ldelim();
            }
            if (message.WPos != null && message.WPos.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.WPos.length; ++i)
                    writer.int32(message.WPos[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified WildData message, length delimited. Does not implicitly {@link jlgq.WildData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.WildData
         * @static
         * @param {jlgq.IWildData} message WildData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WildData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WildData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.WildData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.WildData} WildData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WildData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.WildData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.SPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.WMul && message.WMul.length))
                            message.WMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WMul.push(reader.int32());
                        } else
                            message.WMul.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.WPos && message.WPos.length))
                            message.WPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WPos.push(reader.int32());
                        } else
                            message.WPos.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WildData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.WildData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.WildData} WildData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WildData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WildData message.
         * @function verify
         * @memberof jlgq.WildData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WildData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.SPos != null && message.hasOwnProperty("SPos"))
                if (!$util.isInteger(message.SPos))
                    return "SPos: integer expected";
            if (message.WMul != null && message.hasOwnProperty("WMul")) {
                if (!Array.isArray(message.WMul))
                    return "WMul: array expected";
                for (let i = 0; i < message.WMul.length; ++i)
                    if (!$util.isInteger(message.WMul[i]))
                        return "WMul: integer[] expected";
            }
            if (message.WPos != null && message.hasOwnProperty("WPos")) {
                if (!Array.isArray(message.WPos))
                    return "WPos: array expected";
                for (let i = 0; i < message.WPos.length; ++i)
                    if (!$util.isInteger(message.WPos[i]))
                        return "WPos: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a WildData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.WildData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.WildData} WildData
         */
        WildData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.WildData)
                return object;
            let message = new $root.jlgq.WildData();
            if (object.SPos != null)
                message.SPos = object.SPos | 0;
            if (object.WMul) {
                if (!Array.isArray(object.WMul))
                    throw TypeError(".jlgq.WildData.WMul: array expected");
                message.WMul = [];
                for (let i = 0; i < object.WMul.length; ++i)
                    message.WMul[i] = object.WMul[i] | 0;
            }
            if (object.WPos) {
                if (!Array.isArray(object.WPos))
                    throw TypeError(".jlgq.WildData.WPos: array expected");
                message.WPos = [];
                for (let i = 0; i < object.WPos.length; ++i)
                    message.WPos[i] = object.WPos[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a WildData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.WildData
         * @static
         * @param {jlgq.WildData} message WildData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WildData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.WMul = [];
                object.WPos = [];
            }
            if (options.defaults)
                object.SPos = 0;
            if (message.SPos != null && message.hasOwnProperty("SPos"))
                object.SPos = message.SPos;
            if (message.WMul && message.WMul.length) {
                object.WMul = [];
                for (let j = 0; j < message.WMul.length; ++j)
                    object.WMul[j] = message.WMul[j];
            }
            if (message.WPos && message.WPos.length) {
                object.WPos = [];
                for (let j = 0; j < message.WPos.length; ++j)
                    object.WPos[j] = message.WPos[j];
            }
            return object;
        };

        /**
         * Converts this WildData to JSON.
         * @function toJSON
         * @memberof jlgq.WildData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WildData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WildData
         * @function getTypeUrl
         * @memberof jlgq.WildData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WildData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.WildData";
        };

        return WildData;
    })();

    jlgq.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlgq
         * @interface ISinglePlate
         * @property {Array.<jlgq.IColumn>|null} [OriginPlate] SinglePlate OriginPlate
         * @property {Array.<jlgq.IColumn>|null} [LastPlate] SinglePlate LastPlate
         * @property {Array.<jlgq.IColumn>|null} [WildPlate] SinglePlate WildPlate
         * @property {Array.<jlgq.IAwardData>|null} [AwardDataList] SinglePlate AwardDataList
         * @property {Array.<jlgq.IWildData>|null} [WildDataList] SinglePlate WildDataList
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlgq
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlgq.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.OriginPlate = [];
            this.LastPlate = [];
            this.WildPlate = [];
            this.AwardDataList = [];
            this.WildDataList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate OriginPlate.
         * @member {Array.<jlgq.IColumn>} OriginPlate
         * @memberof jlgq.SinglePlate
         * @instance
         */
        SinglePlate.prototype.OriginPlate = $util.emptyArray;

        /**
         * SinglePlate LastPlate.
         * @member {Array.<jlgq.IColumn>} LastPlate
         * @memberof jlgq.SinglePlate
         * @instance
         */
        SinglePlate.prototype.LastPlate = $util.emptyArray;

        /**
         * SinglePlate WildPlate.
         * @member {Array.<jlgq.IColumn>} WildPlate
         * @memberof jlgq.SinglePlate
         * @instance
         */
        SinglePlate.prototype.WildPlate = $util.emptyArray;

        /**
         * SinglePlate AwardDataList.
         * @member {Array.<jlgq.IAwardData>} AwardDataList
         * @memberof jlgq.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataList = $util.emptyArray;

        /**
         * SinglePlate WildDataList.
         * @member {Array.<jlgq.IWildData>} WildDataList
         * @memberof jlgq.SinglePlate
         * @instance
         */
        SinglePlate.prototype.WildDataList = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jlgq.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlgq.SinglePlate
         * @static
         * @param {jlgq.ISinglePlate=} [properties] Properties to set
         * @returns {jlgq.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlgq.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlgq.SinglePlate
         * @static
         * @param {jlgq.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.OriginPlate != null && message.OriginPlate.length)
                for (let i = 0; i < message.OriginPlate.length; ++i)
                    $root.jlgq.Column.encode(message.OriginPlate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.LastPlate != null && message.LastPlate.length)
                for (let i = 0; i < message.LastPlate.length; ++i)
                    $root.jlgq.Column.encode(message.LastPlate[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.WildPlate != null && message.WildPlate.length)
                for (let i = 0; i < message.WildPlate.length; ++i)
                    $root.jlgq.Column.encode(message.WildPlate[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlgq.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.WildDataList != null && message.WildDataList.length)
                for (let i = 0; i < message.WildDataList.length; ++i)
                    $root.jlgq.WildData.encode(message.WildDataList[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.SingleWin);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlgq.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.SinglePlate
         * @static
         * @param {jlgq.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.OriginPlate && message.OriginPlate.length))
                            message.OriginPlate = [];
                        message.OriginPlate.push($root.jlgq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.LastPlate && message.LastPlate.length))
                            message.LastPlate = [];
                        message.LastPlate.push($root.jlgq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.WildPlate && message.WildPlate.length))
                            message.WildPlate = [];
                        message.WildPlate.push($root.jlgq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlgq.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.WildDataList && message.WildDataList.length))
                            message.WildDataList = [];
                        message.WildDataList.push($root.jlgq.WildData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.SingleWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlgq.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.OriginPlate != null && message.hasOwnProperty("OriginPlate")) {
                if (!Array.isArray(message.OriginPlate))
                    return "OriginPlate: array expected";
                for (let i = 0; i < message.OriginPlate.length; ++i) {
                    let error = $root.jlgq.Column.verify(message.OriginPlate[i]);
                    if (error)
                        return "OriginPlate." + error;
                }
            }
            if (message.LastPlate != null && message.hasOwnProperty("LastPlate")) {
                if (!Array.isArray(message.LastPlate))
                    return "LastPlate: array expected";
                for (let i = 0; i < message.LastPlate.length; ++i) {
                    let error = $root.jlgq.Column.verify(message.LastPlate[i]);
                    if (error)
                        return "LastPlate." + error;
                }
            }
            if (message.WildPlate != null && message.hasOwnProperty("WildPlate")) {
                if (!Array.isArray(message.WildPlate))
                    return "WildPlate: array expected";
                for (let i = 0; i < message.WildPlate.length; ++i) {
                    let error = $root.jlgq.Column.verify(message.WildPlate[i]);
                    if (error)
                        return "WildPlate." + error;
                }
            }
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlgq.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.WildDataList != null && message.hasOwnProperty("WildDataList")) {
                if (!Array.isArray(message.WildDataList))
                    return "WildDataList: array expected";
                for (let i = 0; i < message.WildDataList.length; ++i) {
                    let error = $root.jlgq.WildData.verify(message.WildDataList[i]);
                    if (error)
                        return "WildDataList." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.SinglePlate)
                return object;
            let message = new $root.jlgq.SinglePlate();
            if (object.OriginPlate) {
                if (!Array.isArray(object.OriginPlate))
                    throw TypeError(".jlgq.SinglePlate.OriginPlate: array expected");
                message.OriginPlate = [];
                for (let i = 0; i < object.OriginPlate.length; ++i) {
                    if (typeof object.OriginPlate[i] !== "object")
                        throw TypeError(".jlgq.SinglePlate.OriginPlate: object expected");
                    message.OriginPlate[i] = $root.jlgq.Column.fromObject(object.OriginPlate[i]);
                }
            }
            if (object.LastPlate) {
                if (!Array.isArray(object.LastPlate))
                    throw TypeError(".jlgq.SinglePlate.LastPlate: array expected");
                message.LastPlate = [];
                for (let i = 0; i < object.LastPlate.length; ++i) {
                    if (typeof object.LastPlate[i] !== "object")
                        throw TypeError(".jlgq.SinglePlate.LastPlate: object expected");
                    message.LastPlate[i] = $root.jlgq.Column.fromObject(object.LastPlate[i]);
                }
            }
            if (object.WildPlate) {
                if (!Array.isArray(object.WildPlate))
                    throw TypeError(".jlgq.SinglePlate.WildPlate: array expected");
                message.WildPlate = [];
                for (let i = 0; i < object.WildPlate.length; ++i) {
                    if (typeof object.WildPlate[i] !== "object")
                        throw TypeError(".jlgq.SinglePlate.WildPlate: object expected");
                    message.WildPlate[i] = $root.jlgq.Column.fromObject(object.WildPlate[i]);
                }
            }
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlgq.SinglePlate.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlgq.SinglePlate.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlgq.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.WildDataList) {
                if (!Array.isArray(object.WildDataList))
                    throw TypeError(".jlgq.SinglePlate.WildDataList: array expected");
                message.WildDataList = [];
                for (let i = 0; i < object.WildDataList.length; ++i) {
                    if (typeof object.WildDataList[i] !== "object")
                        throw TypeError(".jlgq.SinglePlate.WildDataList: object expected");
                    message.WildDataList[i] = $root.jlgq.WildData.fromObject(object.WildDataList[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.SinglePlate
         * @static
         * @param {jlgq.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.OriginPlate = [];
                object.LastPlate = [];
                object.WildPlate = [];
                object.AwardDataList = [];
                object.WildDataList = [];
            }
            if (options.defaults)
                object.SingleWin = 0;
            if (message.OriginPlate && message.OriginPlate.length) {
                object.OriginPlate = [];
                for (let j = 0; j < message.OriginPlate.length; ++j)
                    object.OriginPlate[j] = $root.jlgq.Column.toObject(message.OriginPlate[j], options);
            }
            if (message.LastPlate && message.LastPlate.length) {
                object.LastPlate = [];
                for (let j = 0; j < message.LastPlate.length; ++j)
                    object.LastPlate[j] = $root.jlgq.Column.toObject(message.LastPlate[j], options);
            }
            if (message.WildPlate && message.WildPlate.length) {
                object.WildPlate = [];
                for (let j = 0; j < message.WildPlate.length; ++j)
                    object.WildPlate[j] = $root.jlgq.Column.toObject(message.WildPlate[j], options);
            }
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlgq.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.WildDataList && message.WildDataList.length) {
                object.WildDataList = [];
                for (let j = 0; j < message.WildDataList.length; ++j)
                    object.WildDataList[j] = $root.jlgq.WildData.toObject(message.WildDataList[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlgq.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlgq.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.SinglePlate";
        };

        return SinglePlate;
    })();

    jlgq.SingleFree = (function() {

        /**
         * Properties of a SingleFree.
         * @memberof jlgq
         * @interface ISingleFree
         * @property {jlgq.ISinglePlate|null} [FreePlate] SingleFree FreePlate
         * @property {number|null} [AwardType] SingleFree AwardType
         * @property {number|null} [NowMoney] SingleFree NowMoney
         */

        /**
         * Constructs a new SingleFree.
         * @memberof jlgq
         * @classdesc Represents a SingleFree.
         * @implements ISingleFree
         * @constructor
         * @param {jlgq.ISingleFree=} [properties] Properties to set
         */
        function SingleFree(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleFree FreePlate.
         * @member {jlgq.ISinglePlate|null|undefined} FreePlate
         * @memberof jlgq.SingleFree
         * @instance
         */
        SingleFree.prototype.FreePlate = null;

        /**
         * SingleFree AwardType.
         * @member {number} AwardType
         * @memberof jlgq.SingleFree
         * @instance
         */
        SingleFree.prototype.AwardType = 0;

        /**
         * SingleFree NowMoney.
         * @member {number} NowMoney
         * @memberof jlgq.SingleFree
         * @instance
         */
        SingleFree.prototype.NowMoney = 0;

        /**
         * Creates a new SingleFree instance using the specified properties.
         * @function create
         * @memberof jlgq.SingleFree
         * @static
         * @param {jlgq.ISingleFree=} [properties] Properties to set
         * @returns {jlgq.SingleFree} SingleFree instance
         */
        SingleFree.create = function create(properties) {
            return new SingleFree(properties);
        };

        /**
         * Encodes the specified SingleFree message. Does not implicitly {@link jlgq.SingleFree.verify|verify} messages.
         * @function encode
         * @memberof jlgq.SingleFree
         * @static
         * @param {jlgq.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreePlate != null && Object.hasOwnProperty.call(message, "FreePlate"))
                $root.jlgq.SinglePlate.encode(message.FreePlate, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardType);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.NowMoney);
            return writer;
        };

        /**
         * Encodes the specified SingleFree message, length delimited. Does not implicitly {@link jlgq.SingleFree.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.SingleFree
         * @static
         * @param {jlgq.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.SingleFree();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.FreePlate = $root.jlgq.SinglePlate.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 3: {
                        message.NowMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleFree message.
         * @function verify
         * @memberof jlgq.SingleFree
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleFree.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreePlate != null && message.hasOwnProperty("FreePlate")) {
                let error = $root.jlgq.SinglePlate.verify(message.FreePlate);
                if (error)
                    return "FreePlate." + error;
            }
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            return null;
        };

        /**
         * Creates a SingleFree message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.SingleFree
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.SingleFree} SingleFree
         */
        SingleFree.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.SingleFree)
                return object;
            let message = new $root.jlgq.SingleFree();
            if (object.FreePlate != null) {
                if (typeof object.FreePlate !== "object")
                    throw TypeError(".jlgq.SingleFree.FreePlate: object expected");
                message.FreePlate = $root.jlgq.SinglePlate.fromObject(object.FreePlate);
            }
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            return message;
        };

        /**
         * Creates a plain object from a SingleFree message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.SingleFree
         * @static
         * @param {jlgq.SingleFree} message SingleFree
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleFree.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.FreePlate = null;
                object.AwardType = 0;
                object.NowMoney = 0;
            }
            if (message.FreePlate != null && message.hasOwnProperty("FreePlate"))
                object.FreePlate = $root.jlgq.SinglePlate.toObject(message.FreePlate, options);
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            return object;
        };

        /**
         * Converts this SingleFree to JSON.
         * @function toJSON
         * @memberof jlgq.SingleFree
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleFree.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleFree
         * @function getTypeUrl
         * @memberof jlgq.SingleFree
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleFree.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.SingleFree";
        };

        return SingleFree;
    })();

    jlgq.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jlgq
         * @interface IFreeData
         * @property {Array.<jlgq.ISingleFree>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jlgq
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jlgq.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jlgq.ISingleFree>} FreeQueue
         * @memberof jlgq.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlgq.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jlgq.FreeData
         * @static
         * @param {jlgq.IFreeData=} [properties] Properties to set
         * @returns {jlgq.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jlgq.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jlgq.FreeData
         * @static
         * @param {jlgq.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jlgq.SingleFree.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jlgq.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.FreeData
         * @static
         * @param {jlgq.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jlgq.SingleFree.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jlgq.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jlgq.SingleFree.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.FreeData)
                return object;
            let message = new $root.jlgq.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jlgq.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jlgq.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jlgq.SingleFree.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.FreeData
         * @static
         * @param {jlgq.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults)
                object.FreeTotalWin = 0;
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jlgq.SingleFree.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jlgq.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jlgq.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.FreeData";
        };

        return FreeData;
    })();

    jlgq.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlgq
         * @interface ISpinAck
         * @property {jlgq.ISinglePlate|null} [MainPlate] SpinAck MainPlate
         * @property {jlgq.IFreeData|null} [FreeInfo] SpinAck FreeInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlgq
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlgq.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainPlate.
         * @member {jlgq.ISinglePlate|null|undefined} MainPlate
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.MainPlate = null;

        /**
         * SpinAck FreeInfo.
         * @member {jlgq.IFreeData|null|undefined} FreeInfo
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeInfo = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlgq.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlgq.SpinAck
         * @static
         * @param {jlgq.ISpinAck=} [properties] Properties to set
         * @returns {jlgq.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlgq.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlgq.SpinAck
         * @static
         * @param {jlgq.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainPlate != null && Object.hasOwnProperty.call(message, "MainPlate"))
                $root.jlgq.SinglePlate.encode(message.MainPlate, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeInfo != null && Object.hasOwnProperty.call(message, "FreeInfo"))
                $root.jlgq.FreeData.encode(message.FreeInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlgq.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.SpinAck
         * @static
         * @param {jlgq.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainPlate = $root.jlgq.SinglePlate.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeInfo = $root.jlgq.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlgq.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainPlate != null && message.hasOwnProperty("MainPlate")) {
                let error = $root.jlgq.SinglePlate.verify(message.MainPlate);
                if (error)
                    return "MainPlate." + error;
            }
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo")) {
                let error = $root.jlgq.FreeData.verify(message.FreeInfo);
                if (error)
                    return "FreeInfo." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.SpinAck)
                return object;
            let message = new $root.jlgq.SpinAck();
            if (object.MainPlate != null) {
                if (typeof object.MainPlate !== "object")
                    throw TypeError(".jlgq.SpinAck.MainPlate: object expected");
                message.MainPlate = $root.jlgq.SinglePlate.fromObject(object.MainPlate);
            }
            if (object.FreeInfo != null) {
                if (typeof object.FreeInfo !== "object")
                    throw TypeError(".jlgq.SpinAck.FreeInfo: object expected");
                message.FreeInfo = $root.jlgq.FreeData.fromObject(object.FreeInfo);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.SpinAck
         * @static
         * @param {jlgq.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainPlate = null;
                object.FreeInfo = null;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.MainPlate != null && message.hasOwnProperty("MainPlate"))
                object.MainPlate = $root.jlgq.SinglePlate.toObject(message.MainPlate, options);
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo"))
                object.FreeInfo = $root.jlgq.FreeData.toObject(message.FreeInfo, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlgq.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlgq.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.SpinAck";
        };

        return SpinAck;
    })();

    jlgq.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlgq
         * @interface IGameInfoAck
         * @property {jlgq.IVolInfo|null} [GameVolInfo] GameInfoAck GameVolInfo
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlgq
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlgq.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck GameVolInfo.
         * @member {jlgq.IVolInfo|null|undefined} GameVolInfo
         * @memberof jlgq.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.GameVolInfo = null;

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlgq.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {jlgq.IGameInfoAck=} [properties] Properties to set
         * @returns {jlgq.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlgq.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {jlgq.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.GameVolInfo != null && Object.hasOwnProperty.call(message, "GameVolInfo"))
                $root.jlgq.VolInfo.encode(message.GameVolInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlgq.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {jlgq.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameVolInfo = $root.jlgq.VolInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.GameVolInfo != null && message.hasOwnProperty("GameVolInfo")) {
                let error = $root.jlgq.VolInfo.verify(message.GameVolInfo);
                if (error)
                    return "GameVolInfo." + error;
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.GameInfoAck)
                return object;
            let message = new $root.jlgq.GameInfoAck();
            if (object.GameVolInfo != null) {
                if (typeof object.GameVolInfo !== "object")
                    throw TypeError(".jlgq.GameInfoAck.GameVolInfo: object expected");
                message.GameVolInfo = $root.jlgq.VolInfo.fromObject(object.GameVolInfo);
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlgq.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {jlgq.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.GameVolInfo = null;
            if (message.GameVolInfo != null && message.hasOwnProperty("GameVolInfo"))
                object.GameVolInfo = $root.jlgq.VolInfo.toObject(message.GameVolInfo, options);
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlgq.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlgq.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jlgq.VolInfo = (function() {

        /**
         * Properties of a VolInfo.
         * @memberof jlgq
         * @interface IVolInfo
         * @property {number|null} [VolPos] VolInfo VolPos
         * @property {Array.<number>|null} [VolList] VolInfo VolList
         */

        /**
         * Constructs a new VolInfo.
         * @memberof jlgq
         * @classdesc Represents a VolInfo.
         * @implements IVolInfo
         * @constructor
         * @param {jlgq.IVolInfo=} [properties] Properties to set
         */
        function VolInfo(properties) {
            this.VolList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VolInfo VolPos.
         * @member {number} VolPos
         * @memberof jlgq.VolInfo
         * @instance
         */
        VolInfo.prototype.VolPos = 0;

        /**
         * VolInfo VolList.
         * @member {Array.<number>} VolList
         * @memberof jlgq.VolInfo
         * @instance
         */
        VolInfo.prototype.VolList = $util.emptyArray;

        /**
         * Creates a new VolInfo instance using the specified properties.
         * @function create
         * @memberof jlgq.VolInfo
         * @static
         * @param {jlgq.IVolInfo=} [properties] Properties to set
         * @returns {jlgq.VolInfo} VolInfo instance
         */
        VolInfo.create = function create(properties) {
            return new VolInfo(properties);
        };

        /**
         * Encodes the specified VolInfo message. Does not implicitly {@link jlgq.VolInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgq.VolInfo
         * @static
         * @param {jlgq.IVolInfo} message VolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VolInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.VolPos != null && Object.hasOwnProperty.call(message, "VolPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.VolPos);
            if (message.VolList != null && message.VolList.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.VolList.length; ++i)
                    writer.int32(message.VolList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified VolInfo message, length delimited. Does not implicitly {@link jlgq.VolInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgq.VolInfo
         * @static
         * @param {jlgq.IVolInfo} message VolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VolInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VolInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgq.VolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgq.VolInfo} VolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VolInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgq.VolInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.VolPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.VolList && message.VolList.length))
                            message.VolList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.VolList.push(reader.int32());
                        } else
                            message.VolList.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VolInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgq.VolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgq.VolInfo} VolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VolInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VolInfo message.
         * @function verify
         * @memberof jlgq.VolInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VolInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.VolPos != null && message.hasOwnProperty("VolPos"))
                if (!$util.isInteger(message.VolPos))
                    return "VolPos: integer expected";
            if (message.VolList != null && message.hasOwnProperty("VolList")) {
                if (!Array.isArray(message.VolList))
                    return "VolList: array expected";
                for (let i = 0; i < message.VolList.length; ++i)
                    if (!$util.isInteger(message.VolList[i]))
                        return "VolList: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a VolInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgq.VolInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgq.VolInfo} VolInfo
         */
        VolInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgq.VolInfo)
                return object;
            let message = new $root.jlgq.VolInfo();
            if (object.VolPos != null)
                message.VolPos = object.VolPos | 0;
            if (object.VolList) {
                if (!Array.isArray(object.VolList))
                    throw TypeError(".jlgq.VolInfo.VolList: array expected");
                message.VolList = [];
                for (let i = 0; i < object.VolList.length; ++i)
                    message.VolList[i] = object.VolList[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a VolInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgq.VolInfo
         * @static
         * @param {jlgq.VolInfo} message VolInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VolInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.VolList = [];
            if (options.defaults)
                object.VolPos = 0;
            if (message.VolPos != null && message.hasOwnProperty("VolPos"))
                object.VolPos = message.VolPos;
            if (message.VolList && message.VolList.length) {
                object.VolList = [];
                for (let j = 0; j < message.VolList.length; ++j)
                    object.VolList[j] = message.VolList[j];
            }
            return object;
        };

        /**
         * Converts this VolInfo to JSON.
         * @function toJSON
         * @memberof jlgq.VolInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VolInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VolInfo
         * @function getTypeUrl
         * @memberof jlgq.VolInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VolInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgq.VolInfo";
        };

        return VolInfo;
    })();

    return jlgq;
})();

export const jlgr = $root.jlgr = (() => {

    /**
     * Namespace jlgr.
     * @exports jlgr
     * @namespace
     */
    const jlgr = {};

    jlgr.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlgr
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlgr
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlgr.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlgr.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlgr.Column
         * @static
         * @param {jlgr.IColumn=} [properties] Properties to set
         * @returns {jlgr.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlgr.Column.verify|verify} messages.
         * @function encode
         * @memberof jlgr.Column
         * @static
         * @param {jlgr.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlgr.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgr.Column
         * @static
         * @param {jlgr.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlgr.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgr.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgr.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgr.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgr.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlgr.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgr.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgr.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgr.Column)
                return object;
            let message = new $root.jlgr.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlgr.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgr.Column
         * @static
         * @param {jlgr.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlgr.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlgr.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgr.Column";
        };

        return Column;
    })();

    jlgr.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlgr
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlgr
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlgr.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlgr.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {jlgr.IColumnFloat=} [properties] Properties to set
         * @returns {jlgr.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlgr.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {jlgr.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlgr.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {jlgr.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgr.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgr.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgr.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgr.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgr.ColumnFloat)
                return object;
            let message = new $root.jlgr.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlgr.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {jlgr.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlgr.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlgr.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgr.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlgr.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlgr
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Position] AwardData Position
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlgr
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlgr.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlgr.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlgr.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlgr.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlgr.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Position.
         * @member {number} Position
         * @memberof jlgr.AwardData
         * @instance
         */
        AwardData.prototype.Position = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlgr.AwardData
         * @static
         * @param {jlgr.IAwardData=} [properties] Properties to set
         * @returns {jlgr.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlgr.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlgr.AwardData
         * @static
         * @param {jlgr.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Position != null && Object.hasOwnProperty.call(message, "Position"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.Position);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlgr.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgr.AwardData
         * @static
         * @param {jlgr.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgr.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgr.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgr.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        message.Position = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgr.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgr.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlgr.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Position != null && message.hasOwnProperty("Position"))
                if (!$util.isInteger(message.Position))
                    return "Position: integer expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgr.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgr.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgr.AwardData)
                return object;
            let message = new $root.jlgr.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Position != null)
                message.Position = object.Position | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgr.AwardData
         * @static
         * @param {jlgr.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
                object.Position = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Position != null && message.hasOwnProperty("Position"))
                object.Position = message.Position;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlgr.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlgr.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgr.AwardData";
        };

        return AwardData;
    })();

    jlgr.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlgr
         * @interface IRoundInfo
         * @property {Array.<jlgr.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlgr.IColumnFloat>|null} [PlateSymbolNum] RoundInfo PlateSymbolNum
         * @property {Array.<jlgr.IColumnFloat>|null} [MultPlate] RoundInfo MultPlate
         * @property {Array.<jlgr.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [Free2Base] RoundInfo Free2Base
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [PlusRound] RoundInfo PlusRound
         * @property {number|null} [CountRound] RoundInfo CountRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlgr
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlgr.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            this.MultPlate = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlgr.IColumn>} PlateSymbol
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNum.
         * @member {Array.<jlgr.IColumnFloat>} PlateSymbolNum
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * RoundInfo MultPlate.
         * @member {Array.<jlgr.IColumnFloat>} MultPlate
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MultPlate = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlgr.IAwardData>} AwardDataVec
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo Free2Base.
         * @member {number} Free2Base
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Free2Base = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo PlusRound.
         * @member {number} PlusRound
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlusRound = 0;

        /**
         * RoundInfo CountRound.
         * @member {number} CountRound
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CountRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlgr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlgr.RoundInfo
         * @static
         * @param {jlgr.IRoundInfo=} [properties] Properties to set
         * @returns {jlgr.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlgr.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgr.RoundInfo
         * @static
         * @param {jlgr.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlgr.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jlgr.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.MultPlate != null && message.MultPlate.length)
                for (let i = 0; i < message.MultPlate.length; ++i)
                    $root.jlgr.ColumnFloat.encode(message.MultPlate[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlgr.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.Free2Base != null && Object.hasOwnProperty.call(message, "Free2Base"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Free2Base);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.PlusRound != null && Object.hasOwnProperty.call(message, "PlusRound"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.PlusRound);
            if (message.CountRound != null && Object.hasOwnProperty.call(message, "CountRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.CountRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlgr.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgr.RoundInfo
         * @static
         * @param {jlgr.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgr.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgr.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgr.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlgr.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jlgr.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.MultPlate && message.MultPlate.length))
                            message.MultPlate = [];
                        message.MultPlate.push($root.jlgr.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlgr.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.Free2Base = reader.double();
                        break;
                    }
                case 7: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        message.PlusRound = reader.int32();
                        break;
                    }
                case 10: {
                        message.CountRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgr.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgr.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlgr.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlgr.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jlgr.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.MultPlate != null && message.hasOwnProperty("MultPlate")) {
                if (!Array.isArray(message.MultPlate))
                    return "MultPlate: array expected";
                for (let i = 0; i < message.MultPlate.length; ++i) {
                    let error = $root.jlgr.ColumnFloat.verify(message.MultPlate[i]);
                    if (error)
                        return "MultPlate." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlgr.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.Free2Base != null && message.hasOwnProperty("Free2Base"))
                if (typeof message.Free2Base !== "number")
                    return "Free2Base: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.PlusRound != null && message.hasOwnProperty("PlusRound"))
                if (!$util.isInteger(message.PlusRound))
                    return "PlusRound: integer expected";
            if (message.CountRound != null && message.hasOwnProperty("CountRound"))
                if (!$util.isInteger(message.CountRound))
                    return "CountRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgr.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgr.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgr.RoundInfo)
                return object;
            let message = new $root.jlgr.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlgr.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlgr.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlgr.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jlgr.RoundInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jlgr.RoundInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jlgr.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.MultPlate) {
                if (!Array.isArray(object.MultPlate))
                    throw TypeError(".jlgr.RoundInfo.MultPlate: array expected");
                message.MultPlate = [];
                for (let i = 0; i < object.MultPlate.length; ++i) {
                    if (typeof object.MultPlate[i] !== "object")
                        throw TypeError(".jlgr.RoundInfo.MultPlate: object expected");
                    message.MultPlate[i] = $root.jlgr.ColumnFloat.fromObject(object.MultPlate[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlgr.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlgr.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlgr.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.Free2Base != null)
                message.Free2Base = Number(object.Free2Base);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.PlusRound != null)
                message.PlusRound = object.PlusRound | 0;
            if (object.CountRound != null)
                message.CountRound = object.CountRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgr.RoundInfo
         * @static
         * @param {jlgr.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
                object.MultPlate = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.Free2Base = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.PlusRound = 0;
                object.CountRound = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlgr.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jlgr.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.MultPlate && message.MultPlate.length) {
                object.MultPlate = [];
                for (let j = 0; j < message.MultPlate.length; ++j)
                    object.MultPlate[j] = $root.jlgr.ColumnFloat.toObject(message.MultPlate[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlgr.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.Free2Base != null && message.hasOwnProperty("Free2Base"))
                object.Free2Base = options.json && !isFinite(message.Free2Base) ? String(message.Free2Base) : message.Free2Base;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.PlusRound != null && message.hasOwnProperty("PlusRound"))
                object.PlusRound = message.PlusRound;
            if (message.CountRound != null && message.hasOwnProperty("CountRound"))
                object.CountRound = message.CountRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlgr.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlgr.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgr.RoundInfo";
        };

        return RoundInfo;
    })();

    jlgr.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlgr
         * @interface ISpinAck
         * @property {Array.<jlgr.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [Free1TotalWin] SpinAck Free1TotalWin
         * @property {number|null} [Free2TotalWin] SpinAck Free2TotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {Array.<number>|null} [FreeRounds] SpinAck FreeRounds
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlgr
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlgr.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.FreeRounds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlgr.IRoundInfo>} RoundQueue
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck Free1TotalWin.
         * @member {number} Free1TotalWin
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.Free1TotalWin = 0;

        /**
         * SpinAck Free2TotalWin.
         * @member {number} Free2TotalWin
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.Free2TotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck FreeRounds.
         * @member {Array.<number>} FreeRounds
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeRounds = $util.emptyArray;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlgr.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlgr.SpinAck
         * @static
         * @param {jlgr.ISpinAck=} [properties] Properties to set
         * @returns {jlgr.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlgr.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlgr.SpinAck
         * @static
         * @param {jlgr.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlgr.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Free1TotalWin != null && Object.hasOwnProperty.call(message, "Free1TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Free1TotalWin);
            if (message.Free2TotalWin != null && Object.hasOwnProperty.call(message, "Free2TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Free2TotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.Extra);
            if (message.FreeRounds != null && message.FreeRounds.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.FreeRounds.length; ++i)
                    writer.int32(message.FreeRounds[i]);
                writer.ldelim();
            }
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlgr.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgr.SpinAck
         * @static
         * @param {jlgr.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgr.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgr.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgr.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlgr.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Free1TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.Free2TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 9: {
                        if (!(message.FreeRounds && message.FreeRounds.length))
                            message.FreeRounds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FreeRounds.push(reader.int32());
                        } else
                            message.FreeRounds.push(reader.int32());
                        break;
                    }
                case 10: {
                        message.RTP = reader.double();
                        break;
                    }
                case 11: {
                        message.LID = reader.int32();
                        break;
                    }
                case 12: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgr.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgr.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlgr.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlgr.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.Free1TotalWin != null && message.hasOwnProperty("Free1TotalWin"))
                if (typeof message.Free1TotalWin !== "number")
                    return "Free1TotalWin: number expected";
            if (message.Free2TotalWin != null && message.hasOwnProperty("Free2TotalWin"))
                if (typeof message.Free2TotalWin !== "number")
                    return "Free2TotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.FreeRounds != null && message.hasOwnProperty("FreeRounds")) {
                if (!Array.isArray(message.FreeRounds))
                    return "FreeRounds: array expected";
                for (let i = 0; i < message.FreeRounds.length; ++i)
                    if (!$util.isInteger(message.FreeRounds[i]))
                        return "FreeRounds: integer[] expected";
            }
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgr.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgr.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgr.SpinAck)
                return object;
            let message = new $root.jlgr.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlgr.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlgr.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlgr.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.Free1TotalWin != null)
                message.Free1TotalWin = Number(object.Free1TotalWin);
            if (object.Free2TotalWin != null)
                message.Free2TotalWin = Number(object.Free2TotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.FreeRounds) {
                if (!Array.isArray(object.FreeRounds))
                    throw TypeError(".jlgr.SpinAck.FreeRounds: array expected");
                message.FreeRounds = [];
                for (let i = 0; i < object.FreeRounds.length; ++i)
                    message.FreeRounds[i] = object.FreeRounds[i] | 0;
            }
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgr.SpinAck
         * @static
         * @param {jlgr.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.FreeRounds = [];
            }
            if (options.defaults) {
                object.Free1TotalWin = 0;
                object.Free2TotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlgr.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.Free1TotalWin != null && message.hasOwnProperty("Free1TotalWin"))
                object.Free1TotalWin = options.json && !isFinite(message.Free1TotalWin) ? String(message.Free1TotalWin) : message.Free1TotalWin;
            if (message.Free2TotalWin != null && message.hasOwnProperty("Free2TotalWin"))
                object.Free2TotalWin = options.json && !isFinite(message.Free2TotalWin) ? String(message.Free2TotalWin) : message.Free2TotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.FreeRounds && message.FreeRounds.length) {
                object.FreeRounds = [];
                for (let j = 0; j < message.FreeRounds.length; ++j)
                    object.FreeRounds[j] = message.FreeRounds[j];
            }
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlgr.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlgr.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgr.SpinAck";
        };

        return SpinAck;
    })();

    jlgr.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlgr
         * @interface IGameInfoAck
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         * @property {Array.<number>|null} [FakeBonusOdds] GameInfoAck FakeBonusOdds
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlgr
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlgr.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            this.FakeBonusOdds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlgr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoAck FakeBonusOdds.
         * @member {Array.<number>} FakeBonusOdds
         * @memberof jlgr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.FakeBonusOdds = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {jlgr.IGameInfoAck=} [properties] Properties to set
         * @returns {jlgr.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlgr.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {jlgr.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.FakeBonusOdds != null && message.FakeBonusOdds.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.FakeBonusOdds.length; ++i)
                    writer.double(message.FakeBonusOdds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlgr.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {jlgr.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgr.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgr.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 2: {
                        if (!(message.FakeBonusOdds && message.FakeBonusOdds.length))
                            message.FakeBonusOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FakeBonusOdds.push(reader.double());
                        } else
                            message.FakeBonusOdds.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgr.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.FakeBonusOdds != null && message.hasOwnProperty("FakeBonusOdds")) {
                if (!Array.isArray(message.FakeBonusOdds))
                    return "FakeBonusOdds: array expected";
                for (let i = 0; i < message.FakeBonusOdds.length; ++i)
                    if (typeof message.FakeBonusOdds[i] !== "number")
                        return "FakeBonusOdds: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgr.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgr.GameInfoAck)
                return object;
            let message = new $root.jlgr.GameInfoAck();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlgr.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.FakeBonusOdds) {
                if (!Array.isArray(object.FakeBonusOdds))
                    throw TypeError(".jlgr.GameInfoAck.FakeBonusOdds: array expected");
                message.FakeBonusOdds = [];
                for (let i = 0; i < object.FakeBonusOdds.length; ++i)
                    message.FakeBonusOdds[i] = Number(object.FakeBonusOdds[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {jlgr.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.FakeBonusOdds = [];
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.FakeBonusOdds && message.FakeBonusOdds.length) {
                object.FakeBonusOdds = [];
                for (let j = 0; j < message.FakeBonusOdds.length; ++j)
                    object.FakeBonusOdds[j] = options.json && !isFinite(message.FakeBonusOdds[j]) ? String(message.FakeBonusOdds[j]) : message.FakeBonusOdds[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlgr.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlgr.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgr.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlgr;
})();

export const jlgt = $root.jlgt = (() => {

    /**
     * Namespace jlgt.
     * @exports jlgt
     * @namespace
     */
    const jlgt = {};

    jlgt.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlgt
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlgt
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlgt.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlgt.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlgt.Column
         * @static
         * @param {jlgt.IColumn=} [properties] Properties to set
         * @returns {jlgt.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlgt.Column.verify|verify} messages.
         * @function encode
         * @memberof jlgt.Column
         * @static
         * @param {jlgt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlgt.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgt.Column
         * @static
         * @param {jlgt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlgt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgt.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlgt.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgt.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgt.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgt.Column)
                return object;
            let message = new $root.jlgt.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlgt.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgt.Column
         * @static
         * @param {jlgt.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlgt.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlgt.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgt.Column";
        };

        return Column;
    })();

    jlgt.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlgt
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlgt
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlgt.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlgt.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlgt.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlgt.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlgt.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlgt.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlgt.AwardData
         * @static
         * @param {jlgt.IAwardData=} [properties] Properties to set
         * @returns {jlgt.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlgt.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlgt.AwardData
         * @static
         * @param {jlgt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlgt.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgt.AwardData
         * @static
         * @param {jlgt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgt.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlgt.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgt.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgt.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgt.AwardData)
                return object;
            let message = new $root.jlgt.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlgt.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgt.AwardData
         * @static
         * @param {jlgt.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlgt.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlgt.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgt.AwardData";
        };

        return AwardData;
    })();

    jlgt.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlgt
         * @interface IComboStageInfo
         * @property {Array.<jlgt.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlgt.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<number>|null} [HitSlatePosVec] ComboStageInfo HitSlatePosVec
         * @property {Array.<jlgt.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlgt.IColumn>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         * @property {number|null} [AddRounds] ComboStageInfo AddRounds
         * @property {number|null} [AddMults] ComboStageInfo AddMults
         * @property {Array.<number>|null} [FGChange] ComboStageInfo FGChange
         * @property {Array.<number>|null} [Holy] ComboStageInfo Holy
         * @property {number|null} [FGChangeSymbol] ComboStageInfo FGChangeSymbol
         * @property {Array.<jlgt.IColumn>|null} [ComboStageSymbolLog] ComboStageInfo ComboStageSymbolLog
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlgt
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlgt.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.HitSlatePosVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            this.FGChange = [];
            this.Holy = [];
            this.ComboStageSymbolLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlgt.IColumn>} ComboStageSymbol
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlgt.IAwardData>} AwardDataVec
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo HitSlatePosVec.
         * @member {Array.<number>} HitSlatePosVec
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.HitSlatePosVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlgt.IColumn>} FillSymbols
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlgt.IColumn>} ChangeSymbols
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * ComboStageInfo AddRounds.
         * @member {number} AddRounds
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AddRounds = 0;

        /**
         * ComboStageInfo AddMults.
         * @member {number} AddMults
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AddMults = 0;

        /**
         * ComboStageInfo FGChange.
         * @member {Array.<number>} FGChange
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FGChange = $util.emptyArray;

        /**
         * ComboStageInfo Holy.
         * @member {Array.<number>} Holy
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Holy = $util.emptyArray;

        /**
         * ComboStageInfo FGChangeSymbol.
         * @member {number} FGChangeSymbol
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FGChangeSymbol = 0;

        /**
         * ComboStageInfo ComboStageSymbolLog.
         * @member {Array.<jlgt.IColumn>} ComboStageSymbolLog
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbolLog = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlgt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {jlgt.IComboStageInfo=} [properties] Properties to set
         * @returns {jlgt.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlgt.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {jlgt.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlgt.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlgt.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.HitSlatePosVec != null && message.HitSlatePosVec.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.HitSlatePosVec.length; ++i)
                    writer.int32(message.HitSlatePosVec[i]);
                writer.ldelim();
            }
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlgt.Column.encode(message.FillSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlgt.Column.encode(message.ChangeSymbols[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.AddRounds != null && Object.hasOwnProperty.call(message, "AddRounds"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AddRounds);
            if (message.AddMults != null && Object.hasOwnProperty.call(message, "AddMults"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AddMults);
            if (message.FGChange != null && message.FGChange.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.FGChange.length; ++i)
                    writer.int32(message.FGChange[i]);
                writer.ldelim();
            }
            if (message.Holy != null && message.Holy.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.Holy.length; ++i)
                    writer.int32(message.Holy[i]);
                writer.ldelim();
            }
            if (message.FGChangeSymbol != null && Object.hasOwnProperty.call(message, "FGChangeSymbol"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.FGChangeSymbol);
            if (message.ComboStageSymbolLog != null && message.ComboStageSymbolLog.length)
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i)
                    $root.jlgt.Column.encode(message.ComboStageSymbolLog[i], writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlgt.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {jlgt.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgt.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgt.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlgt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlgt.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.HitSlatePosVec && message.HitSlatePosVec.length))
                            message.HitSlatePosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.HitSlatePosVec.push(reader.int32());
                        } else
                            message.HitSlatePosVec.push(reader.int32());
                        break;
                    }
                case 6: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlgt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlgt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.AddRounds = reader.int32();
                        break;
                    }
                case 9: {
                        message.AddMults = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.FGChange && message.FGChange.length))
                            message.FGChange = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FGChange.push(reader.int32());
                        } else
                            message.FGChange.push(reader.int32());
                        break;
                    }
                case 11: {
                        if (!(message.Holy && message.Holy.length))
                            message.Holy = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Holy.push(reader.int32());
                        } else
                            message.Holy.push(reader.int32());
                        break;
                    }
                case 12: {
                        message.FGChangeSymbol = reader.int32();
                        break;
                    }
                case 13: {
                        if (!(message.ComboStageSymbolLog && message.ComboStageSymbolLog.length))
                            message.ComboStageSymbolLog = [];
                        message.ComboStageSymbolLog.push($root.jlgt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 14: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgt.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlgt.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlgt.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.HitSlatePosVec != null && message.hasOwnProperty("HitSlatePosVec")) {
                if (!Array.isArray(message.HitSlatePosVec))
                    return "HitSlatePosVec: array expected";
                for (let i = 0; i < message.HitSlatePosVec.length; ++i)
                    if (!$util.isInteger(message.HitSlatePosVec[i]))
                        return "HitSlatePosVec: integer[] expected";
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlgt.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlgt.Column.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                if (!$util.isInteger(message.AddRounds))
                    return "AddRounds: integer expected";
            if (message.AddMults != null && message.hasOwnProperty("AddMults"))
                if (!$util.isInteger(message.AddMults))
                    return "AddMults: integer expected";
            if (message.FGChange != null && message.hasOwnProperty("FGChange")) {
                if (!Array.isArray(message.FGChange))
                    return "FGChange: array expected";
                for (let i = 0; i < message.FGChange.length; ++i)
                    if (!$util.isInteger(message.FGChange[i]))
                        return "FGChange: integer[] expected";
            }
            if (message.Holy != null && message.hasOwnProperty("Holy")) {
                if (!Array.isArray(message.Holy))
                    return "Holy: array expected";
                for (let i = 0; i < message.Holy.length; ++i)
                    if (!$util.isInteger(message.Holy[i]))
                        return "Holy: integer[] expected";
            }
            if (message.FGChangeSymbol != null && message.hasOwnProperty("FGChangeSymbol"))
                if (!$util.isInteger(message.FGChangeSymbol))
                    return "FGChangeSymbol: integer expected";
            if (message.ComboStageSymbolLog != null && message.hasOwnProperty("ComboStageSymbolLog")) {
                if (!Array.isArray(message.ComboStageSymbolLog))
                    return "ComboStageSymbolLog: array expected";
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i) {
                    let error = $root.jlgt.Column.verify(message.ComboStageSymbolLog[i]);
                    if (error)
                        return "ComboStageSymbolLog." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgt.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgt.ComboStageInfo)
                return object;
            let message = new $root.jlgt.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlgt.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlgt.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlgt.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlgt.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlgt.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlgt.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.HitSlatePosVec) {
                if (!Array.isArray(object.HitSlatePosVec))
                    throw TypeError(".jlgt.ComboStageInfo.HitSlatePosVec: array expected");
                message.HitSlatePosVec = [];
                for (let i = 0; i < object.HitSlatePosVec.length; ++i)
                    message.HitSlatePosVec[i] = object.HitSlatePosVec[i] | 0;
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlgt.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlgt.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlgt.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlgt.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlgt.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlgt.Column.fromObject(object.ChangeSymbols[i]);
                }
            }
            if (object.AddRounds != null)
                message.AddRounds = object.AddRounds | 0;
            if (object.AddMults != null)
                message.AddMults = object.AddMults | 0;
            if (object.FGChange) {
                if (!Array.isArray(object.FGChange))
                    throw TypeError(".jlgt.ComboStageInfo.FGChange: array expected");
                message.FGChange = [];
                for (let i = 0; i < object.FGChange.length; ++i)
                    message.FGChange[i] = object.FGChange[i] | 0;
            }
            if (object.Holy) {
                if (!Array.isArray(object.Holy))
                    throw TypeError(".jlgt.ComboStageInfo.Holy: array expected");
                message.Holy = [];
                for (let i = 0; i < object.Holy.length; ++i)
                    message.Holy[i] = object.Holy[i] | 0;
            }
            if (object.FGChangeSymbol != null)
                message.FGChangeSymbol = object.FGChangeSymbol | 0;
            if (object.ComboStageSymbolLog) {
                if (!Array.isArray(object.ComboStageSymbolLog))
                    throw TypeError(".jlgt.ComboStageInfo.ComboStageSymbolLog: array expected");
                message.ComboStageSymbolLog = [];
                for (let i = 0; i < object.ComboStageSymbolLog.length; ++i) {
                    if (typeof object.ComboStageSymbolLog[i] !== "object")
                        throw TypeError(".jlgt.ComboStageInfo.ComboStageSymbolLog: object expected");
                    message.ComboStageSymbolLog[i] = $root.jlgt.Column.fromObject(object.ComboStageSymbolLog[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {jlgt.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.HitSlatePosVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
                object.FGChange = [];
                object.Holy = [];
                object.ComboStageSymbolLog = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
                object.AddRounds = 0;
                object.AddMults = 0;
                object.FGChangeSymbol = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlgt.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlgt.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.HitSlatePosVec && message.HitSlatePosVec.length) {
                object.HitSlatePosVec = [];
                for (let j = 0; j < message.HitSlatePosVec.length; ++j)
                    object.HitSlatePosVec[j] = message.HitSlatePosVec[j];
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlgt.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlgt.Column.toObject(message.ChangeSymbols[j], options);
            }
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                object.AddRounds = message.AddRounds;
            if (message.AddMults != null && message.hasOwnProperty("AddMults"))
                object.AddMults = message.AddMults;
            if (message.FGChange && message.FGChange.length) {
                object.FGChange = [];
                for (let j = 0; j < message.FGChange.length; ++j)
                    object.FGChange[j] = message.FGChange[j];
            }
            if (message.Holy && message.Holy.length) {
                object.Holy = [];
                for (let j = 0; j < message.Holy.length; ++j)
                    object.Holy[j] = message.Holy[j];
            }
            if (message.FGChangeSymbol != null && message.hasOwnProperty("FGChangeSymbol"))
                object.FGChangeSymbol = message.FGChangeSymbol;
            if (message.ComboStageSymbolLog && message.ComboStageSymbolLog.length) {
                object.ComboStageSymbolLog = [];
                for (let j = 0; j < message.ComboStageSymbolLog.length; ++j)
                    object.ComboStageSymbolLog[j] = $root.jlgt.Column.toObject(message.ComboStageSymbolLog[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlgt.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlgt.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgt.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlgt.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlgt
         * @interface IRoundInfo
         * @property {Array.<jlgt.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRounds] RoundInfo AddRounds
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlgt
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlgt.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlgt.IComboStageInfo>} ComboStageData
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRounds.
         * @member {number} AddRounds
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRounds = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlgt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlgt.RoundInfo
         * @static
         * @param {jlgt.IRoundInfo=} [properties] Properties to set
         * @returns {jlgt.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlgt.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlgt.RoundInfo
         * @static
         * @param {jlgt.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlgt.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeTotalRound);
            if (message.AddRounds != null && Object.hasOwnProperty.call(message, "AddRounds"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AddRounds);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlgt.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgt.RoundInfo
         * @static
         * @param {jlgt.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlgt.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgt.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgt.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlgt.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.AddRounds = reader.int32();
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgt.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgt.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlgt.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlgt.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                if (!$util.isInteger(message.AddRounds))
                    return "AddRounds: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgt.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgt.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgt.RoundInfo)
                return object;
            let message = new $root.jlgt.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlgt.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlgt.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlgt.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRounds != null)
                message.AddRounds = object.AddRounds | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgt.RoundInfo
         * @static
         * @param {jlgt.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
                object.AddRounds = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlgt.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                object.AddRounds = message.AddRounds;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlgt.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlgt.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgt.RoundInfo";
        };

        return RoundInfo;
    })();

    jlgt.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlgt
         * @interface ISpinAck
         * @property {Array.<jlgt.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlgt
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlgt.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlgt.IRoundInfo>} RoundQueue
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlgt.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlgt.SpinAck
         * @static
         * @param {jlgt.ISpinAck=} [properties] Properties to set
         * @returns {jlgt.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlgt.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlgt.SpinAck
         * @static
         * @param {jlgt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlgt.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlgt.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgt.SpinAck
         * @static
         * @param {jlgt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlgt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgt.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlgt.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlgt.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlgt.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgt.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgt.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgt.SpinAck)
                return object;
            let message = new $root.jlgt.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlgt.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlgt.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlgt.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgt.SpinAck
         * @static
         * @param {jlgt.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlgt.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlgt.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlgt.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgt.SpinAck";
        };

        return SpinAck;
    })();

    jlgt.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlgt
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<jlgt.IColumn>|null} [ComboSetting] GameInfoData ComboSetting
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlgt
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlgt.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.ComboSetting = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlgt.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlgt.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData ComboSetting.
         * @member {Array.<jlgt.IColumn>} ComboSetting
         * @memberof jlgt.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ComboSetting = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlgt.GameInfoData
         * @static
         * @param {jlgt.IGameInfoData=} [properties] Properties to set
         * @returns {jlgt.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlgt.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlgt.GameInfoData
         * @static
         * @param {jlgt.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.ComboSetting != null && message.ComboSetting.length)
                for (let i = 0; i < message.ComboSetting.length; ++i)
                    $root.jlgt.Column.encode(message.ComboSetting[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlgt.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlgt.GameInfoData
         * @static
         * @param {jlgt.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlgt.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlgt.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlgt.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.ComboSetting && message.ComboSetting.length))
                            message.ComboSetting = [];
                        message.ComboSetting.push($root.jlgt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlgt.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlgt.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlgt.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.ComboSetting != null && message.hasOwnProperty("ComboSetting")) {
                if (!Array.isArray(message.ComboSetting))
                    return "ComboSetting: array expected";
                for (let i = 0; i < message.ComboSetting.length; ++i) {
                    let error = $root.jlgt.Column.verify(message.ComboSetting[i]);
                    if (error)
                        return "ComboSetting." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlgt.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlgt.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlgt.GameInfoData)
                return object;
            let message = new $root.jlgt.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlgt.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.ComboSetting) {
                if (!Array.isArray(object.ComboSetting))
                    throw TypeError(".jlgt.GameInfoData.ComboSetting: array expected");
                message.ComboSetting = [];
                for (let i = 0; i < object.ComboSetting.length; ++i) {
                    if (typeof object.ComboSetting[i] !== "object")
                        throw TypeError(".jlgt.GameInfoData.ComboSetting: object expected");
                    message.ComboSetting[i] = $root.jlgt.Column.fromObject(object.ComboSetting[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlgt.GameInfoData
         * @static
         * @param {jlgt.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.ComboSetting = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.ComboSetting && message.ComboSetting.length) {
                object.ComboSetting = [];
                for (let j = 0; j < message.ComboSetting.length; ++j)
                    object.ComboSetting[j] = $root.jlgt.Column.toObject(message.ComboSetting[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlgt.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlgt.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlgt.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlgt;
})();

export const jlhb = $root.jlhb = (() => {

    /**
     * Namespace jlhb.
     * @exports jlhb
     * @namespace
     */
    const jlhb = {};

    /**
     * Symbol enum.
     * @name jlhb.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Symbol1=1 Symbol1 value
     * @property {number} Symbol2=2 Symbol2 value
     * @property {number} Symbol3=3 Symbol3 value
     * @property {number} Symbol4=4 Symbol4 value
     * @property {number} Symbol5=5 Symbol5 value
     * @property {number} Symbol6=6 Symbol6 value
     * @property {number} Symbol7=7 Symbol7 value
     */
    jlhb.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Symbol1"] = 1;
        values[valuesById[2] = "Symbol2"] = 2;
        values[valuesById[3] = "Symbol3"] = 3;
        values[valuesById[4] = "Symbol4"] = 4;
        values[valuesById[5] = "Symbol5"] = 5;
        values[valuesById[6] = "Symbol6"] = 6;
        values[valuesById[7] = "Symbol7"] = 7;
        return values;
    })();

    jlhb.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlhb
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlhb
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlhb.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlhb.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlhb.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlhb.Block
         * @static
         * @param {jlhb.IBlock=} [properties] Properties to set
         * @returns {jlhb.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlhb.Block.verify|verify} messages.
         * @function encode
         * @memberof jlhb.Block
         * @static
         * @param {jlhb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlhb.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhb.Block
         * @static
         * @param {jlhb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlhb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhb.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlhb.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhb.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhb.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhb.Block)
                return object;
            let message = new $root.jlhb.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhb.Block
         * @static
         * @param {jlhb.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlhb.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlhb.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhb.Block";
        };

        return Block;
    })();

    jlhb.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlhb
         * @interface IAwardReel
         * @property {number|null} [line] AwardReel line
         * @property {Array.<jlhb.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         * @property {number|null} [dir] AwardReel dir
         * @property {number|null} [symbol] AwardReel symbol
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlhb
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlhb.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel line.
         * @member {number} line
         * @memberof jlhb.AwardReel
         * @instance
         */
        AwardReel.prototype.line = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlhb.IBlock>} block
         * @memberof jlhb.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlhb.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * AwardReel dir.
         * @member {number} dir
         * @memberof jlhb.AwardReel
         * @instance
         */
        AwardReel.prototype.dir = 0;

        /**
         * AwardReel symbol.
         * @member {number} symbol
         * @memberof jlhb.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlhb.AwardReel
         * @static
         * @param {jlhb.IAwardReel=} [properties] Properties to set
         * @returns {jlhb.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlhb.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlhb.AwardReel
         * @static
         * @param {jlhb.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.line);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlhb.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.dir != null && Object.hasOwnProperty.call(message, "dir"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.dir);
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.symbol);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlhb.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhb.AwardReel
         * @static
         * @param {jlhb.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlhb.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhb.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhb.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.line = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlhb.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.dir = reader.uint32();
                        break;
                    }
                case 5: {
                        message.symbol = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhb.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhb.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlhb.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.line != null && message.hasOwnProperty("line"))
                if (!$util.isInteger(message.line))
                    return "line: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlhb.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.dir != null && message.hasOwnProperty("dir"))
                if (!$util.isInteger(message.dir))
                    return "dir: integer expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhb.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhb.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhb.AwardReel)
                return object;
            let message = new $root.jlhb.AwardReel();
            if (object.line != null)
                message.line = object.line >>> 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlhb.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlhb.AwardReel.block: object expected");
                    message.block[i] = $root.jlhb.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.dir != null)
                message.dir = object.dir >>> 0;
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhb.AwardReel
         * @static
         * @param {jlhb.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.line = 0;
                object.win = 0;
                object.dir = 0;
                object.symbol = 0;
            }
            if (message.line != null && message.hasOwnProperty("line"))
                object.line = message.line;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlhb.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.dir != null && message.hasOwnProperty("dir"))
                object.dir = message.dir;
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlhb.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlhb.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhb.AwardReel";
        };

        return AwardReel;
    })();

    jlhb.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlhb
         * @interface IPlate
         * @property {Array.<jlhb.IRow>|null} [column] Plate column
         * @property {Array.<jlhb.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         * @property {boolean|null} [isBonus] Plate isBonus
         */

        /**
         * Constructs a new Plate.
         * @memberof jlhb
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlhb.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlhb.IRow>} column
         * @memberof jlhb.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jlhb.IAwardReel>} award
         * @memberof jlhb.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlhb.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate isBonus.
         * @member {boolean} isBonus
         * @memberof jlhb.Plate
         * @instance
         */
        Plate.prototype.isBonus = false;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlhb.Plate
         * @static
         * @param {jlhb.IPlate=} [properties] Properties to set
         * @returns {jlhb.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlhb.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlhb.Plate
         * @static
         * @param {jlhb.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlhb.Row.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlhb.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.isBonus != null && Object.hasOwnProperty.call(message, "isBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isBonus);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlhb.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhb.Plate
         * @static
         * @param {jlhb.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlhb.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhb.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhb.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlhb.Row.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlhb.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.isBonus = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhb.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhb.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlhb.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlhb.Row.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlhb.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                if (typeof message.isBonus !== "boolean")
                    return "isBonus: boolean expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhb.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhb.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhb.Plate)
                return object;
            let message = new $root.jlhb.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlhb.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlhb.Plate.column: object expected");
                    message.column[i] = $root.jlhb.Row.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlhb.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlhb.Plate.award: object expected");
                    message.award[i] = $root.jlhb.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.isBonus != null)
                message.isBonus = Boolean(object.isBonus);
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhb.Plate
         * @static
         * @param {jlhb.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.isBonus = false;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlhb.Row.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlhb.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                object.isBonus = message.isBonus;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlhb.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlhb.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhb.Plate";
        };

        return Plate;
    })();

    jlhb.Row = (function() {

        /**
         * Properties of a Row.
         * @memberof jlhb
         * @interface IRow
         * @property {Array.<number>|null} [row] Row row
         */

        /**
         * Constructs a new Row.
         * @memberof jlhb
         * @classdesc Represents a Row.
         * @implements IRow
         * @constructor
         * @param {jlhb.IRow=} [properties] Properties to set
         */
        function Row(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Row row.
         * @member {Array.<number>} row
         * @memberof jlhb.Row
         * @instance
         */
        Row.prototype.row = $util.emptyArray;

        /**
         * Creates a new Row instance using the specified properties.
         * @function create
         * @memberof jlhb.Row
         * @static
         * @param {jlhb.IRow=} [properties] Properties to set
         * @returns {jlhb.Row} Row instance
         */
        Row.create = function create(properties) {
            return new Row(properties);
        };

        /**
         * Encodes the specified Row message. Does not implicitly {@link jlhb.Row.verify|verify} messages.
         * @function encode
         * @memberof jlhb.Row
         * @static
         * @param {jlhb.IRow} message Row message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Row.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Row message, length delimited. Does not implicitly {@link jlhb.Row.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhb.Row
         * @static
         * @param {jlhb.IRow} message Row message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Row.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Row message from the specified reader or buffer.
         * @function decode
         * @memberof jlhb.Row
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhb.Row} Row
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Row.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhb.Row();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Row message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhb.Row
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhb.Row} Row
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Row.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Row message.
         * @function verify
         * @memberof jlhb.Row
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Row.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Row message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhb.Row
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhb.Row} Row
         */
        Row.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhb.Row)
                return object;
            let message = new $root.jlhb.Row();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlhb.Row.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Row message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhb.Row
         * @static
         * @param {jlhb.Row} message Row
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Row.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            return object;
        };

        /**
         * Converts this Row to JSON.
         * @function toJSON
         * @memberof jlhb.Row
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Row.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Row
         * @function getTypeUrl
         * @memberof jlhb.Row
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Row.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhb.Row";
        };

        return Row;
    })();

    jlhb.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlhb
         * @interface IAllPlate
         * @property {Array.<jlhb.IPlate>|null} [plate] AllPlate plate
         * @property {jlhb.IExtraInfo|null} [extraInfo] AllPlate extraInfo
         * @property {jlhb.IExtraInfo|null} [nowExtraInfo] AllPlate nowExtraInfo
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlhb
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlhb.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlhb.IPlate>} plate
         * @memberof jlhb.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * AllPlate extraInfo.
         * @member {jlhb.IExtraInfo|null|undefined} extraInfo
         * @memberof jlhb.AllPlate
         * @instance
         */
        AllPlate.prototype.extraInfo = null;

        /**
         * AllPlate nowExtraInfo.
         * @member {jlhb.IExtraInfo|null|undefined} nowExtraInfo
         * @memberof jlhb.AllPlate
         * @instance
         */
        AllPlate.prototype.nowExtraInfo = null;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlhb.AllPlate
         * @static
         * @param {jlhb.IAllPlate=} [properties] Properties to set
         * @returns {jlhb.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlhb.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlhb.AllPlate
         * @static
         * @param {jlhb.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlhb.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.extraInfo != null && Object.hasOwnProperty.call(message, "extraInfo"))
                $root.jlhb.ExtraInfo.encode(message.extraInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.nowExtraInfo != null && Object.hasOwnProperty.call(message, "nowExtraInfo"))
                $root.jlhb.ExtraInfo.encode(message.nowExtraInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlhb.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhb.AllPlate
         * @static
         * @param {jlhb.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlhb.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhb.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhb.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlhb.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.extraInfo = $root.jlhb.ExtraInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.nowExtraInfo = $root.jlhb.ExtraInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhb.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhb.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlhb.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlhb.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            if (message.extraInfo != null && message.hasOwnProperty("extraInfo")) {
                let error = $root.jlhb.ExtraInfo.verify(message.extraInfo);
                if (error)
                    return "extraInfo." + error;
            }
            if (message.nowExtraInfo != null && message.hasOwnProperty("nowExtraInfo")) {
                let error = $root.jlhb.ExtraInfo.verify(message.nowExtraInfo);
                if (error)
                    return "nowExtraInfo." + error;
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhb.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhb.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhb.AllPlate)
                return object;
            let message = new $root.jlhb.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlhb.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlhb.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlhb.Plate.fromObject(object.plate[i]);
                }
            }
            if (object.extraInfo != null) {
                if (typeof object.extraInfo !== "object")
                    throw TypeError(".jlhb.AllPlate.extraInfo: object expected");
                message.extraInfo = $root.jlhb.ExtraInfo.fromObject(object.extraInfo);
            }
            if (object.nowExtraInfo != null) {
                if (typeof object.nowExtraInfo !== "object")
                    throw TypeError(".jlhb.AllPlate.nowExtraInfo: object expected");
                message.nowExtraInfo = $root.jlhb.ExtraInfo.fromObject(object.nowExtraInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhb.AllPlate
         * @static
         * @param {jlhb.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (options.defaults) {
                object.extraInfo = null;
                object.nowExtraInfo = null;
            }
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlhb.Plate.toObject(message.plate[j], options);
            }
            if (message.extraInfo != null && message.hasOwnProperty("extraInfo"))
                object.extraInfo = $root.jlhb.ExtraInfo.toObject(message.extraInfo, options);
            if (message.nowExtraInfo != null && message.hasOwnProperty("nowExtraInfo"))
                object.nowExtraInfo = $root.jlhb.ExtraInfo.toObject(message.nowExtraInfo, options);
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlhb.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlhb.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhb.AllPlate";
        };

        return AllPlate;
    })();

    jlhb.ExtraInfo = (function() {

        /**
         * Properties of an ExtraInfo.
         * @memberof jlhb
         * @interface IExtraInfo
         * @property {number|null} [hyperCount] ExtraInfo hyperCount
         * @property {number|null} [hyperBonus] ExtraInfo hyperBonus
         * @property {number|null} [hyperBet] ExtraInfo hyperBet
         */

        /**
         * Constructs a new ExtraInfo.
         * @memberof jlhb
         * @classdesc Represents an ExtraInfo.
         * @implements IExtraInfo
         * @constructor
         * @param {jlhb.IExtraInfo=} [properties] Properties to set
         */
        function ExtraInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExtraInfo hyperCount.
         * @member {number} hyperCount
         * @memberof jlhb.ExtraInfo
         * @instance
         */
        ExtraInfo.prototype.hyperCount = 0;

        /**
         * ExtraInfo hyperBonus.
         * @member {number} hyperBonus
         * @memberof jlhb.ExtraInfo
         * @instance
         */
        ExtraInfo.prototype.hyperBonus = 0;

        /**
         * ExtraInfo hyperBet.
         * @member {number} hyperBet
         * @memberof jlhb.ExtraInfo
         * @instance
         */
        ExtraInfo.prototype.hyperBet = 0;

        /**
         * Creates a new ExtraInfo instance using the specified properties.
         * @function create
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {jlhb.IExtraInfo=} [properties] Properties to set
         * @returns {jlhb.ExtraInfo} ExtraInfo instance
         */
        ExtraInfo.create = function create(properties) {
            return new ExtraInfo(properties);
        };

        /**
         * Encodes the specified ExtraInfo message. Does not implicitly {@link jlhb.ExtraInfo.verify|verify} messages.
         * @function encode
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {jlhb.IExtraInfo} message ExtraInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExtraInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hyperCount != null && Object.hasOwnProperty.call(message, "hyperCount"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.hyperCount);
            if (message.hyperBonus != null && Object.hasOwnProperty.call(message, "hyperBonus"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.hyperBonus);
            if (message.hyperBet != null && Object.hasOwnProperty.call(message, "hyperBet"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.hyperBet);
            return writer;
        };

        /**
         * Encodes the specified ExtraInfo message, length delimited. Does not implicitly {@link jlhb.ExtraInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {jlhb.IExtraInfo} message ExtraInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExtraInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ExtraInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhb.ExtraInfo} ExtraInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExtraInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhb.ExtraInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.hyperCount = reader.uint32();
                        break;
                    }
                case 2: {
                        message.hyperBonus = reader.double();
                        break;
                    }
                case 3: {
                        message.hyperBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExtraInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhb.ExtraInfo} ExtraInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExtraInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExtraInfo message.
         * @function verify
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExtraInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.hyperCount != null && message.hasOwnProperty("hyperCount"))
                if (!$util.isInteger(message.hyperCount))
                    return "hyperCount: integer expected";
            if (message.hyperBonus != null && message.hasOwnProperty("hyperBonus"))
                if (typeof message.hyperBonus !== "number")
                    return "hyperBonus: number expected";
            if (message.hyperBet != null && message.hasOwnProperty("hyperBet"))
                if (typeof message.hyperBet !== "number")
                    return "hyperBet: number expected";
            return null;
        };

        /**
         * Creates an ExtraInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhb.ExtraInfo} ExtraInfo
         */
        ExtraInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhb.ExtraInfo)
                return object;
            let message = new $root.jlhb.ExtraInfo();
            if (object.hyperCount != null)
                message.hyperCount = object.hyperCount >>> 0;
            if (object.hyperBonus != null)
                message.hyperBonus = Number(object.hyperBonus);
            if (object.hyperBet != null)
                message.hyperBet = Number(object.hyperBet);
            return message;
        };

        /**
         * Creates a plain object from an ExtraInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {jlhb.ExtraInfo} message ExtraInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExtraInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.hyperCount = 0;
                object.hyperBonus = 0;
                object.hyperBet = 0;
            }
            if (message.hyperCount != null && message.hasOwnProperty("hyperCount"))
                object.hyperCount = message.hyperCount;
            if (message.hyperBonus != null && message.hasOwnProperty("hyperBonus"))
                object.hyperBonus = options.json && !isFinite(message.hyperBonus) ? String(message.hyperBonus) : message.hyperBonus;
            if (message.hyperBet != null && message.hasOwnProperty("hyperBet"))
                object.hyperBet = options.json && !isFinite(message.hyperBet) ? String(message.hyperBet) : message.hyperBet;
            return object;
        };

        /**
         * Converts this ExtraInfo to JSON.
         * @function toJSON
         * @memberof jlhb.ExtraInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExtraInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExtraInfo
         * @function getTypeUrl
         * @memberof jlhb.ExtraInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExtraInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhb.ExtraInfo";
        };

        return ExtraInfo;
    })();

    return jlhb;
})();

export const jlhby = $root.jlhby = (() => {

    /**
     * Namespace jlhby.
     * @exports jlhby
     * @namespace
     */
    const jlhby = {};

    /**
     * Symbol enum.
     * @name jlhby.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     * @property {number} BonusAdd=11 BonusAdd value
     * @property {number} BonusGold=12 BonusGold value
     * @property {number} BonusRed=13 BonusRed value
     * @property {number} BonusEmpty=14 BonusEmpty value
     */
    jlhby.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        values[valuesById[11] = "BonusAdd"] = 11;
        values[valuesById[12] = "BonusGold"] = 12;
        values[valuesById[13] = "BonusRed"] = 13;
        values[valuesById[14] = "BonusEmpty"] = 14;
        return values;
    })();

    jlhby.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlhby
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlhby
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlhby.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlhby.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlhby.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlhby.Block
         * @static
         * @param {jlhby.IBlock=} [properties] Properties to set
         * @returns {jlhby.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlhby.Block.verify|verify} messages.
         * @function encode
         * @memberof jlhby.Block
         * @static
         * @param {jlhby.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlhby.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhby.Block
         * @static
         * @param {jlhby.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlhby.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhby.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhby.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhby.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhby.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlhby.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhby.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhby.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhby.Block)
                return object;
            let message = new $root.jlhby.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhby.Block
         * @static
         * @param {jlhby.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlhby.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlhby.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhby.Block";
        };

        return Block;
    })();

    jlhby.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlhby
         * @interface IAwardReel
         * @property {number|null} [line] AwardReel line
         * @property {Array.<jlhby.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlhby
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlhby.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel line.
         * @member {number} line
         * @memberof jlhby.AwardReel
         * @instance
         */
        AwardReel.prototype.line = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlhby.IBlock>} block
         * @memberof jlhby.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlhby.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlhby.AwardReel
         * @static
         * @param {jlhby.IAwardReel=} [properties] Properties to set
         * @returns {jlhby.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlhby.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlhby.AwardReel
         * @static
         * @param {jlhby.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.line);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlhby.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlhby.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhby.AwardReel
         * @static
         * @param {jlhby.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlhby.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhby.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhby.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.line = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlhby.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhby.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhby.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlhby.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.line != null && message.hasOwnProperty("line"))
                if (!$util.isInteger(message.line))
                    return "line: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlhby.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhby.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhby.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhby.AwardReel)
                return object;
            let message = new $root.jlhby.AwardReel();
            if (object.line != null)
                message.line = object.line >>> 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlhby.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlhby.AwardReel.block: object expected");
                    message.block[i] = $root.jlhby.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhby.AwardReel
         * @static
         * @param {jlhby.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.line = 0;
                object.win = 0;
            }
            if (message.line != null && message.hasOwnProperty("line"))
                object.line = message.line;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlhby.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlhby.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlhby.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhby.AwardReel";
        };

        return AwardReel;
    })();

    jlhby.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlhby
         * @interface IPlate
         * @property {Array.<jlhby.IColumnData>|null} [column] Plate column
         * @property {Array.<jlhby.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         * @property {number|null} [ballCount] Plate ballCount
         * @property {number|null} [bonusWin] Plate bonusWin
         */

        /**
         * Constructs a new Plate.
         * @memberof jlhby
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlhby.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlhby.IColumnData>} column
         * @memberof jlhby.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jlhby.IAwardReel>} award
         * @memberof jlhby.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlhby.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate ballCount.
         * @member {number} ballCount
         * @memberof jlhby.Plate
         * @instance
         */
        Plate.prototype.ballCount = 0;

        /**
         * Plate bonusWin.
         * @member {number} bonusWin
         * @memberof jlhby.Plate
         * @instance
         */
        Plate.prototype.bonusWin = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlhby.Plate
         * @static
         * @param {jlhby.IPlate=} [properties] Properties to set
         * @returns {jlhby.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlhby.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlhby.Plate
         * @static
         * @param {jlhby.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlhby.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlhby.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.ballCount != null && Object.hasOwnProperty.call(message, "ballCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.ballCount);
            if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlhby.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhby.Plate
         * @static
         * @param {jlhby.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlhby.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhby.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhby.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlhby.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlhby.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.ballCount = reader.uint32();
                        break;
                    }
                case 5: {
                        message.bonusWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhby.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhby.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlhby.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlhby.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlhby.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                if (!$util.isInteger(message.ballCount))
                    return "ballCount: integer expected";
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                if (typeof message.bonusWin !== "number")
                    return "bonusWin: number expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhby.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhby.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhby.Plate)
                return object;
            let message = new $root.jlhby.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlhby.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlhby.Plate.column: object expected");
                    message.column[i] = $root.jlhby.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlhby.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlhby.Plate.award: object expected");
                    message.award[i] = $root.jlhby.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.ballCount != null)
                message.ballCount = object.ballCount >>> 0;
            if (object.bonusWin != null)
                message.bonusWin = Number(object.bonusWin);
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhby.Plate
         * @static
         * @param {jlhby.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.ballCount = 0;
                object.bonusWin = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlhby.ColumnData.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlhby.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                object.ballCount = message.ballCount;
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlhby.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlhby.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhby.Plate";
        };

        return Plate;
    })();

    jlhby.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlhby
         * @interface IAllPlate
         * @property {Array.<jlhby.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlhby
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlhby.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlhby.IPlate>} plate
         * @memberof jlhby.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlhby.AllPlate
         * @static
         * @param {jlhby.IAllPlate=} [properties] Properties to set
         * @returns {jlhby.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlhby.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlhby.AllPlate
         * @static
         * @param {jlhby.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlhby.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlhby.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhby.AllPlate
         * @static
         * @param {jlhby.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlhby.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhby.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhby.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlhby.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhby.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhby.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlhby.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlhby.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhby.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhby.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhby.AllPlate)
                return object;
            let message = new $root.jlhby.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlhby.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlhby.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlhby.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhby.AllPlate
         * @static
         * @param {jlhby.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlhby.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlhby.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlhby.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhby.AllPlate";
        };

        return AllPlate;
    })();

    jlhby.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlhby
         * @interface IColumnData
         * @property {Array.<jlhby.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlhby
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlhby.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jlhby.ICell>} row
         * @memberof jlhby.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlhby.ColumnData
         * @static
         * @param {jlhby.IColumnData=} [properties] Properties to set
         * @returns {jlhby.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlhby.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlhby.ColumnData
         * @static
         * @param {jlhby.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jlhby.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlhby.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhby.ColumnData
         * @static
         * @param {jlhby.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlhby.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhby.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhby.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jlhby.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhby.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhby.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlhby.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jlhby.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhby.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhby.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhby.ColumnData)
                return object;
            let message = new $root.jlhby.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlhby.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jlhby.ColumnData.row: object expected");
                    message.row[i] = $root.jlhby.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhby.ColumnData
         * @static
         * @param {jlhby.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jlhby.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlhby.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlhby.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhby.ColumnData";
        };

        return ColumnData;
    })();

    jlhby.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlhby
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [value] Cell value
         */

        /**
         * Constructs a new Cell.
         * @memberof jlhby
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlhby.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlhby.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell value.
         * @member {number} value
         * @memberof jlhby.Cell
         * @instance
         */
        Cell.prototype.value = 0;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlhby.Cell
         * @static
         * @param {jlhby.ICell=} [properties] Properties to set
         * @returns {jlhby.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlhby.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlhby.Cell
         * @static
         * @param {jlhby.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlhby.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhby.Cell
         * @static
         * @param {jlhby.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlhby.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhby.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhby.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.value = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhby.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhby.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlhby.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhby.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhby.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhby.Cell)
                return object;
            let message = new $root.jlhby.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.value != null)
                message.value = Number(object.value);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhby.Cell
         * @static
         * @param {jlhby.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.value = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlhby.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlhby.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhby.Cell";
        };

        return Cell;
    })();

    return jlhby;
})();

export const jlhc = $root.jlhc = (() => {

    /**
     * Namespace jlhc.
     * @exports jlhc
     * @namespace
     */
    const jlhc = {};

    jlhc.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlhc
         * @interface IGrid
         * @property {number|null} [Column] Grid Column
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlhc
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlhc.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Column.
         * @member {number} Column
         * @memberof jlhc.Grid
         * @instance
         */
        Grid.prototype.Column = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlhc.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlhc.Grid
         * @static
         * @param {jlhc.IGrid=} [properties] Properties to set
         * @returns {jlhc.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlhc.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlhc.Grid
         * @static
         * @param {jlhc.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && Object.hasOwnProperty.call(message, "Column"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Column);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlhc.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.Grid
         * @static
         * @param {jlhc.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Column = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlhc.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column"))
                if (!$util.isInteger(message.Column))
                    return "Column: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.Grid)
                return object;
            let message = new $root.jlhc.Grid();
            if (object.Column != null)
                message.Column = object.Column | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.Grid
         * @static
         * @param {jlhc.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Column = 0;
                object.Row = 0;
            }
            if (message.Column != null && message.hasOwnProperty("Column"))
                object.Column = message.Column;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlhc.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlhc.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.Grid";
        };

        return Grid;
    })();

    jlhc.LineAwardData = (function() {

        /**
         * Properties of a LineAwardData.
         * @memberof jlhc
         * @interface ILineAwardData
         * @property {number|null} [LineIndex] LineAwardData LineIndex
         * @property {number|null} [Symbol] LineAwardData Symbol
         * @property {number|null} [SymbolCount] LineAwardData SymbolCount
         * @property {number|null} [Win] LineAwardData Win
         */

        /**
         * Constructs a new LineAwardData.
         * @memberof jlhc
         * @classdesc Represents a LineAwardData.
         * @implements ILineAwardData
         * @constructor
         * @param {jlhc.ILineAwardData=} [properties] Properties to set
         */
        function LineAwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LineAwardData LineIndex.
         * @member {number} LineIndex
         * @memberof jlhc.LineAwardData
         * @instance
         */
        LineAwardData.prototype.LineIndex = 0;

        /**
         * LineAwardData Symbol.
         * @member {number} Symbol
         * @memberof jlhc.LineAwardData
         * @instance
         */
        LineAwardData.prototype.Symbol = 0;

        /**
         * LineAwardData SymbolCount.
         * @member {number} SymbolCount
         * @memberof jlhc.LineAwardData
         * @instance
         */
        LineAwardData.prototype.SymbolCount = 0;

        /**
         * LineAwardData Win.
         * @member {number} Win
         * @memberof jlhc.LineAwardData
         * @instance
         */
        LineAwardData.prototype.Win = 0;

        /**
         * Creates a new LineAwardData instance using the specified properties.
         * @function create
         * @memberof jlhc.LineAwardData
         * @static
         * @param {jlhc.ILineAwardData=} [properties] Properties to set
         * @returns {jlhc.LineAwardData} LineAwardData instance
         */
        LineAwardData.create = function create(properties) {
            return new LineAwardData(properties);
        };

        /**
         * Encodes the specified LineAwardData message. Does not implicitly {@link jlhc.LineAwardData.verify|verify} messages.
         * @function encode
         * @memberof jlhc.LineAwardData
         * @static
         * @param {jlhc.ILineAwardData} message LineAwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LineAwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LineIndex != null && Object.hasOwnProperty.call(message, "LineIndex"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.LineIndex);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.SymbolCount != null && Object.hasOwnProperty.call(message, "SymbolCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.SymbolCount);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified LineAwardData message, length delimited. Does not implicitly {@link jlhc.LineAwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.LineAwardData
         * @static
         * @param {jlhc.ILineAwardData} message LineAwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LineAwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LineAwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.LineAwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.LineAwardData} LineAwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LineAwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.LineAwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LineIndex = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.SymbolCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LineAwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.LineAwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.LineAwardData} LineAwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LineAwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LineAwardData message.
         * @function verify
         * @memberof jlhc.LineAwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LineAwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LineIndex != null && message.hasOwnProperty("LineIndex"))
                if (!$util.isInteger(message.LineIndex))
                    return "LineIndex: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.SymbolCount != null && message.hasOwnProperty("SymbolCount"))
                if (!$util.isInteger(message.SymbolCount))
                    return "SymbolCount: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates a LineAwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.LineAwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.LineAwardData} LineAwardData
         */
        LineAwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.LineAwardData)
                return object;
            let message = new $root.jlhc.LineAwardData();
            if (object.LineIndex != null)
                message.LineIndex = object.LineIndex | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.SymbolCount != null)
                message.SymbolCount = object.SymbolCount | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from a LineAwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.LineAwardData
         * @static
         * @param {jlhc.LineAwardData} message LineAwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LineAwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.LineIndex = 0;
                object.Symbol = 0;
                object.SymbolCount = 0;
                object.Win = 0;
            }
            if (message.LineIndex != null && message.hasOwnProperty("LineIndex"))
                object.LineIndex = message.LineIndex;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.SymbolCount != null && message.hasOwnProperty("SymbolCount"))
                object.SymbolCount = message.SymbolCount;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this LineAwardData to JSON.
         * @function toJSON
         * @memberof jlhc.LineAwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LineAwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LineAwardData
         * @function getTypeUrl
         * @memberof jlhc.LineAwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LineAwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.LineAwardData";
        };

        return LineAwardData;
    })();

    jlhc.LineAwardInfo = (function() {

        /**
         * Properties of a LineAwardInfo.
         * @memberof jlhc
         * @interface ILineAwardInfo
         * @property {Array.<jlhc.ILineAwardData>|null} [LineAwardDataVec] LineAwardInfo LineAwardDataVec
         * @property {number|null} [WinMoney] LineAwardInfo WinMoney
         */

        /**
         * Constructs a new LineAwardInfo.
         * @memberof jlhc
         * @classdesc Represents a LineAwardInfo.
         * @implements ILineAwardInfo
         * @constructor
         * @param {jlhc.ILineAwardInfo=} [properties] Properties to set
         */
        function LineAwardInfo(properties) {
            this.LineAwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LineAwardInfo LineAwardDataVec.
         * @member {Array.<jlhc.ILineAwardData>} LineAwardDataVec
         * @memberof jlhc.LineAwardInfo
         * @instance
         */
        LineAwardInfo.prototype.LineAwardDataVec = $util.emptyArray;

        /**
         * LineAwardInfo WinMoney.
         * @member {number} WinMoney
         * @memberof jlhc.LineAwardInfo
         * @instance
         */
        LineAwardInfo.prototype.WinMoney = 0;

        /**
         * Creates a new LineAwardInfo instance using the specified properties.
         * @function create
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {jlhc.ILineAwardInfo=} [properties] Properties to set
         * @returns {jlhc.LineAwardInfo} LineAwardInfo instance
         */
        LineAwardInfo.create = function create(properties) {
            return new LineAwardInfo(properties);
        };

        /**
         * Encodes the specified LineAwardInfo message. Does not implicitly {@link jlhc.LineAwardInfo.verify|verify} messages.
         * @function encode
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {jlhc.ILineAwardInfo} message LineAwardInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LineAwardInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LineAwardDataVec != null && message.LineAwardDataVec.length)
                for (let i = 0; i < message.LineAwardDataVec.length; ++i)
                    $root.jlhc.LineAwardData.encode(message.LineAwardDataVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.WinMoney != null && Object.hasOwnProperty.call(message, "WinMoney"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.WinMoney);
            return writer;
        };

        /**
         * Encodes the specified LineAwardInfo message, length delimited. Does not implicitly {@link jlhc.LineAwardInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {jlhc.ILineAwardInfo} message LineAwardInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LineAwardInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LineAwardInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.LineAwardInfo} LineAwardInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LineAwardInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.LineAwardInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.LineAwardDataVec && message.LineAwardDataVec.length))
                            message.LineAwardDataVec = [];
                        message.LineAwardDataVec.push($root.jlhc.LineAwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.WinMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LineAwardInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.LineAwardInfo} LineAwardInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LineAwardInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LineAwardInfo message.
         * @function verify
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LineAwardInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LineAwardDataVec != null && message.hasOwnProperty("LineAwardDataVec")) {
                if (!Array.isArray(message.LineAwardDataVec))
                    return "LineAwardDataVec: array expected";
                for (let i = 0; i < message.LineAwardDataVec.length; ++i) {
                    let error = $root.jlhc.LineAwardData.verify(message.LineAwardDataVec[i]);
                    if (error)
                        return "LineAwardDataVec." + error;
                }
            }
            if (message.WinMoney != null && message.hasOwnProperty("WinMoney"))
                if (typeof message.WinMoney !== "number")
                    return "WinMoney: number expected";
            return null;
        };

        /**
         * Creates a LineAwardInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.LineAwardInfo} LineAwardInfo
         */
        LineAwardInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.LineAwardInfo)
                return object;
            let message = new $root.jlhc.LineAwardInfo();
            if (object.LineAwardDataVec) {
                if (!Array.isArray(object.LineAwardDataVec))
                    throw TypeError(".jlhc.LineAwardInfo.LineAwardDataVec: array expected");
                message.LineAwardDataVec = [];
                for (let i = 0; i < object.LineAwardDataVec.length; ++i) {
                    if (typeof object.LineAwardDataVec[i] !== "object")
                        throw TypeError(".jlhc.LineAwardInfo.LineAwardDataVec: object expected");
                    message.LineAwardDataVec[i] = $root.jlhc.LineAwardData.fromObject(object.LineAwardDataVec[i]);
                }
            }
            if (object.WinMoney != null)
                message.WinMoney = Number(object.WinMoney);
            return message;
        };

        /**
         * Creates a plain object from a LineAwardInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {jlhc.LineAwardInfo} message LineAwardInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LineAwardInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.LineAwardDataVec = [];
            if (options.defaults)
                object.WinMoney = 0;
            if (message.LineAwardDataVec && message.LineAwardDataVec.length) {
                object.LineAwardDataVec = [];
                for (let j = 0; j < message.LineAwardDataVec.length; ++j)
                    object.LineAwardDataVec[j] = $root.jlhc.LineAwardData.toObject(message.LineAwardDataVec[j], options);
            }
            if (message.WinMoney != null && message.hasOwnProperty("WinMoney"))
                object.WinMoney = options.json && !isFinite(message.WinMoney) ? String(message.WinMoney) : message.WinMoney;
            return object;
        };

        /**
         * Converts this LineAwardInfo to JSON.
         * @function toJSON
         * @memberof jlhc.LineAwardInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LineAwardInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LineAwardInfo
         * @function getTypeUrl
         * @memberof jlhc.LineAwardInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LineAwardInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.LineAwardInfo";
        };

        return LineAwardInfo;
    })();

    jlhc.ScatterAwardInfo = (function() {

        /**
         * Properties of a ScatterAwardInfo.
         * @memberof jlhc
         * @interface IScatterAwardInfo
         * @property {Array.<jlhc.IGrid>|null} [GridVec] ScatterAwardInfo GridVec
         * @property {number|null} [Money] ScatterAwardInfo Money
         * @property {number|null} [FreeGameRound] ScatterAwardInfo FreeGameRound
         * @property {number|null} [ColumnCount] ScatterAwardInfo ColumnCount
         */

        /**
         * Constructs a new ScatterAwardInfo.
         * @memberof jlhc
         * @classdesc Represents a ScatterAwardInfo.
         * @implements IScatterAwardInfo
         * @constructor
         * @param {jlhc.IScatterAwardInfo=} [properties] Properties to set
         */
        function ScatterAwardInfo(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ScatterAwardInfo GridVec.
         * @member {Array.<jlhc.IGrid>} GridVec
         * @memberof jlhc.ScatterAwardInfo
         * @instance
         */
        ScatterAwardInfo.prototype.GridVec = $util.emptyArray;

        /**
         * ScatterAwardInfo Money.
         * @member {number} Money
         * @memberof jlhc.ScatterAwardInfo
         * @instance
         */
        ScatterAwardInfo.prototype.Money = 0;

        /**
         * ScatterAwardInfo FreeGameRound.
         * @member {number} FreeGameRound
         * @memberof jlhc.ScatterAwardInfo
         * @instance
         */
        ScatterAwardInfo.prototype.FreeGameRound = 0;

        /**
         * ScatterAwardInfo ColumnCount.
         * @member {number} ColumnCount
         * @memberof jlhc.ScatterAwardInfo
         * @instance
         */
        ScatterAwardInfo.prototype.ColumnCount = 0;

        /**
         * Creates a new ScatterAwardInfo instance using the specified properties.
         * @function create
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {jlhc.IScatterAwardInfo=} [properties] Properties to set
         * @returns {jlhc.ScatterAwardInfo} ScatterAwardInfo instance
         */
        ScatterAwardInfo.create = function create(properties) {
            return new ScatterAwardInfo(properties);
        };

        /**
         * Encodes the specified ScatterAwardInfo message. Does not implicitly {@link jlhc.ScatterAwardInfo.verify|verify} messages.
         * @function encode
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {jlhc.IScatterAwardInfo} message ScatterAwardInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScatterAwardInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlhc.Grid.encode(message.GridVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Money != null && Object.hasOwnProperty.call(message, "Money"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Money);
            if (message.FreeGameRound != null && Object.hasOwnProperty.call(message, "FreeGameRound"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.FreeGameRound);
            if (message.ColumnCount != null && Object.hasOwnProperty.call(message, "ColumnCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ColumnCount);
            return writer;
        };

        /**
         * Encodes the specified ScatterAwardInfo message, length delimited. Does not implicitly {@link jlhc.ScatterAwardInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {jlhc.IScatterAwardInfo} message ScatterAwardInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScatterAwardInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ScatterAwardInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.ScatterAwardInfo} ScatterAwardInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScatterAwardInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.ScatterAwardInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlhc.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Money = reader.double();
                        break;
                    }
                case 3: {
                        message.FreeGameRound = reader.int32();
                        break;
                    }
                case 4: {
                        message.ColumnCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ScatterAwardInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.ScatterAwardInfo} ScatterAwardInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScatterAwardInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScatterAwardInfo message.
         * @function verify
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScatterAwardInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlhc.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            if (message.Money != null && message.hasOwnProperty("Money"))
                if (typeof message.Money !== "number")
                    return "Money: number expected";
            if (message.FreeGameRound != null && message.hasOwnProperty("FreeGameRound"))
                if (!$util.isInteger(message.FreeGameRound))
                    return "FreeGameRound: integer expected";
            if (message.ColumnCount != null && message.hasOwnProperty("ColumnCount"))
                if (!$util.isInteger(message.ColumnCount))
                    return "ColumnCount: integer expected";
            return null;
        };

        /**
         * Creates a ScatterAwardInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.ScatterAwardInfo} ScatterAwardInfo
         */
        ScatterAwardInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.ScatterAwardInfo)
                return object;
            let message = new $root.jlhc.ScatterAwardInfo();
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlhc.ScatterAwardInfo.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlhc.ScatterAwardInfo.GridVec: object expected");
                    message.GridVec[i] = $root.jlhc.Grid.fromObject(object.GridVec[i]);
                }
            }
            if (object.Money != null)
                message.Money = Number(object.Money);
            if (object.FreeGameRound != null)
                message.FreeGameRound = object.FreeGameRound | 0;
            if (object.ColumnCount != null)
                message.ColumnCount = object.ColumnCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a ScatterAwardInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {jlhc.ScatterAwardInfo} message ScatterAwardInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScatterAwardInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Money = 0;
                object.FreeGameRound = 0;
                object.ColumnCount = 0;
            }
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlhc.Grid.toObject(message.GridVec[j], options);
            }
            if (message.Money != null && message.hasOwnProperty("Money"))
                object.Money = options.json && !isFinite(message.Money) ? String(message.Money) : message.Money;
            if (message.FreeGameRound != null && message.hasOwnProperty("FreeGameRound"))
                object.FreeGameRound = message.FreeGameRound;
            if (message.ColumnCount != null && message.hasOwnProperty("ColumnCount"))
                object.ColumnCount = message.ColumnCount;
            return object;
        };

        /**
         * Converts this ScatterAwardInfo to JSON.
         * @function toJSON
         * @memberof jlhc.ScatterAwardInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScatterAwardInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ScatterAwardInfo
         * @function getTypeUrl
         * @memberof jlhc.ScatterAwardInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ScatterAwardInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.ScatterAwardInfo";
        };

        return ScatterAwardInfo;
    })();

    jlhc.PlateData = (function() {

        /**
         * Properties of a PlateData.
         * @memberof jlhc
         * @interface IPlateData
         * @property {Array.<number>|null} [PlateVec] PlateData PlateVec
         * @property {jlhc.ILineAwardInfo|null} [LineAwardInfo] PlateData LineAwardInfo
         * @property {jlhc.IScatterAwardInfo|null} [ScatterAwardInfo] PlateData ScatterAwardInfo
         */

        /**
         * Constructs a new PlateData.
         * @memberof jlhc
         * @classdesc Represents a PlateData.
         * @implements IPlateData
         * @constructor
         * @param {jlhc.IPlateData=} [properties] Properties to set
         */
        function PlateData(properties) {
            this.PlateVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlateData PlateVec.
         * @member {Array.<number>} PlateVec
         * @memberof jlhc.PlateData
         * @instance
         */
        PlateData.prototype.PlateVec = $util.emptyArray;

        /**
         * PlateData LineAwardInfo.
         * @member {jlhc.ILineAwardInfo|null|undefined} LineAwardInfo
         * @memberof jlhc.PlateData
         * @instance
         */
        PlateData.prototype.LineAwardInfo = null;

        /**
         * PlateData ScatterAwardInfo.
         * @member {jlhc.IScatterAwardInfo|null|undefined} ScatterAwardInfo
         * @memberof jlhc.PlateData
         * @instance
         */
        PlateData.prototype.ScatterAwardInfo = null;

        /**
         * Creates a new PlateData instance using the specified properties.
         * @function create
         * @memberof jlhc.PlateData
         * @static
         * @param {jlhc.IPlateData=} [properties] Properties to set
         * @returns {jlhc.PlateData} PlateData instance
         */
        PlateData.create = function create(properties) {
            return new PlateData(properties);
        };

        /**
         * Encodes the specified PlateData message. Does not implicitly {@link jlhc.PlateData.verify|verify} messages.
         * @function encode
         * @memberof jlhc.PlateData
         * @static
         * @param {jlhc.IPlateData} message PlateData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateVec != null && message.PlateVec.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.PlateVec.length; ++i)
                    writer.int32(message.PlateVec[i]);
                writer.ldelim();
            }
            if (message.LineAwardInfo != null && Object.hasOwnProperty.call(message, "LineAwardInfo"))
                $root.jlhc.LineAwardInfo.encode(message.LineAwardInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ScatterAwardInfo != null && Object.hasOwnProperty.call(message, "ScatterAwardInfo"))
                $root.jlhc.ScatterAwardInfo.encode(message.ScatterAwardInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PlateData message, length delimited. Does not implicitly {@link jlhc.PlateData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.PlateData
         * @static
         * @param {jlhc.IPlateData} message PlateData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlateData message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.PlateData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.PlateData} PlateData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.PlateData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateVec && message.PlateVec.length))
                            message.PlateVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateVec.push(reader.int32());
                        } else
                            message.PlateVec.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.LineAwardInfo = $root.jlhc.LineAwardInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.ScatterAwardInfo = $root.jlhc.ScatterAwardInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlateData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.PlateData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.PlateData} PlateData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlateData message.
         * @function verify
         * @memberof jlhc.PlateData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlateData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateVec != null && message.hasOwnProperty("PlateVec")) {
                if (!Array.isArray(message.PlateVec))
                    return "PlateVec: array expected";
                for (let i = 0; i < message.PlateVec.length; ++i)
                    if (!$util.isInteger(message.PlateVec[i]))
                        return "PlateVec: integer[] expected";
            }
            if (message.LineAwardInfo != null && message.hasOwnProperty("LineAwardInfo")) {
                let error = $root.jlhc.LineAwardInfo.verify(message.LineAwardInfo);
                if (error)
                    return "LineAwardInfo." + error;
            }
            if (message.ScatterAwardInfo != null && message.hasOwnProperty("ScatterAwardInfo")) {
                let error = $root.jlhc.ScatterAwardInfo.verify(message.ScatterAwardInfo);
                if (error)
                    return "ScatterAwardInfo." + error;
            }
            return null;
        };

        /**
         * Creates a PlateData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.PlateData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.PlateData} PlateData
         */
        PlateData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.PlateData)
                return object;
            let message = new $root.jlhc.PlateData();
            if (object.PlateVec) {
                if (!Array.isArray(object.PlateVec))
                    throw TypeError(".jlhc.PlateData.PlateVec: array expected");
                message.PlateVec = [];
                for (let i = 0; i < object.PlateVec.length; ++i)
                    message.PlateVec[i] = object.PlateVec[i] | 0;
            }
            if (object.LineAwardInfo != null) {
                if (typeof object.LineAwardInfo !== "object")
                    throw TypeError(".jlhc.PlateData.LineAwardInfo: object expected");
                message.LineAwardInfo = $root.jlhc.LineAwardInfo.fromObject(object.LineAwardInfo);
            }
            if (object.ScatterAwardInfo != null) {
                if (typeof object.ScatterAwardInfo !== "object")
                    throw TypeError(".jlhc.PlateData.ScatterAwardInfo: object expected");
                message.ScatterAwardInfo = $root.jlhc.ScatterAwardInfo.fromObject(object.ScatterAwardInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from a PlateData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.PlateData
         * @static
         * @param {jlhc.PlateData} message PlateData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlateData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateVec = [];
            if (options.defaults) {
                object.LineAwardInfo = null;
                object.ScatterAwardInfo = null;
            }
            if (message.PlateVec && message.PlateVec.length) {
                object.PlateVec = [];
                for (let j = 0; j < message.PlateVec.length; ++j)
                    object.PlateVec[j] = message.PlateVec[j];
            }
            if (message.LineAwardInfo != null && message.hasOwnProperty("LineAwardInfo"))
                object.LineAwardInfo = $root.jlhc.LineAwardInfo.toObject(message.LineAwardInfo, options);
            if (message.ScatterAwardInfo != null && message.hasOwnProperty("ScatterAwardInfo"))
                object.ScatterAwardInfo = $root.jlhc.ScatterAwardInfo.toObject(message.ScatterAwardInfo, options);
            return object;
        };

        /**
         * Converts this PlateData to JSON.
         * @function toJSON
         * @memberof jlhc.PlateData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlateData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlateData
         * @function getTypeUrl
         * @memberof jlhc.PlateData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlateData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.PlateData";
        };

        return PlateData;
    })();

    jlhc.FreePlateData = (function() {

        /**
         * Properties of a FreePlateData.
         * @memberof jlhc
         * @interface IFreePlateData
         * @property {Array.<number>|null} [PlateVec] FreePlateData PlateVec
         * @property {jlhc.ILineAwardInfo|null} [LineAwardInfo] FreePlateData LineAwardInfo
         * @property {jlhc.IScatterAwardInfo|null} [ScatterAwardInfo] FreePlateData ScatterAwardInfo
         * @property {number|null} [Position] FreePlateData Position
         * @property {number|null} [WildLockCount] FreePlateData WildLockCount
         */

        /**
         * Constructs a new FreePlateData.
         * @memberof jlhc
         * @classdesc Represents a FreePlateData.
         * @implements IFreePlateData
         * @constructor
         * @param {jlhc.IFreePlateData=} [properties] Properties to set
         */
        function FreePlateData(properties) {
            this.PlateVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreePlateData PlateVec.
         * @member {Array.<number>} PlateVec
         * @memberof jlhc.FreePlateData
         * @instance
         */
        FreePlateData.prototype.PlateVec = $util.emptyArray;

        /**
         * FreePlateData LineAwardInfo.
         * @member {jlhc.ILineAwardInfo|null|undefined} LineAwardInfo
         * @memberof jlhc.FreePlateData
         * @instance
         */
        FreePlateData.prototype.LineAwardInfo = null;

        /**
         * FreePlateData ScatterAwardInfo.
         * @member {jlhc.IScatterAwardInfo|null|undefined} ScatterAwardInfo
         * @memberof jlhc.FreePlateData
         * @instance
         */
        FreePlateData.prototype.ScatterAwardInfo = null;

        /**
         * FreePlateData Position.
         * @member {number} Position
         * @memberof jlhc.FreePlateData
         * @instance
         */
        FreePlateData.prototype.Position = 0;

        /**
         * FreePlateData WildLockCount.
         * @member {number} WildLockCount
         * @memberof jlhc.FreePlateData
         * @instance
         */
        FreePlateData.prototype.WildLockCount = 0;

        /**
         * Creates a new FreePlateData instance using the specified properties.
         * @function create
         * @memberof jlhc.FreePlateData
         * @static
         * @param {jlhc.IFreePlateData=} [properties] Properties to set
         * @returns {jlhc.FreePlateData} FreePlateData instance
         */
        FreePlateData.create = function create(properties) {
            return new FreePlateData(properties);
        };

        /**
         * Encodes the specified FreePlateData message. Does not implicitly {@link jlhc.FreePlateData.verify|verify} messages.
         * @function encode
         * @memberof jlhc.FreePlateData
         * @static
         * @param {jlhc.IFreePlateData} message FreePlateData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreePlateData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateVec != null && message.PlateVec.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.PlateVec.length; ++i)
                    writer.int32(message.PlateVec[i]);
                writer.ldelim();
            }
            if (message.LineAwardInfo != null && Object.hasOwnProperty.call(message, "LineAwardInfo"))
                $root.jlhc.LineAwardInfo.encode(message.LineAwardInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ScatterAwardInfo != null && Object.hasOwnProperty.call(message, "ScatterAwardInfo"))
                $root.jlhc.ScatterAwardInfo.encode(message.ScatterAwardInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.Position != null && Object.hasOwnProperty.call(message, "Position"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Position);
            if (message.WildLockCount != null && Object.hasOwnProperty.call(message, "WildLockCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.WildLockCount);
            return writer;
        };

        /**
         * Encodes the specified FreePlateData message, length delimited. Does not implicitly {@link jlhc.FreePlateData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.FreePlateData
         * @static
         * @param {jlhc.IFreePlateData} message FreePlateData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreePlateData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreePlateData message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.FreePlateData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.FreePlateData} FreePlateData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreePlateData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.FreePlateData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateVec && message.PlateVec.length))
                            message.PlateVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateVec.push(reader.int32());
                        } else
                            message.PlateVec.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.LineAwardInfo = $root.jlhc.LineAwardInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.ScatterAwardInfo = $root.jlhc.ScatterAwardInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.Position = reader.int32();
                        break;
                    }
                case 5: {
                        message.WildLockCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreePlateData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.FreePlateData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.FreePlateData} FreePlateData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreePlateData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreePlateData message.
         * @function verify
         * @memberof jlhc.FreePlateData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreePlateData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateVec != null && message.hasOwnProperty("PlateVec")) {
                if (!Array.isArray(message.PlateVec))
                    return "PlateVec: array expected";
                for (let i = 0; i < message.PlateVec.length; ++i)
                    if (!$util.isInteger(message.PlateVec[i]))
                        return "PlateVec: integer[] expected";
            }
            if (message.LineAwardInfo != null && message.hasOwnProperty("LineAwardInfo")) {
                let error = $root.jlhc.LineAwardInfo.verify(message.LineAwardInfo);
                if (error)
                    return "LineAwardInfo." + error;
            }
            if (message.ScatterAwardInfo != null && message.hasOwnProperty("ScatterAwardInfo")) {
                let error = $root.jlhc.ScatterAwardInfo.verify(message.ScatterAwardInfo);
                if (error)
                    return "ScatterAwardInfo." + error;
            }
            if (message.Position != null && message.hasOwnProperty("Position"))
                if (!$util.isInteger(message.Position))
                    return "Position: integer expected";
            if (message.WildLockCount != null && message.hasOwnProperty("WildLockCount"))
                if (!$util.isInteger(message.WildLockCount))
                    return "WildLockCount: integer expected";
            return null;
        };

        /**
         * Creates a FreePlateData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.FreePlateData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.FreePlateData} FreePlateData
         */
        FreePlateData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.FreePlateData)
                return object;
            let message = new $root.jlhc.FreePlateData();
            if (object.PlateVec) {
                if (!Array.isArray(object.PlateVec))
                    throw TypeError(".jlhc.FreePlateData.PlateVec: array expected");
                message.PlateVec = [];
                for (let i = 0; i < object.PlateVec.length; ++i)
                    message.PlateVec[i] = object.PlateVec[i] | 0;
            }
            if (object.LineAwardInfo != null) {
                if (typeof object.LineAwardInfo !== "object")
                    throw TypeError(".jlhc.FreePlateData.LineAwardInfo: object expected");
                message.LineAwardInfo = $root.jlhc.LineAwardInfo.fromObject(object.LineAwardInfo);
            }
            if (object.ScatterAwardInfo != null) {
                if (typeof object.ScatterAwardInfo !== "object")
                    throw TypeError(".jlhc.FreePlateData.ScatterAwardInfo: object expected");
                message.ScatterAwardInfo = $root.jlhc.ScatterAwardInfo.fromObject(object.ScatterAwardInfo);
            }
            if (object.Position != null)
                message.Position = object.Position | 0;
            if (object.WildLockCount != null)
                message.WildLockCount = object.WildLockCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a FreePlateData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.FreePlateData
         * @static
         * @param {jlhc.FreePlateData} message FreePlateData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreePlateData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateVec = [];
            if (options.defaults) {
                object.LineAwardInfo = null;
                object.ScatterAwardInfo = null;
                object.Position = 0;
                object.WildLockCount = 0;
            }
            if (message.PlateVec && message.PlateVec.length) {
                object.PlateVec = [];
                for (let j = 0; j < message.PlateVec.length; ++j)
                    object.PlateVec[j] = message.PlateVec[j];
            }
            if (message.LineAwardInfo != null && message.hasOwnProperty("LineAwardInfo"))
                object.LineAwardInfo = $root.jlhc.LineAwardInfo.toObject(message.LineAwardInfo, options);
            if (message.ScatterAwardInfo != null && message.hasOwnProperty("ScatterAwardInfo"))
                object.ScatterAwardInfo = $root.jlhc.ScatterAwardInfo.toObject(message.ScatterAwardInfo, options);
            if (message.Position != null && message.hasOwnProperty("Position"))
                object.Position = message.Position;
            if (message.WildLockCount != null && message.hasOwnProperty("WildLockCount"))
                object.WildLockCount = message.WildLockCount;
            return object;
        };

        /**
         * Converts this FreePlateData to JSON.
         * @function toJSON
         * @memberof jlhc.FreePlateData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreePlateData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreePlateData
         * @function getTypeUrl
         * @memberof jlhc.FreePlateData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreePlateData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.FreePlateData";
        };

        return FreePlateData;
    })();

    jlhc.AllFreePlateDataPerRound = (function() {

        /**
         * Properties of an AllFreePlateDataPerRound.
         * @memberof jlhc
         * @interface IAllFreePlateDataPerRound
         * @property {Array.<jlhc.IFreePlateData>|null} [FreePlateDataVec] AllFreePlateDataPerRound FreePlateDataVec
         * @property {number|null} [ChilliCount] AllFreePlateDataPerRound ChilliCount
         * @property {number|null} [ThisRoundChilliCount] AllFreePlateDataPerRound ThisRoundChilliCount
         * @property {number|null} [RemainFreeGameRound] AllFreePlateDataPerRound RemainFreeGameRound
         * @property {string|null} [LogIndex] AllFreePlateDataPerRound LogIndex
         */

        /**
         * Constructs a new AllFreePlateDataPerRound.
         * @memberof jlhc
         * @classdesc Represents an AllFreePlateDataPerRound.
         * @implements IAllFreePlateDataPerRound
         * @constructor
         * @param {jlhc.IAllFreePlateDataPerRound=} [properties] Properties to set
         */
        function AllFreePlateDataPerRound(properties) {
            this.FreePlateDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllFreePlateDataPerRound FreePlateDataVec.
         * @member {Array.<jlhc.IFreePlateData>} FreePlateDataVec
         * @memberof jlhc.AllFreePlateDataPerRound
         * @instance
         */
        AllFreePlateDataPerRound.prototype.FreePlateDataVec = $util.emptyArray;

        /**
         * AllFreePlateDataPerRound ChilliCount.
         * @member {number} ChilliCount
         * @memberof jlhc.AllFreePlateDataPerRound
         * @instance
         */
        AllFreePlateDataPerRound.prototype.ChilliCount = 0;

        /**
         * AllFreePlateDataPerRound ThisRoundChilliCount.
         * @member {number} ThisRoundChilliCount
         * @memberof jlhc.AllFreePlateDataPerRound
         * @instance
         */
        AllFreePlateDataPerRound.prototype.ThisRoundChilliCount = 0;

        /**
         * AllFreePlateDataPerRound RemainFreeGameRound.
         * @member {number} RemainFreeGameRound
         * @memberof jlhc.AllFreePlateDataPerRound
         * @instance
         */
        AllFreePlateDataPerRound.prototype.RemainFreeGameRound = 0;

        /**
         * AllFreePlateDataPerRound LogIndex.
         * @member {string} LogIndex
         * @memberof jlhc.AllFreePlateDataPerRound
         * @instance
         */
        AllFreePlateDataPerRound.prototype.LogIndex = "";

        /**
         * Creates a new AllFreePlateDataPerRound instance using the specified properties.
         * @function create
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {jlhc.IAllFreePlateDataPerRound=} [properties] Properties to set
         * @returns {jlhc.AllFreePlateDataPerRound} AllFreePlateDataPerRound instance
         */
        AllFreePlateDataPerRound.create = function create(properties) {
            return new AllFreePlateDataPerRound(properties);
        };

        /**
         * Encodes the specified AllFreePlateDataPerRound message. Does not implicitly {@link jlhc.AllFreePlateDataPerRound.verify|verify} messages.
         * @function encode
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {jlhc.IAllFreePlateDataPerRound} message AllFreePlateDataPerRound message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllFreePlateDataPerRound.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreePlateDataVec != null && message.FreePlateDataVec.length)
                for (let i = 0; i < message.FreePlateDataVec.length; ++i)
                    $root.jlhc.FreePlateData.encode(message.FreePlateDataVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ChilliCount != null && Object.hasOwnProperty.call(message, "ChilliCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ChilliCount);
            if (message.ThisRoundChilliCount != null && Object.hasOwnProperty.call(message, "ThisRoundChilliCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ThisRoundChilliCount);
            if (message.RemainFreeGameRound != null && Object.hasOwnProperty.call(message, "RemainFreeGameRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.RemainFreeGameRound);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.LogIndex);
            return writer;
        };

        /**
         * Encodes the specified AllFreePlateDataPerRound message, length delimited. Does not implicitly {@link jlhc.AllFreePlateDataPerRound.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {jlhc.IAllFreePlateDataPerRound} message AllFreePlateDataPerRound message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllFreePlateDataPerRound.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllFreePlateDataPerRound message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.AllFreePlateDataPerRound} AllFreePlateDataPerRound
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllFreePlateDataPerRound.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.AllFreePlateDataPerRound();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreePlateDataVec && message.FreePlateDataVec.length))
                            message.FreePlateDataVec = [];
                        message.FreePlateDataVec.push($root.jlhc.FreePlateData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ChilliCount = reader.int32();
                        break;
                    }
                case 3: {
                        message.ThisRoundChilliCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.RemainFreeGameRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.LogIndex = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllFreePlateDataPerRound message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.AllFreePlateDataPerRound} AllFreePlateDataPerRound
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllFreePlateDataPerRound.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllFreePlateDataPerRound message.
         * @function verify
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllFreePlateDataPerRound.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreePlateDataVec != null && message.hasOwnProperty("FreePlateDataVec")) {
                if (!Array.isArray(message.FreePlateDataVec))
                    return "FreePlateDataVec: array expected";
                for (let i = 0; i < message.FreePlateDataVec.length; ++i) {
                    let error = $root.jlhc.FreePlateData.verify(message.FreePlateDataVec[i]);
                    if (error)
                        return "FreePlateDataVec." + error;
                }
            }
            if (message.ChilliCount != null && message.hasOwnProperty("ChilliCount"))
                if (!$util.isInteger(message.ChilliCount))
                    return "ChilliCount: integer expected";
            if (message.ThisRoundChilliCount != null && message.hasOwnProperty("ThisRoundChilliCount"))
                if (!$util.isInteger(message.ThisRoundChilliCount))
                    return "ThisRoundChilliCount: integer expected";
            if (message.RemainFreeGameRound != null && message.hasOwnProperty("RemainFreeGameRound"))
                if (!$util.isInteger(message.RemainFreeGameRound))
                    return "RemainFreeGameRound: integer expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isString(message.LogIndex))
                    return "LogIndex: string expected";
            return null;
        };

        /**
         * Creates an AllFreePlateDataPerRound message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.AllFreePlateDataPerRound} AllFreePlateDataPerRound
         */
        AllFreePlateDataPerRound.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.AllFreePlateDataPerRound)
                return object;
            let message = new $root.jlhc.AllFreePlateDataPerRound();
            if (object.FreePlateDataVec) {
                if (!Array.isArray(object.FreePlateDataVec))
                    throw TypeError(".jlhc.AllFreePlateDataPerRound.FreePlateDataVec: array expected");
                message.FreePlateDataVec = [];
                for (let i = 0; i < object.FreePlateDataVec.length; ++i) {
                    if (typeof object.FreePlateDataVec[i] !== "object")
                        throw TypeError(".jlhc.AllFreePlateDataPerRound.FreePlateDataVec: object expected");
                    message.FreePlateDataVec[i] = $root.jlhc.FreePlateData.fromObject(object.FreePlateDataVec[i]);
                }
            }
            if (object.ChilliCount != null)
                message.ChilliCount = object.ChilliCount | 0;
            if (object.ThisRoundChilliCount != null)
                message.ThisRoundChilliCount = object.ThisRoundChilliCount | 0;
            if (object.RemainFreeGameRound != null)
                message.RemainFreeGameRound = object.RemainFreeGameRound | 0;
            if (object.LogIndex != null)
                message.LogIndex = String(object.LogIndex);
            return message;
        };

        /**
         * Creates a plain object from an AllFreePlateDataPerRound message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {jlhc.AllFreePlateDataPerRound} message AllFreePlateDataPerRound
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllFreePlateDataPerRound.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreePlateDataVec = [];
            if (options.defaults) {
                object.ChilliCount = 0;
                object.ThisRoundChilliCount = 0;
                object.RemainFreeGameRound = 0;
                object.LogIndex = "";
            }
            if (message.FreePlateDataVec && message.FreePlateDataVec.length) {
                object.FreePlateDataVec = [];
                for (let j = 0; j < message.FreePlateDataVec.length; ++j)
                    object.FreePlateDataVec[j] = $root.jlhc.FreePlateData.toObject(message.FreePlateDataVec[j], options);
            }
            if (message.ChilliCount != null && message.hasOwnProperty("ChilliCount"))
                object.ChilliCount = message.ChilliCount;
            if (message.ThisRoundChilliCount != null && message.hasOwnProperty("ThisRoundChilliCount"))
                object.ThisRoundChilliCount = message.ThisRoundChilliCount;
            if (message.RemainFreeGameRound != null && message.hasOwnProperty("RemainFreeGameRound"))
                object.RemainFreeGameRound = message.RemainFreeGameRound;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                object.LogIndex = message.LogIndex;
            return object;
        };

        /**
         * Converts this AllFreePlateDataPerRound to JSON.
         * @function toJSON
         * @memberof jlhc.AllFreePlateDataPerRound
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllFreePlateDataPerRound.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllFreePlateDataPerRound
         * @function getTypeUrl
         * @memberof jlhc.AllFreePlateDataPerRound
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllFreePlateDataPerRound.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.AllFreePlateDataPerRound";
        };

        return AllFreePlateDataPerRound;
    })();

    jlhc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlhc
         * @interface ISpinAck
         * @property {jlhc.IPlateData|null} [PlateData] SpinAck PlateData
         * @property {Array.<jlhc.IAllFreePlateDataPerRound>|null} [AllFreePlateDataPerRoundVec] SpinAck AllFreePlateDataPerRoundVec
         * @property {number|null} [FreeGameWin] SpinAck FreeGameWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlhc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlhc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.AllFreePlateDataPerRoundVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateData.
         * @member {jlhc.IPlateData|null|undefined} PlateData
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateData = null;

        /**
         * SpinAck AllFreePlateDataPerRoundVec.
         * @member {Array.<jlhc.IAllFreePlateDataPerRound>} AllFreePlateDataPerRoundVec
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.AllFreePlateDataPerRoundVec = $util.emptyArray;

        /**
         * SpinAck FreeGameWin.
         * @member {number} FreeGameWin
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeGameWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlhc.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlhc.SpinAck
         * @static
         * @param {jlhc.ISpinAck=} [properties] Properties to set
         * @returns {jlhc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlhc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlhc.SpinAck
         * @static
         * @param {jlhc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateData != null && Object.hasOwnProperty.call(message, "PlateData"))
                $root.jlhc.PlateData.encode(message.PlateData, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AllFreePlateDataPerRoundVec != null && message.AllFreePlateDataPerRoundVec.length)
                for (let i = 0; i < message.AllFreePlateDataPerRoundVec.length; ++i)
                    $root.jlhc.AllFreePlateDataPerRound.encode(message.AllFreePlateDataPerRoundVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreeGameWin != null && Object.hasOwnProperty.call(message, "FreeGameWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeGameWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlhc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.SpinAck
         * @static
         * @param {jlhc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlateData = $root.jlhc.PlateData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        if (!(message.AllFreePlateDataPerRoundVec && message.AllFreePlateDataPerRoundVec.length))
                            message.AllFreePlateDataPerRoundVec = [];
                        message.AllFreePlateDataPerRoundVec.push($root.jlhc.AllFreePlateDataPerRound.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.FreeGameWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                case 10: {
                        message.LID = reader.int32();
                        break;
                    }
                case 11: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlhc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateData != null && message.hasOwnProperty("PlateData")) {
                let error = $root.jlhc.PlateData.verify(message.PlateData);
                if (error)
                    return "PlateData." + error;
            }
            if (message.AllFreePlateDataPerRoundVec != null && message.hasOwnProperty("AllFreePlateDataPerRoundVec")) {
                if (!Array.isArray(message.AllFreePlateDataPerRoundVec))
                    return "AllFreePlateDataPerRoundVec: array expected";
                for (let i = 0; i < message.AllFreePlateDataPerRoundVec.length; ++i) {
                    let error = $root.jlhc.AllFreePlateDataPerRound.verify(message.AllFreePlateDataPerRoundVec[i]);
                    if (error)
                        return "AllFreePlateDataPerRoundVec." + error;
                }
            }
            if (message.FreeGameWin != null && message.hasOwnProperty("FreeGameWin"))
                if (typeof message.FreeGameWin !== "number")
                    return "FreeGameWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.SpinAck)
                return object;
            let message = new $root.jlhc.SpinAck();
            if (object.PlateData != null) {
                if (typeof object.PlateData !== "object")
                    throw TypeError(".jlhc.SpinAck.PlateData: object expected");
                message.PlateData = $root.jlhc.PlateData.fromObject(object.PlateData);
            }
            if (object.AllFreePlateDataPerRoundVec) {
                if (!Array.isArray(object.AllFreePlateDataPerRoundVec))
                    throw TypeError(".jlhc.SpinAck.AllFreePlateDataPerRoundVec: array expected");
                message.AllFreePlateDataPerRoundVec = [];
                for (let i = 0; i < object.AllFreePlateDataPerRoundVec.length; ++i) {
                    if (typeof object.AllFreePlateDataPerRoundVec[i] !== "object")
                        throw TypeError(".jlhc.SpinAck.AllFreePlateDataPerRoundVec: object expected");
                    message.AllFreePlateDataPerRoundVec[i] = $root.jlhc.AllFreePlateDataPerRound.fromObject(object.AllFreePlateDataPerRoundVec[i]);
                }
            }
            if (object.FreeGameWin != null)
                message.FreeGameWin = Number(object.FreeGameWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.SpinAck
         * @static
         * @param {jlhc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AllFreePlateDataPerRoundVec = [];
            if (options.defaults) {
                object.PlateData = null;
                object.FreeGameWin = 0;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateData != null && message.hasOwnProperty("PlateData"))
                object.PlateData = $root.jlhc.PlateData.toObject(message.PlateData, options);
            if (message.AllFreePlateDataPerRoundVec && message.AllFreePlateDataPerRoundVec.length) {
                object.AllFreePlateDataPerRoundVec = [];
                for (let j = 0; j < message.AllFreePlateDataPerRoundVec.length; ++j)
                    object.AllFreePlateDataPerRoundVec[j] = $root.jlhc.AllFreePlateDataPerRound.toObject(message.AllFreePlateDataPerRoundVec[j], options);
            }
            if (message.FreeGameWin != null && message.hasOwnProperty("FreeGameWin"))
                object.FreeGameWin = options.json && !isFinite(message.FreeGameWin) ? String(message.FreeGameWin) : message.FreeGameWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlhc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlhc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.SpinAck";
        };

        return SpinAck;
    })();

    jlhc.ChilliSetting = (function() {

        /**
         * Properties of a ChilliSetting.
         * @memberof jlhc
         * @interface IChilliSetting
         * @property {number|null} [ChilliCount] ChilliSetting ChilliCount
         * @property {number|null} [PlateCount] ChilliSetting PlateCount
         * @property {number|null} [WildLockCount] ChilliSetting WildLockCount
         * @property {number|null} [GameType] ChilliSetting GameType
         */

        /**
         * Constructs a new ChilliSetting.
         * @memberof jlhc
         * @classdesc Represents a ChilliSetting.
         * @implements IChilliSetting
         * @constructor
         * @param {jlhc.IChilliSetting=} [properties] Properties to set
         */
        function ChilliSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChilliSetting ChilliCount.
         * @member {number} ChilliCount
         * @memberof jlhc.ChilliSetting
         * @instance
         */
        ChilliSetting.prototype.ChilliCount = 0;

        /**
         * ChilliSetting PlateCount.
         * @member {number} PlateCount
         * @memberof jlhc.ChilliSetting
         * @instance
         */
        ChilliSetting.prototype.PlateCount = 0;

        /**
         * ChilliSetting WildLockCount.
         * @member {number} WildLockCount
         * @memberof jlhc.ChilliSetting
         * @instance
         */
        ChilliSetting.prototype.WildLockCount = 0;

        /**
         * ChilliSetting GameType.
         * @member {number} GameType
         * @memberof jlhc.ChilliSetting
         * @instance
         */
        ChilliSetting.prototype.GameType = 0;

        /**
         * Creates a new ChilliSetting instance using the specified properties.
         * @function create
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {jlhc.IChilliSetting=} [properties] Properties to set
         * @returns {jlhc.ChilliSetting} ChilliSetting instance
         */
        ChilliSetting.create = function create(properties) {
            return new ChilliSetting(properties);
        };

        /**
         * Encodes the specified ChilliSetting message. Does not implicitly {@link jlhc.ChilliSetting.verify|verify} messages.
         * @function encode
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {jlhc.IChilliSetting} message ChilliSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChilliSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ChilliCount != null && Object.hasOwnProperty.call(message, "ChilliCount"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ChilliCount);
            if (message.PlateCount != null && Object.hasOwnProperty.call(message, "PlateCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.PlateCount);
            if (message.WildLockCount != null && Object.hasOwnProperty.call(message, "WildLockCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.WildLockCount);
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.GameType);
            return writer;
        };

        /**
         * Encodes the specified ChilliSetting message, length delimited. Does not implicitly {@link jlhc.ChilliSetting.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {jlhc.IChilliSetting} message ChilliSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChilliSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChilliSetting message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.ChilliSetting} ChilliSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChilliSetting.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.ChilliSetting();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ChilliCount = reader.int32();
                        break;
                    }
                case 2: {
                        message.PlateCount = reader.int32();
                        break;
                    }
                case 3: {
                        message.WildLockCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.GameType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChilliSetting message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.ChilliSetting} ChilliSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChilliSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChilliSetting message.
         * @function verify
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChilliSetting.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ChilliCount != null && message.hasOwnProperty("ChilliCount"))
                if (!$util.isInteger(message.ChilliCount))
                    return "ChilliCount: integer expected";
            if (message.PlateCount != null && message.hasOwnProperty("PlateCount"))
                if (!$util.isInteger(message.PlateCount))
                    return "PlateCount: integer expected";
            if (message.WildLockCount != null && message.hasOwnProperty("WildLockCount"))
                if (!$util.isInteger(message.WildLockCount))
                    return "WildLockCount: integer expected";
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            return null;
        };

        /**
         * Creates a ChilliSetting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.ChilliSetting} ChilliSetting
         */
        ChilliSetting.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.ChilliSetting)
                return object;
            let message = new $root.jlhc.ChilliSetting();
            if (object.ChilliCount != null)
                message.ChilliCount = object.ChilliCount | 0;
            if (object.PlateCount != null)
                message.PlateCount = object.PlateCount | 0;
            if (object.WildLockCount != null)
                message.WildLockCount = object.WildLockCount | 0;
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            return message;
        };

        /**
         * Creates a plain object from a ChilliSetting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {jlhc.ChilliSetting} message ChilliSetting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChilliSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ChilliCount = 0;
                object.PlateCount = 0;
                object.WildLockCount = 0;
                object.GameType = 0;
            }
            if (message.ChilliCount != null && message.hasOwnProperty("ChilliCount"))
                object.ChilliCount = message.ChilliCount;
            if (message.PlateCount != null && message.hasOwnProperty("PlateCount"))
                object.PlateCount = message.PlateCount;
            if (message.WildLockCount != null && message.hasOwnProperty("WildLockCount"))
                object.WildLockCount = message.WildLockCount;
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            return object;
        };

        /**
         * Converts this ChilliSetting to JSON.
         * @function toJSON
         * @memberof jlhc.ChilliSetting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChilliSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChilliSetting
         * @function getTypeUrl
         * @memberof jlhc.ChilliSetting
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChilliSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.ChilliSetting";
        };

        return ChilliSetting;
    })();

    jlhc.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlhc
         * @interface IGameInfoAck
         * @property {Array.<jlhc.IChilliSetting>|null} [ChilliSettingVec] GameInfoAck ChilliSettingVec
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlhc
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlhc.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.ChilliSettingVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck ChilliSettingVec.
         * @member {Array.<jlhc.IChilliSetting>} ChilliSettingVec
         * @memberof jlhc.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.ChilliSettingVec = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {jlhc.IGameInfoAck=} [properties] Properties to set
         * @returns {jlhc.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlhc.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {jlhc.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ChilliSettingVec != null && message.ChilliSettingVec.length)
                for (let i = 0; i < message.ChilliSettingVec.length; ++i)
                    $root.jlhc.ChilliSetting.encode(message.ChilliSettingVec[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlhc.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {jlhc.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlhc.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlhc.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 7: {
                        if (!(message.ChilliSettingVec && message.ChilliSettingVec.length))
                            message.ChilliSettingVec = [];
                        message.ChilliSettingVec.push($root.jlhc.ChilliSetting.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlhc.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ChilliSettingVec != null && message.hasOwnProperty("ChilliSettingVec")) {
                if (!Array.isArray(message.ChilliSettingVec))
                    return "ChilliSettingVec: array expected";
                for (let i = 0; i < message.ChilliSettingVec.length; ++i) {
                    let error = $root.jlhc.ChilliSetting.verify(message.ChilliSettingVec[i]);
                    if (error)
                        return "ChilliSettingVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlhc.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlhc.GameInfoAck)
                return object;
            let message = new $root.jlhc.GameInfoAck();
            if (object.ChilliSettingVec) {
                if (!Array.isArray(object.ChilliSettingVec))
                    throw TypeError(".jlhc.GameInfoAck.ChilliSettingVec: array expected");
                message.ChilliSettingVec = [];
                for (let i = 0; i < object.ChilliSettingVec.length; ++i) {
                    if (typeof object.ChilliSettingVec[i] !== "object")
                        throw TypeError(".jlhc.GameInfoAck.ChilliSettingVec: object expected");
                    message.ChilliSettingVec[i] = $root.jlhc.ChilliSetting.fromObject(object.ChilliSettingVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {jlhc.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ChilliSettingVec = [];
            if (message.ChilliSettingVec && message.ChilliSettingVec.length) {
                object.ChilliSettingVec = [];
                for (let j = 0; j < message.ChilliSettingVec.length; ++j)
                    object.ChilliSettingVec[j] = $root.jlhc.ChilliSetting.toObject(message.ChilliSettingVec[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlhc.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlhc.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlhc.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlhc;
})();

export const jlht = $root.jlht = (() => {

    /**
     * Namespace jlht.
     * @exports jlht
     * @namespace
     */
    const jlht = {};

    jlht.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlht
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlht
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlht.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlht.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlht.Column
         * @static
         * @param {jlht.IColumn=} [properties] Properties to set
         * @returns {jlht.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlht.Column.verify|verify} messages.
         * @function encode
         * @memberof jlht.Column
         * @static
         * @param {jlht.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlht.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.Column
         * @static
         * @param {jlht.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlht.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.Column)
                return object;
            let message = new $root.jlht.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlht.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.Column
         * @static
         * @param {jlht.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlht.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlht.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.Column";
        };

        return Column;
    })();

    jlht.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlht
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlht
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlht.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlht.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlht.ColumnFloat
         * @static
         * @param {jlht.IColumnFloat=} [properties] Properties to set
         * @returns {jlht.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlht.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlht.ColumnFloat
         * @static
         * @param {jlht.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlht.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.ColumnFloat
         * @static
         * @param {jlht.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlht.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.ColumnFloat)
                return object;
            let message = new $root.jlht.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlht.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.ColumnFloat
         * @static
         * @param {jlht.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlht.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlht.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlht.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlht
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         * @property {number|null} [Symbol] Grid Symbol
         */

        /**
         * Constructs a new Grid.
         * @memberof jlht
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlht.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlht.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlht.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Grid Symbol.
         * @member {number} Symbol
         * @memberof jlht.Grid
         * @instance
         */
        Grid.prototype.Symbol = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlht.Grid
         * @static
         * @param {jlht.IGrid=} [properties] Properties to set
         * @returns {jlht.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlht.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlht.Grid
         * @static
         * @param {jlht.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlht.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.Grid
         * @static
         * @param {jlht.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlht.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.Grid)
                return object;
            let message = new $root.jlht.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.Grid
         * @static
         * @param {jlht.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
                object.Symbol = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlht.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlht.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.Grid";
        };

        return Grid;
    })();

    jlht.NumCollectData = (function() {

        /**
         * Properties of a NumCollectData.
         * @memberof jlht
         * @interface INumCollectData
         * @property {Array.<jlht.IGrid>|null} [Target] NumCollectData Target
         * @property {Array.<jlht.IGrid>|null} [Source] NumCollectData Source
         */

        /**
         * Constructs a new NumCollectData.
         * @memberof jlht
         * @classdesc Represents a NumCollectData.
         * @implements INumCollectData
         * @constructor
         * @param {jlht.INumCollectData=} [properties] Properties to set
         */
        function NumCollectData(properties) {
            this.Target = [];
            this.Source = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NumCollectData Target.
         * @member {Array.<jlht.IGrid>} Target
         * @memberof jlht.NumCollectData
         * @instance
         */
        NumCollectData.prototype.Target = $util.emptyArray;

        /**
         * NumCollectData Source.
         * @member {Array.<jlht.IGrid>} Source
         * @memberof jlht.NumCollectData
         * @instance
         */
        NumCollectData.prototype.Source = $util.emptyArray;

        /**
         * Creates a new NumCollectData instance using the specified properties.
         * @function create
         * @memberof jlht.NumCollectData
         * @static
         * @param {jlht.INumCollectData=} [properties] Properties to set
         * @returns {jlht.NumCollectData} NumCollectData instance
         */
        NumCollectData.create = function create(properties) {
            return new NumCollectData(properties);
        };

        /**
         * Encodes the specified NumCollectData message. Does not implicitly {@link jlht.NumCollectData.verify|verify} messages.
         * @function encode
         * @memberof jlht.NumCollectData
         * @static
         * @param {jlht.INumCollectData} message NumCollectData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NumCollectData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Target != null && message.Target.length)
                for (let i = 0; i < message.Target.length; ++i)
                    $root.jlht.Grid.encode(message.Target[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Source != null && message.Source.length)
                for (let i = 0; i < message.Source.length; ++i)
                    $root.jlht.Grid.encode(message.Source[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NumCollectData message, length delimited. Does not implicitly {@link jlht.NumCollectData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.NumCollectData
         * @static
         * @param {jlht.INumCollectData} message NumCollectData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NumCollectData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a NumCollectData message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.NumCollectData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.NumCollectData} NumCollectData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NumCollectData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.NumCollectData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Target && message.Target.length))
                            message.Target = [];
                        message.Target.push($root.jlht.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Source && message.Source.length))
                            message.Source = [];
                        message.Source.push($root.jlht.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NumCollectData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.NumCollectData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.NumCollectData} NumCollectData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NumCollectData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NumCollectData message.
         * @function verify
         * @memberof jlht.NumCollectData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NumCollectData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Target != null && message.hasOwnProperty("Target")) {
                if (!Array.isArray(message.Target))
                    return "Target: array expected";
                for (let i = 0; i < message.Target.length; ++i) {
                    let error = $root.jlht.Grid.verify(message.Target[i]);
                    if (error)
                        return "Target." + error;
                }
            }
            if (message.Source != null && message.hasOwnProperty("Source")) {
                if (!Array.isArray(message.Source))
                    return "Source: array expected";
                for (let i = 0; i < message.Source.length; ++i) {
                    let error = $root.jlht.Grid.verify(message.Source[i]);
                    if (error)
                        return "Source." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NumCollectData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.NumCollectData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.NumCollectData} NumCollectData
         */
        NumCollectData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.NumCollectData)
                return object;
            let message = new $root.jlht.NumCollectData();
            if (object.Target) {
                if (!Array.isArray(object.Target))
                    throw TypeError(".jlht.NumCollectData.Target: array expected");
                message.Target = [];
                for (let i = 0; i < object.Target.length; ++i) {
                    if (typeof object.Target[i] !== "object")
                        throw TypeError(".jlht.NumCollectData.Target: object expected");
                    message.Target[i] = $root.jlht.Grid.fromObject(object.Target[i]);
                }
            }
            if (object.Source) {
                if (!Array.isArray(object.Source))
                    throw TypeError(".jlht.NumCollectData.Source: array expected");
                message.Source = [];
                for (let i = 0; i < object.Source.length; ++i) {
                    if (typeof object.Source[i] !== "object")
                        throw TypeError(".jlht.NumCollectData.Source: object expected");
                    message.Source[i] = $root.jlht.Grid.fromObject(object.Source[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NumCollectData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.NumCollectData
         * @static
         * @param {jlht.NumCollectData} message NumCollectData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NumCollectData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Target = [];
                object.Source = [];
            }
            if (message.Target && message.Target.length) {
                object.Target = [];
                for (let j = 0; j < message.Target.length; ++j)
                    object.Target[j] = $root.jlht.Grid.toObject(message.Target[j], options);
            }
            if (message.Source && message.Source.length) {
                object.Source = [];
                for (let j = 0; j < message.Source.length; ++j)
                    object.Source[j] = $root.jlht.Grid.toObject(message.Source[j], options);
            }
            return object;
        };

        /**
         * Converts this NumCollectData to JSON.
         * @function toJSON
         * @memberof jlht.NumCollectData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NumCollectData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NumCollectData
         * @function getTypeUrl
         * @memberof jlht.NumCollectData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NumCollectData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.NumCollectData";
        };

        return NumCollectData;
    })();

    jlht.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlht
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlht
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlht.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlht.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlht.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlht.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlht.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlht.AwardData
         * @static
         * @param {jlht.IAwardData=} [properties] Properties to set
         * @returns {jlht.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlht.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlht.AwardData
         * @static
         * @param {jlht.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlht.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.AwardData
         * @static
         * @param {jlht.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlht.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.AwardData)
                return object;
            let message = new $root.jlht.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.AwardData
         * @static
         * @param {jlht.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlht.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlht.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.AwardData";
        };

        return AwardData;
    })();

    jlht.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlht
         * @interface IRoundInfo
         * @property {Array.<jlht.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlht.IColumnFloat>|null} [PlateSymbolNum] RoundInfo PlateSymbolNum
         * @property {Array.<jlht.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [BonusRemainRound] RoundInfo BonusRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<jlht.INumCollectData>|null} [NumCollectPosDriver] RoundInfo NumCollectPosDriver
         * @property {Array.<jlht.INumCollectData>|null} [NumCollectPosRolls] RoundInfo NumCollectPosRolls
         * @property {Array.<jlht.IColumnFloat>|null} [PlateSymbolNumLog] RoundInfo PlateSymbolNumLog
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlht
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlht.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            this.AwardDataVec = [];
            this.NumCollectPosDriver = [];
            this.NumCollectPosRolls = [];
            this.PlateSymbolNumLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlht.IColumn>} PlateSymbol
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNum.
         * @member {Array.<jlht.IColumnFloat>} PlateSymbolNum
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlht.IAwardData>} AwardDataVec
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo BonusRemainRound.
         * @member {number} BonusRemainRound
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo NumCollectPosDriver.
         * @member {Array.<jlht.INumCollectData>} NumCollectPosDriver
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NumCollectPosDriver = $util.emptyArray;

        /**
         * RoundInfo NumCollectPosRolls.
         * @member {Array.<jlht.INumCollectData>} NumCollectPosRolls
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NumCollectPosRolls = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNumLog.
         * @member {Array.<jlht.IColumnFloat>} PlateSymbolNumLog
         * @memberof jlht.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNumLog = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlht.RoundInfo
         * @static
         * @param {jlht.IRoundInfo=} [properties] Properties to set
         * @returns {jlht.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlht.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlht.RoundInfo
         * @static
         * @param {jlht.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlht.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jlht.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlht.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.BonusRemainRound != null && Object.hasOwnProperty.call(message, "BonusRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.BonusRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.NumCollectPosDriver != null && message.NumCollectPosDriver.length)
                for (let i = 0; i < message.NumCollectPosDriver.length; ++i)
                    $root.jlht.NumCollectData.encode(message.NumCollectPosDriver[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.NumCollectPosRolls != null && message.NumCollectPosRolls.length)
                for (let i = 0; i < message.NumCollectPosRolls.length; ++i)
                    $root.jlht.NumCollectData.encode(message.NumCollectPosRolls[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.PlateSymbolNumLog != null && message.PlateSymbolNumLog.length)
                for (let i = 0; i < message.PlateSymbolNumLog.length; ++i)
                    $root.jlht.ColumnFloat.encode(message.PlateSymbolNumLog[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlht.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.RoundInfo
         * @static
         * @param {jlht.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlht.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jlht.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlht.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.BonusRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        if (!(message.NumCollectPosDriver && message.NumCollectPosDriver.length))
                            message.NumCollectPosDriver = [];
                        message.NumCollectPosDriver.push($root.jlht.NumCollectData.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        if (!(message.NumCollectPosRolls && message.NumCollectPosRolls.length))
                            message.NumCollectPosRolls = [];
                        message.NumCollectPosRolls.push($root.jlht.NumCollectData.decode(reader, reader.uint32()));
                        break;
                    }
                case 9: {
                        if (!(message.PlateSymbolNumLog && message.PlateSymbolNumLog.length))
                            message.PlateSymbolNumLog = [];
                        message.PlateSymbolNumLog.push($root.jlht.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlht.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlht.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jlht.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlht.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                if (!$util.isInteger(message.BonusRemainRound))
                    return "BonusRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.NumCollectPosDriver != null && message.hasOwnProperty("NumCollectPosDriver")) {
                if (!Array.isArray(message.NumCollectPosDriver))
                    return "NumCollectPosDriver: array expected";
                for (let i = 0; i < message.NumCollectPosDriver.length; ++i) {
                    let error = $root.jlht.NumCollectData.verify(message.NumCollectPosDriver[i]);
                    if (error)
                        return "NumCollectPosDriver." + error;
                }
            }
            if (message.NumCollectPosRolls != null && message.hasOwnProperty("NumCollectPosRolls")) {
                if (!Array.isArray(message.NumCollectPosRolls))
                    return "NumCollectPosRolls: array expected";
                for (let i = 0; i < message.NumCollectPosRolls.length; ++i) {
                    let error = $root.jlht.NumCollectData.verify(message.NumCollectPosRolls[i]);
                    if (error)
                        return "NumCollectPosRolls." + error;
                }
            }
            if (message.PlateSymbolNumLog != null && message.hasOwnProperty("PlateSymbolNumLog")) {
                if (!Array.isArray(message.PlateSymbolNumLog))
                    return "PlateSymbolNumLog: array expected";
                for (let i = 0; i < message.PlateSymbolNumLog.length; ++i) {
                    let error = $root.jlht.ColumnFloat.verify(message.PlateSymbolNumLog[i]);
                    if (error)
                        return "PlateSymbolNumLog." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.RoundInfo)
                return object;
            let message = new $root.jlht.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlht.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlht.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlht.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jlht.RoundInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jlht.RoundInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jlht.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlht.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlht.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlht.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.BonusRemainRound != null)
                message.BonusRemainRound = object.BonusRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.NumCollectPosDriver) {
                if (!Array.isArray(object.NumCollectPosDriver))
                    throw TypeError(".jlht.RoundInfo.NumCollectPosDriver: array expected");
                message.NumCollectPosDriver = [];
                for (let i = 0; i < object.NumCollectPosDriver.length; ++i) {
                    if (typeof object.NumCollectPosDriver[i] !== "object")
                        throw TypeError(".jlht.RoundInfo.NumCollectPosDriver: object expected");
                    message.NumCollectPosDriver[i] = $root.jlht.NumCollectData.fromObject(object.NumCollectPosDriver[i]);
                }
            }
            if (object.NumCollectPosRolls) {
                if (!Array.isArray(object.NumCollectPosRolls))
                    throw TypeError(".jlht.RoundInfo.NumCollectPosRolls: array expected");
                message.NumCollectPosRolls = [];
                for (let i = 0; i < object.NumCollectPosRolls.length; ++i) {
                    if (typeof object.NumCollectPosRolls[i] !== "object")
                        throw TypeError(".jlht.RoundInfo.NumCollectPosRolls: object expected");
                    message.NumCollectPosRolls[i] = $root.jlht.NumCollectData.fromObject(object.NumCollectPosRolls[i]);
                }
            }
            if (object.PlateSymbolNumLog) {
                if (!Array.isArray(object.PlateSymbolNumLog))
                    throw TypeError(".jlht.RoundInfo.PlateSymbolNumLog: array expected");
                message.PlateSymbolNumLog = [];
                for (let i = 0; i < object.PlateSymbolNumLog.length; ++i) {
                    if (typeof object.PlateSymbolNumLog[i] !== "object")
                        throw TypeError(".jlht.RoundInfo.PlateSymbolNumLog: object expected");
                    message.PlateSymbolNumLog[i] = $root.jlht.ColumnFloat.fromObject(object.PlateSymbolNumLog[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.RoundInfo
         * @static
         * @param {jlht.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
                object.AwardDataVec = [];
                object.NumCollectPosDriver = [];
                object.NumCollectPosRolls = [];
                object.PlateSymbolNumLog = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.BonusRemainRound = 0;
                object.RoundWin = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlht.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jlht.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlht.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                object.BonusRemainRound = message.BonusRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.NumCollectPosDriver && message.NumCollectPosDriver.length) {
                object.NumCollectPosDriver = [];
                for (let j = 0; j < message.NumCollectPosDriver.length; ++j)
                    object.NumCollectPosDriver[j] = $root.jlht.NumCollectData.toObject(message.NumCollectPosDriver[j], options);
            }
            if (message.NumCollectPosRolls && message.NumCollectPosRolls.length) {
                object.NumCollectPosRolls = [];
                for (let j = 0; j < message.NumCollectPosRolls.length; ++j)
                    object.NumCollectPosRolls[j] = $root.jlht.NumCollectData.toObject(message.NumCollectPosRolls[j], options);
            }
            if (message.PlateSymbolNumLog && message.PlateSymbolNumLog.length) {
                object.PlateSymbolNumLog = [];
                for (let j = 0; j < message.PlateSymbolNumLog.length; ++j)
                    object.PlateSymbolNumLog[j] = $root.jlht.ColumnFloat.toObject(message.PlateSymbolNumLog[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlht.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlht.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.RoundInfo";
        };

        return RoundInfo;
    })();

    jlht.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlht
         * @interface ISpinAck
         * @property {Array.<jlht.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {Array.<jlht.IColumnFloat>|null} [FinalCheck] SpinAck FinalCheck
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlht
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlht.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.FinalCheck = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlht.IRoundInfo>} RoundQueue
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck FinalCheck.
         * @member {Array.<jlht.IColumnFloat>} FinalCheck
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.FinalCheck = $util.emptyArray;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlht.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlht.SpinAck
         * @static
         * @param {jlht.ISpinAck=} [properties] Properties to set
         * @returns {jlht.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlht.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlht.SpinAck
         * @static
         * @param {jlht.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlht.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.FinalCheck != null && message.FinalCheck.length)
                for (let i = 0; i < message.FinalCheck.length; ++i)
                    $root.jlht.ColumnFloat.encode(message.FinalCheck[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlht.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.SpinAck
         * @static
         * @param {jlht.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlht.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.FinalCheck && message.FinalCheck.length))
                            message.FinalCheck = [];
                        message.FinalCheck.push($root.jlht.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlht.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlht.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.FinalCheck != null && message.hasOwnProperty("FinalCheck")) {
                if (!Array.isArray(message.FinalCheck))
                    return "FinalCheck: array expected";
                for (let i = 0; i < message.FinalCheck.length; ++i) {
                    let error = $root.jlht.ColumnFloat.verify(message.FinalCheck[i]);
                    if (error)
                        return "FinalCheck." + error;
                }
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.SpinAck)
                return object;
            let message = new $root.jlht.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlht.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlht.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlht.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.FinalCheck) {
                if (!Array.isArray(object.FinalCheck))
                    throw TypeError(".jlht.SpinAck.FinalCheck: array expected");
                message.FinalCheck = [];
                for (let i = 0; i < object.FinalCheck.length; ++i) {
                    if (typeof object.FinalCheck[i] !== "object")
                        throw TypeError(".jlht.SpinAck.FinalCheck: object expected");
                    message.FinalCheck[i] = $root.jlht.ColumnFloat.fromObject(object.FinalCheck[i]);
                }
            }
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.SpinAck
         * @static
         * @param {jlht.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.FinalCheck = [];
            }
            if (options.defaults) {
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlht.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.FinalCheck && message.FinalCheck.length) {
                object.FinalCheck = [];
                for (let j = 0; j < message.FinalCheck.length; ++j)
                    object.FinalCheck[j] = $root.jlht.ColumnFloat.toObject(message.FinalCheck[j], options);
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlht.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlht.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.SpinAck";
        };

        return SpinAck;
    })();

    jlht.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlht
         * @interface IGameInfoAck
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlht
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlht.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlht.GameInfoAck
         * @static
         * @param {jlht.IGameInfoAck=} [properties] Properties to set
         * @returns {jlht.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlht.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlht.GameInfoAck
         * @static
         * @param {jlht.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlht.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlht.GameInfoAck
         * @static
         * @param {jlht.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlht.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlht.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlht.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlht.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlht.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlht.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlht.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlht.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlht.GameInfoAck)
                return object;
            return new $root.jlht.GameInfoAck();
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlht.GameInfoAck
         * @static
         * @param {jlht.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlht.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlht.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlht.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlht;
})();

export const jljc = $root.jljc = (() => {

    /**
     * Namespace jljc.
     * @exports jljc
     * @namespace
     */
    const jljc = {};

    jljc.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jljc
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jljc
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jljc.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jljc.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jljc.Column
         * @static
         * @param {jljc.IColumn=} [properties] Properties to set
         * @returns {jljc.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jljc.Column.verify|verify} messages.
         * @function encode
         * @memberof jljc.Column
         * @static
         * @param {jljc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jljc.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljc.Column
         * @static
         * @param {jljc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jljc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljc.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jljc.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljc.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljc.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jljc.Column)
                return object;
            let message = new $root.jljc.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jljc.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljc.Column
         * @static
         * @param {jljc.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jljc.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jljc.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljc.Column";
        };

        return Column;
    })();

    jljc.ColumnM = (function() {

        /**
         * Properties of a ColumnM.
         * @memberof jljc
         * @interface IColumnM
         * @property {Array.<number>|null} [Col] ColumnM Col
         */

        /**
         * Constructs a new ColumnM.
         * @memberof jljc
         * @classdesc Represents a ColumnM.
         * @implements IColumnM
         * @constructor
         * @param {jljc.IColumnM=} [properties] Properties to set
         */
        function ColumnM(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnM Col.
         * @member {Array.<number>} Col
         * @memberof jljc.ColumnM
         * @instance
         */
        ColumnM.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnM instance using the specified properties.
         * @function create
         * @memberof jljc.ColumnM
         * @static
         * @param {jljc.IColumnM=} [properties] Properties to set
         * @returns {jljc.ColumnM} ColumnM instance
         */
        ColumnM.create = function create(properties) {
            return new ColumnM(properties);
        };

        /**
         * Encodes the specified ColumnM message. Does not implicitly {@link jljc.ColumnM.verify|verify} messages.
         * @function encode
         * @memberof jljc.ColumnM
         * @static
         * @param {jljc.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnM message, length delimited. Does not implicitly {@link jljc.ColumnM.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljc.ColumnM
         * @static
         * @param {jljc.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer.
         * @function decode
         * @memberof jljc.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljc.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljc.ColumnM();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljc.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljc.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnM message.
         * @function verify
         * @memberof jljc.ColumnM
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnM.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnM message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljc.ColumnM
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljc.ColumnM} ColumnM
         */
        ColumnM.fromObject = function fromObject(object) {
            if (object instanceof $root.jljc.ColumnM)
                return object;
            let message = new $root.jljc.ColumnM();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jljc.ColumnM.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnM message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljc.ColumnM
         * @static
         * @param {jljc.ColumnM} message ColumnM
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnM.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnM to JSON.
         * @function toJSON
         * @memberof jljc.ColumnM
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnM.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnM
         * @function getTypeUrl
         * @memberof jljc.ColumnM
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnM.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljc.ColumnM";
        };

        return ColumnM;
    })();

    jljc.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jljc
         * @interface IAwardData
         * @property {number|null} [Atype] AwardData Atype
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Pos] AwardData Pos
         * @property {number|null} [Mul] AwardData Mul
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jljc
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jljc.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Atype.
         * @member {number} Atype
         * @memberof jljc.AwardData
         * @instance
         */
        AwardData.prototype.Atype = 0;

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jljc.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jljc.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Pos.
         * @member {number} Pos
         * @memberof jljc.AwardData
         * @instance
         */
        AwardData.prototype.Pos = 0;

        /**
         * AwardData Mul.
         * @member {number} Mul
         * @memberof jljc.AwardData
         * @instance
         */
        AwardData.prototype.Mul = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jljc.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jljc.AwardData
         * @static
         * @param {jljc.IAwardData=} [properties] Properties to set
         * @returns {jljc.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jljc.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jljc.AwardData
         * @static
         * @param {jljc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Atype != null && Object.hasOwnProperty.call(message, "Atype"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Atype);
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Pos);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mul);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jljc.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljc.AwardData
         * @static
         * @param {jljc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jljc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljc.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Atype = reader.int32();
                        break;
                    }
                case 2: {
                        message.Index = reader.int32();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 5: {
                        message.Mul = reader.double();
                        break;
                    }
                case 6: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jljc.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                if (!$util.isInteger(message.Atype))
                    return "Atype: integer expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljc.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljc.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljc.AwardData)
                return object;
            let message = new $root.jljc.AwardData();
            if (object.Atype != null)
                message.Atype = object.Atype | 0;
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljc.AwardData
         * @static
         * @param {jljc.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Atype = 0;
                object.Index = 0;
                object.Symbol = 0;
                object.Pos = 0;
                object.Mul = 0;
                object.Win = 0;
            }
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                object.Atype = message.Atype;
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jljc.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jljc.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljc.AwardData";
        };

        return AwardData;
    })();

    jljc.PotSymbol = (function() {

        /**
         * Properties of a PotSymbol.
         * @memberof jljc
         * @interface IPotSymbol
         * @property {number|null} [Pos] PotSymbol Pos
         * @property {number|null} [Symbol] PotSymbol Symbol
         * @property {number|null} [Mul] PotSymbol Mul
         */

        /**
         * Constructs a new PotSymbol.
         * @memberof jljc
         * @classdesc Represents a PotSymbol.
         * @implements IPotSymbol
         * @constructor
         * @param {jljc.IPotSymbol=} [properties] Properties to set
         */
        function PotSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PotSymbol Pos.
         * @member {number} Pos
         * @memberof jljc.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Pos = 0;

        /**
         * PotSymbol Symbol.
         * @member {number} Symbol
         * @memberof jljc.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Symbol = 0;

        /**
         * PotSymbol Mul.
         * @member {number} Mul
         * @memberof jljc.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Mul = 0;

        /**
         * Creates a new PotSymbol instance using the specified properties.
         * @function create
         * @memberof jljc.PotSymbol
         * @static
         * @param {jljc.IPotSymbol=} [properties] Properties to set
         * @returns {jljc.PotSymbol} PotSymbol instance
         */
        PotSymbol.create = function create(properties) {
            return new PotSymbol(properties);
        };

        /**
         * Encodes the specified PotSymbol message. Does not implicitly {@link jljc.PotSymbol.verify|verify} messages.
         * @function encode
         * @memberof jljc.PotSymbol
         * @static
         * @param {jljc.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified PotSymbol message, length delimited. Does not implicitly {@link jljc.PotSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljc.PotSymbol
         * @static
         * @param {jljc.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jljc.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljc.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljc.PotSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljc.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljc.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PotSymbol message.
         * @function verify
         * @memberof jljc.PotSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PotSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a PotSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljc.PotSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljc.PotSymbol} PotSymbol
         */
        PotSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jljc.PotSymbol)
                return object;
            let message = new $root.jljc.PotSymbol();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a PotSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljc.PotSymbol
         * @static
         * @param {jljc.PotSymbol} message PotSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PotSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this PotSymbol to JSON.
         * @function toJSON
         * @memberof jljc.PotSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PotSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PotSymbol
         * @function getTypeUrl
         * @memberof jljc.PotSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PotSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljc.PotSymbol";
        };

        return PotSymbol;
    })();

    jljc.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jljc
         * @interface IRoundInfo
         * @property {Array.<jljc.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {Array.<jljc.IColumnM>|null} [Mul] RoundInfo Mul
         * @property {Array.<jljc.IColumnM>|null} [Frame] RoundInfo Frame
         * @property {Array.<jljc.IColumn>|null} [FinalSymbol] RoundInfo FinalSymbol
         * @property {Array.<jljc.IColumnM>|null} [FinalMul] RoundInfo FinalMul
         * @property {Array.<jljc.IColumnM>|null} [FinalFrame] RoundInfo FinalFrame
         * @property {Array.<jljc.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<jljc.IPotSymbol>|null} [PotList] RoundInfo PotList
         * @property {number|null} [BonusRemainRound] RoundInfo BonusRemainRound
         * @property {number|null} [BonusTotalRound] RoundInfo BonusTotalRound
         * @property {boolean|null} [GetBonus] RoundInfo GetBonus
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jljc
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jljc.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            this.Frame = [];
            this.FinalSymbol = [];
            this.FinalMul = [];
            this.FinalFrame = [];
            this.AwardDataVec = [];
            this.PotList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jljc.IColumn>} Symbol
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo Mul.
         * @member {Array.<jljc.IColumnM>} Mul
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mul = $util.emptyArray;

        /**
         * RoundInfo Frame.
         * @member {Array.<jljc.IColumnM>} Frame
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Frame = $util.emptyArray;

        /**
         * RoundInfo FinalSymbol.
         * @member {Array.<jljc.IColumn>} FinalSymbol
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalSymbol = $util.emptyArray;

        /**
         * RoundInfo FinalMul.
         * @member {Array.<jljc.IColumnM>} FinalMul
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalMul = $util.emptyArray;

        /**
         * RoundInfo FinalFrame.
         * @member {Array.<jljc.IColumnM>} FinalFrame
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalFrame = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jljc.IAwardData>} AwardDataVec
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo PotList.
         * @member {Array.<jljc.IPotSymbol>} PotList
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PotList = $util.emptyArray;

        /**
         * RoundInfo BonusRemainRound.
         * @member {number} BonusRemainRound
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusRemainRound = 0;

        /**
         * RoundInfo BonusTotalRound.
         * @member {number} BonusTotalRound
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusTotalRound = 0;

        /**
         * RoundInfo GetBonus.
         * @member {boolean} GetBonus
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetBonus = false;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jljc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jljc.RoundInfo
         * @static
         * @param {jljc.IRoundInfo=} [properties] Properties to set
         * @returns {jljc.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jljc.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jljc.RoundInfo
         * @static
         * @param {jljc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jljc.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length)
                for (let i = 0; i < message.Mul.length; ++i)
                    $root.jljc.ColumnM.encode(message.Mul[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.Frame != null && message.Frame.length)
                for (let i = 0; i < message.Frame.length; ++i)
                    $root.jljc.ColumnM.encode(message.Frame[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FinalSymbol != null && message.FinalSymbol.length)
                for (let i = 0; i < message.FinalSymbol.length; ++i)
                    $root.jljc.Column.encode(message.FinalSymbol[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FinalMul != null && message.FinalMul.length)
                for (let i = 0; i < message.FinalMul.length; ++i)
                    $root.jljc.ColumnM.encode(message.FinalMul[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FinalFrame != null && message.FinalFrame.length)
                for (let i = 0; i < message.FinalFrame.length; ++i)
                    $root.jljc.ColumnM.encode(message.FinalFrame[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jljc.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.PotList != null && message.PotList.length)
                for (let i = 0; i < message.PotList.length; ++i)
                    $root.jljc.PotSymbol.encode(message.PotList[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.BonusRemainRound != null && Object.hasOwnProperty.call(message, "BonusRemainRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.BonusRemainRound);
            if (message.BonusTotalRound != null && Object.hasOwnProperty.call(message, "BonusTotalRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.BonusTotalRound);
            if (message.GetBonus != null && Object.hasOwnProperty.call(message, "GetBonus"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.GetBonus);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jljc.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljc.RoundInfo
         * @static
         * @param {jljc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jljc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljc.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jljc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        message.Mul.push($root.jljc.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.Frame && message.Frame.length))
                            message.Frame = [];
                        message.Frame.push($root.jljc.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FinalSymbol && message.FinalSymbol.length))
                            message.FinalSymbol = [];
                        message.FinalSymbol.push($root.jljc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FinalMul && message.FinalMul.length))
                            message.FinalMul = [];
                        message.FinalMul.push($root.jljc.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.FinalFrame && message.FinalFrame.length))
                            message.FinalFrame = [];
                        message.FinalFrame.push($root.jljc.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jljc.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        if (!(message.PotList && message.PotList.length))
                            message.PotList = [];
                        message.PotList.push($root.jljc.PotSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        message.BonusRemainRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.BonusTotalRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.GetBonus = reader.bool();
                        break;
                    }
                case 13: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 14: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jljc.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jljc.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i) {
                    let error = $root.jljc.ColumnM.verify(message.Mul[i]);
                    if (error)
                        return "Mul." + error;
                }
            }
            if (message.Frame != null && message.hasOwnProperty("Frame")) {
                if (!Array.isArray(message.Frame))
                    return "Frame: array expected";
                for (let i = 0; i < message.Frame.length; ++i) {
                    let error = $root.jljc.ColumnM.verify(message.Frame[i]);
                    if (error)
                        return "Frame." + error;
                }
            }
            if (message.FinalSymbol != null && message.hasOwnProperty("FinalSymbol")) {
                if (!Array.isArray(message.FinalSymbol))
                    return "FinalSymbol: array expected";
                for (let i = 0; i < message.FinalSymbol.length; ++i) {
                    let error = $root.jljc.Column.verify(message.FinalSymbol[i]);
                    if (error)
                        return "FinalSymbol." + error;
                }
            }
            if (message.FinalMul != null && message.hasOwnProperty("FinalMul")) {
                if (!Array.isArray(message.FinalMul))
                    return "FinalMul: array expected";
                for (let i = 0; i < message.FinalMul.length; ++i) {
                    let error = $root.jljc.ColumnM.verify(message.FinalMul[i]);
                    if (error)
                        return "FinalMul." + error;
                }
            }
            if (message.FinalFrame != null && message.hasOwnProperty("FinalFrame")) {
                if (!Array.isArray(message.FinalFrame))
                    return "FinalFrame: array expected";
                for (let i = 0; i < message.FinalFrame.length; ++i) {
                    let error = $root.jljc.ColumnM.verify(message.FinalFrame[i]);
                    if (error)
                        return "FinalFrame." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jljc.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.PotList != null && message.hasOwnProperty("PotList")) {
                if (!Array.isArray(message.PotList))
                    return "PotList: array expected";
                for (let i = 0; i < message.PotList.length; ++i) {
                    let error = $root.jljc.PotSymbol.verify(message.PotList[i]);
                    if (error)
                        return "PotList." + error;
                }
            }
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                if (!$util.isInteger(message.BonusRemainRound))
                    return "BonusRemainRound: integer expected";
            if (message.BonusTotalRound != null && message.hasOwnProperty("BonusTotalRound"))
                if (!$util.isInteger(message.BonusTotalRound))
                    return "BonusTotalRound: integer expected";
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                if (typeof message.GetBonus !== "boolean")
                    return "GetBonus: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljc.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljc.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jljc.RoundInfo)
                return object;
            let message = new $root.jljc.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jljc.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jljc.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jljc.RoundInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i) {
                    if (typeof object.Mul[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.Mul: object expected");
                    message.Mul[i] = $root.jljc.ColumnM.fromObject(object.Mul[i]);
                }
            }
            if (object.Frame) {
                if (!Array.isArray(object.Frame))
                    throw TypeError(".jljc.RoundInfo.Frame: array expected");
                message.Frame = [];
                for (let i = 0; i < object.Frame.length; ++i) {
                    if (typeof object.Frame[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.Frame: object expected");
                    message.Frame[i] = $root.jljc.ColumnM.fromObject(object.Frame[i]);
                }
            }
            if (object.FinalSymbol) {
                if (!Array.isArray(object.FinalSymbol))
                    throw TypeError(".jljc.RoundInfo.FinalSymbol: array expected");
                message.FinalSymbol = [];
                for (let i = 0; i < object.FinalSymbol.length; ++i) {
                    if (typeof object.FinalSymbol[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.FinalSymbol: object expected");
                    message.FinalSymbol[i] = $root.jljc.Column.fromObject(object.FinalSymbol[i]);
                }
            }
            if (object.FinalMul) {
                if (!Array.isArray(object.FinalMul))
                    throw TypeError(".jljc.RoundInfo.FinalMul: array expected");
                message.FinalMul = [];
                for (let i = 0; i < object.FinalMul.length; ++i) {
                    if (typeof object.FinalMul[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.FinalMul: object expected");
                    message.FinalMul[i] = $root.jljc.ColumnM.fromObject(object.FinalMul[i]);
                }
            }
            if (object.FinalFrame) {
                if (!Array.isArray(object.FinalFrame))
                    throw TypeError(".jljc.RoundInfo.FinalFrame: array expected");
                message.FinalFrame = [];
                for (let i = 0; i < object.FinalFrame.length; ++i) {
                    if (typeof object.FinalFrame[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.FinalFrame: object expected");
                    message.FinalFrame[i] = $root.jljc.ColumnM.fromObject(object.FinalFrame[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jljc.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jljc.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.PotList) {
                if (!Array.isArray(object.PotList))
                    throw TypeError(".jljc.RoundInfo.PotList: array expected");
                message.PotList = [];
                for (let i = 0; i < object.PotList.length; ++i) {
                    if (typeof object.PotList[i] !== "object")
                        throw TypeError(".jljc.RoundInfo.PotList: object expected");
                    message.PotList[i] = $root.jljc.PotSymbol.fromObject(object.PotList[i]);
                }
            }
            if (object.BonusRemainRound != null)
                message.BonusRemainRound = object.BonusRemainRound | 0;
            if (object.BonusTotalRound != null)
                message.BonusTotalRound = object.BonusTotalRound | 0;
            if (object.GetBonus != null)
                message.GetBonus = Boolean(object.GetBonus);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljc.RoundInfo
         * @static
         * @param {jljc.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
                object.Frame = [];
                object.FinalSymbol = [];
                object.FinalMul = [];
                object.FinalFrame = [];
                object.AwardDataVec = [];
                object.PotList = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.BonusRemainRound = 0;
                object.BonusTotalRound = 0;
                object.GetBonus = false;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jljc.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = $root.jljc.ColumnM.toObject(message.Mul[j], options);
            }
            if (message.Frame && message.Frame.length) {
                object.Frame = [];
                for (let j = 0; j < message.Frame.length; ++j)
                    object.Frame[j] = $root.jljc.ColumnM.toObject(message.Frame[j], options);
            }
            if (message.FinalSymbol && message.FinalSymbol.length) {
                object.FinalSymbol = [];
                for (let j = 0; j < message.FinalSymbol.length; ++j)
                    object.FinalSymbol[j] = $root.jljc.Column.toObject(message.FinalSymbol[j], options);
            }
            if (message.FinalMul && message.FinalMul.length) {
                object.FinalMul = [];
                for (let j = 0; j < message.FinalMul.length; ++j)
                    object.FinalMul[j] = $root.jljc.ColumnM.toObject(message.FinalMul[j], options);
            }
            if (message.FinalFrame && message.FinalFrame.length) {
                object.FinalFrame = [];
                for (let j = 0; j < message.FinalFrame.length; ++j)
                    object.FinalFrame[j] = $root.jljc.ColumnM.toObject(message.FinalFrame[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jljc.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.PotList && message.PotList.length) {
                object.PotList = [];
                for (let j = 0; j < message.PotList.length; ++j)
                    object.PotList[j] = $root.jljc.PotSymbol.toObject(message.PotList[j], options);
            }
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                object.BonusRemainRound = message.BonusRemainRound;
            if (message.BonusTotalRound != null && message.hasOwnProperty("BonusTotalRound"))
                object.BonusTotalRound = message.BonusTotalRound;
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                object.GetBonus = message.GetBonus;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jljc.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jljc.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljc.RoundInfo";
        };

        return RoundInfo;
    })();

    jljc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jljc
         * @interface ISpinAck
         * @property {Array.<jljc.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jljc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jljc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jljc.IRoundInfo>} RoundQueue
         * @memberof jljc.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jljc.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jljc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jljc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jljc.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jljc.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jljc.SpinAck
         * @static
         * @param {jljc.ISpinAck=} [properties] Properties to set
         * @returns {jljc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jljc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jljc.SpinAck
         * @static
         * @param {jljc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jljc.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jljc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljc.SpinAck
         * @static
         * @param {jljc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jljc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jljc.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.RTP = reader.double();
                        break;
                    }
                case 5: {
                        message.LID = reader.int32();
                        break;
                    }
                case 6: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jljc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jljc.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jljc.SpinAck)
                return object;
            let message = new $root.jljc.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jljc.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jljc.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jljc.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljc.SpinAck
         * @static
         * @param {jljc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jljc.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jljc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jljc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljc.SpinAck";
        };

        return SpinAck;
    })();

    return jljc;
})();

export const jljj = $root.jljj = (() => {

    /**
     * Namespace jljj.
     * @exports jljj
     * @namespace
     */
    const jljj = {};

    jljj.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jljj
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jljj
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jljj.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jljj.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jljj.Column
         * @static
         * @param {jljj.IColumn=} [properties] Properties to set
         * @returns {jljj.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jljj.Column.verify|verify} messages.
         * @function encode
         * @memberof jljj.Column
         * @static
         * @param {jljj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jljj.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.Column
         * @static
         * @param {jljj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jljj.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.Column)
                return object;
            let message = new $root.jljj.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jljj.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.Column
         * @static
         * @param {jljj.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jljj.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jljj.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.Column";
        };

        return Column;
    })();

    jljj.Single = (function() {

        /**
         * Properties of a Single.
         * @memberof jljj
         * @interface ISingle
         * @property {Array.<jljj.IColumn>|null} [Col] Single Col
         */

        /**
         * Constructs a new Single.
         * @memberof jljj
         * @classdesc Represents a Single.
         * @implements ISingle
         * @constructor
         * @param {jljj.ISingle=} [properties] Properties to set
         */
        function Single(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Single Col.
         * @member {Array.<jljj.IColumn>} Col
         * @memberof jljj.Single
         * @instance
         */
        Single.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Single instance using the specified properties.
         * @function create
         * @memberof jljj.Single
         * @static
         * @param {jljj.ISingle=} [properties] Properties to set
         * @returns {jljj.Single} Single instance
         */
        Single.create = function create(properties) {
            return new Single(properties);
        };

        /**
         * Encodes the specified Single message. Does not implicitly {@link jljj.Single.verify|verify} messages.
         * @function encode
         * @memberof jljj.Single
         * @static
         * @param {jljj.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jljj.Column.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Single message, length delimited. Does not implicitly {@link jljj.Single.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.Single
         * @static
         * @param {jljj.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Single message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.Single();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jljj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Single message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Single message.
         * @function verify
         * @memberof jljj.Single
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Single.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jljj.Column.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Single message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.Single
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.Single} Single
         */
        Single.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.Single)
                return object;
            let message = new $root.jljj.Single();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jljj.Single.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jljj.Single.Col: object expected");
                    message.Col[i] = $root.jljj.Column.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Single message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.Single
         * @static
         * @param {jljj.Single} message Single
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Single.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jljj.Column.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this Single to JSON.
         * @function toJSON
         * @memberof jljj.Single
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Single.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Single
         * @function getTypeUrl
         * @memberof jljj.Single
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Single.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.Single";
        };

        return Single;
    })();

    jljj.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jljj
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jljj
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jljj.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jljj.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jljj.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jljj.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jljj.AwardData
         * @static
         * @param {jljj.IAwardData=} [properties] Properties to set
         * @returns {jljj.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jljj.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jljj.AwardData
         * @static
         * @param {jljj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jljj.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.AwardData
         * @static
         * @param {jljj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jljj.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.AwardData)
                return object;
            let message = new $root.jljj.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.AwardData
         * @static
         * @param {jljj.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jljj.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jljj.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.AwardData";
        };

        return AwardData;
    })();

    jljj.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jljj
         * @interface ISpinAck
         * @property {Array.<jljj.ISingle>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<number>|null} [PlateMult] SpinAck PlateMult
         * @property {Array.<jljj.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [JpType] SpinAck JpType
         * @property {number|null} [JPMul] SpinAck JPMul
         * @property {number|null} [JPWin] SpinAck JPWin
         * @property {number|null} [Mult] SpinAck Mult
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {boolean|null} [CanBuy] SpinAck CanBuy
         * @property {number|null} [CanBuyRate] SpinAck CanBuyRate
         * @property {number|null} [ReSpinSymbol] SpinAck ReSpinSymbol
         * @property {number|null} [ReSpinColumn] SpinAck ReSpinColumn
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [IsBuy] SpinAck IsBuy
         * @property {number|null} [BaseBet] SpinAck BaseBet
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jljj
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jljj.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.PlateMult = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jljj.ISingle>} PlateSymbol
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck PlateMult.
         * @member {Array.<number>} PlateMult
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateMult = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jljj.IAwardData>} AwardDataVec
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck JpType.
         * @member {number} JpType
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.JpType = 0;

        /**
         * SpinAck JPMul.
         * @member {number} JPMul
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.JPMul = 0;

        /**
         * SpinAck JPWin.
         * @member {number} JPWin
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.JPWin = 0;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck CanBuy.
         * @member {boolean} CanBuy
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.CanBuy = false;

        /**
         * SpinAck CanBuyRate.
         * @member {number} CanBuyRate
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.CanBuyRate = 0;

        /**
         * SpinAck ReSpinSymbol.
         * @member {number} ReSpinSymbol
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.ReSpinSymbol = 0;

        /**
         * SpinAck ReSpinColumn.
         * @member {number} ReSpinColumn
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.ReSpinColumn = 0;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck IsBuy.
         * @member {boolean} IsBuy
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.IsBuy = false;

        /**
         * SpinAck BaseBet.
         * @member {number} BaseBet
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.BaseBet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jljj.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jljj.SpinAck
         * @static
         * @param {jljj.ISpinAck=} [properties] Properties to set
         * @returns {jljj.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jljj.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jljj.SpinAck
         * @static
         * @param {jljj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jljj.Single.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateMult != null && message.PlateMult.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.PlateMult.length; ++i)
                    writer.int32(message.PlateMult[i]);
                writer.ldelim();
            }
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jljj.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.JpType != null && Object.hasOwnProperty.call(message, "JpType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.JpType);
            if (message.JPMul != null && Object.hasOwnProperty.call(message, "JPMul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.JPMul);
            if (message.JPWin != null && Object.hasOwnProperty.call(message, "JPWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.JPWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Mult);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.Extra);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AwardTypeFlag);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.AckType);
            if (message.CanBuy != null && Object.hasOwnProperty.call(message, "CanBuy"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.CanBuy);
            if (message.CanBuyRate != null && Object.hasOwnProperty.call(message, "CanBuyRate"))
                writer.uint32(/* id 15, wireType 1 =*/121).double(message.CanBuyRate);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 16, wireType 1 =*/129).double(message.RTP);
            if (message.IsBuy != null && Object.hasOwnProperty.call(message, "IsBuy"))
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.IsBuy);
            if (message.ReSpinSymbol != null && Object.hasOwnProperty.call(message, "ReSpinSymbol"))
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.ReSpinSymbol);
            if (message.ReSpinColumn != null && Object.hasOwnProperty.call(message, "ReSpinColumn"))
                writer.uint32(/* id 19, wireType 0 =*/152).int32(message.ReSpinColumn);
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 20, wireType 1 =*/161).double(message.BaseBet);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 22, wireType 0 =*/176).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jljj.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.SpinAck
         * @static
         * @param {jljj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jljj.Single.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateMult && message.PlateMult.length))
                            message.PlateMult = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateMult.push(reader.int32());
                        } else
                            message.PlateMult.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jljj.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.JpType = reader.int32();
                        break;
                    }
                case 5: {
                        message.JPMul = reader.double();
                        break;
                    }
                case 6: {
                        message.JPWin = reader.double();
                        break;
                    }
                case 7: {
                        message.Mult = reader.double();
                        break;
                    }
                case 8: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 14: {
                        message.CanBuy = reader.bool();
                        break;
                    }
                case 15: {
                        message.CanBuyRate = reader.double();
                        break;
                    }
                case 18: {
                        message.ReSpinSymbol = reader.int32();
                        break;
                    }
                case 19: {
                        message.ReSpinColumn = reader.int32();
                        break;
                    }
                case 9: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 10: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 11: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 12: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 13: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 17: {
                        message.IsBuy = reader.bool();
                        break;
                    }
                case 20: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 16: {
                        message.RTP = reader.double();
                        break;
                    }
                case 21: {
                        message.LID = reader.int32();
                        break;
                    }
                case 22: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jljj.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jljj.Single.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateMult != null && message.hasOwnProperty("PlateMult")) {
                if (!Array.isArray(message.PlateMult))
                    return "PlateMult: array expected";
                for (let i = 0; i < message.PlateMult.length; ++i)
                    if (!$util.isInteger(message.PlateMult[i]))
                        return "PlateMult: integer[] expected";
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jljj.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.JpType != null && message.hasOwnProperty("JpType"))
                if (!$util.isInteger(message.JpType))
                    return "JpType: integer expected";
            if (message.JPMul != null && message.hasOwnProperty("JPMul"))
                if (typeof message.JPMul !== "number")
                    return "JPMul: number expected";
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                if (typeof message.JPWin !== "number")
                    return "JPWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.CanBuy != null && message.hasOwnProperty("CanBuy"))
                if (typeof message.CanBuy !== "boolean")
                    return "CanBuy: boolean expected";
            if (message.CanBuyRate != null && message.hasOwnProperty("CanBuyRate"))
                if (typeof message.CanBuyRate !== "number")
                    return "CanBuyRate: number expected";
            if (message.ReSpinSymbol != null && message.hasOwnProperty("ReSpinSymbol"))
                if (!$util.isInteger(message.ReSpinSymbol))
                    return "ReSpinSymbol: integer expected";
            if (message.ReSpinColumn != null && message.hasOwnProperty("ReSpinColumn"))
                if (!$util.isInteger(message.ReSpinColumn))
                    return "ReSpinColumn: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.IsBuy != null && message.hasOwnProperty("IsBuy"))
                if (typeof message.IsBuy !== "boolean")
                    return "IsBuy: boolean expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.SpinAck)
                return object;
            let message = new $root.jljj.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jljj.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jljj.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jljj.Single.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateMult) {
                if (!Array.isArray(object.PlateMult))
                    throw TypeError(".jljj.SpinAck.PlateMult: array expected");
                message.PlateMult = [];
                for (let i = 0; i < object.PlateMult.length; ++i)
                    message.PlateMult[i] = object.PlateMult[i] | 0;
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jljj.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jljj.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jljj.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.JpType != null)
                message.JpType = object.JpType | 0;
            if (object.JPMul != null)
                message.JPMul = Number(object.JPMul);
            if (object.JPWin != null)
                message.JPWin = Number(object.JPWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.CanBuy != null)
                message.CanBuy = Boolean(object.CanBuy);
            if (object.CanBuyRate != null)
                message.CanBuyRate = Number(object.CanBuyRate);
            if (object.ReSpinSymbol != null)
                message.ReSpinSymbol = object.ReSpinSymbol | 0;
            if (object.ReSpinColumn != null)
                message.ReSpinColumn = object.ReSpinColumn | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.IsBuy != null)
                message.IsBuy = Boolean(object.IsBuy);
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.SpinAck
         * @static
         * @param {jljj.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateMult = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.JpType = 0;
                object.JPMul = 0;
                object.JPWin = 0;
                object.Mult = 0;
                object.Extra = false;
                object.AwardTypeFlag = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.CanBuy = false;
                object.CanBuyRate = 0;
                object.RTP = 0;
                object.IsBuy = false;
                object.ReSpinSymbol = 0;
                object.ReSpinColumn = 0;
                object.BaseBet = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jljj.Single.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateMult && message.PlateMult.length) {
                object.PlateMult = [];
                for (let j = 0; j < message.PlateMult.length; ++j)
                    object.PlateMult[j] = message.PlateMult[j];
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jljj.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.JpType != null && message.hasOwnProperty("JpType"))
                object.JpType = message.JpType;
            if (message.JPMul != null && message.hasOwnProperty("JPMul"))
                object.JPMul = options.json && !isFinite(message.JPMul) ? String(message.JPMul) : message.JPMul;
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                object.JPWin = options.json && !isFinite(message.JPWin) ? String(message.JPWin) : message.JPWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.CanBuy != null && message.hasOwnProperty("CanBuy"))
                object.CanBuy = message.CanBuy;
            if (message.CanBuyRate != null && message.hasOwnProperty("CanBuyRate"))
                object.CanBuyRate = options.json && !isFinite(message.CanBuyRate) ? String(message.CanBuyRate) : message.CanBuyRate;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.IsBuy != null && message.hasOwnProperty("IsBuy"))
                object.IsBuy = message.IsBuy;
            if (message.ReSpinSymbol != null && message.hasOwnProperty("ReSpinSymbol"))
                object.ReSpinSymbol = message.ReSpinSymbol;
            if (message.ReSpinColumn != null && message.hasOwnProperty("ReSpinColumn"))
                object.ReSpinColumn = message.ReSpinColumn;
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jljj.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jljj.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.SpinAck";
        };

        return SpinAck;
    })();

    jljj.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jljj
         * @interface IGameInfoData
         * @property {Array.<number>|null} [BuyMul] GameInfoData BuyMul
         * @property {Array.<jljj.ISingleRange>|null} [ShowRange] GameInfoData ShowRange
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jljj
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jljj.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.BuyMul = [];
            this.ShowRange = [];
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData BuyMul.
         * @member {Array.<number>} BuyMul
         * @memberof jljj.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BuyMul = $util.emptyArray;

        /**
         * GameInfoData ShowRange.
         * @member {Array.<jljj.ISingleRange>} ShowRange
         * @memberof jljj.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowRange = $util.emptyArray;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jljj.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jljj.GameInfoData
         * @static
         * @param {jljj.IGameInfoData=} [properties] Properties to set
         * @returns {jljj.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jljj.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jljj.GameInfoData
         * @static
         * @param {jljj.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BuyMul != null && message.BuyMul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.BuyMul.length; ++i)
                    writer.double(message.BuyMul[i]);
                writer.ldelim();
            }
            if (message.ShowRange != null && message.ShowRange.length)
                for (let i = 0; i < message.ShowRange.length; ++i)
                    $root.jljj.SingleRange.encode(message.ShowRange[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jljj.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.GameInfoData
         * @static
         * @param {jljj.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BuyMul && message.BuyMul.length))
                            message.BuyMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BuyMul.push(reader.double());
                        } else
                            message.BuyMul.push(reader.double());
                        break;
                    }
                case 2: {
                        if (!(message.ShowRange && message.ShowRange.length))
                            message.ShowRange = [];
                        message.ShowRange.push($root.jljj.SingleRange.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jljj.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul")) {
                if (!Array.isArray(message.BuyMul))
                    return "BuyMul: array expected";
                for (let i = 0; i < message.BuyMul.length; ++i)
                    if (typeof message.BuyMul[i] !== "number")
                        return "BuyMul: number[] expected";
            }
            if (message.ShowRange != null && message.hasOwnProperty("ShowRange")) {
                if (!Array.isArray(message.ShowRange))
                    return "ShowRange: array expected";
                for (let i = 0; i < message.ShowRange.length; ++i) {
                    let error = $root.jljj.SingleRange.verify(message.ShowRange[i]);
                    if (error)
                        return "ShowRange." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.GameInfoData)
                return object;
            let message = new $root.jljj.GameInfoData();
            if (object.BuyMul) {
                if (!Array.isArray(object.BuyMul))
                    throw TypeError(".jljj.GameInfoData.BuyMul: array expected");
                message.BuyMul = [];
                for (let i = 0; i < object.BuyMul.length; ++i)
                    message.BuyMul[i] = Number(object.BuyMul[i]);
            }
            if (object.ShowRange) {
                if (!Array.isArray(object.ShowRange))
                    throw TypeError(".jljj.GameInfoData.ShowRange: array expected");
                message.ShowRange = [];
                for (let i = 0; i < object.ShowRange.length; ++i) {
                    if (typeof object.ShowRange[i] !== "object")
                        throw TypeError(".jljj.GameInfoData.ShowRange: object expected");
                    message.ShowRange[i] = $root.jljj.SingleRange.fromObject(object.ShowRange[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jljj.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.GameInfoData
         * @static
         * @param {jljj.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.BuyMul = [];
                object.ShowRange = [];
                object.Mul = [];
            }
            if (message.BuyMul && message.BuyMul.length) {
                object.BuyMul = [];
                for (let j = 0; j < message.BuyMul.length; ++j)
                    object.BuyMul[j] = options.json && !isFinite(message.BuyMul[j]) ? String(message.BuyMul[j]) : message.BuyMul[j];
            }
            if (message.ShowRange && message.ShowRange.length) {
                object.ShowRange = [];
                for (let j = 0; j < message.ShowRange.length; ++j)
                    object.ShowRange[j] = $root.jljj.SingleRange.toObject(message.ShowRange[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jljj.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jljj.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.GameInfoData";
        };

        return GameInfoData;
    })();

    jljj.SingleRange = (function() {

        /**
         * Properties of a SingleRange.
         * @memberof jljj
         * @interface ISingleRange
         * @property {number|null} [Min] SingleRange Min
         * @property {number|null} [Max] SingleRange Max
         */

        /**
         * Constructs a new SingleRange.
         * @memberof jljj
         * @classdesc Represents a SingleRange.
         * @implements ISingleRange
         * @constructor
         * @param {jljj.ISingleRange=} [properties] Properties to set
         */
        function SingleRange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleRange Min.
         * @member {number} Min
         * @memberof jljj.SingleRange
         * @instance
         */
        SingleRange.prototype.Min = 0;

        /**
         * SingleRange Max.
         * @member {number} Max
         * @memberof jljj.SingleRange
         * @instance
         */
        SingleRange.prototype.Max = 0;

        /**
         * Creates a new SingleRange instance using the specified properties.
         * @function create
         * @memberof jljj.SingleRange
         * @static
         * @param {jljj.ISingleRange=} [properties] Properties to set
         * @returns {jljj.SingleRange} SingleRange instance
         */
        SingleRange.create = function create(properties) {
            return new SingleRange(properties);
        };

        /**
         * Encodes the specified SingleRange message. Does not implicitly {@link jljj.SingleRange.verify|verify} messages.
         * @function encode
         * @memberof jljj.SingleRange
         * @static
         * @param {jljj.ISingleRange} message SingleRange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleRange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Min != null && Object.hasOwnProperty.call(message, "Min"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Min);
            if (message.Max != null && Object.hasOwnProperty.call(message, "Max"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Max);
            return writer;
        };

        /**
         * Encodes the specified SingleRange message, length delimited. Does not implicitly {@link jljj.SingleRange.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.SingleRange
         * @static
         * @param {jljj.ISingleRange} message SingleRange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleRange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleRange message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.SingleRange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.SingleRange} SingleRange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleRange.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.SingleRange();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Min = reader.double();
                        break;
                    }
                case 2: {
                        message.Max = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleRange message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.SingleRange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.SingleRange} SingleRange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleRange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleRange message.
         * @function verify
         * @memberof jljj.SingleRange
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleRange.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Min != null && message.hasOwnProperty("Min"))
                if (typeof message.Min !== "number")
                    return "Min: number expected";
            if (message.Max != null && message.hasOwnProperty("Max"))
                if (typeof message.Max !== "number")
                    return "Max: number expected";
            return null;
        };

        /**
         * Creates a SingleRange message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.SingleRange
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.SingleRange} SingleRange
         */
        SingleRange.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.SingleRange)
                return object;
            let message = new $root.jljj.SingleRange();
            if (object.Min != null)
                message.Min = Number(object.Min);
            if (object.Max != null)
                message.Max = Number(object.Max);
            return message;
        };

        /**
         * Creates a plain object from a SingleRange message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.SingleRange
         * @static
         * @param {jljj.SingleRange} message SingleRange
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleRange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Min = 0;
                object.Max = 0;
            }
            if (message.Min != null && message.hasOwnProperty("Min"))
                object.Min = options.json && !isFinite(message.Min) ? String(message.Min) : message.Min;
            if (message.Max != null && message.hasOwnProperty("Max"))
                object.Max = options.json && !isFinite(message.Max) ? String(message.Max) : message.Max;
            return object;
        };

        /**
         * Converts this SingleRange to JSON.
         * @function toJSON
         * @memberof jljj.SingleRange
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleRange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleRange
         * @function getTypeUrl
         * @memberof jljj.SingleRange
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleRange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.SingleRange";
        };

        return SingleRange;
    })();

    jljj.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jljj
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jljj
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jljj.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jljj.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jljj.BuyReq
         * @static
         * @param {jljj.IBuyReq=} [properties] Properties to set
         * @returns {jljj.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jljj.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jljj.BuyReq
         * @static
         * @param {jljj.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jljj.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.BuyReq
         * @static
         * @param {jljj.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jljj.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.BuyReq)
                return object;
            let message = new $root.jljj.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.BuyReq
         * @static
         * @param {jljj.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.BaseBet = 0;
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jljj.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jljj.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.BuyReq";
        };

        return BuyReq;
    })();

    jljj.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jljj
         * @interface IStateInfo
         * @property {Array.<jljj.IColumn>|null} [Symbol] StateInfo Symbol
         * @property {Array.<number>|null} [Mul] StateInfo Mul
         * @property {number|null} [Buymul] StateInfo Buymul
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jljj
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jljj.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo Symbol.
         * @member {Array.<jljj.IColumn>} Symbol
         * @memberof jljj.StateInfo
         * @instance
         */
        StateInfo.prototype.Symbol = $util.emptyArray;

        /**
         * StateInfo Mul.
         * @member {Array.<number>} Mul
         * @memberof jljj.StateInfo
         * @instance
         */
        StateInfo.prototype.Mul = $util.emptyArray;

        /**
         * StateInfo Buymul.
         * @member {number} Buymul
         * @memberof jljj.StateInfo
         * @instance
         */
        StateInfo.prototype.Buymul = 0;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jljj.StateInfo
         * @static
         * @param {jljj.IStateInfo=} [properties] Properties to set
         * @returns {jljj.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jljj.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jljj.StateInfo
         * @static
         * @param {jljj.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jljj.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.int32(message.Mul[i]);
                writer.ldelim();
            }
            if (message.Buymul != null && Object.hasOwnProperty.call(message, "Buymul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Buymul);
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jljj.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljj.StateInfo
         * @static
         * @param {jljj.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jljj.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljj.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljj.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jljj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.int32());
                        } else
                            message.Mul.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.Buymul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljj.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljj.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jljj.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jljj.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (!$util.isInteger(message.Mul[i]))
                        return "Mul: integer[] expected";
            }
            if (message.Buymul != null && message.hasOwnProperty("Buymul"))
                if (typeof message.Buymul !== "number")
                    return "Buymul: number expected";
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljj.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljj.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jljj.StateInfo)
                return object;
            let message = new $root.jljj.StateInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jljj.StateInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jljj.StateInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jljj.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jljj.StateInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = object.Mul[i] | 0;
            }
            if (object.Buymul != null)
                message.Buymul = Number(object.Buymul);
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljj.StateInfo
         * @static
         * @param {jljj.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
            }
            if (options.defaults)
                object.Buymul = 0;
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jljj.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = message.Mul[j];
            }
            if (message.Buymul != null && message.hasOwnProperty("Buymul"))
                object.Buymul = options.json && !isFinite(message.Buymul) ? String(message.Buymul) : message.Buymul;
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jljj.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jljj.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljj.StateInfo";
        };

        return StateInfo;
    })();

    return jljj;
})();

export const jljjf = $root.jljjf = (() => {

    /**
     * Namespace jljjf.
     * @exports jljjf
     * @namespace
     */
    const jljjf = {};

    jljjf.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jljjf
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {Array.<number>|null} [Pos] AwardData Pos
         * @property {boolean|null} [IsAll] AwardData IsAll
         */

        /**
         * Constructs a new AwardData.
         * @memberof jljjf
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jljjf.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jljjf.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jljjf.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jljjf.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Pos.
         * @member {Array.<number>} Pos
         * @memberof jljjf.AwardData
         * @instance
         */
        AwardData.prototype.Pos = $util.emptyArray;

        /**
         * AwardData IsAll.
         * @member {boolean} IsAll
         * @memberof jljjf.AwardData
         * @instance
         */
        AwardData.prototype.IsAll = false;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jljjf.AwardData
         * @static
         * @param {jljjf.IAwardData=} [properties] Properties to set
         * @returns {jljjf.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jljjf.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jljjf.AwardData
         * @static
         * @param {jljjf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Win);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Pos != null && message.Pos.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.Pos.length; ++i)
                    writer.int32(message.Pos[i]);
                writer.ldelim();
            }
            if (message.IsAll != null && Object.hasOwnProperty.call(message, "IsAll"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.IsAll);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jljjf.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.AwardData
         * @static
         * @param {jljjf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Win = reader.double();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.Pos && message.Pos.length))
                            message.Pos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pos.push(reader.int32());
                        } else
                            message.Pos.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.IsAll = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jljjf.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Pos != null && message.hasOwnProperty("Pos")) {
                if (!Array.isArray(message.Pos))
                    return "Pos: array expected";
                for (let i = 0; i < message.Pos.length; ++i)
                    if (!$util.isInteger(message.Pos[i]))
                        return "Pos: integer[] expected";
            }
            if (message.IsAll != null && message.hasOwnProperty("IsAll"))
                if (typeof message.IsAll !== "boolean")
                    return "IsAll: boolean expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.AwardData)
                return object;
            let message = new $root.jljjf.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Pos) {
                if (!Array.isArray(object.Pos))
                    throw TypeError(".jljjf.AwardData.Pos: array expected");
                message.Pos = [];
                for (let i = 0; i < object.Pos.length; ++i)
                    message.Pos[i] = object.Pos[i] | 0;
            }
            if (object.IsAll != null)
                message.IsAll = Boolean(object.IsAll);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.AwardData
         * @static
         * @param {jljjf.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Pos = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Win = 0;
                object.LineNum = 0;
                object.IsAll = false;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Pos && message.Pos.length) {
                object.Pos = [];
                for (let j = 0; j < message.Pos.length; ++j)
                    object.Pos[j] = message.Pos[j];
            }
            if (message.IsAll != null && message.hasOwnProperty("IsAll"))
                object.IsAll = message.IsAll;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jljjf.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jljjf.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.AwardData";
        };

        return AwardData;
    })();

    jljjf.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jljjf
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jljjf
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jljjf.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jljjf.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jljjf.Column
         * @static
         * @param {jljjf.IColumn=} [properties] Properties to set
         * @returns {jljjf.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jljjf.Column.verify|verify} messages.
         * @function encode
         * @memberof jljjf.Column
         * @static
         * @param {jljjf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jljjf.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.Column
         * @static
         * @param {jljjf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jljjf.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.Column)
                return object;
            let message = new $root.jljjf.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jljjf.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.Column
         * @static
         * @param {jljjf.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jljjf.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jljjf.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.Column";
        };

        return Column;
    })();

    jljjf.SingleRange = (function() {

        /**
         * Properties of a SingleRange.
         * @memberof jljjf
         * @interface ISingleRange
         * @property {number|null} [Min] SingleRange Min
         * @property {number|null} [Max] SingleRange Max
         */

        /**
         * Constructs a new SingleRange.
         * @memberof jljjf
         * @classdesc Represents a SingleRange.
         * @implements ISingleRange
         * @constructor
         * @param {jljjf.ISingleRange=} [properties] Properties to set
         */
        function SingleRange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleRange Min.
         * @member {number} Min
         * @memberof jljjf.SingleRange
         * @instance
         */
        SingleRange.prototype.Min = 0;

        /**
         * SingleRange Max.
         * @member {number} Max
         * @memberof jljjf.SingleRange
         * @instance
         */
        SingleRange.prototype.Max = 0;

        /**
         * Creates a new SingleRange instance using the specified properties.
         * @function create
         * @memberof jljjf.SingleRange
         * @static
         * @param {jljjf.ISingleRange=} [properties] Properties to set
         * @returns {jljjf.SingleRange} SingleRange instance
         */
        SingleRange.create = function create(properties) {
            return new SingleRange(properties);
        };

        /**
         * Encodes the specified SingleRange message. Does not implicitly {@link jljjf.SingleRange.verify|verify} messages.
         * @function encode
         * @memberof jljjf.SingleRange
         * @static
         * @param {jljjf.ISingleRange} message SingleRange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleRange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Min != null && Object.hasOwnProperty.call(message, "Min"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Min);
            if (message.Max != null && Object.hasOwnProperty.call(message, "Max"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Max);
            return writer;
        };

        /**
         * Encodes the specified SingleRange message, length delimited. Does not implicitly {@link jljjf.SingleRange.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.SingleRange
         * @static
         * @param {jljjf.ISingleRange} message SingleRange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleRange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleRange message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.SingleRange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.SingleRange} SingleRange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleRange.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.SingleRange();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Min = reader.double();
                        break;
                    }
                case 2: {
                        message.Max = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleRange message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.SingleRange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.SingleRange} SingleRange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleRange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleRange message.
         * @function verify
         * @memberof jljjf.SingleRange
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleRange.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Min != null && message.hasOwnProperty("Min"))
                if (typeof message.Min !== "number")
                    return "Min: number expected";
            if (message.Max != null && message.hasOwnProperty("Max"))
                if (typeof message.Max !== "number")
                    return "Max: number expected";
            return null;
        };

        /**
         * Creates a SingleRange message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.SingleRange
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.SingleRange} SingleRange
         */
        SingleRange.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.SingleRange)
                return object;
            let message = new $root.jljjf.SingleRange();
            if (object.Min != null)
                message.Min = Number(object.Min);
            if (object.Max != null)
                message.Max = Number(object.Max);
            return message;
        };

        /**
         * Creates a plain object from a SingleRange message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.SingleRange
         * @static
         * @param {jljjf.SingleRange} message SingleRange
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleRange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Min = 0;
                object.Max = 0;
            }
            if (message.Min != null && message.hasOwnProperty("Min"))
                object.Min = options.json && !isFinite(message.Min) ? String(message.Min) : message.Min;
            if (message.Max != null && message.hasOwnProperty("Max"))
                object.Max = options.json && !isFinite(message.Max) ? String(message.Max) : message.Max;
            return object;
        };

        /**
         * Converts this SingleRange to JSON.
         * @function toJSON
         * @memberof jljjf.SingleRange
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleRange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleRange
         * @function getTypeUrl
         * @memberof jljjf.SingleRange
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleRange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.SingleRange";
        };

        return SingleRange;
    })();

    jljjf.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jljjf
         * @interface IRoundInfo
         * @property {Array.<jljjf.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {Array.<number>|null} [UpMult] RoundInfo UpMult
         * @property {Array.<jljjf.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [JpType] RoundInfo JpType
         * @property {number|null} [JPMul] RoundInfo JPMul
         * @property {number|null} [JPWin] RoundInfo JPWin
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {Array.<number>|null} [BuyMul] RoundInfo BuyMul
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jljjf
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jljjf.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.UpMult = [];
            this.AwardDataVec = [];
            this.BuyMul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jljjf.IColumn>} Symbol
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo UpMult.
         * @member {Array.<number>} UpMult
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.UpMult = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jljjf.IAwardData>} AwardDataVec
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo JpType.
         * @member {number} JpType
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.JpType = 0;

        /**
         * RoundInfo JPMul.
         * @member {number} JPMul
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.JPMul = 0;

        /**
         * RoundInfo JPWin.
         * @member {number} JPWin
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.JPWin = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo BuyMul.
         * @member {Array.<number>} BuyMul
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BuyMul = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jljjf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jljjf.RoundInfo
         * @static
         * @param {jljjf.IRoundInfo=} [properties] Properties to set
         * @returns {jljjf.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jljjf.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jljjf.RoundInfo
         * @static
         * @param {jljjf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jljjf.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.UpMult != null && message.UpMult.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.UpMult.length; ++i)
                    writer.int32(message.UpMult[i]);
                writer.ldelim();
            }
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jljjf.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RoundWin);
            if (message.JpType != null && Object.hasOwnProperty.call(message, "JpType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.JpType);
            if (message.JPMul != null && Object.hasOwnProperty.call(message, "JPMul"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.JPMul);
            if (message.JPWin != null && Object.hasOwnProperty.call(message, "JPWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.JPWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Mult);
            if (message.BuyMul != null && message.BuyMul.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.BuyMul.length; ++i)
                    writer.double(message.BuyMul[i]);
                writer.ldelim();
            }
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jljjf.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.RoundInfo
         * @static
         * @param {jljjf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jljjf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.UpMult && message.UpMult.length))
                            message.UpMult = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.UpMult.push(reader.int32());
                        } else
                            message.UpMult.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jljjf.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 5: {
                        message.JpType = reader.int32();
                        break;
                    }
                case 6: {
                        message.JPMul = reader.double();
                        break;
                    }
                case 7: {
                        message.JPWin = reader.double();
                        break;
                    }
                case 8: {
                        message.Mult = reader.double();
                        break;
                    }
                case 9: {
                        if (!(message.BuyMul && message.BuyMul.length))
                            message.BuyMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BuyMul.push(reader.double());
                        } else
                            message.BuyMul.push(reader.double());
                        break;
                    }
                case 10: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 11: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jljjf.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jljjf.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.UpMult != null && message.hasOwnProperty("UpMult")) {
                if (!Array.isArray(message.UpMult))
                    return "UpMult: array expected";
                for (let i = 0; i < message.UpMult.length; ++i)
                    if (!$util.isInteger(message.UpMult[i]))
                        return "UpMult: integer[] expected";
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jljjf.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.JpType != null && message.hasOwnProperty("JpType"))
                if (!$util.isInteger(message.JpType))
                    return "JpType: integer expected";
            if (message.JPMul != null && message.hasOwnProperty("JPMul"))
                if (typeof message.JPMul !== "number")
                    return "JPMul: number expected";
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                if (typeof message.JPWin !== "number")
                    return "JPWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul")) {
                if (!Array.isArray(message.BuyMul))
                    return "BuyMul: array expected";
                for (let i = 0; i < message.BuyMul.length; ++i)
                    if (typeof message.BuyMul[i] !== "number")
                        return "BuyMul: number[] expected";
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.RoundInfo)
                return object;
            let message = new $root.jljjf.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jljjf.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jljjf.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jljjf.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.UpMult) {
                if (!Array.isArray(object.UpMult))
                    throw TypeError(".jljjf.RoundInfo.UpMult: array expected");
                message.UpMult = [];
                for (let i = 0; i < object.UpMult.length; ++i)
                    message.UpMult[i] = object.UpMult[i] | 0;
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jljjf.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jljjf.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jljjf.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.JpType != null)
                message.JpType = object.JpType | 0;
            if (object.JPMul != null)
                message.JPMul = Number(object.JPMul);
            if (object.JPWin != null)
                message.JPWin = Number(object.JPWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.BuyMul) {
                if (!Array.isArray(object.BuyMul))
                    throw TypeError(".jljjf.RoundInfo.BuyMul: array expected");
                message.BuyMul = [];
                for (let i = 0; i < object.BuyMul.length; ++i)
                    message.BuyMul[i] = Number(object.BuyMul[i]);
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.RoundInfo
         * @static
         * @param {jljjf.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.UpMult = [];
                object.AwardDataVec = [];
                object.BuyMul = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.JpType = 0;
                object.JPMul = 0;
                object.JPWin = 0;
                object.Mult = 0;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jljjf.Column.toObject(message.Symbol[j], options);
            }
            if (message.UpMult && message.UpMult.length) {
                object.UpMult = [];
                for (let j = 0; j < message.UpMult.length; ++j)
                    object.UpMult[j] = message.UpMult[j];
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jljjf.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.JpType != null && message.hasOwnProperty("JpType"))
                object.JpType = message.JpType;
            if (message.JPMul != null && message.hasOwnProperty("JPMul"))
                object.JPMul = options.json && !isFinite(message.JPMul) ? String(message.JPMul) : message.JPMul;
            if (message.JPWin != null && message.hasOwnProperty("JPWin"))
                object.JPWin = options.json && !isFinite(message.JPWin) ? String(message.JPWin) : message.JPWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.BuyMul && message.BuyMul.length) {
                object.BuyMul = [];
                for (let j = 0; j < message.BuyMul.length; ++j)
                    object.BuyMul[j] = options.json && !isFinite(message.BuyMul[j]) ? String(message.BuyMul[j]) : message.BuyMul[j];
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jljjf.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jljjf.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.RoundInfo";
        };

        return RoundInfo;
    })();

    jljjf.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jljjf
         * @interface ISpinAck
         * @property {Array.<jljjf.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [GameType] SpinAck GameType
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [Extra] SpinAck Extra
         * @property {boolean|null} [IsBuy] SpinAck IsBuy
         * @property {number|null} [BaseBet] SpinAck BaseBet
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jljjf
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jljjf.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jljjf.IRoundInfo>} RoundQueue
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck GameType.
         * @member {number} GameType
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.GameType = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck Extra.
         * @member {number} Extra
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = 0;

        /**
         * SpinAck IsBuy.
         * @member {boolean} IsBuy
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.IsBuy = false;

        /**
         * SpinAck BaseBet.
         * @member {number} BaseBet
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.BaseBet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jljjf.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jljjf.SpinAck
         * @static
         * @param {jljjf.ISpinAck=} [properties] Properties to set
         * @returns {jljjf.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jljjf.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jljjf.SpinAck
         * @static
         * @param {jljjf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jljjf.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.GameType);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Extra);
            if (message.IsBuy != null && Object.hasOwnProperty.call(message, "IsBuy"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.IsBuy);
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.BaseBet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jljjf.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.SpinAck
         * @static
         * @param {jljjf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jljjf.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.Extra = reader.int32();
                        break;
                    }
                case 5: {
                        message.IsBuy = reader.bool();
                        break;
                    }
                case 6: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jljjf.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jljjf.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            if (message.IsBuy != null && message.hasOwnProperty("IsBuy"))
                if (typeof message.IsBuy !== "boolean")
                    return "IsBuy: boolean expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.SpinAck)
                return object;
            let message = new $root.jljjf.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jljjf.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jljjf.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jljjf.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            if (object.IsBuy != null)
                message.IsBuy = Boolean(object.IsBuy);
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.SpinAck
         * @static
         * @param {jljjf.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.GameType = 0;
                object.TotalWin = 0;
                object.Extra = 0;
                object.IsBuy = false;
                object.BaseBet = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jljjf.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.IsBuy != null && message.hasOwnProperty("IsBuy"))
                object.IsBuy = message.IsBuy;
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jljjf.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jljjf.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.SpinAck";
        };

        return SpinAck;
    })();

    jljjf.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jljjf
         * @interface IGameInfoData
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<jljjf.ISingleRange>|null} [ShowRange] GameInfoData ShowRange
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jljjf
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jljjf.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.ShowRange = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jljjf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData ShowRange.
         * @member {Array.<jljjf.ISingleRange>} ShowRange
         * @memberof jljjf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowRange = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jljjf.GameInfoData
         * @static
         * @param {jljjf.IGameInfoData=} [properties] Properties to set
         * @returns {jljjf.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jljjf.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jljjf.GameInfoData
         * @static
         * @param {jljjf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.ShowRange != null && message.ShowRange.length)
                for (let i = 0; i < message.ShowRange.length; ++i)
                    $root.jljjf.SingleRange.encode(message.ShowRange[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jljjf.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.GameInfoData
         * @static
         * @param {jljjf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 2: {
                        if (!(message.ShowRange && message.ShowRange.length))
                            message.ShowRange = [];
                        message.ShowRange.push($root.jljjf.SingleRange.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jljjf.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.ShowRange != null && message.hasOwnProperty("ShowRange")) {
                if (!Array.isArray(message.ShowRange))
                    return "ShowRange: array expected";
                for (let i = 0; i < message.ShowRange.length; ++i) {
                    let error = $root.jljjf.SingleRange.verify(message.ShowRange[i]);
                    if (error)
                        return "ShowRange." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.GameInfoData)
                return object;
            let message = new $root.jljjf.GameInfoData();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jljjf.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.ShowRange) {
                if (!Array.isArray(object.ShowRange))
                    throw TypeError(".jljjf.GameInfoData.ShowRange: array expected");
                message.ShowRange = [];
                for (let i = 0; i < object.ShowRange.length; ++i) {
                    if (typeof object.ShowRange[i] !== "object")
                        throw TypeError(".jljjf.GameInfoData.ShowRange: object expected");
                    message.ShowRange[i] = $root.jljjf.SingleRange.fromObject(object.ShowRange[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.GameInfoData
         * @static
         * @param {jljjf.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.ShowRange = [];
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.ShowRange && message.ShowRange.length) {
                object.ShowRange = [];
                for (let j = 0; j < message.ShowRange.length; ++j)
                    object.ShowRange[j] = $root.jljjf.SingleRange.toObject(message.ShowRange[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jljjf.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jljjf.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.GameInfoData";
        };

        return GameInfoData;
    })();

    jljjf.BuyReq = (function() {

        /**
         * Properties of a BuyReq.
         * @memberof jljjf
         * @interface IBuyReq
         * @property {number|null} [BaseBet] BuyReq BaseBet
         * @property {number|null} [BuyMul] BuyReq BuyMul
         * @property {number|null} [TargetCol] BuyReq TargetCol
         */

        /**
         * Constructs a new BuyReq.
         * @memberof jljjf
         * @classdesc Represents a BuyReq.
         * @implements IBuyReq
         * @constructor
         * @param {jljjf.IBuyReq=} [properties] Properties to set
         */
        function BuyReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BuyReq BaseBet.
         * @member {number} BaseBet
         * @memberof jljjf.BuyReq
         * @instance
         */
        BuyReq.prototype.BaseBet = 0;

        /**
         * BuyReq BuyMul.
         * @member {number} BuyMul
         * @memberof jljjf.BuyReq
         * @instance
         */
        BuyReq.prototype.BuyMul = 0;

        /**
         * BuyReq TargetCol.
         * @member {number} TargetCol
         * @memberof jljjf.BuyReq
         * @instance
         */
        BuyReq.prototype.TargetCol = 0;

        /**
         * Creates a new BuyReq instance using the specified properties.
         * @function create
         * @memberof jljjf.BuyReq
         * @static
         * @param {jljjf.IBuyReq=} [properties] Properties to set
         * @returns {jljjf.BuyReq} BuyReq instance
         */
        BuyReq.create = function create(properties) {
            return new BuyReq(properties);
        };

        /**
         * Encodes the specified BuyReq message. Does not implicitly {@link jljjf.BuyReq.verify|verify} messages.
         * @function encode
         * @memberof jljjf.BuyReq
         * @static
         * @param {jljjf.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseBet != null && Object.hasOwnProperty.call(message, "BaseBet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.BaseBet);
            if (message.BuyMul != null && Object.hasOwnProperty.call(message, "BuyMul"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BuyMul);
            if (message.TargetCol != null && Object.hasOwnProperty.call(message, "TargetCol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.TargetCol);
            return writer;
        };

        /**
         * Encodes the specified BuyReq message, length delimited. Does not implicitly {@link jljjf.BuyReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.BuyReq
         * @static
         * @param {jljjf.IBuyReq} message BuyReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BuyReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.BuyReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseBet = reader.double();
                        break;
                    }
                case 2: {
                        message.BuyMul = reader.double();
                        break;
                    }
                case 3: {
                        message.TargetCol = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BuyReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.BuyReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.BuyReq} BuyReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BuyReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BuyReq message.
         * @function verify
         * @memberof jljjf.BuyReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BuyReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                if (typeof message.BaseBet !== "number")
                    return "BaseBet: number expected";
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul"))
                if (typeof message.BuyMul !== "number")
                    return "BuyMul: number expected";
            if (message.TargetCol != null && message.hasOwnProperty("TargetCol"))
                if (!$util.isInteger(message.TargetCol))
                    return "TargetCol: integer expected";
            return null;
        };

        /**
         * Creates a BuyReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.BuyReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.BuyReq} BuyReq
         */
        BuyReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.BuyReq)
                return object;
            let message = new $root.jljjf.BuyReq();
            if (object.BaseBet != null)
                message.BaseBet = Number(object.BaseBet);
            if (object.BuyMul != null)
                message.BuyMul = Number(object.BuyMul);
            if (object.TargetCol != null)
                message.TargetCol = object.TargetCol | 0;
            return message;
        };

        /**
         * Creates a plain object from a BuyReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.BuyReq
         * @static
         * @param {jljjf.BuyReq} message BuyReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BuyReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseBet = 0;
                object.BuyMul = 0;
                object.TargetCol = 0;
            }
            if (message.BaseBet != null && message.hasOwnProperty("BaseBet"))
                object.BaseBet = options.json && !isFinite(message.BaseBet) ? String(message.BaseBet) : message.BaseBet;
            if (message.BuyMul != null && message.hasOwnProperty("BuyMul"))
                object.BuyMul = options.json && !isFinite(message.BuyMul) ? String(message.BuyMul) : message.BuyMul;
            if (message.TargetCol != null && message.hasOwnProperty("TargetCol"))
                object.TargetCol = message.TargetCol;
            return object;
        };

        /**
         * Converts this BuyReq to JSON.
         * @function toJSON
         * @memberof jljjf.BuyReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BuyReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BuyReq
         * @function getTypeUrl
         * @memberof jljjf.BuyReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BuyReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.BuyReq";
        };

        return BuyReq;
    })();

    jljjf.ChangeReq = (function() {

        /**
         * Properties of a ChangeReq.
         * @memberof jljjf
         * @interface IChangeReq
         * @property {number|null} [TargetCol] ChangeReq TargetCol
         * @property {number|null} [Extra] ChangeReq Extra
         */

        /**
         * Constructs a new ChangeReq.
         * @memberof jljjf
         * @classdesc Represents a ChangeReq.
         * @implements IChangeReq
         * @constructor
         * @param {jljjf.IChangeReq=} [properties] Properties to set
         */
        function ChangeReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeReq TargetCol.
         * @member {number} TargetCol
         * @memberof jljjf.ChangeReq
         * @instance
         */
        ChangeReq.prototype.TargetCol = 0;

        /**
         * ChangeReq Extra.
         * @member {number} Extra
         * @memberof jljjf.ChangeReq
         * @instance
         */
        ChangeReq.prototype.Extra = 0;

        /**
         * Creates a new ChangeReq instance using the specified properties.
         * @function create
         * @memberof jljjf.ChangeReq
         * @static
         * @param {jljjf.IChangeReq=} [properties] Properties to set
         * @returns {jljjf.ChangeReq} ChangeReq instance
         */
        ChangeReq.create = function create(properties) {
            return new ChangeReq(properties);
        };

        /**
         * Encodes the specified ChangeReq message. Does not implicitly {@link jljjf.ChangeReq.verify|verify} messages.
         * @function encode
         * @memberof jljjf.ChangeReq
         * @static
         * @param {jljjf.IChangeReq} message ChangeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.TargetCol != null && Object.hasOwnProperty.call(message, "TargetCol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.TargetCol);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified ChangeReq message, length delimited. Does not implicitly {@link jljjf.ChangeReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.ChangeReq
         * @static
         * @param {jljjf.IChangeReq} message ChangeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChangeReq message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.ChangeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.ChangeReq} ChangeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.ChangeReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.TargetCol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Extra = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChangeReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.ChangeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.ChangeReq} ChangeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChangeReq message.
         * @function verify
         * @memberof jljjf.ChangeReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChangeReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.TargetCol != null && message.hasOwnProperty("TargetCol"))
                if (!$util.isInteger(message.TargetCol))
                    return "TargetCol: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            return null;
        };

        /**
         * Creates a ChangeReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.ChangeReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.ChangeReq} ChangeReq
         */
        ChangeReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.ChangeReq)
                return object;
            let message = new $root.jljjf.ChangeReq();
            if (object.TargetCol != null)
                message.TargetCol = object.TargetCol | 0;
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            return message;
        };

        /**
         * Creates a plain object from a ChangeReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.ChangeReq
         * @static
         * @param {jljjf.ChangeReq} message ChangeReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.TargetCol = 0;
                object.Extra = 0;
            }
            if (message.TargetCol != null && message.hasOwnProperty("TargetCol"))
                object.TargetCol = message.TargetCol;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this ChangeReq to JSON.
         * @function toJSON
         * @memberof jljjf.ChangeReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeReq
         * @function getTypeUrl
         * @memberof jljjf.ChangeReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.ChangeReq";
        };

        return ChangeReq;
    })();

    jljjf.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jljjf
         * @interface IStateInfo
         * @property {Array.<jljjf.IColumn>|null} [Symbol] StateInfo Symbol
         * @property {Array.<number>|null} [Mul] StateInfo Mul
         * @property {Array.<number>|null} [Buymul] StateInfo Buymul
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jljjf
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jljjf.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            this.Buymul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo Symbol.
         * @member {Array.<jljjf.IColumn>} Symbol
         * @memberof jljjf.StateInfo
         * @instance
         */
        StateInfo.prototype.Symbol = $util.emptyArray;

        /**
         * StateInfo Mul.
         * @member {Array.<number>} Mul
         * @memberof jljjf.StateInfo
         * @instance
         */
        StateInfo.prototype.Mul = $util.emptyArray;

        /**
         * StateInfo Buymul.
         * @member {Array.<number>} Buymul
         * @memberof jljjf.StateInfo
         * @instance
         */
        StateInfo.prototype.Buymul = $util.emptyArray;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jljjf.StateInfo
         * @static
         * @param {jljjf.IStateInfo=} [properties] Properties to set
         * @returns {jljjf.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jljjf.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jljjf.StateInfo
         * @static
         * @param {jljjf.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jljjf.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.int32(message.Mul[i]);
                writer.ldelim();
            }
            if (message.Buymul != null && message.Buymul.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.Buymul.length; ++i)
                    writer.double(message.Buymul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jljjf.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljjf.StateInfo
         * @static
         * @param {jljjf.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jljjf.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljjf.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljjf.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jljjf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.int32());
                        } else
                            message.Mul.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.Buymul && message.Buymul.length))
                            message.Buymul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Buymul.push(reader.double());
                        } else
                            message.Buymul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljjf.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljjf.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jljjf.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jljjf.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (!$util.isInteger(message.Mul[i]))
                        return "Mul: integer[] expected";
            }
            if (message.Buymul != null && message.hasOwnProperty("Buymul")) {
                if (!Array.isArray(message.Buymul))
                    return "Buymul: array expected";
                for (let i = 0; i < message.Buymul.length; ++i)
                    if (typeof message.Buymul[i] !== "number")
                        return "Buymul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljjf.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljjf.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jljjf.StateInfo)
                return object;
            let message = new $root.jljjf.StateInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jljjf.StateInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jljjf.StateInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jljjf.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jljjf.StateInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = object.Mul[i] | 0;
            }
            if (object.Buymul) {
                if (!Array.isArray(object.Buymul))
                    throw TypeError(".jljjf.StateInfo.Buymul: array expected");
                message.Buymul = [];
                for (let i = 0; i < object.Buymul.length; ++i)
                    message.Buymul[i] = Number(object.Buymul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljjf.StateInfo
         * @static
         * @param {jljjf.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
                object.Buymul = [];
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jljjf.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = message.Mul[j];
            }
            if (message.Buymul && message.Buymul.length) {
                object.Buymul = [];
                for (let j = 0; j < message.Buymul.length; ++j)
                    object.Buymul[j] = options.json && !isFinite(message.Buymul[j]) ? String(message.Buymul[j]) : message.Buymul[j];
            }
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jljjf.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jljjf.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljjf.StateInfo";
        };

        return StateInfo;
    })();

    return jljjf;
})();

export const jljk = $root.jljk = (() => {

    /**
     * Namespace jljk.
     * @exports jljk
     * @namespace
     */
    const jljk = {};

    jljk.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jljk
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jljk
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jljk.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jljk.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jljk.Column
         * @static
         * @param {jljk.IColumn=} [properties] Properties to set
         * @returns {jljk.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jljk.Column.verify|verify} messages.
         * @function encode
         * @memberof jljk.Column
         * @static
         * @param {jljk.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jljk.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.Column
         * @static
         * @param {jljk.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jljk.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.Column)
                return object;
            let message = new $root.jljk.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jljk.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.Column
         * @static
         * @param {jljk.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jljk.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jljk.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.Column";
        };

        return Column;
    })();

    jljk.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jljk
         * @interface IAwardData
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Money] AwardData Money
         */

        /**
         * Constructs a new AwardData.
         * @memberof jljk
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jljk.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jljk.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jljk.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jljk.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Money.
         * @member {number} Money
         * @memberof jljk.AwardData
         * @instance
         */
        AwardData.prototype.Money = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jljk.AwardData
         * @static
         * @param {jljk.IAwardData=} [properties] Properties to set
         * @returns {jljk.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jljk.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jljk.AwardData
         * @static
         * @param {jljk.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Index);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Count);
            if (message.Money != null && Object.hasOwnProperty.call(message, "Money"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Money);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jljk.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.AwardData
         * @static
         * @param {jljk.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Index = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Count = reader.int32();
                        break;
                    }
                case 4: {
                        message.Money = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jljk.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Money != null && message.hasOwnProperty("Money"))
                if (typeof message.Money !== "number")
                    return "Money: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.AwardData)
                return object;
            let message = new $root.jljk.AwardData();
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Money != null)
                message.Money = Number(object.Money);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.AwardData
         * @static
         * @param {jljk.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Index = 0;
                object.Symbol = 0;
                object.Count = 0;
                object.Money = 0;
            }
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Money != null && message.hasOwnProperty("Money"))
                object.Money = options.json && !isFinite(message.Money) ? String(message.Money) : message.Money;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jljk.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jljk.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.AwardData";
        };

        return AwardData;
    })();

    jljk.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jljk
         * @interface ISinglePlate
         * @property {Array.<jljk.IColumn>|null} [OriginPlate] SinglePlate OriginPlate
         * @property {Array.<jljk.IColumn>|null} [LastPlate] SinglePlate LastPlate
         * @property {Array.<jljk.IAwardData>|null} [AwardDataList] SinglePlate AwardDataList
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         * @property {number|null} [BonusType] SinglePlate BonusType
         * @property {number|null} [BonusCount] SinglePlate BonusCount
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jljk
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jljk.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.OriginPlate = [];
            this.LastPlate = [];
            this.AwardDataList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate OriginPlate.
         * @member {Array.<jljk.IColumn>} OriginPlate
         * @memberof jljk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.OriginPlate = $util.emptyArray;

        /**
         * SinglePlate LastPlate.
         * @member {Array.<jljk.IColumn>} LastPlate
         * @memberof jljk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.LastPlate = $util.emptyArray;

        /**
         * SinglePlate AwardDataList.
         * @member {Array.<jljk.IAwardData>} AwardDataList
         * @memberof jljk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataList = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jljk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * SinglePlate BonusType.
         * @member {number} BonusType
         * @memberof jljk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusType = 0;

        /**
         * SinglePlate BonusCount.
         * @member {number} BonusCount
         * @memberof jljk.SinglePlate
         * @instance
         */
        SinglePlate.prototype.BonusCount = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jljk.SinglePlate
         * @static
         * @param {jljk.ISinglePlate=} [properties] Properties to set
         * @returns {jljk.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jljk.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jljk.SinglePlate
         * @static
         * @param {jljk.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.OriginPlate != null && message.OriginPlate.length)
                for (let i = 0; i < message.OriginPlate.length; ++i)
                    $root.jljk.Column.encode(message.OriginPlate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.LastPlate != null && message.LastPlate.length)
                for (let i = 0; i < message.LastPlate.length; ++i)
                    $root.jljk.Column.encode(message.LastPlate[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jljk.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.SingleWin);
            if (message.BonusType != null && Object.hasOwnProperty.call(message, "BonusType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.BonusType);
            if (message.BonusCount != null && Object.hasOwnProperty.call(message, "BonusCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.BonusCount);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jljk.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.SinglePlate
         * @static
         * @param {jljk.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.OriginPlate && message.OriginPlate.length))
                            message.OriginPlate = [];
                        message.OriginPlate.push($root.jljk.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.LastPlate && message.LastPlate.length))
                            message.LastPlate = [];
                        message.LastPlate.push($root.jljk.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jljk.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 5: {
                        message.BonusType = reader.int32();
                        break;
                    }
                case 6: {
                        message.BonusCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jljk.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.OriginPlate != null && message.hasOwnProperty("OriginPlate")) {
                if (!Array.isArray(message.OriginPlate))
                    return "OriginPlate: array expected";
                for (let i = 0; i < message.OriginPlate.length; ++i) {
                    let error = $root.jljk.Column.verify(message.OriginPlate[i]);
                    if (error)
                        return "OriginPlate." + error;
                }
            }
            if (message.LastPlate != null && message.hasOwnProperty("LastPlate")) {
                if (!Array.isArray(message.LastPlate))
                    return "LastPlate: array expected";
                for (let i = 0; i < message.LastPlate.length; ++i) {
                    let error = $root.jljk.Column.verify(message.LastPlate[i]);
                    if (error)
                        return "LastPlate." + error;
                }
            }
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jljk.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.BonusType != null && message.hasOwnProperty("BonusType"))
                if (!$util.isInteger(message.BonusType))
                    return "BonusType: integer expected";
            if (message.BonusCount != null && message.hasOwnProperty("BonusCount"))
                if (!$util.isInteger(message.BonusCount))
                    return "BonusCount: integer expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.SinglePlate)
                return object;
            let message = new $root.jljk.SinglePlate();
            if (object.OriginPlate) {
                if (!Array.isArray(object.OriginPlate))
                    throw TypeError(".jljk.SinglePlate.OriginPlate: array expected");
                message.OriginPlate = [];
                for (let i = 0; i < object.OriginPlate.length; ++i) {
                    if (typeof object.OriginPlate[i] !== "object")
                        throw TypeError(".jljk.SinglePlate.OriginPlate: object expected");
                    message.OriginPlate[i] = $root.jljk.Column.fromObject(object.OriginPlate[i]);
                }
            }
            if (object.LastPlate) {
                if (!Array.isArray(object.LastPlate))
                    throw TypeError(".jljk.SinglePlate.LastPlate: array expected");
                message.LastPlate = [];
                for (let i = 0; i < object.LastPlate.length; ++i) {
                    if (typeof object.LastPlate[i] !== "object")
                        throw TypeError(".jljk.SinglePlate.LastPlate: object expected");
                    message.LastPlate[i] = $root.jljk.Column.fromObject(object.LastPlate[i]);
                }
            }
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jljk.SinglePlate.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jljk.SinglePlate.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jljk.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.BonusType != null)
                message.BonusType = object.BonusType | 0;
            if (object.BonusCount != null)
                message.BonusCount = object.BonusCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.SinglePlate
         * @static
         * @param {jljk.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.OriginPlate = [];
                object.LastPlate = [];
                object.AwardDataList = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.BonusType = 0;
                object.BonusCount = 0;
            }
            if (message.OriginPlate && message.OriginPlate.length) {
                object.OriginPlate = [];
                for (let j = 0; j < message.OriginPlate.length; ++j)
                    object.OriginPlate[j] = $root.jljk.Column.toObject(message.OriginPlate[j], options);
            }
            if (message.LastPlate && message.LastPlate.length) {
                object.LastPlate = [];
                for (let j = 0; j < message.LastPlate.length; ++j)
                    object.LastPlate[j] = $root.jljk.Column.toObject(message.LastPlate[j], options);
            }
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jljk.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.BonusType != null && message.hasOwnProperty("BonusType"))
                object.BonusType = message.BonusType;
            if (message.BonusCount != null && message.hasOwnProperty("BonusCount"))
                object.BonusCount = message.BonusCount;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jljk.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jljk.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.SinglePlate";
        };

        return SinglePlate;
    })();

    jljk.MainData = (function() {

        /**
         * Properties of a MainData.
         * @memberof jljk
         * @interface IMainData
         * @property {Array.<jljk.ISinglePlate>|null} [MainPlate] MainData MainPlate
         * @property {number|null} [MainWin] MainData MainWin
         * @property {number|null} [ScatterWin] MainData ScatterWin
         * @property {number|null} [ScatterCount] MainData ScatterCount
         */

        /**
         * Constructs a new MainData.
         * @memberof jljk
         * @classdesc Represents a MainData.
         * @implements IMainData
         * @constructor
         * @param {jljk.IMainData=} [properties] Properties to set
         */
        function MainData(properties) {
            this.MainPlate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MainData MainPlate.
         * @member {Array.<jljk.ISinglePlate>} MainPlate
         * @memberof jljk.MainData
         * @instance
         */
        MainData.prototype.MainPlate = $util.emptyArray;

        /**
         * MainData MainWin.
         * @member {number} MainWin
         * @memberof jljk.MainData
         * @instance
         */
        MainData.prototype.MainWin = 0;

        /**
         * MainData ScatterWin.
         * @member {number} ScatterWin
         * @memberof jljk.MainData
         * @instance
         */
        MainData.prototype.ScatterWin = 0;

        /**
         * MainData ScatterCount.
         * @member {number} ScatterCount
         * @memberof jljk.MainData
         * @instance
         */
        MainData.prototype.ScatterCount = 0;

        /**
         * Creates a new MainData instance using the specified properties.
         * @function create
         * @memberof jljk.MainData
         * @static
         * @param {jljk.IMainData=} [properties] Properties to set
         * @returns {jljk.MainData} MainData instance
         */
        MainData.create = function create(properties) {
            return new MainData(properties);
        };

        /**
         * Encodes the specified MainData message. Does not implicitly {@link jljk.MainData.verify|verify} messages.
         * @function encode
         * @memberof jljk.MainData
         * @static
         * @param {jljk.IMainData} message MainData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MainData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainPlate != null && message.MainPlate.length)
                for (let i = 0; i < message.MainPlate.length; ++i)
                    $root.jljk.SinglePlate.encode(message.MainPlate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.MainWin);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ScatterWin);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified MainData message, length delimited. Does not implicitly {@link jljk.MainData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.MainData
         * @static
         * @param {jljk.IMainData} message MainData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MainData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MainData message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.MainData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.MainData} MainData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MainData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.MainData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MainPlate && message.MainPlate.length))
                            message.MainPlate = [];
                        message.MainPlate.push($root.jljk.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MainData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.MainData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.MainData} MainData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MainData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MainData message.
         * @function verify
         * @memberof jljk.MainData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MainData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainPlate != null && message.hasOwnProperty("MainPlate")) {
                if (!Array.isArray(message.MainPlate))
                    return "MainPlate: array expected";
                for (let i = 0; i < message.MainPlate.length; ++i) {
                    let error = $root.jljk.SinglePlate.verify(message.MainPlate[i]);
                    if (error)
                        return "MainPlate." + error;
                }
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a MainData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.MainData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.MainData} MainData
         */
        MainData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.MainData)
                return object;
            let message = new $root.jljk.MainData();
            if (object.MainPlate) {
                if (!Array.isArray(object.MainPlate))
                    throw TypeError(".jljk.MainData.MainPlate: array expected");
                message.MainPlate = [];
                for (let i = 0; i < object.MainPlate.length; ++i) {
                    if (typeof object.MainPlate[i] !== "object")
                        throw TypeError(".jljk.MainData.MainPlate: object expected");
                    message.MainPlate[i] = $root.jljk.SinglePlate.fromObject(object.MainPlate[i]);
                }
            }
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a MainData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.MainData
         * @static
         * @param {jljk.MainData} message MainData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MainData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MainPlate = [];
            if (options.defaults) {
                object.MainWin = 0;
                object.ScatterWin = 0;
                object.ScatterCount = 0;
            }
            if (message.MainPlate && message.MainPlate.length) {
                object.MainPlate = [];
                for (let j = 0; j < message.MainPlate.length; ++j)
                    object.MainPlate[j] = $root.jljk.SinglePlate.toObject(message.MainPlate[j], options);
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this MainData to JSON.
         * @function toJSON
         * @memberof jljk.MainData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MainData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MainData
         * @function getTypeUrl
         * @memberof jljk.MainData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MainData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.MainData";
        };

        return MainData;
    })();

    jljk.SingleFree = (function() {

        /**
         * Properties of a SingleFree.
         * @memberof jljk
         * @interface ISingleFree
         * @property {jljk.ISinglePlate|null} [FreePlate] SingleFree FreePlate
         * @property {Array.<number>|null} [UpReel] SingleFree UpReel
         * @property {number|null} [PlaneCount] SingleFree PlaneCount
         * @property {number|null} [MaxRound] SingleFree MaxRound
         * @property {number|null} [FreeMul] SingleFree FreeMul
         * @property {number|null} [AwardType] SingleFree AwardType
         * @property {number|null} [NowMoney] SingleFree NowMoney
         */

        /**
         * Constructs a new SingleFree.
         * @memberof jljk
         * @classdesc Represents a SingleFree.
         * @implements ISingleFree
         * @constructor
         * @param {jljk.ISingleFree=} [properties] Properties to set
         */
        function SingleFree(properties) {
            this.UpReel = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleFree FreePlate.
         * @member {jljk.ISinglePlate|null|undefined} FreePlate
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreePlate = null;

        /**
         * SingleFree UpReel.
         * @member {Array.<number>} UpReel
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.UpReel = $util.emptyArray;

        /**
         * SingleFree PlaneCount.
         * @member {number} PlaneCount
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.PlaneCount = 0;

        /**
         * SingleFree MaxRound.
         * @member {number} MaxRound
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.MaxRound = 0;

        /**
         * SingleFree FreeMul.
         * @member {number} FreeMul
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.FreeMul = 0;

        /**
         * SingleFree AwardType.
         * @member {number} AwardType
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.AwardType = 0;

        /**
         * SingleFree NowMoney.
         * @member {number} NowMoney
         * @memberof jljk.SingleFree
         * @instance
         */
        SingleFree.prototype.NowMoney = 0;

        /**
         * Creates a new SingleFree instance using the specified properties.
         * @function create
         * @memberof jljk.SingleFree
         * @static
         * @param {jljk.ISingleFree=} [properties] Properties to set
         * @returns {jljk.SingleFree} SingleFree instance
         */
        SingleFree.create = function create(properties) {
            return new SingleFree(properties);
        };

        /**
         * Encodes the specified SingleFree message. Does not implicitly {@link jljk.SingleFree.verify|verify} messages.
         * @function encode
         * @memberof jljk.SingleFree
         * @static
         * @param {jljk.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreePlate != null && Object.hasOwnProperty.call(message, "FreePlate"))
                $root.jljk.SinglePlate.encode(message.FreePlate, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.UpReel != null && message.UpReel.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.UpReel.length; ++i)
                    writer.int32(message.UpReel[i]);
                writer.ldelim();
            }
            if (message.PlaneCount != null && Object.hasOwnProperty.call(message, "PlaneCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.PlaneCount);
            if (message.MaxRound != null && Object.hasOwnProperty.call(message, "MaxRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.MaxRound);
            if (message.FreeMul != null && Object.hasOwnProperty.call(message, "FreeMul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.FreeMul);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardType);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            return writer;
        };

        /**
         * Encodes the specified SingleFree message, length delimited. Does not implicitly {@link jljk.SingleFree.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.SingleFree
         * @static
         * @param {jljk.ISingleFree} message SingleFree message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleFree.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.SingleFree();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.FreePlate = $root.jljk.SinglePlate.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        if (!(message.UpReel && message.UpReel.length))
                            message.UpReel = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.UpReel.push(reader.int32());
                        } else
                            message.UpReel.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.PlaneCount = reader.int32();
                        break;
                    }
                case 4: {
                        message.MaxRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeMul = reader.double();
                        break;
                    }
                case 6: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleFree message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.SingleFree
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.SingleFree} SingleFree
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleFree.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleFree message.
         * @function verify
         * @memberof jljk.SingleFree
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleFree.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreePlate != null && message.hasOwnProperty("FreePlate")) {
                let error = $root.jljk.SinglePlate.verify(message.FreePlate);
                if (error)
                    return "FreePlate." + error;
            }
            if (message.UpReel != null && message.hasOwnProperty("UpReel")) {
                if (!Array.isArray(message.UpReel))
                    return "UpReel: array expected";
                for (let i = 0; i < message.UpReel.length; ++i)
                    if (!$util.isInteger(message.UpReel[i]))
                        return "UpReel: integer[] expected";
            }
            if (message.PlaneCount != null && message.hasOwnProperty("PlaneCount"))
                if (!$util.isInteger(message.PlaneCount))
                    return "PlaneCount: integer expected";
            if (message.MaxRound != null && message.hasOwnProperty("MaxRound"))
                if (!$util.isInteger(message.MaxRound))
                    return "MaxRound: integer expected";
            if (message.FreeMul != null && message.hasOwnProperty("FreeMul"))
                if (typeof message.FreeMul !== "number")
                    return "FreeMul: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            return null;
        };

        /**
         * Creates a SingleFree message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.SingleFree
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.SingleFree} SingleFree
         */
        SingleFree.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.SingleFree)
                return object;
            let message = new $root.jljk.SingleFree();
            if (object.FreePlate != null) {
                if (typeof object.FreePlate !== "object")
                    throw TypeError(".jljk.SingleFree.FreePlate: object expected");
                message.FreePlate = $root.jljk.SinglePlate.fromObject(object.FreePlate);
            }
            if (object.UpReel) {
                if (!Array.isArray(object.UpReel))
                    throw TypeError(".jljk.SingleFree.UpReel: array expected");
                message.UpReel = [];
                for (let i = 0; i < object.UpReel.length; ++i)
                    message.UpReel[i] = object.UpReel[i] | 0;
            }
            if (object.PlaneCount != null)
                message.PlaneCount = object.PlaneCount | 0;
            if (object.MaxRound != null)
                message.MaxRound = object.MaxRound | 0;
            if (object.FreeMul != null)
                message.FreeMul = Number(object.FreeMul);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            return message;
        };

        /**
         * Creates a plain object from a SingleFree message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.SingleFree
         * @static
         * @param {jljk.SingleFree} message SingleFree
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleFree.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.UpReel = [];
            if (options.defaults) {
                object.FreePlate = null;
                object.PlaneCount = 0;
                object.MaxRound = 0;
                object.FreeMul = 0;
                object.AwardType = 0;
                object.NowMoney = 0;
            }
            if (message.FreePlate != null && message.hasOwnProperty("FreePlate"))
                object.FreePlate = $root.jljk.SinglePlate.toObject(message.FreePlate, options);
            if (message.UpReel && message.UpReel.length) {
                object.UpReel = [];
                for (let j = 0; j < message.UpReel.length; ++j)
                    object.UpReel[j] = message.UpReel[j];
            }
            if (message.PlaneCount != null && message.hasOwnProperty("PlaneCount"))
                object.PlaneCount = message.PlaneCount;
            if (message.MaxRound != null && message.hasOwnProperty("MaxRound"))
                object.MaxRound = message.MaxRound;
            if (message.FreeMul != null && message.hasOwnProperty("FreeMul"))
                object.FreeMul = options.json && !isFinite(message.FreeMul) ? String(message.FreeMul) : message.FreeMul;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            return object;
        };

        /**
         * Converts this SingleFree to JSON.
         * @function toJSON
         * @memberof jljk.SingleFree
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleFree.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleFree
         * @function getTypeUrl
         * @memberof jljk.SingleFree
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleFree.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.SingleFree";
        };

        return SingleFree;
    })();

    jljk.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jljk
         * @interface IFreeData
         * @property {Array.<jljk.ISingleFree>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jljk
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jljk.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jljk.ISingleFree>} FreeQueue
         * @memberof jljk.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jljk.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jljk.FreeData
         * @static
         * @param {jljk.IFreeData=} [properties] Properties to set
         * @returns {jljk.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jljk.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jljk.FreeData
         * @static
         * @param {jljk.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jljk.SingleFree.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jljk.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.FreeData
         * @static
         * @param {jljk.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jljk.SingleFree.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jljk.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jljk.SingleFree.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.FreeData)
                return object;
            let message = new $root.jljk.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jljk.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jljk.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jljk.SingleFree.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.FreeData
         * @static
         * @param {jljk.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults)
                object.FreeTotalWin = 0;
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jljk.SingleFree.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jljk.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jljk.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.FreeData";
        };

        return FreeData;
    })();

    jljk.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jljk
         * @interface ISpinAck
         * @property {jljk.IMainData|null} [MainInfo] SpinAck MainInfo
         * @property {jljk.IFreeData|null} [FreeInfo] SpinAck FreeInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jljk
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jljk.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainInfo.
         * @member {jljk.IMainData|null|undefined} MainInfo
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.MainInfo = null;

        /**
         * SpinAck FreeInfo.
         * @member {jljk.IFreeData|null|undefined} FreeInfo
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeInfo = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jljk.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jljk.SpinAck
         * @static
         * @param {jljk.ISpinAck=} [properties] Properties to set
         * @returns {jljk.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jljk.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jljk.SpinAck
         * @static
         * @param {jljk.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainInfo != null && Object.hasOwnProperty.call(message, "MainInfo"))
                $root.jljk.MainData.encode(message.MainInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeInfo != null && Object.hasOwnProperty.call(message, "FreeInfo"))
                $root.jljk.FreeData.encode(message.FreeInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jljk.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.SpinAck
         * @static
         * @param {jljk.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainInfo = $root.jljk.MainData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeInfo = $root.jljk.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jljk.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainInfo != null && message.hasOwnProperty("MainInfo")) {
                let error = $root.jljk.MainData.verify(message.MainInfo);
                if (error)
                    return "MainInfo." + error;
            }
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo")) {
                let error = $root.jljk.FreeData.verify(message.FreeInfo);
                if (error)
                    return "FreeInfo." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.SpinAck)
                return object;
            let message = new $root.jljk.SpinAck();
            if (object.MainInfo != null) {
                if (typeof object.MainInfo !== "object")
                    throw TypeError(".jljk.SpinAck.MainInfo: object expected");
                message.MainInfo = $root.jljk.MainData.fromObject(object.MainInfo);
            }
            if (object.FreeInfo != null) {
                if (typeof object.FreeInfo !== "object")
                    throw TypeError(".jljk.SpinAck.FreeInfo: object expected");
                message.FreeInfo = $root.jljk.FreeData.fromObject(object.FreeInfo);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.SpinAck
         * @static
         * @param {jljk.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainInfo = null;
                object.FreeInfo = null;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.MainInfo != null && message.hasOwnProperty("MainInfo"))
                object.MainInfo = $root.jljk.MainData.toObject(message.MainInfo, options);
            if (message.FreeInfo != null && message.hasOwnProperty("FreeInfo"))
                object.FreeInfo = $root.jljk.FreeData.toObject(message.FreeInfo, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jljk.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jljk.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.SpinAck";
        };

        return SpinAck;
    })();

    jljk.FreeCount = (function() {

        /**
         * Properties of a FreeCount.
         * @memberof jljk
         * @interface IFreeCount
         * @property {Array.<number>|null} [SCount] FreeCount SCount
         * @property {Array.<number>|null} [CountMul] FreeCount CountMul
         * @property {Array.<number>|null} [CountRound] FreeCount CountRound
         */

        /**
         * Constructs a new FreeCount.
         * @memberof jljk
         * @classdesc Represents a FreeCount.
         * @implements IFreeCount
         * @constructor
         * @param {jljk.IFreeCount=} [properties] Properties to set
         */
        function FreeCount(properties) {
            this.SCount = [];
            this.CountMul = [];
            this.CountRound = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeCount SCount.
         * @member {Array.<number>} SCount
         * @memberof jljk.FreeCount
         * @instance
         */
        FreeCount.prototype.SCount = $util.emptyArray;

        /**
         * FreeCount CountMul.
         * @member {Array.<number>} CountMul
         * @memberof jljk.FreeCount
         * @instance
         */
        FreeCount.prototype.CountMul = $util.emptyArray;

        /**
         * FreeCount CountRound.
         * @member {Array.<number>} CountRound
         * @memberof jljk.FreeCount
         * @instance
         */
        FreeCount.prototype.CountRound = $util.emptyArray;

        /**
         * Creates a new FreeCount instance using the specified properties.
         * @function create
         * @memberof jljk.FreeCount
         * @static
         * @param {jljk.IFreeCount=} [properties] Properties to set
         * @returns {jljk.FreeCount} FreeCount instance
         */
        FreeCount.create = function create(properties) {
            return new FreeCount(properties);
        };

        /**
         * Encodes the specified FreeCount message. Does not implicitly {@link jljk.FreeCount.verify|verify} messages.
         * @function encode
         * @memberof jljk.FreeCount
         * @static
         * @param {jljk.IFreeCount} message FreeCount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeCount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.SCount != null && message.SCount.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.SCount.length; ++i)
                    writer.int32(message.SCount[i]);
                writer.ldelim();
            }
            if (message.CountMul != null && message.CountMul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.CountMul.length; ++i)
                    writer.double(message.CountMul[i]);
                writer.ldelim();
            }
            if (message.CountRound != null && message.CountRound.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.CountRound.length; ++i)
                    writer.int32(message.CountRound[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified FreeCount message, length delimited. Does not implicitly {@link jljk.FreeCount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.FreeCount
         * @static
         * @param {jljk.IFreeCount} message FreeCount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeCount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeCount message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.FreeCount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.FreeCount} FreeCount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeCount.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.FreeCount();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.SCount && message.SCount.length))
                            message.SCount = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.SCount.push(reader.int32());
                        } else
                            message.SCount.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.CountMul && message.CountMul.length))
                            message.CountMul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CountMul.push(reader.double());
                        } else
                            message.CountMul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.CountRound && message.CountRound.length))
                            message.CountRound = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CountRound.push(reader.int32());
                        } else
                            message.CountRound.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeCount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.FreeCount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.FreeCount} FreeCount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeCount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeCount message.
         * @function verify
         * @memberof jljk.FreeCount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeCount.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.SCount != null && message.hasOwnProperty("SCount")) {
                if (!Array.isArray(message.SCount))
                    return "SCount: array expected";
                for (let i = 0; i < message.SCount.length; ++i)
                    if (!$util.isInteger(message.SCount[i]))
                        return "SCount: integer[] expected";
            }
            if (message.CountMul != null && message.hasOwnProperty("CountMul")) {
                if (!Array.isArray(message.CountMul))
                    return "CountMul: array expected";
                for (let i = 0; i < message.CountMul.length; ++i)
                    if (typeof message.CountMul[i] !== "number")
                        return "CountMul: number[] expected";
            }
            if (message.CountRound != null && message.hasOwnProperty("CountRound")) {
                if (!Array.isArray(message.CountRound))
                    return "CountRound: array expected";
                for (let i = 0; i < message.CountRound.length; ++i)
                    if (!$util.isInteger(message.CountRound[i]))
                        return "CountRound: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a FreeCount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.FreeCount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.FreeCount} FreeCount
         */
        FreeCount.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.FreeCount)
                return object;
            let message = new $root.jljk.FreeCount();
            if (object.SCount) {
                if (!Array.isArray(object.SCount))
                    throw TypeError(".jljk.FreeCount.SCount: array expected");
                message.SCount = [];
                for (let i = 0; i < object.SCount.length; ++i)
                    message.SCount[i] = object.SCount[i] | 0;
            }
            if (object.CountMul) {
                if (!Array.isArray(object.CountMul))
                    throw TypeError(".jljk.FreeCount.CountMul: array expected");
                message.CountMul = [];
                for (let i = 0; i < object.CountMul.length; ++i)
                    message.CountMul[i] = Number(object.CountMul[i]);
            }
            if (object.CountRound) {
                if (!Array.isArray(object.CountRound))
                    throw TypeError(".jljk.FreeCount.CountRound: array expected");
                message.CountRound = [];
                for (let i = 0; i < object.CountRound.length; ++i)
                    message.CountRound[i] = object.CountRound[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a FreeCount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.FreeCount
         * @static
         * @param {jljk.FreeCount} message FreeCount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeCount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.SCount = [];
                object.CountMul = [];
                object.CountRound = [];
            }
            if (message.SCount && message.SCount.length) {
                object.SCount = [];
                for (let j = 0; j < message.SCount.length; ++j)
                    object.SCount[j] = message.SCount[j];
            }
            if (message.CountMul && message.CountMul.length) {
                object.CountMul = [];
                for (let j = 0; j < message.CountMul.length; ++j)
                    object.CountMul[j] = options.json && !isFinite(message.CountMul[j]) ? String(message.CountMul[j]) : message.CountMul[j];
            }
            if (message.CountRound && message.CountRound.length) {
                object.CountRound = [];
                for (let j = 0; j < message.CountRound.length; ++j)
                    object.CountRound[j] = message.CountRound[j];
            }
            return object;
        };

        /**
         * Converts this FreeCount to JSON.
         * @function toJSON
         * @memberof jljk.FreeCount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeCount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeCount
         * @function getTypeUrl
         * @memberof jljk.FreeCount
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeCount.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.FreeCount";
        };

        return FreeCount;
    })();

    jljk.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jljk
         * @interface IGameInfoAck
         * @property {Array.<jljk.IFreeCount>|null} [FreeData] GameInfoAck FreeData
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jljk
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jljk.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.FreeData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck FreeData.
         * @member {Array.<jljk.IFreeCount>} FreeData
         * @memberof jljk.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.FreeData = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jljk.GameInfoAck
         * @static
         * @param {jljk.IGameInfoAck=} [properties] Properties to set
         * @returns {jljk.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jljk.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jljk.GameInfoAck
         * @static
         * @param {jljk.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeData != null && message.FreeData.length)
                for (let i = 0; i < message.FreeData.length; ++i)
                    $root.jljk.FreeCount.encode(message.FreeData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jljk.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jljk.GameInfoAck
         * @static
         * @param {jljk.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jljk.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jljk.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jljk.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeData && message.FreeData.length))
                            message.FreeData = [];
                        message.FreeData.push($root.jljk.FreeCount.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jljk.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jljk.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jljk.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeData != null && message.hasOwnProperty("FreeData")) {
                if (!Array.isArray(message.FreeData))
                    return "FreeData: array expected";
                for (let i = 0; i < message.FreeData.length; ++i) {
                    let error = $root.jljk.FreeCount.verify(message.FreeData[i]);
                    if (error)
                        return "FreeData." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jljk.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jljk.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jljk.GameInfoAck)
                return object;
            let message = new $root.jljk.GameInfoAck();
            if (object.FreeData) {
                if (!Array.isArray(object.FreeData))
                    throw TypeError(".jljk.GameInfoAck.FreeData: array expected");
                message.FreeData = [];
                for (let i = 0; i < object.FreeData.length; ++i) {
                    if (typeof object.FreeData[i] !== "object")
                        throw TypeError(".jljk.GameInfoAck.FreeData: object expected");
                    message.FreeData[i] = $root.jljk.FreeCount.fromObject(object.FreeData[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jljk.GameInfoAck
         * @static
         * @param {jljk.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeData = [];
            if (message.FreeData && message.FreeData.length) {
                object.FreeData = [];
                for (let j = 0; j < message.FreeData.length; ++j)
                    object.FreeData[j] = $root.jljk.FreeCount.toObject(message.FreeData[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jljk.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jljk.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jljk.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jljk;
})();

export const jlka = $root.jlka = (() => {

    /**
     * Namespace jlka.
     * @exports jlka
     * @namespace
     */
    const jlka = {};

    jlka.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlka
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlka
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlka.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlka.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlka.Column
         * @static
         * @param {jlka.IColumn=} [properties] Properties to set
         * @returns {jlka.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlka.Column.verify|verify} messages.
         * @function encode
         * @memberof jlka.Column
         * @static
         * @param {jlka.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlka.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlka.Column
         * @static
         * @param {jlka.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlka.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlka.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlka.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlka.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlka.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlka.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlka.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlka.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlka.Column)
                return object;
            let message = new $root.jlka.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlka.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlka.Column
         * @static
         * @param {jlka.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlka.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlka.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlka.Column";
        };

        return Column;
    })();

    jlka.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlka
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlka
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlka.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlka.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlka.ColumnFloat
         * @static
         * @param {jlka.IColumnFloat=} [properties] Properties to set
         * @returns {jlka.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlka.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlka.ColumnFloat
         * @static
         * @param {jlka.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlka.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlka.ColumnFloat
         * @static
         * @param {jlka.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlka.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlka.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlka.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlka.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlka.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlka.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlka.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlka.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlka.ColumnFloat)
                return object;
            let message = new $root.jlka.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlka.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlka.ColumnFloat
         * @static
         * @param {jlka.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlka.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlka.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlka.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlka.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlka
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlka
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlka.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlka.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlka.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlka.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlka.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlka.AwardData
         * @static
         * @param {jlka.IAwardData=} [properties] Properties to set
         * @returns {jlka.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlka.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlka.AwardData
         * @static
         * @param {jlka.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlka.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlka.AwardData
         * @static
         * @param {jlka.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlka.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlka.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlka.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlka.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlka.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlka.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlka.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlka.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlka.AwardData)
                return object;
            let message = new $root.jlka.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlka.AwardData
         * @static
         * @param {jlka.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlka.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlka.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlka.AwardData";
        };

        return AwardData;
    })();

    jlka.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlka
         * @interface IRoundInfo
         * @property {Array.<jlka.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlka.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {Array.<jlka.IColumn>|null} [MGWildVS] RoundInfo MGWildVS
         * @property {Array.<number>|null} [MGWildWinner] RoundInfo MGWildWinner
         * @property {number|null} [MGMult] RoundInfo MGMult
         * @property {Array.<number>|null} [FGNowValue] RoundInfo FGNowValue
         * @property {Array.<jlka.IColumnFloat>|null} [FGNewPlate] RoundInfo FGNewPlate
         * @property {Array.<number>|null} [FGWinner] RoundInfo FGWinner
         * @property {Array.<jlka.IColumn>|null} [PlateSymbolLog] RoundInfo PlateSymbolLog
         * @property {Array.<number>|null} [FGNowValueAfterDuel] RoundInfo FGNowValueAfterDuel
         * @property {Array.<number>|null} [MGMultResult] RoundInfo MGMultResult
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlka
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlka.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.MGWildVS = [];
            this.MGWildWinner = [];
            this.FGNowValue = [];
            this.FGNewPlate = [];
            this.FGWinner = [];
            this.PlateSymbolLog = [];
            this.FGNowValueAfterDuel = [];
            this.MGMultResult = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlka.IColumn>} PlateSymbol
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlka.IAwardData>} AwardDataVec
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo MGWildVS.
         * @member {Array.<jlka.IColumn>} MGWildVS
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGWildVS = $util.emptyArray;

        /**
         * RoundInfo MGWildWinner.
         * @member {Array.<number>} MGWildWinner
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGWildWinner = $util.emptyArray;

        /**
         * RoundInfo MGMult.
         * @member {number} MGMult
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGMult = 0;

        /**
         * RoundInfo FGNowValue.
         * @member {Array.<number>} FGNowValue
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGNowValue = $util.emptyArray;

        /**
         * RoundInfo FGNewPlate.
         * @member {Array.<jlka.IColumnFloat>} FGNewPlate
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGNewPlate = $util.emptyArray;

        /**
         * RoundInfo FGWinner.
         * @member {Array.<number>} FGWinner
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGWinner = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolLog.
         * @member {Array.<jlka.IColumn>} PlateSymbolLog
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo FGNowValueAfterDuel.
         * @member {Array.<number>} FGNowValueAfterDuel
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FGNowValueAfterDuel = $util.emptyArray;

        /**
         * RoundInfo MGMultResult.
         * @member {Array.<number>} MGMultResult
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MGMultResult = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlka.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlka.RoundInfo
         * @static
         * @param {jlka.IRoundInfo=} [properties] Properties to set
         * @returns {jlka.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlka.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlka.RoundInfo
         * @static
         * @param {jlka.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlka.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlka.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RoundWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.MGWildVS != null && message.MGWildVS.length)
                for (let i = 0; i < message.MGWildVS.length; ++i)
                    $root.jlka.Column.encode(message.MGWildVS[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.MGWildWinner != null && message.MGWildWinner.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.MGWildWinner.length; ++i)
                    writer.int32(message.MGWildWinner[i]);
                writer.ldelim();
            }
            if (message.MGMult != null && Object.hasOwnProperty.call(message, "MGMult"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.MGMult);
            if (message.FGNowValue != null && message.FGNowValue.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.FGNowValue.length; ++i)
                    writer.double(message.FGNowValue[i]);
                writer.ldelim();
            }
            if (message.FGNewPlate != null && message.FGNewPlate.length)
                for (let i = 0; i < message.FGNewPlate.length; ++i)
                    $root.jlka.ColumnFloat.encode(message.FGNewPlate[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.FGWinner != null && message.FGWinner.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.FGWinner.length; ++i)
                    writer.int32(message.FGWinner[i]);
                writer.ldelim();
            }
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlka.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.FGNowValueAfterDuel != null && message.FGNowValueAfterDuel.length) {
                writer.uint32(/* id 13, wireType 2 =*/106).fork();
                for (let i = 0; i < message.FGNowValueAfterDuel.length; ++i)
                    writer.double(message.FGNowValueAfterDuel[i]);
                writer.ldelim();
            }
            if (message.MGMultResult != null && message.MGMultResult.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (let i = 0; i < message.MGMultResult.length; ++i)
                    writer.int32(message.MGMultResult[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlka.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlka.RoundInfo
         * @static
         * @param {jlka.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlka.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlka.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlka.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlka.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlka.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.MGWildVS && message.MGWildVS.length))
                            message.MGWildVS = [];
                        message.MGWildVS.push($root.jlka.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.MGWildWinner && message.MGWildWinner.length))
                            message.MGWildWinner = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.MGWildWinner.push(reader.int32());
                        } else
                            message.MGWildWinner.push(reader.int32());
                        break;
                    }
                case 8: {
                        message.MGMult = reader.double();
                        break;
                    }
                case 9: {
                        if (!(message.FGNowValue && message.FGNowValue.length))
                            message.FGNowValue = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FGNowValue.push(reader.double());
                        } else
                            message.FGNowValue.push(reader.double());
                        break;
                    }
                case 10: {
                        if (!(message.FGNewPlate && message.FGNewPlate.length))
                            message.FGNewPlate = [];
                        message.FGNewPlate.push($root.jlka.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        if (!(message.FGWinner && message.FGWinner.length))
                            message.FGWinner = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FGWinner.push(reader.int32());
                        } else
                            message.FGWinner.push(reader.int32());
                        break;
                    }
                case 12: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlka.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 13: {
                        if (!(message.FGNowValueAfterDuel && message.FGNowValueAfterDuel.length))
                            message.FGNowValueAfterDuel = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FGNowValueAfterDuel.push(reader.double());
                        } else
                            message.FGNowValueAfterDuel.push(reader.double());
                        break;
                    }
                case 14: {
                        if (!(message.MGMultResult && message.MGMultResult.length))
                            message.MGMultResult = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.MGMultResult.push(reader.int32());
                        } else
                            message.MGMultResult.push(reader.int32());
                        break;
                    }
                case 15: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlka.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlka.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlka.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlka.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlka.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.MGWildVS != null && message.hasOwnProperty("MGWildVS")) {
                if (!Array.isArray(message.MGWildVS))
                    return "MGWildVS: array expected";
                for (let i = 0; i < message.MGWildVS.length; ++i) {
                    let error = $root.jlka.Column.verify(message.MGWildVS[i]);
                    if (error)
                        return "MGWildVS." + error;
                }
            }
            if (message.MGWildWinner != null && message.hasOwnProperty("MGWildWinner")) {
                if (!Array.isArray(message.MGWildWinner))
                    return "MGWildWinner: array expected";
                for (let i = 0; i < message.MGWildWinner.length; ++i)
                    if (!$util.isInteger(message.MGWildWinner[i]))
                        return "MGWildWinner: integer[] expected";
            }
            if (message.MGMult != null && message.hasOwnProperty("MGMult"))
                if (typeof message.MGMult !== "number")
                    return "MGMult: number expected";
            if (message.FGNowValue != null && message.hasOwnProperty("FGNowValue")) {
                if (!Array.isArray(message.FGNowValue))
                    return "FGNowValue: array expected";
                for (let i = 0; i < message.FGNowValue.length; ++i)
                    if (typeof message.FGNowValue[i] !== "number")
                        return "FGNowValue: number[] expected";
            }
            if (message.FGNewPlate != null && message.hasOwnProperty("FGNewPlate")) {
                if (!Array.isArray(message.FGNewPlate))
                    return "FGNewPlate: array expected";
                for (let i = 0; i < message.FGNewPlate.length; ++i) {
                    let error = $root.jlka.ColumnFloat.verify(message.FGNewPlate[i]);
                    if (error)
                        return "FGNewPlate." + error;
                }
            }
            if (message.FGWinner != null && message.hasOwnProperty("FGWinner")) {
                if (!Array.isArray(message.FGWinner))
                    return "FGWinner: array expected";
                for (let i = 0; i < message.FGWinner.length; ++i)
                    if (!$util.isInteger(message.FGWinner[i]))
                        return "FGWinner: integer[] expected";
            }
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlka.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            if (message.FGNowValueAfterDuel != null && message.hasOwnProperty("FGNowValueAfterDuel")) {
                if (!Array.isArray(message.FGNowValueAfterDuel))
                    return "FGNowValueAfterDuel: array expected";
                for (let i = 0; i < message.FGNowValueAfterDuel.length; ++i)
                    if (typeof message.FGNowValueAfterDuel[i] !== "number")
                        return "FGNowValueAfterDuel: number[] expected";
            }
            if (message.MGMultResult != null && message.hasOwnProperty("MGMultResult")) {
                if (!Array.isArray(message.MGMultResult))
                    return "MGMultResult: array expected";
                for (let i = 0; i < message.MGMultResult.length; ++i)
                    if (!$util.isInteger(message.MGMultResult[i]))
                        return "MGMultResult: integer[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlka.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlka.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlka.RoundInfo)
                return object;
            let message = new $root.jlka.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlka.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlka.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlka.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlka.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlka.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlka.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.MGWildVS) {
                if (!Array.isArray(object.MGWildVS))
                    throw TypeError(".jlka.RoundInfo.MGWildVS: array expected");
                message.MGWildVS = [];
                for (let i = 0; i < object.MGWildVS.length; ++i) {
                    if (typeof object.MGWildVS[i] !== "object")
                        throw TypeError(".jlka.RoundInfo.MGWildVS: object expected");
                    message.MGWildVS[i] = $root.jlka.Column.fromObject(object.MGWildVS[i]);
                }
            }
            if (object.MGWildWinner) {
                if (!Array.isArray(object.MGWildWinner))
                    throw TypeError(".jlka.RoundInfo.MGWildWinner: array expected");
                message.MGWildWinner = [];
                for (let i = 0; i < object.MGWildWinner.length; ++i)
                    message.MGWildWinner[i] = object.MGWildWinner[i] | 0;
            }
            if (object.MGMult != null)
                message.MGMult = Number(object.MGMult);
            if (object.FGNowValue) {
                if (!Array.isArray(object.FGNowValue))
                    throw TypeError(".jlka.RoundInfo.FGNowValue: array expected");
                message.FGNowValue = [];
                for (let i = 0; i < object.FGNowValue.length; ++i)
                    message.FGNowValue[i] = Number(object.FGNowValue[i]);
            }
            if (object.FGNewPlate) {
                if (!Array.isArray(object.FGNewPlate))
                    throw TypeError(".jlka.RoundInfo.FGNewPlate: array expected");
                message.FGNewPlate = [];
                for (let i = 0; i < object.FGNewPlate.length; ++i) {
                    if (typeof object.FGNewPlate[i] !== "object")
                        throw TypeError(".jlka.RoundInfo.FGNewPlate: object expected");
                    message.FGNewPlate[i] = $root.jlka.ColumnFloat.fromObject(object.FGNewPlate[i]);
                }
            }
            if (object.FGWinner) {
                if (!Array.isArray(object.FGWinner))
                    throw TypeError(".jlka.RoundInfo.FGWinner: array expected");
                message.FGWinner = [];
                for (let i = 0; i < object.FGWinner.length; ++i)
                    message.FGWinner[i] = object.FGWinner[i] | 0;
            }
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlka.RoundInfo.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlka.RoundInfo.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlka.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            if (object.FGNowValueAfterDuel) {
                if (!Array.isArray(object.FGNowValueAfterDuel))
                    throw TypeError(".jlka.RoundInfo.FGNowValueAfterDuel: array expected");
                message.FGNowValueAfterDuel = [];
                for (let i = 0; i < object.FGNowValueAfterDuel.length; ++i)
                    message.FGNowValueAfterDuel[i] = Number(object.FGNowValueAfterDuel[i]);
            }
            if (object.MGMultResult) {
                if (!Array.isArray(object.MGMultResult))
                    throw TypeError(".jlka.RoundInfo.MGMultResult: array expected");
                message.MGMultResult = [];
                for (let i = 0; i < object.MGMultResult.length; ++i)
                    message.MGMultResult[i] = object.MGMultResult[i] | 0;
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlka.RoundInfo
         * @static
         * @param {jlka.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.MGWildVS = [];
                object.MGWildWinner = [];
                object.FGNowValue = [];
                object.FGNewPlate = [];
                object.FGWinner = [];
                object.PlateSymbolLog = [];
                object.FGNowValueAfterDuel = [];
                object.MGMultResult = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RoundWin = 0;
                object.FreeRemainRound = 0;
                object.MGMult = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlka.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlka.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.MGWildVS && message.MGWildVS.length) {
                object.MGWildVS = [];
                for (let j = 0; j < message.MGWildVS.length; ++j)
                    object.MGWildVS[j] = $root.jlka.Column.toObject(message.MGWildVS[j], options);
            }
            if (message.MGWildWinner && message.MGWildWinner.length) {
                object.MGWildWinner = [];
                for (let j = 0; j < message.MGWildWinner.length; ++j)
                    object.MGWildWinner[j] = message.MGWildWinner[j];
            }
            if (message.MGMult != null && message.hasOwnProperty("MGMult"))
                object.MGMult = options.json && !isFinite(message.MGMult) ? String(message.MGMult) : message.MGMult;
            if (message.FGNowValue && message.FGNowValue.length) {
                object.FGNowValue = [];
                for (let j = 0; j < message.FGNowValue.length; ++j)
                    object.FGNowValue[j] = options.json && !isFinite(message.FGNowValue[j]) ? String(message.FGNowValue[j]) : message.FGNowValue[j];
            }
            if (message.FGNewPlate && message.FGNewPlate.length) {
                object.FGNewPlate = [];
                for (let j = 0; j < message.FGNewPlate.length; ++j)
                    object.FGNewPlate[j] = $root.jlka.ColumnFloat.toObject(message.FGNewPlate[j], options);
            }
            if (message.FGWinner && message.FGWinner.length) {
                object.FGWinner = [];
                for (let j = 0; j < message.FGWinner.length; ++j)
                    object.FGWinner[j] = message.FGWinner[j];
            }
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlka.Column.toObject(message.PlateSymbolLog[j], options);
            }
            if (message.FGNowValueAfterDuel && message.FGNowValueAfterDuel.length) {
                object.FGNowValueAfterDuel = [];
                for (let j = 0; j < message.FGNowValueAfterDuel.length; ++j)
                    object.FGNowValueAfterDuel[j] = options.json && !isFinite(message.FGNowValueAfterDuel[j]) ? String(message.FGNowValueAfterDuel[j]) : message.FGNowValueAfterDuel[j];
            }
            if (message.MGMultResult && message.MGMultResult.length) {
                object.MGMultResult = [];
                for (let j = 0; j < message.MGMultResult.length; ++j)
                    object.MGMultResult[j] = message.MGMultResult[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlka.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlka.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlka.RoundInfo";
        };

        return RoundInfo;
    })();

    jlka.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlka
         * @interface ISpinAck
         * @property {Array.<jlka.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlka
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlka.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlka.IRoundInfo>} RoundQueue
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlka.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlka.SpinAck
         * @static
         * @param {jlka.ISpinAck=} [properties] Properties to set
         * @returns {jlka.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlka.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlka.SpinAck
         * @static
         * @param {jlka.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlka.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlka.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlka.SpinAck
         * @static
         * @param {jlka.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlka.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlka.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlka.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlka.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Bet = reader.double();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlka.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlka.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlka.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlka.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlka.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlka.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlka.SpinAck)
                return object;
            let message = new $root.jlka.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlka.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlka.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlka.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlka.SpinAck
         * @static
         * @param {jlka.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlka.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlka.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlka.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlka.SpinAck";
        };

        return SpinAck;
    })();

    jlka.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlka
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlka
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlka.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlka.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlka.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlka.GameInfoData
         * @static
         * @param {jlka.IGameInfoData=} [properties] Properties to set
         * @returns {jlka.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlka.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlka.GameInfoData
         * @static
         * @param {jlka.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlka.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlka.GameInfoData
         * @static
         * @param {jlka.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlka.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlka.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlka.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlka.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlka.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlka.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlka.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlka.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlka.GameInfoData)
                return object;
            let message = new $root.jlka.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlka.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlka.GameInfoData
         * @static
         * @param {jlka.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlka.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlka.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlka.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlka;
})();

export const jllb = $root.jllb = (() => {

    /**
     * Namespace jllb.
     * @exports jllb
     * @namespace
     */
    const jllb = {};

    /**
     * Symbol enum.
     * @name jllb.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Free=1 Free value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     */
    jllb.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Free"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        return values;
    })();

    jllb.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jllb
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jllb
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jllb.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jllb.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jllb.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jllb.Block
         * @static
         * @param {jllb.IBlock=} [properties] Properties to set
         * @returns {jllb.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jllb.Block.verify|verify} messages.
         * @function encode
         * @memberof jllb.Block
         * @static
         * @param {jllb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jllb.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllb.Block
         * @static
         * @param {jllb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jllb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllb.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jllb.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllb.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllb.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jllb.Block)
                return object;
            let message = new $root.jllb.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllb.Block
         * @static
         * @param {jllb.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jllb.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jllb.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllb.Block";
        };

        return Block;
    })();

    jllb.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jllb
         * @interface IAwardReel
         * @property {number|null} [line] AwardReel line
         * @property {Array.<jllb.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jllb
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jllb.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel line.
         * @member {number} line
         * @memberof jllb.AwardReel
         * @instance
         */
        AwardReel.prototype.line = 0;

        /**
         * AwardReel block.
         * @member {Array.<jllb.IBlock>} block
         * @memberof jllb.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jllb.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jllb.AwardReel
         * @static
         * @param {jllb.IAwardReel=} [properties] Properties to set
         * @returns {jllb.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jllb.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jllb.AwardReel
         * @static
         * @param {jllb.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.line);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jllb.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jllb.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllb.AwardReel
         * @static
         * @param {jllb.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jllb.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllb.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllb.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.line = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jllb.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllb.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllb.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jllb.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.line != null && message.hasOwnProperty("line"))
                if (!$util.isInteger(message.line))
                    return "line: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jllb.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllb.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllb.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jllb.AwardReel)
                return object;
            let message = new $root.jllb.AwardReel();
            if (object.line != null)
                message.line = object.line >>> 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jllb.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jllb.AwardReel.block: object expected");
                    message.block[i] = $root.jllb.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllb.AwardReel
         * @static
         * @param {jllb.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.line = 0;
                object.win = 0;
            }
            if (message.line != null && message.hasOwnProperty("line"))
                object.line = message.line;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jllb.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jllb.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jllb.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllb.AwardReel";
        };

        return AwardReel;
    })();

    jllb.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jllb
         * @interface IPlate
         * @property {Array.<jllb.IRow>|null} [column] Plate column
         * @property {Array.<jllb.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         */

        /**
         * Constructs a new Plate.
         * @memberof jllb
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jllb.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jllb.IRow>} column
         * @memberof jllb.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jllb.IAwardReel>} award
         * @memberof jllb.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jllb.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jllb.Plate
         * @static
         * @param {jllb.IPlate=} [properties] Properties to set
         * @returns {jllb.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jllb.Plate.verify|verify} messages.
         * @function encode
         * @memberof jllb.Plate
         * @static
         * @param {jllb.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jllb.Row.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jllb.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jllb.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllb.Plate
         * @static
         * @param {jllb.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jllb.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllb.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllb.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jllb.Row.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jllb.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllb.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllb.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jllb.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jllb.Row.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jllb.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllb.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllb.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jllb.Plate)
                return object;
            let message = new $root.jllb.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jllb.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jllb.Plate.column: object expected");
                    message.column[i] = $root.jllb.Row.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jllb.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jllb.Plate.award: object expected");
                    message.award[i] = $root.jllb.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllb.Plate
         * @static
         * @param {jllb.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults)
                object.win = 0;
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jllb.Row.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jllb.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jllb.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jllb.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllb.Plate";
        };

        return Plate;
    })();

    jllb.Row = (function() {

        /**
         * Properties of a Row.
         * @memberof jllb
         * @interface IRow
         * @property {Array.<number>|null} [row] Row row
         * @property {Array.<number>|null} [bonus] Row bonus
         */

        /**
         * Constructs a new Row.
         * @memberof jllb
         * @classdesc Represents a Row.
         * @implements IRow
         * @constructor
         * @param {jllb.IRow=} [properties] Properties to set
         */
        function Row(properties) {
            this.row = [];
            this.bonus = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Row row.
         * @member {Array.<number>} row
         * @memberof jllb.Row
         * @instance
         */
        Row.prototype.row = $util.emptyArray;

        /**
         * Row bonus.
         * @member {Array.<number>} bonus
         * @memberof jllb.Row
         * @instance
         */
        Row.prototype.bonus = $util.emptyArray;

        /**
         * Creates a new Row instance using the specified properties.
         * @function create
         * @memberof jllb.Row
         * @static
         * @param {jllb.IRow=} [properties] Properties to set
         * @returns {jllb.Row} Row instance
         */
        Row.create = function create(properties) {
            return new Row(properties);
        };

        /**
         * Encodes the specified Row message. Does not implicitly {@link jllb.Row.verify|verify} messages.
         * @function encode
         * @memberof jllb.Row
         * @static
         * @param {jllb.IRow} message Row message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Row.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            if (message.bonus != null && message.bonus.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.bonus.length; ++i)
                    writer.double(message.bonus[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Row message, length delimited. Does not implicitly {@link jllb.Row.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllb.Row
         * @static
         * @param {jllb.IRow} message Row message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Row.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Row message from the specified reader or buffer.
         * @function decode
         * @memberof jllb.Row
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllb.Row} Row
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Row.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllb.Row();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.bonus && message.bonus.length))
                            message.bonus = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.bonus.push(reader.double());
                        } else
                            message.bonus.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Row message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllb.Row
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllb.Row} Row
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Row.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Row message.
         * @function verify
         * @memberof jllb.Row
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Row.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            if (message.bonus != null && message.hasOwnProperty("bonus")) {
                if (!Array.isArray(message.bonus))
                    return "bonus: array expected";
                for (let i = 0; i < message.bonus.length; ++i)
                    if (typeof message.bonus[i] !== "number")
                        return "bonus: number[] expected";
            }
            return null;
        };

        /**
         * Creates a Row message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllb.Row
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllb.Row} Row
         */
        Row.fromObject = function fromObject(object) {
            if (object instanceof $root.jllb.Row)
                return object;
            let message = new $root.jllb.Row();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jllb.Row.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            if (object.bonus) {
                if (!Array.isArray(object.bonus))
                    throw TypeError(".jllb.Row.bonus: array expected");
                message.bonus = [];
                for (let i = 0; i < object.bonus.length; ++i)
                    message.bonus[i] = Number(object.bonus[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Row message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllb.Row
         * @static
         * @param {jllb.Row} message Row
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Row.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.row = [];
                object.bonus = [];
            }
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            if (message.bonus && message.bonus.length) {
                object.bonus = [];
                for (let j = 0; j < message.bonus.length; ++j)
                    object.bonus[j] = options.json && !isFinite(message.bonus[j]) ? String(message.bonus[j]) : message.bonus[j];
            }
            return object;
        };

        /**
         * Converts this Row to JSON.
         * @function toJSON
         * @memberof jllb.Row
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Row.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Row
         * @function getTypeUrl
         * @memberof jllb.Row
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Row.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllb.Row";
        };

        return Row;
    })();

    jllb.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jllb
         * @interface IAllPlate
         * @property {Array.<jllb.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jllb
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jllb.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jllb.IPlate>} plate
         * @memberof jllb.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jllb.AllPlate
         * @static
         * @param {jllb.IAllPlate=} [properties] Properties to set
         * @returns {jllb.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jllb.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jllb.AllPlate
         * @static
         * @param {jllb.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jllb.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jllb.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllb.AllPlate
         * @static
         * @param {jllb.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jllb.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllb.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllb.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jllb.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllb.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllb.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jllb.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jllb.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllb.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllb.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jllb.AllPlate)
                return object;
            let message = new $root.jllb.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jllb.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jllb.AllPlate.plate: object expected");
                    message.plate[i] = $root.jllb.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllb.AllPlate
         * @static
         * @param {jllb.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jllb.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jllb.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jllb.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllb.AllPlate";
        };

        return AllPlate;
    })();

    return jllb;
})();

export const jllc = $root.jllc = (() => {

    /**
     * Namespace jllc.
     * @exports jllc
     * @namespace
     */
    const jllc = {};

    jllc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jllc
         * @interface ISpinAck
         * @property {Array.<number>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {number|null} [Award] SpinAck Award
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jllc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jllc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<number>} PlateSymbol
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck Award.
         * @member {number} Award
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.Award = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jllc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jllc.SpinAck
         * @static
         * @param {jllc.ISpinAck=} [properties] Properties to set
         * @returns {jllc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jllc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jllc.SpinAck
         * @static
         * @param {jllc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.int32(message.PlateSymbol[i]);
                writer.ldelim();
            }
            if (message.Award != null && Object.hasOwnProperty.call(message, "Award"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Award);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jllc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllc.SpinAck
         * @static
         * @param {jllc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jllc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateSymbol.push(reader.int32());
                        } else
                            message.PlateSymbol.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.Award = reader.int32();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jllc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isInteger(message.PlateSymbol[i]))
                        return "PlateSymbol: integer[] expected";
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                if (!$util.isInteger(message.Award))
                    return "Award: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jllc.SpinAck)
                return object;
            let message = new $root.jllc.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jllc.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = object.PlateSymbol[i] | 0;
            }
            if (object.Award != null)
                message.Award = object.Award | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllc.SpinAck
         * @static
         * @param {jllc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateSymbol = [];
            if (options.defaults) {
                object.Award = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                object.Award = message.Award;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jllc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jllc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllc.SpinAck";
        };

        return SpinAck;
    })();

    return jllc;
})();

export const jllg = $root.jllg = (() => {

    /**
     * Namespace jllg.
     * @exports jllg
     * @namespace
     */
    const jllg = {};

    /**
     * Symbol enum.
     * @name jllg.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Mix=8 Mix value
     * @property {number} MixBar=9 MixBar value
     */
    jllg.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Mix"] = 8;
        values[valuesById[9] = "MixBar"] = 9;
        return values;
    })();

    jllg.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jllg
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jllg
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jllg.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jllg.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jllg.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jllg.Block
         * @static
         * @param {jllg.IBlock=} [properties] Properties to set
         * @returns {jllg.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jllg.Block.verify|verify} messages.
         * @function encode
         * @memberof jllg.Block
         * @static
         * @param {jllg.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jllg.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.Block
         * @static
         * @param {jllg.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jllg.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.Block)
                return object;
            let message = new $root.jllg.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.Block
         * @static
         * @param {jllg.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jllg.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jllg.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.Block";
        };

        return Block;
    })();

    jllg.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jllg
         * @interface IPlate
         * @property {Array.<jllg.IColumnData>|null} [column] Plate column
         * @property {number|null} [win] Plate win
         * @property {jllg.IBonusInfo|null} [gold] Plate gold
         * @property {number|null} [goldWin] Plate goldWin
         * @property {jllg.IJpInfo|null} [Jp] Plate Jp
         */

        /**
         * Constructs a new Plate.
         * @memberof jllg
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jllg.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jllg.IColumnData>} column
         * @memberof jllg.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jllg.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate gold.
         * @member {jllg.IBonusInfo|null|undefined} gold
         * @memberof jllg.Plate
         * @instance
         */
        Plate.prototype.gold = null;

        /**
         * Plate goldWin.
         * @member {number} goldWin
         * @memberof jllg.Plate
         * @instance
         */
        Plate.prototype.goldWin = 0;

        /**
         * Plate Jp.
         * @member {jllg.IJpInfo|null|undefined} Jp
         * @memberof jllg.Plate
         * @instance
         */
        Plate.prototype.Jp = null;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jllg.Plate
         * @static
         * @param {jllg.IPlate=} [properties] Properties to set
         * @returns {jllg.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jllg.Plate.verify|verify} messages.
         * @function encode
         * @memberof jllg.Plate
         * @static
         * @param {jllg.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jllg.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.win);
            if (message.gold != null && Object.hasOwnProperty.call(message, "gold"))
                $root.jllg.BonusInfo.encode(message.gold, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.goldWin != null && Object.hasOwnProperty.call(message, "goldWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.goldWin);
            if (message.Jp != null && Object.hasOwnProperty.call(message, "Jp"))
                $root.jllg.JpInfo.encode(message.Jp, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jllg.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.Plate
         * @static
         * @param {jllg.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jllg.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.win = reader.double();
                        break;
                    }
                case 3: {
                        message.gold = $root.jllg.BonusInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.goldWin = reader.double();
                        break;
                    }
                case 5: {
                        message.Jp = $root.jllg.JpInfo.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jllg.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jllg.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.gold != null && message.hasOwnProperty("gold")) {
                let error = $root.jllg.BonusInfo.verify(message.gold);
                if (error)
                    return "gold." + error;
            }
            if (message.goldWin != null && message.hasOwnProperty("goldWin"))
                if (typeof message.goldWin !== "number")
                    return "goldWin: number expected";
            if (message.Jp != null && message.hasOwnProperty("Jp")) {
                let error = $root.jllg.JpInfo.verify(message.Jp);
                if (error)
                    return "Jp." + error;
            }
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.Plate)
                return object;
            let message = new $root.jllg.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jllg.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jllg.Plate.column: object expected");
                    message.column[i] = $root.jllg.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.gold != null) {
                if (typeof object.gold !== "object")
                    throw TypeError(".jllg.Plate.gold: object expected");
                message.gold = $root.jllg.BonusInfo.fromObject(object.gold);
            }
            if (object.goldWin != null)
                message.goldWin = Number(object.goldWin);
            if (object.Jp != null) {
                if (typeof object.Jp !== "object")
                    throw TypeError(".jllg.Plate.Jp: object expected");
                message.Jp = $root.jllg.JpInfo.fromObject(object.Jp);
            }
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.Plate
         * @static
         * @param {jllg.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.column = [];
            if (options.defaults) {
                object.win = 0;
                object.gold = null;
                object.goldWin = 0;
                object.Jp = null;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jllg.ColumnData.toObject(message.column[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.gold != null && message.hasOwnProperty("gold"))
                object.gold = $root.jllg.BonusInfo.toObject(message.gold, options);
            if (message.goldWin != null && message.hasOwnProperty("goldWin"))
                object.goldWin = options.json && !isFinite(message.goldWin) ? String(message.goldWin) : message.goldWin;
            if (message.Jp != null && message.hasOwnProperty("Jp"))
                object.Jp = $root.jllg.JpInfo.toObject(message.Jp, options);
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jllg.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jllg.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.Plate";
        };

        return Plate;
    })();

    jllg.BonusInfo = (function() {

        /**
         * Properties of a BonusInfo.
         * @memberof jllg
         * @interface IBonusInfo
         * @property {number|null} [barWin] BonusInfo barWin
         * @property {number|Long|null} [count] BonusInfo count
         * @property {Array.<number>|null} [gold] BonusInfo gold
         */

        /**
         * Constructs a new BonusInfo.
         * @memberof jllg
         * @classdesc Represents a BonusInfo.
         * @implements IBonusInfo
         * @constructor
         * @param {jllg.IBonusInfo=} [properties] Properties to set
         */
        function BonusInfo(properties) {
            this.gold = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusInfo barWin.
         * @member {number} barWin
         * @memberof jllg.BonusInfo
         * @instance
         */
        BonusInfo.prototype.barWin = 0;

        /**
         * BonusInfo count.
         * @member {number|Long} count
         * @memberof jllg.BonusInfo
         * @instance
         */
        BonusInfo.prototype.count = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BonusInfo gold.
         * @member {Array.<number>} gold
         * @memberof jllg.BonusInfo
         * @instance
         */
        BonusInfo.prototype.gold = $util.emptyArray;

        /**
         * Creates a new BonusInfo instance using the specified properties.
         * @function create
         * @memberof jllg.BonusInfo
         * @static
         * @param {jllg.IBonusInfo=} [properties] Properties to set
         * @returns {jllg.BonusInfo} BonusInfo instance
         */
        BonusInfo.create = function create(properties) {
            return new BonusInfo(properties);
        };

        /**
         * Encodes the specified BonusInfo message. Does not implicitly {@link jllg.BonusInfo.verify|verify} messages.
         * @function encode
         * @memberof jllg.BonusInfo
         * @static
         * @param {jllg.IBonusInfo} message BonusInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.barWin != null && Object.hasOwnProperty.call(message, "barWin"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.barWin);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.count);
            if (message.gold != null && message.gold.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.gold.length; ++i)
                    writer.double(message.gold[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified BonusInfo message, length delimited. Does not implicitly {@link jllg.BonusInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.BonusInfo
         * @static
         * @param {jllg.IBonusInfo} message BonusInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.BonusInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.BonusInfo} BonusInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.BonusInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.barWin = reader.double();
                        break;
                    }
                case 2: {
                        message.count = reader.int64();
                        break;
                    }
                case 3: {
                        if (!(message.gold && message.gold.length))
                            message.gold = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.gold.push(reader.double());
                        } else
                            message.gold.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.BonusInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.BonusInfo} BonusInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusInfo message.
         * @function verify
         * @memberof jllg.BonusInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.barWin != null && message.hasOwnProperty("barWin"))
                if (typeof message.barWin !== "number")
                    return "barWin: number expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count) && !(message.count && $util.isInteger(message.count.low) && $util.isInteger(message.count.high)))
                    return "count: integer|Long expected";
            if (message.gold != null && message.hasOwnProperty("gold")) {
                if (!Array.isArray(message.gold))
                    return "gold: array expected";
                for (let i = 0; i < message.gold.length; ++i)
                    if (typeof message.gold[i] !== "number")
                        return "gold: number[] expected";
            }
            return null;
        };

        /**
         * Creates a BonusInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.BonusInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.BonusInfo} BonusInfo
         */
        BonusInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.BonusInfo)
                return object;
            let message = new $root.jllg.BonusInfo();
            if (object.barWin != null)
                message.barWin = Number(object.barWin);
            if (object.count != null)
                if ($util.Long)
                    (message.count = $util.Long.fromValue(object.count)).unsigned = false;
                else if (typeof object.count === "string")
                    message.count = parseInt(object.count, 10);
                else if (typeof object.count === "number")
                    message.count = object.count;
                else if (typeof object.count === "object")
                    message.count = new $util.LongBits(object.count.low >>> 0, object.count.high >>> 0).toNumber();
            if (object.gold) {
                if (!Array.isArray(object.gold))
                    throw TypeError(".jllg.BonusInfo.gold: array expected");
                message.gold = [];
                for (let i = 0; i < object.gold.length; ++i)
                    message.gold[i] = Number(object.gold[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a BonusInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.BonusInfo
         * @static
         * @param {jllg.BonusInfo} message BonusInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gold = [];
            if (options.defaults) {
                object.barWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.count = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.count = options.longs === String ? "0" : 0;
            }
            if (message.barWin != null && message.hasOwnProperty("barWin"))
                object.barWin = options.json && !isFinite(message.barWin) ? String(message.barWin) : message.barWin;
            if (message.count != null && message.hasOwnProperty("count"))
                if (typeof message.count === "number")
                    object.count = options.longs === String ? String(message.count) : message.count;
                else
                    object.count = options.longs === String ? $util.Long.prototype.toString.call(message.count) : options.longs === Number ? new $util.LongBits(message.count.low >>> 0, message.count.high >>> 0).toNumber() : message.count;
            if (message.gold && message.gold.length) {
                object.gold = [];
                for (let j = 0; j < message.gold.length; ++j)
                    object.gold[j] = options.json && !isFinite(message.gold[j]) ? String(message.gold[j]) : message.gold[j];
            }
            return object;
        };

        /**
         * Converts this BonusInfo to JSON.
         * @function toJSON
         * @memberof jllg.BonusInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusInfo
         * @function getTypeUrl
         * @memberof jllg.BonusInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.BonusInfo";
        };

        return BonusInfo;
    })();

    jllg.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jllg
         * @interface IAllPlate
         * @property {Array.<jllg.IPlate>|null} [plate] AllPlate plate
         * @property {Array.<jllg.IJpCurrency>|null} [jp] AllPlate jp
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jllg
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jllg.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            this.jp = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jllg.IPlate>} plate
         * @memberof jllg.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * AllPlate jp.
         * @member {Array.<jllg.IJpCurrency>} jp
         * @memberof jllg.AllPlate
         * @instance
         */
        AllPlate.prototype.jp = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jllg.AllPlate
         * @static
         * @param {jllg.IAllPlate=} [properties] Properties to set
         * @returns {jllg.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jllg.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jllg.AllPlate
         * @static
         * @param {jllg.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jllg.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.jp != null && message.jp.length)
                for (let i = 0; i < message.jp.length; ++i)
                    $root.jllg.JpCurrency.encode(message.jp[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jllg.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.AllPlate
         * @static
         * @param {jllg.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jllg.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.jp && message.jp.length))
                            message.jp = [];
                        message.jp.push($root.jllg.JpCurrency.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jllg.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jllg.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            if (message.jp != null && message.hasOwnProperty("jp")) {
                if (!Array.isArray(message.jp))
                    return "jp: array expected";
                for (let i = 0; i < message.jp.length; ++i) {
                    let error = $root.jllg.JpCurrency.verify(message.jp[i]);
                    if (error)
                        return "jp." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.AllPlate)
                return object;
            let message = new $root.jllg.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jllg.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jllg.AllPlate.plate: object expected");
                    message.plate[i] = $root.jllg.Plate.fromObject(object.plate[i]);
                }
            }
            if (object.jp) {
                if (!Array.isArray(object.jp))
                    throw TypeError(".jllg.AllPlate.jp: array expected");
                message.jp = [];
                for (let i = 0; i < object.jp.length; ++i) {
                    if (typeof object.jp[i] !== "object")
                        throw TypeError(".jllg.AllPlate.jp: object expected");
                    message.jp[i] = $root.jllg.JpCurrency.fromObject(object.jp[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.AllPlate
         * @static
         * @param {jllg.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.plate = [];
                object.jp = [];
            }
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jllg.Plate.toObject(message.plate[j], options);
            }
            if (message.jp && message.jp.length) {
                object.jp = [];
                for (let j = 0; j < message.jp.length; ++j)
                    object.jp[j] = $root.jllg.JpCurrency.toObject(message.jp[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jllg.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jllg.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.AllPlate";
        };

        return AllPlate;
    })();

    jllg.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jllg
         * @interface IColumnData
         * @property {Array.<jllg.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jllg
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jllg.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jllg.ICell>} row
         * @memberof jllg.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jllg.ColumnData
         * @static
         * @param {jllg.IColumnData=} [properties] Properties to set
         * @returns {jllg.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jllg.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jllg.ColumnData
         * @static
         * @param {jllg.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jllg.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jllg.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.ColumnData
         * @static
         * @param {jllg.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jllg.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jllg.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jllg.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.ColumnData)
                return object;
            let message = new $root.jllg.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jllg.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jllg.ColumnData.row: object expected");
                    message.row[i] = $root.jllg.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.ColumnData
         * @static
         * @param {jllg.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jllg.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jllg.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jllg.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.ColumnData";
        };

        return ColumnData;
    })();

    jllg.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jllg
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {boolean|null} [isConnect] Cell isConnect
         */

        /**
         * Constructs a new Cell.
         * @memberof jllg
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jllg.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jllg.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell isConnect.
         * @member {boolean} isConnect
         * @memberof jllg.Cell
         * @instance
         */
        Cell.prototype.isConnect = false;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jllg.Cell
         * @static
         * @param {jllg.ICell=} [properties] Properties to set
         * @returns {jllg.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jllg.Cell.verify|verify} messages.
         * @function encode
         * @memberof jllg.Cell
         * @static
         * @param {jllg.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isConnect);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jllg.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.Cell
         * @static
         * @param {jllg.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.isConnect = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jllg.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.Cell)
                return object;
            let message = new $root.jllg.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.Cell
         * @static
         * @param {jllg.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.isConnect = false;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jllg.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jllg.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.Cell";
        };

        return Cell;
    })();

    jllg.JpCurrency = (function() {

        /**
         * Properties of a JpCurrency.
         * @memberof jllg
         * @interface IJpCurrency
         * @property {number|null} [currency] JpCurrency currency
         * @property {Array.<jllg.IJpInfo>|null} [info] JpCurrency info
         */

        /**
         * Constructs a new JpCurrency.
         * @memberof jllg
         * @classdesc Represents a JpCurrency.
         * @implements IJpCurrency
         * @constructor
         * @param {jllg.IJpCurrency=} [properties] Properties to set
         */
        function JpCurrency(properties) {
            this.info = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JpCurrency currency.
         * @member {number} currency
         * @memberof jllg.JpCurrency
         * @instance
         */
        JpCurrency.prototype.currency = 0;

        /**
         * JpCurrency info.
         * @member {Array.<jllg.IJpInfo>} info
         * @memberof jllg.JpCurrency
         * @instance
         */
        JpCurrency.prototype.info = $util.emptyArray;

        /**
         * Creates a new JpCurrency instance using the specified properties.
         * @function create
         * @memberof jllg.JpCurrency
         * @static
         * @param {jllg.IJpCurrency=} [properties] Properties to set
         * @returns {jllg.JpCurrency} JpCurrency instance
         */
        JpCurrency.create = function create(properties) {
            return new JpCurrency(properties);
        };

        /**
         * Encodes the specified JpCurrency message. Does not implicitly {@link jllg.JpCurrency.verify|verify} messages.
         * @function encode
         * @memberof jllg.JpCurrency
         * @static
         * @param {jllg.IJpCurrency} message JpCurrency message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpCurrency.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.currency);
            if (message.info != null && message.info.length)
                for (let i = 0; i < message.info.length; ++i)
                    $root.jllg.JpInfo.encode(message.info[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified JpCurrency message, length delimited. Does not implicitly {@link jllg.JpCurrency.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.JpCurrency
         * @static
         * @param {jllg.IJpCurrency} message JpCurrency message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpCurrency.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JpCurrency message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.JpCurrency
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.JpCurrency} JpCurrency
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpCurrency.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.JpCurrency();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.currency = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.info && message.info.length))
                            message.info = [];
                        message.info.push($root.jllg.JpInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JpCurrency message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.JpCurrency
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.JpCurrency} JpCurrency
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpCurrency.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JpCurrency message.
         * @function verify
         * @memberof jllg.JpCurrency
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JpCurrency.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.currency != null && message.hasOwnProperty("currency"))
                if (!$util.isInteger(message.currency))
                    return "currency: integer expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                if (!Array.isArray(message.info))
                    return "info: array expected";
                for (let i = 0; i < message.info.length; ++i) {
                    let error = $root.jllg.JpInfo.verify(message.info[i]);
                    if (error)
                        return "info." + error;
                }
            }
            return null;
        };

        /**
         * Creates a JpCurrency message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.JpCurrency
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.JpCurrency} JpCurrency
         */
        JpCurrency.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.JpCurrency)
                return object;
            let message = new $root.jllg.JpCurrency();
            if (object.currency != null)
                message.currency = object.currency | 0;
            if (object.info) {
                if (!Array.isArray(object.info))
                    throw TypeError(".jllg.JpCurrency.info: array expected");
                message.info = [];
                for (let i = 0; i < object.info.length; ++i) {
                    if (typeof object.info[i] !== "object")
                        throw TypeError(".jllg.JpCurrency.info: object expected");
                    message.info[i] = $root.jllg.JpInfo.fromObject(object.info[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a JpCurrency message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.JpCurrency
         * @static
         * @param {jllg.JpCurrency} message JpCurrency
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JpCurrency.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.info = [];
            if (options.defaults)
                object.currency = 0;
            if (message.currency != null && message.hasOwnProperty("currency"))
                object.currency = message.currency;
            if (message.info && message.info.length) {
                object.info = [];
                for (let j = 0; j < message.info.length; ++j)
                    object.info[j] = $root.jllg.JpInfo.toObject(message.info[j], options);
            }
            return object;
        };

        /**
         * Converts this JpCurrency to JSON.
         * @function toJSON
         * @memberof jllg.JpCurrency
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JpCurrency.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JpCurrency
         * @function getTypeUrl
         * @memberof jllg.JpCurrency
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JpCurrency.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.JpCurrency";
        };

        return JpCurrency;
    })();

    jllg.JpInfo = (function() {

        /**
         * Properties of a JpInfo.
         * @memberof jllg
         * @interface IJpInfo
         * @property {number|null} [type] JpInfo type
         * @property {number|null} [value] JpInfo value
         */

        /**
         * Constructs a new JpInfo.
         * @memberof jllg
         * @classdesc Represents a JpInfo.
         * @implements IJpInfo
         * @constructor
         * @param {jllg.IJpInfo=} [properties] Properties to set
         */
        function JpInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JpInfo type.
         * @member {number} type
         * @memberof jllg.JpInfo
         * @instance
         */
        JpInfo.prototype.type = 0;

        /**
         * JpInfo value.
         * @member {number} value
         * @memberof jllg.JpInfo
         * @instance
         */
        JpInfo.prototype.value = 0;

        /**
         * Creates a new JpInfo instance using the specified properties.
         * @function create
         * @memberof jllg.JpInfo
         * @static
         * @param {jllg.IJpInfo=} [properties] Properties to set
         * @returns {jllg.JpInfo} JpInfo instance
         */
        JpInfo.create = function create(properties) {
            return new JpInfo(properties);
        };

        /**
         * Encodes the specified JpInfo message. Does not implicitly {@link jllg.JpInfo.verify|verify} messages.
         * @function encode
         * @memberof jllg.JpInfo
         * @static
         * @param {jllg.IJpInfo} message JpInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
            return writer;
        };

        /**
         * Encodes the specified JpInfo message, length delimited. Does not implicitly {@link jllg.JpInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.JpInfo
         * @static
         * @param {jllg.IJpInfo} message JpInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JpInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.JpInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.JpInfo} JpInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.JpInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                case 2: {
                        message.value = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JpInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.JpInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.JpInfo} JpInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JpInfo message.
         * @function verify
         * @memberof jllg.JpInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JpInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            return null;
        };

        /**
         * Creates a JpInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.JpInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.JpInfo} JpInfo
         */
        JpInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.JpInfo)
                return object;
            let message = new $root.jllg.JpInfo();
            if (object.type != null)
                message.type = object.type | 0;
            if (object.value != null)
                message.value = Number(object.value);
            return message;
        };

        /**
         * Creates a plain object from a JpInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.JpInfo
         * @static
         * @param {jllg.JpInfo} message JpInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JpInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.value = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
            return object;
        };

        /**
         * Converts this JpInfo to JSON.
         * @function toJSON
         * @memberof jllg.JpInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JpInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JpInfo
         * @function getTypeUrl
         * @memberof jllg.JpInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JpInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.JpInfo";
        };

        return JpInfo;
    })();

    /**
     * JpType enum.
     * @name jllg.JpType
     * @enum {number}
     * @property {number} None=0 None value
     * @property {number} Super=1 Super value
     * @property {number} Mega=2 Mega value
     * @property {number} Grand=3 Grand value
     */
    jllg.JpType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "None"] = 0;
        values[valuesById[1] = "Super"] = 1;
        values[valuesById[2] = "Mega"] = 2;
        values[valuesById[3] = "Grand"] = 3;
        return values;
    })();

    jllg.JpReq = (function() {

        /**
         * Properties of a JpReq.
         * @memberof jllg
         * @interface IJpReq
         */

        /**
         * Constructs a new JpReq.
         * @memberof jllg
         * @classdesc Represents a JpReq.
         * @implements IJpReq
         * @constructor
         * @param {jllg.IJpReq=} [properties] Properties to set
         */
        function JpReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new JpReq instance using the specified properties.
         * @function create
         * @memberof jllg.JpReq
         * @static
         * @param {jllg.IJpReq=} [properties] Properties to set
         * @returns {jllg.JpReq} JpReq instance
         */
        JpReq.create = function create(properties) {
            return new JpReq(properties);
        };

        /**
         * Encodes the specified JpReq message. Does not implicitly {@link jllg.JpReq.verify|verify} messages.
         * @function encode
         * @memberof jllg.JpReq
         * @static
         * @param {jllg.IJpReq} message JpReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified JpReq message, length delimited. Does not implicitly {@link jllg.JpReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.JpReq
         * @static
         * @param {jllg.IJpReq} message JpReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JpReq message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.JpReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.JpReq} JpReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.JpReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JpReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.JpReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.JpReq} JpReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JpReq message.
         * @function verify
         * @memberof jllg.JpReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JpReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a JpReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.JpReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.JpReq} JpReq
         */
        JpReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.JpReq)
                return object;
            return new $root.jllg.JpReq();
        };

        /**
         * Creates a plain object from a JpReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.JpReq
         * @static
         * @param {jllg.JpReq} message JpReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JpReq.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this JpReq to JSON.
         * @function toJSON
         * @memberof jllg.JpReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JpReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JpReq
         * @function getTypeUrl
         * @memberof jllg.JpReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JpReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.JpReq";
        };

        return JpReq;
    })();

    jllg.JpResp = (function() {

        /**
         * Properties of a JpResp.
         * @memberof jllg
         * @interface IJpResp
         * @property {Array.<jllg.IJpCurrency>|null} [jp] JpResp jp
         */

        /**
         * Constructs a new JpResp.
         * @memberof jllg
         * @classdesc Represents a JpResp.
         * @implements IJpResp
         * @constructor
         * @param {jllg.IJpResp=} [properties] Properties to set
         */
        function JpResp(properties) {
            this.jp = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JpResp jp.
         * @member {Array.<jllg.IJpCurrency>} jp
         * @memberof jllg.JpResp
         * @instance
         */
        JpResp.prototype.jp = $util.emptyArray;

        /**
         * Creates a new JpResp instance using the specified properties.
         * @function create
         * @memberof jllg.JpResp
         * @static
         * @param {jllg.IJpResp=} [properties] Properties to set
         * @returns {jllg.JpResp} JpResp instance
         */
        JpResp.create = function create(properties) {
            return new JpResp(properties);
        };

        /**
         * Encodes the specified JpResp message. Does not implicitly {@link jllg.JpResp.verify|verify} messages.
         * @function encode
         * @memberof jllg.JpResp
         * @static
         * @param {jllg.IJpResp} message JpResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpResp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.jp != null && message.jp.length)
                for (let i = 0; i < message.jp.length; ++i)
                    $root.jllg.JpCurrency.encode(message.jp[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified JpResp message, length delimited. Does not implicitly {@link jllg.JpResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.JpResp
         * @static
         * @param {jllg.IJpResp} message JpResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JpResp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JpResp message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.JpResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.JpResp} JpResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpResp.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.JpResp();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        if (!(message.jp && message.jp.length))
                            message.jp = [];
                        message.jp.push($root.jllg.JpCurrency.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JpResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.JpResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.JpResp} JpResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JpResp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JpResp message.
         * @function verify
         * @memberof jllg.JpResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JpResp.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.jp != null && message.hasOwnProperty("jp")) {
                if (!Array.isArray(message.jp))
                    return "jp: array expected";
                for (let i = 0; i < message.jp.length; ++i) {
                    let error = $root.jllg.JpCurrency.verify(message.jp[i]);
                    if (error)
                        return "jp." + error;
                }
            }
            return null;
        };

        /**
         * Creates a JpResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.JpResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.JpResp} JpResp
         */
        JpResp.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.JpResp)
                return object;
            let message = new $root.jllg.JpResp();
            if (object.jp) {
                if (!Array.isArray(object.jp))
                    throw TypeError(".jllg.JpResp.jp: array expected");
                message.jp = [];
                for (let i = 0; i < object.jp.length; ++i) {
                    if (typeof object.jp[i] !== "object")
                        throw TypeError(".jllg.JpResp.jp: object expected");
                    message.jp[i] = $root.jllg.JpCurrency.fromObject(object.jp[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a JpResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.JpResp
         * @static
         * @param {jllg.JpResp} message JpResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JpResp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.jp = [];
            if (message.jp && message.jp.length) {
                object.jp = [];
                for (let j = 0; j < message.jp.length; ++j)
                    object.jp[j] = $root.jllg.JpCurrency.toObject(message.jp[j], options);
            }
            return object;
        };

        /**
         * Converts this JpResp to JSON.
         * @function toJSON
         * @memberof jllg.JpResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JpResp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JpResp
         * @function getTypeUrl
         * @memberof jllg.JpResp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JpResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.JpResp";
        };

        return JpResp;
    })();

    /**
     * AckType enum.
     * @name jllg.AckType
     * @enum {number}
     * @property {number} syncJP=0 syncJP value
     */
    jllg.AckType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "syncJP"] = 0;
        return values;
    })();

    /**
     * Error enum.
     * @name jllg.Error
     * @enum {number}
     * @property {number} success=0 success value
     * @property {number} unknown=1 unknown value
     */
    jllg.Error = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "success"] = 0;
        values[valuesById[1] = "unknown"] = 1;
        return values;
    })();

    jllg.CustomReq = (function() {

        /**
         * Properties of a CustomReq.
         * @memberof jllg
         * @interface ICustomReq
         * @property {number|null} [ack] CustomReq ack
         * @property {Uint8Array|null} [req] CustomReq req
         */

        /**
         * Constructs a new CustomReq.
         * @memberof jllg
         * @classdesc Represents a CustomReq.
         * @implements ICustomReq
         * @constructor
         * @param {jllg.ICustomReq=} [properties] Properties to set
         */
        function CustomReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CustomReq ack.
         * @member {number} ack
         * @memberof jllg.CustomReq
         * @instance
         */
        CustomReq.prototype.ack = 0;

        /**
         * CustomReq req.
         * @member {Uint8Array} req
         * @memberof jllg.CustomReq
         * @instance
         */
        CustomReq.prototype.req = $util.newBuffer([]);

        /**
         * Creates a new CustomReq instance using the specified properties.
         * @function create
         * @memberof jllg.CustomReq
         * @static
         * @param {jllg.ICustomReq=} [properties] Properties to set
         * @returns {jllg.CustomReq} CustomReq instance
         */
        CustomReq.create = function create(properties) {
            return new CustomReq(properties);
        };

        /**
         * Encodes the specified CustomReq message. Does not implicitly {@link jllg.CustomReq.verify|verify} messages.
         * @function encode
         * @memberof jllg.CustomReq
         * @static
         * @param {jllg.ICustomReq} message CustomReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ack != null && Object.hasOwnProperty.call(message, "ack"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ack);
            if (message.req != null && Object.hasOwnProperty.call(message, "req"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.req);
            return writer;
        };

        /**
         * Encodes the specified CustomReq message, length delimited. Does not implicitly {@link jllg.CustomReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.CustomReq
         * @static
         * @param {jllg.ICustomReq} message CustomReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CustomReq message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.CustomReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.CustomReq} CustomReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.CustomReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ack = reader.int32();
                        break;
                    }
                case 2: {
                        message.req = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CustomReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.CustomReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.CustomReq} CustomReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CustomReq message.
         * @function verify
         * @memberof jllg.CustomReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CustomReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ack != null && message.hasOwnProperty("ack"))
                if (!$util.isInteger(message.ack))
                    return "ack: integer expected";
            if (message.req != null && message.hasOwnProperty("req"))
                if (!(message.req && typeof message.req.length === "number" || $util.isString(message.req)))
                    return "req: buffer expected";
            return null;
        };

        /**
         * Creates a CustomReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.CustomReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.CustomReq} CustomReq
         */
        CustomReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.CustomReq)
                return object;
            let message = new $root.jllg.CustomReq();
            if (object.ack != null)
                message.ack = object.ack | 0;
            if (object.req != null)
                if (typeof object.req === "string")
                    $util.base64.decode(object.req, message.req = $util.newBuffer($util.base64.length(object.req)), 0);
                else if (object.req.length >= 0)
                    message.req = object.req;
            return message;
        };

        /**
         * Creates a plain object from a CustomReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.CustomReq
         * @static
         * @param {jllg.CustomReq} message CustomReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CustomReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ack = 0;
                if (options.bytes === String)
                    object.req = "";
                else {
                    object.req = [];
                    if (options.bytes !== Array)
                        object.req = $util.newBuffer(object.req);
                }
            }
            if (message.ack != null && message.hasOwnProperty("ack"))
                object.ack = message.ack;
            if (message.req != null && message.hasOwnProperty("req"))
                object.req = options.bytes === String ? $util.base64.encode(message.req, 0, message.req.length) : options.bytes === Array ? Array.prototype.slice.call(message.req) : message.req;
            return object;
        };

        /**
         * Converts this CustomReq to JSON.
         * @function toJSON
         * @memberof jllg.CustomReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CustomReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CustomReq
         * @function getTypeUrl
         * @memberof jllg.CustomReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CustomReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.CustomReq";
        };

        return CustomReq;
    })();

    jllg.CustomResp = (function() {

        /**
         * Properties of a CustomResp.
         * @memberof jllg
         * @interface ICustomResp
         * @property {number|null} [ack] CustomResp ack
         * @property {Uint8Array|null} [response] CustomResp response
         * @property {number|null} [error] CustomResp error
         */

        /**
         * Constructs a new CustomResp.
         * @memberof jllg
         * @classdesc Represents a CustomResp.
         * @implements ICustomResp
         * @constructor
         * @param {jllg.ICustomResp=} [properties] Properties to set
         */
        function CustomResp(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CustomResp ack.
         * @member {number} ack
         * @memberof jllg.CustomResp
         * @instance
         */
        CustomResp.prototype.ack = 0;

        /**
         * CustomResp response.
         * @member {Uint8Array} response
         * @memberof jllg.CustomResp
         * @instance
         */
        CustomResp.prototype.response = $util.newBuffer([]);

        /**
         * CustomResp error.
         * @member {number} error
         * @memberof jllg.CustomResp
         * @instance
         */
        CustomResp.prototype.error = 0;

        /**
         * Creates a new CustomResp instance using the specified properties.
         * @function create
         * @memberof jllg.CustomResp
         * @static
         * @param {jllg.ICustomResp=} [properties] Properties to set
         * @returns {jllg.CustomResp} CustomResp instance
         */
        CustomResp.create = function create(properties) {
            return new CustomResp(properties);
        };

        /**
         * Encodes the specified CustomResp message. Does not implicitly {@link jllg.CustomResp.verify|verify} messages.
         * @function encode
         * @memberof jllg.CustomResp
         * @static
         * @param {jllg.ICustomResp} message CustomResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomResp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ack != null && Object.hasOwnProperty.call(message, "ack"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ack);
            if (message.response != null && Object.hasOwnProperty.call(message, "response"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.response);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.error);
            return writer;
        };

        /**
         * Encodes the specified CustomResp message, length delimited. Does not implicitly {@link jllg.CustomResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllg.CustomResp
         * @static
         * @param {jllg.ICustomResp} message CustomResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CustomResp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CustomResp message from the specified reader or buffer.
         * @function decode
         * @memberof jllg.CustomResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllg.CustomResp} CustomResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomResp.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllg.CustomResp();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ack = reader.int32();
                        break;
                    }
                case 2: {
                        message.response = reader.bytes();
                        break;
                    }
                case 3: {
                        message.error = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CustomResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllg.CustomResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllg.CustomResp} CustomResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CustomResp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CustomResp message.
         * @function verify
         * @memberof jllg.CustomResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CustomResp.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ack != null && message.hasOwnProperty("ack"))
                if (!$util.isInteger(message.ack))
                    return "ack: integer expected";
            if (message.response != null && message.hasOwnProperty("response"))
                if (!(message.response && typeof message.response.length === "number" || $util.isString(message.response)))
                    return "response: buffer expected";
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isInteger(message.error))
                    return "error: integer expected";
            return null;
        };

        /**
         * Creates a CustomResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllg.CustomResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllg.CustomResp} CustomResp
         */
        CustomResp.fromObject = function fromObject(object) {
            if (object instanceof $root.jllg.CustomResp)
                return object;
            let message = new $root.jllg.CustomResp();
            if (object.ack != null)
                message.ack = object.ack | 0;
            if (object.response != null)
                if (typeof object.response === "string")
                    $util.base64.decode(object.response, message.response = $util.newBuffer($util.base64.length(object.response)), 0);
                else if (object.response.length >= 0)
                    message.response = object.response;
            if (object.error != null)
                message.error = object.error | 0;
            return message;
        };

        /**
         * Creates a plain object from a CustomResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllg.CustomResp
         * @static
         * @param {jllg.CustomResp} message CustomResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CustomResp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ack = 0;
                if (options.bytes === String)
                    object.response = "";
                else {
                    object.response = [];
                    if (options.bytes !== Array)
                        object.response = $util.newBuffer(object.response);
                }
                object.error = 0;
            }
            if (message.ack != null && message.hasOwnProperty("ack"))
                object.ack = message.ack;
            if (message.response != null && message.hasOwnProperty("response"))
                object.response = options.bytes === String ? $util.base64.encode(message.response, 0, message.response.length) : options.bytes === Array ? Array.prototype.slice.call(message.response) : message.response;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this CustomResp to JSON.
         * @function toJSON
         * @memberof jllg.CustomResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CustomResp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CustomResp
         * @function getTypeUrl
         * @memberof jllg.CustomResp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CustomResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllg.CustomResp";
        };

        return CustomResp;
    })();

    return jllg;
})();

export const jllib = $root.jllib = (() => {

    /**
     * Namespace jllib.
     * @exports jllib
     * @namespace
     */
    const jllib = {};

    /**
     * U2SCmd enum.
     * @name jllib.U2SCmd
     * @enum {number}
     * @property {number} U2S_Empty=0 U2S_Empty value
     * @property {number} U2S_Bet=12 U2S_Bet value
     * @property {number} U2S_Setting=13 U2S_Setting value
     */
    jllib.U2SCmd = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "U2S_Empty"] = 0;
        values[valuesById[12] = "U2S_Bet"] = 12;
        values[valuesById[13] = "U2S_Setting"] = 13;
        return values;
    })();

    /**
     * S2UCmd enum.
     * @name jllib.S2UCmd
     * @enum {number}
     * @property {number} S2U_Empty=0 S2U_Empty value
     * @property {number} S2U_Bet=12 S2U_Bet value
     * @property {number} S2U_Setting=13 S2U_Setting value
     */
    jllib.S2UCmd = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "S2U_Empty"] = 0;
        values[valuesById[12] = "S2U_Bet"] = 12;
        values[valuesById[13] = "S2U_Setting"] = 13;
        return values;
    })();

    jllib.BetResp = (function() {

        /**
         * Properties of a BetResp.
         * @memberof jllib
         * @interface IBetResp
         * @property {string|null} [cliendSeed] BetResp cliendSeed
         * @property {string|null} [nextServerHash] BetResp nextServerHash
         * @property {number|Long|null} [roundIndex] BetResp roundIndex
         * @property {number|null} [endCoin] BetResp endCoin
         * @property {number|null} [win] BetResp win
         * @property {number|null} [rate] BetResp rate
         * @property {number|null} [bet] BetResp bet
         * @property {boolean|null} [hasBonus] BetResp hasBonus
         */

        /**
         * Constructs a new BetResp.
         * @memberof jllib
         * @classdesc Represents a BetResp.
         * @implements IBetResp
         * @constructor
         * @param {jllib.IBetResp=} [properties] Properties to set
         */
        function BetResp(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BetResp cliendSeed.
         * @member {string} cliendSeed
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.cliendSeed = "";

        /**
         * BetResp nextServerHash.
         * @member {string} nextServerHash
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.nextServerHash = "";

        /**
         * BetResp roundIndex.
         * @member {number|Long} roundIndex
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.roundIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BetResp endCoin.
         * @member {number} endCoin
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.endCoin = 0;

        /**
         * BetResp win.
         * @member {number} win
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.win = 0;

        /**
         * BetResp rate.
         * @member {number} rate
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.rate = 0;

        /**
         * BetResp bet.
         * @member {number} bet
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.bet = 0;

        /**
         * BetResp hasBonus.
         * @member {boolean} hasBonus
         * @memberof jllib.BetResp
         * @instance
         */
        BetResp.prototype.hasBonus = false;

        /**
         * Creates a new BetResp instance using the specified properties.
         * @function create
         * @memberof jllib.BetResp
         * @static
         * @param {jllib.IBetResp=} [properties] Properties to set
         * @returns {jllib.BetResp} BetResp instance
         */
        BetResp.create = function create(properties) {
            return new BetResp(properties);
        };

        /**
         * Encodes the specified BetResp message. Does not implicitly {@link jllib.BetResp.verify|verify} messages.
         * @function encode
         * @memberof jllib.BetResp
         * @static
         * @param {jllib.IBetResp} message BetResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetResp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cliendSeed != null && Object.hasOwnProperty.call(message, "cliendSeed"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.cliendSeed);
            if (message.nextServerHash != null && Object.hasOwnProperty.call(message, "nextServerHash"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nextServerHash);
            if (message.roundIndex != null && Object.hasOwnProperty.call(message, "roundIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.roundIndex);
            if (message.endCoin != null && Object.hasOwnProperty.call(message, "endCoin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.endCoin);
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.win);
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.rate);
            if (message.bet != null && Object.hasOwnProperty.call(message, "bet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.bet);
            if (message.hasBonus != null && Object.hasOwnProperty.call(message, "hasBonus"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.hasBonus);
            return writer;
        };

        /**
         * Encodes the specified BetResp message, length delimited. Does not implicitly {@link jllib.BetResp.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllib.BetResp
         * @static
         * @param {jllib.IBetResp} message BetResp message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetResp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BetResp message from the specified reader or buffer.
         * @function decode
         * @memberof jllib.BetResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllib.BetResp} BetResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetResp.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllib.BetResp();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.cliendSeed = reader.string();
                        break;
                    }
                case 2: {
                        message.nextServerHash = reader.string();
                        break;
                    }
                case 3: {
                        message.roundIndex = reader.int64();
                        break;
                    }
                case 4: {
                        message.endCoin = reader.double();
                        break;
                    }
                case 5: {
                        message.win = reader.double();
                        break;
                    }
                case 6: {
                        message.rate = reader.double();
                        break;
                    }
                case 7: {
                        message.bet = reader.double();
                        break;
                    }
                case 8: {
                        message.hasBonus = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BetResp message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllib.BetResp
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllib.BetResp} BetResp
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetResp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BetResp message.
         * @function verify
         * @memberof jllib.BetResp
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BetResp.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.cliendSeed != null && message.hasOwnProperty("cliendSeed"))
                if (!$util.isString(message.cliendSeed))
                    return "cliendSeed: string expected";
            if (message.nextServerHash != null && message.hasOwnProperty("nextServerHash"))
                if (!$util.isString(message.nextServerHash))
                    return "nextServerHash: string expected";
            if (message.roundIndex != null && message.hasOwnProperty("roundIndex"))
                if (!$util.isInteger(message.roundIndex) && !(message.roundIndex && $util.isInteger(message.roundIndex.low) && $util.isInteger(message.roundIndex.high)))
                    return "roundIndex: integer|Long expected";
            if (message.endCoin != null && message.hasOwnProperty("endCoin"))
                if (typeof message.endCoin !== "number")
                    return "endCoin: number expected";
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.bet != null && message.hasOwnProperty("bet"))
                if (typeof message.bet !== "number")
                    return "bet: number expected";
            if (message.hasBonus != null && message.hasOwnProperty("hasBonus"))
                if (typeof message.hasBonus !== "boolean")
                    return "hasBonus: boolean expected";
            return null;
        };

        /**
         * Creates a BetResp message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllib.BetResp
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllib.BetResp} BetResp
         */
        BetResp.fromObject = function fromObject(object) {
            if (object instanceof $root.jllib.BetResp)
                return object;
            let message = new $root.jllib.BetResp();
            if (object.cliendSeed != null)
                message.cliendSeed = String(object.cliendSeed);
            if (object.nextServerHash != null)
                message.nextServerHash = String(object.nextServerHash);
            if (object.roundIndex != null)
                if ($util.Long)
                    (message.roundIndex = $util.Long.fromValue(object.roundIndex)).unsigned = false;
                else if (typeof object.roundIndex === "string")
                    message.roundIndex = parseInt(object.roundIndex, 10);
                else if (typeof object.roundIndex === "number")
                    message.roundIndex = object.roundIndex;
                else if (typeof object.roundIndex === "object")
                    message.roundIndex = new $util.LongBits(object.roundIndex.low >>> 0, object.roundIndex.high >>> 0).toNumber();
            if (object.endCoin != null)
                message.endCoin = Number(object.endCoin);
            if (object.win != null)
                message.win = Number(object.win);
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.bet != null)
                message.bet = Number(object.bet);
            if (object.hasBonus != null)
                message.hasBonus = Boolean(object.hasBonus);
            return message;
        };

        /**
         * Creates a plain object from a BetResp message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllib.BetResp
         * @static
         * @param {jllib.BetResp} message BetResp
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BetResp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.cliendSeed = "";
                object.nextServerHash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.roundIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.roundIndex = options.longs === String ? "0" : 0;
                object.endCoin = 0;
                object.win = 0;
                object.rate = 0;
                object.bet = 0;
                object.hasBonus = false;
            }
            if (message.cliendSeed != null && message.hasOwnProperty("cliendSeed"))
                object.cliendSeed = message.cliendSeed;
            if (message.nextServerHash != null && message.hasOwnProperty("nextServerHash"))
                object.nextServerHash = message.nextServerHash;
            if (message.roundIndex != null && message.hasOwnProperty("roundIndex"))
                if (typeof message.roundIndex === "number")
                    object.roundIndex = options.longs === String ? String(message.roundIndex) : message.roundIndex;
                else
                    object.roundIndex = options.longs === String ? $util.Long.prototype.toString.call(message.roundIndex) : options.longs === Number ? new $util.LongBits(message.roundIndex.low >>> 0, message.roundIndex.high >>> 0).toNumber() : message.roundIndex;
            if (message.endCoin != null && message.hasOwnProperty("endCoin"))
                object.endCoin = options.json && !isFinite(message.endCoin) ? String(message.endCoin) : message.endCoin;
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.bet != null && message.hasOwnProperty("bet"))
                object.bet = options.json && !isFinite(message.bet) ? String(message.bet) : message.bet;
            if (message.hasBonus != null && message.hasOwnProperty("hasBonus"))
                object.hasBonus = message.hasBonus;
            return object;
        };

        /**
         * Converts this BetResp to JSON.
         * @function toJSON
         * @memberof jllib.BetResp
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BetResp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BetResp
         * @function getTypeUrl
         * @memberof jllib.BetResp
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BetResp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllib.BetResp";
        };

        return BetResp;
    })();

    jllib.BetReq = (function() {

        /**
         * Properties of a BetReq.
         * @memberof jllib
         * @interface IBetReq
         * @property {number|null} [bet] BetReq bet
         * @property {string|null} [clientSeed] BetReq clientSeed
         * @property {number|null} [rate] BetReq rate
         * @property {boolean|null} [useBonus] BetReq useBonus
         */

        /**
         * Constructs a new BetReq.
         * @memberof jllib
         * @classdesc Represents a BetReq.
         * @implements IBetReq
         * @constructor
         * @param {jllib.IBetReq=} [properties] Properties to set
         */
        function BetReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BetReq bet.
         * @member {number} bet
         * @memberof jllib.BetReq
         * @instance
         */
        BetReq.prototype.bet = 0;

        /**
         * BetReq clientSeed.
         * @member {string} clientSeed
         * @memberof jllib.BetReq
         * @instance
         */
        BetReq.prototype.clientSeed = "";

        /**
         * BetReq rate.
         * @member {number} rate
         * @memberof jllib.BetReq
         * @instance
         */
        BetReq.prototype.rate = 0;

        /**
         * BetReq useBonus.
         * @member {boolean} useBonus
         * @memberof jllib.BetReq
         * @instance
         */
        BetReq.prototype.useBonus = false;

        /**
         * Creates a new BetReq instance using the specified properties.
         * @function create
         * @memberof jllib.BetReq
         * @static
         * @param {jllib.IBetReq=} [properties] Properties to set
         * @returns {jllib.BetReq} BetReq instance
         */
        BetReq.create = function create(properties) {
            return new BetReq(properties);
        };

        /**
         * Encodes the specified BetReq message. Does not implicitly {@link jllib.BetReq.verify|verify} messages.
         * @function encode
         * @memberof jllib.BetReq
         * @static
         * @param {jllib.IBetReq} message BetReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bet != null && Object.hasOwnProperty.call(message, "bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.bet);
            if (message.clientSeed != null && Object.hasOwnProperty.call(message, "clientSeed"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientSeed);
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.rate);
            if (message.useBonus != null && Object.hasOwnProperty.call(message, "useBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.useBonus);
            return writer;
        };

        /**
         * Encodes the specified BetReq message, length delimited. Does not implicitly {@link jllib.BetReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllib.BetReq
         * @static
         * @param {jllib.IBetReq} message BetReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BetReq message from the specified reader or buffer.
         * @function decode
         * @memberof jllib.BetReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllib.BetReq} BetReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllib.BetReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bet = reader.double();
                        break;
                    }
                case 2: {
                        message.clientSeed = reader.string();
                        break;
                    }
                case 3: {
                        message.rate = reader.double();
                        break;
                    }
                case 4: {
                        message.useBonus = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BetReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllib.BetReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllib.BetReq} BetReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BetReq message.
         * @function verify
         * @memberof jllib.BetReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BetReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bet != null && message.hasOwnProperty("bet"))
                if (typeof message.bet !== "number")
                    return "bet: number expected";
            if (message.clientSeed != null && message.hasOwnProperty("clientSeed"))
                if (!$util.isString(message.clientSeed))
                    return "clientSeed: string expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.useBonus != null && message.hasOwnProperty("useBonus"))
                if (typeof message.useBonus !== "boolean")
                    return "useBonus: boolean expected";
            return null;
        };

        /**
         * Creates a BetReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllib.BetReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllib.BetReq} BetReq
         */
        BetReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jllib.BetReq)
                return object;
            let message = new $root.jllib.BetReq();
            if (object.bet != null)
                message.bet = Number(object.bet);
            if (object.clientSeed != null)
                message.clientSeed = String(object.clientSeed);
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.useBonus != null)
                message.useBonus = Boolean(object.useBonus);
            return message;
        };

        /**
         * Creates a plain object from a BetReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllib.BetReq
         * @static
         * @param {jllib.BetReq} message BetReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BetReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.bet = 0;
                object.clientSeed = "";
                object.rate = 0;
                object.useBonus = false;
            }
            if (message.bet != null && message.hasOwnProperty("bet"))
                object.bet = options.json && !isFinite(message.bet) ? String(message.bet) : message.bet;
            if (message.clientSeed != null && message.hasOwnProperty("clientSeed"))
                object.clientSeed = message.clientSeed;
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.useBonus != null && message.hasOwnProperty("useBonus"))
                object.useBonus = message.useBonus;
            return object;
        };

        /**
         * Converts this BetReq to JSON.
         * @function toJSON
         * @memberof jllib.BetReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BetReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BetReq
         * @function getTypeUrl
         * @memberof jllib.BetReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BetReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllib.BetReq";
        };

        return BetReq;
    })();

    jllib.Setting = (function() {

        /**
         * Properties of a Setting.
         * @memberof jllib
         * @interface ISetting
         * @property {Array.<jllib.IbonusRate>|null} [bonus] Setting bonus
         * @property {string|null} [hash] Setting hash
         * @property {number|null} [maxWin] Setting maxWin
         */

        /**
         * Constructs a new Setting.
         * @memberof jllib
         * @classdesc Represents a Setting.
         * @implements ISetting
         * @constructor
         * @param {jllib.ISetting=} [properties] Properties to set
         */
        function Setting(properties) {
            this.bonus = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Setting bonus.
         * @member {Array.<jllib.IbonusRate>} bonus
         * @memberof jllib.Setting
         * @instance
         */
        Setting.prototype.bonus = $util.emptyArray;

        /**
         * Setting hash.
         * @member {string} hash
         * @memberof jllib.Setting
         * @instance
         */
        Setting.prototype.hash = "";

        /**
         * Setting maxWin.
         * @member {number} maxWin
         * @memberof jllib.Setting
         * @instance
         */
        Setting.prototype.maxWin = 0;

        /**
         * Creates a new Setting instance using the specified properties.
         * @function create
         * @memberof jllib.Setting
         * @static
         * @param {jllib.ISetting=} [properties] Properties to set
         * @returns {jllib.Setting} Setting instance
         */
        Setting.create = function create(properties) {
            return new Setting(properties);
        };

        /**
         * Encodes the specified Setting message. Does not implicitly {@link jllib.Setting.verify|verify} messages.
         * @function encode
         * @memberof jllib.Setting
         * @static
         * @param {jllib.ISetting} message Setting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Setting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bonus != null && message.bonus.length)
                for (let i = 0; i < message.bonus.length; ++i)
                    $root.jllib.bonusRate.encode(message.bonus[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.hash);
            if (message.maxWin != null && Object.hasOwnProperty.call(message, "maxWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.maxWin);
            return writer;
        };

        /**
         * Encodes the specified Setting message, length delimited. Does not implicitly {@link jllib.Setting.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllib.Setting
         * @static
         * @param {jllib.ISetting} message Setting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Setting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Setting message from the specified reader or buffer.
         * @function decode
         * @memberof jllib.Setting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllib.Setting} Setting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Setting.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllib.Setting();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.bonus && message.bonus.length))
                            message.bonus = [];
                        message.bonus.push($root.jllib.bonusRate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.hash = reader.string();
                        break;
                    }
                case 5: {
                        message.maxWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Setting message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllib.Setting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllib.Setting} Setting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Setting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Setting message.
         * @function verify
         * @memberof jllib.Setting
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Setting.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.bonus != null && message.hasOwnProperty("bonus")) {
                if (!Array.isArray(message.bonus))
                    return "bonus: array expected";
                for (let i = 0; i < message.bonus.length; ++i) {
                    let error = $root.jllib.bonusRate.verify(message.bonus[i]);
                    if (error)
                        return "bonus." + error;
                }
            }
            if (message.hash != null && message.hasOwnProperty("hash"))
                if (!$util.isString(message.hash))
                    return "hash: string expected";
            if (message.maxWin != null && message.hasOwnProperty("maxWin"))
                if (typeof message.maxWin !== "number")
                    return "maxWin: number expected";
            return null;
        };

        /**
         * Creates a Setting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllib.Setting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllib.Setting} Setting
         */
        Setting.fromObject = function fromObject(object) {
            if (object instanceof $root.jllib.Setting)
                return object;
            let message = new $root.jllib.Setting();
            if (object.bonus) {
                if (!Array.isArray(object.bonus))
                    throw TypeError(".jllib.Setting.bonus: array expected");
                message.bonus = [];
                for (let i = 0; i < object.bonus.length; ++i) {
                    if (typeof object.bonus[i] !== "object")
                        throw TypeError(".jllib.Setting.bonus: object expected");
                    message.bonus[i] = $root.jllib.bonusRate.fromObject(object.bonus[i]);
                }
            }
            if (object.hash != null)
                message.hash = String(object.hash);
            if (object.maxWin != null)
                message.maxWin = Number(object.maxWin);
            return message;
        };

        /**
         * Creates a plain object from a Setting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllib.Setting
         * @static
         * @param {jllib.Setting} message Setting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Setting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.bonus = [];
            if (options.defaults) {
                object.hash = "";
                object.maxWin = 0;
            }
            if (message.bonus && message.bonus.length) {
                object.bonus = [];
                for (let j = 0; j < message.bonus.length; ++j)
                    object.bonus[j] = $root.jllib.bonusRate.toObject(message.bonus[j], options);
            }
            if (message.hash != null && message.hasOwnProperty("hash"))
                object.hash = message.hash;
            if (message.maxWin != null && message.hasOwnProperty("maxWin"))
                object.maxWin = options.json && !isFinite(message.maxWin) ? String(message.maxWin) : message.maxWin;
            return object;
        };

        /**
         * Converts this Setting to JSON.
         * @function toJSON
         * @memberof jllib.Setting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Setting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Setting
         * @function getTypeUrl
         * @memberof jllib.Setting
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Setting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllib.Setting";
        };

        return Setting;
    })();

    jllib.bonusRate = (function() {

        /**
         * Properties of a bonusRate.
         * @memberof jllib
         * @interface IbonusRate
         * @property {number|null} [rate] bonusRate rate
         * @property {number|null} [bonus] bonusRate bonus
         */

        /**
         * Constructs a new bonusRate.
         * @memberof jllib
         * @classdesc Represents a bonusRate.
         * @implements IbonusRate
         * @constructor
         * @param {jllib.IbonusRate=} [properties] Properties to set
         */
        function bonusRate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * bonusRate rate.
         * @member {number} rate
         * @memberof jllib.bonusRate
         * @instance
         */
        bonusRate.prototype.rate = 0;

        /**
         * bonusRate bonus.
         * @member {number} bonus
         * @memberof jllib.bonusRate
         * @instance
         */
        bonusRate.prototype.bonus = 0;

        /**
         * Creates a new bonusRate instance using the specified properties.
         * @function create
         * @memberof jllib.bonusRate
         * @static
         * @param {jllib.IbonusRate=} [properties] Properties to set
         * @returns {jllib.bonusRate} bonusRate instance
         */
        bonusRate.create = function create(properties) {
            return new bonusRate(properties);
        };

        /**
         * Encodes the specified bonusRate message. Does not implicitly {@link jllib.bonusRate.verify|verify} messages.
         * @function encode
         * @memberof jllib.bonusRate
         * @static
         * @param {jllib.IbonusRate} message bonusRate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        bonusRate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.rate);
            if (message.bonus != null && Object.hasOwnProperty.call(message, "bonus"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.bonus);
            return writer;
        };

        /**
         * Encodes the specified bonusRate message, length delimited. Does not implicitly {@link jllib.bonusRate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllib.bonusRate
         * @static
         * @param {jllib.IbonusRate} message bonusRate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        bonusRate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a bonusRate message from the specified reader or buffer.
         * @function decode
         * @memberof jllib.bonusRate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllib.bonusRate} bonusRate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        bonusRate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllib.bonusRate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.rate = reader.double();
                        break;
                    }
                case 2: {
                        message.bonus = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a bonusRate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllib.bonusRate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllib.bonusRate} bonusRate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        bonusRate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a bonusRate message.
         * @function verify
         * @memberof jllib.bonusRate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        bonusRate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                if (typeof message.bonus !== "number")
                    return "bonus: number expected";
            return null;
        };

        /**
         * Creates a bonusRate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllib.bonusRate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllib.bonusRate} bonusRate
         */
        bonusRate.fromObject = function fromObject(object) {
            if (object instanceof $root.jllib.bonusRate)
                return object;
            let message = new $root.jllib.bonusRate();
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.bonus != null)
                message.bonus = Number(object.bonus);
            return message;
        };

        /**
         * Creates a plain object from a bonusRate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllib.bonusRate
         * @static
         * @param {jllib.bonusRate} message bonusRate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        bonusRate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.rate = 0;
                object.bonus = 0;
            }
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                object.bonus = options.json && !isFinite(message.bonus) ? String(message.bonus) : message.bonus;
            return object;
        };

        /**
         * Converts this bonusRate to JSON.
         * @function toJSON
         * @memberof jllib.bonusRate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        bonusRate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for bonusRate
         * @function getTypeUrl
         * @memberof jllib.bonusRate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        bonusRate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllib.bonusRate";
        };

        return bonusRate;
    })();

    return jllib;
})();

export const jllj = $root.jllj = (() => {

    /**
     * Namespace jllj.
     * @exports jllj
     * @namespace
     */
    const jllj = {};

    jllj.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jllj
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jllj
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jllj.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jllj.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jllj.Column
         * @static
         * @param {jllj.IColumn=} [properties] Properties to set
         * @returns {jllj.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jllj.Column.verify|verify} messages.
         * @function encode
         * @memberof jllj.Column
         * @static
         * @param {jllj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jllj.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj.Column
         * @static
         * @param {jllj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jllj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jllj.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj.Column)
                return object;
            let message = new $root.jllj.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jllj.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj.Column
         * @static
         * @param {jllj.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jllj.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jllj.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj.Column";
        };

        return Column;
    })();

    jllj.Single = (function() {

        /**
         * Properties of a Single.
         * @memberof jllj
         * @interface ISingle
         * @property {Array.<jllj.IColumn>|null} [Col] Single Col
         */

        /**
         * Constructs a new Single.
         * @memberof jllj
         * @classdesc Represents a Single.
         * @implements ISingle
         * @constructor
         * @param {jllj.ISingle=} [properties] Properties to set
         */
        function Single(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Single Col.
         * @member {Array.<jllj.IColumn>} Col
         * @memberof jllj.Single
         * @instance
         */
        Single.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Single instance using the specified properties.
         * @function create
         * @memberof jllj.Single
         * @static
         * @param {jllj.ISingle=} [properties] Properties to set
         * @returns {jllj.Single} Single instance
         */
        Single.create = function create(properties) {
            return new Single(properties);
        };

        /**
         * Encodes the specified Single message. Does not implicitly {@link jllj.Single.verify|verify} messages.
         * @function encode
         * @memberof jllj.Single
         * @static
         * @param {jllj.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jllj.Column.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Single message, length delimited. Does not implicitly {@link jllj.Single.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj.Single
         * @static
         * @param {jllj.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Single message from the specified reader or buffer.
         * @function decode
         * @memberof jllj.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj.Single();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jllj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Single message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Single message.
         * @function verify
         * @memberof jllj.Single
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Single.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jllj.Column.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Single message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj.Single
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj.Single} Single
         */
        Single.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj.Single)
                return object;
            let message = new $root.jllj.Single();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jllj.Single.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jllj.Single.Col: object expected");
                    message.Col[i] = $root.jllj.Column.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Single message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj.Single
         * @static
         * @param {jllj.Single} message Single
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Single.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jllj.Column.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this Single to JSON.
         * @function toJSON
         * @memberof jllj.Single
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Single.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Single
         * @function getTypeUrl
         * @memberof jllj.Single
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Single.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj.Single";
        };

        return Single;
    })();

    jllj.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jllj
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jllj
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jllj.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jllj.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jllj.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jllj.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jllj.AwardData
         * @static
         * @param {jllj.IAwardData=} [properties] Properties to set
         * @returns {jllj.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jllj.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jllj.AwardData
         * @static
         * @param {jllj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jllj.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj.AwardData
         * @static
         * @param {jllj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jllj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jllj.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj.AwardData)
                return object;
            let message = new $root.jllj.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj.AwardData
         * @static
         * @param {jllj.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jllj.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jllj.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj.AwardData";
        };

        return AwardData;
    })();

    jllj.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jllj
         * @interface ISpinAck
         * @property {Array.<jllj.ISingle>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jllj.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {Array.<number>|null} [WildOdd] SpinAck WildOdd
         * @property {number|null} [WildWin] SpinAck WildWin
         * @property {number|null} [Mult] SpinAck Mult
         * @property {number|null} [GetBonus] SpinAck GetBonus
         * @property {number|null} [BonusSymbol] SpinAck BonusSymbol
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jllj
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jllj.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.WildOdd = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jllj.ISingle>} PlateSymbol
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jllj.IAwardData>} AwardDataVec
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck WildOdd.
         * @member {Array.<number>} WildOdd
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.WildOdd = $util.emptyArray;

        /**
         * SpinAck WildWin.
         * @member {number} WildWin
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.WildWin = 0;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck GetBonus.
         * @member {number} GetBonus
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.GetBonus = 0;

        /**
         * SpinAck BonusSymbol.
         * @member {number} BonusSymbol
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusSymbol = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jllj.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jllj.SpinAck
         * @static
         * @param {jllj.ISpinAck=} [properties] Properties to set
         * @returns {jllj.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jllj.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jllj.SpinAck
         * @static
         * @param {jllj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jllj.Single.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jllj.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.WildOdd != null && message.WildOdd.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.WildOdd.length; ++i)
                    writer.double(message.WildOdd[i]);
                writer.ldelim();
            }
            if (message.WildWin != null && Object.hasOwnProperty.call(message, "WildWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.WildWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mult);
            if (message.GetBonus != null && Object.hasOwnProperty.call(message, "GetBonus"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.GetBonus);
            if (message.BonusSymbol != null && Object.hasOwnProperty.call(message, "BonusSymbol"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.BonusSymbol);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.Extra);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AwardTypeFlag);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jllj.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj.SpinAck
         * @static
         * @param {jllj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jllj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jllj.Single.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jllj.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.WildOdd && message.WildOdd.length))
                            message.WildOdd = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WildOdd.push(reader.double());
                        } else
                            message.WildOdd.push(reader.double());
                        break;
                    }
                case 4: {
                        message.WildWin = reader.double();
                        break;
                    }
                case 5: {
                        message.Mult = reader.double();
                        break;
                    }
                case 6: {
                        message.GetBonus = reader.int32();
                        break;
                    }
                case 7: {
                        message.BonusSymbol = reader.int32();
                        break;
                    }
                case 8: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 9: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 10: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 11: {
                        message.RTP = reader.double();
                        break;
                    }
                case 12: {
                        message.LID = reader.int32();
                        break;
                    }
                case 13: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jllj.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jllj.Single.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jllj.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.WildOdd != null && message.hasOwnProperty("WildOdd")) {
                if (!Array.isArray(message.WildOdd))
                    return "WildOdd: array expected";
                for (let i = 0; i < message.WildOdd.length; ++i)
                    if (typeof message.WildOdd[i] !== "number")
                        return "WildOdd: number[] expected";
            }
            if (message.WildWin != null && message.hasOwnProperty("WildWin"))
                if (typeof message.WildWin !== "number")
                    return "WildWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                if (!$util.isInteger(message.GetBonus))
                    return "GetBonus: integer expected";
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                if (!$util.isInteger(message.BonusSymbol))
                    return "BonusSymbol: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj.SpinAck)
                return object;
            let message = new $root.jllj.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jllj.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jllj.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jllj.Single.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jllj.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jllj.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jllj.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.WildOdd) {
                if (!Array.isArray(object.WildOdd))
                    throw TypeError(".jllj.SpinAck.WildOdd: array expected");
                message.WildOdd = [];
                for (let i = 0; i < object.WildOdd.length; ++i)
                    message.WildOdd[i] = Number(object.WildOdd[i]);
            }
            if (object.WildWin != null)
                message.WildWin = Number(object.WildWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.GetBonus != null)
                message.GetBonus = object.GetBonus | 0;
            if (object.BonusSymbol != null)
                message.BonusSymbol = object.BonusSymbol | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj.SpinAck
         * @static
         * @param {jllj.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.WildOdd = [];
            }
            if (options.defaults) {
                object.WildWin = 0;
                object.Mult = 0;
                object.GetBonus = 0;
                object.BonusSymbol = 0;
                object.Extra = false;
                object.AwardTypeFlag = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jllj.Single.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jllj.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.WildOdd && message.WildOdd.length) {
                object.WildOdd = [];
                for (let j = 0; j < message.WildOdd.length; ++j)
                    object.WildOdd[j] = options.json && !isFinite(message.WildOdd[j]) ? String(message.WildOdd[j]) : message.WildOdd[j];
            }
            if (message.WildWin != null && message.hasOwnProperty("WildWin"))
                object.WildWin = options.json && !isFinite(message.WildWin) ? String(message.WildWin) : message.WildWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                object.GetBonus = message.GetBonus;
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                object.BonusSymbol = message.BonusSymbol;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jllj.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jllj.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj.SpinAck";
        };

        return SpinAck;
    })();

    jllj.GemInfo = (function() {

        /**
         * Properties of a GemInfo.
         * @memberof jllj
         * @interface IGemInfo
         * @property {number|null} [data] GemInfo data
         */

        /**
         * Constructs a new GemInfo.
         * @memberof jllj
         * @classdesc Represents a GemInfo.
         * @implements IGemInfo
         * @constructor
         * @param {jllj.IGemInfo=} [properties] Properties to set
         */
        function GemInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GemInfo data.
         * @member {number} data
         * @memberof jllj.GemInfo
         * @instance
         */
        GemInfo.prototype.data = 0;

        /**
         * Creates a new GemInfo instance using the specified properties.
         * @function create
         * @memberof jllj.GemInfo
         * @static
         * @param {jllj.IGemInfo=} [properties] Properties to set
         * @returns {jllj.GemInfo} GemInfo instance
         */
        GemInfo.create = function create(properties) {
            return new GemInfo(properties);
        };

        /**
         * Encodes the specified GemInfo message. Does not implicitly {@link jllj.GemInfo.verify|verify} messages.
         * @function encode
         * @memberof jllj.GemInfo
         * @static
         * @param {jllj.IGemInfo} message GemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GemInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.data);
            return writer;
        };

        /**
         * Encodes the specified GemInfo message, length delimited. Does not implicitly {@link jllj.GemInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj.GemInfo
         * @static
         * @param {jllj.IGemInfo} message GemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GemInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GemInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jllj.GemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj.GemInfo} GemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GemInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj.GemInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.data = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GemInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj.GemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj.GemInfo} GemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GemInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GemInfo message.
         * @function verify
         * @memberof jllj.GemInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GemInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!$util.isInteger(message.data))
                    return "data: integer expected";
            return null;
        };

        /**
         * Creates a GemInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj.GemInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj.GemInfo} GemInfo
         */
        GemInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj.GemInfo)
                return object;
            let message = new $root.jllj.GemInfo();
            if (object.data != null)
                message.data = object.data | 0;
            return message;
        };

        /**
         * Creates a plain object from a GemInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj.GemInfo
         * @static
         * @param {jllj.GemInfo} message GemInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GemInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.data = 0;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = message.data;
            return object;
        };

        /**
         * Converts this GemInfo to JSON.
         * @function toJSON
         * @memberof jllj.GemInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GemInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GemInfo
         * @function getTypeUrl
         * @memberof jllj.GemInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GemInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj.GemInfo";
        };

        return GemInfo;
    })();

    return jllj;
})();

export const jllj2 = $root.jllj2 = (() => {

    /**
     * Namespace jllj2.
     * @exports jllj2
     * @namespace
     */
    const jllj2 = {};

    jllj2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jllj2
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jllj2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jllj2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jllj2.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jllj2.Column
         * @static
         * @param {jllj2.IColumn=} [properties] Properties to set
         * @returns {jllj2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jllj2.Column.verify|verify} messages.
         * @function encode
         * @memberof jllj2.Column
         * @static
         * @param {jllj2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jllj2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.Column
         * @static
         * @param {jllj2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jllj2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.Column)
                return object;
            let message = new $root.jllj2.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jllj2.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.Column
         * @static
         * @param {jllj2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jllj2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jllj2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.Column";
        };

        return Column;
    })();

    jllj2.Single = (function() {

        /**
         * Properties of a Single.
         * @memberof jllj2
         * @interface ISingle
         * @property {Array.<jllj2.IColumn>|null} [Col] Single Col
         */

        /**
         * Constructs a new Single.
         * @memberof jllj2
         * @classdesc Represents a Single.
         * @implements ISingle
         * @constructor
         * @param {jllj2.ISingle=} [properties] Properties to set
         */
        function Single(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Single Col.
         * @member {Array.<jllj2.IColumn>} Col
         * @memberof jllj2.Single
         * @instance
         */
        Single.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Single instance using the specified properties.
         * @function create
         * @memberof jllj2.Single
         * @static
         * @param {jllj2.ISingle=} [properties] Properties to set
         * @returns {jllj2.Single} Single instance
         */
        Single.create = function create(properties) {
            return new Single(properties);
        };

        /**
         * Encodes the specified Single message. Does not implicitly {@link jllj2.Single.verify|verify} messages.
         * @function encode
         * @memberof jllj2.Single
         * @static
         * @param {jllj2.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jllj2.Column.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Single message, length delimited. Does not implicitly {@link jllj2.Single.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.Single
         * @static
         * @param {jllj2.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Single message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.Single();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jllj2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Single message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Single message.
         * @function verify
         * @memberof jllj2.Single
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Single.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jllj2.Column.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Single message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.Single
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.Single} Single
         */
        Single.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.Single)
                return object;
            let message = new $root.jllj2.Single();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jllj2.Single.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jllj2.Single.Col: object expected");
                    message.Col[i] = $root.jllj2.Column.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Single message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.Single
         * @static
         * @param {jllj2.Single} message Single
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Single.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jllj2.Column.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this Single to JSON.
         * @function toJSON
         * @memberof jllj2.Single
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Single.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Single
         * @function getTypeUrl
         * @memberof jllj2.Single
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Single.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.Single";
        };

        return Single;
    })();

    jllj2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jllj2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         */

        /**
         * Constructs a new AwardData.
         * @memberof jllj2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jllj2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jllj2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jllj2.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jllj2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jllj2.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jllj2.AwardData
         * @static
         * @param {jllj2.IAwardData=} [properties] Properties to set
         * @returns {jllj2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jllj2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jllj2.AwardData
         * @static
         * @param {jllj2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jllj2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.AwardData
         * @static
         * @param {jllj2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jllj2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.AwardData)
                return object;
            let message = new $root.jllj2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.AwardData
         * @static
         * @param {jllj2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jllj2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jllj2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.AwardData";
        };

        return AwardData;
    })();

    jllj2.GameAwardData = (function() {

        /**
         * Properties of a GameAwardData.
         * @memberof jllj2
         * @interface IGameAwardData
         * @property {Array.<jllj2.IAwardData>|null} [ADV] GameAwardData ADV
         */

        /**
         * Constructs a new GameAwardData.
         * @memberof jllj2
         * @classdesc Represents a GameAwardData.
         * @implements IGameAwardData
         * @constructor
         * @param {jllj2.IGameAwardData=} [properties] Properties to set
         */
        function GameAwardData(properties) {
            this.ADV = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameAwardData ADV.
         * @member {Array.<jllj2.IAwardData>} ADV
         * @memberof jllj2.GameAwardData
         * @instance
         */
        GameAwardData.prototype.ADV = $util.emptyArray;

        /**
         * Creates a new GameAwardData instance using the specified properties.
         * @function create
         * @memberof jllj2.GameAwardData
         * @static
         * @param {jllj2.IGameAwardData=} [properties] Properties to set
         * @returns {jllj2.GameAwardData} GameAwardData instance
         */
        GameAwardData.create = function create(properties) {
            return new GameAwardData(properties);
        };

        /**
         * Encodes the specified GameAwardData message. Does not implicitly {@link jllj2.GameAwardData.verify|verify} messages.
         * @function encode
         * @memberof jllj2.GameAwardData
         * @static
         * @param {jllj2.IGameAwardData} message GameAwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameAwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ADV != null && message.ADV.length)
                for (let i = 0; i < message.ADV.length; ++i)
                    $root.jllj2.AwardData.encode(message.ADV[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameAwardData message, length delimited. Does not implicitly {@link jllj2.GameAwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.GameAwardData
         * @static
         * @param {jllj2.IGameAwardData} message GameAwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameAwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameAwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.GameAwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.GameAwardData} GameAwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameAwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.GameAwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ADV && message.ADV.length))
                            message.ADV = [];
                        message.ADV.push($root.jllj2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameAwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.GameAwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.GameAwardData} GameAwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameAwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameAwardData message.
         * @function verify
         * @memberof jllj2.GameAwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameAwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ADV != null && message.hasOwnProperty("ADV")) {
                if (!Array.isArray(message.ADV))
                    return "ADV: array expected";
                for (let i = 0; i < message.ADV.length; ++i) {
                    let error = $root.jllj2.AwardData.verify(message.ADV[i]);
                    if (error)
                        return "ADV." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameAwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.GameAwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.GameAwardData} GameAwardData
         */
        GameAwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.GameAwardData)
                return object;
            let message = new $root.jllj2.GameAwardData();
            if (object.ADV) {
                if (!Array.isArray(object.ADV))
                    throw TypeError(".jllj2.GameAwardData.ADV: array expected");
                message.ADV = [];
                for (let i = 0; i < object.ADV.length; ++i) {
                    if (typeof object.ADV[i] !== "object")
                        throw TypeError(".jllj2.GameAwardData.ADV: object expected");
                    message.ADV[i] = $root.jllj2.AwardData.fromObject(object.ADV[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameAwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.GameAwardData
         * @static
         * @param {jllj2.GameAwardData} message GameAwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameAwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ADV = [];
            if (message.ADV && message.ADV.length) {
                object.ADV = [];
                for (let j = 0; j < message.ADV.length; ++j)
                    object.ADV[j] = $root.jllj2.AwardData.toObject(message.ADV[j], options);
            }
            return object;
        };

        /**
         * Converts this GameAwardData to JSON.
         * @function toJSON
         * @memberof jllj2.GameAwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameAwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameAwardData
         * @function getTypeUrl
         * @memberof jllj2.GameAwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameAwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.GameAwardData";
        };

        return GameAwardData;
    })();

    jllj2.GameWildOdd = (function() {

        /**
         * Properties of a GameWildOdd.
         * @memberof jllj2
         * @interface IGameWildOdd
         * @property {Array.<number>|null} [WO] GameWildOdd WO
         */

        /**
         * Constructs a new GameWildOdd.
         * @memberof jllj2
         * @classdesc Represents a GameWildOdd.
         * @implements IGameWildOdd
         * @constructor
         * @param {jllj2.IGameWildOdd=} [properties] Properties to set
         */
        function GameWildOdd(properties) {
            this.WO = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameWildOdd WO.
         * @member {Array.<number>} WO
         * @memberof jllj2.GameWildOdd
         * @instance
         */
        GameWildOdd.prototype.WO = $util.emptyArray;

        /**
         * Creates a new GameWildOdd instance using the specified properties.
         * @function create
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {jllj2.IGameWildOdd=} [properties] Properties to set
         * @returns {jllj2.GameWildOdd} GameWildOdd instance
         */
        GameWildOdd.create = function create(properties) {
            return new GameWildOdd(properties);
        };

        /**
         * Encodes the specified GameWildOdd message. Does not implicitly {@link jllj2.GameWildOdd.verify|verify} messages.
         * @function encode
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {jllj2.IGameWildOdd} message GameWildOdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameWildOdd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WO != null && message.WO.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.WO.length; ++i)
                    writer.double(message.WO[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameWildOdd message, length delimited. Does not implicitly {@link jllj2.GameWildOdd.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {jllj2.IGameWildOdd} message GameWildOdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameWildOdd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameWildOdd message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.GameWildOdd} GameWildOdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameWildOdd.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.GameWildOdd();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.WO && message.WO.length))
                            message.WO = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WO.push(reader.double());
                        } else
                            message.WO.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameWildOdd message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.GameWildOdd} GameWildOdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameWildOdd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameWildOdd message.
         * @function verify
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameWildOdd.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WO != null && message.hasOwnProperty("WO")) {
                if (!Array.isArray(message.WO))
                    return "WO: array expected";
                for (let i = 0; i < message.WO.length; ++i)
                    if (typeof message.WO[i] !== "number")
                        return "WO: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameWildOdd message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.GameWildOdd} GameWildOdd
         */
        GameWildOdd.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.GameWildOdd)
                return object;
            let message = new $root.jllj2.GameWildOdd();
            if (object.WO) {
                if (!Array.isArray(object.WO))
                    throw TypeError(".jllj2.GameWildOdd.WO: array expected");
                message.WO = [];
                for (let i = 0; i < object.WO.length; ++i)
                    message.WO[i] = Number(object.WO[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameWildOdd message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {jllj2.GameWildOdd} message GameWildOdd
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameWildOdd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.WO = [];
            if (message.WO && message.WO.length) {
                object.WO = [];
                for (let j = 0; j < message.WO.length; ++j)
                    object.WO[j] = options.json && !isFinite(message.WO[j]) ? String(message.WO[j]) : message.WO[j];
            }
            return object;
        };

        /**
         * Converts this GameWildOdd to JSON.
         * @function toJSON
         * @memberof jllj2.GameWildOdd
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameWildOdd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameWildOdd
         * @function getTypeUrl
         * @memberof jllj2.GameWildOdd
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameWildOdd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.GameWildOdd";
        };

        return GameWildOdd;
    })();

    jllj2.GemInfo = (function() {

        /**
         * Properties of a GemInfo.
         * @memberof jllj2
         * @interface IGemInfo
         * @property {number|null} [data] GemInfo data
         */

        /**
         * Constructs a new GemInfo.
         * @memberof jllj2
         * @classdesc Represents a GemInfo.
         * @implements IGemInfo
         * @constructor
         * @param {jllj2.IGemInfo=} [properties] Properties to set
         */
        function GemInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GemInfo data.
         * @member {number} data
         * @memberof jllj2.GemInfo
         * @instance
         */
        GemInfo.prototype.data = 0;

        /**
         * Creates a new GemInfo instance using the specified properties.
         * @function create
         * @memberof jllj2.GemInfo
         * @static
         * @param {jllj2.IGemInfo=} [properties] Properties to set
         * @returns {jllj2.GemInfo} GemInfo instance
         */
        GemInfo.create = function create(properties) {
            return new GemInfo(properties);
        };

        /**
         * Encodes the specified GemInfo message. Does not implicitly {@link jllj2.GemInfo.verify|verify} messages.
         * @function encode
         * @memberof jllj2.GemInfo
         * @static
         * @param {jllj2.IGemInfo} message GemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GemInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.data);
            return writer;
        };

        /**
         * Encodes the specified GemInfo message, length delimited. Does not implicitly {@link jllj2.GemInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.GemInfo
         * @static
         * @param {jllj2.IGemInfo} message GemInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GemInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GemInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.GemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.GemInfo} GemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GemInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.GemInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.data = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GemInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.GemInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.GemInfo} GemInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GemInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GemInfo message.
         * @function verify
         * @memberof jllj2.GemInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GemInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!$util.isInteger(message.data))
                    return "data: integer expected";
            return null;
        };

        /**
         * Creates a GemInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.GemInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.GemInfo} GemInfo
         */
        GemInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.GemInfo)
                return object;
            let message = new $root.jllj2.GemInfo();
            if (object.data != null)
                message.data = object.data | 0;
            return message;
        };

        /**
         * Creates a plain object from a GemInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.GemInfo
         * @static
         * @param {jllj2.GemInfo} message GemInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GemInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.data = 0;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = message.data;
            return object;
        };

        /**
         * Converts this GemInfo to JSON.
         * @function toJSON
         * @memberof jllj2.GemInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GemInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GemInfo
         * @function getTypeUrl
         * @memberof jllj2.GemInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GemInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.GemInfo";
        };

        return GemInfo;
    })();

    jllj2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jllj2
         * @interface ISpinAck
         * @property {Array.<jllj2.ISingle>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jllj2.IGameAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {Array.<jllj2.IGameWildOdd>|null} [WildOdd] SpinAck WildOdd
         * @property {Array.<jllj2.IGameWildOdd>|null} [WildOddDone] SpinAck WildOddDone
         * @property {Array.<number>|null} [WildWin] SpinAck WildWin
         * @property {Array.<number>|null} [BigMult] SpinAck BigMult
         * @property {Array.<number>|null} [GameWin] SpinAck GameWin
         * @property {number|null} [GetBonus] SpinAck GetBonus
         * @property {number|null} [BonusSymbol] SpinAck BonusSymbol
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {boolean|null} [MaxFlag] SpinAck MaxFlag
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jllj2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jllj2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.WildOdd = [];
            this.WildOddDone = [];
            this.WildWin = [];
            this.BigMult = [];
            this.GameWin = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jllj2.ISingle>} PlateSymbol
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jllj2.IGameAwardData>} AwardDataVec
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck WildOdd.
         * @member {Array.<jllj2.IGameWildOdd>} WildOdd
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.WildOdd = $util.emptyArray;

        /**
         * SpinAck WildOddDone.
         * @member {Array.<jllj2.IGameWildOdd>} WildOddDone
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.WildOddDone = $util.emptyArray;

        /**
         * SpinAck WildWin.
         * @member {Array.<number>} WildWin
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.WildWin = $util.emptyArray;

        /**
         * SpinAck BigMult.
         * @member {Array.<number>} BigMult
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.BigMult = $util.emptyArray;

        /**
         * SpinAck GameWin.
         * @member {Array.<number>} GameWin
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.GameWin = $util.emptyArray;

        /**
         * SpinAck GetBonus.
         * @member {number} GetBonus
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.GetBonus = 0;

        /**
         * SpinAck BonusSymbol.
         * @member {number} BonusSymbol
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusSymbol = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jllj2.SpinAck
         * @instance
         */
        SpinAck.prototype.MaxFlag = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jllj2.SpinAck
         * @static
         * @param {jllj2.ISpinAck=} [properties] Properties to set
         * @returns {jllj2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jllj2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jllj2.SpinAck
         * @static
         * @param {jllj2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jllj2.Single.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jllj2.GameAwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.WildOdd != null && message.WildOdd.length)
                for (let i = 0; i < message.WildOdd.length; ++i)
                    $root.jllj2.GameWildOdd.encode(message.WildOdd[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.WildOddDone != null && message.WildOddDone.length)
                for (let i = 0; i < message.WildOddDone.length; ++i)
                    $root.jllj2.GameWildOdd.encode(message.WildOddDone[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.WildWin != null && message.WildWin.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.WildWin.length; ++i)
                    writer.double(message.WildWin[i]);
                writer.ldelim();
            }
            if (message.BigMult != null && message.BigMult.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.BigMult.length; ++i)
                    writer.double(message.BigMult[i]);
                writer.ldelim();
            }
            if (message.GameWin != null && message.GameWin.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.GameWin.length; ++i)
                    writer.double(message.GameWin[i]);
                writer.ldelim();
            }
            if (message.GetBonus != null && Object.hasOwnProperty.call(message, "GetBonus"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.GetBonus);
            if (message.BonusSymbol != null && Object.hasOwnProperty.call(message, "BonusSymbol"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.BonusSymbol);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.Extra);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.AwardTypeFlag);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.RID);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jllj2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllj2.SpinAck
         * @static
         * @param {jllj2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jllj2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllj2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllj2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jllj2.Single.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jllj2.GameAwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.WildOdd && message.WildOdd.length))
                            message.WildOdd = [];
                        message.WildOdd.push($root.jllj2.GameWildOdd.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.WildOddDone && message.WildOddDone.length))
                            message.WildOddDone = [];
                        message.WildOddDone.push($root.jllj2.GameWildOdd.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.WildWin && message.WildWin.length))
                            message.WildWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.WildWin.push(reader.double());
                        } else
                            message.WildWin.push(reader.double());
                        break;
                    }
                case 6: {
                        if (!(message.BigMult && message.BigMult.length))
                            message.BigMult = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BigMult.push(reader.double());
                        } else
                            message.BigMult.push(reader.double());
                        break;
                    }
                case 7: {
                        if (!(message.GameWin && message.GameWin.length))
                            message.GameWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.GameWin.push(reader.double());
                        } else
                            message.GameWin.push(reader.double());
                        break;
                    }
                case 8: {
                        message.GetBonus = reader.int32();
                        break;
                    }
                case 9: {
                        message.BonusSymbol = reader.int32();
                        break;
                    }
                case 10: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 11: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 12: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 13: {
                        message.RTP = reader.double();
                        break;
                    }
                case 14: {
                        message.LID = reader.int32();
                        break;
                    }
                case 15: {
                        message.RID = reader.int32();
                        break;
                    }
                case 16: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllj2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllj2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jllj2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jllj2.Single.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jllj2.GameAwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.WildOdd != null && message.hasOwnProperty("WildOdd")) {
                if (!Array.isArray(message.WildOdd))
                    return "WildOdd: array expected";
                for (let i = 0; i < message.WildOdd.length; ++i) {
                    let error = $root.jllj2.GameWildOdd.verify(message.WildOdd[i]);
                    if (error)
                        return "WildOdd." + error;
                }
            }
            if (message.WildOddDone != null && message.hasOwnProperty("WildOddDone")) {
                if (!Array.isArray(message.WildOddDone))
                    return "WildOddDone: array expected";
                for (let i = 0; i < message.WildOddDone.length; ++i) {
                    let error = $root.jllj2.GameWildOdd.verify(message.WildOddDone[i]);
                    if (error)
                        return "WildOddDone." + error;
                }
            }
            if (message.WildWin != null && message.hasOwnProperty("WildWin")) {
                if (!Array.isArray(message.WildWin))
                    return "WildWin: array expected";
                for (let i = 0; i < message.WildWin.length; ++i)
                    if (typeof message.WildWin[i] !== "number")
                        return "WildWin: number[] expected";
            }
            if (message.BigMult != null && message.hasOwnProperty("BigMult")) {
                if (!Array.isArray(message.BigMult))
                    return "BigMult: array expected";
                for (let i = 0; i < message.BigMult.length; ++i)
                    if (typeof message.BigMult[i] !== "number")
                        return "BigMult: number[] expected";
            }
            if (message.GameWin != null && message.hasOwnProperty("GameWin")) {
                if (!Array.isArray(message.GameWin))
                    return "GameWin: array expected";
                for (let i = 0; i < message.GameWin.length; ++i)
                    if (typeof message.GameWin[i] !== "number")
                        return "GameWin: number[] expected";
            }
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                if (!$util.isInteger(message.GetBonus))
                    return "GetBonus: integer expected";
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                if (!$util.isInteger(message.BonusSymbol))
                    return "BonusSymbol: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllj2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllj2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jllj2.SpinAck)
                return object;
            let message = new $root.jllj2.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jllj2.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jllj2.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jllj2.Single.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jllj2.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jllj2.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jllj2.GameAwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.WildOdd) {
                if (!Array.isArray(object.WildOdd))
                    throw TypeError(".jllj2.SpinAck.WildOdd: array expected");
                message.WildOdd = [];
                for (let i = 0; i < object.WildOdd.length; ++i) {
                    if (typeof object.WildOdd[i] !== "object")
                        throw TypeError(".jllj2.SpinAck.WildOdd: object expected");
                    message.WildOdd[i] = $root.jllj2.GameWildOdd.fromObject(object.WildOdd[i]);
                }
            }
            if (object.WildOddDone) {
                if (!Array.isArray(object.WildOddDone))
                    throw TypeError(".jllj2.SpinAck.WildOddDone: array expected");
                message.WildOddDone = [];
                for (let i = 0; i < object.WildOddDone.length; ++i) {
                    if (typeof object.WildOddDone[i] !== "object")
                        throw TypeError(".jllj2.SpinAck.WildOddDone: object expected");
                    message.WildOddDone[i] = $root.jllj2.GameWildOdd.fromObject(object.WildOddDone[i]);
                }
            }
            if (object.WildWin) {
                if (!Array.isArray(object.WildWin))
                    throw TypeError(".jllj2.SpinAck.WildWin: array expected");
                message.WildWin = [];
                for (let i = 0; i < object.WildWin.length; ++i)
                    message.WildWin[i] = Number(object.WildWin[i]);
            }
            if (object.BigMult) {
                if (!Array.isArray(object.BigMult))
                    throw TypeError(".jllj2.SpinAck.BigMult: array expected");
                message.BigMult = [];
                for (let i = 0; i < object.BigMult.length; ++i)
                    message.BigMult[i] = Number(object.BigMult[i]);
            }
            if (object.GameWin) {
                if (!Array.isArray(object.GameWin))
                    throw TypeError(".jllj2.SpinAck.GameWin: array expected");
                message.GameWin = [];
                for (let i = 0; i < object.GameWin.length; ++i)
                    message.GameWin[i] = Number(object.GameWin[i]);
            }
            if (object.GetBonus != null)
                message.GetBonus = object.GetBonus | 0;
            if (object.BonusSymbol != null)
                message.BonusSymbol = object.BonusSymbol | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllj2.SpinAck
         * @static
         * @param {jllj2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.WildOdd = [];
                object.WildOddDone = [];
                object.WildWin = [];
                object.BigMult = [];
                object.GameWin = [];
            }
            if (options.defaults) {
                object.GetBonus = 0;
                object.BonusSymbol = 0;
                object.Extra = false;
                object.AwardTypeFlag = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jllj2.Single.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jllj2.GameAwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.WildOdd && message.WildOdd.length) {
                object.WildOdd = [];
                for (let j = 0; j < message.WildOdd.length; ++j)
                    object.WildOdd[j] = $root.jllj2.GameWildOdd.toObject(message.WildOdd[j], options);
            }
            if (message.WildOddDone && message.WildOddDone.length) {
                object.WildOddDone = [];
                for (let j = 0; j < message.WildOddDone.length; ++j)
                    object.WildOddDone[j] = $root.jllj2.GameWildOdd.toObject(message.WildOddDone[j], options);
            }
            if (message.WildWin && message.WildWin.length) {
                object.WildWin = [];
                for (let j = 0; j < message.WildWin.length; ++j)
                    object.WildWin[j] = options.json && !isFinite(message.WildWin[j]) ? String(message.WildWin[j]) : message.WildWin[j];
            }
            if (message.BigMult && message.BigMult.length) {
                object.BigMult = [];
                for (let j = 0; j < message.BigMult.length; ++j)
                    object.BigMult[j] = options.json && !isFinite(message.BigMult[j]) ? String(message.BigMult[j]) : message.BigMult[j];
            }
            if (message.GameWin && message.GameWin.length) {
                object.GameWin = [];
                for (let j = 0; j < message.GameWin.length; ++j)
                    object.GameWin[j] = options.json && !isFinite(message.GameWin[j]) ? String(message.GameWin[j]) : message.GameWin[j];
            }
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                object.GetBonus = message.GetBonus;
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                object.BonusSymbol = message.BonusSymbol;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jllj2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jllj2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllj2.SpinAck";
        };

        return SpinAck;
    })();

    return jllj2;
})();

export const jlloe = $root.jlloe = (() => {

    /**
     * Namespace jlloe.
     * @exports jlloe
     * @namespace
     */
    const jlloe = {};

    jlloe.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlloe
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlloe
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlloe.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlloe.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlloe.Column
         * @static
         * @param {jlloe.IColumn=} [properties] Properties to set
         * @returns {jlloe.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlloe.Column.verify|verify} messages.
         * @function encode
         * @memberof jlloe.Column
         * @static
         * @param {jlloe.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlloe.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlloe.Column
         * @static
         * @param {jlloe.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlloe.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlloe.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlloe.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlloe.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlloe.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlloe.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlloe.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlloe.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlloe.Column)
                return object;
            let message = new $root.jlloe.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlloe.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlloe.Column
         * @static
         * @param {jlloe.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlloe.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlloe.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlloe.Column";
        };

        return Column;
    })();

    jlloe.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlloe
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlloe
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlloe.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlloe.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlloe.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlloe.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlloe.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlloe.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlloe.AwardData
         * @static
         * @param {jlloe.IAwardData=} [properties] Properties to set
         * @returns {jlloe.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlloe.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlloe.AwardData
         * @static
         * @param {jlloe.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mult);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlloe.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlloe.AwardData
         * @static
         * @param {jlloe.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlloe.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlloe.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlloe.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        message.Mult = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlloe.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlloe.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlloe.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlloe.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlloe.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlloe.AwardData)
                return object;
            let message = new $root.jlloe.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlloe.AwardData
         * @static
         * @param {jlloe.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlloe.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlloe.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlloe.AwardData";
        };

        return AwardData;
    })();

    jlloe.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlloe
         * @interface IRoundInfo
         * @property {Array.<jlloe.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlloe.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeType] RoundInfo FreeType
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [NudgeMult] RoundInfo NudgeMult
         * @property {number|null} [Nudge] RoundInfo Nudge
         * @property {Array.<number>|null} [NudgeReelResult] RoundInfo NudgeReelResult
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [ScatterWin] RoundInfo ScatterWin
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlloe
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlloe.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.NudgeReelResult = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlloe.IColumn>} PlateSymbol
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlloe.IAwardData>} AwardDataVec
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeType.
         * @member {number} FreeType
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeType = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo NudgeMult.
         * @member {number} NudgeMult
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NudgeMult = 0;

        /**
         * RoundInfo Nudge.
         * @member {number} Nudge
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Nudge = 0;

        /**
         * RoundInfo NudgeReelResult.
         * @member {Array.<number>} NudgeReelResult
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NudgeReelResult = $util.emptyArray;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo ScatterWin.
         * @member {number} ScatterWin
         * @memberof jlloe.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterWin = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlloe.RoundInfo
         * @static
         * @param {jlloe.IRoundInfo=} [properties] Properties to set
         * @returns {jlloe.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlloe.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlloe.RoundInfo
         * @static
         * @param {jlloe.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlloe.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlloe.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeType != null && Object.hasOwnProperty.call(message, "FreeType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeType);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.NudgeMult != null && Object.hasOwnProperty.call(message, "NudgeMult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NudgeMult);
            if (message.Nudge != null && Object.hasOwnProperty.call(message, "Nudge"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.Nudge);
            if (message.NudgeReelResult != null && message.NudgeReelResult.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.NudgeReelResult.length; ++i)
                    writer.int32(message.NudgeReelResult[i]);
                writer.ldelim();
            }
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.ScatterWin);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlloe.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlloe.RoundInfo
         * @static
         * @param {jlloe.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlloe.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlloe.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlloe.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlloe.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlloe.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeType = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.NudgeMult = reader.double();
                        break;
                    }
                case 8: {
                        message.Nudge = reader.int32();
                        break;
                    }
                case 9: {
                        if (!(message.NudgeReelResult && message.NudgeReelResult.length))
                            message.NudgeReelResult = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.NudgeReelResult.push(reader.int32());
                        } else
                            message.NudgeReelResult.push(reader.int32());
                        break;
                    }
                case 10: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 13: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlloe.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlloe.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlloe.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlloe.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlloe.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                if (!$util.isInteger(message.FreeType))
                    return "FreeType: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.NudgeMult != null && message.hasOwnProperty("NudgeMult"))
                if (typeof message.NudgeMult !== "number")
                    return "NudgeMult: number expected";
            if (message.Nudge != null && message.hasOwnProperty("Nudge"))
                if (!$util.isInteger(message.Nudge))
                    return "Nudge: integer expected";
            if (message.NudgeReelResult != null && message.hasOwnProperty("NudgeReelResult")) {
                if (!Array.isArray(message.NudgeReelResult))
                    return "NudgeReelResult: array expected";
                for (let i = 0; i < message.NudgeReelResult.length; ++i)
                    if (!$util.isInteger(message.NudgeReelResult[i]))
                        return "NudgeReelResult: integer[] expected";
            }
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlloe.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlloe.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlloe.RoundInfo)
                return object;
            let message = new $root.jlloe.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlloe.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlloe.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlloe.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlloe.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlloe.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlloe.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeType != null)
                message.FreeType = object.FreeType | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.NudgeMult != null)
                message.NudgeMult = Number(object.NudgeMult);
            if (object.Nudge != null)
                message.Nudge = object.Nudge | 0;
            if (object.NudgeReelResult) {
                if (!Array.isArray(object.NudgeReelResult))
                    throw TypeError(".jlloe.RoundInfo.NudgeReelResult: array expected");
                message.NudgeReelResult = [];
                for (let i = 0; i < object.NudgeReelResult.length; ++i)
                    message.NudgeReelResult[i] = object.NudgeReelResult[i] | 0;
            }
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlloe.RoundInfo
         * @static
         * @param {jlloe.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.NudgeReelResult = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeType = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.NudgeMult = 0;
                object.Nudge = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
                object.ScatterWin = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlloe.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlloe.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                object.FreeType = message.FreeType;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.NudgeMult != null && message.hasOwnProperty("NudgeMult"))
                object.NudgeMult = options.json && !isFinite(message.NudgeMult) ? String(message.NudgeMult) : message.NudgeMult;
            if (message.Nudge != null && message.hasOwnProperty("Nudge"))
                object.Nudge = message.Nudge;
            if (message.NudgeReelResult && message.NudgeReelResult.length) {
                object.NudgeReelResult = [];
                for (let j = 0; j < message.NudgeReelResult.length; ++j)
                    object.NudgeReelResult[j] = message.NudgeReelResult[j];
            }
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlloe.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlloe.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlloe.RoundInfo";
        };

        return RoundInfo;
    })();

    jlloe.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlloe
         * @interface ISpinAck
         * @property {Array.<jlloe.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlloe
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlloe.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlloe.IRoundInfo>} RoundQueue
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlloe.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlloe.SpinAck
         * @static
         * @param {jlloe.ISpinAck=} [properties] Properties to set
         * @returns {jlloe.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlloe.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlloe.SpinAck
         * @static
         * @param {jlloe.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlloe.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlloe.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlloe.SpinAck
         * @static
         * @param {jlloe.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlloe.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlloe.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlloe.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlloe.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlloe.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlloe.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlloe.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlloe.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlloe.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlloe.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlloe.SpinAck)
                return object;
            let message = new $root.jlloe.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlloe.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlloe.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlloe.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlloe.SpinAck
         * @static
         * @param {jlloe.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlloe.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlloe.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlloe.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlloe.SpinAck";
        };

        return SpinAck;
    })();

    jlloe.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlloe
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlloe
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlloe.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlloe.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlloe.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlloe.GameInfoData
         * @static
         * @param {jlloe.IGameInfoData=} [properties] Properties to set
         * @returns {jlloe.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlloe.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlloe.GameInfoData
         * @static
         * @param {jlloe.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlloe.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlloe.GameInfoData
         * @static
         * @param {jlloe.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlloe.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlloe.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlloe.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlloe.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlloe.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlloe.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlloe.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlloe.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlloe.GameInfoData)
                return object;
            let message = new $root.jlloe.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlloe.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlloe.GameInfoData
         * @static
         * @param {jlloe.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlloe.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlloe.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlloe.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlloe;
})();

export const jllt = $root.jllt = (() => {

    /**
     * Namespace jllt.
     * @exports jllt
     * @namespace
     */
    const jllt = {};

    jllt.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jllt
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jllt
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jllt.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jllt.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jllt.Column
         * @static
         * @param {jllt.IColumn=} [properties] Properties to set
         * @returns {jllt.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jllt.Column.verify|verify} messages.
         * @function encode
         * @memberof jllt.Column
         * @static
         * @param {jllt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jllt.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.Column
         * @static
         * @param {jllt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jllt.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.Column)
                return object;
            let message = new $root.jllt.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jllt.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.Column
         * @static
         * @param {jllt.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jllt.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jllt.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.Column";
        };

        return Column;
    })();

    jllt.Award = (function() {

        /**
         * Properties of an Award.
         * @memberof jllt
         * @interface IAward
         * @property {number|null} [AwardTypeFlag] Award AwardTypeFlag
         * @property {number|null} [AwardSymbol] Award AwardSymbol
         * @property {number|null} [AwardLine] Award AwardLine
         * @property {number|null} [Win] Award Win
         */

        /**
         * Constructs a new Award.
         * @memberof jllt
         * @classdesc Represents an Award.
         * @implements IAward
         * @constructor
         * @param {jllt.IAward=} [properties] Properties to set
         */
        function Award(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Award AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jllt.Award
         * @instance
         */
        Award.prototype.AwardTypeFlag = 0;

        /**
         * Award AwardSymbol.
         * @member {number} AwardSymbol
         * @memberof jllt.Award
         * @instance
         */
        Award.prototype.AwardSymbol = 0;

        /**
         * Award AwardLine.
         * @member {number} AwardLine
         * @memberof jllt.Award
         * @instance
         */
        Award.prototype.AwardLine = 0;

        /**
         * Award Win.
         * @member {number} Win
         * @memberof jllt.Award
         * @instance
         */
        Award.prototype.Win = 0;

        /**
         * Creates a new Award instance using the specified properties.
         * @function create
         * @memberof jllt.Award
         * @static
         * @param {jllt.IAward=} [properties] Properties to set
         * @returns {jllt.Award} Award instance
         */
        Award.create = function create(properties) {
            return new Award(properties);
        };

        /**
         * Encodes the specified Award message. Does not implicitly {@link jllt.Award.verify|verify} messages.
         * @function encode
         * @memberof jllt.Award
         * @static
         * @param {jllt.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardTypeFlag);
            if (message.AwardSymbol != null && Object.hasOwnProperty.call(message, "AwardSymbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardSymbol);
            if (message.AwardLine != null && Object.hasOwnProperty.call(message, "AwardLine"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardLine);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified Award message, length delimited. Does not implicitly {@link jllt.Award.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.Award
         * @static
         * @param {jllt.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Award message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.Award();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 2: {
                        message.AwardSymbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.AwardLine = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Award message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Award message.
         * @function verify
         * @memberof jllt.Award
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Award.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                if (!$util.isInteger(message.AwardSymbol))
                    return "AwardSymbol: integer expected";
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                if (!$util.isInteger(message.AwardLine))
                    return "AwardLine: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an Award message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.Award
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.Award} Award
         */
        Award.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.Award)
                return object;
            let message = new $root.jllt.Award();
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.AwardSymbol != null)
                message.AwardSymbol = object.AwardSymbol | 0;
            if (object.AwardLine != null)
                message.AwardLine = object.AwardLine | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an Award message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.Award
         * @static
         * @param {jllt.Award} message Award
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Award.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.AwardSymbol = 0;
                object.AwardLine = 0;
                object.Win = 0;
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                object.AwardSymbol = message.AwardSymbol;
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                object.AwardLine = message.AwardLine;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this Award to JSON.
         * @function toJSON
         * @memberof jllt.Award
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Award.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Award
         * @function getTypeUrl
         * @memberof jllt.Award
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Award.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.Award";
        };

        return Award;
    })();

    jllt.PlateData = (function() {

        /**
         * Properties of a PlateData.
         * @memberof jllt
         * @interface IPlateData
         * @property {Array.<number>|null} [HitPosVec] PlateData HitPosVec
         * @property {Array.<number>|null} [LuckyTreeVec] PlateData LuckyTreeVec
         * @property {Array.<jllt.IAward>|null} [AwardVec] PlateData AwardVec
         * @property {Array.<number>|null} [Plate] PlateData Plate
         * @property {number|null} [WinMoney] PlateData WinMoney
         * @property {number|null} [AwardTypeFlag] PlateData AwardTypeFlag
         * @property {number|null} [FreeGameTime] PlateData FreeGameTime
         */

        /**
         * Constructs a new PlateData.
         * @memberof jllt
         * @classdesc Represents a PlateData.
         * @implements IPlateData
         * @constructor
         * @param {jllt.IPlateData=} [properties] Properties to set
         */
        function PlateData(properties) {
            this.HitPosVec = [];
            this.LuckyTreeVec = [];
            this.AwardVec = [];
            this.Plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlateData HitPosVec.
         * @member {Array.<number>} HitPosVec
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.HitPosVec = $util.emptyArray;

        /**
         * PlateData LuckyTreeVec.
         * @member {Array.<number>} LuckyTreeVec
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.LuckyTreeVec = $util.emptyArray;

        /**
         * PlateData AwardVec.
         * @member {Array.<jllt.IAward>} AwardVec
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.AwardVec = $util.emptyArray;

        /**
         * PlateData Plate.
         * @member {Array.<number>} Plate
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.Plate = $util.emptyArray;

        /**
         * PlateData WinMoney.
         * @member {number} WinMoney
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.WinMoney = 0;

        /**
         * PlateData AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.AwardTypeFlag = 0;

        /**
         * PlateData FreeGameTime.
         * @member {number} FreeGameTime
         * @memberof jllt.PlateData
         * @instance
         */
        PlateData.prototype.FreeGameTime = 0;

        /**
         * Creates a new PlateData instance using the specified properties.
         * @function create
         * @memberof jllt.PlateData
         * @static
         * @param {jllt.IPlateData=} [properties] Properties to set
         * @returns {jllt.PlateData} PlateData instance
         */
        PlateData.create = function create(properties) {
            return new PlateData(properties);
        };

        /**
         * Encodes the specified PlateData message. Does not implicitly {@link jllt.PlateData.verify|verify} messages.
         * @function encode
         * @memberof jllt.PlateData
         * @static
         * @param {jllt.IPlateData} message PlateData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.HitPosVec != null && message.HitPosVec.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.HitPosVec.length; ++i)
                    writer.int32(message.HitPosVec[i]);
                writer.ldelim();
            }
            if (message.LuckyTreeVec != null && message.LuckyTreeVec.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.LuckyTreeVec.length; ++i)
                    writer.int32(message.LuckyTreeVec[i]);
                writer.ldelim();
            }
            if (message.AwardVec != null && message.AwardVec.length)
                for (let i = 0; i < message.AwardVec.length; ++i)
                    $root.jllt.Award.encode(message.AwardVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.Plate != null && message.Plate.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.Plate.length; ++i)
                    writer.int32(message.Plate[i]);
                writer.ldelim();
            }
            if (message.WinMoney != null && Object.hasOwnProperty.call(message, "WinMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.WinMoney);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.FreeGameTime != null && Object.hasOwnProperty.call(message, "FreeGameTime"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeGameTime);
            return writer;
        };

        /**
         * Encodes the specified PlateData message, length delimited. Does not implicitly {@link jllt.PlateData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.PlateData
         * @static
         * @param {jllt.IPlateData} message PlateData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlateData message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.PlateData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.PlateData} PlateData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.PlateData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.HitPosVec && message.HitPosVec.length))
                            message.HitPosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.HitPosVec.push(reader.int32());
                        } else
                            message.HitPosVec.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.LuckyTreeVec && message.LuckyTreeVec.length))
                            message.LuckyTreeVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.LuckyTreeVec.push(reader.int32());
                        } else
                            message.LuckyTreeVec.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.AwardVec && message.AwardVec.length))
                            message.AwardVec = [];
                        message.AwardVec.push($root.jllt.Award.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.Plate && message.Plate.length))
                            message.Plate = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Plate.push(reader.int32());
                        } else
                            message.Plate.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.WinMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeGameTime = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlateData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.PlateData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.PlateData} PlateData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlateData message.
         * @function verify
         * @memberof jllt.PlateData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlateData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.HitPosVec != null && message.hasOwnProperty("HitPosVec")) {
                if (!Array.isArray(message.HitPosVec))
                    return "HitPosVec: array expected";
                for (let i = 0; i < message.HitPosVec.length; ++i)
                    if (!$util.isInteger(message.HitPosVec[i]))
                        return "HitPosVec: integer[] expected";
            }
            if (message.LuckyTreeVec != null && message.hasOwnProperty("LuckyTreeVec")) {
                if (!Array.isArray(message.LuckyTreeVec))
                    return "LuckyTreeVec: array expected";
                for (let i = 0; i < message.LuckyTreeVec.length; ++i)
                    if (!$util.isInteger(message.LuckyTreeVec[i]))
                        return "LuckyTreeVec: integer[] expected";
            }
            if (message.AwardVec != null && message.hasOwnProperty("AwardVec")) {
                if (!Array.isArray(message.AwardVec))
                    return "AwardVec: array expected";
                for (let i = 0; i < message.AwardVec.length; ++i) {
                    let error = $root.jllt.Award.verify(message.AwardVec[i]);
                    if (error)
                        return "AwardVec." + error;
                }
            }
            if (message.Plate != null && message.hasOwnProperty("Plate")) {
                if (!Array.isArray(message.Plate))
                    return "Plate: array expected";
                for (let i = 0; i < message.Plate.length; ++i)
                    if (!$util.isInteger(message.Plate[i]))
                        return "Plate: integer[] expected";
            }
            if (message.WinMoney != null && message.hasOwnProperty("WinMoney"))
                if (typeof message.WinMoney !== "number")
                    return "WinMoney: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeGameTime != null && message.hasOwnProperty("FreeGameTime"))
                if (!$util.isInteger(message.FreeGameTime))
                    return "FreeGameTime: integer expected";
            return null;
        };

        /**
         * Creates a PlateData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.PlateData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.PlateData} PlateData
         */
        PlateData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.PlateData)
                return object;
            let message = new $root.jllt.PlateData();
            if (object.HitPosVec) {
                if (!Array.isArray(object.HitPosVec))
                    throw TypeError(".jllt.PlateData.HitPosVec: array expected");
                message.HitPosVec = [];
                for (let i = 0; i < object.HitPosVec.length; ++i)
                    message.HitPosVec[i] = object.HitPosVec[i] | 0;
            }
            if (object.LuckyTreeVec) {
                if (!Array.isArray(object.LuckyTreeVec))
                    throw TypeError(".jllt.PlateData.LuckyTreeVec: array expected");
                message.LuckyTreeVec = [];
                for (let i = 0; i < object.LuckyTreeVec.length; ++i)
                    message.LuckyTreeVec[i] = object.LuckyTreeVec[i] | 0;
            }
            if (object.AwardVec) {
                if (!Array.isArray(object.AwardVec))
                    throw TypeError(".jllt.PlateData.AwardVec: array expected");
                message.AwardVec = [];
                for (let i = 0; i < object.AwardVec.length; ++i) {
                    if (typeof object.AwardVec[i] !== "object")
                        throw TypeError(".jllt.PlateData.AwardVec: object expected");
                    message.AwardVec[i] = $root.jllt.Award.fromObject(object.AwardVec[i]);
                }
            }
            if (object.Plate) {
                if (!Array.isArray(object.Plate))
                    throw TypeError(".jllt.PlateData.Plate: array expected");
                message.Plate = [];
                for (let i = 0; i < object.Plate.length; ++i)
                    message.Plate[i] = object.Plate[i] | 0;
            }
            if (object.WinMoney != null)
                message.WinMoney = Number(object.WinMoney);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeGameTime != null)
                message.FreeGameTime = object.FreeGameTime | 0;
            return message;
        };

        /**
         * Creates a plain object from a PlateData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.PlateData
         * @static
         * @param {jllt.PlateData} message PlateData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlateData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.HitPosVec = [];
                object.LuckyTreeVec = [];
                object.AwardVec = [];
                object.Plate = [];
            }
            if (options.defaults) {
                object.WinMoney = 0;
                object.AwardTypeFlag = 0;
                object.FreeGameTime = 0;
            }
            if (message.HitPosVec && message.HitPosVec.length) {
                object.HitPosVec = [];
                for (let j = 0; j < message.HitPosVec.length; ++j)
                    object.HitPosVec[j] = message.HitPosVec[j];
            }
            if (message.LuckyTreeVec && message.LuckyTreeVec.length) {
                object.LuckyTreeVec = [];
                for (let j = 0; j < message.LuckyTreeVec.length; ++j)
                    object.LuckyTreeVec[j] = message.LuckyTreeVec[j];
            }
            if (message.AwardVec && message.AwardVec.length) {
                object.AwardVec = [];
                for (let j = 0; j < message.AwardVec.length; ++j)
                    object.AwardVec[j] = $root.jllt.Award.toObject(message.AwardVec[j], options);
            }
            if (message.Plate && message.Plate.length) {
                object.Plate = [];
                for (let j = 0; j < message.Plate.length; ++j)
                    object.Plate[j] = message.Plate[j];
            }
            if (message.WinMoney != null && message.hasOwnProperty("WinMoney"))
                object.WinMoney = options.json && !isFinite(message.WinMoney) ? String(message.WinMoney) : message.WinMoney;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeGameTime != null && message.hasOwnProperty("FreeGameTime"))
                object.FreeGameTime = message.FreeGameTime;
            return object;
        };

        /**
         * Converts this PlateData to JSON.
         * @function toJSON
         * @memberof jllt.PlateData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlateData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlateData
         * @function getTypeUrl
         * @memberof jllt.PlateData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlateData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.PlateData";
        };

        return PlateData;
    })();

    jllt.FreeGameData = (function() {

        /**
         * Properties of a FreeGameData.
         * @memberof jllt
         * @interface IFreeGameData
         * @property {Array.<jllt.IPlateData>|null} [PlateDataVec] FreeGameData PlateDataVec
         * @property {number|null} [TotalWin] FreeGameData TotalWin
         * @property {number|null} [TotalTimes] FreeGameData TotalTimes
         * @property {number|null} [BeginTimes] FreeGameData BeginTimes
         */

        /**
         * Constructs a new FreeGameData.
         * @memberof jllt
         * @classdesc Represents a FreeGameData.
         * @implements IFreeGameData
         * @constructor
         * @param {jllt.IFreeGameData=} [properties] Properties to set
         */
        function FreeGameData(properties) {
            this.PlateDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeGameData PlateDataVec.
         * @member {Array.<jllt.IPlateData>} PlateDataVec
         * @memberof jllt.FreeGameData
         * @instance
         */
        FreeGameData.prototype.PlateDataVec = $util.emptyArray;

        /**
         * FreeGameData TotalWin.
         * @member {number} TotalWin
         * @memberof jllt.FreeGameData
         * @instance
         */
        FreeGameData.prototype.TotalWin = 0;

        /**
         * FreeGameData TotalTimes.
         * @member {number} TotalTimes
         * @memberof jllt.FreeGameData
         * @instance
         */
        FreeGameData.prototype.TotalTimes = 0;

        /**
         * FreeGameData BeginTimes.
         * @member {number} BeginTimes
         * @memberof jllt.FreeGameData
         * @instance
         */
        FreeGameData.prototype.BeginTimes = 0;

        /**
         * Creates a new FreeGameData instance using the specified properties.
         * @function create
         * @memberof jllt.FreeGameData
         * @static
         * @param {jllt.IFreeGameData=} [properties] Properties to set
         * @returns {jllt.FreeGameData} FreeGameData instance
         */
        FreeGameData.create = function create(properties) {
            return new FreeGameData(properties);
        };

        /**
         * Encodes the specified FreeGameData message. Does not implicitly {@link jllt.FreeGameData.verify|verify} messages.
         * @function encode
         * @memberof jllt.FreeGameData
         * @static
         * @param {jllt.IFreeGameData} message FreeGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateDataVec != null && message.PlateDataVec.length)
                for (let i = 0; i < message.PlateDataVec.length; ++i)
                    $root.jllt.PlateData.encode(message.PlateDataVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.TotalTimes != null && Object.hasOwnProperty.call(message, "TotalTimes"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.TotalTimes);
            if (message.BeginTimes != null && Object.hasOwnProperty.call(message, "BeginTimes"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.BeginTimes);
            return writer;
        };

        /**
         * Encodes the specified FreeGameData message, length delimited. Does not implicitly {@link jllt.FreeGameData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.FreeGameData
         * @static
         * @param {jllt.IFreeGameData} message FreeGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeGameData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeGameData message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.FreeGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.FreeGameData} FreeGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.FreeGameData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateDataVec && message.PlateDataVec.length))
                            message.PlateDataVec = [];
                        message.PlateDataVec.push($root.jllt.PlateData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalTimes = reader.int32();
                        break;
                    }
                case 4: {
                        message.BeginTimes = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeGameData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.FreeGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.FreeGameData} FreeGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeGameData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeGameData message.
         * @function verify
         * @memberof jllt.FreeGameData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeGameData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateDataVec != null && message.hasOwnProperty("PlateDataVec")) {
                if (!Array.isArray(message.PlateDataVec))
                    return "PlateDataVec: array expected";
                for (let i = 0; i < message.PlateDataVec.length; ++i) {
                    let error = $root.jllt.PlateData.verify(message.PlateDataVec[i]);
                    if (error)
                        return "PlateDataVec." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.TotalTimes != null && message.hasOwnProperty("TotalTimes"))
                if (!$util.isInteger(message.TotalTimes))
                    return "TotalTimes: integer expected";
            if (message.BeginTimes != null && message.hasOwnProperty("BeginTimes"))
                if (!$util.isInteger(message.BeginTimes))
                    return "BeginTimes: integer expected";
            return null;
        };

        /**
         * Creates a FreeGameData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.FreeGameData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.FreeGameData} FreeGameData
         */
        FreeGameData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.FreeGameData)
                return object;
            let message = new $root.jllt.FreeGameData();
            if (object.PlateDataVec) {
                if (!Array.isArray(object.PlateDataVec))
                    throw TypeError(".jllt.FreeGameData.PlateDataVec: array expected");
                message.PlateDataVec = [];
                for (let i = 0; i < object.PlateDataVec.length; ++i) {
                    if (typeof object.PlateDataVec[i] !== "object")
                        throw TypeError(".jllt.FreeGameData.PlateDataVec: object expected");
                    message.PlateDataVec[i] = $root.jllt.PlateData.fromObject(object.PlateDataVec[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.TotalTimes != null)
                message.TotalTimes = object.TotalTimes | 0;
            if (object.BeginTimes != null)
                message.BeginTimes = object.BeginTimes | 0;
            return message;
        };

        /**
         * Creates a plain object from a FreeGameData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.FreeGameData
         * @static
         * @param {jllt.FreeGameData} message FreeGameData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeGameData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateDataVec = [];
            if (options.defaults) {
                object.TotalWin = 0;
                object.TotalTimes = 0;
                object.BeginTimes = 0;
            }
            if (message.PlateDataVec && message.PlateDataVec.length) {
                object.PlateDataVec = [];
                for (let j = 0; j < message.PlateDataVec.length; ++j)
                    object.PlateDataVec[j] = $root.jllt.PlateData.toObject(message.PlateDataVec[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.TotalTimes != null && message.hasOwnProperty("TotalTimes"))
                object.TotalTimes = message.TotalTimes;
            if (message.BeginTimes != null && message.hasOwnProperty("BeginTimes"))
                object.BeginTimes = message.BeginTimes;
            return object;
        };

        /**
         * Converts this FreeGameData to JSON.
         * @function toJSON
         * @memberof jllt.FreeGameData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeGameData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeGameData
         * @function getTypeUrl
         * @memberof jllt.FreeGameData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeGameData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.FreeGameData";
        };

        return FreeGameData;
    })();

    jllt.BonusHitRecord = (function() {

        /**
         * Properties of a BonusHitRecord.
         * @memberof jllt
         * @interface IBonusHitRecord
         * @property {number|null} [Sym] BonusHitRecord Sym
         * @property {number|null} [HitCount] BonusHitRecord HitCount
         */

        /**
         * Constructs a new BonusHitRecord.
         * @memberof jllt
         * @classdesc Represents a BonusHitRecord.
         * @implements IBonusHitRecord
         * @constructor
         * @param {jllt.IBonusHitRecord=} [properties] Properties to set
         */
        function BonusHitRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusHitRecord Sym.
         * @member {number} Sym
         * @memberof jllt.BonusHitRecord
         * @instance
         */
        BonusHitRecord.prototype.Sym = 0;

        /**
         * BonusHitRecord HitCount.
         * @member {number} HitCount
         * @memberof jllt.BonusHitRecord
         * @instance
         */
        BonusHitRecord.prototype.HitCount = 0;

        /**
         * Creates a new BonusHitRecord instance using the specified properties.
         * @function create
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {jllt.IBonusHitRecord=} [properties] Properties to set
         * @returns {jllt.BonusHitRecord} BonusHitRecord instance
         */
        BonusHitRecord.create = function create(properties) {
            return new BonusHitRecord(properties);
        };

        /**
         * Encodes the specified BonusHitRecord message. Does not implicitly {@link jllt.BonusHitRecord.verify|verify} messages.
         * @function encode
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {jllt.IBonusHitRecord} message BonusHitRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusHitRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Sym != null && Object.hasOwnProperty.call(message, "Sym"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Sym);
            if (message.HitCount != null && Object.hasOwnProperty.call(message, "HitCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.HitCount);
            return writer;
        };

        /**
         * Encodes the specified BonusHitRecord message, length delimited. Does not implicitly {@link jllt.BonusHitRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {jllt.IBonusHitRecord} message BonusHitRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusHitRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusHitRecord message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.BonusHitRecord} BonusHitRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusHitRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.BonusHitRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Sym = reader.int32();
                        break;
                    }
                case 2: {
                        message.HitCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusHitRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.BonusHitRecord} BonusHitRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusHitRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusHitRecord message.
         * @function verify
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusHitRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Sym != null && message.hasOwnProperty("Sym"))
                if (!$util.isInteger(message.Sym))
                    return "Sym: integer expected";
            if (message.HitCount != null && message.hasOwnProperty("HitCount"))
                if (!$util.isInteger(message.HitCount))
                    return "HitCount: integer expected";
            return null;
        };

        /**
         * Creates a BonusHitRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.BonusHitRecord} BonusHitRecord
         */
        BonusHitRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.BonusHitRecord)
                return object;
            let message = new $root.jllt.BonusHitRecord();
            if (object.Sym != null)
                message.Sym = object.Sym | 0;
            if (object.HitCount != null)
                message.HitCount = object.HitCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a BonusHitRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {jllt.BonusHitRecord} message BonusHitRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusHitRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Sym = 0;
                object.HitCount = 0;
            }
            if (message.Sym != null && message.hasOwnProperty("Sym"))
                object.Sym = message.Sym;
            if (message.HitCount != null && message.hasOwnProperty("HitCount"))
                object.HitCount = message.HitCount;
            return object;
        };

        /**
         * Converts this BonusHitRecord to JSON.
         * @function toJSON
         * @memberof jllt.BonusHitRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusHitRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusHitRecord
         * @function getTypeUrl
         * @memberof jllt.BonusHitRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusHitRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.BonusHitRecord";
        };

        return BonusHitRecord;
    })();

    jllt.BonusAwardDetail = (function() {

        /**
         * Properties of a BonusAwardDetail.
         * @memberof jllt
         * @interface IBonusAwardDetail
         * @property {number|null} [Symbol] BonusAwardDetail Symbol
         * @property {number|null} [Count] BonusAwardDetail Count
         * @property {number|null} [Win] BonusAwardDetail Win
         */

        /**
         * Constructs a new BonusAwardDetail.
         * @memberof jllt
         * @classdesc Represents a BonusAwardDetail.
         * @implements IBonusAwardDetail
         * @constructor
         * @param {jllt.IBonusAwardDetail=} [properties] Properties to set
         */
        function BonusAwardDetail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusAwardDetail Symbol.
         * @member {number} Symbol
         * @memberof jllt.BonusAwardDetail
         * @instance
         */
        BonusAwardDetail.prototype.Symbol = 0;

        /**
         * BonusAwardDetail Count.
         * @member {number} Count
         * @memberof jllt.BonusAwardDetail
         * @instance
         */
        BonusAwardDetail.prototype.Count = 0;

        /**
         * BonusAwardDetail Win.
         * @member {number} Win
         * @memberof jllt.BonusAwardDetail
         * @instance
         */
        BonusAwardDetail.prototype.Win = 0;

        /**
         * Creates a new BonusAwardDetail instance using the specified properties.
         * @function create
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {jllt.IBonusAwardDetail=} [properties] Properties to set
         * @returns {jllt.BonusAwardDetail} BonusAwardDetail instance
         */
        BonusAwardDetail.create = function create(properties) {
            return new BonusAwardDetail(properties);
        };

        /**
         * Encodes the specified BonusAwardDetail message. Does not implicitly {@link jllt.BonusAwardDetail.verify|verify} messages.
         * @function encode
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {jllt.IBonusAwardDetail} message BonusAwardDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAwardDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified BonusAwardDetail message, length delimited. Does not implicitly {@link jllt.BonusAwardDetail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {jllt.IBonusAwardDetail} message BonusAwardDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAwardDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusAwardDetail message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.BonusAwardDetail} BonusAwardDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAwardDetail.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.BonusAwardDetail();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusAwardDetail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.BonusAwardDetail} BonusAwardDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAwardDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusAwardDetail message.
         * @function verify
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusAwardDetail.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates a BonusAwardDetail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.BonusAwardDetail} BonusAwardDetail
         */
        BonusAwardDetail.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.BonusAwardDetail)
                return object;
            let message = new $root.jllt.BonusAwardDetail();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from a BonusAwardDetail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {jllt.BonusAwardDetail} message BonusAwardDetail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusAwardDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this BonusAwardDetail to JSON.
         * @function toJSON
         * @memberof jllt.BonusAwardDetail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusAwardDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusAwardDetail
         * @function getTypeUrl
         * @memberof jllt.BonusAwardDetail
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusAwardDetail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.BonusAwardDetail";
        };

        return BonusAwardDetail;
    })();

    jllt.BonusAward = (function() {

        /**
         * Properties of a BonusAward.
         * @memberof jllt
         * @interface IBonusAward
         * @property {number|null} [ColorId] BonusAward ColorId
         * @property {Array.<jllt.IBonusAwardDetail>|null} [AwardDetail] BonusAward AwardDetail
         */

        /**
         * Constructs a new BonusAward.
         * @memberof jllt
         * @classdesc Represents a BonusAward.
         * @implements IBonusAward
         * @constructor
         * @param {jllt.IBonusAward=} [properties] Properties to set
         */
        function BonusAward(properties) {
            this.AwardDetail = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusAward ColorId.
         * @member {number} ColorId
         * @memberof jllt.BonusAward
         * @instance
         */
        BonusAward.prototype.ColorId = 0;

        /**
         * BonusAward AwardDetail.
         * @member {Array.<jllt.IBonusAwardDetail>} AwardDetail
         * @memberof jllt.BonusAward
         * @instance
         */
        BonusAward.prototype.AwardDetail = $util.emptyArray;

        /**
         * Creates a new BonusAward instance using the specified properties.
         * @function create
         * @memberof jllt.BonusAward
         * @static
         * @param {jllt.IBonusAward=} [properties] Properties to set
         * @returns {jllt.BonusAward} BonusAward instance
         */
        BonusAward.create = function create(properties) {
            return new BonusAward(properties);
        };

        /**
         * Encodes the specified BonusAward message. Does not implicitly {@link jllt.BonusAward.verify|verify} messages.
         * @function encode
         * @memberof jllt.BonusAward
         * @static
         * @param {jllt.IBonusAward} message BonusAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ColorId != null && Object.hasOwnProperty.call(message, "ColorId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ColorId);
            if (message.AwardDetail != null && message.AwardDetail.length)
                for (let i = 0; i < message.AwardDetail.length; ++i)
                    $root.jllt.BonusAwardDetail.encode(message.AwardDetail[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BonusAward message, length delimited. Does not implicitly {@link jllt.BonusAward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.BonusAward
         * @static
         * @param {jllt.IBonusAward} message BonusAward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusAward message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.BonusAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.BonusAward} BonusAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.BonusAward();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ColorId = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.AwardDetail && message.AwardDetail.length))
                            message.AwardDetail = [];
                        message.AwardDetail.push($root.jllt.BonusAwardDetail.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusAward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.BonusAward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.BonusAward} BonusAward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusAward message.
         * @function verify
         * @memberof jllt.BonusAward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusAward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ColorId != null && message.hasOwnProperty("ColorId"))
                if (!$util.isInteger(message.ColorId))
                    return "ColorId: integer expected";
            if (message.AwardDetail != null && message.hasOwnProperty("AwardDetail")) {
                if (!Array.isArray(message.AwardDetail))
                    return "AwardDetail: array expected";
                for (let i = 0; i < message.AwardDetail.length; ++i) {
                    let error = $root.jllt.BonusAwardDetail.verify(message.AwardDetail[i]);
                    if (error)
                        return "AwardDetail." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BonusAward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.BonusAward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.BonusAward} BonusAward
         */
        BonusAward.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.BonusAward)
                return object;
            let message = new $root.jllt.BonusAward();
            if (object.ColorId != null)
                message.ColorId = object.ColorId | 0;
            if (object.AwardDetail) {
                if (!Array.isArray(object.AwardDetail))
                    throw TypeError(".jllt.BonusAward.AwardDetail: array expected");
                message.AwardDetail = [];
                for (let i = 0; i < object.AwardDetail.length; ++i) {
                    if (typeof object.AwardDetail[i] !== "object")
                        throw TypeError(".jllt.BonusAward.AwardDetail: object expected");
                    message.AwardDetail[i] = $root.jllt.BonusAwardDetail.fromObject(object.AwardDetail[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BonusAward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.BonusAward
         * @static
         * @param {jllt.BonusAward} message BonusAward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusAward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AwardDetail = [];
            if (options.defaults)
                object.ColorId = 0;
            if (message.ColorId != null && message.hasOwnProperty("ColorId"))
                object.ColorId = message.ColorId;
            if (message.AwardDetail && message.AwardDetail.length) {
                object.AwardDetail = [];
                for (let j = 0; j < message.AwardDetail.length; ++j)
                    object.AwardDetail[j] = $root.jllt.BonusAwardDetail.toObject(message.AwardDetail[j], options);
            }
            return object;
        };

        /**
         * Converts this BonusAward to JSON.
         * @function toJSON
         * @memberof jllt.BonusAward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusAward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusAward
         * @function getTypeUrl
         * @memberof jllt.BonusAward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusAward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.BonusAward";
        };

        return BonusAward;
    })();

    jllt.BonusGameData = (function() {

        /**
         * Properties of a BonusGameData.
         * @memberof jllt
         * @interface IBonusGameData
         * @property {number|null} [TotalRound] BonusGameData TotalRound
         * @property {number|null} [Bet] BonusGameData Bet
         * @property {Array.<jllt.IBonusHitRecord>|null} [HitRecord] BonusGameData HitRecord
         * @property {Array.<number>|null} [Other] BonusGameData Other
         * @property {number|null} [TotalWin] BonusGameData TotalWin
         * @property {Array.<number>|null} [HitCount] BonusGameData HitCount
         * @property {Array.<number>|null} [HitCountWin] BonusGameData HitCountWin
         */

        /**
         * Constructs a new BonusGameData.
         * @memberof jllt
         * @classdesc Represents a BonusGameData.
         * @implements IBonusGameData
         * @constructor
         * @param {jllt.IBonusGameData=} [properties] Properties to set
         */
        function BonusGameData(properties) {
            this.HitRecord = [];
            this.Other = [];
            this.HitCount = [];
            this.HitCountWin = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusGameData TotalRound.
         * @member {number} TotalRound
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.TotalRound = 0;

        /**
         * BonusGameData Bet.
         * @member {number} Bet
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.Bet = 0;

        /**
         * BonusGameData HitRecord.
         * @member {Array.<jllt.IBonusHitRecord>} HitRecord
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.HitRecord = $util.emptyArray;

        /**
         * BonusGameData Other.
         * @member {Array.<number>} Other
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.Other = $util.emptyArray;

        /**
         * BonusGameData TotalWin.
         * @member {number} TotalWin
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.TotalWin = 0;

        /**
         * BonusGameData HitCount.
         * @member {Array.<number>} HitCount
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.HitCount = $util.emptyArray;

        /**
         * BonusGameData HitCountWin.
         * @member {Array.<number>} HitCountWin
         * @memberof jllt.BonusGameData
         * @instance
         */
        BonusGameData.prototype.HitCountWin = $util.emptyArray;

        /**
         * Creates a new BonusGameData instance using the specified properties.
         * @function create
         * @memberof jllt.BonusGameData
         * @static
         * @param {jllt.IBonusGameData=} [properties] Properties to set
         * @returns {jllt.BonusGameData} BonusGameData instance
         */
        BonusGameData.create = function create(properties) {
            return new BonusGameData(properties);
        };

        /**
         * Encodes the specified BonusGameData message. Does not implicitly {@link jllt.BonusGameData.verify|verify} messages.
         * @function encode
         * @memberof jllt.BonusGameData
         * @static
         * @param {jllt.IBonusGameData} message BonusGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.TotalRound != null && Object.hasOwnProperty.call(message, "TotalRound"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.TotalRound);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Bet);
            if (message.HitRecord != null && message.HitRecord.length)
                for (let i = 0; i < message.HitRecord.length; ++i)
                    $root.jllt.BonusHitRecord.encode(message.HitRecord[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.Other != null && message.Other.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.Other.length; ++i)
                    writer.int32(message.Other[i]);
                writer.ldelim();
            }
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.HitCount != null && message.HitCount.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.HitCount.length; ++i)
                    writer.int32(message.HitCount[i]);
                writer.ldelim();
            }
            if (message.HitCountWin != null && message.HitCountWin.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.HitCountWin.length; ++i)
                    writer.double(message.HitCountWin[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified BonusGameData message, length delimited. Does not implicitly {@link jllt.BonusGameData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.BonusGameData
         * @static
         * @param {jllt.IBonusGameData} message BonusGameData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusGameData message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.BonusGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.BonusGameData} BonusGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.BonusGameData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.TotalRound = reader.int32();
                        break;
                    }
                case 2: {
                        message.Bet = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.HitRecord && message.HitRecord.length))
                            message.HitRecord = [];
                        message.HitRecord.push($root.jllt.BonusHitRecord.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.Other && message.Other.length))
                            message.Other = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Other.push(reader.int32());
                        } else
                            message.Other.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        if (!(message.HitCount && message.HitCount.length))
                            message.HitCount = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.HitCount.push(reader.int32());
                        } else
                            message.HitCount.push(reader.int32());
                        break;
                    }
                case 7: {
                        if (!(message.HitCountWin && message.HitCountWin.length))
                            message.HitCountWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.HitCountWin.push(reader.double());
                        } else
                            message.HitCountWin.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusGameData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.BonusGameData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.BonusGameData} BonusGameData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusGameData message.
         * @function verify
         * @memberof jllt.BonusGameData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusGameData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.TotalRound != null && message.hasOwnProperty("TotalRound"))
                if (!$util.isInteger(message.TotalRound))
                    return "TotalRound: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.HitRecord != null && message.hasOwnProperty("HitRecord")) {
                if (!Array.isArray(message.HitRecord))
                    return "HitRecord: array expected";
                for (let i = 0; i < message.HitRecord.length; ++i) {
                    let error = $root.jllt.BonusHitRecord.verify(message.HitRecord[i]);
                    if (error)
                        return "HitRecord." + error;
                }
            }
            if (message.Other != null && message.hasOwnProperty("Other")) {
                if (!Array.isArray(message.Other))
                    return "Other: array expected";
                for (let i = 0; i < message.Other.length; ++i)
                    if (!$util.isInteger(message.Other[i]))
                        return "Other: integer[] expected";
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.HitCount != null && message.hasOwnProperty("HitCount")) {
                if (!Array.isArray(message.HitCount))
                    return "HitCount: array expected";
                for (let i = 0; i < message.HitCount.length; ++i)
                    if (!$util.isInteger(message.HitCount[i]))
                        return "HitCount: integer[] expected";
            }
            if (message.HitCountWin != null && message.hasOwnProperty("HitCountWin")) {
                if (!Array.isArray(message.HitCountWin))
                    return "HitCountWin: array expected";
                for (let i = 0; i < message.HitCountWin.length; ++i)
                    if (typeof message.HitCountWin[i] !== "number")
                        return "HitCountWin: number[] expected";
            }
            return null;
        };

        /**
         * Creates a BonusGameData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.BonusGameData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.BonusGameData} BonusGameData
         */
        BonusGameData.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.BonusGameData)
                return object;
            let message = new $root.jllt.BonusGameData();
            if (object.TotalRound != null)
                message.TotalRound = object.TotalRound | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.HitRecord) {
                if (!Array.isArray(object.HitRecord))
                    throw TypeError(".jllt.BonusGameData.HitRecord: array expected");
                message.HitRecord = [];
                for (let i = 0; i < object.HitRecord.length; ++i) {
                    if (typeof object.HitRecord[i] !== "object")
                        throw TypeError(".jllt.BonusGameData.HitRecord: object expected");
                    message.HitRecord[i] = $root.jllt.BonusHitRecord.fromObject(object.HitRecord[i]);
                }
            }
            if (object.Other) {
                if (!Array.isArray(object.Other))
                    throw TypeError(".jllt.BonusGameData.Other: array expected");
                message.Other = [];
                for (let i = 0; i < object.Other.length; ++i)
                    message.Other[i] = object.Other[i] | 0;
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.HitCount) {
                if (!Array.isArray(object.HitCount))
                    throw TypeError(".jllt.BonusGameData.HitCount: array expected");
                message.HitCount = [];
                for (let i = 0; i < object.HitCount.length; ++i)
                    message.HitCount[i] = object.HitCount[i] | 0;
            }
            if (object.HitCountWin) {
                if (!Array.isArray(object.HitCountWin))
                    throw TypeError(".jllt.BonusGameData.HitCountWin: array expected");
                message.HitCountWin = [];
                for (let i = 0; i < object.HitCountWin.length; ++i)
                    message.HitCountWin[i] = Number(object.HitCountWin[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a BonusGameData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.BonusGameData
         * @static
         * @param {jllt.BonusGameData} message BonusGameData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusGameData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.HitRecord = [];
                object.Other = [];
                object.HitCount = [];
                object.HitCountWin = [];
            }
            if (options.defaults) {
                object.TotalRound = 0;
                object.Bet = 0;
                object.TotalWin = 0;
            }
            if (message.TotalRound != null && message.hasOwnProperty("TotalRound"))
                object.TotalRound = message.TotalRound;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.HitRecord && message.HitRecord.length) {
                object.HitRecord = [];
                for (let j = 0; j < message.HitRecord.length; ++j)
                    object.HitRecord[j] = $root.jllt.BonusHitRecord.toObject(message.HitRecord[j], options);
            }
            if (message.Other && message.Other.length) {
                object.Other = [];
                for (let j = 0; j < message.Other.length; ++j)
                    object.Other[j] = message.Other[j];
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.HitCount && message.HitCount.length) {
                object.HitCount = [];
                for (let j = 0; j < message.HitCount.length; ++j)
                    object.HitCount[j] = message.HitCount[j];
            }
            if (message.HitCountWin && message.HitCountWin.length) {
                object.HitCountWin = [];
                for (let j = 0; j < message.HitCountWin.length; ++j)
                    object.HitCountWin[j] = options.json && !isFinite(message.HitCountWin[j]) ? String(message.HitCountWin[j]) : message.HitCountWin[j];
            }
            return object;
        };

        /**
         * Converts this BonusGameData to JSON.
         * @function toJSON
         * @memberof jllt.BonusGameData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusGameData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusGameData
         * @function getTypeUrl
         * @memberof jllt.BonusGameData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusGameData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.BonusGameData";
        };

        return BonusGameData;
    })();

    jllt.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jllt
         * @interface ISpinAck
         * @property {jllt.IPlateData|null} [PlateData] SpinAck PlateData
         * @property {jllt.IBonusGameData|null} [BonusGameData] SpinAck BonusGameData
         * @property {jllt.IFreeGameData|null} [FreeGameData] SpinAck FreeGameData
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         * @property {boolean|null} [MaxFlag] SpinAck MaxFlag
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jllt
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jllt.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateData.
         * @member {jllt.IPlateData|null|undefined} PlateData
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateData = null;

        /**
         * SpinAck BonusGameData.
         * @member {jllt.IBonusGameData|null|undefined} BonusGameData
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusGameData = null;

        /**
         * SpinAck FreeGameData.
         * @member {jllt.IFreeGameData|null|undefined} FreeGameData
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeGameData = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * SpinAck MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jllt.SpinAck
         * @instance
         */
        SpinAck.prototype.MaxFlag = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jllt.SpinAck
         * @static
         * @param {jllt.ISpinAck=} [properties] Properties to set
         * @returns {jllt.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jllt.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jllt.SpinAck
         * @static
         * @param {jllt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateData != null && Object.hasOwnProperty.call(message, "PlateData"))
                $root.jllt.PlateData.encode(message.PlateData, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusGameData != null && Object.hasOwnProperty.call(message, "BonusGameData"))
                $root.jllt.BonusGameData.encode(message.BonusGameData, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreeGameData != null && Object.hasOwnProperty.call(message, "FreeGameData"))
                $root.jllt.FreeGameData.encode(message.FreeGameData, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.RID);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jllt.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jllt.SpinAck
         * @static
         * @param {jllt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jllt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jllt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jllt.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlateData = $root.jllt.PlateData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.BonusGameData = $root.jllt.BonusGameData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.FreeGameData = $root.jllt.FreeGameData.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                case 10: {
                        message.LID = reader.int32();
                        break;
                    }
                case 11: {
                        message.RID = reader.int32();
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jllt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jllt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jllt.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateData != null && message.hasOwnProperty("PlateData")) {
                let error = $root.jllt.PlateData.verify(message.PlateData);
                if (error)
                    return "PlateData." + error;
            }
            if (message.BonusGameData != null && message.hasOwnProperty("BonusGameData")) {
                let error = $root.jllt.BonusGameData.verify(message.BonusGameData);
                if (error)
                    return "BonusGameData." + error;
            }
            if (message.FreeGameData != null && message.hasOwnProperty("FreeGameData")) {
                let error = $root.jllt.FreeGameData.verify(message.FreeGameData);
                if (error)
                    return "FreeGameData." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jllt.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jllt.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jllt.SpinAck)
                return object;
            let message = new $root.jllt.SpinAck();
            if (object.PlateData != null) {
                if (typeof object.PlateData !== "object")
                    throw TypeError(".jllt.SpinAck.PlateData: object expected");
                message.PlateData = $root.jllt.PlateData.fromObject(object.PlateData);
            }
            if (object.BonusGameData != null) {
                if (typeof object.BonusGameData !== "object")
                    throw TypeError(".jllt.SpinAck.BonusGameData: object expected");
                message.BonusGameData = $root.jllt.BonusGameData.fromObject(object.BonusGameData);
            }
            if (object.FreeGameData != null) {
                if (typeof object.FreeGameData !== "object")
                    throw TypeError(".jllt.SpinAck.FreeGameData: object expected");
                message.FreeGameData = $root.jllt.FreeGameData.fromObject(object.FreeGameData);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jllt.SpinAck
         * @static
         * @param {jllt.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.PlateData = null;
                object.BonusGameData = null;
                object.FreeGameData = null;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
                object.MaxFlag = false;
            }
            if (message.PlateData != null && message.hasOwnProperty("PlateData"))
                object.PlateData = $root.jllt.PlateData.toObject(message.PlateData, options);
            if (message.BonusGameData != null && message.hasOwnProperty("BonusGameData"))
                object.BonusGameData = $root.jllt.BonusGameData.toObject(message.BonusGameData, options);
            if (message.FreeGameData != null && message.hasOwnProperty("FreeGameData"))
                object.FreeGameData = $root.jllt.FreeGameData.toObject(message.FreeGameData, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jllt.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jllt.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jllt.SpinAck";
        };

        return SpinAck;
    })();

    return jllt;
})();

export const jlma = $root.jlma = (() => {

    /**
     * Namespace jlma.
     * @exports jlma
     * @namespace
     */
    const jlma = {};

    jlma.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlma
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlma
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlma.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlma.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlma.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlma.Grid
         * @static
         * @param {jlma.IGrid=} [properties] Properties to set
         * @returns {jlma.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlma.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlma.Grid
         * @static
         * @param {jlma.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlma.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.Grid
         * @static
         * @param {jlma.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlma.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.Grid)
                return object;
            let message = new $root.jlma.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.Grid
         * @static
         * @param {jlma.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlma.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlma.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.Grid";
        };

        return Grid;
    })();

    jlma.GridHit = (function() {

        /**
         * Properties of a GridHit.
         * @memberof jlma
         * @interface IGridHit
         * @property {number|null} [Col] GridHit Col
         * @property {number|null} [Row] GridHit Row
         * @property {boolean|null} [ChangeFlag] GridHit ChangeFlag
         */

        /**
         * Constructs a new GridHit.
         * @memberof jlma
         * @classdesc Represents a GridHit.
         * @implements IGridHit
         * @constructor
         * @param {jlma.IGridHit=} [properties] Properties to set
         */
        function GridHit(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GridHit Col.
         * @member {number} Col
         * @memberof jlma.GridHit
         * @instance
         */
        GridHit.prototype.Col = 0;

        /**
         * GridHit Row.
         * @member {number} Row
         * @memberof jlma.GridHit
         * @instance
         */
        GridHit.prototype.Row = 0;

        /**
         * GridHit ChangeFlag.
         * @member {boolean} ChangeFlag
         * @memberof jlma.GridHit
         * @instance
         */
        GridHit.prototype.ChangeFlag = false;

        /**
         * Creates a new GridHit instance using the specified properties.
         * @function create
         * @memberof jlma.GridHit
         * @static
         * @param {jlma.IGridHit=} [properties] Properties to set
         * @returns {jlma.GridHit} GridHit instance
         */
        GridHit.create = function create(properties) {
            return new GridHit(properties);
        };

        /**
         * Encodes the specified GridHit message. Does not implicitly {@link jlma.GridHit.verify|verify} messages.
         * @function encode
         * @memberof jlma.GridHit
         * @static
         * @param {jlma.IGridHit} message GridHit message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GridHit.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            if (message.ChangeFlag != null && Object.hasOwnProperty.call(message, "ChangeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.ChangeFlag);
            return writer;
        };

        /**
         * Encodes the specified GridHit message, length delimited. Does not implicitly {@link jlma.GridHit.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.GridHit
         * @static
         * @param {jlma.IGridHit} message GridHit message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GridHit.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GridHit message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.GridHit
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.GridHit} GridHit
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GridHit.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.GridHit();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                case 3: {
                        message.ChangeFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GridHit message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.GridHit
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.GridHit} GridHit
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GridHit.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GridHit message.
         * @function verify
         * @memberof jlma.GridHit
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GridHit.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            if (message.ChangeFlag != null && message.hasOwnProperty("ChangeFlag"))
                if (typeof message.ChangeFlag !== "boolean")
                    return "ChangeFlag: boolean expected";
            return null;
        };

        /**
         * Creates a GridHit message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.GridHit
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.GridHit} GridHit
         */
        GridHit.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.GridHit)
                return object;
            let message = new $root.jlma.GridHit();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            if (object.ChangeFlag != null)
                message.ChangeFlag = Boolean(object.ChangeFlag);
            return message;
        };

        /**
         * Creates a plain object from a GridHit message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.GridHit
         * @static
         * @param {jlma.GridHit} message GridHit
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GridHit.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
                object.ChangeFlag = false;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            if (message.ChangeFlag != null && message.hasOwnProperty("ChangeFlag"))
                object.ChangeFlag = message.ChangeFlag;
            return object;
        };

        /**
         * Converts this GridHit to JSON.
         * @function toJSON
         * @memberof jlma.GridHit
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GridHit.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GridHit
         * @function getTypeUrl
         * @memberof jlma.GridHit
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GridHit.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.GridHit";
        };

        return GridHit;
    })();

    jlma.GridJR = (function() {

        /**
         * Properties of a GridJR.
         * @memberof jlma
         * @interface IGridJR
         * @property {number|null} [Col] GridJR Col
         * @property {number|null} [Row] GridJR Row
         * @property {jlma.IGrid|null} [Source] GridJR Source
         */

        /**
         * Constructs a new GridJR.
         * @memberof jlma
         * @classdesc Represents a GridJR.
         * @implements IGridJR
         * @constructor
         * @param {jlma.IGridJR=} [properties] Properties to set
         */
        function GridJR(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GridJR Col.
         * @member {number} Col
         * @memberof jlma.GridJR
         * @instance
         */
        GridJR.prototype.Col = 0;

        /**
         * GridJR Row.
         * @member {number} Row
         * @memberof jlma.GridJR
         * @instance
         */
        GridJR.prototype.Row = 0;

        /**
         * GridJR Source.
         * @member {jlma.IGrid|null|undefined} Source
         * @memberof jlma.GridJR
         * @instance
         */
        GridJR.prototype.Source = null;

        /**
         * Creates a new GridJR instance using the specified properties.
         * @function create
         * @memberof jlma.GridJR
         * @static
         * @param {jlma.IGridJR=} [properties] Properties to set
         * @returns {jlma.GridJR} GridJR instance
         */
        GridJR.create = function create(properties) {
            return new GridJR(properties);
        };

        /**
         * Encodes the specified GridJR message. Does not implicitly {@link jlma.GridJR.verify|verify} messages.
         * @function encode
         * @memberof jlma.GridJR
         * @static
         * @param {jlma.IGridJR} message GridJR message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GridJR.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            if (message.Source != null && Object.hasOwnProperty.call(message, "Source"))
                $root.jlma.Grid.encode(message.Source, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GridJR message, length delimited. Does not implicitly {@link jlma.GridJR.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.GridJR
         * @static
         * @param {jlma.IGridJR} message GridJR message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GridJR.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GridJR message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.GridJR
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.GridJR} GridJR
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GridJR.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.GridJR();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                case 3: {
                        message.Source = $root.jlma.Grid.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GridJR message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.GridJR
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.GridJR} GridJR
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GridJR.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GridJR message.
         * @function verify
         * @memberof jlma.GridJR
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GridJR.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            if (message.Source != null && message.hasOwnProperty("Source")) {
                let error = $root.jlma.Grid.verify(message.Source);
                if (error)
                    return "Source." + error;
            }
            return null;
        };

        /**
         * Creates a GridJR message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.GridJR
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.GridJR} GridJR
         */
        GridJR.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.GridJR)
                return object;
            let message = new $root.jlma.GridJR();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            if (object.Source != null) {
                if (typeof object.Source !== "object")
                    throw TypeError(".jlma.GridJR.Source: object expected");
                message.Source = $root.jlma.Grid.fromObject(object.Source);
            }
            return message;
        };

        /**
         * Creates a plain object from a GridJR message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.GridJR
         * @static
         * @param {jlma.GridJR} message GridJR
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GridJR.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
                object.Source = null;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            if (message.Source != null && message.hasOwnProperty("Source"))
                object.Source = $root.jlma.Grid.toObject(message.Source, options);
            return object;
        };

        /**
         * Converts this GridJR to JSON.
         * @function toJSON
         * @memberof jlma.GridJR
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GridJR.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GridJR
         * @function getTypeUrl
         * @memberof jlma.GridJR
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GridJR.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.GridJR";
        };

        return GridJR;
    })();

    jlma.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlma
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlma
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlma.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlma.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlma.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlma.MSymbol
         * @static
         * @param {jlma.IMSymbol=} [properties] Properties to set
         * @returns {jlma.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlma.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlma.MSymbol
         * @static
         * @param {jlma.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlma.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.MSymbol
         * @static
         * @param {jlma.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlma.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.MSymbol)
                return object;
            let message = new $root.jlma.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.MSymbol
         * @static
         * @param {jlma.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlma.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlma.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.MSymbol";
        };

        return MSymbol;
    })();

    jlma.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlma
         * @interface IMColumn
         * @property {Array.<jlma.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlma
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlma.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlma.IMSymbol>} MColumn
         * @memberof jlma.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlma.MColumn
         * @static
         * @param {jlma.IMColumn=} [properties] Properties to set
         * @returns {jlma.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlma.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlma.MColumn
         * @static
         * @param {jlma.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlma.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlma.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.MColumn
         * @static
         * @param {jlma.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlma.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlma.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlma.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.MColumn)
                return object;
            let message = new $root.jlma.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlma.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlma.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlma.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.MColumn
         * @static
         * @param {jlma.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlma.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlma.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlma.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.MColumn";
        };

        return MColumn;
    })();

    jlma.CSymbol = (function() {

        /**
         * Properties of a CSymbol.
         * @memberof jlma
         * @interface ICSymbol
         * @property {number|null} [Symbol] CSymbol Symbol
         * @property {number|null} [Length] CSymbol Length
         * @property {number|null} [Col] CSymbol Col
         * @property {number|null} [Row] CSymbol Row
         */

        /**
         * Constructs a new CSymbol.
         * @memberof jlma
         * @classdesc Represents a CSymbol.
         * @implements ICSymbol
         * @constructor
         * @param {jlma.ICSymbol=} [properties] Properties to set
         */
        function CSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlma.CSymbol
         * @instance
         */
        CSymbol.prototype.Symbol = 0;

        /**
         * CSymbol Length.
         * @member {number} Length
         * @memberof jlma.CSymbol
         * @instance
         */
        CSymbol.prototype.Length = 0;

        /**
         * CSymbol Col.
         * @member {number} Col
         * @memberof jlma.CSymbol
         * @instance
         */
        CSymbol.prototype.Col = 0;

        /**
         * CSymbol Row.
         * @member {number} Row
         * @memberof jlma.CSymbol
         * @instance
         */
        CSymbol.prototype.Row = 0;

        /**
         * Creates a new CSymbol instance using the specified properties.
         * @function create
         * @memberof jlma.CSymbol
         * @static
         * @param {jlma.ICSymbol=} [properties] Properties to set
         * @returns {jlma.CSymbol} CSymbol instance
         */
        CSymbol.create = function create(properties) {
            return new CSymbol(properties);
        };

        /**
         * Encodes the specified CSymbol message. Does not implicitly {@link jlma.CSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlma.CSymbol
         * @static
         * @param {jlma.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified CSymbol message, length delimited. Does not implicitly {@link jlma.CSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.CSymbol
         * @static
         * @param {jlma.ICSymbol} message CSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.CSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                case 3: {
                        message.Col = reader.int32();
                        break;
                    }
                case 4: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.CSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.CSymbol} CSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CSymbol message.
         * @function verify
         * @memberof jlma.CSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a CSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.CSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.CSymbol} CSymbol
         */
        CSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.CSymbol)
                return object;
            let message = new $root.jlma.CSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a CSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.CSymbol
         * @static
         * @param {jlma.CSymbol} message CSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this CSymbol to JSON.
         * @function toJSON
         * @memberof jlma.CSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CSymbol
         * @function getTypeUrl
         * @memberof jlma.CSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.CSymbol";
        };

        return CSymbol;
    })();

    jlma.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlma
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlma.IGridHit>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlma
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlma.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlma.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlma.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlma.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlma.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlma.IGridHit>} GridVec
         * @memberof jlma.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlma.AwardData
         * @static
         * @param {jlma.IAwardData=} [properties] Properties to set
         * @returns {jlma.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlma.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlma.AwardData
         * @static
         * @param {jlma.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlma.GridHit.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlma.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.AwardData
         * @static
         * @param {jlma.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlma.GridHit.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlma.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlma.GridHit.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.AwardData)
                return object;
            let message = new $root.jlma.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlma.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlma.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlma.GridHit.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.AwardData
         * @static
         * @param {jlma.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlma.GridHit.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlma.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlma.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.AwardData";
        };

        return AwardData;
    })();

    jlma.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlma
         * @interface IComboStageInfo
         * @property {Array.<jlma.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {Array.<jlma.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlma.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlma.ICSymbol>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         * @property {Array.<jlma.IGridJR>|null} [JokerReplace] ComboStageInfo JokerReplace
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlma
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlma.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            this.JokerReplace = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlma.IMColumn>} ComboStageSymbol
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlma.IAwardData>} AwardDataVec
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlma.IMColumn>} FillSymbols
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlma.ICSymbol>} ChangeSymbols
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * ComboStageInfo JokerReplace.
         * @member {Array.<jlma.IGridJR>} JokerReplace
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.JokerReplace = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlma.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {jlma.IComboStageInfo=} [properties] Properties to set
         * @returns {jlma.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlma.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {jlma.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlma.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlma.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlma.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlma.CSymbol.encode(message.ChangeSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.JokerReplace != null && message.JokerReplace.length)
                for (let i = 0; i < message.JokerReplace.length; ++i)
                    $root.jlma.GridJR.encode(message.JokerReplace[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlma.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {jlma.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlma.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlma.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlma.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlma.CSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.JokerReplace && message.JokerReplace.length))
                            message.JokerReplace = [];
                        message.JokerReplace.push($root.jlma.GridJR.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlma.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlma.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlma.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlma.CSymbol.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            if (message.JokerReplace != null && message.hasOwnProperty("JokerReplace")) {
                if (!Array.isArray(message.JokerReplace))
                    return "JokerReplace: array expected";
                for (let i = 0; i < message.JokerReplace.length; ++i) {
                    let error = $root.jlma.GridJR.verify(message.JokerReplace[i]);
                    if (error)
                        return "JokerReplace." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.ComboStageInfo)
                return object;
            let message = new $root.jlma.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlma.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlma.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlma.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlma.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlma.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlma.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlma.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlma.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlma.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlma.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlma.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlma.CSymbol.fromObject(object.ChangeSymbols[i]);
                }
            }
            if (object.JokerReplace) {
                if (!Array.isArray(object.JokerReplace))
                    throw TypeError(".jlma.ComboStageInfo.JokerReplace: array expected");
                message.JokerReplace = [];
                for (let i = 0; i < object.JokerReplace.length; ++i) {
                    if (typeof object.JokerReplace[i] !== "object")
                        throw TypeError(".jlma.ComboStageInfo.JokerReplace: object expected");
                    message.JokerReplace[i] = $root.jlma.GridJR.fromObject(object.JokerReplace[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {jlma.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
                object.JokerReplace = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlma.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlma.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlma.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlma.CSymbol.toObject(message.ChangeSymbols[j], options);
            }
            if (message.JokerReplace && message.JokerReplace.length) {
                object.JokerReplace = [];
                for (let j = 0; j < message.JokerReplace.length; ++j)
                    object.JokerReplace[j] = $root.jlma.GridJR.toObject(message.JokerReplace[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlma.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlma.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlma.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlma
         * @interface IRoundInfo
         * @property {Array.<jlma.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlma
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlma.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlma.IComboStageInfo>} ComboStageData
         * @memberof jlma.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlma.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlma.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlma.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlma.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlma.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlma.RoundInfo
         * @static
         * @param {jlma.IRoundInfo=} [properties] Properties to set
         * @returns {jlma.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlma.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlma.RoundInfo
         * @static
         * @param {jlma.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlma.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ScatterCount);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlma.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.RoundInfo
         * @static
         * @param {jlma.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlma.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlma.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlma.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.RoundInfo)
                return object;
            let message = new $root.jlma.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlma.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlma.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlma.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.RoundInfo
         * @static
         * @param {jlma.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.ScatterCount = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlma.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlma.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlma.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.RoundInfo";
        };

        return RoundInfo;
    })();

    jlma.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlma
         * @interface ISpinAck
         * @property {Array.<jlma.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|Long|null} [LogIndex] SpinAck LogIndex
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlma
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlma.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlma.IRoundInfo>} RoundQueue
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck LogIndex.
         * @member {number|Long} LogIndex
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.LogIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlma.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlma.SpinAck
         * @static
         * @param {jlma.ISpinAck=} [properties] Properties to set
         * @returns {jlma.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlma.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlma.SpinAck
         * @static
         * @param {jlma.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlma.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.LogIndex != null && Object.hasOwnProperty.call(message, "LogIndex"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.LogIndex);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlma.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.SpinAck
         * @static
         * @param {jlma.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlma.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.LogIndex = reader.int64();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlma.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlma.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (!$util.isInteger(message.LogIndex) && !(message.LogIndex && $util.isInteger(message.LogIndex.low) && $util.isInteger(message.LogIndex.high)))
                    return "LogIndex: integer|Long expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.SpinAck)
                return object;
            let message = new $root.jlma.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlma.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlma.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlma.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.LogIndex != null)
                if ($util.Long)
                    (message.LogIndex = $util.Long.fromValue(object.LogIndex)).unsigned = false;
                else if (typeof object.LogIndex === "string")
                    message.LogIndex = parseInt(object.LogIndex, 10);
                else if (typeof object.LogIndex === "number")
                    message.LogIndex = object.LogIndex;
                else if (typeof object.LogIndex === "object")
                    message.LogIndex = new $util.LongBits(object.LogIndex.low >>> 0, object.LogIndex.high >>> 0).toNumber();
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.SpinAck
         * @static
         * @param {jlma.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.LogIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LogIndex = options.longs === String ? "0" : 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlma.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.LogIndex != null && message.hasOwnProperty("LogIndex"))
                if (typeof message.LogIndex === "number")
                    object.LogIndex = options.longs === String ? String(message.LogIndex) : message.LogIndex;
                else
                    object.LogIndex = options.longs === String ? $util.Long.prototype.toString.call(message.LogIndex) : options.longs === Number ? new $util.LongBits(message.LogIndex.low >>> 0, message.LogIndex.high >>> 0).toNumber() : message.LogIndex;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlma.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlma.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.SpinAck";
        };

        return SpinAck;
    })();

    jlma.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlma
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlma
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlma.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlma.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlma.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlma.GameInfoData
         * @static
         * @param {jlma.IGameInfoData=} [properties] Properties to set
         * @returns {jlma.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlma.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlma.GameInfoData
         * @static
         * @param {jlma.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlma.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlma.GameInfoData
         * @static
         * @param {jlma.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlma.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlma.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlma.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlma.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlma.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlma.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlma.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlma.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlma.GameInfoData)
                return object;
            let message = new $root.jlma.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlma.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlma.GameInfoData
         * @static
         * @param {jlma.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlma.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlma.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlma.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlma;
})();

export const jlmc = $root.jlmc = (() => {

    /**
     * Namespace jlmc.
     * @exports jlmc
     * @namespace
     */
    const jlmc = {};

    jlmc.PlateInfoDetail = (function() {

        /**
         * Properties of a PlateInfoDetail.
         * @memberof jlmc
         * @interface IPlateInfoDetail
         * @property {Array.<number>|null} [PlateSymbol] PlateInfoDetail PlateSymbol
         * @property {Array.<number>|null} [Order] PlateInfoDetail Order
         * @property {number|null} [PlateWin] PlateInfoDetail PlateWin
         */

        /**
         * Constructs a new PlateInfoDetail.
         * @memberof jlmc
         * @classdesc Represents a PlateInfoDetail.
         * @implements IPlateInfoDetail
         * @constructor
         * @param {jlmc.IPlateInfoDetail=} [properties] Properties to set
         */
        function PlateInfoDetail(properties) {
            this.PlateSymbol = [];
            this.Order = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PlateInfoDetail PlateSymbol.
         * @member {Array.<number>} PlateSymbol
         * @memberof jlmc.PlateInfoDetail
         * @instance
         */
        PlateInfoDetail.prototype.PlateSymbol = $util.emptyArray;

        /**
         * PlateInfoDetail Order.
         * @member {Array.<number>} Order
         * @memberof jlmc.PlateInfoDetail
         * @instance
         */
        PlateInfoDetail.prototype.Order = $util.emptyArray;

        /**
         * PlateInfoDetail PlateWin.
         * @member {number} PlateWin
         * @memberof jlmc.PlateInfoDetail
         * @instance
         */
        PlateInfoDetail.prototype.PlateWin = 0;

        /**
         * Creates a new PlateInfoDetail instance using the specified properties.
         * @function create
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {jlmc.IPlateInfoDetail=} [properties] Properties to set
         * @returns {jlmc.PlateInfoDetail} PlateInfoDetail instance
         */
        PlateInfoDetail.create = function create(properties) {
            return new PlateInfoDetail(properties);
        };

        /**
         * Encodes the specified PlateInfoDetail message. Does not implicitly {@link jlmc.PlateInfoDetail.verify|verify} messages.
         * @function encode
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {jlmc.IPlateInfoDetail} message PlateInfoDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateInfoDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.int32(message.PlateSymbol[i]);
                writer.ldelim();
            }
            if (message.Order != null && message.Order.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Order.length; ++i)
                    writer.int32(message.Order[i]);
                writer.ldelim();
            }
            if (message.PlateWin != null && Object.hasOwnProperty.call(message, "PlateWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.PlateWin);
            return writer;
        };

        /**
         * Encodes the specified PlateInfoDetail message, length delimited. Does not implicitly {@link jlmc.PlateInfoDetail.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {jlmc.IPlateInfoDetail} message PlateInfoDetail message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PlateInfoDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PlateInfoDetail message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc.PlateInfoDetail} PlateInfoDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateInfoDetail.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc.PlateInfoDetail();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateSymbol.push(reader.int32());
                        } else
                            message.PlateSymbol.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.Order && message.Order.length))
                            message.Order = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Order.push(reader.int32());
                        } else
                            message.Order.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.PlateWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PlateInfoDetail message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc.PlateInfoDetail} PlateInfoDetail
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PlateInfoDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PlateInfoDetail message.
         * @function verify
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PlateInfoDetail.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isInteger(message.PlateSymbol[i]))
                        return "PlateSymbol: integer[] expected";
            }
            if (message.Order != null && message.hasOwnProperty("Order")) {
                if (!Array.isArray(message.Order))
                    return "Order: array expected";
                for (let i = 0; i < message.Order.length; ++i)
                    if (!$util.isInteger(message.Order[i]))
                        return "Order: integer[] expected";
            }
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                if (typeof message.PlateWin !== "number")
                    return "PlateWin: number expected";
            return null;
        };

        /**
         * Creates a PlateInfoDetail message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc.PlateInfoDetail} PlateInfoDetail
         */
        PlateInfoDetail.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc.PlateInfoDetail)
                return object;
            let message = new $root.jlmc.PlateInfoDetail();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmc.PlateInfoDetail.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = object.PlateSymbol[i] | 0;
            }
            if (object.Order) {
                if (!Array.isArray(object.Order))
                    throw TypeError(".jlmc.PlateInfoDetail.Order: array expected");
                message.Order = [];
                for (let i = 0; i < object.Order.length; ++i)
                    message.Order[i] = object.Order[i] | 0;
            }
            if (object.PlateWin != null)
                message.PlateWin = Number(object.PlateWin);
            return message;
        };

        /**
         * Creates a plain object from a PlateInfoDetail message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {jlmc.PlateInfoDetail} message PlateInfoDetail
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PlateInfoDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.Order = [];
            }
            if (options.defaults)
                object.PlateWin = 0;
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.Order && message.Order.length) {
                object.Order = [];
                for (let j = 0; j < message.Order.length; ++j)
                    object.Order[j] = message.Order[j];
            }
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                object.PlateWin = options.json && !isFinite(message.PlateWin) ? String(message.PlateWin) : message.PlateWin;
            return object;
        };

        /**
         * Converts this PlateInfoDetail to JSON.
         * @function toJSON
         * @memberof jlmc.PlateInfoDetail
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PlateInfoDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PlateInfoDetail
         * @function getTypeUrl
         * @memberof jlmc.PlateInfoDetail
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PlateInfoDetail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc.PlateInfoDetail";
        };

        return PlateInfoDetail;
    })();

    jlmc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmc
         * @interface ISpinAck
         * @property {number|null} [WheelWin] SpinAck WheelWin
         * @property {number|null} [PlateWin] SpinAck PlateWin
         * @property {boolean|null} [IsNearWin] SpinAck IsNearWin
         * @property {Array.<jlmc.IPlateInfoDetail>|null} [PlateInfo] SpinAck PlateInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {boolean|null} [IsBonus] SpinAck IsBonus
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck WheelWin.
         * @member {number} WheelWin
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelWin = 0;

        /**
         * SpinAck PlateWin.
         * @member {number} PlateWin
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateWin = 0;

        /**
         * SpinAck IsNearWin.
         * @member {boolean} IsNearWin
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.IsNearWin = false;

        /**
         * SpinAck PlateInfo.
         * @member {Array.<jlmc.IPlateInfoDetail>} PlateInfo
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateInfo = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck IsBonus.
         * @member {boolean} IsBonus
         * @memberof jlmc.SpinAck
         * @instance
         */
        SpinAck.prototype.IsBonus = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmc.SpinAck
         * @static
         * @param {jlmc.ISpinAck=} [properties] Properties to set
         * @returns {jlmc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmc.SpinAck
         * @static
         * @param {jlmc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WheelWin != null && Object.hasOwnProperty.call(message, "WheelWin"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.WheelWin);
            if (message.PlateWin != null && Object.hasOwnProperty.call(message, "PlateWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.PlateWin);
            if (message.IsNearWin != null && Object.hasOwnProperty.call(message, "IsNearWin"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.IsNearWin);
            if (message.PlateInfo != null && message.PlateInfo.length)
                for (let i = 0; i < message.PlateInfo.length; ++i)
                    $root.jlmc.PlateInfoDetail.encode(message.PlateInfo[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.RTP);
            if (message.IsBonus != null && Object.hasOwnProperty.call(message, "IsBonus"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.IsBonus);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc.SpinAck
         * @static
         * @param {jlmc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.WheelWin = reader.double();
                        break;
                    }
                case 2: {
                        message.PlateWin = reader.double();
                        break;
                    }
                case 3: {
                        message.IsNearWin = reader.bool();
                        break;
                    }
                case 4: {
                        if (!(message.PlateInfo && message.PlateInfo.length))
                            message.PlateInfo = [];
                        message.PlateInfo.push($root.jlmc.PlateInfoDetail.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 8: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 9: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 10: {
                        message.RTP = reader.double();
                        break;
                    }
                case 11: {
                        message.IsBonus = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                if (typeof message.WheelWin !== "number")
                    return "WheelWin: number expected";
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                if (typeof message.PlateWin !== "number")
                    return "PlateWin: number expected";
            if (message.IsNearWin != null && message.hasOwnProperty("IsNearWin"))
                if (typeof message.IsNearWin !== "boolean")
                    return "IsNearWin: boolean expected";
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo")) {
                if (!Array.isArray(message.PlateInfo))
                    return "PlateInfo: array expected";
                for (let i = 0; i < message.PlateInfo.length; ++i) {
                    let error = $root.jlmc.PlateInfoDetail.verify(message.PlateInfo[i]);
                    if (error)
                        return "PlateInfo." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                if (typeof message.IsBonus !== "boolean")
                    return "IsBonus: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc.SpinAck)
                return object;
            let message = new $root.jlmc.SpinAck();
            if (object.WheelWin != null)
                message.WheelWin = Number(object.WheelWin);
            if (object.PlateWin != null)
                message.PlateWin = Number(object.PlateWin);
            if (object.IsNearWin != null)
                message.IsNearWin = Boolean(object.IsNearWin);
            if (object.PlateInfo) {
                if (!Array.isArray(object.PlateInfo))
                    throw TypeError(".jlmc.SpinAck.PlateInfo: array expected");
                message.PlateInfo = [];
                for (let i = 0; i < object.PlateInfo.length; ++i) {
                    if (typeof object.PlateInfo[i] !== "object")
                        throw TypeError(".jlmc.SpinAck.PlateInfo: object expected");
                    message.PlateInfo[i] = $root.jlmc.PlateInfoDetail.fromObject(object.PlateInfo[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.IsBonus != null)
                message.IsBonus = Boolean(object.IsBonus);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc.SpinAck
         * @static
         * @param {jlmc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateInfo = [];
            if (options.defaults) {
                object.WheelWin = 0;
                object.PlateWin = 0;
                object.IsNearWin = false;
                object.TotalWin = 0;
                object.AwardTypeFlag = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.IsBonus = false;
            }
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                object.WheelWin = options.json && !isFinite(message.WheelWin) ? String(message.WheelWin) : message.WheelWin;
            if (message.PlateWin != null && message.hasOwnProperty("PlateWin"))
                object.PlateWin = options.json && !isFinite(message.PlateWin) ? String(message.PlateWin) : message.PlateWin;
            if (message.IsNearWin != null && message.hasOwnProperty("IsNearWin"))
                object.IsNearWin = message.IsNearWin;
            if (message.PlateInfo && message.PlateInfo.length) {
                object.PlateInfo = [];
                for (let j = 0; j < message.PlateInfo.length; ++j)
                    object.PlateInfo[j] = $root.jlmc.PlateInfoDetail.toObject(message.PlateInfo[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.IsBonus != null && message.hasOwnProperty("IsBonus"))
                object.IsBonus = message.IsBonus;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc.SpinAck";
        };

        return SpinAck;
    })();

    jlmc.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlmc
         * @interface IGameInfoData
         * @property {Array.<jlmc.IWheel>|null} [WheelList] GameInfoData WheelList
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlmc
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlmc.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.WheelList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData WheelList.
         * @member {Array.<jlmc.IWheel>} WheelList
         * @memberof jlmc.GameInfoData
         * @instance
         */
        GameInfoData.prototype.WheelList = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlmc.GameInfoData
         * @static
         * @param {jlmc.IGameInfoData=} [properties] Properties to set
         * @returns {jlmc.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlmc.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmc.GameInfoData
         * @static
         * @param {jlmc.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WheelList != null && message.WheelList.length)
                for (let i = 0; i < message.WheelList.length; ++i)
                    $root.jlmc.Wheel.encode(message.WheelList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlmc.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc.GameInfoData
         * @static
         * @param {jlmc.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.WheelList && message.WheelList.length))
                            message.WheelList = [];
                        message.WheelList.push($root.jlmc.Wheel.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlmc.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WheelList != null && message.hasOwnProperty("WheelList")) {
                if (!Array.isArray(message.WheelList))
                    return "WheelList: array expected";
                for (let i = 0; i < message.WheelList.length; ++i) {
                    let error = $root.jlmc.Wheel.verify(message.WheelList[i]);
                    if (error)
                        return "WheelList." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc.GameInfoData)
                return object;
            let message = new $root.jlmc.GameInfoData();
            if (object.WheelList) {
                if (!Array.isArray(object.WheelList))
                    throw TypeError(".jlmc.GameInfoData.WheelList: array expected");
                message.WheelList = [];
                for (let i = 0; i < object.WheelList.length; ++i) {
                    if (typeof object.WheelList[i] !== "object")
                        throw TypeError(".jlmc.GameInfoData.WheelList: object expected");
                    message.WheelList[i] = $root.jlmc.Wheel.fromObject(object.WheelList[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc.GameInfoData
         * @static
         * @param {jlmc.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.WheelList = [];
            if (message.WheelList && message.WheelList.length) {
                object.WheelList = [];
                for (let j = 0; j < message.WheelList.length; ++j)
                    object.WheelList[j] = $root.jlmc.Wheel.toObject(message.WheelList[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlmc.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlmc.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc.GameInfoData";
        };

        return GameInfoData;
    })();

    jlmc.Wheel = (function() {

        /**
         * Properties of a Wheel.
         * @memberof jlmc
         * @interface IWheel
         * @property {Array.<number>|null} [money] Wheel money
         */

        /**
         * Constructs a new Wheel.
         * @memberof jlmc
         * @classdesc Represents a Wheel.
         * @implements IWheel
         * @constructor
         * @param {jlmc.IWheel=} [properties] Properties to set
         */
        function Wheel(properties) {
            this.money = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Wheel money.
         * @member {Array.<number>} money
         * @memberof jlmc.Wheel
         * @instance
         */
        Wheel.prototype.money = $util.emptyArray;

        /**
         * Creates a new Wheel instance using the specified properties.
         * @function create
         * @memberof jlmc.Wheel
         * @static
         * @param {jlmc.IWheel=} [properties] Properties to set
         * @returns {jlmc.Wheel} Wheel instance
         */
        Wheel.create = function create(properties) {
            return new Wheel(properties);
        };

        /**
         * Encodes the specified Wheel message. Does not implicitly {@link jlmc.Wheel.verify|verify} messages.
         * @function encode
         * @memberof jlmc.Wheel
         * @static
         * @param {jlmc.IWheel} message Wheel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wheel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.money != null && message.money.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.money.length; ++i)
                    writer.double(message.money[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Wheel message, length delimited. Does not implicitly {@link jlmc.Wheel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc.Wheel
         * @static
         * @param {jlmc.IWheel} message Wheel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wheel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Wheel message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc.Wheel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc.Wheel} Wheel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wheel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc.Wheel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.money && message.money.length))
                            message.money = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.money.push(reader.double());
                        } else
                            message.money.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Wheel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc.Wheel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc.Wheel} Wheel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wheel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Wheel message.
         * @function verify
         * @memberof jlmc.Wheel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Wheel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.money != null && message.hasOwnProperty("money")) {
                if (!Array.isArray(message.money))
                    return "money: array expected";
                for (let i = 0; i < message.money.length; ++i)
                    if (typeof message.money[i] !== "number")
                        return "money: number[] expected";
            }
            return null;
        };

        /**
         * Creates a Wheel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc.Wheel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc.Wheel} Wheel
         */
        Wheel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc.Wheel)
                return object;
            let message = new $root.jlmc.Wheel();
            if (object.money) {
                if (!Array.isArray(object.money))
                    throw TypeError(".jlmc.Wheel.money: array expected");
                message.money = [];
                for (let i = 0; i < object.money.length; ++i)
                    message.money[i] = Number(object.money[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Wheel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc.Wheel
         * @static
         * @param {jlmc.Wheel} message Wheel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Wheel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.money = [];
            if (message.money && message.money.length) {
                object.money = [];
                for (let j = 0; j < message.money.length; ++j)
                    object.money[j] = options.json && !isFinite(message.money[j]) ? String(message.money[j]) : message.money[j];
            }
            return object;
        };

        /**
         * Converts this Wheel to JSON.
         * @function toJSON
         * @memberof jlmc.Wheel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Wheel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Wheel
         * @function getTypeUrl
         * @memberof jlmc.Wheel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Wheel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc.Wheel";
        };

        return Wheel;
    })();

    return jlmc;
})();

export const jlmc2 = $root.jlmc2 = (() => {

    /**
     * Namespace jlmc2.
     * @exports jlmc2
     * @namespace
     */
    const jlmc2 = {};

    jlmc2.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlmc2
         * @interface ISinglePlate
         * @property {Array.<number>|null} [Symbol] SinglePlate Symbol
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlmc2
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlmc2.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.Symbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate Symbol.
         * @member {Array.<number>} Symbol
         * @memberof jlmc2.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Symbol = $util.emptyArray;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {jlmc2.ISinglePlate=} [properties] Properties to set
         * @returns {jlmc2.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlmc2.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {jlmc2.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Symbol.length; ++i)
                    writer.int32(message.Symbol[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlmc2.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {jlmc2.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Symbol.push(reader.int32());
                        } else
                            message.Symbol.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i)
                    if (!$util.isInteger(message.Symbol[i]))
                        return "Symbol: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.SinglePlate)
                return object;
            let message = new $root.jlmc2.SinglePlate();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmc2.SinglePlate.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i)
                    message.Symbol[i] = object.Symbol[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {jlmc2.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Symbol = [];
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = message.Symbol[j];
            }
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlmc2.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlmc2.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.SinglePlate";
        };

        return SinglePlate;
    })();

    jlmc2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmc2
         * @interface ISpinAck
         * @property {Array.<jlmc2.ISinglePlate>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlmc2.ISinglePlate>|null} [PlateWinUp] SpinAck PlateWinUp
         * @property {number|null} [MainWin] SpinAck MainWin
         * @property {number|null} [ReSpinWin] SpinAck ReSpinWin
         * @property {number|null} [WheelWin] SpinAck WheelWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [ShowType] SpinAck ShowType
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmc2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmc2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.PlateWinUp = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlmc2.ISinglePlate>} PlateSymbol
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck PlateWinUp.
         * @member {Array.<jlmc2.ISinglePlate>} PlateWinUp
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateWinUp = $util.emptyArray;

        /**
         * SpinAck MainWin.
         * @member {number} MainWin
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.MainWin = 0;

        /**
         * SpinAck ReSpinWin.
         * @member {number} ReSpinWin
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.ReSpinWin = 0;

        /**
         * SpinAck WheelWin.
         * @member {number} WheelWin
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowType.
         * @member {number} ShowType
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowType = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlmc2.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmc2.SpinAck
         * @static
         * @param {jlmc2.ISpinAck=} [properties] Properties to set
         * @returns {jlmc2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmc2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.SpinAck
         * @static
         * @param {jlmc2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlmc2.SinglePlate.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateWinUp != null && message.PlateWinUp.length)
                for (let i = 0; i < message.PlateWinUp.length; ++i)
                    $root.jlmc2.SinglePlate.encode(message.PlateWinUp[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.MainWin);
            if (message.ReSpinWin != null && Object.hasOwnProperty.call(message, "ReSpinWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.ReSpinWin);
            if (message.WheelWin != null && Object.hasOwnProperty.call(message, "WheelWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.WheelWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.TotalWin);
            if (message.ShowType != null && Object.hasOwnProperty.call(message, "ShowType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.ShowType);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmc2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.SpinAck
         * @static
         * @param {jlmc2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlmc2.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateWinUp && message.PlateWinUp.length))
                            message.PlateWinUp = [];
                        message.PlateWinUp.push($root.jlmc2.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ReSpinWin = reader.double();
                        break;
                    }
                case 5: {
                        message.WheelWin = reader.double();
                        break;
                    }
                case 6: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 7: {
                        message.ShowType = reader.int32();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                case 10: {
                        message.LID = reader.int32();
                        break;
                    }
                case 11: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmc2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlmc2.SinglePlate.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateWinUp != null && message.hasOwnProperty("PlateWinUp")) {
                if (!Array.isArray(message.PlateWinUp))
                    return "PlateWinUp: array expected";
                for (let i = 0; i < message.PlateWinUp.length; ++i) {
                    let error = $root.jlmc2.SinglePlate.verify(message.PlateWinUp[i]);
                    if (error)
                        return "PlateWinUp." + error;
                }
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.ReSpinWin != null && message.hasOwnProperty("ReSpinWin"))
                if (typeof message.ReSpinWin !== "number")
                    return "ReSpinWin: number expected";
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                if (typeof message.WheelWin !== "number")
                    return "WheelWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowType != null && message.hasOwnProperty("ShowType"))
                if (!$util.isInteger(message.ShowType))
                    return "ShowType: integer expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.SpinAck)
                return object;
            let message = new $root.jlmc2.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmc2.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlmc2.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlmc2.SinglePlate.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateWinUp) {
                if (!Array.isArray(object.PlateWinUp))
                    throw TypeError(".jlmc2.SpinAck.PlateWinUp: array expected");
                message.PlateWinUp = [];
                for (let i = 0; i < object.PlateWinUp.length; ++i) {
                    if (typeof object.PlateWinUp[i] !== "object")
                        throw TypeError(".jlmc2.SpinAck.PlateWinUp: object expected");
                    message.PlateWinUp[i] = $root.jlmc2.SinglePlate.fromObject(object.PlateWinUp[i]);
                }
            }
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.ReSpinWin != null)
                message.ReSpinWin = Number(object.ReSpinWin);
            if (object.WheelWin != null)
                message.WheelWin = Number(object.WheelWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowType != null)
                message.ShowType = object.ShowType | 0;
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.SpinAck
         * @static
         * @param {jlmc2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateWinUp = [];
            }
            if (options.defaults) {
                object.MainWin = 0;
                object.ReSpinWin = 0;
                object.WheelWin = 0;
                object.TotalWin = 0;
                object.ShowType = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlmc2.SinglePlate.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateWinUp && message.PlateWinUp.length) {
                object.PlateWinUp = [];
                for (let j = 0; j < message.PlateWinUp.length; ++j)
                    object.PlateWinUp[j] = $root.jlmc2.SinglePlate.toObject(message.PlateWinUp[j], options);
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.ReSpinWin != null && message.hasOwnProperty("ReSpinWin"))
                object.ReSpinWin = options.json && !isFinite(message.ReSpinWin) ? String(message.ReSpinWin) : message.ReSpinWin;
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                object.WheelWin = options.json && !isFinite(message.WheelWin) ? String(message.WheelWin) : message.WheelWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowType != null && message.hasOwnProperty("ShowType"))
                object.ShowType = message.ShowType;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmc2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmc2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.SpinAck";
        };

        return SpinAck;
    })();

    jlmc2.Wheel = (function() {

        /**
         * Properties of a Wheel.
         * @memberof jlmc2
         * @interface IWheel
         * @property {Array.<number>|null} [Mul] Wheel Mul
         */

        /**
         * Constructs a new Wheel.
         * @memberof jlmc2
         * @classdesc Represents a Wheel.
         * @implements IWheel
         * @constructor
         * @param {jlmc2.IWheel=} [properties] Properties to set
         */
        function Wheel(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Wheel Mul.
         * @member {Array.<number>} Mul
         * @memberof jlmc2.Wheel
         * @instance
         */
        Wheel.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new Wheel instance using the specified properties.
         * @function create
         * @memberof jlmc2.Wheel
         * @static
         * @param {jlmc2.IWheel=} [properties] Properties to set
         * @returns {jlmc2.Wheel} Wheel instance
         */
        Wheel.create = function create(properties) {
            return new Wheel(properties);
        };

        /**
         * Encodes the specified Wheel message. Does not implicitly {@link jlmc2.Wheel.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.Wheel
         * @static
         * @param {jlmc2.IWheel} message Wheel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wheel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Wheel message, length delimited. Does not implicitly {@link jlmc2.Wheel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.Wheel
         * @static
         * @param {jlmc2.IWheel} message Wheel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wheel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Wheel message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.Wheel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.Wheel} Wheel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wheel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.Wheel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Wheel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.Wheel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.Wheel} Wheel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wheel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Wheel message.
         * @function verify
         * @memberof jlmc2.Wheel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Wheel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a Wheel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.Wheel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.Wheel} Wheel
         */
        Wheel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.Wheel)
                return object;
            let message = new $root.jlmc2.Wheel();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmc2.Wheel.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Wheel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.Wheel
         * @static
         * @param {jlmc2.Wheel} message Wheel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Wheel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this Wheel to JSON.
         * @function toJSON
         * @memberof jlmc2.Wheel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Wheel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Wheel
         * @function getTypeUrl
         * @memberof jlmc2.Wheel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Wheel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.Wheel";
        };

        return Wheel;
    })();

    jlmc2.SingleBetSymbol = (function() {

        /**
         * Properties of a SingleBetSymbol.
         * @memberof jlmc2
         * @interface ISingleBetSymbol
         * @property {number|null} [Bet] SingleBetSymbol Bet
         * @property {Array.<number>|null} [Column1] SingleBetSymbol Column1
         * @property {Array.<number>|null} [Column2] SingleBetSymbol Column2
         * @property {Array.<number>|null} [Column3] SingleBetSymbol Column3
         * @property {Array.<number>|null} [Column4] SingleBetSymbol Column4
         */

        /**
         * Constructs a new SingleBetSymbol.
         * @memberof jlmc2
         * @classdesc Represents a SingleBetSymbol.
         * @implements ISingleBetSymbol
         * @constructor
         * @param {jlmc2.ISingleBetSymbol=} [properties] Properties to set
         */
        function SingleBetSymbol(properties) {
            this.Column1 = [];
            this.Column2 = [];
            this.Column3 = [];
            this.Column4 = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SingleBetSymbol Bet.
         * @member {number} Bet
         * @memberof jlmc2.SingleBetSymbol
         * @instance
         */
        SingleBetSymbol.prototype.Bet = 0;

        /**
         * SingleBetSymbol Column1.
         * @member {Array.<number>} Column1
         * @memberof jlmc2.SingleBetSymbol
         * @instance
         */
        SingleBetSymbol.prototype.Column1 = $util.emptyArray;

        /**
         * SingleBetSymbol Column2.
         * @member {Array.<number>} Column2
         * @memberof jlmc2.SingleBetSymbol
         * @instance
         */
        SingleBetSymbol.prototype.Column2 = $util.emptyArray;

        /**
         * SingleBetSymbol Column3.
         * @member {Array.<number>} Column3
         * @memberof jlmc2.SingleBetSymbol
         * @instance
         */
        SingleBetSymbol.prototype.Column3 = $util.emptyArray;

        /**
         * SingleBetSymbol Column4.
         * @member {Array.<number>} Column4
         * @memberof jlmc2.SingleBetSymbol
         * @instance
         */
        SingleBetSymbol.prototype.Column4 = $util.emptyArray;

        /**
         * Creates a new SingleBetSymbol instance using the specified properties.
         * @function create
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {jlmc2.ISingleBetSymbol=} [properties] Properties to set
         * @returns {jlmc2.SingleBetSymbol} SingleBetSymbol instance
         */
        SingleBetSymbol.create = function create(properties) {
            return new SingleBetSymbol(properties);
        };

        /**
         * Encodes the specified SingleBetSymbol message. Does not implicitly {@link jlmc2.SingleBetSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {jlmc2.ISingleBetSymbol} message SingleBetSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleBetSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Bet);
            if (message.Column1 != null && message.Column1.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Column1.length; ++i)
                    writer.int32(message.Column1[i]);
                writer.ldelim();
            }
            if (message.Column2 != null && message.Column2.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.Column2.length; ++i)
                    writer.int32(message.Column2[i]);
                writer.ldelim();
            }
            if (message.Column3 != null && message.Column3.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.Column3.length; ++i)
                    writer.int32(message.Column3[i]);
                writer.ldelim();
            }
            if (message.Column4 != null && message.Column4.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.Column4.length; ++i)
                    writer.int32(message.Column4[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SingleBetSymbol message, length delimited. Does not implicitly {@link jlmc2.SingleBetSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {jlmc2.ISingleBetSymbol} message SingleBetSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SingleBetSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SingleBetSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.SingleBetSymbol} SingleBetSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleBetSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.SingleBetSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Bet = reader.double();
                        break;
                    }
                case 2: {
                        if (!(message.Column1 && message.Column1.length))
                            message.Column1 = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column1.push(reader.int32());
                        } else
                            message.Column1.push(reader.int32());
                        break;
                    }
                case 3: {
                        if (!(message.Column2 && message.Column2.length))
                            message.Column2 = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column2.push(reader.int32());
                        } else
                            message.Column2.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.Column3 && message.Column3.length))
                            message.Column3 = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column3.push(reader.int32());
                        } else
                            message.Column3.push(reader.int32());
                        break;
                    }
                case 5: {
                        if (!(message.Column4 && message.Column4.length))
                            message.Column4 = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column4.push(reader.int32());
                        } else
                            message.Column4.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SingleBetSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.SingleBetSymbol} SingleBetSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SingleBetSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SingleBetSymbol message.
         * @function verify
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SingleBetSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Column1 != null && message.hasOwnProperty("Column1")) {
                if (!Array.isArray(message.Column1))
                    return "Column1: array expected";
                for (let i = 0; i < message.Column1.length; ++i)
                    if (!$util.isInteger(message.Column1[i]))
                        return "Column1: integer[] expected";
            }
            if (message.Column2 != null && message.hasOwnProperty("Column2")) {
                if (!Array.isArray(message.Column2))
                    return "Column2: array expected";
                for (let i = 0; i < message.Column2.length; ++i)
                    if (!$util.isInteger(message.Column2[i]))
                        return "Column2: integer[] expected";
            }
            if (message.Column3 != null && message.hasOwnProperty("Column3")) {
                if (!Array.isArray(message.Column3))
                    return "Column3: array expected";
                for (let i = 0; i < message.Column3.length; ++i)
                    if (!$util.isInteger(message.Column3[i]))
                        return "Column3: integer[] expected";
            }
            if (message.Column4 != null && message.hasOwnProperty("Column4")) {
                if (!Array.isArray(message.Column4))
                    return "Column4: array expected";
                for (let i = 0; i < message.Column4.length; ++i)
                    if (!$util.isInteger(message.Column4[i]))
                        return "Column4: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SingleBetSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.SingleBetSymbol} SingleBetSymbol
         */
        SingleBetSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.SingleBetSymbol)
                return object;
            let message = new $root.jlmc2.SingleBetSymbol();
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Column1) {
                if (!Array.isArray(object.Column1))
                    throw TypeError(".jlmc2.SingleBetSymbol.Column1: array expected");
                message.Column1 = [];
                for (let i = 0; i < object.Column1.length; ++i)
                    message.Column1[i] = object.Column1[i] | 0;
            }
            if (object.Column2) {
                if (!Array.isArray(object.Column2))
                    throw TypeError(".jlmc2.SingleBetSymbol.Column2: array expected");
                message.Column2 = [];
                for (let i = 0; i < object.Column2.length; ++i)
                    message.Column2[i] = object.Column2[i] | 0;
            }
            if (object.Column3) {
                if (!Array.isArray(object.Column3))
                    throw TypeError(".jlmc2.SingleBetSymbol.Column3: array expected");
                message.Column3 = [];
                for (let i = 0; i < object.Column3.length; ++i)
                    message.Column3[i] = object.Column3[i] | 0;
            }
            if (object.Column4) {
                if (!Array.isArray(object.Column4))
                    throw TypeError(".jlmc2.SingleBetSymbol.Column4: array expected");
                message.Column4 = [];
                for (let i = 0; i < object.Column4.length; ++i)
                    message.Column4[i] = object.Column4[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SingleBetSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {jlmc2.SingleBetSymbol} message SingleBetSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SingleBetSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Column1 = [];
                object.Column2 = [];
                object.Column3 = [];
                object.Column4 = [];
            }
            if (options.defaults)
                object.Bet = 0;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Column1 && message.Column1.length) {
                object.Column1 = [];
                for (let j = 0; j < message.Column1.length; ++j)
                    object.Column1[j] = message.Column1[j];
            }
            if (message.Column2 && message.Column2.length) {
                object.Column2 = [];
                for (let j = 0; j < message.Column2.length; ++j)
                    object.Column2[j] = message.Column2[j];
            }
            if (message.Column3 && message.Column3.length) {
                object.Column3 = [];
                for (let j = 0; j < message.Column3.length; ++j)
                    object.Column3[j] = message.Column3[j];
            }
            if (message.Column4 && message.Column4.length) {
                object.Column4 = [];
                for (let j = 0; j < message.Column4.length; ++j)
                    object.Column4[j] = message.Column4[j];
            }
            return object;
        };

        /**
         * Converts this SingleBetSymbol to JSON.
         * @function toJSON
         * @memberof jlmc2.SingleBetSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SingleBetSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SingleBetSymbol
         * @function getTypeUrl
         * @memberof jlmc2.SingleBetSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SingleBetSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.SingleBetSymbol";
        };

        return SingleBetSymbol;
    })();

    jlmc2.BetSymbol = (function() {

        /**
         * Properties of a BetSymbol.
         * @memberof jlmc2
         * @interface IBetSymbol
         * @property {Array.<jlmc2.ISingleBetSymbol>|null} [BetSymbolMap] BetSymbol BetSymbolMap
         */

        /**
         * Constructs a new BetSymbol.
         * @memberof jlmc2
         * @classdesc Represents a BetSymbol.
         * @implements IBetSymbol
         * @constructor
         * @param {jlmc2.IBetSymbol=} [properties] Properties to set
         */
        function BetSymbol(properties) {
            this.BetSymbolMap = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BetSymbol BetSymbolMap.
         * @member {Array.<jlmc2.ISingleBetSymbol>} BetSymbolMap
         * @memberof jlmc2.BetSymbol
         * @instance
         */
        BetSymbol.prototype.BetSymbolMap = $util.emptyArray;

        /**
         * Creates a new BetSymbol instance using the specified properties.
         * @function create
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {jlmc2.IBetSymbol=} [properties] Properties to set
         * @returns {jlmc2.BetSymbol} BetSymbol instance
         */
        BetSymbol.create = function create(properties) {
            return new BetSymbol(properties);
        };

        /**
         * Encodes the specified BetSymbol message. Does not implicitly {@link jlmc2.BetSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {jlmc2.IBetSymbol} message BetSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BetSymbolMap != null && message.BetSymbolMap.length)
                for (let i = 0; i < message.BetSymbolMap.length; ++i)
                    $root.jlmc2.SingleBetSymbol.encode(message.BetSymbolMap[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BetSymbol message, length delimited. Does not implicitly {@link jlmc2.BetSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {jlmc2.IBetSymbol} message BetSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BetSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BetSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.BetSymbol} BetSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.BetSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.BetSymbolMap && message.BetSymbolMap.length))
                            message.BetSymbolMap = [];
                        message.BetSymbolMap.push($root.jlmc2.SingleBetSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BetSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.BetSymbol} BetSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BetSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BetSymbol message.
         * @function verify
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BetSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BetSymbolMap != null && message.hasOwnProperty("BetSymbolMap")) {
                if (!Array.isArray(message.BetSymbolMap))
                    return "BetSymbolMap: array expected";
                for (let i = 0; i < message.BetSymbolMap.length; ++i) {
                    let error = $root.jlmc2.SingleBetSymbol.verify(message.BetSymbolMap[i]);
                    if (error)
                        return "BetSymbolMap." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BetSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.BetSymbol} BetSymbol
         */
        BetSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.BetSymbol)
                return object;
            let message = new $root.jlmc2.BetSymbol();
            if (object.BetSymbolMap) {
                if (!Array.isArray(object.BetSymbolMap))
                    throw TypeError(".jlmc2.BetSymbol.BetSymbolMap: array expected");
                message.BetSymbolMap = [];
                for (let i = 0; i < object.BetSymbolMap.length; ++i) {
                    if (typeof object.BetSymbolMap[i] !== "object")
                        throw TypeError(".jlmc2.BetSymbol.BetSymbolMap: object expected");
                    message.BetSymbolMap[i] = $root.jlmc2.SingleBetSymbol.fromObject(object.BetSymbolMap[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BetSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {jlmc2.BetSymbol} message BetSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BetSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BetSymbolMap = [];
            if (message.BetSymbolMap && message.BetSymbolMap.length) {
                object.BetSymbolMap = [];
                for (let j = 0; j < message.BetSymbolMap.length; ++j)
                    object.BetSymbolMap[j] = $root.jlmc2.SingleBetSymbol.toObject(message.BetSymbolMap[j], options);
            }
            return object;
        };

        /**
         * Converts this BetSymbol to JSON.
         * @function toJSON
         * @memberof jlmc2.BetSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BetSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BetSymbol
         * @function getTypeUrl
         * @memberof jlmc2.BetSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BetSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.BetSymbol";
        };

        return BetSymbol;
    })();

    jlmc2.WinUpInfo = (function() {

        /**
         * Properties of a WinUpInfo.
         * @memberof jlmc2
         * @interface IWinUpInfo
         * @property {number|null} [WinUp] WinUpInfo WinUp
         */

        /**
         * Constructs a new WinUpInfo.
         * @memberof jlmc2
         * @classdesc Represents a WinUpInfo.
         * @implements IWinUpInfo
         * @constructor
         * @param {jlmc2.IWinUpInfo=} [properties] Properties to set
         */
        function WinUpInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WinUpInfo WinUp.
         * @member {number} WinUp
         * @memberof jlmc2.WinUpInfo
         * @instance
         */
        WinUpInfo.prototype.WinUp = 0;

        /**
         * Creates a new WinUpInfo instance using the specified properties.
         * @function create
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {jlmc2.IWinUpInfo=} [properties] Properties to set
         * @returns {jlmc2.WinUpInfo} WinUpInfo instance
         */
        WinUpInfo.create = function create(properties) {
            return new WinUpInfo(properties);
        };

        /**
         * Encodes the specified WinUpInfo message. Does not implicitly {@link jlmc2.WinUpInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {jlmc2.IWinUpInfo} message WinUpInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WinUpInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WinUp != null && Object.hasOwnProperty.call(message, "WinUp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.WinUp);
            return writer;
        };

        /**
         * Encodes the specified WinUpInfo message, length delimited. Does not implicitly {@link jlmc2.WinUpInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {jlmc2.IWinUpInfo} message WinUpInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WinUpInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WinUpInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.WinUpInfo} WinUpInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WinUpInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.WinUpInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.WinUp = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WinUpInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.WinUpInfo} WinUpInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WinUpInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WinUpInfo message.
         * @function verify
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WinUpInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WinUp != null && message.hasOwnProperty("WinUp"))
                if (!$util.isInteger(message.WinUp))
                    return "WinUp: integer expected";
            return null;
        };

        /**
         * Creates a WinUpInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.WinUpInfo} WinUpInfo
         */
        WinUpInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.WinUpInfo)
                return object;
            let message = new $root.jlmc2.WinUpInfo();
            if (object.WinUp != null)
                message.WinUp = object.WinUp | 0;
            return message;
        };

        /**
         * Creates a plain object from a WinUpInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {jlmc2.WinUpInfo} message WinUpInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WinUpInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.WinUp = 0;
            if (message.WinUp != null && message.hasOwnProperty("WinUp"))
                object.WinUp = message.WinUp;
            return object;
        };

        /**
         * Converts this WinUpInfo to JSON.
         * @function toJSON
         * @memberof jlmc2.WinUpInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WinUpInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WinUpInfo
         * @function getTypeUrl
         * @memberof jlmc2.WinUpInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WinUpInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.WinUpInfo";
        };

        return WinUpInfo;
    })();

    jlmc2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlmc2
         * @interface IGameInfoData
         * @property {Array.<jlmc2.IWheel>|null} [WheelList] GameInfoData WheelList
         * @property {Array.<number>|null} [AllBetList] GameInfoData AllBetList
         * @property {jlmc2.IBetSymbol|null} [BetMap] GameInfoData BetMap
         * @property {number|null} [WinUp] GameInfoData WinUp
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlmc2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlmc2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.WheelList = [];
            this.AllBetList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData WheelList.
         * @member {Array.<jlmc2.IWheel>} WheelList
         * @memberof jlmc2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.WheelList = $util.emptyArray;

        /**
         * GameInfoData AllBetList.
         * @member {Array.<number>} AllBetList
         * @memberof jlmc2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.AllBetList = $util.emptyArray;

        /**
         * GameInfoData BetMap.
         * @member {jlmc2.IBetSymbol|null|undefined} BetMap
         * @memberof jlmc2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BetMap = null;

        /**
         * GameInfoData WinUp.
         * @member {number} WinUp
         * @memberof jlmc2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.WinUp = 0;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {jlmc2.IGameInfoData=} [properties] Properties to set
         * @returns {jlmc2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlmc2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {jlmc2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WheelList != null && message.WheelList.length)
                for (let i = 0; i < message.WheelList.length; ++i)
                    $root.jlmc2.Wheel.encode(message.WheelList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AllBetList != null && message.AllBetList.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.AllBetList.length; ++i)
                    writer.double(message.AllBetList[i]);
                writer.ldelim();
            }
            if (message.BetMap != null && Object.hasOwnProperty.call(message, "BetMap"))
                $root.jlmc2.BetSymbol.encode(message.BetMap, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.WinUp != null && Object.hasOwnProperty.call(message, "WinUp"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.WinUp);
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlmc2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {jlmc2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmc2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmc2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.WheelList && message.WheelList.length))
                            message.WheelList = [];
                        message.WheelList.push($root.jlmc2.Wheel.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AllBetList && message.AllBetList.length))
                            message.AllBetList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AllBetList.push(reader.double());
                        } else
                            message.AllBetList.push(reader.double());
                        break;
                    }
                case 3: {
                        message.BetMap = $root.jlmc2.BetSymbol.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.WinUp = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmc2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WheelList != null && message.hasOwnProperty("WheelList")) {
                if (!Array.isArray(message.WheelList))
                    return "WheelList: array expected";
                for (let i = 0; i < message.WheelList.length; ++i) {
                    let error = $root.jlmc2.Wheel.verify(message.WheelList[i]);
                    if (error)
                        return "WheelList." + error;
                }
            }
            if (message.AllBetList != null && message.hasOwnProperty("AllBetList")) {
                if (!Array.isArray(message.AllBetList))
                    return "AllBetList: array expected";
                for (let i = 0; i < message.AllBetList.length; ++i)
                    if (typeof message.AllBetList[i] !== "number")
                        return "AllBetList: number[] expected";
            }
            if (message.BetMap != null && message.hasOwnProperty("BetMap")) {
                let error = $root.jlmc2.BetSymbol.verify(message.BetMap);
                if (error)
                    return "BetMap." + error;
            }
            if (message.WinUp != null && message.hasOwnProperty("WinUp"))
                if (!$util.isInteger(message.WinUp))
                    return "WinUp: integer expected";
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmc2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmc2.GameInfoData)
                return object;
            let message = new $root.jlmc2.GameInfoData();
            if (object.WheelList) {
                if (!Array.isArray(object.WheelList))
                    throw TypeError(".jlmc2.GameInfoData.WheelList: array expected");
                message.WheelList = [];
                for (let i = 0; i < object.WheelList.length; ++i) {
                    if (typeof object.WheelList[i] !== "object")
                        throw TypeError(".jlmc2.GameInfoData.WheelList: object expected");
                    message.WheelList[i] = $root.jlmc2.Wheel.fromObject(object.WheelList[i]);
                }
            }
            if (object.AllBetList) {
                if (!Array.isArray(object.AllBetList))
                    throw TypeError(".jlmc2.GameInfoData.AllBetList: array expected");
                message.AllBetList = [];
                for (let i = 0; i < object.AllBetList.length; ++i)
                    message.AllBetList[i] = Number(object.AllBetList[i]);
            }
            if (object.BetMap != null) {
                if (typeof object.BetMap !== "object")
                    throw TypeError(".jlmc2.GameInfoData.BetMap: object expected");
                message.BetMap = $root.jlmc2.BetSymbol.fromObject(object.BetMap);
            }
            if (object.WinUp != null)
                message.WinUp = object.WinUp | 0;
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {jlmc2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.WheelList = [];
                object.AllBetList = [];
            }
            if (options.defaults) {
                object.BetMap = null;
                object.WinUp = 0;
            }
            if (message.WheelList && message.WheelList.length) {
                object.WheelList = [];
                for (let j = 0; j < message.WheelList.length; ++j)
                    object.WheelList[j] = $root.jlmc2.Wheel.toObject(message.WheelList[j], options);
            }
            if (message.AllBetList && message.AllBetList.length) {
                object.AllBetList = [];
                for (let j = 0; j < message.AllBetList.length; ++j)
                    object.AllBetList[j] = options.json && !isFinite(message.AllBetList[j]) ? String(message.AllBetList[j]) : message.AllBetList[j];
            }
            if (message.BetMap != null && message.hasOwnProperty("BetMap"))
                object.BetMap = $root.jlmc2.BetSymbol.toObject(message.BetMap, options);
            if (message.WinUp != null && message.hasOwnProperty("WinUp"))
                object.WinUp = message.WinUp;
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlmc2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlmc2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmc2.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlmc2;
})();

export const jlmceb = $root.jlmceb = (() => {

    /**
     * Namespace jlmceb.
     * @exports jlmceb
     * @namespace
     */
    const jlmceb = {};

    jlmceb.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlmceb
         * @interface ISinglePlate
         * @property {Array.<number>|null} [Symbol] SinglePlate Symbol
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlmceb
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlmceb.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.Symbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate Symbol.
         * @member {Array.<number>} Symbol
         * @memberof jlmceb.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Symbol = $util.emptyArray;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {jlmceb.ISinglePlate=} [properties] Properties to set
         * @returns {jlmceb.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlmceb.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {jlmceb.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Symbol.length; ++i)
                    writer.int32(message.Symbol[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlmceb.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {jlmceb.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmceb.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmceb.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Symbol.push(reader.int32());
                        } else
                            message.Symbol.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmceb.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i)
                    if (!$util.isInteger(message.Symbol[i]))
                        return "Symbol: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmceb.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmceb.SinglePlate)
                return object;
            let message = new $root.jlmceb.SinglePlate();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmceb.SinglePlate.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i)
                    message.Symbol[i] = object.Symbol[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {jlmceb.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Symbol = [];
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = message.Symbol[j];
            }
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlmceb.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlmceb.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmceb.SinglePlate";
        };

        return SinglePlate;
    })();

    jlmceb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmceb
         * @interface ISpinAck
         * @property {Array.<jlmceb.ISinglePlate>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {number|null} [MainWin] SpinAck MainWin
         * @property {number|null} [ReSpinWin] SpinAck ReSpinWin
         * @property {number|null} [WheelWin] SpinAck WheelWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [ShowType] SpinAck ShowType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmceb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmceb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlmceb.ISinglePlate>} PlateSymbol
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck MainWin.
         * @member {number} MainWin
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.MainWin = 0;

        /**
         * SpinAck ReSpinWin.
         * @member {number} ReSpinWin
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.ReSpinWin = 0;

        /**
         * SpinAck WheelWin.
         * @member {number} WheelWin
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.WheelWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck ShowType.
         * @member {number} ShowType
         * @memberof jlmceb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmceb.SpinAck
         * @static
         * @param {jlmceb.ISpinAck=} [properties] Properties to set
         * @returns {jlmceb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmceb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmceb.SpinAck
         * @static
         * @param {jlmceb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlmceb.SinglePlate.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.MainWin);
            if (message.ReSpinWin != null && Object.hasOwnProperty.call(message, "ReSpinWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ReSpinWin);
            if (message.WheelWin != null && Object.hasOwnProperty.call(message, "WheelWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.WheelWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            if (message.ShowType != null && Object.hasOwnProperty.call(message, "ShowType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.ShowType);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmceb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmceb.SpinAck
         * @static
         * @param {jlmceb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmceb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmceb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmceb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlmceb.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ReSpinWin = reader.double();
                        break;
                    }
                case 4: {
                        message.WheelWin = reader.double();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                case 10: {
                        message.ShowType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmceb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmceb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmceb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlmceb.SinglePlate.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.ReSpinWin != null && message.hasOwnProperty("ReSpinWin"))
                if (typeof message.ReSpinWin !== "number")
                    return "ReSpinWin: number expected";
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                if (typeof message.WheelWin !== "number")
                    return "WheelWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.ShowType != null && message.hasOwnProperty("ShowType"))
                if (!$util.isInteger(message.ShowType))
                    return "ShowType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmceb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmceb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmceb.SpinAck)
                return object;
            let message = new $root.jlmceb.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmceb.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlmceb.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlmceb.SinglePlate.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.ReSpinWin != null)
                message.ReSpinWin = Number(object.ReSpinWin);
            if (object.WheelWin != null)
                message.WheelWin = Number(object.WheelWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.ShowType != null)
                message.ShowType = object.ShowType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmceb.SpinAck
         * @static
         * @param {jlmceb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateSymbol = [];
            if (options.defaults) {
                object.MainWin = 0;
                object.ReSpinWin = 0;
                object.WheelWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.ShowType = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlmceb.SinglePlate.toObject(message.PlateSymbol[j], options);
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.ReSpinWin != null && message.hasOwnProperty("ReSpinWin"))
                object.ReSpinWin = options.json && !isFinite(message.ReSpinWin) ? String(message.ReSpinWin) : message.ReSpinWin;
            if (message.WheelWin != null && message.hasOwnProperty("WheelWin"))
                object.WheelWin = options.json && !isFinite(message.WheelWin) ? String(message.WheelWin) : message.WheelWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.ShowType != null && message.hasOwnProperty("ShowType"))
                object.ShowType = message.ShowType;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmceb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmceb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmceb.SpinAck";
        };

        return SpinAck;
    })();

    jlmceb.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlmceb
         * @interface IGameInfoData
         * @property {Array.<jlmceb.IWheel>|null} [WheelList] GameInfoData WheelList
         * @property {Array.<number>|null} [AllBetList] GameInfoData AllBetList
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlmceb
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlmceb.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.WheelList = [];
            this.AllBetList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData WheelList.
         * @member {Array.<jlmceb.IWheel>} WheelList
         * @memberof jlmceb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.WheelList = $util.emptyArray;

        /**
         * GameInfoData AllBetList.
         * @member {Array.<number>} AllBetList
         * @memberof jlmceb.GameInfoData
         * @instance
         */
        GameInfoData.prototype.AllBetList = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {jlmceb.IGameInfoData=} [properties] Properties to set
         * @returns {jlmceb.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlmceb.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {jlmceb.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WheelList != null && message.WheelList.length)
                for (let i = 0; i < message.WheelList.length; ++i)
                    $root.jlmceb.Wheel.encode(message.WheelList[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AllBetList != null && message.AllBetList.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.AllBetList.length; ++i)
                    writer.double(message.AllBetList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlmceb.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {jlmceb.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmceb.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmceb.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.WheelList && message.WheelList.length))
                            message.WheelList = [];
                        message.WheelList.push($root.jlmceb.Wheel.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AllBetList && message.AllBetList.length))
                            message.AllBetList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.AllBetList.push(reader.double());
                        } else
                            message.AllBetList.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmceb.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WheelList != null && message.hasOwnProperty("WheelList")) {
                if (!Array.isArray(message.WheelList))
                    return "WheelList: array expected";
                for (let i = 0; i < message.WheelList.length; ++i) {
                    let error = $root.jlmceb.Wheel.verify(message.WheelList[i]);
                    if (error)
                        return "WheelList." + error;
                }
            }
            if (message.AllBetList != null && message.hasOwnProperty("AllBetList")) {
                if (!Array.isArray(message.AllBetList))
                    return "AllBetList: array expected";
                for (let i = 0; i < message.AllBetList.length; ++i)
                    if (typeof message.AllBetList[i] !== "number")
                        return "AllBetList: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmceb.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmceb.GameInfoData)
                return object;
            let message = new $root.jlmceb.GameInfoData();
            if (object.WheelList) {
                if (!Array.isArray(object.WheelList))
                    throw TypeError(".jlmceb.GameInfoData.WheelList: array expected");
                message.WheelList = [];
                for (let i = 0; i < object.WheelList.length; ++i) {
                    if (typeof object.WheelList[i] !== "object")
                        throw TypeError(".jlmceb.GameInfoData.WheelList: object expected");
                    message.WheelList[i] = $root.jlmceb.Wheel.fromObject(object.WheelList[i]);
                }
            }
            if (object.AllBetList) {
                if (!Array.isArray(object.AllBetList))
                    throw TypeError(".jlmceb.GameInfoData.AllBetList: array expected");
                message.AllBetList = [];
                for (let i = 0; i < object.AllBetList.length; ++i)
                    message.AllBetList[i] = Number(object.AllBetList[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {jlmceb.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.WheelList = [];
                object.AllBetList = [];
            }
            if (message.WheelList && message.WheelList.length) {
                object.WheelList = [];
                for (let j = 0; j < message.WheelList.length; ++j)
                    object.WheelList[j] = $root.jlmceb.Wheel.toObject(message.WheelList[j], options);
            }
            if (message.AllBetList && message.AllBetList.length) {
                object.AllBetList = [];
                for (let j = 0; j < message.AllBetList.length; ++j)
                    object.AllBetList[j] = options.json && !isFinite(message.AllBetList[j]) ? String(message.AllBetList[j]) : message.AllBetList[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlmceb.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlmceb.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmceb.GameInfoData";
        };

        return GameInfoData;
    })();

    jlmceb.Wheel = (function() {

        /**
         * Properties of a Wheel.
         * @memberof jlmceb
         * @interface IWheel
         * @property {Array.<number>|null} [Symbol] Wheel Symbol
         */

        /**
         * Constructs a new Wheel.
         * @memberof jlmceb
         * @classdesc Represents a Wheel.
         * @implements IWheel
         * @constructor
         * @param {jlmceb.IWheel=} [properties] Properties to set
         */
        function Wheel(properties) {
            this.Symbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Wheel Symbol.
         * @member {Array.<number>} Symbol
         * @memberof jlmceb.Wheel
         * @instance
         */
        Wheel.prototype.Symbol = $util.emptyArray;

        /**
         * Creates a new Wheel instance using the specified properties.
         * @function create
         * @memberof jlmceb.Wheel
         * @static
         * @param {jlmceb.IWheel=} [properties] Properties to set
         * @returns {jlmceb.Wheel} Wheel instance
         */
        Wheel.create = function create(properties) {
            return new Wheel(properties);
        };

        /**
         * Encodes the specified Wheel message. Does not implicitly {@link jlmceb.Wheel.verify|verify} messages.
         * @function encode
         * @memberof jlmceb.Wheel
         * @static
         * @param {jlmceb.IWheel} message Wheel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wheel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Symbol.length; ++i)
                    writer.int32(message.Symbol[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Wheel message, length delimited. Does not implicitly {@link jlmceb.Wheel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmceb.Wheel
         * @static
         * @param {jlmceb.IWheel} message Wheel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wheel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Wheel message from the specified reader or buffer.
         * @function decode
         * @memberof jlmceb.Wheel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmceb.Wheel} Wheel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wheel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmceb.Wheel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Symbol.push(reader.int32());
                        } else
                            message.Symbol.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Wheel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmceb.Wheel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmceb.Wheel} Wheel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wheel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Wheel message.
         * @function verify
         * @memberof jlmceb.Wheel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Wheel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i)
                    if (!$util.isInteger(message.Symbol[i]))
                        return "Symbol: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Wheel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmceb.Wheel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmceb.Wheel} Wheel
         */
        Wheel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmceb.Wheel)
                return object;
            let message = new $root.jlmceb.Wheel();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmceb.Wheel.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i)
                    message.Symbol[i] = object.Symbol[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Wheel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmceb.Wheel
         * @static
         * @param {jlmceb.Wheel} message Wheel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Wheel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Symbol = [];
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = message.Symbol[j];
            }
            return object;
        };

        /**
         * Converts this Wheel to JSON.
         * @function toJSON
         * @memberof jlmceb.Wheel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Wheel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Wheel
         * @function getTypeUrl
         * @memberof jlmceb.Wheel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Wheel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmceb.Wheel";
        };

        return Wheel;
    })();

    return jlmceb;
})();

export const jlmd = $root.jlmd = (() => {

    /**
     * Namespace jlmd.
     * @exports jlmd
     * @namespace
     */
    const jlmd = {};

    jlmd.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlmd
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlmd
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlmd.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlmd.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlmd.Column
         * @static
         * @param {jlmd.IColumn=} [properties] Properties to set
         * @returns {jlmd.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlmd.Column.verify|verify} messages.
         * @function encode
         * @memberof jlmd.Column
         * @static
         * @param {jlmd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlmd.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.Column
         * @static
         * @param {jlmd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlmd.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.Column)
                return object;
            let message = new $root.jlmd.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlmd.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.Column
         * @static
         * @param {jlmd.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlmd.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlmd.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.Column";
        };

        return Column;
    })();

    jlmd.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlmd
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlmd
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlmd.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlmd.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {jlmd.IColumnFloat=} [properties] Properties to set
         * @returns {jlmd.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlmd.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {jlmd.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlmd.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {jlmd.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.ColumnFloat)
                return object;
            let message = new $root.jlmd.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlmd.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {jlmd.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlmd.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlmd.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlmd.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlmd
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlmd
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlmd.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlmd.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlmd.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlmd.Grid
         * @static
         * @param {jlmd.IGrid=} [properties] Properties to set
         * @returns {jlmd.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlmd.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlmd.Grid
         * @static
         * @param {jlmd.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlmd.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.Grid
         * @static
         * @param {jlmd.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlmd.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.Grid)
                return object;
            let message = new $root.jlmd.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.Grid
         * @static
         * @param {jlmd.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlmd.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlmd.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.Grid";
        };

        return Grid;
    })();

    jlmd.NumCollectData = (function() {

        /**
         * Properties of a NumCollectData.
         * @memberof jlmd
         * @interface INumCollectData
         * @property {Array.<jlmd.IGrid>|null} [Target] NumCollectData Target
         * @property {Array.<jlmd.IGrid>|null} [Source] NumCollectData Source
         */

        /**
         * Constructs a new NumCollectData.
         * @memberof jlmd
         * @classdesc Represents a NumCollectData.
         * @implements INumCollectData
         * @constructor
         * @param {jlmd.INumCollectData=} [properties] Properties to set
         */
        function NumCollectData(properties) {
            this.Target = [];
            this.Source = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NumCollectData Target.
         * @member {Array.<jlmd.IGrid>} Target
         * @memberof jlmd.NumCollectData
         * @instance
         */
        NumCollectData.prototype.Target = $util.emptyArray;

        /**
         * NumCollectData Source.
         * @member {Array.<jlmd.IGrid>} Source
         * @memberof jlmd.NumCollectData
         * @instance
         */
        NumCollectData.prototype.Source = $util.emptyArray;

        /**
         * Creates a new NumCollectData instance using the specified properties.
         * @function create
         * @memberof jlmd.NumCollectData
         * @static
         * @param {jlmd.INumCollectData=} [properties] Properties to set
         * @returns {jlmd.NumCollectData} NumCollectData instance
         */
        NumCollectData.create = function create(properties) {
            return new NumCollectData(properties);
        };

        /**
         * Encodes the specified NumCollectData message. Does not implicitly {@link jlmd.NumCollectData.verify|verify} messages.
         * @function encode
         * @memberof jlmd.NumCollectData
         * @static
         * @param {jlmd.INumCollectData} message NumCollectData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NumCollectData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Target != null && message.Target.length)
                for (let i = 0; i < message.Target.length; ++i)
                    $root.jlmd.Grid.encode(message.Target[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Source != null && message.Source.length)
                for (let i = 0; i < message.Source.length; ++i)
                    $root.jlmd.Grid.encode(message.Source[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NumCollectData message, length delimited. Does not implicitly {@link jlmd.NumCollectData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.NumCollectData
         * @static
         * @param {jlmd.INumCollectData} message NumCollectData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NumCollectData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a NumCollectData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.NumCollectData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.NumCollectData} NumCollectData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NumCollectData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.NumCollectData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Target && message.Target.length))
                            message.Target = [];
                        message.Target.push($root.jlmd.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Source && message.Source.length))
                            message.Source = [];
                        message.Source.push($root.jlmd.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NumCollectData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.NumCollectData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.NumCollectData} NumCollectData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NumCollectData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NumCollectData message.
         * @function verify
         * @memberof jlmd.NumCollectData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NumCollectData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Target != null && message.hasOwnProperty("Target")) {
                if (!Array.isArray(message.Target))
                    return "Target: array expected";
                for (let i = 0; i < message.Target.length; ++i) {
                    let error = $root.jlmd.Grid.verify(message.Target[i]);
                    if (error)
                        return "Target." + error;
                }
            }
            if (message.Source != null && message.hasOwnProperty("Source")) {
                if (!Array.isArray(message.Source))
                    return "Source: array expected";
                for (let i = 0; i < message.Source.length; ++i) {
                    let error = $root.jlmd.Grid.verify(message.Source[i]);
                    if (error)
                        return "Source." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NumCollectData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.NumCollectData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.NumCollectData} NumCollectData
         */
        NumCollectData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.NumCollectData)
                return object;
            let message = new $root.jlmd.NumCollectData();
            if (object.Target) {
                if (!Array.isArray(object.Target))
                    throw TypeError(".jlmd.NumCollectData.Target: array expected");
                message.Target = [];
                for (let i = 0; i < object.Target.length; ++i) {
                    if (typeof object.Target[i] !== "object")
                        throw TypeError(".jlmd.NumCollectData.Target: object expected");
                    message.Target[i] = $root.jlmd.Grid.fromObject(object.Target[i]);
                }
            }
            if (object.Source) {
                if (!Array.isArray(object.Source))
                    throw TypeError(".jlmd.NumCollectData.Source: array expected");
                message.Source = [];
                for (let i = 0; i < object.Source.length; ++i) {
                    if (typeof object.Source[i] !== "object")
                        throw TypeError(".jlmd.NumCollectData.Source: object expected");
                    message.Source[i] = $root.jlmd.Grid.fromObject(object.Source[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NumCollectData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.NumCollectData
         * @static
         * @param {jlmd.NumCollectData} message NumCollectData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NumCollectData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Target = [];
                object.Source = [];
            }
            if (message.Target && message.Target.length) {
                object.Target = [];
                for (let j = 0; j < message.Target.length; ++j)
                    object.Target[j] = $root.jlmd.Grid.toObject(message.Target[j], options);
            }
            if (message.Source && message.Source.length) {
                object.Source = [];
                for (let j = 0; j < message.Source.length; ++j)
                    object.Source[j] = $root.jlmd.Grid.toObject(message.Source[j], options);
            }
            return object;
        };

        /**
         * Converts this NumCollectData to JSON.
         * @function toJSON
         * @memberof jlmd.NumCollectData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NumCollectData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NumCollectData
         * @function getTypeUrl
         * @memberof jlmd.NumCollectData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NumCollectData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.NumCollectData";
        };

        return NumCollectData;
    })();

    jlmd.Free1MedusaData = (function() {

        /**
         * Properties of a Free1MedusaData.
         * @memberof jlmd
         * @interface IFree1MedusaData
         * @property {number|null} [Num] Free1MedusaData Num
         * @property {jlmd.IGrid|null} [Grid] Free1MedusaData Grid
         */

        /**
         * Constructs a new Free1MedusaData.
         * @memberof jlmd
         * @classdesc Represents a Free1MedusaData.
         * @implements IFree1MedusaData
         * @constructor
         * @param {jlmd.IFree1MedusaData=} [properties] Properties to set
         */
        function Free1MedusaData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Free1MedusaData Num.
         * @member {number} Num
         * @memberof jlmd.Free1MedusaData
         * @instance
         */
        Free1MedusaData.prototype.Num = 0;

        /**
         * Free1MedusaData Grid.
         * @member {jlmd.IGrid|null|undefined} Grid
         * @memberof jlmd.Free1MedusaData
         * @instance
         */
        Free1MedusaData.prototype.Grid = null;

        /**
         * Creates a new Free1MedusaData instance using the specified properties.
         * @function create
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {jlmd.IFree1MedusaData=} [properties] Properties to set
         * @returns {jlmd.Free1MedusaData} Free1MedusaData instance
         */
        Free1MedusaData.create = function create(properties) {
            return new Free1MedusaData(properties);
        };

        /**
         * Encodes the specified Free1MedusaData message. Does not implicitly {@link jlmd.Free1MedusaData.verify|verify} messages.
         * @function encode
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {jlmd.IFree1MedusaData} message Free1MedusaData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Free1MedusaData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Num != null && Object.hasOwnProperty.call(message, "Num"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Num);
            if (message.Grid != null && Object.hasOwnProperty.call(message, "Grid"))
                $root.jlmd.Grid.encode(message.Grid, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Free1MedusaData message, length delimited. Does not implicitly {@link jlmd.Free1MedusaData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {jlmd.IFree1MedusaData} message Free1MedusaData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Free1MedusaData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Free1MedusaData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.Free1MedusaData} Free1MedusaData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Free1MedusaData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.Free1MedusaData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Num = reader.double();
                        break;
                    }
                case 2: {
                        message.Grid = $root.jlmd.Grid.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Free1MedusaData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.Free1MedusaData} Free1MedusaData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Free1MedusaData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Free1MedusaData message.
         * @function verify
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Free1MedusaData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Num != null && message.hasOwnProperty("Num"))
                if (typeof message.Num !== "number")
                    return "Num: number expected";
            if (message.Grid != null && message.hasOwnProperty("Grid")) {
                let error = $root.jlmd.Grid.verify(message.Grid);
                if (error)
                    return "Grid." + error;
            }
            return null;
        };

        /**
         * Creates a Free1MedusaData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.Free1MedusaData} Free1MedusaData
         */
        Free1MedusaData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.Free1MedusaData)
                return object;
            let message = new $root.jlmd.Free1MedusaData();
            if (object.Num != null)
                message.Num = Number(object.Num);
            if (object.Grid != null) {
                if (typeof object.Grid !== "object")
                    throw TypeError(".jlmd.Free1MedusaData.Grid: object expected");
                message.Grid = $root.jlmd.Grid.fromObject(object.Grid);
            }
            return message;
        };

        /**
         * Creates a plain object from a Free1MedusaData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {jlmd.Free1MedusaData} message Free1MedusaData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Free1MedusaData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Num = 0;
                object.Grid = null;
            }
            if (message.Num != null && message.hasOwnProperty("Num"))
                object.Num = options.json && !isFinite(message.Num) ? String(message.Num) : message.Num;
            if (message.Grid != null && message.hasOwnProperty("Grid"))
                object.Grid = $root.jlmd.Grid.toObject(message.Grid, options);
            return object;
        };

        /**
         * Converts this Free1MedusaData to JSON.
         * @function toJSON
         * @memberof jlmd.Free1MedusaData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Free1MedusaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Free1MedusaData
         * @function getTypeUrl
         * @memberof jlmd.Free1MedusaData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Free1MedusaData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.Free1MedusaData";
        };

        return Free1MedusaData;
    })();

    jlmd.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlmd
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlmd.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlmd
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlmd.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlmd.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlmd.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlmd.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlmd.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlmd.IGrid>} GridVec
         * @memberof jlmd.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlmd.AwardData
         * @static
         * @param {jlmd.IAwardData=} [properties] Properties to set
         * @returns {jlmd.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlmd.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlmd.AwardData
         * @static
         * @param {jlmd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlmd.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlmd.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.AwardData
         * @static
         * @param {jlmd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlmd.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlmd.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlmd.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.AwardData)
                return object;
            let message = new $root.jlmd.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlmd.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlmd.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlmd.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.AwardData
         * @static
         * @param {jlmd.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlmd.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlmd.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlmd.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.AwardData";
        };

        return AwardData;
    })();

    jlmd.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlmd
         * @interface IRoundInfo
         * @property {Array.<jlmd.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlmd.IColumnFloat>|null} [PlateSymbolNum] RoundInfo PlateSymbolNum
         * @property {Array.<jlmd.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [MedusaWin] RoundInfo MedusaWin
         * @property {number|null} [LineWin] RoundInfo LineWin
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {Array.<jlmd.INumCollectData>|null} [NumCollectPos] RoundInfo NumCollectPos
         * @property {Array.<jlmd.IFree1MedusaData>|null} [Free1Medusa] RoundInfo Free1Medusa
         * @property {number|null} [Free2Base] RoundInfo Free2Base
         * @property {number|null} [LogMode] RoundInfo LogMode
         * @property {number|null} [Free1MedusaWin] RoundInfo Free1MedusaWin
         * @property {number|null} [TrueRoundWin] RoundInfo TrueRoundWin
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlmd
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlmd.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            this.AwardDataVec = [];
            this.NumCollectPos = [];
            this.Free1Medusa = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlmd.IColumn>} PlateSymbol
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNum.
         * @member {Array.<jlmd.IColumnFloat>} PlateSymbolNum
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlmd.IAwardData>} AwardDataVec
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo MedusaWin.
         * @member {number} MedusaWin
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MedusaWin = 0;

        /**
         * RoundInfo LineWin.
         * @member {number} LineWin
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LineWin = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo NumCollectPos.
         * @member {Array.<jlmd.INumCollectData>} NumCollectPos
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.NumCollectPos = $util.emptyArray;

        /**
         * RoundInfo Free1Medusa.
         * @member {Array.<jlmd.IFree1MedusaData>} Free1Medusa
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Free1Medusa = $util.emptyArray;

        /**
         * RoundInfo Free2Base.
         * @member {number} Free2Base
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Free2Base = 0;

        /**
         * RoundInfo LogMode.
         * @member {number} LogMode
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LogMode = 0;

        /**
         * RoundInfo Free1MedusaWin.
         * @member {number} Free1MedusaWin
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Free1MedusaWin = 0;

        /**
         * RoundInfo TrueRoundWin.
         * @member {number} TrueRoundWin
         * @memberof jlmd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.TrueRoundWin = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlmd.RoundInfo
         * @static
         * @param {jlmd.IRoundInfo=} [properties] Properties to set
         * @returns {jlmd.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlmd.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmd.RoundInfo
         * @static
         * @param {jlmd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlmd.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jlmd.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlmd.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.MedusaWin != null && Object.hasOwnProperty.call(message, "MedusaWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.MedusaWin);
            if (message.LineWin != null && Object.hasOwnProperty.call(message, "LineWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.LineWin);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeRemainRound);
            if (message.NumCollectPos != null && message.NumCollectPos.length)
                for (let i = 0; i < message.NumCollectPos.length; ++i)
                    $root.jlmd.NumCollectData.encode(message.NumCollectPos[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.Free1Medusa != null && message.Free1Medusa.length)
                for (let i = 0; i < message.Free1Medusa.length; ++i)
                    $root.jlmd.Free1MedusaData.encode(message.Free1Medusa[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.Free2Base != null && Object.hasOwnProperty.call(message, "Free2Base"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.Free2Base);
            if (message.LogMode != null && Object.hasOwnProperty.call(message, "LogMode"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.LogMode);
            if (message.Free1MedusaWin != null && Object.hasOwnProperty.call(message, "Free1MedusaWin"))
                writer.uint32(/* id 13, wireType 1 =*/105).double(message.Free1MedusaWin);
            if (message.TrueRoundWin != null && Object.hasOwnProperty.call(message, "TrueRoundWin"))
                writer.uint32(/* id 14, wireType 1 =*/113).double(message.TrueRoundWin);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlmd.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.RoundInfo
         * @static
         * @param {jlmd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlmd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jlmd.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlmd.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.MedusaWin = reader.double();
                        break;
                    }
                case 5: {
                        message.LineWin = reader.double();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 9: {
                        if (!(message.NumCollectPos && message.NumCollectPos.length))
                            message.NumCollectPos = [];
                        message.NumCollectPos.push($root.jlmd.NumCollectData.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        if (!(message.Free1Medusa && message.Free1Medusa.length))
                            message.Free1Medusa = [];
                        message.Free1Medusa.push($root.jlmd.Free1MedusaData.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.Free2Base = reader.double();
                        break;
                    }
                case 12: {
                        message.LogMode = reader.int32();
                        break;
                    }
                case 13: {
                        message.Free1MedusaWin = reader.double();
                        break;
                    }
                case 14: {
                        message.TrueRoundWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlmd.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlmd.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jlmd.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlmd.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.MedusaWin != null && message.hasOwnProperty("MedusaWin"))
                if (typeof message.MedusaWin !== "number")
                    return "MedusaWin: number expected";
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                if (typeof message.LineWin !== "number")
                    return "LineWin: number expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.NumCollectPos != null && message.hasOwnProperty("NumCollectPos")) {
                if (!Array.isArray(message.NumCollectPos))
                    return "NumCollectPos: array expected";
                for (let i = 0; i < message.NumCollectPos.length; ++i) {
                    let error = $root.jlmd.NumCollectData.verify(message.NumCollectPos[i]);
                    if (error)
                        return "NumCollectPos." + error;
                }
            }
            if (message.Free1Medusa != null && message.hasOwnProperty("Free1Medusa")) {
                if (!Array.isArray(message.Free1Medusa))
                    return "Free1Medusa: array expected";
                for (let i = 0; i < message.Free1Medusa.length; ++i) {
                    let error = $root.jlmd.Free1MedusaData.verify(message.Free1Medusa[i]);
                    if (error)
                        return "Free1Medusa." + error;
                }
            }
            if (message.Free2Base != null && message.hasOwnProperty("Free2Base"))
                if (typeof message.Free2Base !== "number")
                    return "Free2Base: number expected";
            if (message.LogMode != null && message.hasOwnProperty("LogMode"))
                if (!$util.isInteger(message.LogMode))
                    return "LogMode: integer expected";
            if (message.Free1MedusaWin != null && message.hasOwnProperty("Free1MedusaWin"))
                if (typeof message.Free1MedusaWin !== "number")
                    return "Free1MedusaWin: number expected";
            if (message.TrueRoundWin != null && message.hasOwnProperty("TrueRoundWin"))
                if (typeof message.TrueRoundWin !== "number")
                    return "TrueRoundWin: number expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.RoundInfo)
                return object;
            let message = new $root.jlmd.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmd.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlmd.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlmd.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jlmd.RoundInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jlmd.RoundInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jlmd.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlmd.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlmd.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlmd.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.MedusaWin != null)
                message.MedusaWin = Number(object.MedusaWin);
            if (object.LineWin != null)
                message.LineWin = Number(object.LineWin);
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.NumCollectPos) {
                if (!Array.isArray(object.NumCollectPos))
                    throw TypeError(".jlmd.RoundInfo.NumCollectPos: array expected");
                message.NumCollectPos = [];
                for (let i = 0; i < object.NumCollectPos.length; ++i) {
                    if (typeof object.NumCollectPos[i] !== "object")
                        throw TypeError(".jlmd.RoundInfo.NumCollectPos: object expected");
                    message.NumCollectPos[i] = $root.jlmd.NumCollectData.fromObject(object.NumCollectPos[i]);
                }
            }
            if (object.Free1Medusa) {
                if (!Array.isArray(object.Free1Medusa))
                    throw TypeError(".jlmd.RoundInfo.Free1Medusa: array expected");
                message.Free1Medusa = [];
                for (let i = 0; i < object.Free1Medusa.length; ++i) {
                    if (typeof object.Free1Medusa[i] !== "object")
                        throw TypeError(".jlmd.RoundInfo.Free1Medusa: object expected");
                    message.Free1Medusa[i] = $root.jlmd.Free1MedusaData.fromObject(object.Free1Medusa[i]);
                }
            }
            if (object.Free2Base != null)
                message.Free2Base = Number(object.Free2Base);
            if (object.LogMode != null)
                message.LogMode = object.LogMode | 0;
            if (object.Free1MedusaWin != null)
                message.Free1MedusaWin = Number(object.Free1MedusaWin);
            if (object.TrueRoundWin != null)
                message.TrueRoundWin = Number(object.TrueRoundWin);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.RoundInfo
         * @static
         * @param {jlmd.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
                object.AwardDataVec = [];
                object.NumCollectPos = [];
                object.Free1Medusa = [];
            }
            if (options.defaults) {
                object.MedusaWin = 0;
                object.LineWin = 0;
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.Free2Base = 0;
                object.LogMode = 0;
                object.Free1MedusaWin = 0;
                object.TrueRoundWin = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlmd.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jlmd.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlmd.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.MedusaWin != null && message.hasOwnProperty("MedusaWin"))
                object.MedusaWin = options.json && !isFinite(message.MedusaWin) ? String(message.MedusaWin) : message.MedusaWin;
            if (message.LineWin != null && message.hasOwnProperty("LineWin"))
                object.LineWin = options.json && !isFinite(message.LineWin) ? String(message.LineWin) : message.LineWin;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.NumCollectPos && message.NumCollectPos.length) {
                object.NumCollectPos = [];
                for (let j = 0; j < message.NumCollectPos.length; ++j)
                    object.NumCollectPos[j] = $root.jlmd.NumCollectData.toObject(message.NumCollectPos[j], options);
            }
            if (message.Free1Medusa && message.Free1Medusa.length) {
                object.Free1Medusa = [];
                for (let j = 0; j < message.Free1Medusa.length; ++j)
                    object.Free1Medusa[j] = $root.jlmd.Free1MedusaData.toObject(message.Free1Medusa[j], options);
            }
            if (message.Free2Base != null && message.hasOwnProperty("Free2Base"))
                object.Free2Base = options.json && !isFinite(message.Free2Base) ? String(message.Free2Base) : message.Free2Base;
            if (message.LogMode != null && message.hasOwnProperty("LogMode"))
                object.LogMode = message.LogMode;
            if (message.Free1MedusaWin != null && message.hasOwnProperty("Free1MedusaWin"))
                object.Free1MedusaWin = options.json && !isFinite(message.Free1MedusaWin) ? String(message.Free1MedusaWin) : message.Free1MedusaWin;
            if (message.TrueRoundWin != null && message.hasOwnProperty("TrueRoundWin"))
                object.TrueRoundWin = options.json && !isFinite(message.TrueRoundWin) ? String(message.TrueRoundWin) : message.TrueRoundWin;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlmd.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlmd.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.RoundInfo";
        };

        return RoundInfo;
    })();

    jlmd.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmd
         * @interface ISpinAck
         * @property {Array.<jlmd.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {boolean|null} [IsFullEnd] SpinAck IsFullEnd
         * @property {boolean|null} [Is8MedusaEnd] SpinAck Is8MedusaEnd
         * @property {boolean|null} [IsNew] SpinAck IsNew
         * @property {number|null} [CardType] SpinAck CardType
         * @property {number|null} [MainWin] SpinAck MainWin
         * @property {number|null} [FreeWin] SpinAck FreeWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmd
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmd.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlmd.IRoundInfo>} RoundQueue
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck IsFullEnd.
         * @member {boolean} IsFullEnd
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.IsFullEnd = false;

        /**
         * SpinAck Is8MedusaEnd.
         * @member {boolean} Is8MedusaEnd
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.Is8MedusaEnd = false;

        /**
         * SpinAck IsNew.
         * @member {boolean} IsNew
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.IsNew = false;

        /**
         * SpinAck CardType.
         * @member {number} CardType
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.CardType = 0;

        /**
         * SpinAck MainWin.
         * @member {number} MainWin
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.MainWin = 0;

        /**
         * SpinAck FreeWin.
         * @member {number} FreeWin
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmd.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmd.SpinAck
         * @static
         * @param {jlmd.ISpinAck=} [properties] Properties to set
         * @returns {jlmd.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmd.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmd.SpinAck
         * @static
         * @param {jlmd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlmd.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.IsFullEnd != null && Object.hasOwnProperty.call(message, "IsFullEnd"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.IsFullEnd);
            if (message.Is8MedusaEnd != null && Object.hasOwnProperty.call(message, "Is8MedusaEnd"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.Is8MedusaEnd);
            if (message.IsNew != null && Object.hasOwnProperty.call(message, "IsNew"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.IsNew);
            if (message.CardType != null && Object.hasOwnProperty.call(message, "CardType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.CardType);
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.MainWin);
            if (message.FreeWin != null && Object.hasOwnProperty.call(message, "FreeWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.FreeWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmd.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.SpinAck
         * @static
         * @param {jlmd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlmd.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.IsFullEnd = reader.bool();
                        break;
                    }
                case 3: {
                        message.Is8MedusaEnd = reader.bool();
                        break;
                    }
                case 4: {
                        message.IsNew = reader.bool();
                        break;
                    }
                case 5: {
                        message.CardType = reader.int32();
                        break;
                    }
                case 6: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 7: {
                        message.FreeWin = reader.double();
                        break;
                    }
                case 8: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 9: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 10: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 11: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmd.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlmd.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.IsFullEnd != null && message.hasOwnProperty("IsFullEnd"))
                if (typeof message.IsFullEnd !== "boolean")
                    return "IsFullEnd: boolean expected";
            if (message.Is8MedusaEnd != null && message.hasOwnProperty("Is8MedusaEnd"))
                if (typeof message.Is8MedusaEnd !== "boolean")
                    return "Is8MedusaEnd: boolean expected";
            if (message.IsNew != null && message.hasOwnProperty("IsNew"))
                if (typeof message.IsNew !== "boolean")
                    return "IsNew: boolean expected";
            if (message.CardType != null && message.hasOwnProperty("CardType"))
                if (!$util.isInteger(message.CardType))
                    return "CardType: integer expected";
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.FreeWin != null && message.hasOwnProperty("FreeWin"))
                if (typeof message.FreeWin !== "number")
                    return "FreeWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.SpinAck)
                return object;
            let message = new $root.jlmd.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlmd.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlmd.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlmd.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.IsFullEnd != null)
                message.IsFullEnd = Boolean(object.IsFullEnd);
            if (object.Is8MedusaEnd != null)
                message.Is8MedusaEnd = Boolean(object.Is8MedusaEnd);
            if (object.IsNew != null)
                message.IsNew = Boolean(object.IsNew);
            if (object.CardType != null)
                message.CardType = object.CardType | 0;
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.FreeWin != null)
                message.FreeWin = Number(object.FreeWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.SpinAck
         * @static
         * @param {jlmd.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.IsFullEnd = false;
                object.Is8MedusaEnd = false;
                object.IsNew = false;
                object.CardType = 0;
                object.MainWin = 0;
                object.FreeWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlmd.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.IsFullEnd != null && message.hasOwnProperty("IsFullEnd"))
                object.IsFullEnd = message.IsFullEnd;
            if (message.Is8MedusaEnd != null && message.hasOwnProperty("Is8MedusaEnd"))
                object.Is8MedusaEnd = message.Is8MedusaEnd;
            if (message.IsNew != null && message.hasOwnProperty("IsNew"))
                object.IsNew = message.IsNew;
            if (message.CardType != null && message.hasOwnProperty("CardType"))
                object.CardType = message.CardType;
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.FreeWin != null && message.hasOwnProperty("FreeWin"))
                object.FreeWin = options.json && !isFinite(message.FreeWin) ? String(message.FreeWin) : message.FreeWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmd.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmd.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.SpinAck";
        };

        return SpinAck;
    })();

    jlmd.FreeReq = (function() {

        /**
         * Properties of a FreeReq.
         * @memberof jlmd
         * @interface IFreeReq
         * @property {number|null} [FreeType] FreeReq FreeType
         */

        /**
         * Constructs a new FreeReq.
         * @memberof jlmd
         * @classdesc Represents a FreeReq.
         * @implements IFreeReq
         * @constructor
         * @param {jlmd.IFreeReq=} [properties] Properties to set
         */
        function FreeReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeReq FreeType.
         * @member {number} FreeType
         * @memberof jlmd.FreeReq
         * @instance
         */
        FreeReq.prototype.FreeType = 0;

        /**
         * Creates a new FreeReq instance using the specified properties.
         * @function create
         * @memberof jlmd.FreeReq
         * @static
         * @param {jlmd.IFreeReq=} [properties] Properties to set
         * @returns {jlmd.FreeReq} FreeReq instance
         */
        FreeReq.create = function create(properties) {
            return new FreeReq(properties);
        };

        /**
         * Encodes the specified FreeReq message. Does not implicitly {@link jlmd.FreeReq.verify|verify} messages.
         * @function encode
         * @memberof jlmd.FreeReq
         * @static
         * @param {jlmd.IFreeReq} message FreeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeType != null && Object.hasOwnProperty.call(message, "FreeType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.FreeType);
            return writer;
        };

        /**
         * Encodes the specified FreeReq message, length delimited. Does not implicitly {@link jlmd.FreeReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.FreeReq
         * @static
         * @param {jlmd.IFreeReq} message FreeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.FreeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.FreeReq} FreeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.FreeReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.FreeType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.FreeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.FreeReq} FreeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeReq message.
         * @function verify
         * @memberof jlmd.FreeReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                if (!$util.isInteger(message.FreeType))
                    return "FreeType: integer expected";
            return null;
        };

        /**
         * Creates a FreeReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.FreeReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.FreeReq} FreeReq
         */
        FreeReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.FreeReq)
                return object;
            let message = new $root.jlmd.FreeReq();
            if (object.FreeType != null)
                message.FreeType = object.FreeType | 0;
            return message;
        };

        /**
         * Creates a plain object from a FreeReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.FreeReq
         * @static
         * @param {jlmd.FreeReq} message FreeReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.FreeType = 0;
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                object.FreeType = message.FreeType;
            return object;
        };

        /**
         * Converts this FreeReq to JSON.
         * @function toJSON
         * @memberof jlmd.FreeReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeReq
         * @function getTypeUrl
         * @memberof jlmd.FreeReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.FreeReq";
        };

        return FreeReq;
    })();

    jlmd.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlmd
         * @interface IStateInfo
         * @property {Array.<jlmd.IColumn>|null} [PlateSymbol] StateInfo PlateSymbol
         * @property {Array.<jlmd.IColumnFloat>|null} [PlateSymbolNum] StateInfo PlateSymbolNum
         * @property {number|null} [FreeBet] StateInfo FreeBet
         * @property {number|null} [FreeGameType] StateInfo FreeGameType
         * @property {number|null} [BetIndex] StateInfo BetIndex
         * @property {boolean|null} [NeedchooseFree] StateInfo NeedchooseFree
         * @property {boolean|null} [IsExtra] StateInfo IsExtra
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlmd
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlmd.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo PlateSymbol.
         * @member {Array.<jlmd.IColumn>} PlateSymbol
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * StateInfo PlateSymbolNum.
         * @member {Array.<jlmd.IColumnFloat>} PlateSymbolNum
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * StateInfo FreeBet.
         * @member {number} FreeBet
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.FreeBet = 0;

        /**
         * StateInfo FreeGameType.
         * @member {number} FreeGameType
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.FreeGameType = 0;

        /**
         * StateInfo BetIndex.
         * @member {number} BetIndex
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.BetIndex = 0;

        /**
         * StateInfo NeedchooseFree.
         * @member {boolean} NeedchooseFree
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.NeedchooseFree = false;

        /**
         * StateInfo IsExtra.
         * @member {boolean} IsExtra
         * @memberof jlmd.StateInfo
         * @instance
         */
        StateInfo.prototype.IsExtra = false;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlmd.StateInfo
         * @static
         * @param {jlmd.IStateInfo=} [properties] Properties to set
         * @returns {jlmd.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlmd.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmd.StateInfo
         * @static
         * @param {jlmd.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlmd.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jlmd.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FreeBet != null && Object.hasOwnProperty.call(message, "FreeBet"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeBet);
            if (message.FreeGameType != null && Object.hasOwnProperty.call(message, "FreeGameType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeGameType);
            if (message.BetIndex != null && Object.hasOwnProperty.call(message, "BetIndex"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.BetIndex);
            if (message.NeedchooseFree != null && Object.hasOwnProperty.call(message, "NeedchooseFree"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.NeedchooseFree);
            if (message.IsExtra != null && Object.hasOwnProperty.call(message, "IsExtra"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.IsExtra);
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlmd.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.StateInfo
         * @static
         * @param {jlmd.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlmd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jlmd.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.FreeBet = reader.double();
                        break;
                    }
                case 4: {
                        message.FreeGameType = reader.int32();
                        break;
                    }
                case 5: {
                        message.BetIndex = reader.int32();
                        break;
                    }
                case 6: {
                        message.NeedchooseFree = reader.bool();
                        break;
                    }
                case 7: {
                        message.IsExtra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlmd.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlmd.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jlmd.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.FreeBet != null && message.hasOwnProperty("FreeBet"))
                if (typeof message.FreeBet !== "number")
                    return "FreeBet: number expected";
            if (message.FreeGameType != null && message.hasOwnProperty("FreeGameType"))
                if (!$util.isInteger(message.FreeGameType))
                    return "FreeGameType: integer expected";
            if (message.BetIndex != null && message.hasOwnProperty("BetIndex"))
                if (!$util.isInteger(message.BetIndex))
                    return "BetIndex: integer expected";
            if (message.NeedchooseFree != null && message.hasOwnProperty("NeedchooseFree"))
                if (typeof message.NeedchooseFree !== "boolean")
                    return "NeedchooseFree: boolean expected";
            if (message.IsExtra != null && message.hasOwnProperty("IsExtra"))
                if (typeof message.IsExtra !== "boolean")
                    return "IsExtra: boolean expected";
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.StateInfo)
                return object;
            let message = new $root.jlmd.StateInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmd.StateInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlmd.StateInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlmd.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jlmd.StateInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jlmd.StateInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jlmd.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.FreeBet != null)
                message.FreeBet = Number(object.FreeBet);
            if (object.FreeGameType != null)
                message.FreeGameType = object.FreeGameType | 0;
            if (object.BetIndex != null)
                message.BetIndex = object.BetIndex | 0;
            if (object.NeedchooseFree != null)
                message.NeedchooseFree = Boolean(object.NeedchooseFree);
            if (object.IsExtra != null)
                message.IsExtra = Boolean(object.IsExtra);
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.StateInfo
         * @static
         * @param {jlmd.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
            }
            if (options.defaults) {
                object.FreeBet = 0;
                object.FreeGameType = 0;
                object.BetIndex = 0;
                object.NeedchooseFree = false;
                object.IsExtra = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlmd.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jlmd.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.FreeBet != null && message.hasOwnProperty("FreeBet"))
                object.FreeBet = options.json && !isFinite(message.FreeBet) ? String(message.FreeBet) : message.FreeBet;
            if (message.FreeGameType != null && message.hasOwnProperty("FreeGameType"))
                object.FreeGameType = message.FreeGameType;
            if (message.BetIndex != null && message.hasOwnProperty("BetIndex"))
                object.BetIndex = message.BetIndex;
            if (message.NeedchooseFree != null && message.hasOwnProperty("NeedchooseFree"))
                object.NeedchooseFree = message.NeedchooseFree;
            if (message.IsExtra != null && message.hasOwnProperty("IsExtra"))
                object.IsExtra = message.IsExtra;
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlmd.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlmd.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.StateInfo";
        };

        return StateInfo;
    })();

    jlmd.RoundRecord = (function() {

        /**
         * Properties of a RoundRecord.
         * @memberof jlmd
         * @interface IRoundRecord
         * @property {number|Long|null} [RoundIndex] RoundRecord RoundIndex
         */

        /**
         * Constructs a new RoundRecord.
         * @memberof jlmd
         * @classdesc Represents a RoundRecord.
         * @implements IRoundRecord
         * @constructor
         * @param {jlmd.IRoundRecord=} [properties] Properties to set
         */
        function RoundRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundRecord RoundIndex.
         * @member {number|Long} RoundIndex
         * @memberof jlmd.RoundRecord
         * @instance
         */
        RoundRecord.prototype.RoundIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RoundRecord instance using the specified properties.
         * @function create
         * @memberof jlmd.RoundRecord
         * @static
         * @param {jlmd.IRoundRecord=} [properties] Properties to set
         * @returns {jlmd.RoundRecord} RoundRecord instance
         */
        RoundRecord.create = function create(properties) {
            return new RoundRecord(properties);
        };

        /**
         * Encodes the specified RoundRecord message. Does not implicitly {@link jlmd.RoundRecord.verify|verify} messages.
         * @function encode
         * @memberof jlmd.RoundRecord
         * @static
         * @param {jlmd.IRoundRecord} message RoundRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundIndex != null && Object.hasOwnProperty.call(message, "RoundIndex"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoundIndex);
            return writer;
        };

        /**
         * Encodes the specified RoundRecord message, length delimited. Does not implicitly {@link jlmd.RoundRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.RoundRecord
         * @static
         * @param {jlmd.IRoundRecord} message RoundRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundRecord message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.RoundRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.RoundRecord} RoundRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.RoundRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoundIndex = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.RoundRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.RoundRecord} RoundRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundRecord message.
         * @function verify
         * @memberof jlmd.RoundRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundIndex != null && message.hasOwnProperty("RoundIndex"))
                if (!$util.isInteger(message.RoundIndex) && !(message.RoundIndex && $util.isInteger(message.RoundIndex.low) && $util.isInteger(message.RoundIndex.high)))
                    return "RoundIndex: integer|Long expected";
            return null;
        };

        /**
         * Creates a RoundRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.RoundRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.RoundRecord} RoundRecord
         */
        RoundRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.RoundRecord)
                return object;
            let message = new $root.jlmd.RoundRecord();
            if (object.RoundIndex != null)
                if ($util.Long)
                    (message.RoundIndex = $util.Long.fromValue(object.RoundIndex)).unsigned = false;
                else if (typeof object.RoundIndex === "string")
                    message.RoundIndex = parseInt(object.RoundIndex, 10);
                else if (typeof object.RoundIndex === "number")
                    message.RoundIndex = object.RoundIndex;
                else if (typeof object.RoundIndex === "object")
                    message.RoundIndex = new $util.LongBits(object.RoundIndex.low >>> 0, object.RoundIndex.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RoundRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.RoundRecord
         * @static
         * @param {jlmd.RoundRecord} message RoundRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.RoundIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.RoundIndex = options.longs === String ? "0" : 0;
            if (message.RoundIndex != null && message.hasOwnProperty("RoundIndex"))
                if (typeof message.RoundIndex === "number")
                    object.RoundIndex = options.longs === String ? String(message.RoundIndex) : message.RoundIndex;
                else
                    object.RoundIndex = options.longs === String ? $util.Long.prototype.toString.call(message.RoundIndex) : options.longs === Number ? new $util.LongBits(message.RoundIndex.low >>> 0, message.RoundIndex.high >>> 0).toNumber() : message.RoundIndex;
            return object;
        };

        /**
         * Converts this RoundRecord to JSON.
         * @function toJSON
         * @memberof jlmd.RoundRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundRecord
         * @function getTypeUrl
         * @memberof jlmd.RoundRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.RoundRecord";
        };

        return RoundRecord;
    })();

    jlmd.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlmd
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlmd
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlmd.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlmd.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlmd.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlmd.GameInfoData
         * @static
         * @param {jlmd.IGameInfoData=} [properties] Properties to set
         * @returns {jlmd.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlmd.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmd.GameInfoData
         * @static
         * @param {jlmd.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlmd.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmd.GameInfoData
         * @static
         * @param {jlmd.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmd.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmd.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmd.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmd.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmd.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlmd.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmd.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmd.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmd.GameInfoData)
                return object;
            let message = new $root.jlmd.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmd.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmd.GameInfoData
         * @static
         * @param {jlmd.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlmd.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlmd.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmd.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlmd;
})();

export const jlme = $root.jlme = (() => {

    /**
     * Namespace jlme.
     * @exports jlme
     * @namespace
     */
    const jlme = {};

    jlme.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlme
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlme
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlme.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlme.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlme.Column
         * @static
         * @param {jlme.IColumn=} [properties] Properties to set
         * @returns {jlme.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlme.Column.verify|verify} messages.
         * @function encode
         * @memberof jlme.Column
         * @static
         * @param {jlme.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlme.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlme.Column
         * @static
         * @param {jlme.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlme.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlme.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlme.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlme.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlme.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlme.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlme.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlme.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlme.Column)
                return object;
            let message = new $root.jlme.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlme.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlme.Column
         * @static
         * @param {jlme.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlme.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlme.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlme.Column";
        };

        return Column;
    })();

    jlme.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlme
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlme
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlme.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlme.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlme.ColumnFloat
         * @static
         * @param {jlme.IColumnFloat=} [properties] Properties to set
         * @returns {jlme.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlme.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlme.ColumnFloat
         * @static
         * @param {jlme.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlme.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlme.ColumnFloat
         * @static
         * @param {jlme.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlme.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlme.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlme.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlme.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlme.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlme.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlme.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlme.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlme.ColumnFloat)
                return object;
            let message = new $root.jlme.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlme.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlme.ColumnFloat
         * @static
         * @param {jlme.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlme.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlme.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlme.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlme.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlme
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlme
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlme.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlme.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlme.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlme.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlme.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlme.AwardData
         * @static
         * @param {jlme.IAwardData=} [properties] Properties to set
         * @returns {jlme.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlme.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlme.AwardData
         * @static
         * @param {jlme.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlme.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlme.AwardData
         * @static
         * @param {jlme.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlme.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlme.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlme.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlme.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlme.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlme.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlme.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlme.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlme.AwardData)
                return object;
            let message = new $root.jlme.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlme.AwardData
         * @static
         * @param {jlme.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlme.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlme.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlme.AwardData";
        };

        return AwardData;
    })();

    jlme.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlme
         * @interface IRoundInfo
         * @property {Array.<jlme.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlme.IColumnFloat>|null} [PlateSymbolNum] RoundInfo PlateSymbolNum
         * @property {Array.<jlme.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [BonusRemainRound] RoundInfo BonusRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [CountRound] RoundInfo CountRound
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlme
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlme.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlme.IColumn>} PlateSymbol
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNum.
         * @member {Array.<jlme.IColumnFloat>} PlateSymbolNum
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlme.IAwardData>} AwardDataVec
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo BonusRemainRound.
         * @member {number} BonusRemainRound
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo CountRound.
         * @member {number} CountRound
         * @memberof jlme.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CountRound = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlme.RoundInfo
         * @static
         * @param {jlme.IRoundInfo=} [properties] Properties to set
         * @returns {jlme.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlme.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlme.RoundInfo
         * @static
         * @param {jlme.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlme.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jlme.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlme.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeTotalRound);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeRemainRound);
            if (message.BonusRemainRound != null && Object.hasOwnProperty.call(message, "BonusRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.BonusRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            if (message.CountRound != null && Object.hasOwnProperty.call(message, "CountRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.CountRound);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlme.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlme.RoundInfo
         * @static
         * @param {jlme.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlme.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlme.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlme.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlme.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jlme.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlme.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.BonusRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 10: {
                        message.CountRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlme.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlme.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlme.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlme.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jlme.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlme.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                if (!$util.isInteger(message.BonusRemainRound))
                    return "BonusRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.CountRound != null && message.hasOwnProperty("CountRound"))
                if (!$util.isInteger(message.CountRound))
                    return "CountRound: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlme.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlme.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlme.RoundInfo)
                return object;
            let message = new $root.jlme.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlme.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlme.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlme.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jlme.RoundInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jlme.RoundInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jlme.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlme.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlme.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlme.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.BonusRemainRound != null)
                message.BonusRemainRound = object.BonusRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.CountRound != null)
                message.CountRound = object.CountRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlme.RoundInfo
         * @static
         * @param {jlme.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeTotalRound = 0;
                object.FreeRemainRound = 0;
                object.BonusRemainRound = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
                object.CountRound = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlme.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jlme.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlme.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.BonusRemainRound != null && message.hasOwnProperty("BonusRemainRound"))
                object.BonusRemainRound = message.BonusRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.CountRound != null && message.hasOwnProperty("CountRound"))
                object.CountRound = message.CountRound;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlme.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlme.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlme.RoundInfo";
        };

        return RoundInfo;
    })();

    jlme.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlme
         * @interface ISpinAck
         * @property {Array.<jlme.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {Array.<number>|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FreeRound] SpinAck FreeRound
         * @property {number|null} [LogBonus] SpinAck LogBonus
         * @property {Array.<number>|null} [BonusRounds] SpinAck BonusRounds
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlme
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlme.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.BonusTotalWin = [];
            this.BonusRounds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlme.IRoundInfo>} RoundQueue
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck BonusTotalWin.
         * @member {Array.<number>} BonusTotalWin
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = $util.emptyArray;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FreeRound.
         * @member {number} FreeRound
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeRound = 0;

        /**
         * SpinAck LogBonus.
         * @member {number} LogBonus
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.LogBonus = 0;

        /**
         * SpinAck BonusRounds.
         * @member {Array.<number>} BonusRounds
         * @memberof jlme.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusRounds = $util.emptyArray;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlme.SpinAck
         * @static
         * @param {jlme.ISpinAck=} [properties] Properties to set
         * @returns {jlme.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlme.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlme.SpinAck
         * @static
         * @param {jlme.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlme.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.BonusTotalWin != null && message.BonusTotalWin.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.BonusTotalWin.length; ++i)
                    writer.double(message.BonusTotalWin[i]);
                writer.ldelim();
            }
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Extra);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.Bet);
            if (message.FreeRound != null && Object.hasOwnProperty.call(message, "FreeRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.FreeRound);
            if (message.LogBonus != null && Object.hasOwnProperty.call(message, "LogBonus"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.LogBonus);
            if (message.BonusRounds != null && message.BonusRounds.length) {
                writer.uint32(/* id 13, wireType 2 =*/106).fork();
                for (let i = 0; i < message.BonusRounds.length; ++i)
                    writer.int32(message.BonusRounds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlme.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlme.SpinAck
         * @static
         * @param {jlme.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlme.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlme.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlme.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlme.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.BonusTotalWin && message.BonusTotalWin.length))
                            message.BonusTotalWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BonusTotalWin.push(reader.double());
                        } else
                            message.BonusTotalWin.push(reader.double());
                        break;
                    }
                case 4: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                case 10: {
                        message.Bet = reader.double();
                        break;
                    }
                case 11: {
                        message.FreeRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.LogBonus = reader.int32();
                        break;
                    }
                case 13: {
                        if (!(message.BonusRounds && message.BonusRounds.length))
                            message.BonusRounds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BonusRounds.push(reader.int32());
                        } else
                            message.BonusRounds.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlme.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlme.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlme.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlme.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin")) {
                if (!Array.isArray(message.BonusTotalWin))
                    return "BonusTotalWin: array expected";
                for (let i = 0; i < message.BonusTotalWin.length; ++i)
                    if (typeof message.BonusTotalWin[i] !== "number")
                        return "BonusTotalWin: number[] expected";
            }
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FreeRound != null && message.hasOwnProperty("FreeRound"))
                if (!$util.isInteger(message.FreeRound))
                    return "FreeRound: integer expected";
            if (message.LogBonus != null && message.hasOwnProperty("LogBonus"))
                if (!$util.isInteger(message.LogBonus))
                    return "LogBonus: integer expected";
            if (message.BonusRounds != null && message.hasOwnProperty("BonusRounds")) {
                if (!Array.isArray(message.BonusRounds))
                    return "BonusRounds: array expected";
                for (let i = 0; i < message.BonusRounds.length; ++i)
                    if (!$util.isInteger(message.BonusRounds[i]))
                        return "BonusRounds: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlme.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlme.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlme.SpinAck)
                return object;
            let message = new $root.jlme.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlme.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlme.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlme.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.BonusTotalWin) {
                if (!Array.isArray(object.BonusTotalWin))
                    throw TypeError(".jlme.SpinAck.BonusTotalWin: array expected");
                message.BonusTotalWin = [];
                for (let i = 0; i < object.BonusTotalWin.length; ++i)
                    message.BonusTotalWin[i] = Number(object.BonusTotalWin[i]);
            }
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FreeRound != null)
                message.FreeRound = object.FreeRound | 0;
            if (object.LogBonus != null)
                message.LogBonus = object.LogBonus | 0;
            if (object.BonusRounds) {
                if (!Array.isArray(object.BonusRounds))
                    throw TypeError(".jlme.SpinAck.BonusRounds: array expected");
                message.BonusRounds = [];
                for (let i = 0; i < object.BonusRounds.length; ++i)
                    message.BonusRounds[i] = object.BonusRounds[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlme.SpinAck
         * @static
         * @param {jlme.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.BonusTotalWin = [];
                object.BonusRounds = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.Extra = false;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
                object.FreeRound = 0;
                object.LogBonus = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlme.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.BonusTotalWin && message.BonusTotalWin.length) {
                object.BonusTotalWin = [];
                for (let j = 0; j < message.BonusTotalWin.length; ++j)
                    object.BonusTotalWin[j] = options.json && !isFinite(message.BonusTotalWin[j]) ? String(message.BonusTotalWin[j]) : message.BonusTotalWin[j];
            }
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FreeRound != null && message.hasOwnProperty("FreeRound"))
                object.FreeRound = message.FreeRound;
            if (message.LogBonus != null && message.hasOwnProperty("LogBonus"))
                object.LogBonus = message.LogBonus;
            if (message.BonusRounds && message.BonusRounds.length) {
                object.BonusRounds = [];
                for (let j = 0; j < message.BonusRounds.length; ++j)
                    object.BonusRounds[j] = message.BonusRounds[j];
            }
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlme.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlme.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlme.SpinAck";
        };

        return SpinAck;
    })();

    jlme.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlme
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlme
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlme.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlme.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlme.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlme.GameInfoData
         * @static
         * @param {jlme.IGameInfoData=} [properties] Properties to set
         * @returns {jlme.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlme.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlme.GameInfoData
         * @static
         * @param {jlme.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlme.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlme.GameInfoData
         * @static
         * @param {jlme.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlme.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlme.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlme.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlme.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlme.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlme.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlme.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlme.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlme.GameInfoData)
                return object;
            let message = new $root.jlme.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlme.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlme.GameInfoData
         * @static
         * @param {jlme.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlme.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlme.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlme.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlme;
})();

export const jlml = $root.jlml = (() => {

    /**
     * Namespace jlml.
     * @exports jlml
     * @namespace
     */
    const jlml = {};

    jlml.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlml
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlml
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlml.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlml.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlml.Column
         * @static
         * @param {jlml.IColumn=} [properties] Properties to set
         * @returns {jlml.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlml.Column.verify|verify} messages.
         * @function encode
         * @memberof jlml.Column
         * @static
         * @param {jlml.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlml.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlml.Column
         * @static
         * @param {jlml.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlml.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlml.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlml.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlml.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlml.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlml.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlml.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlml.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlml.Column)
                return object;
            let message = new $root.jlml.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlml.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlml.Column
         * @static
         * @param {jlml.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlml.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlml.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlml.Column";
        };

        return Column;
    })();

    jlml.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlml
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlml
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlml.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlml.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlml.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlml.Grid
         * @static
         * @param {jlml.IGrid=} [properties] Properties to set
         * @returns {jlml.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlml.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlml.Grid
         * @static
         * @param {jlml.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlml.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlml.Grid
         * @static
         * @param {jlml.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlml.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlml.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlml.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlml.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlml.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlml.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlml.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlml.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlml.Grid)
                return object;
            let message = new $root.jlml.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlml.Grid
         * @static
         * @param {jlml.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlml.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlml.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlml.Grid";
        };

        return Grid;
    })();

    jlml.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlml
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlml.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlml
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlml.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlml.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlml.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlml.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlml.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlml.IGrid>} GridVec
         * @memberof jlml.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlml.AwardData
         * @static
         * @param {jlml.IAwardData=} [properties] Properties to set
         * @returns {jlml.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlml.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlml.AwardData
         * @static
         * @param {jlml.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlml.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlml.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlml.AwardData
         * @static
         * @param {jlml.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlml.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlml.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlml.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlml.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlml.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlml.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlml.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlml.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlml.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlml.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlml.AwardData)
                return object;
            let message = new $root.jlml.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlml.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlml.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlml.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlml.AwardData
         * @static
         * @param {jlml.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlml.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlml.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlml.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlml.AwardData";
        };

        return AwardData;
    })();

    jlml.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlml
         * @interface IRoundInfo
         * @property {Array.<jlml.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlml.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [WaysWin] RoundInfo WaysWin
         * @property {number|null} [LampWin] RoundInfo LampWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [MysteryTarget] RoundInfo MysteryTarget
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlml
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlml.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlml.IColumn>} PlateSymbol
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlml.IAwardData>} AwardDataVec
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo WaysWin.
         * @member {number} WaysWin
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.WaysWin = 0;

        /**
         * RoundInfo LampWin.
         * @member {number} LampWin
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.LampWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo MysteryTarget.
         * @member {number} MysteryTarget
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MysteryTarget = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlml.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlml.RoundInfo
         * @static
         * @param {jlml.IRoundInfo=} [properties] Properties to set
         * @returns {jlml.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlml.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlml.RoundInfo
         * @static
         * @param {jlml.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlml.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlml.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundWin);
            if (message.WaysWin != null && Object.hasOwnProperty.call(message, "WaysWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.WaysWin);
            if (message.LampWin != null && Object.hasOwnProperty.call(message, "LampWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.LampWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeRemainRound);
            if (message.MysteryTarget != null && Object.hasOwnProperty.call(message, "MysteryTarget"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.MysteryTarget);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlml.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlml.RoundInfo
         * @static
         * @param {jlml.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlml.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlml.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlml.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlml.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlml.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 4: {
                        message.WaysWin = reader.double();
                        break;
                    }
                case 5: {
                        message.LampWin = reader.double();
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.MysteryTarget = reader.int32();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 10: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlml.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlml.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlml.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlml.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlml.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.WaysWin != null && message.hasOwnProperty("WaysWin"))
                if (typeof message.WaysWin !== "number")
                    return "WaysWin: number expected";
            if (message.LampWin != null && message.hasOwnProperty("LampWin"))
                if (typeof message.LampWin !== "number")
                    return "LampWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.MysteryTarget != null && message.hasOwnProperty("MysteryTarget"))
                if (!$util.isInteger(message.MysteryTarget))
                    return "MysteryTarget: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlml.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlml.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlml.RoundInfo)
                return object;
            let message = new $root.jlml.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlml.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlml.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlml.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlml.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlml.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlml.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.WaysWin != null)
                message.WaysWin = Number(object.WaysWin);
            if (object.LampWin != null)
                message.LampWin = Number(object.LampWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.MysteryTarget != null)
                message.MysteryTarget = object.MysteryTarget | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlml.RoundInfo
         * @static
         * @param {jlml.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.WaysWin = 0;
                object.LampWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.MysteryTarget = 0;
                object.MaxFlag = false;
                object.ScatterCount = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlml.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlml.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.WaysWin != null && message.hasOwnProperty("WaysWin"))
                object.WaysWin = options.json && !isFinite(message.WaysWin) ? String(message.WaysWin) : message.WaysWin;
            if (message.LampWin != null && message.hasOwnProperty("LampWin"))
                object.LampWin = options.json && !isFinite(message.LampWin) ? String(message.LampWin) : message.LampWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.MysteryTarget != null && message.hasOwnProperty("MysteryTarget"))
                object.MysteryTarget = message.MysteryTarget;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlml.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlml.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlml.RoundInfo";
        };

        return RoundInfo;
    })();

    jlml.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlml
         * @interface ISpinAck
         * @property {Array.<jlml.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlml
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlml.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlml.IRoundInfo>} RoundQueue
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlml.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlml.SpinAck
         * @static
         * @param {jlml.ISpinAck=} [properties] Properties to set
         * @returns {jlml.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlml.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlml.SpinAck
         * @static
         * @param {jlml.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlml.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlml.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlml.SpinAck
         * @static
         * @param {jlml.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlml.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlml.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlml.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlml.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlml.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlml.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlml.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlml.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlml.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlml.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlml.SpinAck)
                return object;
            let message = new $root.jlml.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlml.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlml.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlml.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlml.SpinAck
         * @static
         * @param {jlml.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlml.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlml.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlml.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlml.SpinAck";
        };

        return SpinAck;
    })();

    jlml.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlml
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlml
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlml.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlml.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlml.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlml.GameInfoData
         * @static
         * @param {jlml.IGameInfoData=} [properties] Properties to set
         * @returns {jlml.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlml.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlml.GameInfoData
         * @static
         * @param {jlml.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlml.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlml.GameInfoData
         * @static
         * @param {jlml.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlml.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlml.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlml.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlml.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlml.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlml.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlml.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlml.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlml.GameInfoData)
                return object;
            let message = new $root.jlml.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlml.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlml.GameInfoData
         * @static
         * @param {jlml.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlml.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlml.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlml.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlml;
})();

export const jlmp = $root.jlmp = (() => {

    /**
     * Namespace jlmp.
     * @exports jlmp
     * @namespace
     */
    const jlmp = {};

    jlmp.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlmp
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlmp
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlmp.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlmp.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlmp.Column
         * @static
         * @param {jlmp.IColumn=} [properties] Properties to set
         * @returns {jlmp.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlmp.Column.verify|verify} messages.
         * @function encode
         * @memberof jlmp.Column
         * @static
         * @param {jlmp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlmp.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.Column
         * @static
         * @param {jlmp.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlmp.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.Column)
                return object;
            let message = new $root.jlmp.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlmp.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.Column
         * @static
         * @param {jlmp.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlmp.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlmp.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.Column";
        };

        return Column;
    })();

    jlmp.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlmp
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [AwardLine] AwardData AwardLine
         * @property {number|null} [AwardMoney] AwardData AwardMoney
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlmp
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlmp.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlmp.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData AwardLine.
         * @member {number} AwardLine
         * @memberof jlmp.AwardData
         * @instance
         */
        AwardData.prototype.AwardLine = 0;

        /**
         * AwardData AwardMoney.
         * @member {number} AwardMoney
         * @memberof jlmp.AwardData
         * @instance
         */
        AwardData.prototype.AwardMoney = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlmp.AwardData
         * @static
         * @param {jlmp.IAwardData=} [properties] Properties to set
         * @returns {jlmp.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlmp.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlmp.AwardData
         * @static
         * @param {jlmp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.AwardLine != null && Object.hasOwnProperty.call(message, "AwardLine"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardLine);
            if (message.AwardMoney != null && Object.hasOwnProperty.call(message, "AwardMoney"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.AwardMoney);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlmp.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.AwardData
         * @static
         * @param {jlmp.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.AwardLine = reader.int32();
                        break;
                    }
                case 3: {
                        message.AwardMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlmp.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                if (!$util.isInteger(message.AwardLine))
                    return "AwardLine: integer expected";
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                if (typeof message.AwardMoney !== "number")
                    return "AwardMoney: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.AwardData)
                return object;
            let message = new $root.jlmp.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.AwardLine != null)
                message.AwardLine = object.AwardLine | 0;
            if (object.AwardMoney != null)
                message.AwardMoney = Number(object.AwardMoney);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.AwardData
         * @static
         * @param {jlmp.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.AwardLine = 0;
                object.AwardMoney = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                object.AwardLine = message.AwardLine;
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                object.AwardMoney = options.json && !isFinite(message.AwardMoney) ? String(message.AwardMoney) : message.AwardMoney;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlmp.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlmp.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.AwardData";
        };

        return AwardData;
    })();

    jlmp.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlmp
         * @interface ISinglePlate
         * @property {number|null} [AwardTypeFlag] SinglePlate AwardTypeFlag
         * @property {Array.<jlmp.IAwardData>|null} [AwardDataList] SinglePlate AwardDataList
         * @property {Array.<jlmp.IColumn>|null} [PlateSymbol] SinglePlate PlateSymbol
         * @property {number|null} [PlateWinMoney] SinglePlate PlateWinMoney
         * @property {Array.<number>|null} [TripleWuKong] SinglePlate TripleWuKong
         * @property {Array.<number>|null} [TripleWuKongMUL] SinglePlate TripleWuKongMUL
         * @property {number|null} [TripleWuKongFaker] SinglePlate TripleWuKongFaker
         * @property {number|null} [AllPlate] SinglePlate AllPlate
         * @property {number|null} [PZWN] SinglePlate PZWN
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlmp
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlmp.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.AwardDataList = [];
            this.PlateSymbol = [];
            this.TripleWuKong = [];
            this.TripleWuKongMUL = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardTypeFlag = 0;

        /**
         * SinglePlate AwardDataList.
         * @member {Array.<jlmp.IAwardData>} AwardDataList
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataList = $util.emptyArray;

        /**
         * SinglePlate PlateSymbol.
         * @member {Array.<jlmp.IColumn>} PlateSymbol
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SinglePlate PlateWinMoney.
         * @member {number} PlateWinMoney
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PlateWinMoney = 0;

        /**
         * SinglePlate TripleWuKong.
         * @member {Array.<number>} TripleWuKong
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.TripleWuKong = $util.emptyArray;

        /**
         * SinglePlate TripleWuKongMUL.
         * @member {Array.<number>} TripleWuKongMUL
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.TripleWuKongMUL = $util.emptyArray;

        /**
         * SinglePlate TripleWuKongFaker.
         * @member {number} TripleWuKongFaker
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.TripleWuKongFaker = 0;

        /**
         * SinglePlate AllPlate.
         * @member {number} AllPlate
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AllPlate = 0;

        /**
         * SinglePlate PZWN.
         * @member {number} PZWN
         * @memberof jlmp.SinglePlate
         * @instance
         */
        SinglePlate.prototype.PZWN = 0;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlmp.SinglePlate
         * @static
         * @param {jlmp.ISinglePlate=} [properties] Properties to set
         * @returns {jlmp.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlmp.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlmp.SinglePlate
         * @static
         * @param {jlmp.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardTypeFlag);
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlmp.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlmp.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.PlateWinMoney != null && Object.hasOwnProperty.call(message, "PlateWinMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.PlateWinMoney);
            if (message.TripleWuKong != null && message.TripleWuKong.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.TripleWuKong.length; ++i)
                    writer.int32(message.TripleWuKong[i]);
                writer.ldelim();
            }
            if (message.TripleWuKongMUL != null && message.TripleWuKongMUL.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.TripleWuKongMUL.length; ++i)
                    writer.double(message.TripleWuKongMUL[i]);
                writer.ldelim();
            }
            if (message.TripleWuKongFaker != null && Object.hasOwnProperty.call(message, "TripleWuKongFaker"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.TripleWuKongFaker);
            if (message.AllPlate != null && Object.hasOwnProperty.call(message, "AllPlate"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AllPlate);
            if (message.PZWN != null && Object.hasOwnProperty.call(message, "PZWN"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.PZWN);
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlmp.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.SinglePlate
         * @static
         * @param {jlmp.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlmp.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlmp.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.PlateWinMoney = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.TripleWuKong && message.TripleWuKong.length))
                            message.TripleWuKong = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.TripleWuKong.push(reader.int32());
                        } else
                            message.TripleWuKong.push(reader.int32());
                        break;
                    }
                case 6: {
                        if (!(message.TripleWuKongMUL && message.TripleWuKongMUL.length))
                            message.TripleWuKongMUL = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.TripleWuKongMUL.push(reader.double());
                        } else
                            message.TripleWuKongMUL.push(reader.double());
                        break;
                    }
                case 7: {
                        message.TripleWuKongFaker = reader.double();
                        break;
                    }
                case 8: {
                        message.AllPlate = reader.int32();
                        break;
                    }
                case 9: {
                        message.PZWN = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlmp.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlmp.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlmp.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateWinMoney != null && message.hasOwnProperty("PlateWinMoney"))
                if (typeof message.PlateWinMoney !== "number")
                    return "PlateWinMoney: number expected";
            if (message.TripleWuKong != null && message.hasOwnProperty("TripleWuKong")) {
                if (!Array.isArray(message.TripleWuKong))
                    return "TripleWuKong: array expected";
                for (let i = 0; i < message.TripleWuKong.length; ++i)
                    if (!$util.isInteger(message.TripleWuKong[i]))
                        return "TripleWuKong: integer[] expected";
            }
            if (message.TripleWuKongMUL != null && message.hasOwnProperty("TripleWuKongMUL")) {
                if (!Array.isArray(message.TripleWuKongMUL))
                    return "TripleWuKongMUL: array expected";
                for (let i = 0; i < message.TripleWuKongMUL.length; ++i)
                    if (typeof message.TripleWuKongMUL[i] !== "number")
                        return "TripleWuKongMUL: number[] expected";
            }
            if (message.TripleWuKongFaker != null && message.hasOwnProperty("TripleWuKongFaker"))
                if (typeof message.TripleWuKongFaker !== "number")
                    return "TripleWuKongFaker: number expected";
            if (message.AllPlate != null && message.hasOwnProperty("AllPlate"))
                if (!$util.isInteger(message.AllPlate))
                    return "AllPlate: integer expected";
            if (message.PZWN != null && message.hasOwnProperty("PZWN"))
                if (!$util.isInteger(message.PZWN))
                    return "PZWN: integer expected";
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.SinglePlate)
                return object;
            let message = new $root.jlmp.SinglePlate();
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlmp.SinglePlate.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlmp.SinglePlate.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlmp.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmp.SinglePlate.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlmp.SinglePlate.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlmp.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateWinMoney != null)
                message.PlateWinMoney = Number(object.PlateWinMoney);
            if (object.TripleWuKong) {
                if (!Array.isArray(object.TripleWuKong))
                    throw TypeError(".jlmp.SinglePlate.TripleWuKong: array expected");
                message.TripleWuKong = [];
                for (let i = 0; i < object.TripleWuKong.length; ++i)
                    message.TripleWuKong[i] = object.TripleWuKong[i] | 0;
            }
            if (object.TripleWuKongMUL) {
                if (!Array.isArray(object.TripleWuKongMUL))
                    throw TypeError(".jlmp.SinglePlate.TripleWuKongMUL: array expected");
                message.TripleWuKongMUL = [];
                for (let i = 0; i < object.TripleWuKongMUL.length; ++i)
                    message.TripleWuKongMUL[i] = Number(object.TripleWuKongMUL[i]);
            }
            if (object.TripleWuKongFaker != null)
                message.TripleWuKongFaker = Number(object.TripleWuKongFaker);
            if (object.AllPlate != null)
                message.AllPlate = object.AllPlate | 0;
            if (object.PZWN != null)
                message.PZWN = object.PZWN | 0;
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.SinglePlate
         * @static
         * @param {jlmp.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.AwardDataList = [];
                object.PlateSymbol = [];
                object.TripleWuKong = [];
                object.TripleWuKongMUL = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.PlateWinMoney = 0;
                object.TripleWuKongFaker = 0;
                object.AllPlate = 0;
                object.PZWN = 0;
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlmp.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlmp.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateWinMoney != null && message.hasOwnProperty("PlateWinMoney"))
                object.PlateWinMoney = options.json && !isFinite(message.PlateWinMoney) ? String(message.PlateWinMoney) : message.PlateWinMoney;
            if (message.TripleWuKong && message.TripleWuKong.length) {
                object.TripleWuKong = [];
                for (let j = 0; j < message.TripleWuKong.length; ++j)
                    object.TripleWuKong[j] = message.TripleWuKong[j];
            }
            if (message.TripleWuKongMUL && message.TripleWuKongMUL.length) {
                object.TripleWuKongMUL = [];
                for (let j = 0; j < message.TripleWuKongMUL.length; ++j)
                    object.TripleWuKongMUL[j] = options.json && !isFinite(message.TripleWuKongMUL[j]) ? String(message.TripleWuKongMUL[j]) : message.TripleWuKongMUL[j];
            }
            if (message.TripleWuKongFaker != null && message.hasOwnProperty("TripleWuKongFaker"))
                object.TripleWuKongFaker = options.json && !isFinite(message.TripleWuKongFaker) ? String(message.TripleWuKongFaker) : message.TripleWuKongFaker;
            if (message.AllPlate != null && message.hasOwnProperty("AllPlate"))
                object.AllPlate = message.AllPlate;
            if (message.PZWN != null && message.hasOwnProperty("PZWN"))
                object.PZWN = message.PZWN;
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlmp.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlmp.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.SinglePlate";
        };

        return SinglePlate;
    })();

    jlmp.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmp
         * @interface ISpinAck
         * @property {Array.<jlmp.ISinglePlate>|null} [PlateInfo] SpinAck PlateInfo
         * @property {number|null} [Count] SpinAck Count
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmp
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmp.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateInfo.
         * @member {Array.<jlmp.ISinglePlate>} PlateInfo
         * @memberof jlmp.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateInfo = $util.emptyArray;

        /**
         * SpinAck Count.
         * @member {number} Count
         * @memberof jlmp.SpinAck
         * @instance
         */
        SpinAck.prototype.Count = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmp.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlmp.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlmp.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmp.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmp.SpinAck
         * @static
         * @param {jlmp.ISpinAck=} [properties] Properties to set
         * @returns {jlmp.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmp.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.SpinAck
         * @static
         * @param {jlmp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateInfo != null && message.PlateInfo.length)
                for (let i = 0; i < message.PlateInfo.length; ++i)
                    $root.jlmp.SinglePlate.encode(message.PlateInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmp.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.SpinAck
         * @static
         * @param {jlmp.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateInfo && message.PlateInfo.length))
                            message.PlateInfo = [];
                        message.PlateInfo.push($root.jlmp.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmp.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo")) {
                if (!Array.isArray(message.PlateInfo))
                    return "PlateInfo: array expected";
                for (let i = 0; i < message.PlateInfo.length; ++i) {
                    let error = $root.jlmp.SinglePlate.verify(message.PlateInfo[i]);
                    if (error)
                        return "PlateInfo." + error;
                }
            }
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.SpinAck)
                return object;
            let message = new $root.jlmp.SpinAck();
            if (object.PlateInfo) {
                if (!Array.isArray(object.PlateInfo))
                    throw TypeError(".jlmp.SpinAck.PlateInfo: array expected");
                message.PlateInfo = [];
                for (let i = 0; i < object.PlateInfo.length; ++i) {
                    if (typeof object.PlateInfo[i] !== "object")
                        throw TypeError(".jlmp.SpinAck.PlateInfo: object expected");
                    message.PlateInfo[i] = $root.jlmp.SinglePlate.fromObject(object.PlateInfo[i]);
                }
            }
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.SpinAck
         * @static
         * @param {jlmp.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateInfo = [];
            if (options.defaults) {
                object.Count = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
            }
            if (message.PlateInfo && message.PlateInfo.length) {
                object.PlateInfo = [];
                for (let j = 0; j < message.PlateInfo.length; ++j)
                    object.PlateInfo[j] = $root.jlmp.SinglePlate.toObject(message.PlateInfo[j], options);
            }
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmp.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmp.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.SpinAck";
        };

        return SpinAck;
    })();

    jlmp.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlmp
         * @interface IGameInfoAck
         * @property {Array.<jlmp.IWalletInfo>|null} [WalletInfo] GameInfoAck WalletInfo
         * @property {number|null} [ArcadeNo] GameInfoAck ArcadeNo
         * @property {boolean|null} [CanIntoArcade] GameInfoAck CanIntoArcade
         * @property {boolean|null} [Test] GameInfoAck Test
         * @property {jlmp.IPreferRoundShow|null} [Prefer] GameInfoAck Prefer
         * @property {number|null} [FreeSpinRemain] GameInfoAck FreeSpinRemain
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlmp
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlmp.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.WalletInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck WalletInfo.
         * @member {Array.<jlmp.IWalletInfo>} WalletInfo
         * @memberof jlmp.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.WalletInfo = $util.emptyArray;

        /**
         * GameInfoAck ArcadeNo.
         * @member {number} ArcadeNo
         * @memberof jlmp.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.ArcadeNo = 0;

        /**
         * GameInfoAck CanIntoArcade.
         * @member {boolean} CanIntoArcade
         * @memberof jlmp.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.CanIntoArcade = false;

        /**
         * GameInfoAck Test.
         * @member {boolean} Test
         * @memberof jlmp.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Test = false;

        /**
         * GameInfoAck Prefer.
         * @member {jlmp.IPreferRoundShow|null|undefined} Prefer
         * @memberof jlmp.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Prefer = null;

        /**
         * GameInfoAck FreeSpinRemain.
         * @member {number} FreeSpinRemain
         * @memberof jlmp.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.FreeSpinRemain = 0;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {jlmp.IGameInfoAck=} [properties] Properties to set
         * @returns {jlmp.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlmp.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {jlmp.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WalletInfo != null && message.WalletInfo.length)
                for (let i = 0; i < message.WalletInfo.length; ++i)
                    $root.jlmp.WalletInfo.encode(message.WalletInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ArcadeNo != null && Object.hasOwnProperty.call(message, "ArcadeNo"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ArcadeNo);
            if (message.CanIntoArcade != null && Object.hasOwnProperty.call(message, "CanIntoArcade"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.CanIntoArcade);
            if (message.Test != null && Object.hasOwnProperty.call(message, "Test"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Test);
            if (message.Prefer != null && Object.hasOwnProperty.call(message, "Prefer"))
                $root.jlmp.PreferRoundShow.encode(message.Prefer, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FreeSpinRemain != null && Object.hasOwnProperty.call(message, "FreeSpinRemain"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeSpinRemain);
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlmp.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {jlmp.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.WalletInfo && message.WalletInfo.length))
                            message.WalletInfo = [];
                        message.WalletInfo.push($root.jlmp.WalletInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ArcadeNo = reader.int32();
                        break;
                    }
                case 3: {
                        message.CanIntoArcade = reader.bool();
                        break;
                    }
                case 4: {
                        message.Test = reader.bool();
                        break;
                    }
                case 5: {
                        message.Prefer = $root.jlmp.PreferRoundShow.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.FreeSpinRemain = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WalletInfo != null && message.hasOwnProperty("WalletInfo")) {
                if (!Array.isArray(message.WalletInfo))
                    return "WalletInfo: array expected";
                for (let i = 0; i < message.WalletInfo.length; ++i) {
                    let error = $root.jlmp.WalletInfo.verify(message.WalletInfo[i]);
                    if (error)
                        return "WalletInfo." + error;
                }
            }
            if (message.ArcadeNo != null && message.hasOwnProperty("ArcadeNo"))
                if (!$util.isInteger(message.ArcadeNo))
                    return "ArcadeNo: integer expected";
            if (message.CanIntoArcade != null && message.hasOwnProperty("CanIntoArcade"))
                if (typeof message.CanIntoArcade !== "boolean")
                    return "CanIntoArcade: boolean expected";
            if (message.Test != null && message.hasOwnProperty("Test"))
                if (typeof message.Test !== "boolean")
                    return "Test: boolean expected";
            if (message.Prefer != null && message.hasOwnProperty("Prefer")) {
                let error = $root.jlmp.PreferRoundShow.verify(message.Prefer);
                if (error)
                    return "Prefer." + error;
            }
            if (message.FreeSpinRemain != null && message.hasOwnProperty("FreeSpinRemain"))
                if (!$util.isInteger(message.FreeSpinRemain))
                    return "FreeSpinRemain: integer expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.GameInfoAck)
                return object;
            let message = new $root.jlmp.GameInfoAck();
            if (object.WalletInfo) {
                if (!Array.isArray(object.WalletInfo))
                    throw TypeError(".jlmp.GameInfoAck.WalletInfo: array expected");
                message.WalletInfo = [];
                for (let i = 0; i < object.WalletInfo.length; ++i) {
                    if (typeof object.WalletInfo[i] !== "object")
                        throw TypeError(".jlmp.GameInfoAck.WalletInfo: object expected");
                    message.WalletInfo[i] = $root.jlmp.WalletInfo.fromObject(object.WalletInfo[i]);
                }
            }
            if (object.ArcadeNo != null)
                message.ArcadeNo = object.ArcadeNo | 0;
            if (object.CanIntoArcade != null)
                message.CanIntoArcade = Boolean(object.CanIntoArcade);
            if (object.Test != null)
                message.Test = Boolean(object.Test);
            if (object.Prefer != null) {
                if (typeof object.Prefer !== "object")
                    throw TypeError(".jlmp.GameInfoAck.Prefer: object expected");
                message.Prefer = $root.jlmp.PreferRoundShow.fromObject(object.Prefer);
            }
            if (object.FreeSpinRemain != null)
                message.FreeSpinRemain = object.FreeSpinRemain | 0;
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {jlmp.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.WalletInfo = [];
            if (options.defaults) {
                object.ArcadeNo = 0;
                object.CanIntoArcade = false;
                object.Test = false;
                object.Prefer = null;
                object.FreeSpinRemain = 0;
            }
            if (message.WalletInfo && message.WalletInfo.length) {
                object.WalletInfo = [];
                for (let j = 0; j < message.WalletInfo.length; ++j)
                    object.WalletInfo[j] = $root.jlmp.WalletInfo.toObject(message.WalletInfo[j], options);
            }
            if (message.ArcadeNo != null && message.hasOwnProperty("ArcadeNo"))
                object.ArcadeNo = message.ArcadeNo;
            if (message.CanIntoArcade != null && message.hasOwnProperty("CanIntoArcade"))
                object.CanIntoArcade = message.CanIntoArcade;
            if (message.Test != null && message.hasOwnProperty("Test"))
                object.Test = message.Test;
            if (message.Prefer != null && message.hasOwnProperty("Prefer"))
                object.Prefer = $root.jlmp.PreferRoundShow.toObject(message.Prefer, options);
            if (message.FreeSpinRemain != null && message.hasOwnProperty("FreeSpinRemain"))
                object.FreeSpinRemain = message.FreeSpinRemain;
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlmp.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlmp.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jlmp.VolInfo = (function() {

        /**
         * Properties of a VolInfo.
         * @memberof jlmp
         * @interface IVolInfo
         * @property {number|null} [VolPos] VolInfo VolPos
         * @property {Array.<number>|null} [VolList] VolInfo VolList
         */

        /**
         * Constructs a new VolInfo.
         * @memberof jlmp
         * @classdesc Represents a VolInfo.
         * @implements IVolInfo
         * @constructor
         * @param {jlmp.IVolInfo=} [properties] Properties to set
         */
        function VolInfo(properties) {
            this.VolList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VolInfo VolPos.
         * @member {number} VolPos
         * @memberof jlmp.VolInfo
         * @instance
         */
        VolInfo.prototype.VolPos = 0;

        /**
         * VolInfo VolList.
         * @member {Array.<number>} VolList
         * @memberof jlmp.VolInfo
         * @instance
         */
        VolInfo.prototype.VolList = $util.emptyArray;

        /**
         * Creates a new VolInfo instance using the specified properties.
         * @function create
         * @memberof jlmp.VolInfo
         * @static
         * @param {jlmp.IVolInfo=} [properties] Properties to set
         * @returns {jlmp.VolInfo} VolInfo instance
         */
        VolInfo.create = function create(properties) {
            return new VolInfo(properties);
        };

        /**
         * Encodes the specified VolInfo message. Does not implicitly {@link jlmp.VolInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmp.VolInfo
         * @static
         * @param {jlmp.IVolInfo} message VolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VolInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.VolPos != null && Object.hasOwnProperty.call(message, "VolPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.VolPos);
            if (message.VolList != null && message.VolList.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.VolList.length; ++i)
                    writer.int32(message.VolList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified VolInfo message, length delimited. Does not implicitly {@link jlmp.VolInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.VolInfo
         * @static
         * @param {jlmp.IVolInfo} message VolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VolInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VolInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.VolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.VolInfo} VolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VolInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.VolInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.VolPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.VolList && message.VolList.length))
                            message.VolList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.VolList.push(reader.int32());
                        } else
                            message.VolList.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VolInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.VolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.VolInfo} VolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VolInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VolInfo message.
         * @function verify
         * @memberof jlmp.VolInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VolInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.VolPos != null && message.hasOwnProperty("VolPos"))
                if (!$util.isInteger(message.VolPos))
                    return "VolPos: integer expected";
            if (message.VolList != null && message.hasOwnProperty("VolList")) {
                if (!Array.isArray(message.VolList))
                    return "VolList: array expected";
                for (let i = 0; i < message.VolList.length; ++i)
                    if (!$util.isInteger(message.VolList[i]))
                        return "VolList: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a VolInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.VolInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.VolInfo} VolInfo
         */
        VolInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.VolInfo)
                return object;
            let message = new $root.jlmp.VolInfo();
            if (object.VolPos != null)
                message.VolPos = object.VolPos | 0;
            if (object.VolList) {
                if (!Array.isArray(object.VolList))
                    throw TypeError(".jlmp.VolInfo.VolList: array expected");
                message.VolList = [];
                for (let i = 0; i < object.VolList.length; ++i)
                    message.VolList[i] = object.VolList[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a VolInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.VolInfo
         * @static
         * @param {jlmp.VolInfo} message VolInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VolInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.VolList = [];
            if (options.defaults)
                object.VolPos = 0;
            if (message.VolPos != null && message.hasOwnProperty("VolPos"))
                object.VolPos = message.VolPos;
            if (message.VolList && message.VolList.length) {
                object.VolList = [];
                for (let j = 0; j < message.VolList.length; ++j)
                    object.VolList[j] = message.VolList[j];
            }
            return object;
        };

        /**
         * Converts this VolInfo to JSON.
         * @function toJSON
         * @memberof jlmp.VolInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VolInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VolInfo
         * @function getTypeUrl
         * @memberof jlmp.VolInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VolInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.VolInfo";
        };

        return VolInfo;
    })();

    jlmp.GameReqData = (function() {

        /**
         * Properties of a GameReqData.
         * @memberof jlmp
         * @interface IGameReqData
         * @property {number|null} [aid] GameReqData aid
         * @property {number|null} [apiid] GameReqData apiid
         * @property {string|null} [token] GameReqData token
         * @property {Uint8Array|null} [encode] GameReqData encode
         * @property {jlmp.ISpinReq|null} [spin] GameReqData spin
         * @property {jlmp.ICheckMallReq|null} [checkMall] GameReqData checkMall
         */

        /**
         * Constructs a new GameReqData.
         * @memberof jlmp
         * @classdesc Represents a GameReqData.
         * @implements IGameReqData
         * @constructor
         * @param {jlmp.IGameReqData=} [properties] Properties to set
         */
        function GameReqData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameReqData aid.
         * @member {number} aid
         * @memberof jlmp.GameReqData
         * @instance
         */
        GameReqData.prototype.aid = 0;

        /**
         * GameReqData apiid.
         * @member {number} apiid
         * @memberof jlmp.GameReqData
         * @instance
         */
        GameReqData.prototype.apiid = 0;

        /**
         * GameReqData token.
         * @member {string} token
         * @memberof jlmp.GameReqData
         * @instance
         */
        GameReqData.prototype.token = "";

        /**
         * GameReqData encode.
         * @member {Uint8Array} encode
         * @memberof jlmp.GameReqData
         * @instance
         */
        GameReqData.prototype.encode = $util.newBuffer([]);

        /**
         * GameReqData spin.
         * @member {jlmp.ISpinReq|null|undefined} spin
         * @memberof jlmp.GameReqData
         * @instance
         */
        GameReqData.prototype.spin = null;

        /**
         * GameReqData checkMall.
         * @member {jlmp.ICheckMallReq|null|undefined} checkMall
         * @memberof jlmp.GameReqData
         * @instance
         */
        GameReqData.prototype.checkMall = null;

        /**
         * Creates a new GameReqData instance using the specified properties.
         * @function create
         * @memberof jlmp.GameReqData
         * @static
         * @param {jlmp.IGameReqData=} [properties] Properties to set
         * @returns {jlmp.GameReqData} GameReqData instance
         */
        GameReqData.create = function create(properties) {
            return new GameReqData(properties);
        };

        /**
         * Encodes the specified GameReqData message. Does not implicitly {@link jlmp.GameReqData.verify|verify} messages.
         * @function encode
         * @memberof jlmp.GameReqData
         * @static
         * @param {jlmp.IGameReqData} message GameReqData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameReqData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            if (message.apiid != null && Object.hasOwnProperty.call(message, "apiid"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.apiid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
            if (message.encode != null && Object.hasOwnProperty.call(message, "encode"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encode);
            if (message.spin != null && Object.hasOwnProperty.call(message, "spin"))
                $root.jlmp.SpinReq.encode(message.spin, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.checkMall != null && Object.hasOwnProperty.call(message, "checkMall"))
                $root.jlmp.CheckMallReq.encode(message.checkMall, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameReqData message, length delimited. Does not implicitly {@link jlmp.GameReqData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.GameReqData
         * @static
         * @param {jlmp.IGameReqData} message GameReqData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameReqData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameReqData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.GameReqData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.GameReqData} GameReqData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameReqData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.GameReqData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                case 2: {
                        message.apiid = reader.int32();
                        break;
                    }
                case 3: {
                        message.token = reader.string();
                        break;
                    }
                case 4: {
                        message.encode = reader.bytes();
                        break;
                    }
                case 5: {
                        message.spin = $root.jlmp.SpinReq.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.checkMall = $root.jlmp.CheckMallReq.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameReqData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.GameReqData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.GameReqData} GameReqData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameReqData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameReqData message.
         * @function verify
         * @memberof jlmp.GameReqData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameReqData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            if (message.apiid != null && message.hasOwnProperty("apiid"))
                if (!$util.isInteger(message.apiid))
                    return "apiid: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.encode != null && message.hasOwnProperty("encode"))
                if (!(message.encode && typeof message.encode.length === "number" || $util.isString(message.encode)))
                    return "encode: buffer expected";
            if (message.spin != null && message.hasOwnProperty("spin")) {
                let error = $root.jlmp.SpinReq.verify(message.spin);
                if (error)
                    return "spin." + error;
            }
            if (message.checkMall != null && message.hasOwnProperty("checkMall")) {
                let error = $root.jlmp.CheckMallReq.verify(message.checkMall);
                if (error)
                    return "checkMall." + error;
            }
            return null;
        };

        /**
         * Creates a GameReqData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.GameReqData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.GameReqData} GameReqData
         */
        GameReqData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.GameReqData)
                return object;
            let message = new $root.jlmp.GameReqData();
            if (object.aid != null)
                message.aid = object.aid | 0;
            if (object.apiid != null)
                message.apiid = object.apiid | 0;
            if (object.token != null)
                message.token = String(object.token);
            if (object.encode != null)
                if (typeof object.encode === "string")
                    $util.base64.decode(object.encode, message.encode = $util.newBuffer($util.base64.length(object.encode)), 0);
                else if (object.encode.length >= 0)
                    message.encode = object.encode;
            if (object.spin != null) {
                if (typeof object.spin !== "object")
                    throw TypeError(".jlmp.GameReqData.spin: object expected");
                message.spin = $root.jlmp.SpinReq.fromObject(object.spin);
            }
            if (object.checkMall != null) {
                if (typeof object.checkMall !== "object")
                    throw TypeError(".jlmp.GameReqData.checkMall: object expected");
                message.checkMall = $root.jlmp.CheckMallReq.fromObject(object.checkMall);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameReqData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.GameReqData
         * @static
         * @param {jlmp.GameReqData} message GameReqData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameReqData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.aid = 0;
                object.apiid = 0;
                object.token = "";
                if (options.bytes === String)
                    object.encode = "";
                else {
                    object.encode = [];
                    if (options.bytes !== Array)
                        object.encode = $util.newBuffer(object.encode);
                }
                object.spin = null;
                object.checkMall = null;
            }
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            if (message.apiid != null && message.hasOwnProperty("apiid"))
                object.apiid = message.apiid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.encode != null && message.hasOwnProperty("encode"))
                object.encode = options.bytes === String ? $util.base64.encode(message.encode, 0, message.encode.length) : options.bytes === Array ? Array.prototype.slice.call(message.encode) : message.encode;
            if (message.spin != null && message.hasOwnProperty("spin"))
                object.spin = $root.jlmp.SpinReq.toObject(message.spin, options);
            if (message.checkMall != null && message.hasOwnProperty("checkMall"))
                object.checkMall = $root.jlmp.CheckMallReq.toObject(message.checkMall, options);
            return object;
        };

        /**
         * Converts this GameReqData to JSON.
         * @function toJSON
         * @memberof jlmp.GameReqData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameReqData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameReqData
         * @function getTypeUrl
         * @memberof jlmp.GameReqData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameReqData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.GameReqData";
        };

        return GameReqData;
    })();

    jlmp.SpinReq = (function() {

        /**
         * Properties of a SpinReq.
         * @memberof jlmp
         * @interface ISpinReq
         * @property {number|null} [Bet] SpinReq Bet
         * @property {number|null} [Currency] SpinReq Currency
         * @property {number|null} [TestMode] SpinReq TestMode
         * @property {number|null} [MallID] SpinReq MallID
         * @property {number|null} [MallBet] SpinReq MallBet
         * @property {number|null} [VipLv] SpinReq VipLv
         * @property {number|null} [Free] SpinReq Free
         * @property {number|null} [Trial] SpinReq Trial
         * @property {number|null} [VolType] SpinReq VolType
         */

        /**
         * Constructs a new SpinReq.
         * @memberof jlmp
         * @classdesc Represents a SpinReq.
         * @implements ISpinReq
         * @constructor
         * @param {jlmp.ISpinReq=} [properties] Properties to set
         */
        function SpinReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinReq Bet.
         * @member {number} Bet
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.Bet = 0;

        /**
         * SpinReq Currency.
         * @member {number} Currency
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.Currency = 0;

        /**
         * SpinReq TestMode.
         * @member {number} TestMode
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.TestMode = 0;

        /**
         * SpinReq MallID.
         * @member {number} MallID
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.MallID = 0;

        /**
         * SpinReq MallBet.
         * @member {number} MallBet
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.MallBet = 0;

        /**
         * SpinReq VipLv.
         * @member {number} VipLv
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.VipLv = 0;

        /**
         * SpinReq Free.
         * @member {number} Free
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.Free = 0;

        /**
         * SpinReq Trial.
         * @member {number} Trial
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.Trial = 0;

        /**
         * SpinReq VolType.
         * @member {number} VolType
         * @memberof jlmp.SpinReq
         * @instance
         */
        SpinReq.prototype.VolType = 0;

        /**
         * Creates a new SpinReq instance using the specified properties.
         * @function create
         * @memberof jlmp.SpinReq
         * @static
         * @param {jlmp.ISpinReq=} [properties] Properties to set
         * @returns {jlmp.SpinReq} SpinReq instance
         */
        SpinReq.create = function create(properties) {
            return new SpinReq(properties);
        };

        /**
         * Encodes the specified SpinReq message. Does not implicitly {@link jlmp.SpinReq.verify|verify} messages.
         * @function encode
         * @memberof jlmp.SpinReq
         * @static
         * @param {jlmp.ISpinReq} message SpinReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Bet);
            if (message.Currency != null && Object.hasOwnProperty.call(message, "Currency"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Currency);
            if (message.TestMode != null && Object.hasOwnProperty.call(message, "TestMode"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.TestMode);
            if (message.MallID != null && Object.hasOwnProperty.call(message, "MallID"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.MallID);
            if (message.MallBet != null && Object.hasOwnProperty.call(message, "MallBet"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.MallBet);
            if (message.VipLv != null && Object.hasOwnProperty.call(message, "VipLv"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.VipLv);
            if (message.Free != null && Object.hasOwnProperty.call(message, "Free"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.Free);
            if (message.Trial != null && Object.hasOwnProperty.call(message, "Trial"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.Trial);
            if (message.VolType != null && Object.hasOwnProperty.call(message, "VolType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.VolType);
            return writer;
        };

        /**
         * Encodes the specified SpinReq message, length delimited. Does not implicitly {@link jlmp.SpinReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.SpinReq
         * @static
         * @param {jlmp.ISpinReq} message SpinReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.SpinReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.SpinReq} SpinReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.SpinReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Bet = reader.double();
                        break;
                    }
                case 2: {
                        message.Currency = reader.int32();
                        break;
                    }
                case 3: {
                        message.TestMode = reader.int32();
                        break;
                    }
                case 4: {
                        message.MallID = reader.int32();
                        break;
                    }
                case 5: {
                        message.MallBet = reader.double();
                        break;
                    }
                case 6: {
                        message.VipLv = reader.int32();
                        break;
                    }
                case 7: {
                        message.Free = reader.int32();
                        break;
                    }
                case 8: {
                        message.Trial = reader.int32();
                        break;
                    }
                case 9: {
                        message.VolType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.SpinReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.SpinReq} SpinReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinReq message.
         * @function verify
         * @memberof jlmp.SpinReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                if (!$util.isInteger(message.Currency))
                    return "Currency: integer expected";
            if (message.TestMode != null && message.hasOwnProperty("TestMode"))
                if (!$util.isInteger(message.TestMode))
                    return "TestMode: integer expected";
            if (message.MallID != null && message.hasOwnProperty("MallID"))
                if (!$util.isInteger(message.MallID))
                    return "MallID: integer expected";
            if (message.MallBet != null && message.hasOwnProperty("MallBet"))
                if (typeof message.MallBet !== "number")
                    return "MallBet: number expected";
            if (message.VipLv != null && message.hasOwnProperty("VipLv"))
                if (!$util.isInteger(message.VipLv))
                    return "VipLv: integer expected";
            if (message.Free != null && message.hasOwnProperty("Free"))
                if (!$util.isInteger(message.Free))
                    return "Free: integer expected";
            if (message.Trial != null && message.hasOwnProperty("Trial"))
                if (!$util.isInteger(message.Trial))
                    return "Trial: integer expected";
            if (message.VolType != null && message.hasOwnProperty("VolType"))
                if (!$util.isInteger(message.VolType))
                    return "VolType: integer expected";
            return null;
        };

        /**
         * Creates a SpinReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.SpinReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.SpinReq} SpinReq
         */
        SpinReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.SpinReq)
                return object;
            let message = new $root.jlmp.SpinReq();
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Currency != null)
                message.Currency = object.Currency | 0;
            if (object.TestMode != null)
                message.TestMode = object.TestMode | 0;
            if (object.MallID != null)
                message.MallID = object.MallID | 0;
            if (object.MallBet != null)
                message.MallBet = Number(object.MallBet);
            if (object.VipLv != null)
                message.VipLv = object.VipLv | 0;
            if (object.Free != null)
                message.Free = object.Free | 0;
            if (object.Trial != null)
                message.Trial = object.Trial | 0;
            if (object.VolType != null)
                message.VolType = object.VolType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.SpinReq
         * @static
         * @param {jlmp.SpinReq} message SpinReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Bet = 0;
                object.Currency = 0;
                object.TestMode = 0;
                object.MallID = 0;
                object.MallBet = 0;
                object.VipLv = 0;
                object.Free = 0;
                object.Trial = 0;
                object.VolType = 0;
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                object.Currency = message.Currency;
            if (message.TestMode != null && message.hasOwnProperty("TestMode"))
                object.TestMode = message.TestMode;
            if (message.MallID != null && message.hasOwnProperty("MallID"))
                object.MallID = message.MallID;
            if (message.MallBet != null && message.hasOwnProperty("MallBet"))
                object.MallBet = options.json && !isFinite(message.MallBet) ? String(message.MallBet) : message.MallBet;
            if (message.VipLv != null && message.hasOwnProperty("VipLv"))
                object.VipLv = message.VipLv;
            if (message.Free != null && message.hasOwnProperty("Free"))
                object.Free = message.Free;
            if (message.Trial != null && message.hasOwnProperty("Trial"))
                object.Trial = message.Trial;
            if (message.VolType != null && message.hasOwnProperty("VolType"))
                object.VolType = message.VolType;
            return object;
        };

        /**
         * Converts this SpinReq to JSON.
         * @function toJSON
         * @memberof jlmp.SpinReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinReq
         * @function getTypeUrl
         * @memberof jlmp.SpinReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.SpinReq";
        };

        return SpinReq;
    })();

    jlmp.ResData = (function() {

        /**
         * Properties of a ResData.
         * @memberof jlmp
         * @interface IResData
         * @property {number|null} [ret] ResData ret
         * @property {number|null} [type] ResData type
         * @property {string|null} [token] ResData token
         * @property {string|null} [msg] ResData msg
         * @property {jlmp.IInfoData|null} [data] ResData data
         */

        /**
         * Constructs a new ResData.
         * @memberof jlmp
         * @classdesc Represents a ResData.
         * @implements IResData
         * @constructor
         * @param {jlmp.IResData=} [properties] Properties to set
         */
        function ResData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ResData ret.
         * @member {number} ret
         * @memberof jlmp.ResData
         * @instance
         */
        ResData.prototype.ret = 0;

        /**
         * ResData type.
         * @member {number} type
         * @memberof jlmp.ResData
         * @instance
         */
        ResData.prototype.type = 0;

        /**
         * ResData token.
         * @member {string} token
         * @memberof jlmp.ResData
         * @instance
         */
        ResData.prototype.token = "";

        /**
         * ResData msg.
         * @member {string} msg
         * @memberof jlmp.ResData
         * @instance
         */
        ResData.prototype.msg = "";

        /**
         * ResData data.
         * @member {jlmp.IInfoData|null|undefined} data
         * @memberof jlmp.ResData
         * @instance
         */
        ResData.prototype.data = null;

        /**
         * Creates a new ResData instance using the specified properties.
         * @function create
         * @memberof jlmp.ResData
         * @static
         * @param {jlmp.IResData=} [properties] Properties to set
         * @returns {jlmp.ResData} ResData instance
         */
        ResData.create = function create(properties) {
            return new ResData(properties);
        };

        /**
         * Encodes the specified ResData message. Does not implicitly {@link jlmp.ResData.verify|verify} messages.
         * @function encode
         * @memberof jlmp.ResData
         * @static
         * @param {jlmp.IResData} message ResData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ret != null && Object.hasOwnProperty.call(message, "ret"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ret);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
            if (message.msg != null && Object.hasOwnProperty.call(message, "msg"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.msg);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.jlmp.InfoData.encode(message.data, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ResData message, length delimited. Does not implicitly {@link jlmp.ResData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.ResData
         * @static
         * @param {jlmp.IResData} message ResData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ResData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.ResData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.ResData} ResData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.ResData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ret = reader.int32();
                        break;
                    }
                case 2: {
                        message.type = reader.int32();
                        break;
                    }
                case 3: {
                        message.token = reader.string();
                        break;
                    }
                case 4: {
                        message.msg = reader.string();
                        break;
                    }
                case 5: {
                        message.data = $root.jlmp.InfoData.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ResData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.ResData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.ResData} ResData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResData message.
         * @function verify
         * @memberof jlmp.ResData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ret != null && message.hasOwnProperty("ret"))
                if (!$util.isInteger(message.ret))
                    return "ret: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.msg != null && message.hasOwnProperty("msg"))
                if (!$util.isString(message.msg))
                    return "msg: string expected";
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.jlmp.InfoData.verify(message.data);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        /**
         * Creates a ResData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.ResData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.ResData} ResData
         */
        ResData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.ResData)
                return object;
            let message = new $root.jlmp.ResData();
            if (object.ret != null)
                message.ret = object.ret | 0;
            if (object.type != null)
                message.type = object.type | 0;
            if (object.token != null)
                message.token = String(object.token);
            if (object.msg != null)
                message.msg = String(object.msg);
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".jlmp.ResData.data: object expected");
                message.data = $root.jlmp.InfoData.fromObject(object.data);
            }
            return message;
        };

        /**
         * Creates a plain object from a ResData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.ResData
         * @static
         * @param {jlmp.ResData} message ResData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ret = 0;
                object.type = 0;
                object.token = "";
                object.msg = "";
                object.data = null;
            }
            if (message.ret != null && message.hasOwnProperty("ret"))
                object.ret = message.ret;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.msg != null && message.hasOwnProperty("msg"))
                object.msg = message.msg;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.jlmp.InfoData.toObject(message.data, options);
            return object;
        };

        /**
         * Converts this ResData to JSON.
         * @function toJSON
         * @memberof jlmp.ResData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ResData
         * @function getTypeUrl
         * @memberof jlmp.ResData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ResData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.ResData";
        };

        return ResData;
    })();

    jlmp.InfoData = (function() {

        /**
         * Properties of an InfoData.
         * @memberof jlmp
         * @interface IInfoData
         * @property {Uint8Array|null} [encode] InfoData encode
         * @property {jlmp.ISpinAllData|null} [spinAll] InfoData spinAll
         * @property {jlmp.IGameInfoAck|null} [gameInfo] InfoData gameInfo
         * @property {jlmp.ICheckMallAck|null} [checkMall] InfoData checkMall
         * @property {jlmp.ILoginDataAck|null} [loginData] InfoData loginData
         * @property {jlmp.IHeartAck|null} [heart] InfoData heart
         * @property {jlmp.IExchangeAck|null} [exchange] InfoData exchange
         * @property {jlmp.ILastSpinAck|null} [lastSpin] InfoData lastSpin
         */

        /**
         * Constructs a new InfoData.
         * @memberof jlmp
         * @classdesc Represents an InfoData.
         * @implements IInfoData
         * @constructor
         * @param {jlmp.IInfoData=} [properties] Properties to set
         */
        function InfoData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InfoData encode.
         * @member {Uint8Array} encode
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.encode = $util.newBuffer([]);

        /**
         * InfoData spinAll.
         * @member {jlmp.ISpinAllData|null|undefined} spinAll
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.spinAll = null;

        /**
         * InfoData gameInfo.
         * @member {jlmp.IGameInfoAck|null|undefined} gameInfo
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.gameInfo = null;

        /**
         * InfoData checkMall.
         * @member {jlmp.ICheckMallAck|null|undefined} checkMall
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.checkMall = null;

        /**
         * InfoData loginData.
         * @member {jlmp.ILoginDataAck|null|undefined} loginData
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.loginData = null;

        /**
         * InfoData heart.
         * @member {jlmp.IHeartAck|null|undefined} heart
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.heart = null;

        /**
         * InfoData exchange.
         * @member {jlmp.IExchangeAck|null|undefined} exchange
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.exchange = null;

        /**
         * InfoData lastSpin.
         * @member {jlmp.ILastSpinAck|null|undefined} lastSpin
         * @memberof jlmp.InfoData
         * @instance
         */
        InfoData.prototype.lastSpin = null;

        /**
         * Creates a new InfoData instance using the specified properties.
         * @function create
         * @memberof jlmp.InfoData
         * @static
         * @param {jlmp.IInfoData=} [properties] Properties to set
         * @returns {jlmp.InfoData} InfoData instance
         */
        InfoData.create = function create(properties) {
            return new InfoData(properties);
        };

        /**
         * Encodes the specified InfoData message. Does not implicitly {@link jlmp.InfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmp.InfoData
         * @static
         * @param {jlmp.IInfoData} message InfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.encode != null && Object.hasOwnProperty.call(message, "encode"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encode);
            if (message.spinAll != null && Object.hasOwnProperty.call(message, "spinAll"))
                $root.jlmp.SpinAllData.encode(message.spinAll, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.gameInfo != null && Object.hasOwnProperty.call(message, "gameInfo"))
                $root.jlmp.GameInfoAck.encode(message.gameInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.checkMall != null && Object.hasOwnProperty.call(message, "checkMall"))
                $root.jlmp.CheckMallAck.encode(message.checkMall, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.loginData != null && Object.hasOwnProperty.call(message, "loginData"))
                $root.jlmp.LoginDataAck.encode(message.loginData, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.heart != null && Object.hasOwnProperty.call(message, "heart"))
                $root.jlmp.HeartAck.encode(message.heart, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.exchange != null && Object.hasOwnProperty.call(message, "exchange"))
                $root.jlmp.ExchangeAck.encode(message.exchange, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.lastSpin != null && Object.hasOwnProperty.call(message, "lastSpin"))
                $root.jlmp.LastSpinAck.encode(message.lastSpin, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InfoData message, length delimited. Does not implicitly {@link jlmp.InfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.InfoData
         * @static
         * @param {jlmp.IInfoData} message InfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.InfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.InfoData} InfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.InfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encode = reader.bytes();
                        break;
                    }
                case 2: {
                        message.spinAll = $root.jlmp.SpinAllData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.gameInfo = $root.jlmp.GameInfoAck.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.checkMall = $root.jlmp.CheckMallAck.decode(reader, reader.uint32());
                        break;
                    }
                case 5: {
                        message.loginData = $root.jlmp.LoginDataAck.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.heart = $root.jlmp.HeartAck.decode(reader, reader.uint32());
                        break;
                    }
                case 7: {
                        message.exchange = $root.jlmp.ExchangeAck.decode(reader, reader.uint32());
                        break;
                    }
                case 8: {
                        message.lastSpin = $root.jlmp.LastSpinAck.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.InfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.InfoData} InfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InfoData message.
         * @function verify
         * @memberof jlmp.InfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.encode != null && message.hasOwnProperty("encode"))
                if (!(message.encode && typeof message.encode.length === "number" || $util.isString(message.encode)))
                    return "encode: buffer expected";
            if (message.spinAll != null && message.hasOwnProperty("spinAll")) {
                let error = $root.jlmp.SpinAllData.verify(message.spinAll);
                if (error)
                    return "spinAll." + error;
            }
            if (message.gameInfo != null && message.hasOwnProperty("gameInfo")) {
                let error = $root.jlmp.GameInfoAck.verify(message.gameInfo);
                if (error)
                    return "gameInfo." + error;
            }
            if (message.checkMall != null && message.hasOwnProperty("checkMall")) {
                let error = $root.jlmp.CheckMallAck.verify(message.checkMall);
                if (error)
                    return "checkMall." + error;
            }
            if (message.loginData != null && message.hasOwnProperty("loginData")) {
                let error = $root.jlmp.LoginDataAck.verify(message.loginData);
                if (error)
                    return "loginData." + error;
            }
            if (message.heart != null && message.hasOwnProperty("heart")) {
                let error = $root.jlmp.HeartAck.verify(message.heart);
                if (error)
                    return "heart." + error;
            }
            if (message.exchange != null && message.hasOwnProperty("exchange")) {
                let error = $root.jlmp.ExchangeAck.verify(message.exchange);
                if (error)
                    return "exchange." + error;
            }
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin")) {
                let error = $root.jlmp.LastSpinAck.verify(message.lastSpin);
                if (error)
                    return "lastSpin." + error;
            }
            return null;
        };

        /**
         * Creates an InfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.InfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.InfoData} InfoData
         */
        InfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.InfoData)
                return object;
            let message = new $root.jlmp.InfoData();
            if (object.encode != null)
                if (typeof object.encode === "string")
                    $util.base64.decode(object.encode, message.encode = $util.newBuffer($util.base64.length(object.encode)), 0);
                else if (object.encode.length >= 0)
                    message.encode = object.encode;
            if (object.spinAll != null) {
                if (typeof object.spinAll !== "object")
                    throw TypeError(".jlmp.InfoData.spinAll: object expected");
                message.spinAll = $root.jlmp.SpinAllData.fromObject(object.spinAll);
            }
            if (object.gameInfo != null) {
                if (typeof object.gameInfo !== "object")
                    throw TypeError(".jlmp.InfoData.gameInfo: object expected");
                message.gameInfo = $root.jlmp.GameInfoAck.fromObject(object.gameInfo);
            }
            if (object.checkMall != null) {
                if (typeof object.checkMall !== "object")
                    throw TypeError(".jlmp.InfoData.checkMall: object expected");
                message.checkMall = $root.jlmp.CheckMallAck.fromObject(object.checkMall);
            }
            if (object.loginData != null) {
                if (typeof object.loginData !== "object")
                    throw TypeError(".jlmp.InfoData.loginData: object expected");
                message.loginData = $root.jlmp.LoginDataAck.fromObject(object.loginData);
            }
            if (object.heart != null) {
                if (typeof object.heart !== "object")
                    throw TypeError(".jlmp.InfoData.heart: object expected");
                message.heart = $root.jlmp.HeartAck.fromObject(object.heart);
            }
            if (object.exchange != null) {
                if (typeof object.exchange !== "object")
                    throw TypeError(".jlmp.InfoData.exchange: object expected");
                message.exchange = $root.jlmp.ExchangeAck.fromObject(object.exchange);
            }
            if (object.lastSpin != null) {
                if (typeof object.lastSpin !== "object")
                    throw TypeError(".jlmp.InfoData.lastSpin: object expected");
                message.lastSpin = $root.jlmp.LastSpinAck.fromObject(object.lastSpin);
            }
            return message;
        };

        /**
         * Creates a plain object from an InfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.InfoData
         * @static
         * @param {jlmp.InfoData} message InfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encode = "";
                else {
                    object.encode = [];
                    if (options.bytes !== Array)
                        object.encode = $util.newBuffer(object.encode);
                }
                object.spinAll = null;
                object.gameInfo = null;
                object.checkMall = null;
                object.loginData = null;
                object.heart = null;
                object.exchange = null;
                object.lastSpin = null;
            }
            if (message.encode != null && message.hasOwnProperty("encode"))
                object.encode = options.bytes === String ? $util.base64.encode(message.encode, 0, message.encode.length) : options.bytes === Array ? Array.prototype.slice.call(message.encode) : message.encode;
            if (message.spinAll != null && message.hasOwnProperty("spinAll"))
                object.spinAll = $root.jlmp.SpinAllData.toObject(message.spinAll, options);
            if (message.gameInfo != null && message.hasOwnProperty("gameInfo"))
                object.gameInfo = $root.jlmp.GameInfoAck.toObject(message.gameInfo, options);
            if (message.checkMall != null && message.hasOwnProperty("checkMall"))
                object.checkMall = $root.jlmp.CheckMallAck.toObject(message.checkMall, options);
            if (message.loginData != null && message.hasOwnProperty("loginData"))
                object.loginData = $root.jlmp.LoginDataAck.toObject(message.loginData, options);
            if (message.heart != null && message.hasOwnProperty("heart"))
                object.heart = $root.jlmp.HeartAck.toObject(message.heart, options);
            if (message.exchange != null && message.hasOwnProperty("exchange"))
                object.exchange = $root.jlmp.ExchangeAck.toObject(message.exchange, options);
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin"))
                object.lastSpin = $root.jlmp.LastSpinAck.toObject(message.lastSpin, options);
            return object;
        };

        /**
         * Converts this InfoData to JSON.
         * @function toJSON
         * @memberof jlmp.InfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InfoData
         * @function getTypeUrl
         * @memberof jlmp.InfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.InfoData";
        };

        return InfoData;
    })();

    jlmp.SpinAllData = (function() {

        /**
         * Properties of a SpinAllData.
         * @memberof jlmp
         * @interface ISpinAllData
         * @property {jlmp.ISpinAck|null} [info] SpinAllData info
         * @property {jlmp.IServiceData|null} [serviceInfo] SpinAllData serviceInfo
         */

        /**
         * Constructs a new SpinAllData.
         * @memberof jlmp
         * @classdesc Represents a SpinAllData.
         * @implements ISpinAllData
         * @constructor
         * @param {jlmp.ISpinAllData=} [properties] Properties to set
         */
        function SpinAllData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAllData info.
         * @member {jlmp.ISpinAck|null|undefined} info
         * @memberof jlmp.SpinAllData
         * @instance
         */
        SpinAllData.prototype.info = null;

        /**
         * SpinAllData serviceInfo.
         * @member {jlmp.IServiceData|null|undefined} serviceInfo
         * @memberof jlmp.SpinAllData
         * @instance
         */
        SpinAllData.prototype.serviceInfo = null;

        /**
         * Creates a new SpinAllData instance using the specified properties.
         * @function create
         * @memberof jlmp.SpinAllData
         * @static
         * @param {jlmp.ISpinAllData=} [properties] Properties to set
         * @returns {jlmp.SpinAllData} SpinAllData instance
         */
        SpinAllData.create = function create(properties) {
            return new SpinAllData(properties);
        };

        /**
         * Encodes the specified SpinAllData message. Does not implicitly {@link jlmp.SpinAllData.verify|verify} messages.
         * @function encode
         * @memberof jlmp.SpinAllData
         * @static
         * @param {jlmp.ISpinAllData} message SpinAllData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAllData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.jlmp.SpinAck.encode(message.info, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.serviceInfo != null && Object.hasOwnProperty.call(message, "serviceInfo"))
                $root.jlmp.ServiceData.encode(message.serviceInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SpinAllData message, length delimited. Does not implicitly {@link jlmp.SpinAllData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.SpinAllData
         * @static
         * @param {jlmp.ISpinAllData} message SpinAllData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAllData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAllData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.SpinAllData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.SpinAllData} SpinAllData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAllData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.SpinAllData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.info = $root.jlmp.SpinAck.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.serviceInfo = $root.jlmp.ServiceData.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAllData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.SpinAllData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.SpinAllData} SpinAllData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAllData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAllData message.
         * @function verify
         * @memberof jlmp.SpinAllData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAllData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                let error = $root.jlmp.SpinAck.verify(message.info);
                if (error)
                    return "info." + error;
            }
            if (message.serviceInfo != null && message.hasOwnProperty("serviceInfo")) {
                let error = $root.jlmp.ServiceData.verify(message.serviceInfo);
                if (error)
                    return "serviceInfo." + error;
            }
            return null;
        };

        /**
         * Creates a SpinAllData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.SpinAllData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.SpinAllData} SpinAllData
         */
        SpinAllData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.SpinAllData)
                return object;
            let message = new $root.jlmp.SpinAllData();
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".jlmp.SpinAllData.info: object expected");
                message.info = $root.jlmp.SpinAck.fromObject(object.info);
            }
            if (object.serviceInfo != null) {
                if (typeof object.serviceInfo !== "object")
                    throw TypeError(".jlmp.SpinAllData.serviceInfo: object expected");
                message.serviceInfo = $root.jlmp.ServiceData.fromObject(object.serviceInfo);
            }
            return message;
        };

        /**
         * Creates a plain object from a SpinAllData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.SpinAllData
         * @static
         * @param {jlmp.SpinAllData} message SpinAllData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAllData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.info = null;
                object.serviceInfo = null;
            }
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.jlmp.SpinAck.toObject(message.info, options);
            if (message.serviceInfo != null && message.hasOwnProperty("serviceInfo"))
                object.serviceInfo = $root.jlmp.ServiceData.toObject(message.serviceInfo, options);
            return object;
        };

        /**
         * Converts this SpinAllData to JSON.
         * @function toJSON
         * @memberof jlmp.SpinAllData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAllData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAllData
         * @function getTypeUrl
         * @memberof jlmp.SpinAllData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAllData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.SpinAllData";
        };

        return SpinAllData;
    })();

    jlmp.ServiceData = (function() {

        /**
         * Properties of a ServiceData.
         * @memberof jlmp
         * @interface IServiceData
         * @property {number|null} [missionReward] ServiceData missionReward
         * @property {number|null} [jpType] ServiceData jpType
         * @property {number|null} [jpWin] ServiceData jpWin
         * @property {number|null} [fullJpWin] ServiceData fullJpWin
         * @property {number|null} [trialfinish] ServiceData trialfinish
         * @property {number|null} [trialaward] ServiceData trialaward
         * @property {number|null} [freeremain] ServiceData freeremain
         */

        /**
         * Constructs a new ServiceData.
         * @memberof jlmp
         * @classdesc Represents a ServiceData.
         * @implements IServiceData
         * @constructor
         * @param {jlmp.IServiceData=} [properties] Properties to set
         */
        function ServiceData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ServiceData missionReward.
         * @member {number} missionReward
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.missionReward = 0;

        /**
         * ServiceData jpType.
         * @member {number} jpType
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.jpType = 0;

        /**
         * ServiceData jpWin.
         * @member {number} jpWin
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.jpWin = 0;

        /**
         * ServiceData fullJpWin.
         * @member {number} fullJpWin
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.fullJpWin = 0;

        /**
         * ServiceData trialfinish.
         * @member {number} trialfinish
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.trialfinish = 0;

        /**
         * ServiceData trialaward.
         * @member {number} trialaward
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.trialaward = 0;

        /**
         * ServiceData freeremain.
         * @member {number} freeremain
         * @memberof jlmp.ServiceData
         * @instance
         */
        ServiceData.prototype.freeremain = 0;

        /**
         * Creates a new ServiceData instance using the specified properties.
         * @function create
         * @memberof jlmp.ServiceData
         * @static
         * @param {jlmp.IServiceData=} [properties] Properties to set
         * @returns {jlmp.ServiceData} ServiceData instance
         */
        ServiceData.create = function create(properties) {
            return new ServiceData(properties);
        };

        /**
         * Encodes the specified ServiceData message. Does not implicitly {@link jlmp.ServiceData.verify|verify} messages.
         * @function encode
         * @memberof jlmp.ServiceData
         * @static
         * @param {jlmp.IServiceData} message ServiceData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServiceData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.missionReward != null && Object.hasOwnProperty.call(message, "missionReward"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.missionReward);
            if (message.jpType != null && Object.hasOwnProperty.call(message, "jpType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.jpType);
            if (message.jpWin != null && Object.hasOwnProperty.call(message, "jpWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.jpWin);
            if (message.fullJpWin != null && Object.hasOwnProperty.call(message, "fullJpWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.fullJpWin);
            if (message.trialfinish != null && Object.hasOwnProperty.call(message, "trialfinish"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.trialfinish);
            if (message.trialaward != null && Object.hasOwnProperty.call(message, "trialaward"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.trialaward);
            if (message.freeremain != null && Object.hasOwnProperty.call(message, "freeremain"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.freeremain);
            return writer;
        };

        /**
         * Encodes the specified ServiceData message, length delimited. Does not implicitly {@link jlmp.ServiceData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.ServiceData
         * @static
         * @param {jlmp.IServiceData} message ServiceData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServiceData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ServiceData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.ServiceData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.ServiceData} ServiceData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServiceData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.ServiceData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.missionReward = reader.uint32();
                        break;
                    }
                case 2: {
                        message.jpType = reader.int32();
                        break;
                    }
                case 3: {
                        message.jpWin = reader.double();
                        break;
                    }
                case 4: {
                        message.fullJpWin = reader.double();
                        break;
                    }
                case 5: {
                        message.trialfinish = reader.int32();
                        break;
                    }
                case 6: {
                        message.trialaward = reader.int32();
                        break;
                    }
                case 7: {
                        message.freeremain = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ServiceData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.ServiceData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.ServiceData} ServiceData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServiceData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServiceData message.
         * @function verify
         * @memberof jlmp.ServiceData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServiceData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.missionReward != null && message.hasOwnProperty("missionReward"))
                if (!$util.isInteger(message.missionReward))
                    return "missionReward: integer expected";
            if (message.jpType != null && message.hasOwnProperty("jpType"))
                if (!$util.isInteger(message.jpType))
                    return "jpType: integer expected";
            if (message.jpWin != null && message.hasOwnProperty("jpWin"))
                if (typeof message.jpWin !== "number")
                    return "jpWin: number expected";
            if (message.fullJpWin != null && message.hasOwnProperty("fullJpWin"))
                if (typeof message.fullJpWin !== "number")
                    return "fullJpWin: number expected";
            if (message.trialfinish != null && message.hasOwnProperty("trialfinish"))
                if (!$util.isInteger(message.trialfinish))
                    return "trialfinish: integer expected";
            if (message.trialaward != null && message.hasOwnProperty("trialaward"))
                if (!$util.isInteger(message.trialaward))
                    return "trialaward: integer expected";
            if (message.freeremain != null && message.hasOwnProperty("freeremain"))
                if (!$util.isInteger(message.freeremain))
                    return "freeremain: integer expected";
            return null;
        };

        /**
         * Creates a ServiceData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.ServiceData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.ServiceData} ServiceData
         */
        ServiceData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.ServiceData)
                return object;
            let message = new $root.jlmp.ServiceData();
            if (object.missionReward != null)
                message.missionReward = object.missionReward >>> 0;
            if (object.jpType != null)
                message.jpType = object.jpType | 0;
            if (object.jpWin != null)
                message.jpWin = Number(object.jpWin);
            if (object.fullJpWin != null)
                message.fullJpWin = Number(object.fullJpWin);
            if (object.trialfinish != null)
                message.trialfinish = object.trialfinish | 0;
            if (object.trialaward != null)
                message.trialaward = object.trialaward | 0;
            if (object.freeremain != null)
                message.freeremain = object.freeremain | 0;
            return message;
        };

        /**
         * Creates a plain object from a ServiceData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.ServiceData
         * @static
         * @param {jlmp.ServiceData} message ServiceData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServiceData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.missionReward = 0;
                object.jpType = 0;
                object.jpWin = 0;
                object.fullJpWin = 0;
                object.trialfinish = 0;
                object.trialaward = 0;
                object.freeremain = 0;
            }
            if (message.missionReward != null && message.hasOwnProperty("missionReward"))
                object.missionReward = message.missionReward;
            if (message.jpType != null && message.hasOwnProperty("jpType"))
                object.jpType = message.jpType;
            if (message.jpWin != null && message.hasOwnProperty("jpWin"))
                object.jpWin = options.json && !isFinite(message.jpWin) ? String(message.jpWin) : message.jpWin;
            if (message.fullJpWin != null && message.hasOwnProperty("fullJpWin"))
                object.fullJpWin = options.json && !isFinite(message.fullJpWin) ? String(message.fullJpWin) : message.fullJpWin;
            if (message.trialfinish != null && message.hasOwnProperty("trialfinish"))
                object.trialfinish = message.trialfinish;
            if (message.trialaward != null && message.hasOwnProperty("trialaward"))
                object.trialaward = message.trialaward;
            if (message.freeremain != null && message.hasOwnProperty("freeremain"))
                object.freeremain = message.freeremain;
            return object;
        };

        /**
         * Converts this ServiceData to JSON.
         * @function toJSON
         * @memberof jlmp.ServiceData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServiceData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ServiceData
         * @function getTypeUrl
         * @memberof jlmp.ServiceData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ServiceData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.ServiceData";
        };

        return ServiceData;
    })();

    jlmp.WalletInfo = (function() {

        /**
         * Properties of a WalletInfo.
         * @memberof jlmp
         * @interface IWalletInfo
         * @property {number|null} [currencyNumber] WalletInfo currencyNumber
         * @property {string|null} [currencyName] WalletInfo currencyName
         * @property {string|null} [currencySymbol] WalletInfo currencySymbol
         * @property {number|null} [coin] WalletInfo coin
         * @property {Array.<number>|null} [bet] WalletInfo bet
         * @property {number|null} [unit] WalletInfo unit
         * @property {number|null} [ratio] WalletInfo ratio
         * @property {number|null} [rate] WalletInfo rate
         * @property {number|null} [upper] WalletInfo upper
         * @property {number|null} [lower] WalletInfo lower
         * @property {number|null} [cycle] WalletInfo cycle
         */

        /**
         * Constructs a new WalletInfo.
         * @memberof jlmp
         * @classdesc Represents a WalletInfo.
         * @implements IWalletInfo
         * @constructor
         * @param {jlmp.IWalletInfo=} [properties] Properties to set
         */
        function WalletInfo(properties) {
            this.bet = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WalletInfo currencyNumber.
         * @member {number} currencyNumber
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.currencyNumber = 0;

        /**
         * WalletInfo currencyName.
         * @member {string} currencyName
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.currencyName = "";

        /**
         * WalletInfo currencySymbol.
         * @member {string} currencySymbol
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.currencySymbol = "";

        /**
         * WalletInfo coin.
         * @member {number} coin
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.coin = 0;

        /**
         * WalletInfo bet.
         * @member {Array.<number>} bet
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.bet = $util.emptyArray;

        /**
         * WalletInfo unit.
         * @member {number} unit
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.unit = 0;

        /**
         * WalletInfo ratio.
         * @member {number} ratio
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.ratio = 0;

        /**
         * WalletInfo rate.
         * @member {number} rate
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.rate = 0;

        /**
         * WalletInfo upper.
         * @member {number} upper
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.upper = 0;

        /**
         * WalletInfo lower.
         * @member {number} lower
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.lower = 0;

        /**
         * WalletInfo cycle.
         * @member {number} cycle
         * @memberof jlmp.WalletInfo
         * @instance
         */
        WalletInfo.prototype.cycle = 0;

        /**
         * Creates a new WalletInfo instance using the specified properties.
         * @function create
         * @memberof jlmp.WalletInfo
         * @static
         * @param {jlmp.IWalletInfo=} [properties] Properties to set
         * @returns {jlmp.WalletInfo} WalletInfo instance
         */
        WalletInfo.create = function create(properties) {
            return new WalletInfo(properties);
        };

        /**
         * Encodes the specified WalletInfo message. Does not implicitly {@link jlmp.WalletInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmp.WalletInfo
         * @static
         * @param {jlmp.IWalletInfo} message WalletInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.currencyNumber != null && Object.hasOwnProperty.call(message, "currencyNumber"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.currencyNumber);
            if (message.currencyName != null && Object.hasOwnProperty.call(message, "currencyName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.currencyName);
            if (message.currencySymbol != null && Object.hasOwnProperty.call(message, "currencySymbol"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.currencySymbol);
            if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.coin);
            if (message.bet != null && message.bet.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.bet.length; ++i)
                    writer.double(message.bet[i]);
                writer.ldelim();
            }
            if (message.unit != null && Object.hasOwnProperty.call(message, "unit"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.unit);
            if (message.ratio != null && Object.hasOwnProperty.call(message, "ratio"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.ratio);
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.rate);
            if (message.upper != null && Object.hasOwnProperty.call(message, "upper"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.upper);
            if (message.lower != null && Object.hasOwnProperty.call(message, "lower"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.lower);
            if (message.cycle != null && Object.hasOwnProperty.call(message, "cycle"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.cycle);
            return writer;
        };

        /**
         * Encodes the specified WalletInfo message, length delimited. Does not implicitly {@link jlmp.WalletInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.WalletInfo
         * @static
         * @param {jlmp.IWalletInfo} message WalletInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WalletInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.WalletInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.WalletInfo} WalletInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.WalletInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.currencyNumber = reader.int32();
                        break;
                    }
                case 2: {
                        message.currencyName = reader.string();
                        break;
                    }
                case 3: {
                        message.currencySymbol = reader.string();
                        break;
                    }
                case 4: {
                        message.coin = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.bet && message.bet.length))
                            message.bet = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.bet.push(reader.double());
                        } else
                            message.bet.push(reader.double());
                        break;
                    }
                case 6: {
                        message.unit = reader.double();
                        break;
                    }
                case 7: {
                        message.ratio = reader.double();
                        break;
                    }
                case 8: {
                        message.rate = reader.double();
                        break;
                    }
                case 9: {
                        message.upper = reader.double();
                        break;
                    }
                case 10: {
                        message.lower = reader.double();
                        break;
                    }
                case 11: {
                        message.cycle = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WalletInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.WalletInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.WalletInfo} WalletInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WalletInfo message.
         * @function verify
         * @memberof jlmp.WalletInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WalletInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                if (!$util.isInteger(message.currencyNumber))
                    return "currencyNumber: integer expected";
            if (message.currencyName != null && message.hasOwnProperty("currencyName"))
                if (!$util.isString(message.currencyName))
                    return "currencyName: string expected";
            if (message.currencySymbol != null && message.hasOwnProperty("currencySymbol"))
                if (!$util.isString(message.currencySymbol))
                    return "currencySymbol: string expected";
            if (message.coin != null && message.hasOwnProperty("coin"))
                if (typeof message.coin !== "number")
                    return "coin: number expected";
            if (message.bet != null && message.hasOwnProperty("bet")) {
                if (!Array.isArray(message.bet))
                    return "bet: array expected";
                for (let i = 0; i < message.bet.length; ++i)
                    if (typeof message.bet[i] !== "number")
                        return "bet: number[] expected";
            }
            if (message.unit != null && message.hasOwnProperty("unit"))
                if (typeof message.unit !== "number")
                    return "unit: number expected";
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                if (typeof message.ratio !== "number")
                    return "ratio: number expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.upper != null && message.hasOwnProperty("upper"))
                if (typeof message.upper !== "number")
                    return "upper: number expected";
            if (message.lower != null && message.hasOwnProperty("lower"))
                if (typeof message.lower !== "number")
                    return "lower: number expected";
            if (message.cycle != null && message.hasOwnProperty("cycle"))
                if (!$util.isInteger(message.cycle))
                    return "cycle: integer expected";
            return null;
        };

        /**
         * Creates a WalletInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.WalletInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.WalletInfo} WalletInfo
         */
        WalletInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.WalletInfo)
                return object;
            let message = new $root.jlmp.WalletInfo();
            if (object.currencyNumber != null)
                message.currencyNumber = object.currencyNumber | 0;
            if (object.currencyName != null)
                message.currencyName = String(object.currencyName);
            if (object.currencySymbol != null)
                message.currencySymbol = String(object.currencySymbol);
            if (object.coin != null)
                message.coin = Number(object.coin);
            if (object.bet) {
                if (!Array.isArray(object.bet))
                    throw TypeError(".jlmp.WalletInfo.bet: array expected");
                message.bet = [];
                for (let i = 0; i < object.bet.length; ++i)
                    message.bet[i] = Number(object.bet[i]);
            }
            if (object.unit != null)
                message.unit = Number(object.unit);
            if (object.ratio != null)
                message.ratio = Number(object.ratio);
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.upper != null)
                message.upper = Number(object.upper);
            if (object.lower != null)
                message.lower = Number(object.lower);
            if (object.cycle != null)
                message.cycle = object.cycle | 0;
            return message;
        };

        /**
         * Creates a plain object from a WalletInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.WalletInfo
         * @static
         * @param {jlmp.WalletInfo} message WalletInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WalletInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.bet = [];
            if (options.defaults) {
                object.currencyNumber = 0;
                object.currencyName = "";
                object.currencySymbol = "";
                object.coin = 0;
                object.unit = 0;
                object.ratio = 0;
                object.rate = 0;
                object.upper = 0;
                object.lower = 0;
                object.cycle = 0;
            }
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                object.currencyNumber = message.currencyNumber;
            if (message.currencyName != null && message.hasOwnProperty("currencyName"))
                object.currencyName = message.currencyName;
            if (message.currencySymbol != null && message.hasOwnProperty("currencySymbol"))
                object.currencySymbol = message.currencySymbol;
            if (message.coin != null && message.hasOwnProperty("coin"))
                object.coin = options.json && !isFinite(message.coin) ? String(message.coin) : message.coin;
            if (message.bet && message.bet.length) {
                object.bet = [];
                for (let j = 0; j < message.bet.length; ++j)
                    object.bet[j] = options.json && !isFinite(message.bet[j]) ? String(message.bet[j]) : message.bet[j];
            }
            if (message.unit != null && message.hasOwnProperty("unit"))
                object.unit = options.json && !isFinite(message.unit) ? String(message.unit) : message.unit;
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                object.ratio = options.json && !isFinite(message.ratio) ? String(message.ratio) : message.ratio;
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.upper != null && message.hasOwnProperty("upper"))
                object.upper = options.json && !isFinite(message.upper) ? String(message.upper) : message.upper;
            if (message.lower != null && message.hasOwnProperty("lower"))
                object.lower = options.json && !isFinite(message.lower) ? String(message.lower) : message.lower;
            if (message.cycle != null && message.hasOwnProperty("cycle"))
                object.cycle = message.cycle;
            return object;
        };

        /**
         * Converts this WalletInfo to JSON.
         * @function toJSON
         * @memberof jlmp.WalletInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WalletInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WalletInfo
         * @function getTypeUrl
         * @memberof jlmp.WalletInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WalletInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.WalletInfo";
        };

        return WalletInfo;
    })();

    jlmp.PreferRoundShow = (function() {

        /**
         * Properties of a PreferRoundShow.
         * @memberof jlmp
         * @interface IPreferRoundShow
         * @property {number|null} [BaseRound] PreferRoundShow BaseRound
         * @property {number|null} [SigmaRound] PreferRoundShow SigmaRound
         * @property {number|null} [CV] PreferRoundShow CV
         * @property {number|null} [HR] PreferRoundShow HR
         * @property {number|null} [MGR] PreferRoundShow MGR
         * @property {number|null} [Range] PreferRoundShow Range
         * @property {number|null} [Show] PreferRoundShow Show
         */

        /**
         * Constructs a new PreferRoundShow.
         * @memberof jlmp
         * @classdesc Represents a PreferRoundShow.
         * @implements IPreferRoundShow
         * @constructor
         * @param {jlmp.IPreferRoundShow=} [properties] Properties to set
         */
        function PreferRoundShow(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PreferRoundShow BaseRound.
         * @member {number} BaseRound
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.BaseRound = 0;

        /**
         * PreferRoundShow SigmaRound.
         * @member {number} SigmaRound
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.SigmaRound = 0;

        /**
         * PreferRoundShow CV.
         * @member {number} CV
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.CV = 0;

        /**
         * PreferRoundShow HR.
         * @member {number} HR
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.HR = 0;

        /**
         * PreferRoundShow MGR.
         * @member {number} MGR
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.MGR = 0;

        /**
         * PreferRoundShow Range.
         * @member {number} Range
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.Range = 0;

        /**
         * PreferRoundShow Show.
         * @member {number} Show
         * @memberof jlmp.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.Show = 0;

        /**
         * Creates a new PreferRoundShow instance using the specified properties.
         * @function create
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {jlmp.IPreferRoundShow=} [properties] Properties to set
         * @returns {jlmp.PreferRoundShow} PreferRoundShow instance
         */
        PreferRoundShow.create = function create(properties) {
            return new PreferRoundShow(properties);
        };

        /**
         * Encodes the specified PreferRoundShow message. Does not implicitly {@link jlmp.PreferRoundShow.verify|verify} messages.
         * @function encode
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {jlmp.IPreferRoundShow} message PreferRoundShow message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreferRoundShow.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseRound != null && Object.hasOwnProperty.call(message, "BaseRound"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.BaseRound);
            if (message.SigmaRound != null && Object.hasOwnProperty.call(message, "SigmaRound"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SigmaRound);
            if (message.CV != null && Object.hasOwnProperty.call(message, "CV"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.CV);
            if (message.HR != null && Object.hasOwnProperty.call(message, "HR"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.HR);
            if (message.MGR != null && Object.hasOwnProperty.call(message, "MGR"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.MGR);
            if (message.Range != null && Object.hasOwnProperty.call(message, "Range"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.Range);
            if (message.Show != null && Object.hasOwnProperty.call(message, "Show"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.Show);
            return writer;
        };

        /**
         * Encodes the specified PreferRoundShow message, length delimited. Does not implicitly {@link jlmp.PreferRoundShow.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {jlmp.IPreferRoundShow} message PreferRoundShow message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreferRoundShow.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PreferRoundShow message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.PreferRoundShow} PreferRoundShow
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreferRoundShow.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.PreferRoundShow();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseRound = reader.int32();
                        break;
                    }
                case 2: {
                        message.SigmaRound = reader.int32();
                        break;
                    }
                case 3: {
                        message.CV = reader.double();
                        break;
                    }
                case 4: {
                        message.HR = reader.double();
                        break;
                    }
                case 5: {
                        message.MGR = reader.double();
                        break;
                    }
                case 6: {
                        message.Range = reader.int32();
                        break;
                    }
                case 7: {
                        message.Show = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PreferRoundShow message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.PreferRoundShow} PreferRoundShow
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreferRoundShow.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PreferRoundShow message.
         * @function verify
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PreferRoundShow.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseRound != null && message.hasOwnProperty("BaseRound"))
                if (!$util.isInteger(message.BaseRound))
                    return "BaseRound: integer expected";
            if (message.SigmaRound != null && message.hasOwnProperty("SigmaRound"))
                if (!$util.isInteger(message.SigmaRound))
                    return "SigmaRound: integer expected";
            if (message.CV != null && message.hasOwnProperty("CV"))
                if (typeof message.CV !== "number")
                    return "CV: number expected";
            if (message.HR != null && message.hasOwnProperty("HR"))
                if (typeof message.HR !== "number")
                    return "HR: number expected";
            if (message.MGR != null && message.hasOwnProperty("MGR"))
                if (typeof message.MGR !== "number")
                    return "MGR: number expected";
            if (message.Range != null && message.hasOwnProperty("Range"))
                if (!$util.isInteger(message.Range))
                    return "Range: integer expected";
            if (message.Show != null && message.hasOwnProperty("Show"))
                if (!$util.isInteger(message.Show))
                    return "Show: integer expected";
            return null;
        };

        /**
         * Creates a PreferRoundShow message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.PreferRoundShow} PreferRoundShow
         */
        PreferRoundShow.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.PreferRoundShow)
                return object;
            let message = new $root.jlmp.PreferRoundShow();
            if (object.BaseRound != null)
                message.BaseRound = object.BaseRound | 0;
            if (object.SigmaRound != null)
                message.SigmaRound = object.SigmaRound | 0;
            if (object.CV != null)
                message.CV = Number(object.CV);
            if (object.HR != null)
                message.HR = Number(object.HR);
            if (object.MGR != null)
                message.MGR = Number(object.MGR);
            if (object.Range != null)
                message.Range = object.Range | 0;
            if (object.Show != null)
                message.Show = object.Show | 0;
            return message;
        };

        /**
         * Creates a plain object from a PreferRoundShow message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {jlmp.PreferRoundShow} message PreferRoundShow
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreferRoundShow.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseRound = 0;
                object.SigmaRound = 0;
                object.CV = 0;
                object.HR = 0;
                object.MGR = 0;
                object.Range = 0;
                object.Show = 0;
            }
            if (message.BaseRound != null && message.hasOwnProperty("BaseRound"))
                object.BaseRound = message.BaseRound;
            if (message.SigmaRound != null && message.hasOwnProperty("SigmaRound"))
                object.SigmaRound = message.SigmaRound;
            if (message.CV != null && message.hasOwnProperty("CV"))
                object.CV = options.json && !isFinite(message.CV) ? String(message.CV) : message.CV;
            if (message.HR != null && message.hasOwnProperty("HR"))
                object.HR = options.json && !isFinite(message.HR) ? String(message.HR) : message.HR;
            if (message.MGR != null && message.hasOwnProperty("MGR"))
                object.MGR = options.json && !isFinite(message.MGR) ? String(message.MGR) : message.MGR;
            if (message.Range != null && message.hasOwnProperty("Range"))
                object.Range = message.Range;
            if (message.Show != null && message.hasOwnProperty("Show"))
                object.Show = message.Show;
            return object;
        };

        /**
         * Converts this PreferRoundShow to JSON.
         * @function toJSON
         * @memberof jlmp.PreferRoundShow
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreferRoundShow.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PreferRoundShow
         * @function getTypeUrl
         * @memberof jlmp.PreferRoundShow
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PreferRoundShow.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.PreferRoundShow";
        };

        return PreferRoundShow;
    })();

    jlmp.CheckMallReq = (function() {

        /**
         * Properties of a CheckMallReq.
         * @memberof jlmp
         * @interface ICheckMallReq
         * @property {number|null} [Currency] CheckMallReq Currency
         */

        /**
         * Constructs a new CheckMallReq.
         * @memberof jlmp
         * @classdesc Represents a CheckMallReq.
         * @implements ICheckMallReq
         * @constructor
         * @param {jlmp.ICheckMallReq=} [properties] Properties to set
         */
        function CheckMallReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CheckMallReq Currency.
         * @member {number} Currency
         * @memberof jlmp.CheckMallReq
         * @instance
         */
        CheckMallReq.prototype.Currency = 0;

        /**
         * Creates a new CheckMallReq instance using the specified properties.
         * @function create
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {jlmp.ICheckMallReq=} [properties] Properties to set
         * @returns {jlmp.CheckMallReq} CheckMallReq instance
         */
        CheckMallReq.create = function create(properties) {
            return new CheckMallReq(properties);
        };

        /**
         * Encodes the specified CheckMallReq message. Does not implicitly {@link jlmp.CheckMallReq.verify|verify} messages.
         * @function encode
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {jlmp.ICheckMallReq} message CheckMallReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Currency != null && Object.hasOwnProperty.call(message, "Currency"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Currency);
            return writer;
        };

        /**
         * Encodes the specified CheckMallReq message, length delimited. Does not implicitly {@link jlmp.CheckMallReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {jlmp.ICheckMallReq} message CheckMallReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CheckMallReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.CheckMallReq} CheckMallReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.CheckMallReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Currency = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CheckMallReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.CheckMallReq} CheckMallReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckMallReq message.
         * @function verify
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckMallReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                if (!$util.isInteger(message.Currency))
                    return "Currency: integer expected";
            return null;
        };

        /**
         * Creates a CheckMallReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.CheckMallReq} CheckMallReq
         */
        CheckMallReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.CheckMallReq)
                return object;
            let message = new $root.jlmp.CheckMallReq();
            if (object.Currency != null)
                message.Currency = object.Currency | 0;
            return message;
        };

        /**
         * Creates a plain object from a CheckMallReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {jlmp.CheckMallReq} message CheckMallReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckMallReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.Currency = 0;
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                object.Currency = message.Currency;
            return object;
        };

        /**
         * Converts this CheckMallReq to JSON.
         * @function toJSON
         * @memberof jlmp.CheckMallReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckMallReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CheckMallReq
         * @function getTypeUrl
         * @memberof jlmp.CheckMallReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CheckMallReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.CheckMallReq";
        };

        return CheckMallReq;
    })();

    jlmp.CheckMallAck = (function() {

        /**
         * Properties of a CheckMallAck.
         * @memberof jlmp
         * @interface ICheckMallAck
         * @property {Array.<jlmp.IGameMallSetting>|null} [Settings] CheckMallAck Settings
         * @property {number|null} [Show] CheckMallAck Show
         */

        /**
         * Constructs a new CheckMallAck.
         * @memberof jlmp
         * @classdesc Represents a CheckMallAck.
         * @implements ICheckMallAck
         * @constructor
         * @param {jlmp.ICheckMallAck=} [properties] Properties to set
         */
        function CheckMallAck(properties) {
            this.Settings = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CheckMallAck Settings.
         * @member {Array.<jlmp.IGameMallSetting>} Settings
         * @memberof jlmp.CheckMallAck
         * @instance
         */
        CheckMallAck.prototype.Settings = $util.emptyArray;

        /**
         * CheckMallAck Show.
         * @member {number} Show
         * @memberof jlmp.CheckMallAck
         * @instance
         */
        CheckMallAck.prototype.Show = 0;

        /**
         * Creates a new CheckMallAck instance using the specified properties.
         * @function create
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {jlmp.ICheckMallAck=} [properties] Properties to set
         * @returns {jlmp.CheckMallAck} CheckMallAck instance
         */
        CheckMallAck.create = function create(properties) {
            return new CheckMallAck(properties);
        };

        /**
         * Encodes the specified CheckMallAck message. Does not implicitly {@link jlmp.CheckMallAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {jlmp.ICheckMallAck} message CheckMallAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Settings != null && message.Settings.length)
                for (let i = 0; i < message.Settings.length; ++i)
                    $root.jlmp.GameMallSetting.encode(message.Settings[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Show != null && Object.hasOwnProperty.call(message, "Show"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Show);
            return writer;
        };

        /**
         * Encodes the specified CheckMallAck message, length delimited. Does not implicitly {@link jlmp.CheckMallAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {jlmp.ICheckMallAck} message CheckMallAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CheckMallAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.CheckMallAck} CheckMallAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.CheckMallAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Settings && message.Settings.length))
                            message.Settings = [];
                        message.Settings.push($root.jlmp.GameMallSetting.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Show = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CheckMallAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.CheckMallAck} CheckMallAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckMallAck message.
         * @function verify
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckMallAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Settings != null && message.hasOwnProperty("Settings")) {
                if (!Array.isArray(message.Settings))
                    return "Settings: array expected";
                for (let i = 0; i < message.Settings.length; ++i) {
                    let error = $root.jlmp.GameMallSetting.verify(message.Settings[i]);
                    if (error)
                        return "Settings." + error;
                }
            }
            if (message.Show != null && message.hasOwnProperty("Show"))
                if (!$util.isInteger(message.Show))
                    return "Show: integer expected";
            return null;
        };

        /**
         * Creates a CheckMallAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.CheckMallAck} CheckMallAck
         */
        CheckMallAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.CheckMallAck)
                return object;
            let message = new $root.jlmp.CheckMallAck();
            if (object.Settings) {
                if (!Array.isArray(object.Settings))
                    throw TypeError(".jlmp.CheckMallAck.Settings: array expected");
                message.Settings = [];
                for (let i = 0; i < object.Settings.length; ++i) {
                    if (typeof object.Settings[i] !== "object")
                        throw TypeError(".jlmp.CheckMallAck.Settings: object expected");
                    message.Settings[i] = $root.jlmp.GameMallSetting.fromObject(object.Settings[i]);
                }
            }
            if (object.Show != null)
                message.Show = object.Show | 0;
            return message;
        };

        /**
         * Creates a plain object from a CheckMallAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {jlmp.CheckMallAck} message CheckMallAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckMallAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Settings = [];
            if (options.defaults)
                object.Show = 0;
            if (message.Settings && message.Settings.length) {
                object.Settings = [];
                for (let j = 0; j < message.Settings.length; ++j)
                    object.Settings[j] = $root.jlmp.GameMallSetting.toObject(message.Settings[j], options);
            }
            if (message.Show != null && message.hasOwnProperty("Show"))
                object.Show = message.Show;
            return object;
        };

        /**
         * Converts this CheckMallAck to JSON.
         * @function toJSON
         * @memberof jlmp.CheckMallAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckMallAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CheckMallAck
         * @function getTypeUrl
         * @memberof jlmp.CheckMallAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CheckMallAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.CheckMallAck";
        };

        return CheckMallAck;
    })();

    jlmp.GameMallSetting = (function() {

        /**
         * Properties of a GameMallSetting.
         * @memberof jlmp
         * @interface IGameMallSetting
         * @property {number|null} [GameID] GameMallSetting GameID
         * @property {number|null} [APIID] GameMallSetting APIID
         * @property {number|null} [AlterID] GameMallSetting AlterID
         * @property {number|null} [DescType] GameMallSetting DescType
         * @property {number|null} [DescRounds] GameMallSetting DescRounds
         * @property {number|null} [MaxBet] GameMallSetting MaxBet
         * @property {number|null} [PriceOdd] GameMallSetting PriceOdd
         * @property {boolean|null} [ForSale] GameMallSetting ForSale
         * @property {Array.<number>|null} [BetVec] GameMallSetting BetVec
         * @property {number|null} [ReelNo] GameMallSetting ReelNo
         * @property {number|null} [BetNo] GameMallSetting BetNo
         * @property {boolean|null} [Event] GameMallSetting Event
         */

        /**
         * Constructs a new GameMallSetting.
         * @memberof jlmp
         * @classdesc Represents a GameMallSetting.
         * @implements IGameMallSetting
         * @constructor
         * @param {jlmp.IGameMallSetting=} [properties] Properties to set
         */
        function GameMallSetting(properties) {
            this.BetVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameMallSetting GameID.
         * @member {number} GameID
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.GameID = 0;

        /**
         * GameMallSetting APIID.
         * @member {number} APIID
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.APIID = 0;

        /**
         * GameMallSetting AlterID.
         * @member {number} AlterID
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.AlterID = 0;

        /**
         * GameMallSetting DescType.
         * @member {number} DescType
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.DescType = 0;

        /**
         * GameMallSetting DescRounds.
         * @member {number} DescRounds
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.DescRounds = 0;

        /**
         * GameMallSetting MaxBet.
         * @member {number} MaxBet
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.MaxBet = 0;

        /**
         * GameMallSetting PriceOdd.
         * @member {number} PriceOdd
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.PriceOdd = 0;

        /**
         * GameMallSetting ForSale.
         * @member {boolean} ForSale
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.ForSale = false;

        /**
         * GameMallSetting BetVec.
         * @member {Array.<number>} BetVec
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.BetVec = $util.emptyArray;

        /**
         * GameMallSetting ReelNo.
         * @member {number} ReelNo
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.ReelNo = 0;

        /**
         * GameMallSetting BetNo.
         * @member {number} BetNo
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.BetNo = 0;

        /**
         * GameMallSetting Event.
         * @member {boolean} Event
         * @memberof jlmp.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.Event = false;

        /**
         * Creates a new GameMallSetting instance using the specified properties.
         * @function create
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {jlmp.IGameMallSetting=} [properties] Properties to set
         * @returns {jlmp.GameMallSetting} GameMallSetting instance
         */
        GameMallSetting.create = function create(properties) {
            return new GameMallSetting(properties);
        };

        /**
         * Encodes the specified GameMallSetting message. Does not implicitly {@link jlmp.GameMallSetting.verify|verify} messages.
         * @function encode
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {jlmp.IGameMallSetting} message GameMallSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameMallSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.GameID != null && Object.hasOwnProperty.call(message, "GameID"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.GameID);
            if (message.APIID != null && Object.hasOwnProperty.call(message, "APIID"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.APIID);
            if (message.AlterID != null && Object.hasOwnProperty.call(message, "AlterID"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AlterID);
            if (message.DescType != null && Object.hasOwnProperty.call(message, "DescType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.DescType);
            if (message.DescRounds != null && Object.hasOwnProperty.call(message, "DescRounds"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.DescRounds);
            if (message.MaxBet != null && Object.hasOwnProperty.call(message, "MaxBet"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.MaxBet);
            if (message.PriceOdd != null && Object.hasOwnProperty.call(message, "PriceOdd"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.PriceOdd);
            if (message.ForSale != null && Object.hasOwnProperty.call(message, "ForSale"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.ForSale);
            if (message.BetVec != null && message.BetVec.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.BetVec.length; ++i)
                    writer.double(message.BetVec[i]);
                writer.ldelim();
            }
            if (message.ReelNo != null && Object.hasOwnProperty.call(message, "ReelNo"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.ReelNo);
            if (message.BetNo != null && Object.hasOwnProperty.call(message, "BetNo"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.BetNo);
            if (message.Event != null && Object.hasOwnProperty.call(message, "Event"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.Event);
            return writer;
        };

        /**
         * Encodes the specified GameMallSetting message, length delimited. Does not implicitly {@link jlmp.GameMallSetting.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {jlmp.IGameMallSetting} message GameMallSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameMallSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameMallSetting message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.GameMallSetting} GameMallSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameMallSetting.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.GameMallSetting();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameID = reader.int32();
                        break;
                    }
                case 2: {
                        message.APIID = reader.int32();
                        break;
                    }
                case 3: {
                        message.AlterID = reader.int32();
                        break;
                    }
                case 4: {
                        message.DescType = reader.int32();
                        break;
                    }
                case 5: {
                        message.DescRounds = reader.int32();
                        break;
                    }
                case 6: {
                        message.MaxBet = reader.double();
                        break;
                    }
                case 7: {
                        message.PriceOdd = reader.double();
                        break;
                    }
                case 8: {
                        message.ForSale = reader.bool();
                        break;
                    }
                case 9: {
                        if (!(message.BetVec && message.BetVec.length))
                            message.BetVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BetVec.push(reader.double());
                        } else
                            message.BetVec.push(reader.double());
                        break;
                    }
                case 10: {
                        message.ReelNo = reader.int32();
                        break;
                    }
                case 11: {
                        message.BetNo = reader.int32();
                        break;
                    }
                case 12: {
                        message.Event = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameMallSetting message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.GameMallSetting} GameMallSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameMallSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameMallSetting message.
         * @function verify
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameMallSetting.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.GameID != null && message.hasOwnProperty("GameID"))
                if (!$util.isInteger(message.GameID))
                    return "GameID: integer expected";
            if (message.APIID != null && message.hasOwnProperty("APIID"))
                if (!$util.isInteger(message.APIID))
                    return "APIID: integer expected";
            if (message.AlterID != null && message.hasOwnProperty("AlterID"))
                if (!$util.isInteger(message.AlterID))
                    return "AlterID: integer expected";
            if (message.DescType != null && message.hasOwnProperty("DescType"))
                if (!$util.isInteger(message.DescType))
                    return "DescType: integer expected";
            if (message.DescRounds != null && message.hasOwnProperty("DescRounds"))
                if (!$util.isInteger(message.DescRounds))
                    return "DescRounds: integer expected";
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                if (typeof message.MaxBet !== "number")
                    return "MaxBet: number expected";
            if (message.PriceOdd != null && message.hasOwnProperty("PriceOdd"))
                if (typeof message.PriceOdd !== "number")
                    return "PriceOdd: number expected";
            if (message.ForSale != null && message.hasOwnProperty("ForSale"))
                if (typeof message.ForSale !== "boolean")
                    return "ForSale: boolean expected";
            if (message.BetVec != null && message.hasOwnProperty("BetVec")) {
                if (!Array.isArray(message.BetVec))
                    return "BetVec: array expected";
                for (let i = 0; i < message.BetVec.length; ++i)
                    if (typeof message.BetVec[i] !== "number")
                        return "BetVec: number[] expected";
            }
            if (message.ReelNo != null && message.hasOwnProperty("ReelNo"))
                if (!$util.isInteger(message.ReelNo))
                    return "ReelNo: integer expected";
            if (message.BetNo != null && message.hasOwnProperty("BetNo"))
                if (!$util.isInteger(message.BetNo))
                    return "BetNo: integer expected";
            if (message.Event != null && message.hasOwnProperty("Event"))
                if (typeof message.Event !== "boolean")
                    return "Event: boolean expected";
            return null;
        };

        /**
         * Creates a GameMallSetting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.GameMallSetting} GameMallSetting
         */
        GameMallSetting.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.GameMallSetting)
                return object;
            let message = new $root.jlmp.GameMallSetting();
            if (object.GameID != null)
                message.GameID = object.GameID | 0;
            if (object.APIID != null)
                message.APIID = object.APIID | 0;
            if (object.AlterID != null)
                message.AlterID = object.AlterID | 0;
            if (object.DescType != null)
                message.DescType = object.DescType | 0;
            if (object.DescRounds != null)
                message.DescRounds = object.DescRounds | 0;
            if (object.MaxBet != null)
                message.MaxBet = Number(object.MaxBet);
            if (object.PriceOdd != null)
                message.PriceOdd = Number(object.PriceOdd);
            if (object.ForSale != null)
                message.ForSale = Boolean(object.ForSale);
            if (object.BetVec) {
                if (!Array.isArray(object.BetVec))
                    throw TypeError(".jlmp.GameMallSetting.BetVec: array expected");
                message.BetVec = [];
                for (let i = 0; i < object.BetVec.length; ++i)
                    message.BetVec[i] = Number(object.BetVec[i]);
            }
            if (object.ReelNo != null)
                message.ReelNo = object.ReelNo | 0;
            if (object.BetNo != null)
                message.BetNo = object.BetNo | 0;
            if (object.Event != null)
                message.Event = Boolean(object.Event);
            return message;
        };

        /**
         * Creates a plain object from a GameMallSetting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {jlmp.GameMallSetting} message GameMallSetting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameMallSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BetVec = [];
            if (options.defaults) {
                object.GameID = 0;
                object.APIID = 0;
                object.AlterID = 0;
                object.DescType = 0;
                object.DescRounds = 0;
                object.MaxBet = 0;
                object.PriceOdd = 0;
                object.ForSale = false;
                object.ReelNo = 0;
                object.BetNo = 0;
                object.Event = false;
            }
            if (message.GameID != null && message.hasOwnProperty("GameID"))
                object.GameID = message.GameID;
            if (message.APIID != null && message.hasOwnProperty("APIID"))
                object.APIID = message.APIID;
            if (message.AlterID != null && message.hasOwnProperty("AlterID"))
                object.AlterID = message.AlterID;
            if (message.DescType != null && message.hasOwnProperty("DescType"))
                object.DescType = message.DescType;
            if (message.DescRounds != null && message.hasOwnProperty("DescRounds"))
                object.DescRounds = message.DescRounds;
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                object.MaxBet = options.json && !isFinite(message.MaxBet) ? String(message.MaxBet) : message.MaxBet;
            if (message.PriceOdd != null && message.hasOwnProperty("PriceOdd"))
                object.PriceOdd = options.json && !isFinite(message.PriceOdd) ? String(message.PriceOdd) : message.PriceOdd;
            if (message.ForSale != null && message.hasOwnProperty("ForSale"))
                object.ForSale = message.ForSale;
            if (message.BetVec && message.BetVec.length) {
                object.BetVec = [];
                for (let j = 0; j < message.BetVec.length; ++j)
                    object.BetVec[j] = options.json && !isFinite(message.BetVec[j]) ? String(message.BetVec[j]) : message.BetVec[j];
            }
            if (message.ReelNo != null && message.hasOwnProperty("ReelNo"))
                object.ReelNo = message.ReelNo;
            if (message.BetNo != null && message.hasOwnProperty("BetNo"))
                object.BetNo = message.BetNo;
            if (message.Event != null && message.hasOwnProperty("Event"))
                object.Event = message.Event;
            return object;
        };

        /**
         * Converts this GameMallSetting to JSON.
         * @function toJSON
         * @memberof jlmp.GameMallSetting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameMallSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameMallSetting
         * @function getTypeUrl
         * @memberof jlmp.GameMallSetting
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameMallSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.GameMallSetting";
        };

        return GameMallSetting;
    })();

    jlmp.LoginDataAck = (function() {

        /**
         * Properties of a LoginDataAck.
         * @memberof jlmp
         * @interface ILoginDataAck
         * @property {number|null} [result] LoginDataAck result
         * @property {number|null} [aid] LoginDataAck aid
         */

        /**
         * Constructs a new LoginDataAck.
         * @memberof jlmp
         * @classdesc Represents a LoginDataAck.
         * @implements ILoginDataAck
         * @constructor
         * @param {jlmp.ILoginDataAck=} [properties] Properties to set
         */
        function LoginDataAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginDataAck result.
         * @member {number} result
         * @memberof jlmp.LoginDataAck
         * @instance
         */
        LoginDataAck.prototype.result = 0;

        /**
         * LoginDataAck aid.
         * @member {number} aid
         * @memberof jlmp.LoginDataAck
         * @instance
         */
        LoginDataAck.prototype.aid = 0;

        /**
         * Creates a new LoginDataAck instance using the specified properties.
         * @function create
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {jlmp.ILoginDataAck=} [properties] Properties to set
         * @returns {jlmp.LoginDataAck} LoginDataAck instance
         */
        LoginDataAck.create = function create(properties) {
            return new LoginDataAck(properties);
        };

        /**
         * Encodes the specified LoginDataAck message. Does not implicitly {@link jlmp.LoginDataAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {jlmp.ILoginDataAck} message LoginDataAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.result);
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.aid);
            return writer;
        };

        /**
         * Encodes the specified LoginDataAck message, length delimited. Does not implicitly {@link jlmp.LoginDataAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {jlmp.ILoginDataAck} message LoginDataAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LoginDataAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.LoginDataAck} LoginDataAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.LoginDataAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.result = reader.int32();
                        break;
                    }
                case 2: {
                        message.aid = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoginDataAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.LoginDataAck} LoginDataAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoginDataAck message.
         * @function verify
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoginDataAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            return null;
        };

        /**
         * Creates a LoginDataAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.LoginDataAck} LoginDataAck
         */
        LoginDataAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.LoginDataAck)
                return object;
            let message = new $root.jlmp.LoginDataAck();
            if (object.result != null)
                message.result = object.result | 0;
            if (object.aid != null)
                message.aid = object.aid | 0;
            return message;
        };

        /**
         * Creates a plain object from a LoginDataAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {jlmp.LoginDataAck} message LoginDataAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginDataAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.result = 0;
                object.aid = 0;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            return object;
        };

        /**
         * Converts this LoginDataAck to JSON.
         * @function toJSON
         * @memberof jlmp.LoginDataAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginDataAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginDataAck
         * @function getTypeUrl
         * @memberof jlmp.LoginDataAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginDataAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.LoginDataAck";
        };

        return LoginDataAck;
    })();

    jlmp.LoginDataReq = (function() {

        /**
         * Properties of a LoginDataReq.
         * @memberof jlmp
         * @interface ILoginDataReq
         * @property {string|null} [Token] LoginDataReq Token
         * @property {number|null} [Source] LoginDataReq Source
         * @property {string|null} [OSType] LoginDataReq OSType
         * @property {string|null} [OSVersion] LoginDataReq OSVersion
         * @property {string|null} [Browser] LoginDataReq Browser
         * @property {string|null} [BrowserVersion] LoginDataReq BrowserVersion
         * @property {number|null} [BrowserTag] LoginDataReq BrowserTag
         * @property {string|null} [Machine] LoginDataReq Machine
         * @property {number|null} [Width] LoginDataReq Width
         * @property {number|null} [Height] LoginDataReq Height
         * @property {number|null} [Ratio] LoginDataReq Ratio
         * @property {string|null} [Language] LoginDataReq Language
         * @property {number|null} [Cheat] LoginDataReq Cheat
         */

        /**
         * Constructs a new LoginDataReq.
         * @memberof jlmp
         * @classdesc Represents a LoginDataReq.
         * @implements ILoginDataReq
         * @constructor
         * @param {jlmp.ILoginDataReq=} [properties] Properties to set
         */
        function LoginDataReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginDataReq Token.
         * @member {string} Token
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Token = "";

        /**
         * LoginDataReq Source.
         * @member {number} Source
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Source = 0;

        /**
         * LoginDataReq OSType.
         * @member {string} OSType
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.OSType = "";

        /**
         * LoginDataReq OSVersion.
         * @member {string} OSVersion
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.OSVersion = "";

        /**
         * LoginDataReq Browser.
         * @member {string} Browser
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Browser = "";

        /**
         * LoginDataReq BrowserVersion.
         * @member {string} BrowserVersion
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.BrowserVersion = "";

        /**
         * LoginDataReq BrowserTag.
         * @member {number} BrowserTag
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.BrowserTag = 0;

        /**
         * LoginDataReq Machine.
         * @member {string} Machine
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Machine = "";

        /**
         * LoginDataReq Width.
         * @member {number} Width
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Width = 0;

        /**
         * LoginDataReq Height.
         * @member {number} Height
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Height = 0;

        /**
         * LoginDataReq Ratio.
         * @member {number} Ratio
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Ratio = 0;

        /**
         * LoginDataReq Language.
         * @member {string} Language
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Language = "";

        /**
         * LoginDataReq Cheat.
         * @member {number} Cheat
         * @memberof jlmp.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Cheat = 0;

        /**
         * Creates a new LoginDataReq instance using the specified properties.
         * @function create
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {jlmp.ILoginDataReq=} [properties] Properties to set
         * @returns {jlmp.LoginDataReq} LoginDataReq instance
         */
        LoginDataReq.create = function create(properties) {
            return new LoginDataReq(properties);
        };

        /**
         * Encodes the specified LoginDataReq message. Does not implicitly {@link jlmp.LoginDataReq.verify|verify} messages.
         * @function encode
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {jlmp.ILoginDataReq} message LoginDataReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Token != null && Object.hasOwnProperty.call(message, "Token"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.Token);
            if (message.Source != null && Object.hasOwnProperty.call(message, "Source"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Source);
            if (message.OSType != null && Object.hasOwnProperty.call(message, "OSType"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.OSType);
            if (message.OSVersion != null && Object.hasOwnProperty.call(message, "OSVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.OSVersion);
            if (message.Browser != null && Object.hasOwnProperty.call(message, "Browser"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.Browser);
            if (message.BrowserVersion != null && Object.hasOwnProperty.call(message, "BrowserVersion"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.BrowserVersion);
            if (message.BrowserTag != null && Object.hasOwnProperty.call(message, "BrowserTag"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.BrowserTag);
            if (message.Machine != null && Object.hasOwnProperty.call(message, "Machine"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.Machine);
            if (message.Width != null && Object.hasOwnProperty.call(message, "Width"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.Width);
            if (message.Height != null && Object.hasOwnProperty.call(message, "Height"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.Height);
            if (message.Ratio != null && Object.hasOwnProperty.call(message, "Ratio"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.Ratio);
            if (message.Language != null && Object.hasOwnProperty.call(message, "Language"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.Language);
            if (message.Cheat != null && Object.hasOwnProperty.call(message, "Cheat"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.Cheat);
            return writer;
        };

        /**
         * Encodes the specified LoginDataReq message, length delimited. Does not implicitly {@link jlmp.LoginDataReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {jlmp.ILoginDataReq} message LoginDataReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LoginDataReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.LoginDataReq} LoginDataReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.LoginDataReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Token = reader.string();
                        break;
                    }
                case 2: {
                        message.Source = reader.int32();
                        break;
                    }
                case 3: {
                        message.OSType = reader.string();
                        break;
                    }
                case 4: {
                        message.OSVersion = reader.string();
                        break;
                    }
                case 5: {
                        message.Browser = reader.string();
                        break;
                    }
                case 6: {
                        message.BrowserVersion = reader.string();
                        break;
                    }
                case 7: {
                        message.BrowserTag = reader.int32();
                        break;
                    }
                case 8: {
                        message.Machine = reader.string();
                        break;
                    }
                case 9: {
                        message.Width = reader.int32();
                        break;
                    }
                case 10: {
                        message.Height = reader.int32();
                        break;
                    }
                case 11: {
                        message.Ratio = reader.double();
                        break;
                    }
                case 12: {
                        message.Language = reader.string();
                        break;
                    }
                case 13: {
                        message.Cheat = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoginDataReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.LoginDataReq} LoginDataReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoginDataReq message.
         * @function verify
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoginDataReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Token != null && message.hasOwnProperty("Token"))
                if (!$util.isString(message.Token))
                    return "Token: string expected";
            if (message.Source != null && message.hasOwnProperty("Source"))
                if (!$util.isInteger(message.Source))
                    return "Source: integer expected";
            if (message.OSType != null && message.hasOwnProperty("OSType"))
                if (!$util.isString(message.OSType))
                    return "OSType: string expected";
            if (message.OSVersion != null && message.hasOwnProperty("OSVersion"))
                if (!$util.isString(message.OSVersion))
                    return "OSVersion: string expected";
            if (message.Browser != null && message.hasOwnProperty("Browser"))
                if (!$util.isString(message.Browser))
                    return "Browser: string expected";
            if (message.BrowserVersion != null && message.hasOwnProperty("BrowserVersion"))
                if (!$util.isString(message.BrowserVersion))
                    return "BrowserVersion: string expected";
            if (message.BrowserTag != null && message.hasOwnProperty("BrowserTag"))
                if (!$util.isInteger(message.BrowserTag))
                    return "BrowserTag: integer expected";
            if (message.Machine != null && message.hasOwnProperty("Machine"))
                if (!$util.isString(message.Machine))
                    return "Machine: string expected";
            if (message.Width != null && message.hasOwnProperty("Width"))
                if (!$util.isInteger(message.Width))
                    return "Width: integer expected";
            if (message.Height != null && message.hasOwnProperty("Height"))
                if (!$util.isInteger(message.Height))
                    return "Height: integer expected";
            if (message.Ratio != null && message.hasOwnProperty("Ratio"))
                if (typeof message.Ratio !== "number")
                    return "Ratio: number expected";
            if (message.Language != null && message.hasOwnProperty("Language"))
                if (!$util.isString(message.Language))
                    return "Language: string expected";
            if (message.Cheat != null && message.hasOwnProperty("Cheat"))
                if (!$util.isInteger(message.Cheat))
                    return "Cheat: integer expected";
            return null;
        };

        /**
         * Creates a LoginDataReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.LoginDataReq} LoginDataReq
         */
        LoginDataReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.LoginDataReq)
                return object;
            let message = new $root.jlmp.LoginDataReq();
            if (object.Token != null)
                message.Token = String(object.Token);
            if (object.Source != null)
                message.Source = object.Source | 0;
            if (object.OSType != null)
                message.OSType = String(object.OSType);
            if (object.OSVersion != null)
                message.OSVersion = String(object.OSVersion);
            if (object.Browser != null)
                message.Browser = String(object.Browser);
            if (object.BrowserVersion != null)
                message.BrowserVersion = String(object.BrowserVersion);
            if (object.BrowserTag != null)
                message.BrowserTag = object.BrowserTag | 0;
            if (object.Machine != null)
                message.Machine = String(object.Machine);
            if (object.Width != null)
                message.Width = object.Width | 0;
            if (object.Height != null)
                message.Height = object.Height | 0;
            if (object.Ratio != null)
                message.Ratio = Number(object.Ratio);
            if (object.Language != null)
                message.Language = String(object.Language);
            if (object.Cheat != null)
                message.Cheat = object.Cheat | 0;
            return message;
        };

        /**
         * Creates a plain object from a LoginDataReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {jlmp.LoginDataReq} message LoginDataReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginDataReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Token = "";
                object.Source = 0;
                object.OSType = "";
                object.OSVersion = "";
                object.Browser = "";
                object.BrowserVersion = "";
                object.BrowserTag = 0;
                object.Machine = "";
                object.Width = 0;
                object.Height = 0;
                object.Ratio = 0;
                object.Language = "";
                object.Cheat = 0;
            }
            if (message.Token != null && message.hasOwnProperty("Token"))
                object.Token = message.Token;
            if (message.Source != null && message.hasOwnProperty("Source"))
                object.Source = message.Source;
            if (message.OSType != null && message.hasOwnProperty("OSType"))
                object.OSType = message.OSType;
            if (message.OSVersion != null && message.hasOwnProperty("OSVersion"))
                object.OSVersion = message.OSVersion;
            if (message.Browser != null && message.hasOwnProperty("Browser"))
                object.Browser = message.Browser;
            if (message.BrowserVersion != null && message.hasOwnProperty("BrowserVersion"))
                object.BrowserVersion = message.BrowserVersion;
            if (message.BrowserTag != null && message.hasOwnProperty("BrowserTag"))
                object.BrowserTag = message.BrowserTag;
            if (message.Machine != null && message.hasOwnProperty("Machine"))
                object.Machine = message.Machine;
            if (message.Width != null && message.hasOwnProperty("Width"))
                object.Width = message.Width;
            if (message.Height != null && message.hasOwnProperty("Height"))
                object.Height = message.Height;
            if (message.Ratio != null && message.hasOwnProperty("Ratio"))
                object.Ratio = options.json && !isFinite(message.Ratio) ? String(message.Ratio) : message.Ratio;
            if (message.Language != null && message.hasOwnProperty("Language"))
                object.Language = message.Language;
            if (message.Cheat != null && message.hasOwnProperty("Cheat"))
                object.Cheat = message.Cheat;
            return object;
        };

        /**
         * Converts this LoginDataReq to JSON.
         * @function toJSON
         * @memberof jlmp.LoginDataReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginDataReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginDataReq
         * @function getTypeUrl
         * @memberof jlmp.LoginDataReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginDataReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.LoginDataReq";
        };

        return LoginDataReq;
    })();

    jlmp.LogoutReq = (function() {

        /**
         * Properties of a LogoutReq.
         * @memberof jlmp
         * @interface ILogoutReq
         * @property {number|null} [aid] LogoutReq aid
         */

        /**
         * Constructs a new LogoutReq.
         * @memberof jlmp
         * @classdesc Represents a LogoutReq.
         * @implements ILogoutReq
         * @constructor
         * @param {jlmp.ILogoutReq=} [properties] Properties to set
         */
        function LogoutReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LogoutReq aid.
         * @member {number} aid
         * @memberof jlmp.LogoutReq
         * @instance
         */
        LogoutReq.prototype.aid = 0;

        /**
         * Creates a new LogoutReq instance using the specified properties.
         * @function create
         * @memberof jlmp.LogoutReq
         * @static
         * @param {jlmp.ILogoutReq=} [properties] Properties to set
         * @returns {jlmp.LogoutReq} LogoutReq instance
         */
        LogoutReq.create = function create(properties) {
            return new LogoutReq(properties);
        };

        /**
         * Encodes the specified LogoutReq message. Does not implicitly {@link jlmp.LogoutReq.verify|verify} messages.
         * @function encode
         * @memberof jlmp.LogoutReq
         * @static
         * @param {jlmp.ILogoutReq} message LogoutReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogoutReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            return writer;
        };

        /**
         * Encodes the specified LogoutReq message, length delimited. Does not implicitly {@link jlmp.LogoutReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.LogoutReq
         * @static
         * @param {jlmp.ILogoutReq} message LogoutReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogoutReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LogoutReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.LogoutReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.LogoutReq} LogoutReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogoutReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.LogoutReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LogoutReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.LogoutReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.LogoutReq} LogoutReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogoutReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LogoutReq message.
         * @function verify
         * @memberof jlmp.LogoutReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LogoutReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            return null;
        };

        /**
         * Creates a LogoutReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.LogoutReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.LogoutReq} LogoutReq
         */
        LogoutReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.LogoutReq)
                return object;
            let message = new $root.jlmp.LogoutReq();
            if (object.aid != null)
                message.aid = object.aid | 0;
            return message;
        };

        /**
         * Creates a plain object from a LogoutReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.LogoutReq
         * @static
         * @param {jlmp.LogoutReq} message LogoutReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LogoutReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.aid = 0;
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            return object;
        };

        /**
         * Converts this LogoutReq to JSON.
         * @function toJSON
         * @memberof jlmp.LogoutReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LogoutReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LogoutReq
         * @function getTypeUrl
         * @memberof jlmp.LogoutReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LogoutReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.LogoutReq";
        };

        return LogoutReq;
    })();

    jlmp.HeartAck = (function() {

        /**
         * Properties of a HeartAck.
         * @memberof jlmp
         * @interface IHeartAck
         * @property {number|null} [state] HeartAck state
         * @property {string|null} [message] HeartAck message
         */

        /**
         * Constructs a new HeartAck.
         * @memberof jlmp
         * @classdesc Represents a HeartAck.
         * @implements IHeartAck
         * @constructor
         * @param {jlmp.IHeartAck=} [properties] Properties to set
         */
        function HeartAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HeartAck state.
         * @member {number} state
         * @memberof jlmp.HeartAck
         * @instance
         */
        HeartAck.prototype.state = 0;

        /**
         * HeartAck message.
         * @member {string} message
         * @memberof jlmp.HeartAck
         * @instance
         */
        HeartAck.prototype.message = "";

        /**
         * Creates a new HeartAck instance using the specified properties.
         * @function create
         * @memberof jlmp.HeartAck
         * @static
         * @param {jlmp.IHeartAck=} [properties] Properties to set
         * @returns {jlmp.HeartAck} HeartAck instance
         */
        HeartAck.create = function create(properties) {
            return new HeartAck(properties);
        };

        /**
         * Encodes the specified HeartAck message. Does not implicitly {@link jlmp.HeartAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.HeartAck
         * @static
         * @param {jlmp.IHeartAck} message HeartAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeartAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            return writer;
        };

        /**
         * Encodes the specified HeartAck message, length delimited. Does not implicitly {@link jlmp.HeartAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.HeartAck
         * @static
         * @param {jlmp.IHeartAck} message HeartAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeartAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HeartAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.HeartAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.HeartAck} HeartAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeartAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.HeartAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.state = reader.int32();
                        break;
                    }
                case 2: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HeartAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.HeartAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.HeartAck} HeartAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeartAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HeartAck message.
         * @function verify
         * @memberof jlmp.HeartAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HeartAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a HeartAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.HeartAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.HeartAck} HeartAck
         */
        HeartAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.HeartAck)
                return object;
            let message = new $root.jlmp.HeartAck();
            if (object.state != null)
                message.state = object.state | 0;
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a HeartAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.HeartAck
         * @static
         * @param {jlmp.HeartAck} message HeartAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HeartAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.state = 0;
                object.message = "";
            }
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this HeartAck to JSON.
         * @function toJSON
         * @memberof jlmp.HeartAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HeartAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HeartAck
         * @function getTypeUrl
         * @memberof jlmp.HeartAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HeartAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.HeartAck";
        };

        return HeartAck;
    })();

    jlmp.ExchangeReq = (function() {

        /**
         * Properties of an ExchangeReq.
         * @memberof jlmp
         * @interface IExchangeReq
         * @property {number|null} [aid] ExchangeReq aid
         * @property {string|null} [token] ExchangeReq token
         * @property {string|null} [stamp] ExchangeReq stamp
         */

        /**
         * Constructs a new ExchangeReq.
         * @memberof jlmp
         * @classdesc Represents an ExchangeReq.
         * @implements IExchangeReq
         * @constructor
         * @param {jlmp.IExchangeReq=} [properties] Properties to set
         */
        function ExchangeReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExchangeReq aid.
         * @member {number} aid
         * @memberof jlmp.ExchangeReq
         * @instance
         */
        ExchangeReq.prototype.aid = 0;

        /**
         * ExchangeReq token.
         * @member {string} token
         * @memberof jlmp.ExchangeReq
         * @instance
         */
        ExchangeReq.prototype.token = "";

        /**
         * ExchangeReq stamp.
         * @member {string} stamp
         * @memberof jlmp.ExchangeReq
         * @instance
         */
        ExchangeReq.prototype.stamp = "";

        /**
         * Creates a new ExchangeReq instance using the specified properties.
         * @function create
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {jlmp.IExchangeReq=} [properties] Properties to set
         * @returns {jlmp.ExchangeReq} ExchangeReq instance
         */
        ExchangeReq.create = function create(properties) {
            return new ExchangeReq(properties);
        };

        /**
         * Encodes the specified ExchangeReq message. Does not implicitly {@link jlmp.ExchangeReq.verify|verify} messages.
         * @function encode
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {jlmp.IExchangeReq} message ExchangeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
            if (message.stamp != null && Object.hasOwnProperty.call(message, "stamp"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.stamp);
            return writer;
        };

        /**
         * Encodes the specified ExchangeReq message, length delimited. Does not implicitly {@link jlmp.ExchangeReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {jlmp.IExchangeReq} message ExchangeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ExchangeReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.ExchangeReq} ExchangeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.ExchangeReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                case 2: {
                        message.token = reader.string();
                        break;
                    }
                case 3: {
                        message.stamp = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExchangeReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.ExchangeReq} ExchangeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExchangeReq message.
         * @function verify
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExchangeReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                if (!$util.isString(message.stamp))
                    return "stamp: string expected";
            return null;
        };

        /**
         * Creates an ExchangeReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.ExchangeReq} ExchangeReq
         */
        ExchangeReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.ExchangeReq)
                return object;
            let message = new $root.jlmp.ExchangeReq();
            if (object.aid != null)
                message.aid = object.aid | 0;
            if (object.token != null)
                message.token = String(object.token);
            if (object.stamp != null)
                message.stamp = String(object.stamp);
            return message;
        };

        /**
         * Creates a plain object from an ExchangeReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {jlmp.ExchangeReq} message ExchangeReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExchangeReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.aid = 0;
                object.token = "";
                object.stamp = "";
            }
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                object.stamp = message.stamp;
            return object;
        };

        /**
         * Converts this ExchangeReq to JSON.
         * @function toJSON
         * @memberof jlmp.ExchangeReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExchangeReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExchangeReq
         * @function getTypeUrl
         * @memberof jlmp.ExchangeReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExchangeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.ExchangeReq";
        };

        return ExchangeReq;
    })();

    jlmp.ExchangeAck = (function() {

        /**
         * Properties of an ExchangeAck.
         * @memberof jlmp
         * @interface IExchangeAck
         * @property {Array.<jlmp.IWallet>|null} [wallet] ExchangeAck wallet
         * @property {string|null} [stamp] ExchangeAck stamp
         */

        /**
         * Constructs a new ExchangeAck.
         * @memberof jlmp
         * @classdesc Represents an ExchangeAck.
         * @implements IExchangeAck
         * @constructor
         * @param {jlmp.IExchangeAck=} [properties] Properties to set
         */
        function ExchangeAck(properties) {
            this.wallet = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExchangeAck wallet.
         * @member {Array.<jlmp.IWallet>} wallet
         * @memberof jlmp.ExchangeAck
         * @instance
         */
        ExchangeAck.prototype.wallet = $util.emptyArray;

        /**
         * ExchangeAck stamp.
         * @member {string} stamp
         * @memberof jlmp.ExchangeAck
         * @instance
         */
        ExchangeAck.prototype.stamp = "";

        /**
         * Creates a new ExchangeAck instance using the specified properties.
         * @function create
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {jlmp.IExchangeAck=} [properties] Properties to set
         * @returns {jlmp.ExchangeAck} ExchangeAck instance
         */
        ExchangeAck.create = function create(properties) {
            return new ExchangeAck(properties);
        };

        /**
         * Encodes the specified ExchangeAck message. Does not implicitly {@link jlmp.ExchangeAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {jlmp.IExchangeAck} message ExchangeAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.wallet != null && message.wallet.length)
                for (let i = 0; i < message.wallet.length; ++i)
                    $root.jlmp.Wallet.encode(message.wallet[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.stamp != null && Object.hasOwnProperty.call(message, "stamp"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.stamp);
            return writer;
        };

        /**
         * Encodes the specified ExchangeAck message, length delimited. Does not implicitly {@link jlmp.ExchangeAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {jlmp.IExchangeAck} message ExchangeAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ExchangeAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.ExchangeAck} ExchangeAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.ExchangeAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.wallet && message.wallet.length))
                            message.wallet = [];
                        message.wallet.push($root.jlmp.Wallet.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.stamp = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExchangeAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.ExchangeAck} ExchangeAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExchangeAck message.
         * @function verify
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExchangeAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.wallet != null && message.hasOwnProperty("wallet")) {
                if (!Array.isArray(message.wallet))
                    return "wallet: array expected";
                for (let i = 0; i < message.wallet.length; ++i) {
                    let error = $root.jlmp.Wallet.verify(message.wallet[i]);
                    if (error)
                        return "wallet." + error;
                }
            }
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                if (!$util.isString(message.stamp))
                    return "stamp: string expected";
            return null;
        };

        /**
         * Creates an ExchangeAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.ExchangeAck} ExchangeAck
         */
        ExchangeAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.ExchangeAck)
                return object;
            let message = new $root.jlmp.ExchangeAck();
            if (object.wallet) {
                if (!Array.isArray(object.wallet))
                    throw TypeError(".jlmp.ExchangeAck.wallet: array expected");
                message.wallet = [];
                for (let i = 0; i < object.wallet.length; ++i) {
                    if (typeof object.wallet[i] !== "object")
                        throw TypeError(".jlmp.ExchangeAck.wallet: object expected");
                    message.wallet[i] = $root.jlmp.Wallet.fromObject(object.wallet[i]);
                }
            }
            if (object.stamp != null)
                message.stamp = String(object.stamp);
            return message;
        };

        /**
         * Creates a plain object from an ExchangeAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {jlmp.ExchangeAck} message ExchangeAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExchangeAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.wallet = [];
            if (options.defaults)
                object.stamp = "";
            if (message.wallet && message.wallet.length) {
                object.wallet = [];
                for (let j = 0; j < message.wallet.length; ++j)
                    object.wallet[j] = $root.jlmp.Wallet.toObject(message.wallet[j], options);
            }
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                object.stamp = message.stamp;
            return object;
        };

        /**
         * Converts this ExchangeAck to JSON.
         * @function toJSON
         * @memberof jlmp.ExchangeAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExchangeAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExchangeAck
         * @function getTypeUrl
         * @memberof jlmp.ExchangeAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExchangeAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.ExchangeAck";
        };

        return ExchangeAck;
    })();

    jlmp.Wallet = (function() {

        /**
         * Properties of a Wallet.
         * @memberof jlmp
         * @interface IWallet
         * @property {number|null} [coin] Wallet coin
         * @property {number|null} [currencyNumber] Wallet currencyNumber
         * @property {number|null} [ratio] Wallet ratio
         * @property {number|null} [rate] Wallet rate
         * @property {number|null} [unit] Wallet unit
         */

        /**
         * Constructs a new Wallet.
         * @memberof jlmp
         * @classdesc Represents a Wallet.
         * @implements IWallet
         * @constructor
         * @param {jlmp.IWallet=} [properties] Properties to set
         */
        function Wallet(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Wallet coin.
         * @member {number} coin
         * @memberof jlmp.Wallet
         * @instance
         */
        Wallet.prototype.coin = 0;

        /**
         * Wallet currencyNumber.
         * @member {number} currencyNumber
         * @memberof jlmp.Wallet
         * @instance
         */
        Wallet.prototype.currencyNumber = 0;

        /**
         * Wallet ratio.
         * @member {number} ratio
         * @memberof jlmp.Wallet
         * @instance
         */
        Wallet.prototype.ratio = 0;

        /**
         * Wallet rate.
         * @member {number} rate
         * @memberof jlmp.Wallet
         * @instance
         */
        Wallet.prototype.rate = 0;

        /**
         * Wallet unit.
         * @member {number} unit
         * @memberof jlmp.Wallet
         * @instance
         */
        Wallet.prototype.unit = 0;

        /**
         * Creates a new Wallet instance using the specified properties.
         * @function create
         * @memberof jlmp.Wallet
         * @static
         * @param {jlmp.IWallet=} [properties] Properties to set
         * @returns {jlmp.Wallet} Wallet instance
         */
        Wallet.create = function create(properties) {
            return new Wallet(properties);
        };

        /**
         * Encodes the specified Wallet message. Does not implicitly {@link jlmp.Wallet.verify|verify} messages.
         * @function encode
         * @memberof jlmp.Wallet
         * @static
         * @param {jlmp.IWallet} message Wallet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wallet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.coin);
            if (message.currencyNumber != null && Object.hasOwnProperty.call(message, "currencyNumber"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.currencyNumber);
            if (message.ratio != null && Object.hasOwnProperty.call(message, "ratio"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ratio);
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.rate);
            if (message.unit != null && Object.hasOwnProperty.call(message, "unit"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.unit);
            return writer;
        };

        /**
         * Encodes the specified Wallet message, length delimited. Does not implicitly {@link jlmp.Wallet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.Wallet
         * @static
         * @param {jlmp.IWallet} message Wallet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wallet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Wallet message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.Wallet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.Wallet} Wallet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wallet.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.Wallet();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.coin = reader.double();
                        break;
                    }
                case 2: {
                        message.currencyNumber = reader.int32();
                        break;
                    }
                case 3: {
                        message.ratio = reader.double();
                        break;
                    }
                case 4: {
                        message.rate = reader.double();
                        break;
                    }
                case 5: {
                        message.unit = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Wallet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.Wallet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.Wallet} Wallet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wallet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Wallet message.
         * @function verify
         * @memberof jlmp.Wallet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Wallet.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.coin != null && message.hasOwnProperty("coin"))
                if (typeof message.coin !== "number")
                    return "coin: number expected";
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                if (!$util.isInteger(message.currencyNumber))
                    return "currencyNumber: integer expected";
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                if (typeof message.ratio !== "number")
                    return "ratio: number expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.unit != null && message.hasOwnProperty("unit"))
                if (typeof message.unit !== "number")
                    return "unit: number expected";
            return null;
        };

        /**
         * Creates a Wallet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.Wallet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.Wallet} Wallet
         */
        Wallet.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.Wallet)
                return object;
            let message = new $root.jlmp.Wallet();
            if (object.coin != null)
                message.coin = Number(object.coin);
            if (object.currencyNumber != null)
                message.currencyNumber = object.currencyNumber | 0;
            if (object.ratio != null)
                message.ratio = Number(object.ratio);
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.unit != null)
                message.unit = Number(object.unit);
            return message;
        };

        /**
         * Creates a plain object from a Wallet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.Wallet
         * @static
         * @param {jlmp.Wallet} message Wallet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Wallet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.coin = 0;
                object.currencyNumber = 0;
                object.ratio = 0;
                object.rate = 0;
                object.unit = 0;
            }
            if (message.coin != null && message.hasOwnProperty("coin"))
                object.coin = options.json && !isFinite(message.coin) ? String(message.coin) : message.coin;
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                object.currencyNumber = message.currencyNumber;
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                object.ratio = options.json && !isFinite(message.ratio) ? String(message.ratio) : message.ratio;
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.unit != null && message.hasOwnProperty("unit"))
                object.unit = options.json && !isFinite(message.unit) ? String(message.unit) : message.unit;
            return object;
        };

        /**
         * Converts this Wallet to JSON.
         * @function toJSON
         * @memberof jlmp.Wallet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Wallet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Wallet
         * @function getTypeUrl
         * @memberof jlmp.Wallet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Wallet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.Wallet";
        };

        return Wallet;
    })();

    jlmp.LastReq = (function() {

        /**
         * Properties of a LastReq.
         * @memberof jlmp
         * @interface ILastReq
         * @property {number|null} [aid] LastReq aid
         * @property {string|null} [token] LastReq token
         */

        /**
         * Constructs a new LastReq.
         * @memberof jlmp
         * @classdesc Represents a LastReq.
         * @implements ILastReq
         * @constructor
         * @param {jlmp.ILastReq=} [properties] Properties to set
         */
        function LastReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LastReq aid.
         * @member {number} aid
         * @memberof jlmp.LastReq
         * @instance
         */
        LastReq.prototype.aid = 0;

        /**
         * LastReq token.
         * @member {string} token
         * @memberof jlmp.LastReq
         * @instance
         */
        LastReq.prototype.token = "";

        /**
         * Creates a new LastReq instance using the specified properties.
         * @function create
         * @memberof jlmp.LastReq
         * @static
         * @param {jlmp.ILastReq=} [properties] Properties to set
         * @returns {jlmp.LastReq} LastReq instance
         */
        LastReq.create = function create(properties) {
            return new LastReq(properties);
        };

        /**
         * Encodes the specified LastReq message. Does not implicitly {@link jlmp.LastReq.verify|verify} messages.
         * @function encode
         * @memberof jlmp.LastReq
         * @static
         * @param {jlmp.ILastReq} message LastReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
            return writer;
        };

        /**
         * Encodes the specified LastReq message, length delimited. Does not implicitly {@link jlmp.LastReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.LastReq
         * @static
         * @param {jlmp.ILastReq} message LastReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LastReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.LastReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.LastReq} LastReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.LastReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                case 2: {
                        message.token = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LastReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.LastReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.LastReq} LastReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LastReq message.
         * @function verify
         * @memberof jlmp.LastReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LastReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        /**
         * Creates a LastReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.LastReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.LastReq} LastReq
         */
        LastReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.LastReq)
                return object;
            let message = new $root.jlmp.LastReq();
            if (object.aid != null)
                message.aid = object.aid | 0;
            if (object.token != null)
                message.token = String(object.token);
            return message;
        };

        /**
         * Creates a plain object from a LastReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.LastReq
         * @static
         * @param {jlmp.LastReq} message LastReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LastReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.aid = 0;
                object.token = "";
            }
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        /**
         * Converts this LastReq to JSON.
         * @function toJSON
         * @memberof jlmp.LastReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LastReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LastReq
         * @function getTypeUrl
         * @memberof jlmp.LastReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LastReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.LastReq";
        };

        return LastReq;
    })();

    jlmp.LastSpinAck = (function() {

        /**
         * Properties of a LastSpinAck.
         * @memberof jlmp
         * @interface ILastSpinAck
         * @property {number|null} [result] LastSpinAck result
         * @property {jlmp.ISpinAck|null} [lastSpin] LastSpinAck lastSpin
         */

        /**
         * Constructs a new LastSpinAck.
         * @memberof jlmp
         * @classdesc Represents a LastSpinAck.
         * @implements ILastSpinAck
         * @constructor
         * @param {jlmp.ILastSpinAck=} [properties] Properties to set
         */
        function LastSpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LastSpinAck result.
         * @member {number} result
         * @memberof jlmp.LastSpinAck
         * @instance
         */
        LastSpinAck.prototype.result = 0;

        /**
         * LastSpinAck lastSpin.
         * @member {jlmp.ISpinAck|null|undefined} lastSpin
         * @memberof jlmp.LastSpinAck
         * @instance
         */
        LastSpinAck.prototype.lastSpin = null;

        /**
         * Creates a new LastSpinAck instance using the specified properties.
         * @function create
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {jlmp.ILastSpinAck=} [properties] Properties to set
         * @returns {jlmp.LastSpinAck} LastSpinAck instance
         */
        LastSpinAck.create = function create(properties) {
            return new LastSpinAck(properties);
        };

        /**
         * Encodes the specified LastSpinAck message. Does not implicitly {@link jlmp.LastSpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {jlmp.ILastSpinAck} message LastSpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastSpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.result);
            if (message.lastSpin != null && Object.hasOwnProperty.call(message, "lastSpin"))
                $root.jlmp.SpinAck.encode(message.lastSpin, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified LastSpinAck message, length delimited. Does not implicitly {@link jlmp.LastSpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {jlmp.ILastSpinAck} message LastSpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastSpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LastSpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmp.LastSpinAck} LastSpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastSpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmp.LastSpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.result = reader.int32();
                        break;
                    }
                case 2: {
                        message.lastSpin = $root.jlmp.SpinAck.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LastSpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmp.LastSpinAck} LastSpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastSpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LastSpinAck message.
         * @function verify
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LastSpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin")) {
                let error = $root.jlmp.SpinAck.verify(message.lastSpin);
                if (error)
                    return "lastSpin." + error;
            }
            return null;
        };

        /**
         * Creates a LastSpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmp.LastSpinAck} LastSpinAck
         */
        LastSpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmp.LastSpinAck)
                return object;
            let message = new $root.jlmp.LastSpinAck();
            if (object.result != null)
                message.result = object.result | 0;
            if (object.lastSpin != null) {
                if (typeof object.lastSpin !== "object")
                    throw TypeError(".jlmp.LastSpinAck.lastSpin: object expected");
                message.lastSpin = $root.jlmp.SpinAck.fromObject(object.lastSpin);
            }
            return message;
        };

        /**
         * Creates a plain object from a LastSpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {jlmp.LastSpinAck} message LastSpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LastSpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.result = 0;
                object.lastSpin = null;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin"))
                object.lastSpin = $root.jlmp.SpinAck.toObject(message.lastSpin, options);
            return object;
        };

        /**
         * Converts this LastSpinAck to JSON.
         * @function toJSON
         * @memberof jlmp.LastSpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LastSpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LastSpinAck
         * @function getTypeUrl
         * @memberof jlmp.LastSpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LastSpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmp.LastSpinAck";
        };

        return LastSpinAck;
    })();

    return jlmp;
})();

export const jlmpd = $root.jlmpd = (() => {

    /**
     * Namespace jlmpd.
     * @exports jlmpd
     * @namespace
     */
    const jlmpd = {};

    jlmpd.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlmpd
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlmpd
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlmpd.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlmpd.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlmpd.Column
         * @static
         * @param {jlmpd.IColumn=} [properties] Properties to set
         * @returns {jlmpd.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlmpd.Column.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.Column
         * @static
         * @param {jlmpd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlmpd.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.Column
         * @static
         * @param {jlmpd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlmpd.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.Column)
                return object;
            let message = new $root.jlmpd.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlmpd.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.Column
         * @static
         * @param {jlmpd.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlmpd.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlmpd.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.Column";
        };

        return Column;
    })();

    jlmpd.ColumnM = (function() {

        /**
         * Properties of a ColumnM.
         * @memberof jlmpd
         * @interface IColumnM
         * @property {Array.<number>|null} [Col] ColumnM Col
         */

        /**
         * Constructs a new ColumnM.
         * @memberof jlmpd
         * @classdesc Represents a ColumnM.
         * @implements IColumnM
         * @constructor
         * @param {jlmpd.IColumnM=} [properties] Properties to set
         */
        function ColumnM(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnM Col.
         * @member {Array.<number>} Col
         * @memberof jlmpd.ColumnM
         * @instance
         */
        ColumnM.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnM instance using the specified properties.
         * @function create
         * @memberof jlmpd.ColumnM
         * @static
         * @param {jlmpd.IColumnM=} [properties] Properties to set
         * @returns {jlmpd.ColumnM} ColumnM instance
         */
        ColumnM.create = function create(properties) {
            return new ColumnM(properties);
        };

        /**
         * Encodes the specified ColumnM message. Does not implicitly {@link jlmpd.ColumnM.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.ColumnM
         * @static
         * @param {jlmpd.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnM message, length delimited. Does not implicitly {@link jlmpd.ColumnM.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.ColumnM
         * @static
         * @param {jlmpd.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.ColumnM();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnM message.
         * @function verify
         * @memberof jlmpd.ColumnM
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnM.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnM message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.ColumnM
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.ColumnM} ColumnM
         */
        ColumnM.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.ColumnM)
                return object;
            let message = new $root.jlmpd.ColumnM();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlmpd.ColumnM.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnM message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.ColumnM
         * @static
         * @param {jlmpd.ColumnM} message ColumnM
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnM.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnM to JSON.
         * @function toJSON
         * @memberof jlmpd.ColumnM
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnM.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnM
         * @function getTypeUrl
         * @memberof jlmpd.ColumnM
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnM.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.ColumnM";
        };

        return ColumnM;
    })();

    jlmpd.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlmpd
         * @interface IAwardData
         * @property {number|null} [Atype] AwardData Atype
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Count] AwardData Count
         * @property {Array.<number>|null} [Symbol] AwardData Symbol
         * @property {number|null} [Pos] AwardData Pos
         * @property {Array.<number>|null} [Mul] AwardData Mul
         * @property {number|null} [AllMul] AwardData AllMul
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlmpd
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlmpd.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Symbol = [];
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Atype.
         * @member {number} Atype
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Atype = 0;

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Symbol.
         * @member {Array.<number>} Symbol
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = $util.emptyArray;

        /**
         * AwardData Pos.
         * @member {number} Pos
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Pos = 0;

        /**
         * AwardData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Mul = $util.emptyArray;

        /**
         * AwardData AllMul.
         * @member {number} AllMul
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.AllMul = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlmpd.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlmpd.AwardData
         * @static
         * @param {jlmpd.IAwardData=} [properties] Properties to set
         * @returns {jlmpd.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlmpd.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.AwardData
         * @static
         * @param {jlmpd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Atype != null && Object.hasOwnProperty.call(message, "Atype"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Atype);
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Index);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Count);
            if (message.Symbol != null && message.Symbol.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.Symbol.length; ++i)
                    writer.int32(message.Symbol[i]);
                writer.ldelim();
            }
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.Pos);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.AllMul != null && Object.hasOwnProperty.call(message, "AllMul"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.AllMul);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlmpd.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.AwardData
         * @static
         * @param {jlmpd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Atype = reader.int32();
                        break;
                    }
                case 2: {
                        message.Index = reader.int32();
                        break;
                    }
                case 3: {
                        message.Count = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Symbol.push(reader.int32());
                        } else
                            message.Symbol.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 7: {
                        message.AllMul = reader.double();
                        break;
                    }
                case 8: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlmpd.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                if (!$util.isInteger(message.Atype))
                    return "Atype: integer expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i)
                    if (!$util.isInteger(message.Symbol[i]))
                        return "Symbol: integer[] expected";
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.AllMul != null && message.hasOwnProperty("AllMul"))
                if (typeof message.AllMul !== "number")
                    return "AllMul: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.AwardData)
                return object;
            let message = new $root.jlmpd.AwardData();
            if (object.Atype != null)
                message.Atype = object.Atype | 0;
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmpd.AwardData.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i)
                    message.Symbol[i] = object.Symbol[i] | 0;
            }
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmpd.AwardData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.AllMul != null)
                message.AllMul = Number(object.AllMul);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.AwardData
         * @static
         * @param {jlmpd.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
            }
            if (options.defaults) {
                object.Atype = 0;
                object.Index = 0;
                object.Count = 0;
                object.Pos = 0;
                object.AllMul = 0;
                object.Win = 0;
            }
            if (message.Atype != null && message.hasOwnProperty("Atype"))
                object.Atype = message.Atype;
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = message.Symbol[j];
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.AllMul != null && message.hasOwnProperty("AllMul"))
                object.AllMul = options.json && !isFinite(message.AllMul) ? String(message.AllMul) : message.AllMul;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlmpd.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlmpd.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.AwardData";
        };

        return AwardData;
    })();

    jlmpd.PotSymbol = (function() {

        /**
         * Properties of a PotSymbol.
         * @memberof jlmpd
         * @interface IPotSymbol
         * @property {number|null} [Pos] PotSymbol Pos
         * @property {number|null} [Symbol] PotSymbol Symbol
         * @property {number|null} [Mul] PotSymbol Mul
         */

        /**
         * Constructs a new PotSymbol.
         * @memberof jlmpd
         * @classdesc Represents a PotSymbol.
         * @implements IPotSymbol
         * @constructor
         * @param {jlmpd.IPotSymbol=} [properties] Properties to set
         */
        function PotSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PotSymbol Pos.
         * @member {number} Pos
         * @memberof jlmpd.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Pos = 0;

        /**
         * PotSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlmpd.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Symbol = 0;

        /**
         * PotSymbol Mul.
         * @member {number} Mul
         * @memberof jlmpd.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Mul = 0;

        /**
         * Creates a new PotSymbol instance using the specified properties.
         * @function create
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {jlmpd.IPotSymbol=} [properties] Properties to set
         * @returns {jlmpd.PotSymbol} PotSymbol instance
         */
        PotSymbol.create = function create(properties) {
            return new PotSymbol(properties);
        };

        /**
         * Encodes the specified PotSymbol message. Does not implicitly {@link jlmpd.PotSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {jlmpd.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified PotSymbol message, length delimited. Does not implicitly {@link jlmpd.PotSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {jlmpd.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.PotSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PotSymbol message.
         * @function verify
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PotSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a PotSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.PotSymbol} PotSymbol
         */
        PotSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.PotSymbol)
                return object;
            let message = new $root.jlmpd.PotSymbol();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a PotSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {jlmpd.PotSymbol} message PotSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PotSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this PotSymbol to JSON.
         * @function toJSON
         * @memberof jlmpd.PotSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PotSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PotSymbol
         * @function getTypeUrl
         * @memberof jlmpd.PotSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PotSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.PotSymbol";
        };

        return PotSymbol;
    })();

    jlmpd.Mystery = (function() {

        /**
         * Properties of a Mystery.
         * @memberof jlmpd
         * @interface IMystery
         * @property {boolean|null} [IsJP] Mystery IsJP
         * @property {number|null} [Pos] Mystery Pos
         * @property {number|null} [Symbol] Mystery Symbol
         * @property {number|null} [Mul] Mystery Mul
         */

        /**
         * Constructs a new Mystery.
         * @memberof jlmpd
         * @classdesc Represents a Mystery.
         * @implements IMystery
         * @constructor
         * @param {jlmpd.IMystery=} [properties] Properties to set
         */
        function Mystery(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Mystery IsJP.
         * @member {boolean} IsJP
         * @memberof jlmpd.Mystery
         * @instance
         */
        Mystery.prototype.IsJP = false;

        /**
         * Mystery Pos.
         * @member {number} Pos
         * @memberof jlmpd.Mystery
         * @instance
         */
        Mystery.prototype.Pos = 0;

        /**
         * Mystery Symbol.
         * @member {number} Symbol
         * @memberof jlmpd.Mystery
         * @instance
         */
        Mystery.prototype.Symbol = 0;

        /**
         * Mystery Mul.
         * @member {number} Mul
         * @memberof jlmpd.Mystery
         * @instance
         */
        Mystery.prototype.Mul = 0;

        /**
         * Creates a new Mystery instance using the specified properties.
         * @function create
         * @memberof jlmpd.Mystery
         * @static
         * @param {jlmpd.IMystery=} [properties] Properties to set
         * @returns {jlmpd.Mystery} Mystery instance
         */
        Mystery.create = function create(properties) {
            return new Mystery(properties);
        };

        /**
         * Encodes the specified Mystery message. Does not implicitly {@link jlmpd.Mystery.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.Mystery
         * @static
         * @param {jlmpd.IMystery} message Mystery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Mystery.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.IsJP != null && Object.hasOwnProperty.call(message, "IsJP"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.IsJP);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified Mystery message, length delimited. Does not implicitly {@link jlmpd.Mystery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.Mystery
         * @static
         * @param {jlmpd.IMystery} message Mystery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Mystery.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Mystery message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.Mystery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.Mystery} Mystery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Mystery.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.Mystery();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.IsJP = reader.bool();
                        break;
                    }
                case 2: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Mystery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.Mystery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.Mystery} Mystery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Mystery.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Mystery message.
         * @function verify
         * @memberof jlmpd.Mystery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Mystery.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.IsJP != null && message.hasOwnProperty("IsJP"))
                if (typeof message.IsJP !== "boolean")
                    return "IsJP: boolean expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a Mystery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.Mystery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.Mystery} Mystery
         */
        Mystery.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.Mystery)
                return object;
            let message = new $root.jlmpd.Mystery();
            if (object.IsJP != null)
                message.IsJP = Boolean(object.IsJP);
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a Mystery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.Mystery
         * @static
         * @param {jlmpd.Mystery} message Mystery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Mystery.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.IsJP = false;
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
            }
            if (message.IsJP != null && message.hasOwnProperty("IsJP"))
                object.IsJP = message.IsJP;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this Mystery to JSON.
         * @function toJSON
         * @memberof jlmpd.Mystery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Mystery.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Mystery
         * @function getTypeUrl
         * @memberof jlmpd.Mystery
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Mystery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.Mystery";
        };

        return Mystery;
    })();

    jlmpd.Collect = (function() {

        /**
         * Properties of a Collect.
         * @memberof jlmpd
         * @interface ICollect
         * @property {number|null} [Pos] Collect Pos
         * @property {Array.<number>|null} [CPos] Collect CPos
         * @property {number|null} [Mul] Collect Mul
         * @property {number|null} [SymbolMul] Collect SymbolMul
         */

        /**
         * Constructs a new Collect.
         * @memberof jlmpd
         * @classdesc Represents a Collect.
         * @implements ICollect
         * @constructor
         * @param {jlmpd.ICollect=} [properties] Properties to set
         */
        function Collect(properties) {
            this.CPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Collect Pos.
         * @member {number} Pos
         * @memberof jlmpd.Collect
         * @instance
         */
        Collect.prototype.Pos = 0;

        /**
         * Collect CPos.
         * @member {Array.<number>} CPos
         * @memberof jlmpd.Collect
         * @instance
         */
        Collect.prototype.CPos = $util.emptyArray;

        /**
         * Collect Mul.
         * @member {number} Mul
         * @memberof jlmpd.Collect
         * @instance
         */
        Collect.prototype.Mul = 0;

        /**
         * Collect SymbolMul.
         * @member {number} SymbolMul
         * @memberof jlmpd.Collect
         * @instance
         */
        Collect.prototype.SymbolMul = 0;

        /**
         * Creates a new Collect instance using the specified properties.
         * @function create
         * @memberof jlmpd.Collect
         * @static
         * @param {jlmpd.ICollect=} [properties] Properties to set
         * @returns {jlmpd.Collect} Collect instance
         */
        Collect.create = function create(properties) {
            return new Collect(properties);
        };

        /**
         * Encodes the specified Collect message. Does not implicitly {@link jlmpd.Collect.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.Collect
         * @static
         * @param {jlmpd.ICollect} message Collect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Collect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.CPos != null && message.CPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.CPos.length; ++i)
                    writer.int32(message.CPos[i]);
                writer.ldelim();
            }
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            if (message.SymbolMul != null && Object.hasOwnProperty.call(message, "SymbolMul"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.SymbolMul);
            return writer;
        };

        /**
         * Encodes the specified Collect message, length delimited. Does not implicitly {@link jlmpd.Collect.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.Collect
         * @static
         * @param {jlmpd.ICollect} message Collect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Collect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Collect message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.Collect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.Collect} Collect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Collect.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.Collect();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.CPos && message.CPos.length))
                            message.CPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CPos.push(reader.int32());
                        } else
                            message.CPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                case 4: {
                        message.SymbolMul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Collect message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.Collect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.Collect} Collect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Collect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Collect message.
         * @function verify
         * @memberof jlmpd.Collect
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Collect.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.CPos != null && message.hasOwnProperty("CPos")) {
                if (!Array.isArray(message.CPos))
                    return "CPos: array expected";
                for (let i = 0; i < message.CPos.length; ++i)
                    if (!$util.isInteger(message.CPos[i]))
                        return "CPos: integer[] expected";
            }
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.SymbolMul != null && message.hasOwnProperty("SymbolMul"))
                if (typeof message.SymbolMul !== "number")
                    return "SymbolMul: number expected";
            return null;
        };

        /**
         * Creates a Collect message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.Collect
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.Collect} Collect
         */
        Collect.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.Collect)
                return object;
            let message = new $root.jlmpd.Collect();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.CPos) {
                if (!Array.isArray(object.CPos))
                    throw TypeError(".jlmpd.Collect.CPos: array expected");
                message.CPos = [];
                for (let i = 0; i < object.CPos.length; ++i)
                    message.CPos[i] = object.CPos[i] | 0;
            }
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.SymbolMul != null)
                message.SymbolMul = Number(object.SymbolMul);
            return message;
        };

        /**
         * Creates a plain object from a Collect message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.Collect
         * @static
         * @param {jlmpd.Collect} message Collect
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Collect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CPos = [];
            if (options.defaults) {
                object.Pos = 0;
                object.Mul = 0;
                object.SymbolMul = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.CPos && message.CPos.length) {
                object.CPos = [];
                for (let j = 0; j < message.CPos.length; ++j)
                    object.CPos[j] = message.CPos[j];
            }
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.SymbolMul != null && message.hasOwnProperty("SymbolMul"))
                object.SymbolMul = options.json && !isFinite(message.SymbolMul) ? String(message.SymbolMul) : message.SymbolMul;
            return object;
        };

        /**
         * Converts this Collect to JSON.
         * @function toJSON
         * @memberof jlmpd.Collect
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Collect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Collect
         * @function getTypeUrl
         * @memberof jlmpd.Collect
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Collect.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.Collect";
        };

        return Collect;
    })();

    jlmpd.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlmpd
         * @interface IRoundInfo
         * @property {Array.<jlmpd.IColumn>|null} [Symbol] RoundInfo Symbol
         * @property {Array.<jlmpd.IColumnM>|null} [Mul] RoundInfo Mul
         * @property {Array.<jlmpd.IColumn>|null} [FinalSymbol] RoundInfo FinalSymbol
         * @property {Array.<jlmpd.IColumnM>|null} [FinalMul] RoundInfo FinalMul
         * @property {Array.<jlmpd.ICollect>|null} [CollectList] RoundInfo CollectList
         * @property {Array.<jlmpd.IMystery>|null} [MysteryList] RoundInfo MysteryList
         * @property {Array.<jlmpd.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [PotType] RoundInfo PotType
         * @property {Array.<jlmpd.IPotSymbol>|null} [PotList] RoundInfo PotList
         * @property {number|null} [ReSpinRemainRound] RoundInfo ReSpinRemainRound
         * @property {number|null} [ReSpinTotalRound] RoundInfo ReSpinTotalRound
         * @property {boolean|null} [GetReSpin] RoundInfo GetReSpin
         * @property {boolean|null} [GetAdd] RoundInfo GetAdd
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlmpd
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlmpd.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            this.FinalSymbol = [];
            this.FinalMul = [];
            this.CollectList = [];
            this.MysteryList = [];
            this.AwardDataVec = [];
            this.PotList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo Symbol.
         * @member {Array.<jlmpd.IColumn>} Symbol
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Symbol = $util.emptyArray;

        /**
         * RoundInfo Mul.
         * @member {Array.<jlmpd.IColumnM>} Mul
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mul = $util.emptyArray;

        /**
         * RoundInfo FinalSymbol.
         * @member {Array.<jlmpd.IColumn>} FinalSymbol
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalSymbol = $util.emptyArray;

        /**
         * RoundInfo FinalMul.
         * @member {Array.<jlmpd.IColumnM>} FinalMul
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FinalMul = $util.emptyArray;

        /**
         * RoundInfo CollectList.
         * @member {Array.<jlmpd.ICollect>} CollectList
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CollectList = $util.emptyArray;

        /**
         * RoundInfo MysteryList.
         * @member {Array.<jlmpd.IMystery>} MysteryList
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MysteryList = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlmpd.IAwardData>} AwardDataVec
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo PotType.
         * @member {number} PotType
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PotType = 0;

        /**
         * RoundInfo PotList.
         * @member {Array.<jlmpd.IPotSymbol>} PotList
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PotList = $util.emptyArray;

        /**
         * RoundInfo ReSpinRemainRound.
         * @member {number} ReSpinRemainRound
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ReSpinRemainRound = 0;

        /**
         * RoundInfo ReSpinTotalRound.
         * @member {number} ReSpinTotalRound
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ReSpinTotalRound = 0;

        /**
         * RoundInfo GetReSpin.
         * @member {boolean} GetReSpin
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetReSpin = false;

        /**
         * RoundInfo GetAdd.
         * @member {boolean} GetAdd
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetAdd = false;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlmpd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {jlmpd.IRoundInfo=} [properties] Properties to set
         * @returns {jlmpd.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlmpd.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {jlmpd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlmpd.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length)
                for (let i = 0; i < message.Mul.length; ++i)
                    $root.jlmpd.ColumnM.encode(message.Mul[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FinalSymbol != null && message.FinalSymbol.length)
                for (let i = 0; i < message.FinalSymbol.length; ++i)
                    $root.jlmpd.Column.encode(message.FinalSymbol[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FinalMul != null && message.FinalMul.length)
                for (let i = 0; i < message.FinalMul.length; ++i)
                    $root.jlmpd.ColumnM.encode(message.FinalMul[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.CollectList != null && message.CollectList.length)
                for (let i = 0; i < message.CollectList.length; ++i)
                    $root.jlmpd.Collect.encode(message.CollectList[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.MysteryList != null && message.MysteryList.length)
                for (let i = 0; i < message.MysteryList.length; ++i)
                    $root.jlmpd.Mystery.encode(message.MysteryList[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlmpd.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.PotType != null && Object.hasOwnProperty.call(message, "PotType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.PotType);
            if (message.PotList != null && message.PotList.length)
                for (let i = 0; i < message.PotList.length; ++i)
                    $root.jlmpd.PotSymbol.encode(message.PotList[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.ReSpinRemainRound != null && Object.hasOwnProperty.call(message, "ReSpinRemainRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.ReSpinRemainRound);
            if (message.ReSpinTotalRound != null && Object.hasOwnProperty.call(message, "ReSpinTotalRound"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.ReSpinTotalRound);
            if (message.GetReSpin != null && Object.hasOwnProperty.call(message, "GetReSpin"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.GetReSpin);
            if (message.GetAdd != null && Object.hasOwnProperty.call(message, "GetAdd"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.GetAdd);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlmpd.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {jlmpd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlmpd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        message.Mul.push($root.jlmpd.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FinalSymbol && message.FinalSymbol.length))
                            message.FinalSymbol = [];
                        message.FinalSymbol.push($root.jlmpd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FinalMul && message.FinalMul.length))
                            message.FinalMul = [];
                        message.FinalMul.push($root.jlmpd.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.CollectList && message.CollectList.length))
                            message.CollectList = [];
                        message.CollectList.push($root.jlmpd.Collect.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.MysteryList && message.MysteryList.length))
                            message.MysteryList = [];
                        message.MysteryList.push($root.jlmpd.Mystery.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlmpd.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        message.PotType = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.PotList && message.PotList.length))
                            message.PotList = [];
                        message.PotList.push($root.jlmpd.PotSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.ReSpinRemainRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.ReSpinTotalRound = reader.int32();
                        break;
                    }
                case 13: {
                        message.GetReSpin = reader.bool();
                        break;
                    }
                case 14: {
                        message.GetAdd = reader.bool();
                        break;
                    }
                case 15: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 16: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlmpd.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i) {
                    let error = $root.jlmpd.ColumnM.verify(message.Mul[i]);
                    if (error)
                        return "Mul." + error;
                }
            }
            if (message.FinalSymbol != null && message.hasOwnProperty("FinalSymbol")) {
                if (!Array.isArray(message.FinalSymbol))
                    return "FinalSymbol: array expected";
                for (let i = 0; i < message.FinalSymbol.length; ++i) {
                    let error = $root.jlmpd.Column.verify(message.FinalSymbol[i]);
                    if (error)
                        return "FinalSymbol." + error;
                }
            }
            if (message.FinalMul != null && message.hasOwnProperty("FinalMul")) {
                if (!Array.isArray(message.FinalMul))
                    return "FinalMul: array expected";
                for (let i = 0; i < message.FinalMul.length; ++i) {
                    let error = $root.jlmpd.ColumnM.verify(message.FinalMul[i]);
                    if (error)
                        return "FinalMul." + error;
                }
            }
            if (message.CollectList != null && message.hasOwnProperty("CollectList")) {
                if (!Array.isArray(message.CollectList))
                    return "CollectList: array expected";
                for (let i = 0; i < message.CollectList.length; ++i) {
                    let error = $root.jlmpd.Collect.verify(message.CollectList[i]);
                    if (error)
                        return "CollectList." + error;
                }
            }
            if (message.MysteryList != null && message.hasOwnProperty("MysteryList")) {
                if (!Array.isArray(message.MysteryList))
                    return "MysteryList: array expected";
                for (let i = 0; i < message.MysteryList.length; ++i) {
                    let error = $root.jlmpd.Mystery.verify(message.MysteryList[i]);
                    if (error)
                        return "MysteryList." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlmpd.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.PotType != null && message.hasOwnProperty("PotType"))
                if (!$util.isInteger(message.PotType))
                    return "PotType: integer expected";
            if (message.PotList != null && message.hasOwnProperty("PotList")) {
                if (!Array.isArray(message.PotList))
                    return "PotList: array expected";
                for (let i = 0; i < message.PotList.length; ++i) {
                    let error = $root.jlmpd.PotSymbol.verify(message.PotList[i]);
                    if (error)
                        return "PotList." + error;
                }
            }
            if (message.ReSpinRemainRound != null && message.hasOwnProperty("ReSpinRemainRound"))
                if (!$util.isInteger(message.ReSpinRemainRound))
                    return "ReSpinRemainRound: integer expected";
            if (message.ReSpinTotalRound != null && message.hasOwnProperty("ReSpinTotalRound"))
                if (!$util.isInteger(message.ReSpinTotalRound))
                    return "ReSpinTotalRound: integer expected";
            if (message.GetReSpin != null && message.hasOwnProperty("GetReSpin"))
                if (typeof message.GetReSpin !== "boolean")
                    return "GetReSpin: boolean expected";
            if (message.GetAdd != null && message.hasOwnProperty("GetAdd"))
                if (typeof message.GetAdd !== "boolean")
                    return "GetAdd: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.RoundInfo)
                return object;
            let message = new $root.jlmpd.RoundInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmpd.RoundInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jlmpd.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmpd.RoundInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i) {
                    if (typeof object.Mul[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.Mul: object expected");
                    message.Mul[i] = $root.jlmpd.ColumnM.fromObject(object.Mul[i]);
                }
            }
            if (object.FinalSymbol) {
                if (!Array.isArray(object.FinalSymbol))
                    throw TypeError(".jlmpd.RoundInfo.FinalSymbol: array expected");
                message.FinalSymbol = [];
                for (let i = 0; i < object.FinalSymbol.length; ++i) {
                    if (typeof object.FinalSymbol[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.FinalSymbol: object expected");
                    message.FinalSymbol[i] = $root.jlmpd.Column.fromObject(object.FinalSymbol[i]);
                }
            }
            if (object.FinalMul) {
                if (!Array.isArray(object.FinalMul))
                    throw TypeError(".jlmpd.RoundInfo.FinalMul: array expected");
                message.FinalMul = [];
                for (let i = 0; i < object.FinalMul.length; ++i) {
                    if (typeof object.FinalMul[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.FinalMul: object expected");
                    message.FinalMul[i] = $root.jlmpd.ColumnM.fromObject(object.FinalMul[i]);
                }
            }
            if (object.CollectList) {
                if (!Array.isArray(object.CollectList))
                    throw TypeError(".jlmpd.RoundInfo.CollectList: array expected");
                message.CollectList = [];
                for (let i = 0; i < object.CollectList.length; ++i) {
                    if (typeof object.CollectList[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.CollectList: object expected");
                    message.CollectList[i] = $root.jlmpd.Collect.fromObject(object.CollectList[i]);
                }
            }
            if (object.MysteryList) {
                if (!Array.isArray(object.MysteryList))
                    throw TypeError(".jlmpd.RoundInfo.MysteryList: array expected");
                message.MysteryList = [];
                for (let i = 0; i < object.MysteryList.length; ++i) {
                    if (typeof object.MysteryList[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.MysteryList: object expected");
                    message.MysteryList[i] = $root.jlmpd.Mystery.fromObject(object.MysteryList[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlmpd.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlmpd.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.PotType != null)
                message.PotType = object.PotType | 0;
            if (object.PotList) {
                if (!Array.isArray(object.PotList))
                    throw TypeError(".jlmpd.RoundInfo.PotList: array expected");
                message.PotList = [];
                for (let i = 0; i < object.PotList.length; ++i) {
                    if (typeof object.PotList[i] !== "object")
                        throw TypeError(".jlmpd.RoundInfo.PotList: object expected");
                    message.PotList[i] = $root.jlmpd.PotSymbol.fromObject(object.PotList[i]);
                }
            }
            if (object.ReSpinRemainRound != null)
                message.ReSpinRemainRound = object.ReSpinRemainRound | 0;
            if (object.ReSpinTotalRound != null)
                message.ReSpinTotalRound = object.ReSpinTotalRound | 0;
            if (object.GetReSpin != null)
                message.GetReSpin = Boolean(object.GetReSpin);
            if (object.GetAdd != null)
                message.GetAdd = Boolean(object.GetAdd);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {jlmpd.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
                object.FinalSymbol = [];
                object.FinalMul = [];
                object.CollectList = [];
                object.MysteryList = [];
                object.AwardDataVec = [];
                object.PotList = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.PotType = 0;
                object.ReSpinRemainRound = 0;
                object.ReSpinTotalRound = 0;
                object.GetReSpin = false;
                object.GetAdd = false;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlmpd.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = $root.jlmpd.ColumnM.toObject(message.Mul[j], options);
            }
            if (message.FinalSymbol && message.FinalSymbol.length) {
                object.FinalSymbol = [];
                for (let j = 0; j < message.FinalSymbol.length; ++j)
                    object.FinalSymbol[j] = $root.jlmpd.Column.toObject(message.FinalSymbol[j], options);
            }
            if (message.FinalMul && message.FinalMul.length) {
                object.FinalMul = [];
                for (let j = 0; j < message.FinalMul.length; ++j)
                    object.FinalMul[j] = $root.jlmpd.ColumnM.toObject(message.FinalMul[j], options);
            }
            if (message.CollectList && message.CollectList.length) {
                object.CollectList = [];
                for (let j = 0; j < message.CollectList.length; ++j)
                    object.CollectList[j] = $root.jlmpd.Collect.toObject(message.CollectList[j], options);
            }
            if (message.MysteryList && message.MysteryList.length) {
                object.MysteryList = [];
                for (let j = 0; j < message.MysteryList.length; ++j)
                    object.MysteryList[j] = $root.jlmpd.Mystery.toObject(message.MysteryList[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlmpd.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.PotType != null && message.hasOwnProperty("PotType"))
                object.PotType = message.PotType;
            if (message.PotList && message.PotList.length) {
                object.PotList = [];
                for (let j = 0; j < message.PotList.length; ++j)
                    object.PotList[j] = $root.jlmpd.PotSymbol.toObject(message.PotList[j], options);
            }
            if (message.ReSpinRemainRound != null && message.hasOwnProperty("ReSpinRemainRound"))
                object.ReSpinRemainRound = message.ReSpinRemainRound;
            if (message.ReSpinTotalRound != null && message.hasOwnProperty("ReSpinTotalRound"))
                object.ReSpinTotalRound = message.ReSpinTotalRound;
            if (message.GetReSpin != null && message.hasOwnProperty("GetReSpin"))
                object.GetReSpin = message.GetReSpin;
            if (message.GetAdd != null && message.hasOwnProperty("GetAdd"))
                object.GetAdd = message.GetAdd;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlmpd.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlmpd.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.RoundInfo";
        };

        return RoundInfo;
    })();

    jlmpd.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmpd
         * @interface ISpinAck
         * @property {Array.<jlmpd.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [GameType] SpinAck GameType
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmpd
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmpd.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlmpd.IRoundInfo>} RoundQueue
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck GameType.
         * @member {number} GameType
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.GameType = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck Extra.
         * @member {number} Extra
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlmpd.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmpd.SpinAck
         * @static
         * @param {jlmpd.ISpinAck=} [properties] Properties to set
         * @returns {jlmpd.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmpd.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.SpinAck
         * @static
         * @param {jlmpd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlmpd.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.GameType);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmpd.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.SpinAck
         * @static
         * @param {jlmpd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlmpd.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.Extra = reader.int32();
                        break;
                    }
                case 5: {
                        message.RTP = reader.double();
                        break;
                    }
                case 6: {
                        message.LID = reader.int32();
                        break;
                    }
                case 7: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmpd.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlmpd.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.SpinAck)
                return object;
            let message = new $root.jlmpd.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlmpd.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlmpd.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlmpd.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.SpinAck
         * @static
         * @param {jlmpd.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.GameType = 0;
                object.TotalWin = 0;
                object.Extra = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlmpd.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmpd.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmpd.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.SpinAck";
        };

        return SpinAck;
    })();

    jlmpd.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlmpd
         * @interface IGameInfoData
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlmpd
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlmpd.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlmpd.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {jlmpd.IGameInfoData=} [properties] Properties to set
         * @returns {jlmpd.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlmpd.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {jlmpd.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlmpd.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {jlmpd.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpd.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpd.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpd.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpd.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpd.GameInfoData)
                return object;
            let message = new $root.jlmpd.GameInfoData();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmpd.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {jlmpd.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlmpd.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlmpd.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpd.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlmpd;
})();

export const jlmpt = $root.jlmpt = (() => {

    /**
     * Namespace jlmpt.
     * @exports jlmpt
     * @namespace
     */
    const jlmpt = {};

    jlmpt.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlmpt
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlmpt
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlmpt.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlmpt.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlmpt.Column
         * @static
         * @param {jlmpt.IColumn=} [properties] Properties to set
         * @returns {jlmpt.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlmpt.Column.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.Column
         * @static
         * @param {jlmpt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlmpt.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.Column
         * @static
         * @param {jlmpt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlmpt.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.Column)
                return object;
            let message = new $root.jlmpt.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlmpt.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.Column
         * @static
         * @param {jlmpt.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlmpt.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlmpt.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.Column";
        };

        return Column;
    })();

    jlmpt.ColumnM = (function() {

        /**
         * Properties of a ColumnM.
         * @memberof jlmpt
         * @interface IColumnM
         * @property {Array.<number>|null} [Col] ColumnM Col
         */

        /**
         * Constructs a new ColumnM.
         * @memberof jlmpt
         * @classdesc Represents a ColumnM.
         * @implements IColumnM
         * @constructor
         * @param {jlmpt.IColumnM=} [properties] Properties to set
         */
        function ColumnM(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnM Col.
         * @member {Array.<number>} Col
         * @memberof jlmpt.ColumnM
         * @instance
         */
        ColumnM.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnM instance using the specified properties.
         * @function create
         * @memberof jlmpt.ColumnM
         * @static
         * @param {jlmpt.IColumnM=} [properties] Properties to set
         * @returns {jlmpt.ColumnM} ColumnM instance
         */
        ColumnM.create = function create(properties) {
            return new ColumnM(properties);
        };

        /**
         * Encodes the specified ColumnM message. Does not implicitly {@link jlmpt.ColumnM.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.ColumnM
         * @static
         * @param {jlmpt.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnM message, length delimited. Does not implicitly {@link jlmpt.ColumnM.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.ColumnM
         * @static
         * @param {jlmpt.IColumnM} message ColumnM message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnM.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.ColumnM();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnM message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.ColumnM
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.ColumnM} ColumnM
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnM.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnM message.
         * @function verify
         * @memberof jlmpt.ColumnM
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnM.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnM message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.ColumnM
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.ColumnM} ColumnM
         */
        ColumnM.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.ColumnM)
                return object;
            let message = new $root.jlmpt.ColumnM();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlmpt.ColumnM.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnM message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.ColumnM
         * @static
         * @param {jlmpt.ColumnM} message ColumnM
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnM.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnM to JSON.
         * @function toJSON
         * @memberof jlmpt.ColumnM
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnM.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnM
         * @function getTypeUrl
         * @memberof jlmpt.ColumnM
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnM.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.ColumnM";
        };

        return ColumnM;
    })();

    jlmpt.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlmpt
         * @interface IAwardData
         * @property {number|null} [Index] AwardData Index
         * @property {number|null} [Pos] AwardData Pos
         * @property {Array.<number>|null} [Symbol] AwardData Symbol
         * @property {Array.<number>|null} [Mul] AwardData Mul
         * @property {number|null} [AllMul] AwardData AllMul
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlmpt
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlmpt.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Symbol = [];
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Index.
         * @member {number} Index
         * @memberof jlmpt.AwardData
         * @instance
         */
        AwardData.prototype.Index = 0;

        /**
         * AwardData Pos.
         * @member {number} Pos
         * @memberof jlmpt.AwardData
         * @instance
         */
        AwardData.prototype.Pos = 0;

        /**
         * AwardData Symbol.
         * @member {Array.<number>} Symbol
         * @memberof jlmpt.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = $util.emptyArray;

        /**
         * AwardData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlmpt.AwardData
         * @instance
         */
        AwardData.prototype.Mul = $util.emptyArray;

        /**
         * AwardData AllMul.
         * @member {number} AllMul
         * @memberof jlmpt.AwardData
         * @instance
         */
        AwardData.prototype.AllMul = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlmpt.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlmpt.AwardData
         * @static
         * @param {jlmpt.IAwardData=} [properties] Properties to set
         * @returns {jlmpt.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlmpt.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.AwardData
         * @static
         * @param {jlmpt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Index != null && Object.hasOwnProperty.call(message, "Index"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Index);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Pos);
            if (message.Symbol != null && message.Symbol.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.Symbol.length; ++i)
                    writer.int32(message.Symbol[i]);
                writer.ldelim();
            }
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.AllMul != null && Object.hasOwnProperty.call(message, "AllMul"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.AllMul);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlmpt.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.AwardData
         * @static
         * @param {jlmpt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Index = reader.int32();
                        break;
                    }
                case 2: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Symbol.push(reader.int32());
                        } else
                            message.Symbol.push(reader.int32());
                        break;
                    }
                case 4: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 5: {
                        message.AllMul = reader.double();
                        break;
                    }
                case 6: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlmpt.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Index != null && message.hasOwnProperty("Index"))
                if (!$util.isInteger(message.Index))
                    return "Index: integer expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i)
                    if (!$util.isInteger(message.Symbol[i]))
                        return "Symbol: integer[] expected";
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.AllMul != null && message.hasOwnProperty("AllMul"))
                if (typeof message.AllMul !== "number")
                    return "AllMul: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.AwardData)
                return object;
            let message = new $root.jlmpt.AwardData();
            if (object.Index != null)
                message.Index = object.Index | 0;
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmpt.AwardData.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i)
                    message.Symbol[i] = object.Symbol[i] | 0;
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmpt.AwardData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.AllMul != null)
                message.AllMul = Number(object.AllMul);
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.AwardData
         * @static
         * @param {jlmpt.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
            }
            if (options.defaults) {
                object.Index = 0;
                object.Pos = 0;
                object.AllMul = 0;
                object.Win = 0;
            }
            if (message.Index != null && message.hasOwnProperty("Index"))
                object.Index = message.Index;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = message.Symbol[j];
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.AllMul != null && message.hasOwnProperty("AllMul"))
                object.AllMul = options.json && !isFinite(message.AllMul) ? String(message.AllMul) : message.AllMul;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlmpt.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlmpt.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.AwardData";
        };

        return AwardData;
    })();

    jlmpt.PotSymbol = (function() {

        /**
         * Properties of a PotSymbol.
         * @memberof jlmpt
         * @interface IPotSymbol
         * @property {number|null} [Pos] PotSymbol Pos
         * @property {number|null} [Symbol] PotSymbol Symbol
         * @property {number|null} [Mul] PotSymbol Mul
         */

        /**
         * Constructs a new PotSymbol.
         * @memberof jlmpt
         * @classdesc Represents a PotSymbol.
         * @implements IPotSymbol
         * @constructor
         * @param {jlmpt.IPotSymbol=} [properties] Properties to set
         */
        function PotSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PotSymbol Pos.
         * @member {number} Pos
         * @memberof jlmpt.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Pos = 0;

        /**
         * PotSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlmpt.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Symbol = 0;

        /**
         * PotSymbol Mul.
         * @member {number} Mul
         * @memberof jlmpt.PotSymbol
         * @instance
         */
        PotSymbol.prototype.Mul = 0;

        /**
         * Creates a new PotSymbol instance using the specified properties.
         * @function create
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {jlmpt.IPotSymbol=} [properties] Properties to set
         * @returns {jlmpt.PotSymbol} PotSymbol instance
         */
        PotSymbol.create = function create(properties) {
            return new PotSymbol(properties);
        };

        /**
         * Encodes the specified PotSymbol message. Does not implicitly {@link jlmpt.PotSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {jlmpt.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified PotSymbol message, length delimited. Does not implicitly {@link jlmpt.PotSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {jlmpt.IPotSymbol} message PotSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PotSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.PotSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PotSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.PotSymbol} PotSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PotSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PotSymbol message.
         * @function verify
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PotSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a PotSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.PotSymbol} PotSymbol
         */
        PotSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.PotSymbol)
                return object;
            let message = new $root.jlmpt.PotSymbol();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a PotSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {jlmpt.PotSymbol} message PotSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PotSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this PotSymbol to JSON.
         * @function toJSON
         * @memberof jlmpt.PotSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PotSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PotSymbol
         * @function getTypeUrl
         * @memberof jlmpt.PotSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PotSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.PotSymbol";
        };

        return PotSymbol;
    })();

    jlmpt.Mystery = (function() {

        /**
         * Properties of a Mystery.
         * @memberof jlmpt
         * @interface IMystery
         * @property {boolean|null} [IsJP] Mystery IsJP
         * @property {number|null} [Pos] Mystery Pos
         * @property {number|null} [Symbol] Mystery Symbol
         * @property {number|null} [Mul] Mystery Mul
         */

        /**
         * Constructs a new Mystery.
         * @memberof jlmpt
         * @classdesc Represents a Mystery.
         * @implements IMystery
         * @constructor
         * @param {jlmpt.IMystery=} [properties] Properties to set
         */
        function Mystery(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Mystery IsJP.
         * @member {boolean} IsJP
         * @memberof jlmpt.Mystery
         * @instance
         */
        Mystery.prototype.IsJP = false;

        /**
         * Mystery Pos.
         * @member {number} Pos
         * @memberof jlmpt.Mystery
         * @instance
         */
        Mystery.prototype.Pos = 0;

        /**
         * Mystery Symbol.
         * @member {number} Symbol
         * @memberof jlmpt.Mystery
         * @instance
         */
        Mystery.prototype.Symbol = 0;

        /**
         * Mystery Mul.
         * @member {number} Mul
         * @memberof jlmpt.Mystery
         * @instance
         */
        Mystery.prototype.Mul = 0;

        /**
         * Creates a new Mystery instance using the specified properties.
         * @function create
         * @memberof jlmpt.Mystery
         * @static
         * @param {jlmpt.IMystery=} [properties] Properties to set
         * @returns {jlmpt.Mystery} Mystery instance
         */
        Mystery.create = function create(properties) {
            return new Mystery(properties);
        };

        /**
         * Encodes the specified Mystery message. Does not implicitly {@link jlmpt.Mystery.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.Mystery
         * @static
         * @param {jlmpt.IMystery} message Mystery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Mystery.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.IsJP != null && Object.hasOwnProperty.call(message, "IsJP"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.IsJP);
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Pos);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified Mystery message, length delimited. Does not implicitly {@link jlmpt.Mystery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.Mystery
         * @static
         * @param {jlmpt.IMystery} message Mystery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Mystery.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Mystery message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.Mystery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.Mystery} Mystery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Mystery.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.Mystery();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.IsJP = reader.bool();
                        break;
                    }
                case 2: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Mystery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.Mystery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.Mystery} Mystery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Mystery.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Mystery message.
         * @function verify
         * @memberof jlmpt.Mystery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Mystery.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.IsJP != null && message.hasOwnProperty("IsJP"))
                if (typeof message.IsJP !== "boolean")
                    return "IsJP: boolean expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a Mystery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.Mystery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.Mystery} Mystery
         */
        Mystery.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.Mystery)
                return object;
            let message = new $root.jlmpt.Mystery();
            if (object.IsJP != null)
                message.IsJP = Boolean(object.IsJP);
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a Mystery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.Mystery
         * @static
         * @param {jlmpt.Mystery} message Mystery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Mystery.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.IsJP = false;
                object.Pos = 0;
                object.Symbol = 0;
                object.Mul = 0;
            }
            if (message.IsJP != null && message.hasOwnProperty("IsJP"))
                object.IsJP = message.IsJP;
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this Mystery to JSON.
         * @function toJSON
         * @memberof jlmpt.Mystery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Mystery.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Mystery
         * @function getTypeUrl
         * @memberof jlmpt.Mystery
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Mystery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.Mystery";
        };

        return Mystery;
    })();

    jlmpt.Collect = (function() {

        /**
         * Properties of a Collect.
         * @memberof jlmpt
         * @interface ICollect
         * @property {number|null} [Pos] Collect Pos
         * @property {Array.<number>|null} [CPos] Collect CPos
         * @property {number|null} [Mul] Collect Mul
         * @property {number|null} [Extra] Collect Extra
         */

        /**
         * Constructs a new Collect.
         * @memberof jlmpt
         * @classdesc Represents a Collect.
         * @implements ICollect
         * @constructor
         * @param {jlmpt.ICollect=} [properties] Properties to set
         */
        function Collect(properties) {
            this.CPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Collect Pos.
         * @member {number} Pos
         * @memberof jlmpt.Collect
         * @instance
         */
        Collect.prototype.Pos = 0;

        /**
         * Collect CPos.
         * @member {Array.<number>} CPos
         * @memberof jlmpt.Collect
         * @instance
         */
        Collect.prototype.CPos = $util.emptyArray;

        /**
         * Collect Mul.
         * @member {number} Mul
         * @memberof jlmpt.Collect
         * @instance
         */
        Collect.prototype.Mul = 0;

        /**
         * Collect Extra.
         * @member {number} Extra
         * @memberof jlmpt.Collect
         * @instance
         */
        Collect.prototype.Extra = 0;

        /**
         * Creates a new Collect instance using the specified properties.
         * @function create
         * @memberof jlmpt.Collect
         * @static
         * @param {jlmpt.ICollect=} [properties] Properties to set
         * @returns {jlmpt.Collect} Collect instance
         */
        Collect.create = function create(properties) {
            return new Collect(properties);
        };

        /**
         * Encodes the specified Collect message. Does not implicitly {@link jlmpt.Collect.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.Collect
         * @static
         * @param {jlmpt.ICollect} message Collect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Collect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Pos != null && Object.hasOwnProperty.call(message, "Pos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Pos);
            if (message.CPos != null && message.CPos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.CPos.length; ++i)
                    writer.int32(message.CPos[i]);
                writer.ldelim();
            }
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mul);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified Collect message, length delimited. Does not implicitly {@link jlmpt.Collect.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.Collect
         * @static
         * @param {jlmpt.ICollect} message Collect message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Collect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Collect message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.Collect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.Collect} Collect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Collect.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.Collect();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Pos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.CPos && message.CPos.length))
                            message.CPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.CPos.push(reader.int32());
                        } else
                            message.CPos.push(reader.int32());
                        break;
                    }
                case 3: {
                        message.Mul = reader.double();
                        break;
                    }
                case 4: {
                        message.Extra = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Collect message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.Collect
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.Collect} Collect
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Collect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Collect message.
         * @function verify
         * @memberof jlmpt.Collect
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Collect.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                if (!$util.isInteger(message.Pos))
                    return "Pos: integer expected";
            if (message.CPos != null && message.hasOwnProperty("CPos")) {
                if (!Array.isArray(message.CPos))
                    return "CPos: array expected";
                for (let i = 0; i < message.CPos.length; ++i)
                    if (!$util.isInteger(message.CPos[i]))
                        return "CPos: integer[] expected";
            }
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "number")
                    return "Extra: number expected";
            return null;
        };

        /**
         * Creates a Collect message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.Collect
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.Collect} Collect
         */
        Collect.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.Collect)
                return object;
            let message = new $root.jlmpt.Collect();
            if (object.Pos != null)
                message.Pos = object.Pos | 0;
            if (object.CPos) {
                if (!Array.isArray(object.CPos))
                    throw TypeError(".jlmpt.Collect.CPos: array expected");
                message.CPos = [];
                for (let i = 0; i < object.CPos.length; ++i)
                    message.CPos[i] = object.CPos[i] | 0;
            }
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.Extra != null)
                message.Extra = Number(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a Collect message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.Collect
         * @static
         * @param {jlmpt.Collect} message Collect
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Collect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.CPos = [];
            if (options.defaults) {
                object.Pos = 0;
                object.Mul = 0;
                object.Extra = 0;
            }
            if (message.Pos != null && message.hasOwnProperty("Pos"))
                object.Pos = message.Pos;
            if (message.CPos && message.CPos.length) {
                object.CPos = [];
                for (let j = 0; j < message.CPos.length; ++j)
                    object.CPos[j] = message.CPos[j];
            }
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = options.json && !isFinite(message.Extra) ? String(message.Extra) : message.Extra;
            return object;
        };

        /**
         * Converts this Collect to JSON.
         * @function toJSON
         * @memberof jlmpt.Collect
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Collect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Collect
         * @function getTypeUrl
         * @memberof jlmpt.Collect
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Collect.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.Collect";
        };

        return Collect;
    })();

    jlmpt.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlmpt
         * @interface IComboStageInfo
         * @property {Array.<jlmpt.IColumn>|null} [Symbol] ComboStageInfo Symbol
         * @property {Array.<jlmpt.IColumnM>|null} [Mul] ComboStageInfo Mul
         * @property {Array.<jlmpt.IColumn>|null} [FinalSymbol] ComboStageInfo FinalSymbol
         * @property {Array.<jlmpt.IColumnM>|null} [FinalMul] ComboStageInfo FinalMul
         * @property {Array.<jlmpt.ICollect>|null} [CollectList] ComboStageInfo CollectList
         * @property {Array.<jlmpt.IMystery>|null} [MysteryList] ComboStageInfo MysteryList
         * @property {Array.<jlmpt.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [PotType] ComboStageInfo PotType
         * @property {Array.<jlmpt.IPotSymbol>|null} [PotList] ComboStageInfo PotList
         * @property {number|null} [ReSpinRemainRound] ComboStageInfo ReSpinRemainRound
         * @property {number|null} [ReSpinTotalRound] ComboStageInfo ReSpinTotalRound
         * @property {boolean|null} [GetReSpin] ComboStageInfo GetReSpin
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlmpt
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlmpt.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.Symbol = [];
            this.Mul = [];
            this.FinalSymbol = [];
            this.FinalMul = [];
            this.CollectList = [];
            this.MysteryList = [];
            this.AwardDataVec = [];
            this.PotList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo Symbol.
         * @member {Array.<jlmpt.IColumn>} Symbol
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Symbol = $util.emptyArray;

        /**
         * ComboStageInfo Mul.
         * @member {Array.<jlmpt.IColumnM>} Mul
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Mul = $util.emptyArray;

        /**
         * ComboStageInfo FinalSymbol.
         * @member {Array.<jlmpt.IColumn>} FinalSymbol
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FinalSymbol = $util.emptyArray;

        /**
         * ComboStageInfo FinalMul.
         * @member {Array.<jlmpt.IColumnM>} FinalMul
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FinalMul = $util.emptyArray;

        /**
         * ComboStageInfo CollectList.
         * @member {Array.<jlmpt.ICollect>} CollectList
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.CollectList = $util.emptyArray;

        /**
         * ComboStageInfo MysteryList.
         * @member {Array.<jlmpt.IMystery>} MysteryList
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MysteryList = $util.emptyArray;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlmpt.IAwardData>} AwardDataVec
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo PotType.
         * @member {number} PotType
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.PotType = 0;

        /**
         * ComboStageInfo PotList.
         * @member {Array.<jlmpt.IPotSymbol>} PotList
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.PotList = $util.emptyArray;

        /**
         * ComboStageInfo ReSpinRemainRound.
         * @member {number} ReSpinRemainRound
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ReSpinRemainRound = 0;

        /**
         * ComboStageInfo ReSpinTotalRound.
         * @member {number} ReSpinTotalRound
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ReSpinTotalRound = 0;

        /**
         * ComboStageInfo GetReSpin.
         * @member {boolean} GetReSpin
         * @memberof jlmpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.GetReSpin = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {jlmpt.IComboStageInfo=} [properties] Properties to set
         * @returns {jlmpt.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlmpt.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {jlmpt.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlmpt.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length)
                for (let i = 0; i < message.Mul.length; ++i)
                    $root.jlmpt.ColumnM.encode(message.Mul[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.FinalSymbol != null && message.FinalSymbol.length)
                for (let i = 0; i < message.FinalSymbol.length; ++i)
                    $root.jlmpt.Column.encode(message.FinalSymbol[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FinalMul != null && message.FinalMul.length)
                for (let i = 0; i < message.FinalMul.length; ++i)
                    $root.jlmpt.ColumnM.encode(message.FinalMul[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.CollectList != null && message.CollectList.length)
                for (let i = 0; i < message.CollectList.length; ++i)
                    $root.jlmpt.Collect.encode(message.CollectList[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.MysteryList != null && message.MysteryList.length)
                for (let i = 0; i < message.MysteryList.length; ++i)
                    $root.jlmpt.Mystery.encode(message.MysteryList[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlmpt.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.ComboStageWin);
            if (message.PotType != null && Object.hasOwnProperty.call(message, "PotType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.PotType);
            if (message.PotList != null && message.PotList.length)
                for (let i = 0; i < message.PotList.length; ++i)
                    $root.jlmpt.PotSymbol.encode(message.PotList[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.ReSpinRemainRound != null && Object.hasOwnProperty.call(message, "ReSpinRemainRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.ReSpinRemainRound);
            if (message.ReSpinTotalRound != null && Object.hasOwnProperty.call(message, "ReSpinTotalRound"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.ReSpinTotalRound);
            if (message.GetReSpin != null && Object.hasOwnProperty.call(message, "GetReSpin"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.GetReSpin);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlmpt.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {jlmpt.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlmpt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        message.Mul.push($root.jlmpt.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.FinalSymbol && message.FinalSymbol.length))
                            message.FinalSymbol = [];
                        message.FinalSymbol.push($root.jlmpt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FinalMul && message.FinalMul.length))
                            message.FinalMul = [];
                        message.FinalMul.push($root.jlmpt.ColumnM.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.CollectList && message.CollectList.length))
                            message.CollectList = [];
                        message.CollectList.push($root.jlmpt.Collect.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.MysteryList && message.MysteryList.length))
                            message.MysteryList = [];
                        message.MysteryList.push($root.jlmpt.Mystery.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlmpt.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 9: {
                        message.PotType = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.PotList && message.PotList.length))
                            message.PotList = [];
                        message.PotList.push($root.jlmpt.PotSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.ReSpinRemainRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.ReSpinTotalRound = reader.int32();
                        break;
                    }
                case 13: {
                        message.GetReSpin = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlmpt.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i) {
                    let error = $root.jlmpt.ColumnM.verify(message.Mul[i]);
                    if (error)
                        return "Mul." + error;
                }
            }
            if (message.FinalSymbol != null && message.hasOwnProperty("FinalSymbol")) {
                if (!Array.isArray(message.FinalSymbol))
                    return "FinalSymbol: array expected";
                for (let i = 0; i < message.FinalSymbol.length; ++i) {
                    let error = $root.jlmpt.Column.verify(message.FinalSymbol[i]);
                    if (error)
                        return "FinalSymbol." + error;
                }
            }
            if (message.FinalMul != null && message.hasOwnProperty("FinalMul")) {
                if (!Array.isArray(message.FinalMul))
                    return "FinalMul: array expected";
                for (let i = 0; i < message.FinalMul.length; ++i) {
                    let error = $root.jlmpt.ColumnM.verify(message.FinalMul[i]);
                    if (error)
                        return "FinalMul." + error;
                }
            }
            if (message.CollectList != null && message.hasOwnProperty("CollectList")) {
                if (!Array.isArray(message.CollectList))
                    return "CollectList: array expected";
                for (let i = 0; i < message.CollectList.length; ++i) {
                    let error = $root.jlmpt.Collect.verify(message.CollectList[i]);
                    if (error)
                        return "CollectList." + error;
                }
            }
            if (message.MysteryList != null && message.hasOwnProperty("MysteryList")) {
                if (!Array.isArray(message.MysteryList))
                    return "MysteryList: array expected";
                for (let i = 0; i < message.MysteryList.length; ++i) {
                    let error = $root.jlmpt.Mystery.verify(message.MysteryList[i]);
                    if (error)
                        return "MysteryList." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlmpt.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.PotType != null && message.hasOwnProperty("PotType"))
                if (!$util.isInteger(message.PotType))
                    return "PotType: integer expected";
            if (message.PotList != null && message.hasOwnProperty("PotList")) {
                if (!Array.isArray(message.PotList))
                    return "PotList: array expected";
                for (let i = 0; i < message.PotList.length; ++i) {
                    let error = $root.jlmpt.PotSymbol.verify(message.PotList[i]);
                    if (error)
                        return "PotList." + error;
                }
            }
            if (message.ReSpinRemainRound != null && message.hasOwnProperty("ReSpinRemainRound"))
                if (!$util.isInteger(message.ReSpinRemainRound))
                    return "ReSpinRemainRound: integer expected";
            if (message.ReSpinTotalRound != null && message.hasOwnProperty("ReSpinTotalRound"))
                if (!$util.isInteger(message.ReSpinTotalRound))
                    return "ReSpinTotalRound: integer expected";
            if (message.GetReSpin != null && message.hasOwnProperty("GetReSpin"))
                if (typeof message.GetReSpin !== "boolean")
                    return "GetReSpin: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.ComboStageInfo)
                return object;
            let message = new $root.jlmpt.ComboStageInfo();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlmpt.ComboStageInfo.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.Symbol: object expected");
                    message.Symbol[i] = $root.jlmpt.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmpt.ComboStageInfo.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i) {
                    if (typeof object.Mul[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.Mul: object expected");
                    message.Mul[i] = $root.jlmpt.ColumnM.fromObject(object.Mul[i]);
                }
            }
            if (object.FinalSymbol) {
                if (!Array.isArray(object.FinalSymbol))
                    throw TypeError(".jlmpt.ComboStageInfo.FinalSymbol: array expected");
                message.FinalSymbol = [];
                for (let i = 0; i < object.FinalSymbol.length; ++i) {
                    if (typeof object.FinalSymbol[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.FinalSymbol: object expected");
                    message.FinalSymbol[i] = $root.jlmpt.Column.fromObject(object.FinalSymbol[i]);
                }
            }
            if (object.FinalMul) {
                if (!Array.isArray(object.FinalMul))
                    throw TypeError(".jlmpt.ComboStageInfo.FinalMul: array expected");
                message.FinalMul = [];
                for (let i = 0; i < object.FinalMul.length; ++i) {
                    if (typeof object.FinalMul[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.FinalMul: object expected");
                    message.FinalMul[i] = $root.jlmpt.ColumnM.fromObject(object.FinalMul[i]);
                }
            }
            if (object.CollectList) {
                if (!Array.isArray(object.CollectList))
                    throw TypeError(".jlmpt.ComboStageInfo.CollectList: array expected");
                message.CollectList = [];
                for (let i = 0; i < object.CollectList.length; ++i) {
                    if (typeof object.CollectList[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.CollectList: object expected");
                    message.CollectList[i] = $root.jlmpt.Collect.fromObject(object.CollectList[i]);
                }
            }
            if (object.MysteryList) {
                if (!Array.isArray(object.MysteryList))
                    throw TypeError(".jlmpt.ComboStageInfo.MysteryList: array expected");
                message.MysteryList = [];
                for (let i = 0; i < object.MysteryList.length; ++i) {
                    if (typeof object.MysteryList[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.MysteryList: object expected");
                    message.MysteryList[i] = $root.jlmpt.Mystery.fromObject(object.MysteryList[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlmpt.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlmpt.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.PotType != null)
                message.PotType = object.PotType | 0;
            if (object.PotList) {
                if (!Array.isArray(object.PotList))
                    throw TypeError(".jlmpt.ComboStageInfo.PotList: array expected");
                message.PotList = [];
                for (let i = 0; i < object.PotList.length; ++i) {
                    if (typeof object.PotList[i] !== "object")
                        throw TypeError(".jlmpt.ComboStageInfo.PotList: object expected");
                    message.PotList[i] = $root.jlmpt.PotSymbol.fromObject(object.PotList[i]);
                }
            }
            if (object.ReSpinRemainRound != null)
                message.ReSpinRemainRound = object.ReSpinRemainRound | 0;
            if (object.ReSpinTotalRound != null)
                message.ReSpinTotalRound = object.ReSpinTotalRound | 0;
            if (object.GetReSpin != null)
                message.GetReSpin = Boolean(object.GetReSpin);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {jlmpt.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.Mul = [];
                object.FinalSymbol = [];
                object.FinalMul = [];
                object.CollectList = [];
                object.MysteryList = [];
                object.AwardDataVec = [];
                object.PotList = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.PotType = 0;
                object.ReSpinRemainRound = 0;
                object.ReSpinTotalRound = 0;
                object.GetReSpin = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlmpt.Column.toObject(message.Symbol[j], options);
            }
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = $root.jlmpt.ColumnM.toObject(message.Mul[j], options);
            }
            if (message.FinalSymbol && message.FinalSymbol.length) {
                object.FinalSymbol = [];
                for (let j = 0; j < message.FinalSymbol.length; ++j)
                    object.FinalSymbol[j] = $root.jlmpt.Column.toObject(message.FinalSymbol[j], options);
            }
            if (message.FinalMul && message.FinalMul.length) {
                object.FinalMul = [];
                for (let j = 0; j < message.FinalMul.length; ++j)
                    object.FinalMul[j] = $root.jlmpt.ColumnM.toObject(message.FinalMul[j], options);
            }
            if (message.CollectList && message.CollectList.length) {
                object.CollectList = [];
                for (let j = 0; j < message.CollectList.length; ++j)
                    object.CollectList[j] = $root.jlmpt.Collect.toObject(message.CollectList[j], options);
            }
            if (message.MysteryList && message.MysteryList.length) {
                object.MysteryList = [];
                for (let j = 0; j < message.MysteryList.length; ++j)
                    object.MysteryList[j] = $root.jlmpt.Mystery.toObject(message.MysteryList[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlmpt.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.PotType != null && message.hasOwnProperty("PotType"))
                object.PotType = message.PotType;
            if (message.PotList && message.PotList.length) {
                object.PotList = [];
                for (let j = 0; j < message.PotList.length; ++j)
                    object.PotList[j] = $root.jlmpt.PotSymbol.toObject(message.PotList[j], options);
            }
            if (message.ReSpinRemainRound != null && message.hasOwnProperty("ReSpinRemainRound"))
                object.ReSpinRemainRound = message.ReSpinRemainRound;
            if (message.ReSpinTotalRound != null && message.hasOwnProperty("ReSpinTotalRound"))
                object.ReSpinTotalRound = message.ReSpinTotalRound;
            if (message.GetReSpin != null && message.hasOwnProperty("GetReSpin"))
                object.GetReSpin = message.GetReSpin;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlmpt.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlmpt.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlmpt.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmpt
         * @interface ISpinAck
         * @property {Array.<jlmpt.IComboStageInfo>|null} [RoundInfo] SpinAck RoundInfo
         * @property {number|null} [GameType] SpinAck GameType
         * @property {number|null} [ReelType] SpinAck ReelType
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Extra] SpinAck Extra
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmpt
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmpt.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundInfo.
         * @member {Array.<jlmpt.IComboStageInfo>} RoundInfo
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundInfo = $util.emptyArray;

        /**
         * SpinAck GameType.
         * @member {number} GameType
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.GameType = 0;

        /**
         * SpinAck ReelType.
         * @member {number} ReelType
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.ReelType = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {number} Extra
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlmpt.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmpt.SpinAck
         * @static
         * @param {jlmpt.ISpinAck=} [properties] Properties to set
         * @returns {jlmpt.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmpt.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.SpinAck
         * @static
         * @param {jlmpt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundInfo != null && message.RoundInfo.length)
                for (let i = 0; i < message.RoundInfo.length; ++i)
                    $root.jlmpt.ComboStageInfo.encode(message.RoundInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.GameType != null && Object.hasOwnProperty.call(message, "GameType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.GameType);
            if (message.ReelType != null && Object.hasOwnProperty.call(message, "ReelType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ReelType);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.Extra);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmpt.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.SpinAck
         * @static
         * @param {jlmpt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundInfo && message.RoundInfo.length))
                            message.RoundInfo = [];
                        message.RoundInfo.push($root.jlmpt.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.GameType = reader.int32();
                        break;
                    }
                case 3: {
                        message.ReelType = reader.int32();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.Extra = reader.int32();
                        break;
                    }
                case 10: {
                        message.LID = reader.int32();
                        break;
                    }
                case 11: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmpt.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundInfo != null && message.hasOwnProperty("RoundInfo")) {
                if (!Array.isArray(message.RoundInfo))
                    return "RoundInfo: array expected";
                for (let i = 0; i < message.RoundInfo.length; ++i) {
                    let error = $root.jlmpt.ComboStageInfo.verify(message.RoundInfo[i]);
                    if (error)
                        return "RoundInfo." + error;
                }
            }
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                if (!$util.isInteger(message.GameType))
                    return "GameType: integer expected";
            if (message.ReelType != null && message.hasOwnProperty("ReelType"))
                if (!$util.isInteger(message.ReelType))
                    return "ReelType: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.SpinAck)
                return object;
            let message = new $root.jlmpt.SpinAck();
            if (object.RoundInfo) {
                if (!Array.isArray(object.RoundInfo))
                    throw TypeError(".jlmpt.SpinAck.RoundInfo: array expected");
                message.RoundInfo = [];
                for (let i = 0; i < object.RoundInfo.length; ++i) {
                    if (typeof object.RoundInfo[i] !== "object")
                        throw TypeError(".jlmpt.SpinAck.RoundInfo: object expected");
                    message.RoundInfo[i] = $root.jlmpt.ComboStageInfo.fromObject(object.RoundInfo[i]);
                }
            }
            if (object.GameType != null)
                message.GameType = object.GameType | 0;
            if (object.ReelType != null)
                message.ReelType = object.ReelType | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.SpinAck
         * @static
         * @param {jlmpt.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundInfo = [];
            if (options.defaults) {
                object.GameType = 0;
                object.ReelType = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundInfo && message.RoundInfo.length) {
                object.RoundInfo = [];
                for (let j = 0; j < message.RoundInfo.length; ++j)
                    object.RoundInfo[j] = $root.jlmpt.ComboStageInfo.toObject(message.RoundInfo[j], options);
            }
            if (message.GameType != null && message.hasOwnProperty("GameType"))
                object.GameType = message.GameType;
            if (message.ReelType != null && message.hasOwnProperty("ReelType"))
                object.ReelType = message.ReelType;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmpt.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmpt.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.SpinAck";
        };

        return SpinAck;
    })();

    jlmpt.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlmpt
         * @interface IGameInfoData
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlmpt
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlmpt.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlmpt.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {jlmpt.IGameInfoData=} [properties] Properties to set
         * @returns {jlmpt.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlmpt.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {jlmpt.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlmpt.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {jlmpt.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmpt.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmpt.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmpt.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmpt.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmpt.GameInfoData)
                return object;
            let message = new $root.jlmpt.GameInfoData();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlmpt.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {jlmpt.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlmpt.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlmpt.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmpt.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlmpt;
})();

export const jlmt = $root.jlmt = (() => {

    /**
     * Namespace jlmt.
     * @exports jlmt
     * @namespace
     */
    const jlmt = {};

    jlmt.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlmt
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlmt
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlmt.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlmt.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlmt.Column
         * @static
         * @param {jlmt.IColumn=} [properties] Properties to set
         * @returns {jlmt.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlmt.Column.verify|verify} messages.
         * @function encode
         * @memberof jlmt.Column
         * @static
         * @param {jlmt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlmt.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmt.Column
         * @static
         * @param {jlmt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlmt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmt.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlmt.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmt.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmt.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmt.Column)
                return object;
            let message = new $root.jlmt.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlmt.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmt.Column
         * @static
         * @param {jlmt.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlmt.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlmt.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmt.Column";
        };

        return Column;
    })();

    jlmt.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlmt
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlmt
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlmt.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlmt.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlmt.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlmt.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlmt.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlmt.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlmt.AwardData
         * @static
         * @param {jlmt.IAwardData=} [properties] Properties to set
         * @returns {jlmt.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlmt.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlmt.AwardData
         * @static
         * @param {jlmt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlmt.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmt.AwardData
         * @static
         * @param {jlmt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlmt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmt.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlmt.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmt.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmt.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmt.AwardData)
                return object;
            let message = new $root.jlmt.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlmt.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmt.AwardData
         * @static
         * @param {jlmt.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlmt.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlmt.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmt.AwardData";
        };

        return AwardData;
    })();

    jlmt.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlmt
         * @interface IRoundInfo
         * @property {Array.<jlmt.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlmt.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {number|null} [AddMult] RoundInfo AddMult
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {boolean|null} [HasMR] RoundInfo HasMR
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlmt
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlmt.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlmt.IColumn>} PlateSymbol
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlmt.IAwardData>} AwardDataVec
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo AddMult.
         * @member {number} AddMult
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddMult = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo HasMR.
         * @member {boolean} HasMR
         * @memberof jlmt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.HasMR = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlmt.RoundInfo
         * @static
         * @param {jlmt.IRoundInfo=} [properties] Properties to set
         * @returns {jlmt.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlmt.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlmt.RoundInfo
         * @static
         * @param {jlmt.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlmt.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlmt.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeTotalRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Mult);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AddRound);
            if (message.AddMult != null && Object.hasOwnProperty.call(message, "AddMult"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.AddMult);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.ScatterCount);
            if (message.HasMR != null && Object.hasOwnProperty.call(message, "HasMR"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.HasMR);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlmt.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmt.RoundInfo
         * @static
         * @param {jlmt.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlmt.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmt.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmt.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlmt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlmt.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.Mult = reader.double();
                        break;
                    }
                case 8: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.AddMult = reader.double();
                        break;
                    }
                case 10: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 11: {
                        message.HasMR = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmt.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmt.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlmt.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlmt.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlmt.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.AddMult != null && message.hasOwnProperty("AddMult"))
                if (typeof message.AddMult !== "number")
                    return "AddMult: number expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.HasMR != null && message.hasOwnProperty("HasMR"))
                if (typeof message.HasMR !== "boolean")
                    return "HasMR: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmt.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmt.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmt.RoundInfo)
                return object;
            let message = new $root.jlmt.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlmt.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlmt.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlmt.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlmt.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlmt.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlmt.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.AddMult != null)
                message.AddMult = Number(object.AddMult);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.HasMR != null)
                message.HasMR = Boolean(object.HasMR);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmt.RoundInfo
         * @static
         * @param {jlmt.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
                object.RoundWin = 0;
                object.Mult = 0;
                object.AddRound = 0;
                object.AddMult = 0;
                object.ScatterCount = 0;
                object.HasMR = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlmt.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlmt.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.AddMult != null && message.hasOwnProperty("AddMult"))
                object.AddMult = options.json && !isFinite(message.AddMult) ? String(message.AddMult) : message.AddMult;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.HasMR != null && message.hasOwnProperty("HasMR"))
                object.HasMR = message.HasMR;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlmt.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlmt.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmt.RoundInfo";
        };

        return RoundInfo;
    })();

    jlmt.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlmt
         * @interface ISpinAck
         * @property {Array.<jlmt.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlmt
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlmt.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlmt.IRoundInfo>} RoundQueue
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlmt.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlmt.SpinAck
         * @static
         * @param {jlmt.ISpinAck=} [properties] Properties to set
         * @returns {jlmt.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlmt.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlmt.SpinAck
         * @static
         * @param {jlmt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlmt.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlmt.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmt.SpinAck
         * @static
         * @param {jlmt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmt.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlmt.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlmt.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlmt.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmt.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmt.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmt.SpinAck)
                return object;
            let message = new $root.jlmt.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlmt.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlmt.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlmt.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmt.SpinAck
         * @static
         * @param {jlmt.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlmt.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlmt.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlmt.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmt.SpinAck";
        };

        return SpinAck;
    })();

    jlmt.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlmt
         * @interface IGameInfoAck
         * @property {Array.<boolean>|null} [ShowExtra] GameInfoAck ShowExtra
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlmt
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlmt.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.ShowExtra = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck ShowExtra.
         * @member {Array.<boolean>} ShowExtra
         * @memberof jlmt.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.ShowExtra = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {jlmt.IGameInfoAck=} [properties] Properties to set
         * @returns {jlmt.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlmt.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {jlmt.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && message.ShowExtra.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.ShowExtra.length; ++i)
                    writer.bool(message.ShowExtra[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlmt.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {jlmt.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlmt.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlmt.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 7: {
                        if (!(message.ShowExtra && message.ShowExtra.length))
                            message.ShowExtra = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ShowExtra.push(reader.bool());
                        } else
                            message.ShowExtra.push(reader.bool());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlmt.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra")) {
                if (!Array.isArray(message.ShowExtra))
                    return "ShowExtra: array expected";
                for (let i = 0; i < message.ShowExtra.length; ++i)
                    if (typeof message.ShowExtra[i] !== "boolean")
                        return "ShowExtra: boolean[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlmt.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlmt.GameInfoAck)
                return object;
            let message = new $root.jlmt.GameInfoAck();
            if (object.ShowExtra) {
                if (!Array.isArray(object.ShowExtra))
                    throw TypeError(".jlmt.GameInfoAck.ShowExtra: array expected");
                message.ShowExtra = [];
                for (let i = 0; i < object.ShowExtra.length; ++i)
                    message.ShowExtra[i] = Boolean(object.ShowExtra[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {jlmt.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ShowExtra = [];
            if (message.ShowExtra && message.ShowExtra.length) {
                object.ShowExtra = [];
                for (let j = 0; j < message.ShowExtra.length; ++j)
                    object.ShowExtra[j] = message.ShowExtra[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlmt.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlmt.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlmt.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlmt;
})();

export const jlnc = $root.jlnc = (() => {

    /**
     * Namespace jlnc.
     * @exports jlnc
     * @namespace
     */
    const jlnc = {};

    /**
     * Symbol enum.
     * @name jlnc.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     * @property {number} WildExtend=11 WildExtend value
     * @property {number} WildMonkey=12 WildMonkey value
     */
    jlnc.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        values[valuesById[11] = "WildExtend"] = 11;
        values[valuesById[12] = "WildMonkey"] = 12;
        return values;
    })();

    jlnc.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlnc
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlnc
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlnc.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlnc.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlnc.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlnc.Block
         * @static
         * @param {jlnc.IBlock=} [properties] Properties to set
         * @returns {jlnc.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlnc.Block.verify|verify} messages.
         * @function encode
         * @memberof jlnc.Block
         * @static
         * @param {jlnc.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlnc.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnc.Block
         * @static
         * @param {jlnc.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlnc.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnc.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnc.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnc.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnc.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlnc.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnc.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnc.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnc.Block)
                return object;
            let message = new $root.jlnc.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnc.Block
         * @static
         * @param {jlnc.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlnc.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlnc.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnc.Block";
        };

        return Block;
    })();

    jlnc.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlnc
         * @interface IPlate
         * @property {Array.<jlnc.IColumnData>|null} [column] Plate column
         * @property {Array.<jlnc.Plate.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         * @property {number|null} [bonusSymbol] Plate bonusSymbol
         * @property {number|null} [bonusWin] Plate bonusWin
         * @property {number|null} [bonusReelCount] Plate bonusReelCount
         */

        /**
         * Constructs a new Plate.
         * @memberof jlnc
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlnc.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlnc.IColumnData>} column
         * @memberof jlnc.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jlnc.Plate.IAwardReel>} award
         * @memberof jlnc.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlnc.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate bonusSymbol.
         * @member {number} bonusSymbol
         * @memberof jlnc.Plate
         * @instance
         */
        Plate.prototype.bonusSymbol = 0;

        /**
         * Plate bonusWin.
         * @member {number} bonusWin
         * @memberof jlnc.Plate
         * @instance
         */
        Plate.prototype.bonusWin = 0;

        /**
         * Plate bonusReelCount.
         * @member {number} bonusReelCount
         * @memberof jlnc.Plate
         * @instance
         */
        Plate.prototype.bonusReelCount = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlnc.Plate
         * @static
         * @param {jlnc.IPlate=} [properties] Properties to set
         * @returns {jlnc.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlnc.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlnc.Plate
         * @static
         * @param {jlnc.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlnc.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlnc.Plate.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.bonusSymbol != null && Object.hasOwnProperty.call(message, "bonusSymbol"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.bonusSymbol);
            if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
            if (message.bonusReelCount != null && Object.hasOwnProperty.call(message, "bonusReelCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.bonusReelCount);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlnc.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnc.Plate
         * @static
         * @param {jlnc.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlnc.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnc.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnc.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlnc.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlnc.Plate.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.bonusSymbol = reader.int32();
                        break;
                    }
                case 5: {
                        message.bonusWin = reader.double();
                        break;
                    }
                case 6: {
                        message.bonusReelCount = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnc.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnc.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlnc.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlnc.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlnc.Plate.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.bonusSymbol != null && message.hasOwnProperty("bonusSymbol"))
                if (!$util.isInteger(message.bonusSymbol))
                    return "bonusSymbol: integer expected";
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                if (typeof message.bonusWin !== "number")
                    return "bonusWin: number expected";
            if (message.bonusReelCount != null && message.hasOwnProperty("bonusReelCount"))
                if (!$util.isInteger(message.bonusReelCount))
                    return "bonusReelCount: integer expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnc.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnc.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnc.Plate)
                return object;
            let message = new $root.jlnc.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlnc.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlnc.Plate.column: object expected");
                    message.column[i] = $root.jlnc.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlnc.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlnc.Plate.award: object expected");
                    message.award[i] = $root.jlnc.Plate.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.bonusSymbol != null)
                message.bonusSymbol = object.bonusSymbol | 0;
            if (object.bonusWin != null)
                message.bonusWin = Number(object.bonusWin);
            if (object.bonusReelCount != null)
                message.bonusReelCount = object.bonusReelCount >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnc.Plate
         * @static
         * @param {jlnc.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.bonusSymbol = 0;
                object.bonusWin = 0;
                object.bonusReelCount = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlnc.ColumnData.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlnc.Plate.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.bonusSymbol != null && message.hasOwnProperty("bonusSymbol"))
                object.bonusSymbol = message.bonusSymbol;
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
            if (message.bonusReelCount != null && message.hasOwnProperty("bonusReelCount"))
                object.bonusReelCount = message.bonusReelCount;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlnc.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlnc.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnc.Plate";
        };

        Plate.AwardReel = (function() {

            /**
             * Properties of an AwardReel.
             * @memberof jlnc.Plate
             * @interface IAwardReel
             * @property {number|null} [symbol] AwardReel symbol
             * @property {number|null} [column] AwardReel column
             * @property {number|null} [win] AwardReel win
             * @property {number|null} [count] AwardReel count
             */

            /**
             * Constructs a new AwardReel.
             * @memberof jlnc.Plate
             * @classdesc Represents an AwardReel.
             * @implements IAwardReel
             * @constructor
             * @param {jlnc.Plate.IAwardReel=} [properties] Properties to set
             */
            function AwardReel(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null)
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AwardReel symbol.
             * @member {number} symbol
             * @memberof jlnc.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.symbol = 0;

            /**
             * AwardReel column.
             * @member {number} column
             * @memberof jlnc.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.column = 0;

            /**
             * AwardReel win.
             * @member {number} win
             * @memberof jlnc.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.win = 0;

            /**
             * AwardReel count.
             * @member {number} count
             * @memberof jlnc.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.count = 0;

            /**
             * Creates a new AwardReel instance using the specified properties.
             * @function create
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {jlnc.Plate.IAwardReel=} [properties] Properties to set
             * @returns {jlnc.Plate.AwardReel} AwardReel instance
             */
            AwardReel.create = function create(properties) {
                return new AwardReel(properties);
            };

            /**
             * Encodes the specified AwardReel message. Does not implicitly {@link jlnc.Plate.AwardReel.verify|verify} messages.
             * @function encode
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {jlnc.Plate.IAwardReel} message AwardReel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AwardReel.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
                if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
                if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.count);
                return writer;
            };

            /**
             * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlnc.Plate.AwardReel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {jlnc.Plate.IAwardReel} message AwardReel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AwardReel message from the specified reader or buffer.
             * @function decode
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {jlnc.Plate.AwardReel} AwardReel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AwardReel.decode = function decode(reader, length, error) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnc.Plate.AwardReel();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.symbol = reader.int32();
                            break;
                        }
                    case 2: {
                            message.column = reader.uint32();
                            break;
                        }
                    case 3: {
                            message.win = reader.double();
                            break;
                        }
                    case 4: {
                            message.count = reader.uint32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AwardReel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {jlnc.Plate.AwardReel} AwardReel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AwardReel.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AwardReel message.
             * @function verify
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AwardReel.verify = function verify(message) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (message.symbol != null && message.hasOwnProperty("symbol"))
                    if (!$util.isInteger(message.symbol))
                        return "symbol: integer expected";
                if (message.column != null && message.hasOwnProperty("column"))
                    if (!$util.isInteger(message.column))
                        return "column: integer expected";
                if (message.win != null && message.hasOwnProperty("win"))
                    if (typeof message.win !== "number")
                        return "win: number expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            /**
             * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {jlnc.Plate.AwardReel} AwardReel
             */
            AwardReel.fromObject = function fromObject(object) {
                if (object instanceof $root.jlnc.Plate.AwardReel)
                    return object;
                let message = new $root.jlnc.Plate.AwardReel();
                if (object.symbol != null)
                    message.symbol = object.symbol | 0;
                if (object.column != null)
                    message.column = object.column >>> 0;
                if (object.win != null)
                    message.win = Number(object.win);
                if (object.count != null)
                    message.count = object.count >>> 0;
                return message;
            };

            /**
             * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {jlnc.Plate.AwardReel} message AwardReel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AwardReel.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.symbol = 0;
                    object.column = 0;
                    object.win = 0;
                    object.count = 0;
                }
                if (message.symbol != null && message.hasOwnProperty("symbol"))
                    object.symbol = message.symbol;
                if (message.column != null && message.hasOwnProperty("column"))
                    object.column = message.column;
                if (message.win != null && message.hasOwnProperty("win"))
                    object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            /**
             * Converts this AwardReel to JSON.
             * @function toJSON
             * @memberof jlnc.Plate.AwardReel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AwardReel.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AwardReel
             * @function getTypeUrl
             * @memberof jlnc.Plate.AwardReel
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/jlnc.Plate.AwardReel";
            };

            return AwardReel;
        })();

        return Plate;
    })();

    jlnc.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlnc
         * @interface IAllPlate
         * @property {Array.<jlnc.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlnc
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlnc.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlnc.IPlate>} plate
         * @memberof jlnc.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlnc.AllPlate
         * @static
         * @param {jlnc.IAllPlate=} [properties] Properties to set
         * @returns {jlnc.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlnc.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlnc.AllPlate
         * @static
         * @param {jlnc.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlnc.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlnc.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnc.AllPlate
         * @static
         * @param {jlnc.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlnc.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnc.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnc.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlnc.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnc.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnc.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlnc.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlnc.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnc.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnc.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnc.AllPlate)
                return object;
            let message = new $root.jlnc.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlnc.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlnc.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlnc.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnc.AllPlate
         * @static
         * @param {jlnc.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlnc.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlnc.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlnc.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnc.AllPlate";
        };

        return AllPlate;
    })();

    jlnc.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlnc
         * @interface IColumnData
         * @property {Array.<jlnc.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlnc
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlnc.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jlnc.ICell>} row
         * @memberof jlnc.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlnc.ColumnData
         * @static
         * @param {jlnc.IColumnData=} [properties] Properties to set
         * @returns {jlnc.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlnc.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlnc.ColumnData
         * @static
         * @param {jlnc.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jlnc.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlnc.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnc.ColumnData
         * @static
         * @param {jlnc.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlnc.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnc.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnc.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jlnc.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnc.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnc.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlnc.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jlnc.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnc.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnc.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnc.ColumnData)
                return object;
            let message = new $root.jlnc.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlnc.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jlnc.ColumnData.row: object expected");
                    message.row[i] = $root.jlnc.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnc.ColumnData
         * @static
         * @param {jlnc.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jlnc.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlnc.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlnc.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnc.ColumnData";
        };

        return ColumnData;
    })();

    jlnc.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlnc
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [value] Cell value
         * @property {boolean|null} [isConnect] Cell isConnect
         */

        /**
         * Constructs a new Cell.
         * @memberof jlnc
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlnc.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlnc.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell value.
         * @member {number} value
         * @memberof jlnc.Cell
         * @instance
         */
        Cell.prototype.value = 0;

        /**
         * Cell isConnect.
         * @member {boolean} isConnect
         * @memberof jlnc.Cell
         * @instance
         */
        Cell.prototype.isConnect = false;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlnc.Cell
         * @static
         * @param {jlnc.ICell=} [properties] Properties to set
         * @returns {jlnc.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlnc.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlnc.Cell
         * @static
         * @param {jlnc.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isConnect);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlnc.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnc.Cell
         * @static
         * @param {jlnc.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlnc.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnc.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnc.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.value = reader.double();
                        break;
                    }
                case 3: {
                        message.isConnect = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnc.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnc.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlnc.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (typeof message.value !== "number")
                    return "value: number expected";
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnc.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnc.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnc.Cell)
                return object;
            let message = new $root.jlnc.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.value != null)
                message.value = Number(object.value);
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnc.Cell
         * @static
         * @param {jlnc.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.value = 0;
                object.isConnect = false;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = options.json && !isFinite(message.value) ? String(message.value) : message.value;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlnc.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlnc.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnc.Cell";
        };

        return Cell;
    })();

    return jlnc;
})();

export const jlnf = $root.jlnf = (() => {

    /**
     * Namespace jlnf.
     * @exports jlnf
     * @namespace
     */
    const jlnf = {};

    jlnf.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlnf
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlnf
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlnf.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlnf.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlnf.Column
         * @static
         * @param {jlnf.IColumn=} [properties] Properties to set
         * @returns {jlnf.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlnf.Column.verify|verify} messages.
         * @function encode
         * @memberof jlnf.Column
         * @static
         * @param {jlnf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlnf.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnf.Column
         * @static
         * @param {jlnf.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlnf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnf.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnf.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnf.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlnf.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnf.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnf.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnf.Column)
                return object;
            let message = new $root.jlnf.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlnf.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnf.Column
         * @static
         * @param {jlnf.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlnf.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlnf.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnf.Column";
        };

        return Column;
    })();

    jlnf.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlnf
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlnf
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlnf.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlnf.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlnf.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlnf.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlnf.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlnf.AwardData
         * @static
         * @param {jlnf.IAwardData=} [properties] Properties to set
         * @returns {jlnf.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlnf.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlnf.AwardData
         * @static
         * @param {jlnf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlnf.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnf.AwardData
         * @static
         * @param {jlnf.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlnf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnf.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnf.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnf.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlnf.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnf.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnf.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnf.AwardData)
                return object;
            let message = new $root.jlnf.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnf.AwardData
         * @static
         * @param {jlnf.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlnf.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlnf.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnf.AwardData";
        };

        return AwardData;
    })();

    jlnf.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlnf
         * @interface IRoundInfo
         * @property {Array.<jlnf.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlnf.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {Array.<number>|null} [TransformSymbols] RoundInfo TransformSymbols
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<jlnf.IColumn>|null} [PlateSymbolReal] RoundInfo PlateSymbolReal
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlnf
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlnf.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.TransformSymbols = [];
            this.PlateSymbolReal = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlnf.IColumn>} PlateSymbol
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlnf.IAwardData>} AwardDataVec
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo TransformSymbols.
         * @member {Array.<number>} TransformSymbols
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.TransformSymbols = $util.emptyArray;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo PlateSymbolReal.
         * @member {Array.<jlnf.IColumn>} PlateSymbolReal
         * @memberof jlnf.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolReal = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlnf.RoundInfo
         * @static
         * @param {jlnf.IRoundInfo=} [properties] Properties to set
         * @returns {jlnf.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlnf.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlnf.RoundInfo
         * @static
         * @param {jlnf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlnf.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlnf.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.TransformSymbols != null && message.TransformSymbols.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.TransformSymbols.length; ++i)
                    writer.int32(message.TransformSymbols[i]);
                writer.ldelim();
            }
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.PlateSymbolReal != null && message.PlateSymbolReal.length)
                for (let i = 0; i < message.PlateSymbolReal.length; ++i)
                    $root.jlnf.Column.encode(message.PlateSymbolReal[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlnf.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnf.RoundInfo
         * @static
         * @param {jlnf.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlnf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnf.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlnf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlnf.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.TransformSymbols && message.TransformSymbols.length))
                            message.TransformSymbols = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.TransformSymbols.push(reader.int32());
                        } else
                            message.TransformSymbols.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        if (!(message.PlateSymbolReal && message.PlateSymbolReal.length))
                            message.PlateSymbolReal = [];
                        message.PlateSymbolReal.push($root.jlnf.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnf.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnf.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlnf.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlnf.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlnf.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.TransformSymbols != null && message.hasOwnProperty("TransformSymbols")) {
                if (!Array.isArray(message.TransformSymbols))
                    return "TransformSymbols: array expected";
                for (let i = 0; i < message.TransformSymbols.length; ++i)
                    if (!$util.isInteger(message.TransformSymbols[i]))
                        return "TransformSymbols: integer[] expected";
            }
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.PlateSymbolReal != null && message.hasOwnProperty("PlateSymbolReal")) {
                if (!Array.isArray(message.PlateSymbolReal))
                    return "PlateSymbolReal: array expected";
                for (let i = 0; i < message.PlateSymbolReal.length; ++i) {
                    let error = $root.jlnf.Column.verify(message.PlateSymbolReal[i]);
                    if (error)
                        return "PlateSymbolReal." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnf.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnf.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnf.RoundInfo)
                return object;
            let message = new $root.jlnf.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlnf.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlnf.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlnf.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlnf.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlnf.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlnf.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.TransformSymbols) {
                if (!Array.isArray(object.TransformSymbols))
                    throw TypeError(".jlnf.RoundInfo.TransformSymbols: array expected");
                message.TransformSymbols = [];
                for (let i = 0; i < object.TransformSymbols.length; ++i)
                    message.TransformSymbols[i] = object.TransformSymbols[i] | 0;
            }
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.PlateSymbolReal) {
                if (!Array.isArray(object.PlateSymbolReal))
                    throw TypeError(".jlnf.RoundInfo.PlateSymbolReal: array expected");
                message.PlateSymbolReal = [];
                for (let i = 0; i < object.PlateSymbolReal.length; ++i) {
                    if (typeof object.PlateSymbolReal[i] !== "object")
                        throw TypeError(".jlnf.RoundInfo.PlateSymbolReal: object expected");
                    message.PlateSymbolReal[i] = $root.jlnf.Column.fromObject(object.PlateSymbolReal[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnf.RoundInfo
         * @static
         * @param {jlnf.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.TransformSymbols = [];
                object.PlateSymbolReal = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlnf.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlnf.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.TransformSymbols && message.TransformSymbols.length) {
                object.TransformSymbols = [];
                for (let j = 0; j < message.TransformSymbols.length; ++j)
                    object.TransformSymbols[j] = message.TransformSymbols[j];
            }
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.PlateSymbolReal && message.PlateSymbolReal.length) {
                object.PlateSymbolReal = [];
                for (let j = 0; j < message.PlateSymbolReal.length; ++j)
                    object.PlateSymbolReal[j] = $root.jlnf.Column.toObject(message.PlateSymbolReal[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlnf.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlnf.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnf.RoundInfo";
        };

        return RoundInfo;
    })();

    jlnf.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlnf
         * @interface ISpinAck
         * @property {Array.<jlnf.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {Array.<number>|null} [FreeMysColumns] SpinAck FreeMysColumns
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlnf
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlnf.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.FreeMysColumns = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlnf.IRoundInfo>} RoundQueue
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck FreeMysColumns.
         * @member {Array.<number>} FreeMysColumns
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeMysColumns = $util.emptyArray;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlnf.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlnf.SpinAck
         * @static
         * @param {jlnf.ISpinAck=} [properties] Properties to set
         * @returns {jlnf.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlnf.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlnf.SpinAck
         * @static
         * @param {jlnf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlnf.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.FreeMysColumns != null && message.FreeMysColumns.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.FreeMysColumns.length; ++i)
                    writer.int32(message.FreeMysColumns[i]);
                writer.ldelim();
            }
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Extra);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlnf.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnf.SpinAck
         * @static
         * @param {jlnf.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlnf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnf.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlnf.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.FreeMysColumns && message.FreeMysColumns.length))
                            message.FreeMysColumns = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FreeMysColumns.push(reader.int32());
                        } else
                            message.FreeMysColumns.push(reader.int32());
                        break;
                    }
                case 4: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnf.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnf.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlnf.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlnf.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.FreeMysColumns != null && message.hasOwnProperty("FreeMysColumns")) {
                if (!Array.isArray(message.FreeMysColumns))
                    return "FreeMysColumns: array expected";
                for (let i = 0; i < message.FreeMysColumns.length; ++i)
                    if (!$util.isInteger(message.FreeMysColumns[i]))
                        return "FreeMysColumns: integer[] expected";
            }
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnf.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnf.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnf.SpinAck)
                return object;
            let message = new $root.jlnf.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlnf.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlnf.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlnf.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.FreeMysColumns) {
                if (!Array.isArray(object.FreeMysColumns))
                    throw TypeError(".jlnf.SpinAck.FreeMysColumns: array expected");
                message.FreeMysColumns = [];
                for (let i = 0; i < object.FreeMysColumns.length; ++i)
                    message.FreeMysColumns[i] = object.FreeMysColumns[i] | 0;
            }
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnf.SpinAck
         * @static
         * @param {jlnf.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.FreeMysColumns = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.Extra = false;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlnf.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.FreeMysColumns && message.FreeMysColumns.length) {
                object.FreeMysColumns = [];
                for (let j = 0; j < message.FreeMysColumns.length; ++j)
                    object.FreeMysColumns[j] = message.FreeMysColumns[j];
            }
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlnf.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlnf.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnf.SpinAck";
        };

        return SpinAck;
    })();

    jlnf.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlnf
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlnf
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlnf.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlnf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlnf.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlnf.GameInfoData
         * @static
         * @param {jlnf.IGameInfoData=} [properties] Properties to set
         * @returns {jlnf.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlnf.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlnf.GameInfoData
         * @static
         * @param {jlnf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlnf.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlnf.GameInfoData
         * @static
         * @param {jlnf.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlnf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlnf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlnf.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlnf.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlnf.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlnf.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlnf.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlnf.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlnf.GameInfoData)
                return object;
            let message = new $root.jlnf.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlnf.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlnf.GameInfoData
         * @static
         * @param {jlnf.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlnf.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlnf.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlnf.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlnf;
})();

export const jlpn = $root.jlpn = (() => {

    /**
     * Namespace jlpn.
     * @exports jlpn
     * @namespace
     */
    const jlpn = {};

    /**
     * Symbol enum.
     * @name jlpn.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Coin=2 Coin value
     * @property {number} Symbol1=3 Symbol1 value
     * @property {number} Symbol2=4 Symbol2 value
     * @property {number} Symbol3=5 Symbol3 value
     * @property {number} Symbol4=6 Symbol4 value
     * @property {number} Symbol5=7 Symbol5 value
     * @property {number} Symbol6=8 Symbol6 value
     * @property {number} Symbol7=9 Symbol7 value
     * @property {number} Symbol8=10 Symbol8 value
     * @property {number} Symbol9=11 Symbol9 value
     * @property {number} SymbolMax=12 SymbolMax value
     */
    jlpn.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Coin"] = 2;
        values[valuesById[3] = "Symbol1"] = 3;
        values[valuesById[4] = "Symbol2"] = 4;
        values[valuesById[5] = "Symbol3"] = 5;
        values[valuesById[6] = "Symbol4"] = 6;
        values[valuesById[7] = "Symbol5"] = 7;
        values[valuesById[8] = "Symbol6"] = 8;
        values[valuesById[9] = "Symbol7"] = 9;
        values[valuesById[10] = "Symbol8"] = 10;
        values[valuesById[11] = "Symbol9"] = 11;
        values[valuesById[12] = "SymbolMax"] = 12;
        return values;
    })();

    jlpn.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlpn
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlpn
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlpn.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlpn.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlpn.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlpn.Block
         * @static
         * @param {jlpn.IBlock=} [properties] Properties to set
         * @returns {jlpn.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlpn.Block.verify|verify} messages.
         * @function encode
         * @memberof jlpn.Block
         * @static
         * @param {jlpn.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlpn.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpn.Block
         * @static
         * @param {jlpn.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlpn.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpn.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpn.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpn.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpn.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlpn.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpn.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpn.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpn.Block)
                return object;
            let message = new $root.jlpn.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpn.Block
         * @static
         * @param {jlpn.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlpn.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlpn.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpn.Block";
        };

        return Block;
    })();

    jlpn.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlpn
         * @interface IPlate
         * @property {Array.<jlpn.IColumnData>|null} [column] Plate column
         * @property {number|null} [win] Plate win
         * @property {Array.<jlpn.IAward>|null} [award] Plate award
         */

        /**
         * Constructs a new Plate.
         * @memberof jlpn
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlpn.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlpn.IColumnData>} column
         * @memberof jlpn.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlpn.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate award.
         * @member {Array.<jlpn.IAward>} award
         * @memberof jlpn.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlpn.Plate
         * @static
         * @param {jlpn.IPlate=} [properties] Properties to set
         * @returns {jlpn.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlpn.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlpn.Plate
         * @static
         * @param {jlpn.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlpn.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.win);
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlpn.Award.encode(message.award[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlpn.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpn.Plate
         * @static
         * @param {jlpn.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlpn.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpn.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpn.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlpn.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.win = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlpn.Award.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpn.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpn.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlpn.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlpn.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlpn.Award.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpn.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpn.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpn.Plate)
                return object;
            let message = new $root.jlpn.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlpn.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlpn.Plate.column: object expected");
                    message.column[i] = $root.jlpn.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlpn.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlpn.Plate.award: object expected");
                    message.award[i] = $root.jlpn.Award.fromObject(object.award[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpn.Plate
         * @static
         * @param {jlpn.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults)
                object.win = 0;
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlpn.ColumnData.toObject(message.column[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlpn.Award.toObject(message.award[j], options);
            }
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlpn.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlpn.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpn.Plate";
        };

        return Plate;
    })();

    jlpn.Award = (function() {

        /**
         * Properties of an Award.
         * @memberof jlpn
         * @interface IAward
         * @property {number|null} [symbol] Award symbol
         * @property {number|null} [win] Award win
         * @property {number|Long|null} [maxColumn] Award maxColumn
         * @property {number|Long|null} [connect] Award connect
         */

        /**
         * Constructs a new Award.
         * @memberof jlpn
         * @classdesc Represents an Award.
         * @implements IAward
         * @constructor
         * @param {jlpn.IAward=} [properties] Properties to set
         */
        function Award(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Award symbol.
         * @member {number} symbol
         * @memberof jlpn.Award
         * @instance
         */
        Award.prototype.symbol = 0;

        /**
         * Award win.
         * @member {number} win
         * @memberof jlpn.Award
         * @instance
         */
        Award.prototype.win = 0;

        /**
         * Award maxColumn.
         * @member {number|Long} maxColumn
         * @memberof jlpn.Award
         * @instance
         */
        Award.prototype.maxColumn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Award connect.
         * @member {number|Long} connect
         * @memberof jlpn.Award
         * @instance
         */
        Award.prototype.connect = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Award instance using the specified properties.
         * @function create
         * @memberof jlpn.Award
         * @static
         * @param {jlpn.IAward=} [properties] Properties to set
         * @returns {jlpn.Award} Award instance
         */
        Award.create = function create(properties) {
            return new Award(properties);
        };

        /**
         * Encodes the specified Award message. Does not implicitly {@link jlpn.Award.verify|verify} messages.
         * @function encode
         * @memberof jlpn.Award
         * @static
         * @param {jlpn.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.win);
            if (message.maxColumn != null && Object.hasOwnProperty.call(message, "maxColumn"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.maxColumn);
            if (message.connect != null && Object.hasOwnProperty.call(message, "connect"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.connect);
            return writer;
        };

        /**
         * Encodes the specified Award message, length delimited. Does not implicitly {@link jlpn.Award.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpn.Award
         * @static
         * @param {jlpn.IAward} message Award message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Award.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Award message from the specified reader or buffer.
         * @function decode
         * @memberof jlpn.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpn.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpn.Award();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.win = reader.double();
                        break;
                    }
                case 3: {
                        message.maxColumn = reader.int64();
                        break;
                    }
                case 4: {
                        message.connect = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Award message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpn.Award
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpn.Award} Award
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Award.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Award message.
         * @function verify
         * @memberof jlpn.Award
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Award.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.maxColumn != null && message.hasOwnProperty("maxColumn"))
                if (!$util.isInteger(message.maxColumn) && !(message.maxColumn && $util.isInteger(message.maxColumn.low) && $util.isInteger(message.maxColumn.high)))
                    return "maxColumn: integer|Long expected";
            if (message.connect != null && message.hasOwnProperty("connect"))
                if (!$util.isInteger(message.connect) && !(message.connect && $util.isInteger(message.connect.low) && $util.isInteger(message.connect.high)))
                    return "connect: integer|Long expected";
            return null;
        };

        /**
         * Creates an Award message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpn.Award
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpn.Award} Award
         */
        Award.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpn.Award)
                return object;
            let message = new $root.jlpn.Award();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.win != null)
                message.win = Number(object.win);
            if (object.maxColumn != null)
                if ($util.Long)
                    (message.maxColumn = $util.Long.fromValue(object.maxColumn)).unsigned = false;
                else if (typeof object.maxColumn === "string")
                    message.maxColumn = parseInt(object.maxColumn, 10);
                else if (typeof object.maxColumn === "number")
                    message.maxColumn = object.maxColumn;
                else if (typeof object.maxColumn === "object")
                    message.maxColumn = new $util.LongBits(object.maxColumn.low >>> 0, object.maxColumn.high >>> 0).toNumber();
            if (object.connect != null)
                if ($util.Long)
                    (message.connect = $util.Long.fromValue(object.connect)).unsigned = false;
                else if (typeof object.connect === "string")
                    message.connect = parseInt(object.connect, 10);
                else if (typeof object.connect === "number")
                    message.connect = object.connect;
                else if (typeof object.connect === "object")
                    message.connect = new $util.LongBits(object.connect.low >>> 0, object.connect.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an Award message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpn.Award
         * @static
         * @param {jlpn.Award} message Award
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Award.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.win = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.maxColumn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.maxColumn = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.connect = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.connect = options.longs === String ? "0" : 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.maxColumn != null && message.hasOwnProperty("maxColumn"))
                if (typeof message.maxColumn === "number")
                    object.maxColumn = options.longs === String ? String(message.maxColumn) : message.maxColumn;
                else
                    object.maxColumn = options.longs === String ? $util.Long.prototype.toString.call(message.maxColumn) : options.longs === Number ? new $util.LongBits(message.maxColumn.low >>> 0, message.maxColumn.high >>> 0).toNumber() : message.maxColumn;
            if (message.connect != null && message.hasOwnProperty("connect"))
                if (typeof message.connect === "number")
                    object.connect = options.longs === String ? String(message.connect) : message.connect;
                else
                    object.connect = options.longs === String ? $util.Long.prototype.toString.call(message.connect) : options.longs === Number ? new $util.LongBits(message.connect.low >>> 0, message.connect.high >>> 0).toNumber() : message.connect;
            return object;
        };

        /**
         * Converts this Award to JSON.
         * @function toJSON
         * @memberof jlpn.Award
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Award.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Award
         * @function getTypeUrl
         * @memberof jlpn.Award
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Award.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpn.Award";
        };

        return Award;
    })();

    jlpn.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlpn
         * @interface IAllPlate
         * @property {Array.<jlpn.IPlate>|null} [plate] AllPlate plate
         * @property {number|Long|null} [freeCount] AllPlate freeCount
         * @property {number|Long|null} [freeRate] AllPlate freeRate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlpn
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlpn.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlpn.IPlate>} plate
         * @memberof jlpn.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * AllPlate freeCount.
         * @member {number|Long} freeCount
         * @memberof jlpn.AllPlate
         * @instance
         */
        AllPlate.prototype.freeCount = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AllPlate freeRate.
         * @member {number|Long} freeRate
         * @memberof jlpn.AllPlate
         * @instance
         */
        AllPlate.prototype.freeRate = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlpn.AllPlate
         * @static
         * @param {jlpn.IAllPlate=} [properties] Properties to set
         * @returns {jlpn.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlpn.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlpn.AllPlate
         * @static
         * @param {jlpn.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlpn.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.freeCount != null && Object.hasOwnProperty.call(message, "freeCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.freeCount);
            if (message.freeRate != null && Object.hasOwnProperty.call(message, "freeRate"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.freeRate);
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlpn.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpn.AllPlate
         * @static
         * @param {jlpn.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlpn.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpn.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpn.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlpn.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.freeCount = reader.int64();
                        break;
                    }
                case 3: {
                        message.freeRate = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpn.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpn.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlpn.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlpn.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            if (message.freeCount != null && message.hasOwnProperty("freeCount"))
                if (!$util.isInteger(message.freeCount) && !(message.freeCount && $util.isInteger(message.freeCount.low) && $util.isInteger(message.freeCount.high)))
                    return "freeCount: integer|Long expected";
            if (message.freeRate != null && message.hasOwnProperty("freeRate"))
                if (!$util.isInteger(message.freeRate) && !(message.freeRate && $util.isInteger(message.freeRate.low) && $util.isInteger(message.freeRate.high)))
                    return "freeRate: integer|Long expected";
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpn.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpn.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpn.AllPlate)
                return object;
            let message = new $root.jlpn.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlpn.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlpn.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlpn.Plate.fromObject(object.plate[i]);
                }
            }
            if (object.freeCount != null)
                if ($util.Long)
                    (message.freeCount = $util.Long.fromValue(object.freeCount)).unsigned = false;
                else if (typeof object.freeCount === "string")
                    message.freeCount = parseInt(object.freeCount, 10);
                else if (typeof object.freeCount === "number")
                    message.freeCount = object.freeCount;
                else if (typeof object.freeCount === "object")
                    message.freeCount = new $util.LongBits(object.freeCount.low >>> 0, object.freeCount.high >>> 0).toNumber();
            if (object.freeRate != null)
                if ($util.Long)
                    (message.freeRate = $util.Long.fromValue(object.freeRate)).unsigned = false;
                else if (typeof object.freeRate === "string")
                    message.freeRate = parseInt(object.freeRate, 10);
                else if (typeof object.freeRate === "number")
                    message.freeRate = object.freeRate;
                else if (typeof object.freeRate === "object")
                    message.freeRate = new $util.LongBits(object.freeRate.low >>> 0, object.freeRate.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpn.AllPlate
         * @static
         * @param {jlpn.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.freeCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.freeCount = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.freeRate = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.freeRate = options.longs === String ? "0" : 0;
            }
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlpn.Plate.toObject(message.plate[j], options);
            }
            if (message.freeCount != null && message.hasOwnProperty("freeCount"))
                if (typeof message.freeCount === "number")
                    object.freeCount = options.longs === String ? String(message.freeCount) : message.freeCount;
                else
                    object.freeCount = options.longs === String ? $util.Long.prototype.toString.call(message.freeCount) : options.longs === Number ? new $util.LongBits(message.freeCount.low >>> 0, message.freeCount.high >>> 0).toNumber() : message.freeCount;
            if (message.freeRate != null && message.hasOwnProperty("freeRate"))
                if (typeof message.freeRate === "number")
                    object.freeRate = options.longs === String ? String(message.freeRate) : message.freeRate;
                else
                    object.freeRate = options.longs === String ? $util.Long.prototype.toString.call(message.freeRate) : options.longs === Number ? new $util.LongBits(message.freeRate.low >>> 0, message.freeRate.high >>> 0).toNumber() : message.freeRate;
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlpn.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlpn.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpn.AllPlate";
        };

        return AllPlate;
    })();

    jlpn.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlpn
         * @interface IColumnData
         * @property {Array.<jlpn.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlpn
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlpn.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jlpn.ICell>} row
         * @memberof jlpn.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlpn.ColumnData
         * @static
         * @param {jlpn.IColumnData=} [properties] Properties to set
         * @returns {jlpn.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlpn.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlpn.ColumnData
         * @static
         * @param {jlpn.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jlpn.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlpn.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpn.ColumnData
         * @static
         * @param {jlpn.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpn.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpn.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpn.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jlpn.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpn.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpn.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlpn.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jlpn.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpn.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpn.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpn.ColumnData)
                return object;
            let message = new $root.jlpn.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlpn.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jlpn.ColumnData.row: object expected");
                    message.row[i] = $root.jlpn.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpn.ColumnData
         * @static
         * @param {jlpn.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jlpn.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlpn.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlpn.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpn.ColumnData";
        };

        return ColumnData;
    })();

    jlpn.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlpn
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {boolean|null} [isConnect] Cell isConnect
         */

        /**
         * Constructs a new Cell.
         * @memberof jlpn
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlpn.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlpn.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell isConnect.
         * @member {boolean} isConnect
         * @memberof jlpn.Cell
         * @instance
         */
        Cell.prototype.isConnect = false;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlpn.Cell
         * @static
         * @param {jlpn.ICell=} [properties] Properties to set
         * @returns {jlpn.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlpn.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlpn.Cell
         * @static
         * @param {jlpn.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isConnect);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlpn.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpn.Cell
         * @static
         * @param {jlpn.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlpn.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpn.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpn.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.isConnect = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpn.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpn.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlpn.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpn.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpn.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpn.Cell)
                return object;
            let message = new $root.jlpn.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpn.Cell
         * @static
         * @param {jlpn.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.isConnect = false;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlpn.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlpn.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpn.Cell";
        };

        return Cell;
    })();

    return jlpn;
})();

export const jlpq = $root.jlpq = (() => {

    /**
     * Namespace jlpq.
     * @exports jlpq
     * @namespace
     */
    const jlpq = {};

    jlpq.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlpq
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlpq
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlpq.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlpq.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlpq.Column
         * @static
         * @param {jlpq.IColumn=} [properties] Properties to set
         * @returns {jlpq.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlpq.Column.verify|verify} messages.
         * @function encode
         * @memberof jlpq.Column
         * @static
         * @param {jlpq.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlpq.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq.Column
         * @static
         * @param {jlpq.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlpq.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq.Column)
                return object;
            let message = new $root.jlpq.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlpq.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq.Column
         * @static
         * @param {jlpq.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlpq.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlpq.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq.Column";
        };

        return Column;
    })();

    jlpq.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlpq
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlpq
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlpq.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlpq.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlpq.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlpq.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlpq.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlpq.AwardData
         * @static
         * @param {jlpq.IAwardData=} [properties] Properties to set
         * @returns {jlpq.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlpq.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlpq.AwardData
         * @static
         * @param {jlpq.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlpq.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq.AwardData
         * @static
         * @param {jlpq.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlpq.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq.AwardData)
                return object;
            let message = new $root.jlpq.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlpq.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq.AwardData
         * @static
         * @param {jlpq.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlpq.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlpq.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq.AwardData";
        };

        return AwardData;
    })();

    jlpq.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlpq
         * @interface IComboStageInfo
         * @property {Array.<jlpq.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlpq.IColumn>|null} [ComboStageMults] ComboStageInfo ComboStageMults
         * @property {Array.<jlpq.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlpq.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlpq.IColumn>|null} [FillMults] ComboStageInfo FillMults
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlpq
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlpq.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.ComboStageMults = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.FillMults = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlpq.IColumn>} ComboStageSymbol
         * @memberof jlpq.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlpq.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMults.
         * @member {Array.<jlpq.IColumn>} ComboStageMults
         * @memberof jlpq.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMults = $util.emptyArray;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlpq.IAwardData>} AwardDataVec
         * @memberof jlpq.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlpq.IColumn>} FillSymbols
         * @memberof jlpq.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo FillMults.
         * @member {Array.<jlpq.IColumn>} FillMults
         * @memberof jlpq.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillMults = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {jlpq.IComboStageInfo=} [properties] Properties to set
         * @returns {jlpq.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlpq.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {jlpq.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlpq.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMults != null && message.ComboStageMults.length)
                for (let i = 0; i < message.ComboStageMults.length; ++i)
                    $root.jlpq.Column.encode(message.ComboStageMults[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlpq.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlpq.Column.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FillMults != null && message.FillMults.length)
                for (let i = 0; i < message.FillMults.length; ++i)
                    $root.jlpq.Column.encode(message.FillMults[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlpq.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {jlpq.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlpq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.ComboStageMults && message.ComboStageMults.length))
                            message.ComboStageMults = [];
                        message.ComboStageMults.push($root.jlpq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlpq.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlpq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.FillMults && message.FillMults.length))
                            message.FillMults = [];
                        message.FillMults.push($root.jlpq.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlpq.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMults != null && message.hasOwnProperty("ComboStageMults")) {
                if (!Array.isArray(message.ComboStageMults))
                    return "ComboStageMults: array expected";
                for (let i = 0; i < message.ComboStageMults.length; ++i) {
                    let error = $root.jlpq.Column.verify(message.ComboStageMults[i]);
                    if (error)
                        return "ComboStageMults." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlpq.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlpq.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.FillMults != null && message.hasOwnProperty("FillMults")) {
                if (!Array.isArray(message.FillMults))
                    return "FillMults: array expected";
                for (let i = 0; i < message.FillMults.length; ++i) {
                    let error = $root.jlpq.Column.verify(message.FillMults[i]);
                    if (error)
                        return "FillMults." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq.ComboStageInfo)
                return object;
            let message = new $root.jlpq.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlpq.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlpq.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlpq.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMults) {
                if (!Array.isArray(object.ComboStageMults))
                    throw TypeError(".jlpq.ComboStageInfo.ComboStageMults: array expected");
                message.ComboStageMults = [];
                for (let i = 0; i < object.ComboStageMults.length; ++i) {
                    if (typeof object.ComboStageMults[i] !== "object")
                        throw TypeError(".jlpq.ComboStageInfo.ComboStageMults: object expected");
                    message.ComboStageMults[i] = $root.jlpq.Column.fromObject(object.ComboStageMults[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlpq.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlpq.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlpq.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlpq.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlpq.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlpq.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.FillMults) {
                if (!Array.isArray(object.FillMults))
                    throw TypeError(".jlpq.ComboStageInfo.FillMults: array expected");
                message.FillMults = [];
                for (let i = 0; i < object.FillMults.length; ++i) {
                    if (typeof object.FillMults[i] !== "object")
                        throw TypeError(".jlpq.ComboStageInfo.FillMults: object expected");
                    message.FillMults[i] = $root.jlpq.Column.fromObject(object.FillMults[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {jlpq.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.ComboStageMults = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.FillMults = [];
            }
            if (options.defaults)
                object.ComboStageWin = 0;
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlpq.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMults && message.ComboStageMults.length) {
                object.ComboStageMults = [];
                for (let j = 0; j < message.ComboStageMults.length; ++j)
                    object.ComboStageMults[j] = $root.jlpq.Column.toObject(message.ComboStageMults[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlpq.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlpq.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.FillMults && message.FillMults.length) {
                object.FillMults = [];
                for (let j = 0; j < message.FillMults.length; ++j)
                    object.FillMults[j] = $root.jlpq.Column.toObject(message.FillMults[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlpq.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlpq.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlpq.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlpq
         * @interface IRoundInfo
         * @property {Array.<jlpq.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [RoundMult] RoundInfo RoundMult
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [TargetSymbol] RoundInfo TargetSymbol
         * @property {Array.<number>|null} [TargetPosVec] RoundInfo TargetPosVec
         * @property {number|null} [CountRound] RoundInfo CountRound
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlpq
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlpq.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            this.TargetPosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlpq.IComboStageInfo>} ComboStageData
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo RoundMult.
         * @member {number} RoundMult
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundMult = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo TargetSymbol.
         * @member {number} TargetSymbol
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.TargetSymbol = 0;

        /**
         * RoundInfo TargetPosVec.
         * @member {Array.<number>} TargetPosVec
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.TargetPosVec = $util.emptyArray;

        /**
         * RoundInfo CountRound.
         * @member {number} CountRound
         * @memberof jlpq.RoundInfo
         * @instance
         */
        RoundInfo.prototype.CountRound = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlpq.RoundInfo
         * @static
         * @param {jlpq.IRoundInfo=} [properties] Properties to set
         * @returns {jlpq.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlpq.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpq.RoundInfo
         * @static
         * @param {jlpq.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlpq.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.RoundMult != null && Object.hasOwnProperty.call(message, "RoundMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundMult);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.TargetSymbol != null && Object.hasOwnProperty.call(message, "TargetSymbol"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.TargetSymbol);
            if (message.TargetPosVec != null && message.TargetPosVec.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.TargetPosVec.length; ++i)
                    writer.int32(message.TargetPosVec[i]);
                writer.ldelim();
            }
            if (message.CountRound != null && Object.hasOwnProperty.call(message, "CountRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.CountRound);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlpq.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq.RoundInfo
         * @static
         * @param {jlpq.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlpq.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.RoundMult = reader.double();
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.TargetSymbol = reader.int32();
                        break;
                    }
                case 7: {
                        if (!(message.TargetPosVec && message.TargetPosVec.length))
                            message.TargetPosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.TargetPosVec.push(reader.int32());
                        } else
                            message.TargetPosVec.push(reader.int32());
                        break;
                    }
                case 8: {
                        message.CountRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlpq.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlpq.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.RoundMult != null && message.hasOwnProperty("RoundMult"))
                if (typeof message.RoundMult !== "number")
                    return "RoundMult: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.TargetSymbol != null && message.hasOwnProperty("TargetSymbol"))
                if (!$util.isInteger(message.TargetSymbol))
                    return "TargetSymbol: integer expected";
            if (message.TargetPosVec != null && message.hasOwnProperty("TargetPosVec")) {
                if (!Array.isArray(message.TargetPosVec))
                    return "TargetPosVec: array expected";
                for (let i = 0; i < message.TargetPosVec.length; ++i)
                    if (!$util.isInteger(message.TargetPosVec[i]))
                        return "TargetPosVec: integer[] expected";
            }
            if (message.CountRound != null && message.hasOwnProperty("CountRound"))
                if (!$util.isInteger(message.CountRound))
                    return "CountRound: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq.RoundInfo)
                return object;
            let message = new $root.jlpq.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlpq.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlpq.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlpq.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.RoundMult != null)
                message.RoundMult = Number(object.RoundMult);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.TargetSymbol != null)
                message.TargetSymbol = object.TargetSymbol | 0;
            if (object.TargetPosVec) {
                if (!Array.isArray(object.TargetPosVec))
                    throw TypeError(".jlpq.RoundInfo.TargetPosVec: array expected");
                message.TargetPosVec = [];
                for (let i = 0; i < object.TargetPosVec.length; ++i)
                    message.TargetPosVec[i] = object.TargetPosVec[i] | 0;
            }
            if (object.CountRound != null)
                message.CountRound = object.CountRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq.RoundInfo
         * @static
         * @param {jlpq.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageData = [];
                object.TargetPosVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.RoundMult = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.TargetSymbol = 0;
                object.CountRound = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlpq.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.RoundMult != null && message.hasOwnProperty("RoundMult"))
                object.RoundMult = options.json && !isFinite(message.RoundMult) ? String(message.RoundMult) : message.RoundMult;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.TargetSymbol != null && message.hasOwnProperty("TargetSymbol"))
                object.TargetSymbol = message.TargetSymbol;
            if (message.TargetPosVec && message.TargetPosVec.length) {
                object.TargetPosVec = [];
                for (let j = 0; j < message.TargetPosVec.length; ++j)
                    object.TargetPosVec[j] = message.TargetPosVec[j];
            }
            if (message.CountRound != null && message.hasOwnProperty("CountRound"))
                object.CountRound = message.CountRound;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlpq.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlpq.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq.RoundInfo";
        };

        return RoundInfo;
    })();

    jlpq.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlpq
         * @interface ISpinAck
         * @property {Array.<jlpq.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {boolean|null} [Extra] SpinAck Extra
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlpq
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlpq.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlpq.IRoundInfo>} RoundQueue
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlpq.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlpq.SpinAck
         * @static
         * @param {jlpq.ISpinAck=} [properties] Properties to set
         * @returns {jlpq.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlpq.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlpq.SpinAck
         * @static
         * @param {jlpq.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlpq.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.Extra);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlpq.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq.SpinAck
         * @static
         * @param {jlpq.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlpq.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Extra = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlpq.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlpq.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq.SpinAck)
                return object;
            let message = new $root.jlpq.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlpq.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlpq.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlpq.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq.SpinAck
         * @static
         * @param {jlpq.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Extra = false;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlpq.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlpq.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlpq.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq.SpinAck";
        };

        return SpinAck;
    })();

    jlpq.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlpq
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlpq
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlpq.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlpq.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlpq.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlpq.GameInfoData
         * @static
         * @param {jlpq.IGameInfoData=} [properties] Properties to set
         * @returns {jlpq.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlpq.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlpq.GameInfoData
         * @static
         * @param {jlpq.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlpq.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq.GameInfoData
         * @static
         * @param {jlpq.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlpq.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq.GameInfoData)
                return object;
            let message = new $root.jlpq.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlpq.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq.GameInfoData
         * @static
         * @param {jlpq.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlpq.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlpq.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlpq;
})();

export const jlpq2 = $root.jlpq2 = (() => {

    /**
     * Namespace jlpq2.
     * @exports jlpq2
     * @namespace
     */
    const jlpq2 = {};

    jlpq2.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlpq2
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlpq2
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlpq2.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlpq2.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlpq2.Column
         * @static
         * @param {jlpq2.IColumn=} [properties] Properties to set
         * @returns {jlpq2.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlpq2.Column.verify|verify} messages.
         * @function encode
         * @memberof jlpq2.Column
         * @static
         * @param {jlpq2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlpq2.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq2.Column
         * @static
         * @param {jlpq2.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq2.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq2.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq2.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlpq2.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq2.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq2.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq2.Column)
                return object;
            let message = new $root.jlpq2.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlpq2.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq2.Column
         * @static
         * @param {jlpq2.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlpq2.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlpq2.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq2.Column";
        };

        return Column;
    })();

    jlpq2.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlpq2
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlpq2
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlpq2.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlpq2.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlpq2.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlpq2.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlpq2.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlpq2.AwardData
         * @static
         * @param {jlpq2.IAwardData=} [properties] Properties to set
         * @returns {jlpq2.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlpq2.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlpq2.AwardData
         * @static
         * @param {jlpq2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlpq2.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq2.AwardData
         * @static
         * @param {jlpq2.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq2.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq2.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq2.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlpq2.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq2.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq2.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq2.AwardData)
                return object;
            let message = new $root.jlpq2.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlpq2.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq2.AwardData
         * @static
         * @param {jlpq2.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlpq2.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlpq2.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq2.AwardData";
        };

        return AwardData;
    })();

    jlpq2.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlpq2
         * @interface IComboStageInfo
         * @property {Array.<jlpq2.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlpq2.IColumn>|null} [ComboStageMults] ComboStageInfo ComboStageMults
         * @property {Array.<jlpq2.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlpq2.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlpq2.IColumn>|null} [FillMults] ComboStageInfo FillMults
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlpq2
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlpq2.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.ComboStageMults = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.FillMults = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlpq2.IColumn>} ComboStageSymbol
         * @memberof jlpq2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlpq2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMults.
         * @member {Array.<jlpq2.IColumn>} ComboStageMults
         * @memberof jlpq2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMults = $util.emptyArray;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlpq2.IAwardData>} AwardDataVec
         * @memberof jlpq2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlpq2.IColumn>} FillSymbols
         * @memberof jlpq2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo FillMults.
         * @member {Array.<jlpq2.IColumn>} FillMults
         * @memberof jlpq2.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillMults = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {jlpq2.IComboStageInfo=} [properties] Properties to set
         * @returns {jlpq2.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlpq2.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {jlpq2.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlpq2.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMults != null && message.ComboStageMults.length)
                for (let i = 0; i < message.ComboStageMults.length; ++i)
                    $root.jlpq2.Column.encode(message.ComboStageMults[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlpq2.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlpq2.Column.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FillMults != null && message.FillMults.length)
                for (let i = 0; i < message.FillMults.length; ++i)
                    $root.jlpq2.Column.encode(message.FillMults[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlpq2.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {jlpq2.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq2.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq2.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlpq2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.ComboStageMults && message.ComboStageMults.length))
                            message.ComboStageMults = [];
                        message.ComboStageMults.push($root.jlpq2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlpq2.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlpq2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.FillMults && message.FillMults.length))
                            message.FillMults = [];
                        message.FillMults.push($root.jlpq2.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq2.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlpq2.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMults != null && message.hasOwnProperty("ComboStageMults")) {
                if (!Array.isArray(message.ComboStageMults))
                    return "ComboStageMults: array expected";
                for (let i = 0; i < message.ComboStageMults.length; ++i) {
                    let error = $root.jlpq2.Column.verify(message.ComboStageMults[i]);
                    if (error)
                        return "ComboStageMults." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlpq2.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlpq2.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.FillMults != null && message.hasOwnProperty("FillMults")) {
                if (!Array.isArray(message.FillMults))
                    return "FillMults: array expected";
                for (let i = 0; i < message.FillMults.length; ++i) {
                    let error = $root.jlpq2.Column.verify(message.FillMults[i]);
                    if (error)
                        return "FillMults." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq2.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq2.ComboStageInfo)
                return object;
            let message = new $root.jlpq2.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlpq2.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlpq2.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlpq2.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMults) {
                if (!Array.isArray(object.ComboStageMults))
                    throw TypeError(".jlpq2.ComboStageInfo.ComboStageMults: array expected");
                message.ComboStageMults = [];
                for (let i = 0; i < object.ComboStageMults.length; ++i) {
                    if (typeof object.ComboStageMults[i] !== "object")
                        throw TypeError(".jlpq2.ComboStageInfo.ComboStageMults: object expected");
                    message.ComboStageMults[i] = $root.jlpq2.Column.fromObject(object.ComboStageMults[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlpq2.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlpq2.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlpq2.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlpq2.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlpq2.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlpq2.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.FillMults) {
                if (!Array.isArray(object.FillMults))
                    throw TypeError(".jlpq2.ComboStageInfo.FillMults: array expected");
                message.FillMults = [];
                for (let i = 0; i < object.FillMults.length; ++i) {
                    if (typeof object.FillMults[i] !== "object")
                        throw TypeError(".jlpq2.ComboStageInfo.FillMults: object expected");
                    message.FillMults[i] = $root.jlpq2.Column.fromObject(object.FillMults[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {jlpq2.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.ComboStageMults = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.FillMults = [];
            }
            if (options.defaults)
                object.ComboStageWin = 0;
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlpq2.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMults && message.ComboStageMults.length) {
                object.ComboStageMults = [];
                for (let j = 0; j < message.ComboStageMults.length; ++j)
                    object.ComboStageMults[j] = $root.jlpq2.Column.toObject(message.ComboStageMults[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlpq2.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlpq2.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.FillMults && message.FillMults.length) {
                object.FillMults = [];
                for (let j = 0; j < message.FillMults.length; ++j)
                    object.FillMults[j] = $root.jlpq2.Column.toObject(message.FillMults[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlpq2.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlpq2.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq2.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlpq2.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlpq2
         * @interface IRoundInfo
         * @property {Array.<jlpq2.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [RoundMult] RoundInfo RoundMult
         * @property {number|null} [AddMult] RoundInfo AddMult
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlpq2
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlpq2.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlpq2.IComboStageInfo>} ComboStageData
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo RoundMult.
         * @member {number} RoundMult
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundMult = 0;

        /**
         * RoundInfo AddMult.
         * @member {number} AddMult
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddMult = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlpq2.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {jlpq2.IRoundInfo=} [properties] Properties to set
         * @returns {jlpq2.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlpq2.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {jlpq2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlpq2.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.RoundMult != null && Object.hasOwnProperty.call(message, "RoundMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundMult);
            if (message.AddMult != null && Object.hasOwnProperty.call(message, "AddMult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.AddMult);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeTotalRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlpq2.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {jlpq2.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq2.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlpq2.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.RoundMult = reader.double();
                        break;
                    }
                case 4: {
                        message.AddMult = reader.double();
                        break;
                    }
                case 5: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq2.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlpq2.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.RoundMult != null && message.hasOwnProperty("RoundMult"))
                if (typeof message.RoundMult !== "number")
                    return "RoundMult: number expected";
            if (message.AddMult != null && message.hasOwnProperty("AddMult"))
                if (typeof message.AddMult !== "number")
                    return "AddMult: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq2.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq2.RoundInfo)
                return object;
            let message = new $root.jlpq2.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlpq2.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlpq2.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlpq2.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.RoundMult != null)
                message.RoundMult = Number(object.RoundMult);
            if (object.AddMult != null)
                message.AddMult = Number(object.AddMult);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {jlpq2.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.RoundMult = 0;
                object.AddMult = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlpq2.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.RoundMult != null && message.hasOwnProperty("RoundMult"))
                object.RoundMult = options.json && !isFinite(message.RoundMult) ? String(message.RoundMult) : message.RoundMult;
            if (message.AddMult != null && message.hasOwnProperty("AddMult"))
                object.AddMult = options.json && !isFinite(message.AddMult) ? String(message.AddMult) : message.AddMult;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlpq2.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlpq2.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq2.RoundInfo";
        };

        return RoundInfo;
    })();

    jlpq2.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlpq2
         * @interface ISpinAck
         * @property {Array.<jlpq2.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlpq2
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlpq2.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlpq2.IRoundInfo>} RoundQueue
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlpq2.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlpq2.SpinAck
         * @static
         * @param {jlpq2.ISpinAck=} [properties] Properties to set
         * @returns {jlpq2.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlpq2.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlpq2.SpinAck
         * @static
         * @param {jlpq2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlpq2.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlpq2.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq2.SpinAck
         * @static
         * @param {jlpq2.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq2.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlpq2.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq2.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq2.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlpq2.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlpq2.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq2.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq2.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq2.SpinAck)
                return object;
            let message = new $root.jlpq2.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlpq2.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlpq2.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlpq2.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq2.SpinAck
         * @static
         * @param {jlpq2.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlpq2.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlpq2.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlpq2.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq2.SpinAck";
        };

        return SpinAck;
    })();

    jlpq2.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlpq2
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlpq2
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlpq2.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlpq2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlpq2.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {jlpq2.IGameInfoData=} [properties] Properties to set
         * @returns {jlpq2.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlpq2.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {jlpq2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlpq2.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {jlpq2.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpq2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpq2.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpq2.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpq2.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpq2.GameInfoData)
                return object;
            let message = new $root.jlpq2.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlpq2.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {jlpq2.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlpq2.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlpq2.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpq2.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlpq2;
})();

export const jlps = $root.jlps = (() => {

    /**
     * Namespace jlps.
     * @exports jlps
     * @namespace
     */
    const jlps = {};

    jlps.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlps
         * @interface IMColumn
         * @property {Array.<jlps.IMSymbol>|null} [Col] MColumn Col
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlps
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlps.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn Col.
         * @member {Array.<jlps.IMSymbol>} Col
         * @memberof jlps.MColumn
         * @instance
         */
        MColumn.prototype.Col = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlps.MColumn
         * @static
         * @param {jlps.IMColumn=} [properties] Properties to set
         * @returns {jlps.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlps.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlps.MColumn
         * @static
         * @param {jlps.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jlps.MSymbol.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlps.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.MColumn
         * @static
         * @param {jlps.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jlps.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlps.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jlps.MSymbol.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.MColumn)
                return object;
            let message = new $root.jlps.MColumn();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlps.MColumn.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jlps.MColumn.Col: object expected");
                    message.Col[i] = $root.jlps.MSymbol.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.MColumn
         * @static
         * @param {jlps.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jlps.MSymbol.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlps.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlps.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.MColumn";
        };

        return MColumn;
    })();

    jlps.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlps
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [HSymbol] MSymbol HSymbol
         * @property {number|null} [HStack] MSymbol HStack
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlps
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlps.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlps.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol HSymbol.
         * @member {number} HSymbol
         * @memberof jlps.MSymbol
         * @instance
         */
        MSymbol.prototype.HSymbol = 0;

        /**
         * MSymbol HStack.
         * @member {number} HStack
         * @memberof jlps.MSymbol
         * @instance
         */
        MSymbol.prototype.HStack = 0;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlps.MSymbol
         * @static
         * @param {jlps.IMSymbol=} [properties] Properties to set
         * @returns {jlps.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlps.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlps.MSymbol
         * @static
         * @param {jlps.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.HSymbol != null && Object.hasOwnProperty.call(message, "HSymbol"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.HSymbol);
            if (message.HStack != null && Object.hasOwnProperty.call(message, "HStack"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.HStack);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlps.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.MSymbol
         * @static
         * @param {jlps.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.HSymbol = reader.int32();
                        break;
                    }
                case 3: {
                        message.HStack = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlps.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.HSymbol != null && message.hasOwnProperty("HSymbol"))
                if (!$util.isInteger(message.HSymbol))
                    return "HSymbol: integer expected";
            if (message.HStack != null && message.hasOwnProperty("HStack"))
                if (!$util.isInteger(message.HStack))
                    return "HStack: integer expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.MSymbol)
                return object;
            let message = new $root.jlps.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.HSymbol != null)
                message.HSymbol = object.HSymbol | 0;
            if (object.HStack != null)
                message.HStack = object.HStack | 0;
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.MSymbol
         * @static
         * @param {jlps.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.HSymbol = 0;
                object.HStack = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.HSymbol != null && message.hasOwnProperty("HSymbol"))
                object.HSymbol = message.HSymbol;
            if (message.HStack != null && message.hasOwnProperty("HStack"))
                object.HStack = message.HStack;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlps.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlps.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.MSymbol";
        };

        return MSymbol;
    })();

    jlps.PartyData = (function() {

        /**
         * Properties of a PartyData.
         * @memberof jlps
         * @interface IPartyData
         * @property {number|null} [LR] PartyData LR
         * @property {number|null} [State] PartyData State
         * @property {boolean|null} [Worked] PartyData Worked
         * @property {number|null} [StackCol] PartyData StackCol
         * @property {number|null} [StackRow] PartyData StackRow
         * @property {number|null} [HSymbol] PartyData HSymbol
         */

        /**
         * Constructs a new PartyData.
         * @memberof jlps
         * @classdesc Represents a PartyData.
         * @implements IPartyData
         * @constructor
         * @param {jlps.IPartyData=} [properties] Properties to set
         */
        function PartyData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PartyData LR.
         * @member {number} LR
         * @memberof jlps.PartyData
         * @instance
         */
        PartyData.prototype.LR = 0;

        /**
         * PartyData State.
         * @member {number} State
         * @memberof jlps.PartyData
         * @instance
         */
        PartyData.prototype.State = 0;

        /**
         * PartyData Worked.
         * @member {boolean} Worked
         * @memberof jlps.PartyData
         * @instance
         */
        PartyData.prototype.Worked = false;

        /**
         * PartyData StackCol.
         * @member {number} StackCol
         * @memberof jlps.PartyData
         * @instance
         */
        PartyData.prototype.StackCol = 0;

        /**
         * PartyData StackRow.
         * @member {number} StackRow
         * @memberof jlps.PartyData
         * @instance
         */
        PartyData.prototype.StackRow = 0;

        /**
         * PartyData HSymbol.
         * @member {number} HSymbol
         * @memberof jlps.PartyData
         * @instance
         */
        PartyData.prototype.HSymbol = 0;

        /**
         * Creates a new PartyData instance using the specified properties.
         * @function create
         * @memberof jlps.PartyData
         * @static
         * @param {jlps.IPartyData=} [properties] Properties to set
         * @returns {jlps.PartyData} PartyData instance
         */
        PartyData.create = function create(properties) {
            return new PartyData(properties);
        };

        /**
         * Encodes the specified PartyData message. Does not implicitly {@link jlps.PartyData.verify|verify} messages.
         * @function encode
         * @memberof jlps.PartyData
         * @static
         * @param {jlps.IPartyData} message PartyData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PartyData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LR != null && Object.hasOwnProperty.call(message, "LR"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.LR);
            if (message.State != null && Object.hasOwnProperty.call(message, "State"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.State);
            if (message.Worked != null && Object.hasOwnProperty.call(message, "Worked"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.Worked);
            if (message.StackCol != null && Object.hasOwnProperty.call(message, "StackCol"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.StackCol);
            if (message.StackRow != null && Object.hasOwnProperty.call(message, "StackRow"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.StackRow);
            if (message.HSymbol != null && Object.hasOwnProperty.call(message, "HSymbol"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.HSymbol);
            return writer;
        };

        /**
         * Encodes the specified PartyData message, length delimited. Does not implicitly {@link jlps.PartyData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.PartyData
         * @static
         * @param {jlps.IPartyData} message PartyData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PartyData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PartyData message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.PartyData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.PartyData} PartyData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PartyData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.PartyData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LR = reader.int32();
                        break;
                    }
                case 2: {
                        message.State = reader.int32();
                        break;
                    }
                case 3: {
                        message.Worked = reader.bool();
                        break;
                    }
                case 4: {
                        message.StackCol = reader.int32();
                        break;
                    }
                case 5: {
                        message.StackRow = reader.int32();
                        break;
                    }
                case 6: {
                        message.HSymbol = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PartyData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.PartyData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.PartyData} PartyData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PartyData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PartyData message.
         * @function verify
         * @memberof jlps.PartyData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PartyData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LR != null && message.hasOwnProperty("LR"))
                if (!$util.isInteger(message.LR))
                    return "LR: integer expected";
            if (message.State != null && message.hasOwnProperty("State"))
                if (!$util.isInteger(message.State))
                    return "State: integer expected";
            if (message.Worked != null && message.hasOwnProperty("Worked"))
                if (typeof message.Worked !== "boolean")
                    return "Worked: boolean expected";
            if (message.StackCol != null && message.hasOwnProperty("StackCol"))
                if (!$util.isInteger(message.StackCol))
                    return "StackCol: integer expected";
            if (message.StackRow != null && message.hasOwnProperty("StackRow"))
                if (!$util.isInteger(message.StackRow))
                    return "StackRow: integer expected";
            if (message.HSymbol != null && message.hasOwnProperty("HSymbol"))
                if (!$util.isInteger(message.HSymbol))
                    return "HSymbol: integer expected";
            return null;
        };

        /**
         * Creates a PartyData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.PartyData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.PartyData} PartyData
         */
        PartyData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.PartyData)
                return object;
            let message = new $root.jlps.PartyData();
            if (object.LR != null)
                message.LR = object.LR | 0;
            if (object.State != null)
                message.State = object.State | 0;
            if (object.Worked != null)
                message.Worked = Boolean(object.Worked);
            if (object.StackCol != null)
                message.StackCol = object.StackCol | 0;
            if (object.StackRow != null)
                message.StackRow = object.StackRow | 0;
            if (object.HSymbol != null)
                message.HSymbol = object.HSymbol | 0;
            return message;
        };

        /**
         * Creates a plain object from a PartyData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.PartyData
         * @static
         * @param {jlps.PartyData} message PartyData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PartyData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.LR = 0;
                object.State = 0;
                object.Worked = false;
                object.StackCol = 0;
                object.StackRow = 0;
                object.HSymbol = 0;
            }
            if (message.LR != null && message.hasOwnProperty("LR"))
                object.LR = message.LR;
            if (message.State != null && message.hasOwnProperty("State"))
                object.State = message.State;
            if (message.Worked != null && message.hasOwnProperty("Worked"))
                object.Worked = message.Worked;
            if (message.StackCol != null && message.hasOwnProperty("StackCol"))
                object.StackCol = message.StackCol;
            if (message.StackRow != null && message.hasOwnProperty("StackRow"))
                object.StackRow = message.StackRow;
            if (message.HSymbol != null && message.hasOwnProperty("HSymbol"))
                object.HSymbol = message.HSymbol;
            return object;
        };

        /**
         * Converts this PartyData to JSON.
         * @function toJSON
         * @memberof jlps.PartyData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PartyData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PartyData
         * @function getTypeUrl
         * @memberof jlps.PartyData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PartyData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.PartyData";
        };

        return PartyData;
    })();

    jlps.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlps
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Direction] AwardData Direction
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlps
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlps.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlps.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlps.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlps.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Direction.
         * @member {number} Direction
         * @memberof jlps.AwardData
         * @instance
         */
        AwardData.prototype.Direction = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlps.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlps.AwardData
         * @static
         * @param {jlps.IAwardData=} [properties] Properties to set
         * @returns {jlps.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlps.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlps.AwardData
         * @static
         * @param {jlps.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Direction != null && Object.hasOwnProperty.call(message, "Direction"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Direction);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlps.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.AwardData
         * @static
         * @param {jlps.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Direction = reader.int32();
                        break;
                    }
                case 5: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlps.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Direction != null && message.hasOwnProperty("Direction"))
                if (!$util.isInteger(message.Direction))
                    return "Direction: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.AwardData)
                return object;
            let message = new $root.jlps.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Direction != null)
                message.Direction = object.Direction | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.AwardData
         * @static
         * @param {jlps.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Direction = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Direction != null && message.hasOwnProperty("Direction"))
                object.Direction = message.Direction;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlps.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlps.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.AwardData";
        };

        return AwardData;
    })();

    jlps.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlps
         * @interface IComboStageInfo
         * @property {Array.<jlps.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlps.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlps.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlps.IMColumn>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         * @property {Array.<jlps.IPartyData>|null} [Party] ComboStageInfo Party
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         * @property {Array.<jlps.IMColumn>|null} [ComboStageSymbolLog] ComboStageInfo ComboStageSymbolLog
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlps
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlps.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            this.Party = [];
            this.ComboStageSymbolLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlps.IMColumn>} ComboStageSymbol
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlps.IAwardData>} AwardDataVec
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlps.IMColumn>} FillSymbols
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlps.IMColumn>} ChangeSymbols
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * ComboStageInfo Party.
         * @member {Array.<jlps.IPartyData>} Party
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Party = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * ComboStageInfo ComboStageSymbolLog.
         * @member {Array.<jlps.IMColumn>} ComboStageSymbolLog
         * @memberof jlps.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbolLog = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {jlps.IComboStageInfo=} [properties] Properties to set
         * @returns {jlps.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlps.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {jlps.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlps.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlps.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlps.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlps.MColumn.encode(message.ChangeSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.Party != null && message.Party.length)
                for (let i = 0; i < message.Party.length; ++i)
                    $root.jlps.PartyData.encode(message.Party[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            if (message.ComboStageSymbolLog != null && message.ComboStageSymbolLog.length)
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i)
                    $root.jlps.MColumn.encode(message.ComboStageSymbolLog[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlps.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {jlps.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlps.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlps.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlps.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlps.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.Party && message.Party.length))
                            message.Party = [];
                        message.Party.push($root.jlps.PartyData.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 8: {
                        if (!(message.ComboStageSymbolLog && message.ComboStageSymbolLog.length))
                            message.ComboStageSymbolLog = [];
                        message.ComboStageSymbolLog.push($root.jlps.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlps.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlps.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlps.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlps.MColumn.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            if (message.Party != null && message.hasOwnProperty("Party")) {
                if (!Array.isArray(message.Party))
                    return "Party: array expected";
                for (let i = 0; i < message.Party.length; ++i) {
                    let error = $root.jlps.PartyData.verify(message.Party[i]);
                    if (error)
                        return "Party." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.ComboStageSymbolLog != null && message.hasOwnProperty("ComboStageSymbolLog")) {
                if (!Array.isArray(message.ComboStageSymbolLog))
                    return "ComboStageSymbolLog: array expected";
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i) {
                    let error = $root.jlps.MColumn.verify(message.ComboStageSymbolLog[i]);
                    if (error)
                        return "ComboStageSymbolLog." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.ComboStageInfo)
                return object;
            let message = new $root.jlps.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlps.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlps.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlps.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlps.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlps.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlps.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlps.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlps.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlps.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlps.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlps.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlps.MColumn.fromObject(object.ChangeSymbols[i]);
                }
            }
            if (object.Party) {
                if (!Array.isArray(object.Party))
                    throw TypeError(".jlps.ComboStageInfo.Party: array expected");
                message.Party = [];
                for (let i = 0; i < object.Party.length; ++i) {
                    if (typeof object.Party[i] !== "object")
                        throw TypeError(".jlps.ComboStageInfo.Party: object expected");
                    message.Party[i] = $root.jlps.PartyData.fromObject(object.Party[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.ComboStageSymbolLog) {
                if (!Array.isArray(object.ComboStageSymbolLog))
                    throw TypeError(".jlps.ComboStageInfo.ComboStageSymbolLog: array expected");
                message.ComboStageSymbolLog = [];
                for (let i = 0; i < object.ComboStageSymbolLog.length; ++i) {
                    if (typeof object.ComboStageSymbolLog[i] !== "object")
                        throw TypeError(".jlps.ComboStageInfo.ComboStageSymbolLog: object expected");
                    message.ComboStageSymbolLog[i] = $root.jlps.MColumn.fromObject(object.ComboStageSymbolLog[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {jlps.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
                object.Party = [];
                object.ComboStageSymbolLog = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlps.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlps.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlps.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlps.MColumn.toObject(message.ChangeSymbols[j], options);
            }
            if (message.Party && message.Party.length) {
                object.Party = [];
                for (let j = 0; j < message.Party.length; ++j)
                    object.Party[j] = $root.jlps.PartyData.toObject(message.Party[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.ComboStageSymbolLog && message.ComboStageSymbolLog.length) {
                object.ComboStageSymbolLog = [];
                for (let j = 0; j < message.ComboStageSymbolLog.length; ++j)
                    object.ComboStageSymbolLog[j] = $root.jlps.MColumn.toObject(message.ComboStageSymbolLog[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlps.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlps.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlps.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlps
         * @interface IRoundInfo
         * @property {Array.<jlps.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<number>|null} [SBGHeight] RoundInfo SBGHeight
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlps
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlps.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            this.SBGHeight = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlps.IComboStageInfo>} ComboStageData
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo SBGHeight.
         * @member {Array.<number>} SBGHeight
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.SBGHeight = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlps.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlps.RoundInfo
         * @static
         * @param {jlps.IRoundInfo=} [properties] Properties to set
         * @returns {jlps.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlps.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlps.RoundInfo
         * @static
         * @param {jlps.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlps.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeTotalRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AddRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RoundWin);
            if (message.SBGHeight != null && message.SBGHeight.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.SBGHeight.length; ++i)
                    writer.int32(message.SBGHeight[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlps.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.RoundInfo
         * @static
         * @param {jlps.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlps.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 3: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 8: {
                        if (!(message.SBGHeight && message.SBGHeight.length))
                            message.SBGHeight = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.SBGHeight.push(reader.int32());
                        } else
                            message.SBGHeight.push(reader.int32());
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlps.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlps.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.SBGHeight != null && message.hasOwnProperty("SBGHeight")) {
                if (!Array.isArray(message.SBGHeight))
                    return "SBGHeight: array expected";
                for (let i = 0; i < message.SBGHeight.length; ++i)
                    if (!$util.isInteger(message.SBGHeight[i]))
                        return "SBGHeight: integer[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.RoundInfo)
                return object;
            let message = new $root.jlps.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlps.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlps.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlps.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.SBGHeight) {
                if (!Array.isArray(object.SBGHeight))
                    throw TypeError(".jlps.RoundInfo.SBGHeight: array expected");
                message.SBGHeight = [];
                for (let i = 0; i < object.SBGHeight.length; ++i)
                    message.SBGHeight[i] = object.SBGHeight[i] | 0;
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.RoundInfo
         * @static
         * @param {jlps.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageData = [];
                object.SBGHeight = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.AddRound = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlps.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.SBGHeight && message.SBGHeight.length) {
                object.SBGHeight = [];
                for (let j = 0; j < message.SBGHeight.length; ++j)
                    object.SBGHeight[j] = message.SBGHeight[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlps.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlps.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.RoundInfo";
        };

        return RoundInfo;
    })();

    jlps.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlps
         * @interface ISpinAck
         * @property {Array.<jlps.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlps
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlps.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlps.IRoundInfo>} RoundQueue
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlps.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlps.SpinAck
         * @static
         * @param {jlps.ISpinAck=} [properties] Properties to set
         * @returns {jlps.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlps.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlps.SpinAck
         * @static
         * @param {jlps.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlps.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlps.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.SpinAck
         * @static
         * @param {jlps.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlps.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Bet = reader.double();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlps.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlps.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.SpinAck)
                return object;
            let message = new $root.jlps.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlps.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlps.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlps.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.SpinAck
         * @static
         * @param {jlps.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlps.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlps.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlps.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.SpinAck";
        };

        return SpinAck;
    })();

    jlps.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlps
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlps
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlps.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlps.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlps.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlps.GameInfoData
         * @static
         * @param {jlps.IGameInfoData=} [properties] Properties to set
         * @returns {jlps.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlps.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlps.GameInfoData
         * @static
         * @param {jlps.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlps.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlps.GameInfoData
         * @static
         * @param {jlps.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlps.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlps.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlps.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlps.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlps.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlps.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlps.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlps.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlps.GameInfoData)
                return object;
            let message = new $root.jlps.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlps.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlps.GameInfoData
         * @static
         * @param {jlps.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlps.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlps.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlps.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlps;
})();

export const jlpsd = $root.jlpsd = (() => {

    /**
     * Namespace jlpsd.
     * @exports jlpsd
     * @namespace
     */
    const jlpsd = {};

    jlpsd.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlpsd
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlpsd
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlpsd.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlpsd.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlpsd.Column
         * @static
         * @param {jlpsd.IColumn=} [properties] Properties to set
         * @returns {jlpsd.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlpsd.Column.verify|verify} messages.
         * @function encode
         * @memberof jlpsd.Column
         * @static
         * @param {jlpsd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlpsd.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpsd.Column
         * @static
         * @param {jlpsd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlpsd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpsd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpsd.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpsd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpsd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlpsd.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpsd.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpsd.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpsd.Column)
                return object;
            let message = new $root.jlpsd.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlpsd.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpsd.Column
         * @static
         * @param {jlpsd.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlpsd.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlpsd.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpsd.Column";
        };

        return Column;
    })();

    jlpsd.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlpsd
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlpsd
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlpsd.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlpsd.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlpsd.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlpsd.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlpsd.AwardData
         * @static
         * @param {jlpsd.IAwardData=} [properties] Properties to set
         * @returns {jlpsd.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlpsd.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlpsd.AwardData
         * @static
         * @param {jlpsd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlpsd.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpsd.AwardData
         * @static
         * @param {jlpsd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpsd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpsd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpsd.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpsd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpsd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlpsd.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpsd.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpsd.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpsd.AwardData)
                return object;
            let message = new $root.jlpsd.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpsd.AwardData
         * @static
         * @param {jlpsd.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlpsd.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlpsd.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpsd.AwardData";
        };

        return AwardData;
    })();

    jlpsd.MegaSingle = (function() {

        /**
         * Properties of a MegaSingle.
         * @memberof jlpsd
         * @interface IMegaSingle
         * @property {number|null} [Size] MegaSingle Size
         * @property {Array.<number>|null} [Pos] MegaSingle Pos
         */

        /**
         * Constructs a new MegaSingle.
         * @memberof jlpsd
         * @classdesc Represents a MegaSingle.
         * @implements IMegaSingle
         * @constructor
         * @param {jlpsd.IMegaSingle=} [properties] Properties to set
         */
        function MegaSingle(properties) {
            this.Pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MegaSingle Size.
         * @member {number} Size
         * @memberof jlpsd.MegaSingle
         * @instance
         */
        MegaSingle.prototype.Size = 0;

        /**
         * MegaSingle Pos.
         * @member {Array.<number>} Pos
         * @memberof jlpsd.MegaSingle
         * @instance
         */
        MegaSingle.prototype.Pos = $util.emptyArray;

        /**
         * Creates a new MegaSingle instance using the specified properties.
         * @function create
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {jlpsd.IMegaSingle=} [properties] Properties to set
         * @returns {jlpsd.MegaSingle} MegaSingle instance
         */
        MegaSingle.create = function create(properties) {
            return new MegaSingle(properties);
        };

        /**
         * Encodes the specified MegaSingle message. Does not implicitly {@link jlpsd.MegaSingle.verify|verify} messages.
         * @function encode
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {jlpsd.IMegaSingle} message MegaSingle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MegaSingle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Size != null && Object.hasOwnProperty.call(message, "Size"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Size);
            if (message.Pos != null && message.Pos.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Pos.length; ++i)
                    writer.int32(message.Pos[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified MegaSingle message, length delimited. Does not implicitly {@link jlpsd.MegaSingle.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {jlpsd.IMegaSingle} message MegaSingle message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MegaSingle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MegaSingle message from the specified reader or buffer.
         * @function decode
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpsd.MegaSingle} MegaSingle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MegaSingle.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpsd.MegaSingle();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Size = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.Pos && message.Pos.length))
                            message.Pos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pos.push(reader.int32());
                        } else
                            message.Pos.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MegaSingle message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpsd.MegaSingle} MegaSingle
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MegaSingle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MegaSingle message.
         * @function verify
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MegaSingle.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Size != null && message.hasOwnProperty("Size"))
                if (!$util.isInteger(message.Size))
                    return "Size: integer expected";
            if (message.Pos != null && message.hasOwnProperty("Pos")) {
                if (!Array.isArray(message.Pos))
                    return "Pos: array expected";
                for (let i = 0; i < message.Pos.length; ++i)
                    if (!$util.isInteger(message.Pos[i]))
                        return "Pos: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a MegaSingle message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpsd.MegaSingle} MegaSingle
         */
        MegaSingle.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpsd.MegaSingle)
                return object;
            let message = new $root.jlpsd.MegaSingle();
            if (object.Size != null)
                message.Size = object.Size | 0;
            if (object.Pos) {
                if (!Array.isArray(object.Pos))
                    throw TypeError(".jlpsd.MegaSingle.Pos: array expected");
                message.Pos = [];
                for (let i = 0; i < object.Pos.length; ++i)
                    message.Pos[i] = object.Pos[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a MegaSingle message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {jlpsd.MegaSingle} message MegaSingle
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MegaSingle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Pos = [];
            if (options.defaults)
                object.Size = 0;
            if (message.Size != null && message.hasOwnProperty("Size"))
                object.Size = message.Size;
            if (message.Pos && message.Pos.length) {
                object.Pos = [];
                for (let j = 0; j < message.Pos.length; ++j)
                    object.Pos[j] = message.Pos[j];
            }
            return object;
        };

        /**
         * Converts this MegaSingle to JSON.
         * @function toJSON
         * @memberof jlpsd.MegaSingle
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MegaSingle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MegaSingle
         * @function getTypeUrl
         * @memberof jlpsd.MegaSingle
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MegaSingle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpsd.MegaSingle";
        };

        return MegaSingle;
    })();

    jlpsd.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlpsd
         * @interface IComboStageInfo
         * @property {Array.<jlpsd.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {Array.<jlpsd.IColumn>|null} [ComboStageFinal] ComboStageInfo ComboStageFinal
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlpsd.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {boolean|null} [NeedEliminate] ComboStageInfo NeedEliminate
         * @property {Array.<jlpsd.IColumn>|null} [Eliminate] ComboStageInfo Eliminate
         * @property {Array.<jlpsd.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {number|null} [SpType] ComboStageInfo SpType
         * @property {number|null} [Mul] ComboStageInfo Mul
         * @property {number|null} [MegaSymbol] ComboStageInfo MegaSymbol
         * @property {Array.<jlpsd.IMegaSingle>|null} [MegaList] ComboStageInfo MegaList
         * @property {number|null} [UpTarget] ComboStageInfo UpTarget
         * @property {number|null} [UpFinal] ComboStageInfo UpFinal
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlpsd
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlpsd.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.ComboStageFinal = [];
            this.AwardDataVec = [];
            this.Eliminate = [];
            this.FillSymbols = [];
            this.MegaList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlpsd.IColumn>} ComboStageSymbol
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageFinal.
         * @member {Array.<jlpsd.IColumn>} ComboStageFinal
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageFinal = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlpsd.IAwardData>} AwardDataVec
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo NeedEliminate.
         * @member {boolean} NeedEliminate
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NeedEliminate = false;

        /**
         * ComboStageInfo Eliminate.
         * @member {Array.<jlpsd.IColumn>} Eliminate
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Eliminate = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlpsd.IColumn>} FillSymbols
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo SpType.
         * @member {number} SpType
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.SpType = 0;

        /**
         * ComboStageInfo Mul.
         * @member {number} Mul
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Mul = 0;

        /**
         * ComboStageInfo MegaSymbol.
         * @member {number} MegaSymbol
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MegaSymbol = 0;

        /**
         * ComboStageInfo MegaList.
         * @member {Array.<jlpsd.IMegaSingle>} MegaList
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MegaList = $util.emptyArray;

        /**
         * ComboStageInfo UpTarget.
         * @member {number} UpTarget
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.UpTarget = 0;

        /**
         * ComboStageInfo UpFinal.
         * @member {number} UpFinal
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.UpFinal = 0;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlpsd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {jlpsd.IComboStageInfo=} [properties] Properties to set
         * @returns {jlpsd.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlpsd.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {jlpsd.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlpsd.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageFinal != null && message.ComboStageFinal.length)
                for (let i = 0; i < message.ComboStageFinal.length; ++i)
                    $root.jlpsd.Column.encode(message.ComboStageFinal[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlpsd.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.NeedEliminate != null && Object.hasOwnProperty.call(message, "NeedEliminate"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.NeedEliminate);
            if (message.Eliminate != null && message.Eliminate.length)
                for (let i = 0; i < message.Eliminate.length; ++i)
                    $root.jlpsd.Column.encode(message.Eliminate[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlpsd.Column.encode(message.FillSymbols[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.SpType != null && Object.hasOwnProperty.call(message, "SpType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.SpType);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.Mul);
            if (message.MegaSymbol != null && Object.hasOwnProperty.call(message, "MegaSymbol"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.MegaSymbol);
            if (message.MegaList != null && message.MegaList.length)
                for (let i = 0; i < message.MegaList.length; ++i)
                    $root.jlpsd.MegaSingle.encode(message.MegaList[i], writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.UpTarget != null && Object.hasOwnProperty.call(message, "UpTarget"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.UpTarget);
            if (message.UpFinal != null && Object.hasOwnProperty.call(message, "UpFinal"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.UpFinal);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlpsd.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {jlpsd.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpsd.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpsd.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlpsd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.ComboStageFinal && message.ComboStageFinal.length))
                            message.ComboStageFinal = [];
                        message.ComboStageFinal.push($root.jlpsd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlpsd.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.NeedEliminate = reader.bool();
                        break;
                    }
                case 6: {
                        if (!(message.Eliminate && message.Eliminate.length))
                            message.Eliminate = [];
                        message.Eliminate.push($root.jlpsd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlpsd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.SpType = reader.int32();
                        break;
                    }
                case 9: {
                        message.Mul = reader.double();
                        break;
                    }
                case 10: {
                        message.MegaSymbol = reader.int32();
                        break;
                    }
                case 11: {
                        if (!(message.MegaList && message.MegaList.length))
                            message.MegaList = [];
                        message.MegaList.push($root.jlpsd.MegaSingle.decode(reader, reader.uint32()));
                        break;
                    }
                case 12: {
                        message.UpTarget = reader.int32();
                        break;
                    }
                case 13: {
                        message.UpFinal = reader.int32();
                        break;
                    }
                case 14: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpsd.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlpsd.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageFinal != null && message.hasOwnProperty("ComboStageFinal")) {
                if (!Array.isArray(message.ComboStageFinal))
                    return "ComboStageFinal: array expected";
                for (let i = 0; i < message.ComboStageFinal.length; ++i) {
                    let error = $root.jlpsd.Column.verify(message.ComboStageFinal[i]);
                    if (error)
                        return "ComboStageFinal." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlpsd.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                if (typeof message.NeedEliminate !== "boolean")
                    return "NeedEliminate: boolean expected";
            if (message.Eliminate != null && message.hasOwnProperty("Eliminate")) {
                if (!Array.isArray(message.Eliminate))
                    return "Eliminate: array expected";
                for (let i = 0; i < message.Eliminate.length; ++i) {
                    let error = $root.jlpsd.Column.verify(message.Eliminate[i]);
                    if (error)
                        return "Eliminate." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlpsd.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.SpType != null && message.hasOwnProperty("SpType"))
                if (!$util.isInteger(message.SpType))
                    return "SpType: integer expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            if (message.MegaSymbol != null && message.hasOwnProperty("MegaSymbol"))
                if (!$util.isInteger(message.MegaSymbol))
                    return "MegaSymbol: integer expected";
            if (message.MegaList != null && message.hasOwnProperty("MegaList")) {
                if (!Array.isArray(message.MegaList))
                    return "MegaList: array expected";
                for (let i = 0; i < message.MegaList.length; ++i) {
                    let error = $root.jlpsd.MegaSingle.verify(message.MegaList[i]);
                    if (error)
                        return "MegaList." + error;
                }
            }
            if (message.UpTarget != null && message.hasOwnProperty("UpTarget"))
                if (!$util.isInteger(message.UpTarget))
                    return "UpTarget: integer expected";
            if (message.UpFinal != null && message.hasOwnProperty("UpFinal"))
                if (!$util.isInteger(message.UpFinal))
                    return "UpFinal: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpsd.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpsd.ComboStageInfo)
                return object;
            let message = new $root.jlpsd.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlpsd.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlpsd.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlpsd.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageFinal) {
                if (!Array.isArray(object.ComboStageFinal))
                    throw TypeError(".jlpsd.ComboStageInfo.ComboStageFinal: array expected");
                message.ComboStageFinal = [];
                for (let i = 0; i < object.ComboStageFinal.length; ++i) {
                    if (typeof object.ComboStageFinal[i] !== "object")
                        throw TypeError(".jlpsd.ComboStageInfo.ComboStageFinal: object expected");
                    message.ComboStageFinal[i] = $root.jlpsd.Column.fromObject(object.ComboStageFinal[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlpsd.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlpsd.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlpsd.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.NeedEliminate != null)
                message.NeedEliminate = Boolean(object.NeedEliminate);
            if (object.Eliminate) {
                if (!Array.isArray(object.Eliminate))
                    throw TypeError(".jlpsd.ComboStageInfo.Eliminate: array expected");
                message.Eliminate = [];
                for (let i = 0; i < object.Eliminate.length; ++i) {
                    if (typeof object.Eliminate[i] !== "object")
                        throw TypeError(".jlpsd.ComboStageInfo.Eliminate: object expected");
                    message.Eliminate[i] = $root.jlpsd.Column.fromObject(object.Eliminate[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlpsd.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlpsd.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlpsd.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.SpType != null)
                message.SpType = object.SpType | 0;
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            if (object.MegaSymbol != null)
                message.MegaSymbol = object.MegaSymbol | 0;
            if (object.MegaList) {
                if (!Array.isArray(object.MegaList))
                    throw TypeError(".jlpsd.ComboStageInfo.MegaList: array expected");
                message.MegaList = [];
                for (let i = 0; i < object.MegaList.length; ++i) {
                    if (typeof object.MegaList[i] !== "object")
                        throw TypeError(".jlpsd.ComboStageInfo.MegaList: object expected");
                    message.MegaList[i] = $root.jlpsd.MegaSingle.fromObject(object.MegaList[i]);
                }
            }
            if (object.UpTarget != null)
                message.UpTarget = object.UpTarget | 0;
            if (object.UpFinal != null)
                message.UpFinal = object.UpFinal | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {jlpsd.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.ComboStageFinal = [];
                object.AwardDataVec = [];
                object.Eliminate = [];
                object.FillSymbols = [];
                object.MegaList = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.NeedEliminate = false;
                object.SpType = 0;
                object.Mul = 0;
                object.MegaSymbol = 0;
                object.UpTarget = 0;
                object.UpFinal = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlpsd.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageFinal && message.ComboStageFinal.length) {
                object.ComboStageFinal = [];
                for (let j = 0; j < message.ComboStageFinal.length; ++j)
                    object.ComboStageFinal[j] = $root.jlpsd.Column.toObject(message.ComboStageFinal[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlpsd.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                object.NeedEliminate = message.NeedEliminate;
            if (message.Eliminate && message.Eliminate.length) {
                object.Eliminate = [];
                for (let j = 0; j < message.Eliminate.length; ++j)
                    object.Eliminate[j] = $root.jlpsd.Column.toObject(message.Eliminate[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlpsd.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.SpType != null && message.hasOwnProperty("SpType"))
                object.SpType = message.SpType;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            if (message.MegaSymbol != null && message.hasOwnProperty("MegaSymbol"))
                object.MegaSymbol = message.MegaSymbol;
            if (message.MegaList && message.MegaList.length) {
                object.MegaList = [];
                for (let j = 0; j < message.MegaList.length; ++j)
                    object.MegaList[j] = $root.jlpsd.MegaSingle.toObject(message.MegaList[j], options);
            }
            if (message.UpTarget != null && message.hasOwnProperty("UpTarget"))
                object.UpTarget = message.UpTarget;
            if (message.UpFinal != null && message.hasOwnProperty("UpFinal"))
                object.UpFinal = message.UpFinal;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlpsd.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlpsd.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpsd.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlpsd.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlpsd
         * @interface IRoundInfo
         * @property {Array.<jlpsd.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRounds] RoundInfo AddRounds
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlpsd
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlpsd.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlpsd.IComboStageInfo>} ComboStageData
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRounds.
         * @member {number} AddRounds
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRounds = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlpsd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {jlpsd.IRoundInfo=} [properties] Properties to set
         * @returns {jlpsd.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlpsd.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {jlpsd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlpsd.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ScatterCount);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.AddRounds != null && Object.hasOwnProperty.call(message, "AddRounds"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AddRounds);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlpsd.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {jlpsd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpsd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpsd.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlpsd.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AddRounds = reader.int32();
                        break;
                    }
                case 8: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpsd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlpsd.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                if (!$util.isInteger(message.AddRounds))
                    return "AddRounds: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpsd.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpsd.RoundInfo)
                return object;
            let message = new $root.jlpsd.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlpsd.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlpsd.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlpsd.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRounds != null)
                message.AddRounds = object.AddRounds | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {jlpsd.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.ScatterCount = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
                object.AddRounds = 0;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlpsd.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                object.AddRounds = message.AddRounds;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlpsd.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlpsd.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpsd.RoundInfo";
        };

        return RoundInfo;
    })();

    jlpsd.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlpsd
         * @interface ISpinAck
         * @property {Array.<jlpsd.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlpsd
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlpsd.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlpsd.IRoundInfo>} RoundQueue
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlpsd.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlpsd.SpinAck
         * @static
         * @param {jlpsd.ISpinAck=} [properties] Properties to set
         * @returns {jlpsd.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlpsd.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlpsd.SpinAck
         * @static
         * @param {jlpsd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlpsd.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlpsd.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpsd.SpinAck
         * @static
         * @param {jlpsd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlpsd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpsd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpsd.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlpsd.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpsd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpsd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlpsd.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlpsd.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpsd.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpsd.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpsd.SpinAck)
                return object;
            let message = new $root.jlpsd.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlpsd.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlpsd.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlpsd.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpsd.SpinAck
         * @static
         * @param {jlpsd.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlpsd.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlpsd.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlpsd.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpsd.SpinAck";
        };

        return SpinAck;
    })();

    return jlpsd;
})();

export const jlpt = $root.jlpt = (() => {

    /**
     * Namespace jlpt.
     * @exports jlpt
     * @namespace
     */
    const jlpt = {};

    jlpt.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlpt
         * @interface IGrid
         * @property {number|null} [Col] Grid Col
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlpt
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlpt.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Col.
         * @member {number} Col
         * @memberof jlpt.Grid
         * @instance
         */
        Grid.prototype.Col = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlpt.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlpt.Grid
         * @static
         * @param {jlpt.IGrid=} [properties] Properties to set
         * @returns {jlpt.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlpt.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlpt.Grid
         * @static
         * @param {jlpt.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && Object.hasOwnProperty.call(message, "Col"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Col);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlpt.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.Grid
         * @static
         * @param {jlpt.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Col = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlpt.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col"))
                if (!$util.isInteger(message.Col))
                    return "Col: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.Grid)
                return object;
            let message = new $root.jlpt.Grid();
            if (object.Col != null)
                message.Col = object.Col | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.Grid
         * @static
         * @param {jlpt.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Col = 0;
                object.Row = 0;
            }
            if (message.Col != null && message.hasOwnProperty("Col"))
                object.Col = message.Col;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlpt.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlpt.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.Grid";
        };

        return Grid;
    })();

    jlpt.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlpt
         * @interface IColumn
         * @property {Array.<number>|null} [Column] Column Column
         */

        /**
         * Constructs a new Column.
         * @memberof jlpt
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlpt.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Column = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Column.
         * @member {Array.<number>} Column
         * @memberof jlpt.Column
         * @instance
         */
        Column.prototype.Column = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlpt.Column
         * @static
         * @param {jlpt.IColumn=} [properties] Properties to set
         * @returns {jlpt.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlpt.Column.verify|verify} messages.
         * @function encode
         * @memberof jlpt.Column
         * @static
         * @param {jlpt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && message.Column.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Column.length; ++i)
                    writer.int32(message.Column[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlpt.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.Column
         * @static
         * @param {jlpt.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Column && message.Column.length))
                            message.Column = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column.push(reader.int32());
                        } else
                            message.Column.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlpt.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column")) {
                if (!Array.isArray(message.Column))
                    return "Column: array expected";
                for (let i = 0; i < message.Column.length; ++i)
                    if (!$util.isInteger(message.Column[i]))
                        return "Column: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.Column)
                return object;
            let message = new $root.jlpt.Column();
            if (object.Column) {
                if (!Array.isArray(object.Column))
                    throw TypeError(".jlpt.Column.Column: array expected");
                message.Column = [];
                for (let i = 0; i < object.Column.length; ++i)
                    message.Column[i] = object.Column[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.Column
         * @static
         * @param {jlpt.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Column = [];
            if (message.Column && message.Column.length) {
                object.Column = [];
                for (let j = 0; j < message.Column.length; ++j)
                    object.Column[j] = message.Column[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlpt.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlpt.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.Column";
        };

        return Column;
    })();

    jlpt.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlpt
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<jlpt.IGrid>|null} [GridVec] AwardData GridVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlpt
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlpt.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.GridVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlpt.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlpt.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlpt.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlpt.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData GridVec.
         * @member {Array.<jlpt.IGrid>} GridVec
         * @memberof jlpt.AwardData
         * @instance
         */
        AwardData.prototype.GridVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlpt.AwardData
         * @static
         * @param {jlpt.IAwardData=} [properties] Properties to set
         * @returns {jlpt.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlpt.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlpt.AwardData
         * @static
         * @param {jlpt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.GridVec != null && message.GridVec.length)
                for (let i = 0; i < message.GridVec.length; ++i)
                    $root.jlpt.Grid.encode(message.GridVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlpt.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.AwardData
         * @static
         * @param {jlpt.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.GridVec && message.GridVec.length))
                            message.GridVec = [];
                        message.GridVec.push($root.jlpt.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlpt.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.GridVec != null && message.hasOwnProperty("GridVec")) {
                if (!Array.isArray(message.GridVec))
                    return "GridVec: array expected";
                for (let i = 0; i < message.GridVec.length; ++i) {
                    let error = $root.jlpt.Grid.verify(message.GridVec[i]);
                    if (error)
                        return "GridVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.AwardData)
                return object;
            let message = new $root.jlpt.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.GridVec) {
                if (!Array.isArray(object.GridVec))
                    throw TypeError(".jlpt.AwardData.GridVec: array expected");
                message.GridVec = [];
                for (let i = 0; i < object.GridVec.length; ++i) {
                    if (typeof object.GridVec[i] !== "object")
                        throw TypeError(".jlpt.AwardData.GridVec: object expected");
                    message.GridVec[i] = $root.jlpt.Grid.fromObject(object.GridVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.AwardData
         * @static
         * @param {jlpt.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.GridVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.GridVec && message.GridVec.length) {
                object.GridVec = [];
                for (let j = 0; j < message.GridVec.length; ++j)
                    object.GridVec[j] = $root.jlpt.Grid.toObject(message.GridVec[j], options);
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlpt.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlpt.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.AwardData";
        };

        return AwardData;
    })();

    jlpt.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlpt
         * @interface IComboStageInfo
         * @property {Array.<jlpt.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {number|null} [MysteryTarget] ComboStageInfo MysteryTarget
         * @property {Array.<jlpt.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlpt.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlpt.IColumn>|null} [NewFrameSymbols] ComboStageInfo NewFrameSymbols
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlpt
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlpt.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.NewFrameSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlpt.IColumn>} ComboStageSymbol
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {number} ComboStageMult
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = 0;

        /**
         * ComboStageInfo MysteryTarget.
         * @member {number} MysteryTarget
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MysteryTarget = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlpt.IAwardData>} AwardDataVec
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlpt.IColumn>} FillSymbols
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo NewFrameSymbols.
         * @member {Array.<jlpt.IColumn>} NewFrameSymbols
         * @memberof jlpt.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NewFrameSymbols = $util.emptyArray;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {jlpt.IComboStageInfo=} [properties] Properties to set
         * @returns {jlpt.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlpt.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {jlpt.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlpt.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboStageMult != null && Object.hasOwnProperty.call(message, "ComboStageMult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageMult);
            if (message.MysteryTarget != null && Object.hasOwnProperty.call(message, "MysteryTarget"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.MysteryTarget);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlpt.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlpt.Column.encode(message.FillSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.NewFrameSymbols != null && message.NewFrameSymbols.length)
                for (let i = 0; i < message.NewFrameSymbols.length; ++i)
                    $root.jlpt.Column.encode(message.NewFrameSymbols[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlpt.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {jlpt.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlpt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboStageMult = reader.double();
                        break;
                    }
                case 4: {
                        message.MysteryTarget = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlpt.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlpt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.NewFrameSymbols && message.NewFrameSymbols.length))
                            message.NewFrameSymbols = [];
                        message.NewFrameSymbols.push($root.jlpt.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlpt.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                if (typeof message.ComboStageMult !== "number")
                    return "ComboStageMult: number expected";
            if (message.MysteryTarget != null && message.hasOwnProperty("MysteryTarget"))
                if (!$util.isInteger(message.MysteryTarget))
                    return "MysteryTarget: integer expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlpt.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlpt.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.NewFrameSymbols != null && message.hasOwnProperty("NewFrameSymbols")) {
                if (!Array.isArray(message.NewFrameSymbols))
                    return "NewFrameSymbols: array expected";
                for (let i = 0; i < message.NewFrameSymbols.length; ++i) {
                    let error = $root.jlpt.Column.verify(message.NewFrameSymbols[i]);
                    if (error)
                        return "NewFrameSymbols." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.ComboStageInfo)
                return object;
            let message = new $root.jlpt.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlpt.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlpt.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlpt.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboStageMult != null)
                message.ComboStageMult = Number(object.ComboStageMult);
            if (object.MysteryTarget != null)
                message.MysteryTarget = object.MysteryTarget | 0;
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlpt.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlpt.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlpt.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlpt.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlpt.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlpt.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.NewFrameSymbols) {
                if (!Array.isArray(object.NewFrameSymbols))
                    throw TypeError(".jlpt.ComboStageInfo.NewFrameSymbols: array expected");
                message.NewFrameSymbols = [];
                for (let i = 0; i < object.NewFrameSymbols.length; ++i) {
                    if (typeof object.NewFrameSymbols[i] !== "object")
                        throw TypeError(".jlpt.ComboStageInfo.NewFrameSymbols: object expected");
                    message.NewFrameSymbols[i] = $root.jlpt.Column.fromObject(object.NewFrameSymbols[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {jlpt.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.NewFrameSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboStageMult = 0;
                object.MysteryTarget = 0;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlpt.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult"))
                object.ComboStageMult = options.json && !isFinite(message.ComboStageMult) ? String(message.ComboStageMult) : message.ComboStageMult;
            if (message.MysteryTarget != null && message.hasOwnProperty("MysteryTarget"))
                object.MysteryTarget = message.MysteryTarget;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlpt.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlpt.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.NewFrameSymbols && message.NewFrameSymbols.length) {
                object.NewFrameSymbols = [];
                for (let j = 0; j < message.NewFrameSymbols.length; ++j)
                    object.NewFrameSymbols[j] = $root.jlpt.Column.toObject(message.NewFrameSymbols[j], options);
            }
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlpt.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlpt.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlpt.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlpt
         * @interface IRoundInfo
         * @property {Array.<jlpt.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeCollectionCount] RoundInfo FreeCollectionCount
         * @property {number|null} [FreeCollectionLv] RoundInfo FreeCollectionLv
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlpt
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlpt.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlpt.IComboStageInfo>} ComboStageData
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeCollectionCount.
         * @member {number} FreeCollectionCount
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeCollectionCount = 0;

        /**
         * RoundInfo FreeCollectionLv.
         * @member {number} FreeCollectionLv
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeCollectionLv = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlpt.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlpt.RoundInfo
         * @static
         * @param {jlpt.IRoundInfo=} [properties] Properties to set
         * @returns {jlpt.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlpt.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpt.RoundInfo
         * @static
         * @param {jlpt.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlpt.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeCollectionCount != null && Object.hasOwnProperty.call(message, "FreeCollectionCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeCollectionCount);
            if (message.FreeCollectionLv != null && Object.hasOwnProperty.call(message, "FreeCollectionLv"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeCollectionLv);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlpt.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.RoundInfo
         * @static
         * @param {jlpt.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlpt.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeCollectionCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeCollectionLv = reader.int32();
                        break;
                    }
                case 7: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlpt.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlpt.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeCollectionCount != null && message.hasOwnProperty("FreeCollectionCount"))
                if (!$util.isInteger(message.FreeCollectionCount))
                    return "FreeCollectionCount: integer expected";
            if (message.FreeCollectionLv != null && message.hasOwnProperty("FreeCollectionLv"))
                if (!$util.isInteger(message.FreeCollectionLv))
                    return "FreeCollectionLv: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.RoundInfo)
                return object;
            let message = new $root.jlpt.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlpt.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlpt.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlpt.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeCollectionCount != null)
                message.FreeCollectionCount = object.FreeCollectionCount | 0;
            if (object.FreeCollectionLv != null)
                message.FreeCollectionLv = object.FreeCollectionLv | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.RoundInfo
         * @static
         * @param {jlpt.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeCollectionCount = 0;
                object.FreeCollectionLv = 0;
                object.ScatterCount = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlpt.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeCollectionCount != null && message.hasOwnProperty("FreeCollectionCount"))
                object.FreeCollectionCount = message.FreeCollectionCount;
            if (message.FreeCollectionLv != null && message.hasOwnProperty("FreeCollectionLv"))
                object.FreeCollectionLv = message.FreeCollectionLv;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlpt.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlpt.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.RoundInfo";
        };

        return RoundInfo;
    })();

    jlpt.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlpt
         * @interface ISpinAck
         * @property {Array.<jlpt.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlpt
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlpt.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlpt.IRoundInfo>} RoundQueue
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlpt.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlpt.SpinAck
         * @static
         * @param {jlpt.ISpinAck=} [properties] Properties to set
         * @returns {jlpt.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlpt.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlpt.SpinAck
         * @static
         * @param {jlpt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlpt.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlpt.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.SpinAck
         * @static
         * @param {jlpt.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlpt.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlpt.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlpt.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.SpinAck)
                return object;
            let message = new $root.jlpt.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlpt.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlpt.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlpt.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.SpinAck
         * @static
         * @param {jlpt.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlpt.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlpt.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlpt.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.SpinAck";
        };

        return SpinAck;
    })();

    jlpt.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlpt
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlpt
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlpt.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlpt.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlpt.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlpt.GameInfoData
         * @static
         * @param {jlpt.IGameInfoData=} [properties] Properties to set
         * @returns {jlpt.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlpt.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlpt.GameInfoData
         * @static
         * @param {jlpt.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlpt.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpt.GameInfoData
         * @static
         * @param {jlpt.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpt.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpt.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpt.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpt.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpt.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlpt.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpt.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpt.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpt.GameInfoData)
                return object;
            let message = new $root.jlpt.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlpt.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpt.GameInfoData
         * @static
         * @param {jlpt.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlpt.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlpt.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpt.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlpt;
})();

export const jlpw = $root.jlpw = (() => {

    /**
     * Namespace jlpw.
     * @exports jlpw
     * @namespace
     */
    const jlpw = {};

    jlpw.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlpw
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlpw
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlpw.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlpw.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlpw.Column
         * @static
         * @param {jlpw.IColumn=} [properties] Properties to set
         * @returns {jlpw.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlpw.Column.verify|verify} messages.
         * @function encode
         * @memberof jlpw.Column
         * @static
         * @param {jlpw.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlpw.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpw.Column
         * @static
         * @param {jlpw.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlpw.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpw.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpw.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpw.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpw.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlpw.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpw.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpw.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpw.Column)
                return object;
            let message = new $root.jlpw.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlpw.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpw.Column
         * @static
         * @param {jlpw.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlpw.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlpw.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpw.Column";
        };

        return Column;
    })();

    jlpw.Single = (function() {

        /**
         * Properties of a Single.
         * @memberof jlpw
         * @interface ISingle
         * @property {Array.<jlpw.IColumn>|null} [Col] Single Col
         */

        /**
         * Constructs a new Single.
         * @memberof jlpw
         * @classdesc Represents a Single.
         * @implements ISingle
         * @constructor
         * @param {jlpw.ISingle=} [properties] Properties to set
         */
        function Single(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Single Col.
         * @member {Array.<jlpw.IColumn>} Col
         * @memberof jlpw.Single
         * @instance
         */
        Single.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Single instance using the specified properties.
         * @function create
         * @memberof jlpw.Single
         * @static
         * @param {jlpw.ISingle=} [properties] Properties to set
         * @returns {jlpw.Single} Single instance
         */
        Single.create = function create(properties) {
            return new Single(properties);
        };

        /**
         * Encodes the specified Single message. Does not implicitly {@link jlpw.Single.verify|verify} messages.
         * @function encode
         * @memberof jlpw.Single
         * @static
         * @param {jlpw.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length)
                for (let i = 0; i < message.Col.length; ++i)
                    $root.jlpw.Column.encode(message.Col[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Single message, length delimited. Does not implicitly {@link jlpw.Single.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpw.Single
         * @static
         * @param {jlpw.ISingle} message Single message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Single.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Single message from the specified reader or buffer.
         * @function decode
         * @memberof jlpw.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpw.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpw.Single();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        message.Col.push($root.jlpw.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Single message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpw.Single
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpw.Single} Single
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Single.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Single message.
         * @function verify
         * @memberof jlpw.Single
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Single.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i) {
                    let error = $root.jlpw.Column.verify(message.Col[i]);
                    if (error)
                        return "Col." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Single message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpw.Single
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpw.Single} Single
         */
        Single.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpw.Single)
                return object;
            let message = new $root.jlpw.Single();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlpw.Single.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i) {
                    if (typeof object.Col[i] !== "object")
                        throw TypeError(".jlpw.Single.Col: object expected");
                    message.Col[i] = $root.jlpw.Column.fromObject(object.Col[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Single message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpw.Single
         * @static
         * @param {jlpw.Single} message Single
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Single.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = $root.jlpw.Column.toObject(message.Col[j], options);
            }
            return object;
        };

        /**
         * Converts this Single to JSON.
         * @function toJSON
         * @memberof jlpw.Single
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Single.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Single
         * @function getTypeUrl
         * @memberof jlpw.Single
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Single.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpw.Single";
        };

        return Single;
    })();

    jlpw.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlpw
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlpw
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlpw.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlpw.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlpw.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlpw.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlpw.AwardData
         * @static
         * @param {jlpw.IAwardData=} [properties] Properties to set
         * @returns {jlpw.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlpw.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlpw.AwardData
         * @static
         * @param {jlpw.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlpw.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpw.AwardData
         * @static
         * @param {jlpw.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpw.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpw.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpw.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Line = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpw.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpw.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlpw.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpw.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpw.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpw.AwardData)
                return object;
            let message = new $root.jlpw.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpw.AwardData
         * @static
         * @param {jlpw.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlpw.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlpw.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpw.AwardData";
        };

        return AwardData;
    })();

    jlpw.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlpw
         * @interface ISpinAck
         * @property {Array.<jlpw.ISingle>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {Array.<jlpw.IAwardData>|null} [AwardDataVec] SpinAck AwardDataVec
         * @property {number|null} [Mult] SpinAck Mult
         * @property {boolean|null} [GetBonus] SpinAck GetBonus
         * @property {number|null} [BonusSymbol] SpinAck BonusSymbol
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [AwardTypeFlag] SpinAck AwardTypeFlag
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlpw
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlpw.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<jlpw.ISingle>} PlateSymbol
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck AwardDataVec.
         * @member {Array.<jlpw.IAwardData>} AwardDataVec
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SpinAck Mult.
         * @member {number} Mult
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.Mult = 0;

        /**
         * SpinAck GetBonus.
         * @member {boolean} GetBonus
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.GetBonus = false;

        /**
         * SpinAck BonusSymbol.
         * @member {number} BonusSymbol
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusSymbol = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardTypeFlag = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlpw.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlpw.SpinAck
         * @static
         * @param {jlpw.ISpinAck=} [properties] Properties to set
         * @returns {jlpw.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlpw.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlpw.SpinAck
         * @static
         * @param {jlpw.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlpw.Single.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlpw.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Mult);
            if (message.GetBonus != null && Object.hasOwnProperty.call(message, "GetBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.GetBonus);
            if (message.BonusSymbol != null && Object.hasOwnProperty.call(message, "BonusSymbol"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.BonusSymbol);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.Extra);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AwardTypeFlag);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 12, wireType 1 =*/97).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlpw.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpw.SpinAck
         * @static
         * @param {jlpw.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlpw.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpw.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpw.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlpw.Single.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlpw.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.Mult = reader.double();
                        break;
                    }
                case 4: {
                        message.GetBonus = reader.bool();
                        break;
                    }
                case 5: {
                        message.BonusSymbol = reader.int32();
                        break;
                    }
                case 6: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 7: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 8: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 9: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 10: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 11: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 12: {
                        message.RTP = reader.double();
                        break;
                    }
                case 13: {
                        message.LID = reader.int32();
                        break;
                    }
                case 14: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpw.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpw.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlpw.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlpw.Single.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlpw.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                if (typeof message.GetBonus !== "boolean")
                    return "GetBonus: boolean expected";
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                if (!$util.isInteger(message.BonusSymbol))
                    return "BonusSymbol: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpw.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpw.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpw.SpinAck)
                return object;
            let message = new $root.jlpw.SpinAck();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlpw.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlpw.SpinAck.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlpw.Single.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlpw.SpinAck.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlpw.SpinAck.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlpw.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.GetBonus != null)
                message.GetBonus = Boolean(object.GetBonus);
            if (object.BonusSymbol != null)
                message.BonusSymbol = object.BonusSymbol | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpw.SpinAck
         * @static
         * @param {jlpw.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.Mult = 0;
                object.GetBonus = false;
                object.BonusSymbol = 0;
                object.Extra = false;
                object.AwardTypeFlag = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlpw.Single.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlpw.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.GetBonus != null && message.hasOwnProperty("GetBonus"))
                object.GetBonus = message.GetBonus;
            if (message.BonusSymbol != null && message.hasOwnProperty("BonusSymbol"))
                object.BonusSymbol = message.BonusSymbol;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlpw.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlpw.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpw.SpinAck";
        };

        return SpinAck;
    })();

    jlpw.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlpw
         * @interface IGameInfoData
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlpw
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlpw.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlpw.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlpw.GameInfoData
         * @static
         * @param {jlpw.IGameInfoData=} [properties] Properties to set
         * @returns {jlpw.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlpw.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlpw.GameInfoData
         * @static
         * @param {jlpw.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlpw.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpw.GameInfoData
         * @static
         * @param {jlpw.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpw.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpw.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpw.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpw.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpw.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlpw.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpw.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpw.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpw.GameInfoData)
                return object;
            let message = new $root.jlpw.GameInfoData();
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlpw.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpw.GameInfoData
         * @static
         * @param {jlpw.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlpw.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlpw.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpw.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlpw;
})();

export const jlpx = $root.jlpx = (() => {

    /**
     * Namespace jlpx.
     * @exports jlpx
     * @namespace
     */
    const jlpx = {};

    jlpx.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlpx
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlpx
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlpx.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlpx.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlpx.Column
         * @static
         * @param {jlpx.IColumn=} [properties] Properties to set
         * @returns {jlpx.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlpx.Column.verify|verify} messages.
         * @function encode
         * @memberof jlpx.Column
         * @static
         * @param {jlpx.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlpx.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpx.Column
         * @static
         * @param {jlpx.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlpx.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpx.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpx.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpx.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpx.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlpx.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpx.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpx.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpx.Column)
                return object;
            let message = new $root.jlpx.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlpx.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpx.Column
         * @static
         * @param {jlpx.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlpx.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlpx.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpx.Column";
        };

        return Column;
    })();

    jlpx.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlpx
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlpx
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlpx.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlpx.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlpx.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlpx.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlpx.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlpx.AwardData
         * @static
         * @param {jlpx.IAwardData=} [properties] Properties to set
         * @returns {jlpx.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlpx.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlpx.AwardData
         * @static
         * @param {jlpx.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlpx.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpx.AwardData
         * @static
         * @param {jlpx.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpx.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpx.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpx.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpx.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpx.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlpx.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpx.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpx.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpx.AwardData)
                return object;
            let message = new $root.jlpx.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpx.AwardData
         * @static
         * @param {jlpx.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlpx.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlpx.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpx.AwardData";
        };

        return AwardData;
    })();

    jlpx.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlpx
         * @interface IComboStageInfo
         * @property {Array.<jlpx.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {Array.<jlpx.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {number|null} [PlateHitSum] ComboStageInfo PlateHitSum
         * @property {number|null} [PlateLevel] ComboStageInfo PlateLevel
         * @property {Array.<number>|null} [EliminatedPos] ComboStageInfo EliminatedPos
         * @property {Array.<jlpx.IAwardData>|null} [TempAward] ComboStageInfo TempAward
         * @property {Array.<number>|null} [FGChange] ComboStageInfo FGChange
         * @property {Array.<jlpx.IColumn>|null} [ComboStageSymbolChanged] ComboStageInfo ComboStageSymbolChanged
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlpx
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlpx.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.FillSymbols = [];
            this.EliminatedPos = [];
            this.TempAward = [];
            this.FGChange = [];
            this.ComboStageSymbolChanged = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlpx.IColumn>} ComboStageSymbol
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlpx.IColumn>} FillSymbols
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo PlateHitSum.
         * @member {number} PlateHitSum
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.PlateHitSum = 0;

        /**
         * ComboStageInfo PlateLevel.
         * @member {number} PlateLevel
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.PlateLevel = 0;

        /**
         * ComboStageInfo EliminatedPos.
         * @member {Array.<number>} EliminatedPos
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.EliminatedPos = $util.emptyArray;

        /**
         * ComboStageInfo TempAward.
         * @member {Array.<jlpx.IAwardData>} TempAward
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.TempAward = $util.emptyArray;

        /**
         * ComboStageInfo FGChange.
         * @member {Array.<number>} FGChange
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FGChange = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageSymbolChanged.
         * @member {Array.<jlpx.IColumn>} ComboStageSymbolChanged
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbolChanged = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlpx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {jlpx.IComboStageInfo=} [properties] Properties to set
         * @returns {jlpx.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlpx.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {jlpx.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlpx.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlpx.Column.encode(message.FillSymbols[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.PlateHitSum != null && Object.hasOwnProperty.call(message, "PlateHitSum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.PlateHitSum);
            if (message.PlateLevel != null && Object.hasOwnProperty.call(message, "PlateLevel"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.PlateLevel);
            if (message.EliminatedPos != null && message.EliminatedPos.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.EliminatedPos.length; ++i)
                    writer.int32(message.EliminatedPos[i]);
                writer.ldelim();
            }
            if (message.TempAward != null && message.TempAward.length)
                for (let i = 0; i < message.TempAward.length; ++i)
                    $root.jlpx.AwardData.encode(message.TempAward[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.FGChange != null && message.FGChange.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.FGChange.length; ++i)
                    writer.int32(message.FGChange[i]);
                writer.ldelim();
            }
            if (message.ComboStageSymbolChanged != null && message.ComboStageSymbolChanged.length)
                for (let i = 0; i < message.ComboStageSymbolChanged.length; ++i)
                    $root.jlpx.Column.encode(message.ComboStageSymbolChanged[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlpx.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {jlpx.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpx.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpx.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlpx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlpx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.PlateHitSum = reader.int32();
                        break;
                    }
                case 4: {
                        message.PlateLevel = reader.int32();
                        break;
                    }
                case 5: {
                        if (!(message.EliminatedPos && message.EliminatedPos.length))
                            message.EliminatedPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.EliminatedPos.push(reader.int32());
                        } else
                            message.EliminatedPos.push(reader.int32());
                        break;
                    }
                case 6: {
                        if (!(message.TempAward && message.TempAward.length))
                            message.TempAward = [];
                        message.TempAward.push($root.jlpx.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.FGChange && message.FGChange.length))
                            message.FGChange = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.FGChange.push(reader.int32());
                        } else
                            message.FGChange.push(reader.int32());
                        break;
                    }
                case 8: {
                        if (!(message.ComboStageSymbolChanged && message.ComboStageSymbolChanged.length))
                            message.ComboStageSymbolChanged = [];
                        message.ComboStageSymbolChanged.push($root.jlpx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpx.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlpx.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlpx.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.PlateHitSum != null && message.hasOwnProperty("PlateHitSum"))
                if (!$util.isInteger(message.PlateHitSum))
                    return "PlateHitSum: integer expected";
            if (message.PlateLevel != null && message.hasOwnProperty("PlateLevel"))
                if (!$util.isInteger(message.PlateLevel))
                    return "PlateLevel: integer expected";
            if (message.EliminatedPos != null && message.hasOwnProperty("EliminatedPos")) {
                if (!Array.isArray(message.EliminatedPos))
                    return "EliminatedPos: array expected";
                for (let i = 0; i < message.EliminatedPos.length; ++i)
                    if (!$util.isInteger(message.EliminatedPos[i]))
                        return "EliminatedPos: integer[] expected";
            }
            if (message.TempAward != null && message.hasOwnProperty("TempAward")) {
                if (!Array.isArray(message.TempAward))
                    return "TempAward: array expected";
                for (let i = 0; i < message.TempAward.length; ++i) {
                    let error = $root.jlpx.AwardData.verify(message.TempAward[i]);
                    if (error)
                        return "TempAward." + error;
                }
            }
            if (message.FGChange != null && message.hasOwnProperty("FGChange")) {
                if (!Array.isArray(message.FGChange))
                    return "FGChange: array expected";
                for (let i = 0; i < message.FGChange.length; ++i)
                    if (!$util.isInteger(message.FGChange[i]))
                        return "FGChange: integer[] expected";
            }
            if (message.ComboStageSymbolChanged != null && message.hasOwnProperty("ComboStageSymbolChanged")) {
                if (!Array.isArray(message.ComboStageSymbolChanged))
                    return "ComboStageSymbolChanged: array expected";
                for (let i = 0; i < message.ComboStageSymbolChanged.length; ++i) {
                    let error = $root.jlpx.Column.verify(message.ComboStageSymbolChanged[i]);
                    if (error)
                        return "ComboStageSymbolChanged." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpx.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpx.ComboStageInfo)
                return object;
            let message = new $root.jlpx.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlpx.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlpx.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlpx.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlpx.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlpx.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlpx.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.PlateHitSum != null)
                message.PlateHitSum = object.PlateHitSum | 0;
            if (object.PlateLevel != null)
                message.PlateLevel = object.PlateLevel | 0;
            if (object.EliminatedPos) {
                if (!Array.isArray(object.EliminatedPos))
                    throw TypeError(".jlpx.ComboStageInfo.EliminatedPos: array expected");
                message.EliminatedPos = [];
                for (let i = 0; i < object.EliminatedPos.length; ++i)
                    message.EliminatedPos[i] = object.EliminatedPos[i] | 0;
            }
            if (object.TempAward) {
                if (!Array.isArray(object.TempAward))
                    throw TypeError(".jlpx.ComboStageInfo.TempAward: array expected");
                message.TempAward = [];
                for (let i = 0; i < object.TempAward.length; ++i) {
                    if (typeof object.TempAward[i] !== "object")
                        throw TypeError(".jlpx.ComboStageInfo.TempAward: object expected");
                    message.TempAward[i] = $root.jlpx.AwardData.fromObject(object.TempAward[i]);
                }
            }
            if (object.FGChange) {
                if (!Array.isArray(object.FGChange))
                    throw TypeError(".jlpx.ComboStageInfo.FGChange: array expected");
                message.FGChange = [];
                for (let i = 0; i < object.FGChange.length; ++i)
                    message.FGChange[i] = object.FGChange[i] | 0;
            }
            if (object.ComboStageSymbolChanged) {
                if (!Array.isArray(object.ComboStageSymbolChanged))
                    throw TypeError(".jlpx.ComboStageInfo.ComboStageSymbolChanged: array expected");
                message.ComboStageSymbolChanged = [];
                for (let i = 0; i < object.ComboStageSymbolChanged.length; ++i) {
                    if (typeof object.ComboStageSymbolChanged[i] !== "object")
                        throw TypeError(".jlpx.ComboStageInfo.ComboStageSymbolChanged: object expected");
                    message.ComboStageSymbolChanged[i] = $root.jlpx.Column.fromObject(object.ComboStageSymbolChanged[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {jlpx.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.FillSymbols = [];
                object.EliminatedPos = [];
                object.TempAward = [];
                object.FGChange = [];
                object.ComboStageSymbolChanged = [];
            }
            if (options.defaults) {
                object.PlateHitSum = 0;
                object.PlateLevel = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlpx.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlpx.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.PlateHitSum != null && message.hasOwnProperty("PlateHitSum"))
                object.PlateHitSum = message.PlateHitSum;
            if (message.PlateLevel != null && message.hasOwnProperty("PlateLevel"))
                object.PlateLevel = message.PlateLevel;
            if (message.EliminatedPos && message.EliminatedPos.length) {
                object.EliminatedPos = [];
                for (let j = 0; j < message.EliminatedPos.length; ++j)
                    object.EliminatedPos[j] = message.EliminatedPos[j];
            }
            if (message.TempAward && message.TempAward.length) {
                object.TempAward = [];
                for (let j = 0; j < message.TempAward.length; ++j)
                    object.TempAward[j] = $root.jlpx.AwardData.toObject(message.TempAward[j], options);
            }
            if (message.FGChange && message.FGChange.length) {
                object.FGChange = [];
                for (let j = 0; j < message.FGChange.length; ++j)
                    object.FGChange[j] = message.FGChange[j];
            }
            if (message.ComboStageSymbolChanged && message.ComboStageSymbolChanged.length) {
                object.ComboStageSymbolChanged = [];
                for (let j = 0; j < message.ComboStageSymbolChanged.length; ++j)
                    object.ComboStageSymbolChanged[j] = $root.jlpx.Column.toObject(message.ComboStageSymbolChanged[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlpx.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlpx.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpx.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlpx.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlpx
         * @interface IRoundInfo
         * @property {Array.<jlpx.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {Array.<jlpx.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlpx
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlpx.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlpx.IComboStageInfo>} ComboStageData
         * @memberof jlpx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlpx.IAwardData>} AwardDataVec
         * @memberof jlpx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlpx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlpx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlpx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlpx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlpx.RoundInfo
         * @static
         * @param {jlpx.IRoundInfo=} [properties] Properties to set
         * @returns {jlpx.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlpx.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlpx.RoundInfo
         * @static
         * @param {jlpx.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlpx.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlpx.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlpx.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpx.RoundInfo
         * @static
         * @param {jlpx.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlpx.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpx.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpx.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlpx.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlpx.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpx.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpx.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlpx.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlpx.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlpx.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpx.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpx.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpx.RoundInfo)
                return object;
            let message = new $root.jlpx.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlpx.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlpx.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlpx.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlpx.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlpx.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlpx.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpx.RoundInfo
         * @static
         * @param {jlpx.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageData = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlpx.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlpx.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlpx.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlpx.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpx.RoundInfo";
        };

        return RoundInfo;
    })();

    jlpx.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlpx
         * @interface ISpinAck
         * @property {Array.<jlpx.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlpx
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlpx.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlpx.IRoundInfo>} RoundQueue
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlpx.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlpx.SpinAck
         * @static
         * @param {jlpx.ISpinAck=} [properties] Properties to set
         * @returns {jlpx.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlpx.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlpx.SpinAck
         * @static
         * @param {jlpx.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlpx.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlpx.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpx.SpinAck
         * @static
         * @param {jlpx.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlpx.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpx.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpx.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlpx.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Bet = reader.double();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpx.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpx.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlpx.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlpx.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpx.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpx.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpx.SpinAck)
                return object;
            let message = new $root.jlpx.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlpx.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlpx.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlpx.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpx.SpinAck
         * @static
         * @param {jlpx.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlpx.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlpx.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlpx.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpx.SpinAck";
        };

        return SpinAck;
    })();

    jlpx.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlpx
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlpx
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlpx.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlpx.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlpx.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlpx.GameInfoData
         * @static
         * @param {jlpx.IGameInfoData=} [properties] Properties to set
         * @returns {jlpx.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlpx.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlpx.GameInfoData
         * @static
         * @param {jlpx.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlpx.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlpx.GameInfoData
         * @static
         * @param {jlpx.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlpx.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlpx.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlpx.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlpx.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlpx.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlpx.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlpx.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlpx.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlpx.GameInfoData)
                return object;
            let message = new $root.jlpx.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlpx.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlpx.GameInfoData
         * @static
         * @param {jlpx.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlpx.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlpx.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlpx.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlpx;
})();

export const jlrx = $root.jlrx = (() => {

    /**
     * Namespace jlrx.
     * @exports jlrx
     * @namespace
     */
    const jlrx = {};

    jlrx.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlrx
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlrx
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlrx.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlrx.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlrx.Column
         * @static
         * @param {jlrx.IColumn=} [properties] Properties to set
         * @returns {jlrx.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlrx.Column.verify|verify} messages.
         * @function encode
         * @memberof jlrx.Column
         * @static
         * @param {jlrx.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlrx.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.Column
         * @static
         * @param {jlrx.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlrx.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.Column)
                return object;
            let message = new $root.jlrx.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlrx.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.Column
         * @static
         * @param {jlrx.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlrx.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlrx.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.Column";
        };

        return Column;
    })();

    jlrx.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlrx
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlrx
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlrx.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlrx.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlrx.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlrx.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlrx.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlrx.AwardData
         * @static
         * @param {jlrx.IAwardData=} [properties] Properties to set
         * @returns {jlrx.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlrx.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlrx.AwardData
         * @static
         * @param {jlrx.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlrx.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.AwardData
         * @static
         * @param {jlrx.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlrx.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.AwardData)
                return object;
            let message = new $root.jlrx.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.AwardData
         * @static
         * @param {jlrx.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlrx.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlrx.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.AwardData";
        };

        return AwardData;
    })();

    jlrx.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlrx
         * @interface IComboStageInfo
         * @property {Array.<jlrx.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlrx.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlrx.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {boolean|null} [BonusHit] ComboStageInfo BonusHit
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlrx
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlrx.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlrx.IColumn>} ComboStageSymbol
         * @memberof jlrx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlrx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlrx.IAwardData>} AwardDataVec
         * @memberof jlrx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlrx.IColumn>} FillSymbols
         * @memberof jlrx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo BonusHit.
         * @member {boolean} BonusHit
         * @memberof jlrx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.BonusHit = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {jlrx.IComboStageInfo=} [properties] Properties to set
         * @returns {jlrx.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlrx.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {jlrx.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlrx.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlrx.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlrx.Column.encode(message.FillSymbols[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.BonusHit != null && Object.hasOwnProperty.call(message, "BonusHit"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.BonusHit);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlrx.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {jlrx.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlrx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlrx.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlrx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.BonusHit = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlrx.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlrx.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlrx.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.BonusHit != null && message.hasOwnProperty("BonusHit"))
                if (typeof message.BonusHit !== "boolean")
                    return "BonusHit: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.ComboStageInfo)
                return object;
            let message = new $root.jlrx.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlrx.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlrx.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlrx.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlrx.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlrx.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlrx.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlrx.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlrx.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlrx.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.BonusHit != null)
                message.BonusHit = Boolean(object.BonusHit);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {jlrx.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.BonusHit = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlrx.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlrx.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlrx.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.BonusHit != null && message.hasOwnProperty("BonusHit"))
                object.BonusHit = message.BonusHit;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlrx.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlrx.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlrx.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlrx
         * @interface IRoundInfo
         * @property {Array.<jlrx.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlrx
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlrx.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlrx.IComboStageInfo>} ComboStageData
         * @memberof jlrx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlrx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlrx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlrx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlrx.RoundInfo
         * @static
         * @param {jlrx.IRoundInfo=} [properties] Properties to set
         * @returns {jlrx.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlrx.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlrx.RoundInfo
         * @static
         * @param {jlrx.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlrx.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlrx.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.RoundInfo
         * @static
         * @param {jlrx.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlrx.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlrx.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlrx.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.RoundInfo)
                return object;
            let message = new $root.jlrx.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlrx.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlrx.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlrx.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.RoundInfo
         * @static
         * @param {jlrx.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlrx.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlrx.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlrx.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.RoundInfo";
        };

        return RoundInfo;
    })();

    jlrx.BonusGameInfo = (function() {

        /**
         * Properties of a BonusGameInfo.
         * @memberof jlrx
         * @interface IBonusGameInfo
         * @property {number|null} [Symbol] BonusGameInfo Symbol
         * @property {number|null} [Result] BonusGameInfo Result
         * @property {number|null} [Win] BonusGameInfo Win
         * @property {number|null} [HealthPoint] BonusGameInfo HealthPoint
         */

        /**
         * Constructs a new BonusGameInfo.
         * @memberof jlrx
         * @classdesc Represents a BonusGameInfo.
         * @implements IBonusGameInfo
         * @constructor
         * @param {jlrx.IBonusGameInfo=} [properties] Properties to set
         */
        function BonusGameInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusGameInfo Symbol.
         * @member {number} Symbol
         * @memberof jlrx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Symbol = 0;

        /**
         * BonusGameInfo Result.
         * @member {number} Result
         * @memberof jlrx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Result = 0;

        /**
         * BonusGameInfo Win.
         * @member {number} Win
         * @memberof jlrx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Win = 0;

        /**
         * BonusGameInfo HealthPoint.
         * @member {number} HealthPoint
         * @memberof jlrx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.HealthPoint = 0;

        /**
         * Creates a new BonusGameInfo instance using the specified properties.
         * @function create
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {jlrx.IBonusGameInfo=} [properties] Properties to set
         * @returns {jlrx.BonusGameInfo} BonusGameInfo instance
         */
        BonusGameInfo.create = function create(properties) {
            return new BonusGameInfo(properties);
        };

        /**
         * Encodes the specified BonusGameInfo message. Does not implicitly {@link jlrx.BonusGameInfo.verify|verify} messages.
         * @function encode
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {jlrx.IBonusGameInfo} message BonusGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Result != null && Object.hasOwnProperty.call(message, "Result"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Result);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.HealthPoint != null && Object.hasOwnProperty.call(message, "HealthPoint"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.HealthPoint);
            return writer;
        };

        /**
         * Encodes the specified BonusGameInfo message, length delimited. Does not implicitly {@link jlrx.BonusGameInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {jlrx.IBonusGameInfo} message BonusGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusGameInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.BonusGameInfo} BonusGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.BonusGameInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Result = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.HealthPoint = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusGameInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.BonusGameInfo} BonusGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusGameInfo message.
         * @function verify
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusGameInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Result != null && message.hasOwnProperty("Result"))
                if (!$util.isInteger(message.Result))
                    return "Result: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.HealthPoint != null && message.hasOwnProperty("HealthPoint"))
                if (!$util.isInteger(message.HealthPoint))
                    return "HealthPoint: integer expected";
            return null;
        };

        /**
         * Creates a BonusGameInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.BonusGameInfo} BonusGameInfo
         */
        BonusGameInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.BonusGameInfo)
                return object;
            let message = new $root.jlrx.BonusGameInfo();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Result != null)
                message.Result = object.Result | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.HealthPoint != null)
                message.HealthPoint = object.HealthPoint | 0;
            return message;
        };

        /**
         * Creates a plain object from a BonusGameInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {jlrx.BonusGameInfo} message BonusGameInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusGameInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Result = 0;
                object.Win = 0;
                object.HealthPoint = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Result != null && message.hasOwnProperty("Result"))
                object.Result = message.Result;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.HealthPoint != null && message.hasOwnProperty("HealthPoint"))
                object.HealthPoint = message.HealthPoint;
            return object;
        };

        /**
         * Converts this BonusGameInfo to JSON.
         * @function toJSON
         * @memberof jlrx.BonusGameInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusGameInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusGameInfo
         * @function getTypeUrl
         * @memberof jlrx.BonusGameInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.BonusGameInfo";
        };

        return BonusGameInfo;
    })();

    jlrx.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlrx
         * @interface ISpinAck
         * @property {Array.<jlrx.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {Array.<jlrx.IBonusGameInfo>|null} [BonusInfo] SpinAck BonusInfo
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlrx
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlrx.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.BonusInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlrx.IRoundInfo>} RoundQueue
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck BonusInfo.
         * @member {Array.<jlrx.IBonusGameInfo>} BonusInfo
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusInfo = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlrx.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlrx.SpinAck
         * @static
         * @param {jlrx.ISpinAck=} [properties] Properties to set
         * @returns {jlrx.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlrx.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlrx.SpinAck
         * @static
         * @param {jlrx.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlrx.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.BonusInfo != null && message.BonusInfo.length)
                for (let i = 0; i < message.BonusInfo.length; ++i)
                    $root.jlrx.BonusGameInfo.encode(message.BonusInfo[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlrx.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.SpinAck
         * @static
         * @param {jlrx.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlrx.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.BonusInfo && message.BonusInfo.length))
                            message.BonusInfo = [];
                        message.BonusInfo.push($root.jlrx.BonusGameInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlrx.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlrx.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.BonusInfo != null && message.hasOwnProperty("BonusInfo")) {
                if (!Array.isArray(message.BonusInfo))
                    return "BonusInfo: array expected";
                for (let i = 0; i < message.BonusInfo.length; ++i) {
                    let error = $root.jlrx.BonusGameInfo.verify(message.BonusInfo[i]);
                    if (error)
                        return "BonusInfo." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.SpinAck)
                return object;
            let message = new $root.jlrx.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlrx.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlrx.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlrx.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.BonusInfo) {
                if (!Array.isArray(object.BonusInfo))
                    throw TypeError(".jlrx.SpinAck.BonusInfo: array expected");
                message.BonusInfo = [];
                for (let i = 0; i < object.BonusInfo.length; ++i) {
                    if (typeof object.BonusInfo[i] !== "object")
                        throw TypeError(".jlrx.SpinAck.BonusInfo: object expected");
                    message.BonusInfo[i] = $root.jlrx.BonusGameInfo.fromObject(object.BonusInfo[i]);
                }
            }
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.SpinAck
         * @static
         * @param {jlrx.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.BonusInfo = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlrx.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.BonusInfo && message.BonusInfo.length) {
                object.BonusInfo = [];
                for (let j = 0; j < message.BonusInfo.length; ++j)
                    object.BonusInfo[j] = $root.jlrx.BonusGameInfo.toObject(message.BonusInfo[j], options);
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlrx.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlrx.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.SpinAck";
        };

        return SpinAck;
    })();

    jlrx.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlrx
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlrx
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlrx.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlrx.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlrx.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlrx.GameInfoData
         * @static
         * @param {jlrx.IGameInfoData=} [properties] Properties to set
         * @returns {jlrx.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlrx.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlrx.GameInfoData
         * @static
         * @param {jlrx.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlrx.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrx.GameInfoData
         * @static
         * @param {jlrx.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlrx.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrx.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrx.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrx.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrx.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlrx.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrx.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrx.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrx.GameInfoData)
                return object;
            let message = new $root.jlrx.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlrx.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrx.GameInfoData
         * @static
         * @param {jlrx.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlrx.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlrx.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrx.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlrx;
})();

export const jlrxd = $root.jlrxd = (() => {

    /**
     * Namespace jlrxd.
     * @exports jlrxd
     * @namespace
     */
    const jlrxd = {};

    jlrxd.MSymbol = (function() {

        /**
         * Properties of a MSymbol.
         * @memberof jlrxd
         * @interface IMSymbol
         * @property {number|null} [Symbol] MSymbol Symbol
         * @property {number|null} [Length] MSymbol Length
         */

        /**
         * Constructs a new MSymbol.
         * @memberof jlrxd
         * @classdesc Represents a MSymbol.
         * @implements IMSymbol
         * @constructor
         * @param {jlrxd.IMSymbol=} [properties] Properties to set
         */
        function MSymbol(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MSymbol Symbol.
         * @member {number} Symbol
         * @memberof jlrxd.MSymbol
         * @instance
         */
        MSymbol.prototype.Symbol = 0;

        /**
         * MSymbol Length.
         * @member {number} Length
         * @memberof jlrxd.MSymbol
         * @instance
         */
        MSymbol.prototype.Length = 0;

        /**
         * Creates a new MSymbol instance using the specified properties.
         * @function create
         * @memberof jlrxd.MSymbol
         * @static
         * @param {jlrxd.IMSymbol=} [properties] Properties to set
         * @returns {jlrxd.MSymbol} MSymbol instance
         */
        MSymbol.create = function create(properties) {
            return new MSymbol(properties);
        };

        /**
         * Encodes the specified MSymbol message. Does not implicitly {@link jlrxd.MSymbol.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.MSymbol
         * @static
         * @param {jlrxd.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Length != null && Object.hasOwnProperty.call(message, "Length"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Length);
            return writer;
        };

        /**
         * Encodes the specified MSymbol message, length delimited. Does not implicitly {@link jlrxd.MSymbol.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.MSymbol
         * @static
         * @param {jlrxd.IMSymbol} message MSymbol message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MSymbol.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.MSymbol();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Length = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MSymbol message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.MSymbol
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.MSymbol} MSymbol
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MSymbol.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MSymbol message.
         * @function verify
         * @memberof jlrxd.MSymbol
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MSymbol.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Length != null && message.hasOwnProperty("Length"))
                if (!$util.isInteger(message.Length))
                    return "Length: integer expected";
            return null;
        };

        /**
         * Creates a MSymbol message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.MSymbol
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.MSymbol} MSymbol
         */
        MSymbol.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.MSymbol)
                return object;
            let message = new $root.jlrxd.MSymbol();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Length != null)
                message.Length = object.Length | 0;
            return message;
        };

        /**
         * Creates a plain object from a MSymbol message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.MSymbol
         * @static
         * @param {jlrxd.MSymbol} message MSymbol
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MSymbol.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Length = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Length != null && message.hasOwnProperty("Length"))
                object.Length = message.Length;
            return object;
        };

        /**
         * Converts this MSymbol to JSON.
         * @function toJSON
         * @memberof jlrxd.MSymbol
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MSymbol.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MSymbol
         * @function getTypeUrl
         * @memberof jlrxd.MSymbol
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MSymbol.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.MSymbol";
        };

        return MSymbol;
    })();

    jlrxd.MColumn = (function() {

        /**
         * Properties of a MColumn.
         * @memberof jlrxd
         * @interface IMColumn
         * @property {Array.<jlrxd.IMSymbol>|null} [MColumn] MColumn MColumn
         */

        /**
         * Constructs a new MColumn.
         * @memberof jlrxd
         * @classdesc Represents a MColumn.
         * @implements IMColumn
         * @constructor
         * @param {jlrxd.IMColumn=} [properties] Properties to set
         */
        function MColumn(properties) {
            this.MColumn = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MColumn MColumn.
         * @member {Array.<jlrxd.IMSymbol>} MColumn
         * @memberof jlrxd.MColumn
         * @instance
         */
        MColumn.prototype.MColumn = $util.emptyArray;

        /**
         * Creates a new MColumn instance using the specified properties.
         * @function create
         * @memberof jlrxd.MColumn
         * @static
         * @param {jlrxd.IMColumn=} [properties] Properties to set
         * @returns {jlrxd.MColumn} MColumn instance
         */
        MColumn.create = function create(properties) {
            return new MColumn(properties);
        };

        /**
         * Encodes the specified MColumn message. Does not implicitly {@link jlrxd.MColumn.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.MColumn
         * @static
         * @param {jlrxd.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MColumn != null && message.MColumn.length)
                for (let i = 0; i < message.MColumn.length; ++i)
                    $root.jlrxd.MSymbol.encode(message.MColumn[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MColumn message, length delimited. Does not implicitly {@link jlrxd.MColumn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.MColumn
         * @static
         * @param {jlrxd.IMColumn} message MColumn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MColumn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.MColumn();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MColumn && message.MColumn.length))
                            message.MColumn = [];
                        message.MColumn.push($root.jlrxd.MSymbol.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MColumn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.MColumn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.MColumn} MColumn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MColumn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MColumn message.
         * @function verify
         * @memberof jlrxd.MColumn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MColumn.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MColumn != null && message.hasOwnProperty("MColumn")) {
                if (!Array.isArray(message.MColumn))
                    return "MColumn: array expected";
                for (let i = 0; i < message.MColumn.length; ++i) {
                    let error = $root.jlrxd.MSymbol.verify(message.MColumn[i]);
                    if (error)
                        return "MColumn." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MColumn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.MColumn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.MColumn} MColumn
         */
        MColumn.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.MColumn)
                return object;
            let message = new $root.jlrxd.MColumn();
            if (object.MColumn) {
                if (!Array.isArray(object.MColumn))
                    throw TypeError(".jlrxd.MColumn.MColumn: array expected");
                message.MColumn = [];
                for (let i = 0; i < object.MColumn.length; ++i) {
                    if (typeof object.MColumn[i] !== "object")
                        throw TypeError(".jlrxd.MColumn.MColumn: object expected");
                    message.MColumn[i] = $root.jlrxd.MSymbol.fromObject(object.MColumn[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MColumn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.MColumn
         * @static
         * @param {jlrxd.MColumn} message MColumn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MColumn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.MColumn = [];
            if (message.MColumn && message.MColumn.length) {
                object.MColumn = [];
                for (let j = 0; j < message.MColumn.length; ++j)
                    object.MColumn[j] = $root.jlrxd.MSymbol.toObject(message.MColumn[j], options);
            }
            return object;
        };

        /**
         * Converts this MColumn to JSON.
         * @function toJSON
         * @memberof jlrxd.MColumn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MColumn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MColumn
         * @function getTypeUrl
         * @memberof jlrxd.MColumn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MColumn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.MColumn";
        };

        return MColumn;
    })();

    jlrxd.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlrxd
         * @interface IColumn
         * @property {Array.<number>|null} [Column] Column Column
         */

        /**
         * Constructs a new Column.
         * @memberof jlrxd
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlrxd.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Column = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Column.
         * @member {Array.<number>} Column
         * @memberof jlrxd.Column
         * @instance
         */
        Column.prototype.Column = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlrxd.Column
         * @static
         * @param {jlrxd.IColumn=} [properties] Properties to set
         * @returns {jlrxd.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlrxd.Column.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.Column
         * @static
         * @param {jlrxd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && message.Column.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Column.length; ++i)
                    writer.int32(message.Column[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlrxd.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.Column
         * @static
         * @param {jlrxd.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Column && message.Column.length))
                            message.Column = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Column.push(reader.int32());
                        } else
                            message.Column.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlrxd.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column")) {
                if (!Array.isArray(message.Column))
                    return "Column: array expected";
                for (let i = 0; i < message.Column.length; ++i)
                    if (!$util.isInteger(message.Column[i]))
                        return "Column: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.Column)
                return object;
            let message = new $root.jlrxd.Column();
            if (object.Column) {
                if (!Array.isArray(object.Column))
                    throw TypeError(".jlrxd.Column.Column: array expected");
                message.Column = [];
                for (let i = 0; i < object.Column.length; ++i)
                    message.Column[i] = object.Column[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.Column
         * @static
         * @param {jlrxd.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Column = [];
            if (message.Column && message.Column.length) {
                object.Column = [];
                for (let j = 0; j < message.Column.length; ++j)
                    object.Column[j] = message.Column[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlrxd.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlrxd.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.Column";
        };

        return Column;
    })();

    jlrxd.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlrxd
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlrxd
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlrxd.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlrxd.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlrxd.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlrxd.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlrxd.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlrxd.AwardData
         * @static
         * @param {jlrxd.IAwardData=} [properties] Properties to set
         * @returns {jlrxd.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlrxd.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.AwardData
         * @static
         * @param {jlrxd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlrxd.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.AwardData
         * @static
         * @param {jlrxd.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlrxd.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.AwardData)
                return object;
            let message = new $root.jlrxd.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.AwardData
         * @static
         * @param {jlrxd.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlrxd.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlrxd.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.AwardData";
        };

        return AwardData;
    })();

    jlrxd.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlrxd
         * @interface IComboStageInfo
         * @property {Array.<jlrxd.IMColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlrxd.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlrxd.IMColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {boolean|null} [BonusHit] ComboStageInfo BonusHit
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlrxd
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlrxd.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlrxd.IMColumn>} ComboStageSymbol
         * @memberof jlrxd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlrxd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlrxd.IAwardData>} AwardDataVec
         * @memberof jlrxd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlrxd.IMColumn>} FillSymbols
         * @memberof jlrxd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo BonusHit.
         * @member {boolean} BonusHit
         * @memberof jlrxd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.BonusHit = false;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlrxd.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {jlrxd.IComboStageInfo=} [properties] Properties to set
         * @returns {jlrxd.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlrxd.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {jlrxd.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlrxd.MColumn.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlrxd.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlrxd.MColumn.encode(message.FillSymbols[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.BonusHit != null && Object.hasOwnProperty.call(message, "BonusHit"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.BonusHit);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlrxd.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {jlrxd.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlrxd.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlrxd.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlrxd.MColumn.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.BonusHit = reader.bool();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlrxd.MColumn.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlrxd.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlrxd.MColumn.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.BonusHit != null && message.hasOwnProperty("BonusHit"))
                if (typeof message.BonusHit !== "boolean")
                    return "BonusHit: boolean expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.ComboStageInfo)
                return object;
            let message = new $root.jlrxd.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlrxd.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlrxd.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlrxd.MColumn.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlrxd.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlrxd.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlrxd.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlrxd.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlrxd.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlrxd.MColumn.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.BonusHit != null)
                message.BonusHit = Boolean(object.BonusHit);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {jlrxd.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.BonusHit = false;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlrxd.MColumn.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlrxd.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlrxd.MColumn.toObject(message.FillSymbols[j], options);
            }
            if (message.BonusHit != null && message.hasOwnProperty("BonusHit"))
                object.BonusHit = message.BonusHit;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlrxd.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlrxd.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlrxd.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlrxd
         * @interface IRoundInfo
         * @property {Array.<jlrxd.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlrxd
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlrxd.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlrxd.IComboStageInfo>} ComboStageData
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlrxd.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {jlrxd.IRoundInfo=} [properties] Properties to set
         * @returns {jlrxd.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlrxd.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {jlrxd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlrxd.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AddRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlrxd.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {jlrxd.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlrxd.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlrxd.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.RoundInfo)
                return object;
            let message = new $root.jlrxd.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlrxd.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlrxd.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlrxd.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {jlrxd.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.AddRound = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlrxd.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlrxd.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlrxd.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.RoundInfo";
        };

        return RoundInfo;
    })();

    jlrxd.BonusGameInfo = (function() {

        /**
         * Properties of a BonusGameInfo.
         * @memberof jlrxd
         * @interface IBonusGameInfo
         * @property {number|null} [Symbol] BonusGameInfo Symbol
         * @property {number|null} [FightResult] BonusGameInfo FightResult
         * @property {number|null} [LionLevel] BonusGameInfo LionLevel
         * @property {number|null} [Mult] BonusGameInfo Mult
         * @property {number|null} [Win] BonusGameInfo Win
         * @property {number|null} [HealthPoint] BonusGameInfo HealthPoint
         * @property {number|null} [NowRound] BonusGameInfo NowRound
         * @property {number|null} [CrazyRemainRound] BonusGameInfo CrazyRemainRound
         * @property {number|null} [CrazyType] BonusGameInfo CrazyType
         * @property {boolean|null} [MaxFlag] BonusGameInfo MaxFlag
         */

        /**
         * Constructs a new BonusGameInfo.
         * @memberof jlrxd
         * @classdesc Represents a BonusGameInfo.
         * @implements IBonusGameInfo
         * @constructor
         * @param {jlrxd.IBonusGameInfo=} [properties] Properties to set
         */
        function BonusGameInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusGameInfo Symbol.
         * @member {number} Symbol
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Symbol = 0;

        /**
         * BonusGameInfo FightResult.
         * @member {number} FightResult
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.FightResult = 0;

        /**
         * BonusGameInfo LionLevel.
         * @member {number} LionLevel
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.LionLevel = 0;

        /**
         * BonusGameInfo Mult.
         * @member {number} Mult
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Mult = 0;

        /**
         * BonusGameInfo Win.
         * @member {number} Win
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Win = 0;

        /**
         * BonusGameInfo HealthPoint.
         * @member {number} HealthPoint
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.HealthPoint = 0;

        /**
         * BonusGameInfo NowRound.
         * @member {number} NowRound
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.NowRound = 0;

        /**
         * BonusGameInfo CrazyRemainRound.
         * @member {number} CrazyRemainRound
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.CrazyRemainRound = 0;

        /**
         * BonusGameInfo CrazyType.
         * @member {number} CrazyType
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.CrazyType = 0;

        /**
         * BonusGameInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlrxd.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.MaxFlag = false;

        /**
         * Creates a new BonusGameInfo instance using the specified properties.
         * @function create
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {jlrxd.IBonusGameInfo=} [properties] Properties to set
         * @returns {jlrxd.BonusGameInfo} BonusGameInfo instance
         */
        BonusGameInfo.create = function create(properties) {
            return new BonusGameInfo(properties);
        };

        /**
         * Encodes the specified BonusGameInfo message. Does not implicitly {@link jlrxd.BonusGameInfo.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {jlrxd.IBonusGameInfo} message BonusGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.FightResult != null && Object.hasOwnProperty.call(message, "FightResult"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.FightResult);
            if (message.LionLevel != null && Object.hasOwnProperty.call(message, "LionLevel"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LionLevel);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Win);
            if (message.HealthPoint != null && Object.hasOwnProperty.call(message, "HealthPoint"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.HealthPoint);
            if (message.NowRound != null && Object.hasOwnProperty.call(message, "NowRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.NowRound);
            if (message.CrazyRemainRound != null && Object.hasOwnProperty.call(message, "CrazyRemainRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.CrazyRemainRound);
            if (message.CrazyType != null && Object.hasOwnProperty.call(message, "CrazyType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.CrazyType);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified BonusGameInfo message, length delimited. Does not implicitly {@link jlrxd.BonusGameInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {jlrxd.IBonusGameInfo} message BonusGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusGameInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.BonusGameInfo} BonusGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.BonusGameInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.FightResult = reader.int32();
                        break;
                    }
                case 3: {
                        message.LionLevel = reader.int32();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        message.Win = reader.double();
                        break;
                    }
                case 6: {
                        message.HealthPoint = reader.int32();
                        break;
                    }
                case 7: {
                        message.NowRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.CrazyRemainRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.CrazyType = reader.int32();
                        break;
                    }
                case 10: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusGameInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.BonusGameInfo} BonusGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusGameInfo message.
         * @function verify
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusGameInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.FightResult != null && message.hasOwnProperty("FightResult"))
                if (!$util.isInteger(message.FightResult))
                    return "FightResult: integer expected";
            if (message.LionLevel != null && message.hasOwnProperty("LionLevel"))
                if (!$util.isInteger(message.LionLevel))
                    return "LionLevel: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.HealthPoint != null && message.hasOwnProperty("HealthPoint"))
                if (!$util.isInteger(message.HealthPoint))
                    return "HealthPoint: integer expected";
            if (message.NowRound != null && message.hasOwnProperty("NowRound"))
                if (!$util.isInteger(message.NowRound))
                    return "NowRound: integer expected";
            if (message.CrazyRemainRound != null && message.hasOwnProperty("CrazyRemainRound"))
                if (!$util.isInteger(message.CrazyRemainRound))
                    return "CrazyRemainRound: integer expected";
            if (message.CrazyType != null && message.hasOwnProperty("CrazyType"))
                if (!$util.isInteger(message.CrazyType))
                    return "CrazyType: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a BonusGameInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.BonusGameInfo} BonusGameInfo
         */
        BonusGameInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.BonusGameInfo)
                return object;
            let message = new $root.jlrxd.BonusGameInfo();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.FightResult != null)
                message.FightResult = object.FightResult | 0;
            if (object.LionLevel != null)
                message.LionLevel = object.LionLevel | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.HealthPoint != null)
                message.HealthPoint = object.HealthPoint | 0;
            if (object.NowRound != null)
                message.NowRound = object.NowRound | 0;
            if (object.CrazyRemainRound != null)
                message.CrazyRemainRound = object.CrazyRemainRound | 0;
            if (object.CrazyType != null)
                message.CrazyType = object.CrazyType | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a BonusGameInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {jlrxd.BonusGameInfo} message BonusGameInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusGameInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.FightResult = 0;
                object.LionLevel = 0;
                object.Mult = 0;
                object.Win = 0;
                object.HealthPoint = 0;
                object.NowRound = 0;
                object.CrazyRemainRound = 0;
                object.CrazyType = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.FightResult != null && message.hasOwnProperty("FightResult"))
                object.FightResult = message.FightResult;
            if (message.LionLevel != null && message.hasOwnProperty("LionLevel"))
                object.LionLevel = message.LionLevel;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.HealthPoint != null && message.hasOwnProperty("HealthPoint"))
                object.HealthPoint = message.HealthPoint;
            if (message.NowRound != null && message.hasOwnProperty("NowRound"))
                object.NowRound = message.NowRound;
            if (message.CrazyRemainRound != null && message.hasOwnProperty("CrazyRemainRound"))
                object.CrazyRemainRound = message.CrazyRemainRound;
            if (message.CrazyType != null && message.hasOwnProperty("CrazyType"))
                object.CrazyType = message.CrazyType;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this BonusGameInfo to JSON.
         * @function toJSON
         * @memberof jlrxd.BonusGameInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusGameInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusGameInfo
         * @function getTypeUrl
         * @memberof jlrxd.BonusGameInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.BonusGameInfo";
        };

        return BonusGameInfo;
    })();

    jlrxd.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlrxd
         * @interface ISpinAck
         * @property {Array.<jlrxd.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {Array.<jlrxd.IBonusGameInfo>|null} [BonusInfo] SpinAck BonusInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [AckType] SpinAck AckType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlrxd
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlrxd.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.BonusInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlrxd.IRoundInfo>} RoundQueue
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = 0;

        /**
         * SpinAck BonusInfo.
         * @member {Array.<jlrxd.IBonusGameInfo>} BonusInfo
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusInfo = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlrxd.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlrxd.SpinAck
         * @static
         * @param {jlrxd.ISpinAck=} [properties] Properties to set
         * @returns {jlrxd.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlrxd.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.SpinAck
         * @static
         * @param {jlrxd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlrxd.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.BonusTotalWin);
            if (message.BonusInfo != null && message.BonusInfo.length)
                for (let i = 0; i < message.BonusInfo.length; ++i)
                    $root.jlrxd.BonusGameInfo.encode(message.BonusInfo[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RTP);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlrxd.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.SpinAck
         * @static
         * @param {jlrxd.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlrxd.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.BonusInfo && message.BonusInfo.length))
                            message.BonusInfo = [];
                        message.BonusInfo.push($root.jlrxd.BonusGameInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 6: {
                        message.RTP = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlrxd.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlrxd.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.BonusInfo != null && message.hasOwnProperty("BonusInfo")) {
                if (!Array.isArray(message.BonusInfo))
                    return "BonusInfo: array expected";
                for (let i = 0; i < message.BonusInfo.length; ++i) {
                    let error = $root.jlrxd.BonusGameInfo.verify(message.BonusInfo[i]);
                    if (error)
                        return "BonusInfo." + error;
                }
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.SpinAck)
                return object;
            let message = new $root.jlrxd.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlrxd.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlrxd.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlrxd.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.BonusInfo) {
                if (!Array.isArray(object.BonusInfo))
                    throw TypeError(".jlrxd.SpinAck.BonusInfo: array expected");
                message.BonusInfo = [];
                for (let i = 0; i < object.BonusInfo.length; ++i) {
                    if (typeof object.BonusInfo[i] !== "object")
                        throw TypeError(".jlrxd.SpinAck.BonusInfo: object expected");
                    message.BonusInfo[i] = $root.jlrxd.BonusGameInfo.fromObject(object.BonusInfo[i]);
                }
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.SpinAck
         * @static
         * @param {jlrxd.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.BonusInfo = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.RTP = 0;
                object.AckType = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlrxd.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.BonusInfo && message.BonusInfo.length) {
                object.BonusInfo = [];
                for (let j = 0; j < message.BonusInfo.length; ++j)
                    object.BonusInfo[j] = $root.jlrxd.BonusGameInfo.toObject(message.BonusInfo[j], options);
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlrxd.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlrxd.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.SpinAck";
        };

        return SpinAck;
    })();

    jlrxd.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlrxd
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<jlrxd.IColumn>|null} [BonusSetting] GameInfoData BonusSetting
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlrxd
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlrxd.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.BonusSetting = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlrxd.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlrxd.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData BonusSetting.
         * @member {Array.<jlrxd.IColumn>} BonusSetting
         * @memberof jlrxd.GameInfoData
         * @instance
         */
        GameInfoData.prototype.BonusSetting = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {jlrxd.IGameInfoData=} [properties] Properties to set
         * @returns {jlrxd.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlrxd.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {jlrxd.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.BonusSetting != null && message.BonusSetting.length)
                for (let i = 0; i < message.BonusSetting.length; ++i)
                    $root.jlrxd.Column.encode(message.BonusSetting[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlrxd.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {jlrxd.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlrxd.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlrxd.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.BonusSetting && message.BonusSetting.length))
                            message.BonusSetting = [];
                        message.BonusSetting.push($root.jlrxd.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlrxd.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.BonusSetting != null && message.hasOwnProperty("BonusSetting")) {
                if (!Array.isArray(message.BonusSetting))
                    return "BonusSetting: array expected";
                for (let i = 0; i < message.BonusSetting.length; ++i) {
                    let error = $root.jlrxd.Column.verify(message.BonusSetting[i]);
                    if (error)
                        return "BonusSetting." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlrxd.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlrxd.GameInfoData)
                return object;
            let message = new $root.jlrxd.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlrxd.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.BonusSetting) {
                if (!Array.isArray(object.BonusSetting))
                    throw TypeError(".jlrxd.GameInfoData.BonusSetting: array expected");
                message.BonusSetting = [];
                for (let i = 0; i < object.BonusSetting.length; ++i) {
                    if (typeof object.BonusSetting[i] !== "object")
                        throw TypeError(".jlrxd.GameInfoData.BonusSetting: object expected");
                    message.BonusSetting[i] = $root.jlrxd.Column.fromObject(object.BonusSetting[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {jlrxd.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.BonusSetting = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.BonusSetting && message.BonusSetting.length) {
                object.BonusSetting = [];
                for (let j = 0; j < message.BonusSetting.length; ++j)
                    object.BonusSetting[j] = $root.jlrxd.Column.toObject(message.BonusSetting[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlrxd.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlrxd.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlrxd.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlrxd;
})();

export const jlsa = $root.jlsa = (() => {

    /**
     * Namespace jlsa.
     * @exports jlsa
     * @namespace
     */
    const jlsa = {};

    /**
     * Symbol enum.
     * @name jlsa.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} SymbolMax=10 SymbolMax value
     */
    jlsa.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "SymbolMax"] = 10;
        return values;
    })();

    jlsa.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlsa
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlsa
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlsa.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlsa.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlsa.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlsa.Block
         * @static
         * @param {jlsa.IBlock=} [properties] Properties to set
         * @returns {jlsa.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlsa.Block.verify|verify} messages.
         * @function encode
         * @memberof jlsa.Block
         * @static
         * @param {jlsa.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlsa.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.Block
         * @static
         * @param {jlsa.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlsa.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.Block)
                return object;
            let message = new $root.jlsa.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.Block
         * @static
         * @param {jlsa.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlsa.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlsa.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.Block";
        };

        return Block;
    })();

    jlsa.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlsa
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [row] Cell row
         * @property {number|null} [column] Cell column
         * @property {number|null} [isGold] Cell isGold
         * @property {number|null} [lastGold] Cell lastGold
         */

        /**
         * Constructs a new Cell.
         * @memberof jlsa
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlsa.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlsa.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell row.
         * @member {number} row
         * @memberof jlsa.Cell
         * @instance
         */
        Cell.prototype.row = 0;

        /**
         * Cell column.
         * @member {number} column
         * @memberof jlsa.Cell
         * @instance
         */
        Cell.prototype.column = 0;

        /**
         * Cell isGold.
         * @member {number} isGold
         * @memberof jlsa.Cell
         * @instance
         */
        Cell.prototype.isGold = 0;

        /**
         * Cell lastGold.
         * @member {number} lastGold
         * @memberof jlsa.Cell
         * @instance
         */
        Cell.prototype.lastGold = 0;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlsa.Cell
         * @static
         * @param {jlsa.ICell=} [properties] Properties to set
         * @returns {jlsa.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlsa.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlsa.Cell
         * @static
         * @param {jlsa.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.column);
            if (message.isGold != null && Object.hasOwnProperty.call(message, "isGold"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.isGold);
            if (message.lastGold != null && Object.hasOwnProperty.call(message, "lastGold"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.lastGold);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlsa.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.Cell
         * @static
         * @param {jlsa.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.row = reader.uint32();
                        break;
                    }
                case 3: {
                        message.column = reader.uint32();
                        break;
                    }
                case 4: {
                        message.isGold = reader.uint32();
                        break;
                    }
                case 5: {
                        message.lastGold = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlsa.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                if (!$util.isInteger(message.isGold))
                    return "isGold: integer expected";
            if (message.lastGold != null && message.hasOwnProperty("lastGold"))
                if (!$util.isInteger(message.lastGold))
                    return "lastGold: integer expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.Cell)
                return object;
            let message = new $root.jlsa.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            if (object.isGold != null)
                message.isGold = object.isGold >>> 0;
            if (object.lastGold != null)
                message.lastGold = object.lastGold >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.Cell
         * @static
         * @param {jlsa.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.row = 0;
                object.column = 0;
                object.isGold = 0;
                object.lastGold = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                object.isGold = message.isGold;
            if (message.lastGold != null && message.hasOwnProperty("lastGold"))
                object.lastGold = message.lastGold;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlsa.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlsa.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.Cell";
        };

        return Cell;
    })();

    jlsa.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlsa
         * @interface IAwardReel
         * @property {number|null} [symbol] AwardReel symbol
         * @property {Array.<jlsa.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         * @property {number|null} [bonus] AwardReel bonus
         * @property {number|null} [maxLen] AwardReel maxLen
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlsa
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlsa.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel symbol.
         * @member {number} symbol
         * @memberof jlsa.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlsa.IBlock>} block
         * @memberof jlsa.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlsa.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * AwardReel bonus.
         * @member {number} bonus
         * @memberof jlsa.AwardReel
         * @instance
         */
        AwardReel.prototype.bonus = 0;

        /**
         * AwardReel maxLen.
         * @member {number} maxLen
         * @memberof jlsa.AwardReel
         * @instance
         */
        AwardReel.prototype.maxLen = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlsa.AwardReel
         * @static
         * @param {jlsa.IAwardReel=} [properties] Properties to set
         * @returns {jlsa.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlsa.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlsa.AwardReel
         * @static
         * @param {jlsa.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlsa.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.bonus != null && Object.hasOwnProperty.call(message, "bonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.bonus);
            if (message.maxLen != null && Object.hasOwnProperty.call(message, "maxLen"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.maxLen);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlsa.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.AwardReel
         * @static
         * @param {jlsa.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlsa.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.bonus = reader.uint32();
                        break;
                    }
                case 5: {
                        message.maxLen = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlsa.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlsa.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                if (!$util.isInteger(message.bonus))
                    return "bonus: integer expected";
            if (message.maxLen != null && message.hasOwnProperty("maxLen"))
                if (!$util.isInteger(message.maxLen))
                    return "maxLen: integer expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.AwardReel)
                return object;
            let message = new $root.jlsa.AwardReel();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlsa.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlsa.AwardReel.block: object expected");
                    message.block[i] = $root.jlsa.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.bonus != null)
                message.bonus = object.bonus >>> 0;
            if (object.maxLen != null)
                message.maxLen = object.maxLen >>> 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.AwardReel
         * @static
         * @param {jlsa.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.symbol = 0;
                object.win = 0;
                object.bonus = 0;
                object.maxLen = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlsa.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                object.bonus = message.bonus;
            if (message.maxLen != null && message.hasOwnProperty("maxLen"))
                object.maxLen = message.maxLen;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlsa.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlsa.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.AwardReel";
        };

        return AwardReel;
    })();

    jlsa.Combo = (function() {

        /**
         * Properties of a Combo.
         * @memberof jlsa
         * @interface ICombo
         * @property {Array.<jlsa.ICell>|null} [change] Combo change
         * @property {Array.<jlsa.IAwardReel>|null} [award] Combo award
         * @property {number|null} [win] Combo win
         * @property {number|null} [comboBonus] Combo comboBonus
         * @property {Array.<jlsa.ICell>|null} [extraChange] Combo extraChange
         */

        /**
         * Constructs a new Combo.
         * @memberof jlsa
         * @classdesc Represents a Combo.
         * @implements ICombo
         * @constructor
         * @param {jlsa.ICombo=} [properties] Properties to set
         */
        function Combo(properties) {
            this.change = [];
            this.award = [];
            this.extraChange = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Combo change.
         * @member {Array.<jlsa.ICell>} change
         * @memberof jlsa.Combo
         * @instance
         */
        Combo.prototype.change = $util.emptyArray;

        /**
         * Combo award.
         * @member {Array.<jlsa.IAwardReel>} award
         * @memberof jlsa.Combo
         * @instance
         */
        Combo.prototype.award = $util.emptyArray;

        /**
         * Combo win.
         * @member {number} win
         * @memberof jlsa.Combo
         * @instance
         */
        Combo.prototype.win = 0;

        /**
         * Combo comboBonus.
         * @member {number} comboBonus
         * @memberof jlsa.Combo
         * @instance
         */
        Combo.prototype.comboBonus = 0;

        /**
         * Combo extraChange.
         * @member {Array.<jlsa.ICell>} extraChange
         * @memberof jlsa.Combo
         * @instance
         */
        Combo.prototype.extraChange = $util.emptyArray;

        /**
         * Creates a new Combo instance using the specified properties.
         * @function create
         * @memberof jlsa.Combo
         * @static
         * @param {jlsa.ICombo=} [properties] Properties to set
         * @returns {jlsa.Combo} Combo instance
         */
        Combo.create = function create(properties) {
            return new Combo(properties);
        };

        /**
         * Encodes the specified Combo message. Does not implicitly {@link jlsa.Combo.verify|verify} messages.
         * @function encode
         * @memberof jlsa.Combo
         * @static
         * @param {jlsa.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.change != null && message.change.length)
                for (let i = 0; i < message.change.length; ++i)
                    $root.jlsa.Cell.encode(message.change[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlsa.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.comboBonus != null && Object.hasOwnProperty.call(message, "comboBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.comboBonus);
            if (message.extraChange != null && message.extraChange.length)
                for (let i = 0; i < message.extraChange.length; ++i)
                    $root.jlsa.Cell.encode(message.extraChange[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Combo message, length delimited. Does not implicitly {@link jlsa.Combo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.Combo
         * @static
         * @param {jlsa.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Combo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.Combo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.change && message.change.length))
                            message.change = [];
                        message.change.push($root.jlsa.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlsa.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.comboBonus = reader.uint32();
                        break;
                    }
                case 5: {
                        if (!(message.extraChange && message.extraChange.length))
                            message.extraChange = [];
                        message.extraChange.push($root.jlsa.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Combo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Combo message.
         * @function verify
         * @memberof jlsa.Combo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Combo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.change != null && message.hasOwnProperty("change")) {
                if (!Array.isArray(message.change))
                    return "change: array expected";
                for (let i = 0; i < message.change.length; ++i) {
                    let error = $root.jlsa.Cell.verify(message.change[i]);
                    if (error)
                        return "change." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlsa.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.comboBonus != null && message.hasOwnProperty("comboBonus"))
                if (!$util.isInteger(message.comboBonus))
                    return "comboBonus: integer expected";
            if (message.extraChange != null && message.hasOwnProperty("extraChange")) {
                if (!Array.isArray(message.extraChange))
                    return "extraChange: array expected";
                for (let i = 0; i < message.extraChange.length; ++i) {
                    let error = $root.jlsa.Cell.verify(message.extraChange[i]);
                    if (error)
                        return "extraChange." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Combo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.Combo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.Combo} Combo
         */
        Combo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.Combo)
                return object;
            let message = new $root.jlsa.Combo();
            if (object.change) {
                if (!Array.isArray(object.change))
                    throw TypeError(".jlsa.Combo.change: array expected");
                message.change = [];
                for (let i = 0; i < object.change.length; ++i) {
                    if (typeof object.change[i] !== "object")
                        throw TypeError(".jlsa.Combo.change: object expected");
                    message.change[i] = $root.jlsa.Cell.fromObject(object.change[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlsa.Combo.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlsa.Combo.award: object expected");
                    message.award[i] = $root.jlsa.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.comboBonus != null)
                message.comboBonus = object.comboBonus >>> 0;
            if (object.extraChange) {
                if (!Array.isArray(object.extraChange))
                    throw TypeError(".jlsa.Combo.extraChange: array expected");
                message.extraChange = [];
                for (let i = 0; i < object.extraChange.length; ++i) {
                    if (typeof object.extraChange[i] !== "object")
                        throw TypeError(".jlsa.Combo.extraChange: object expected");
                    message.extraChange[i] = $root.jlsa.Cell.fromObject(object.extraChange[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Combo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.Combo
         * @static
         * @param {jlsa.Combo} message Combo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Combo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change = [];
                object.award = [];
                object.extraChange = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.comboBonus = 0;
            }
            if (message.change && message.change.length) {
                object.change = [];
                for (let j = 0; j < message.change.length; ++j)
                    object.change[j] = $root.jlsa.Cell.toObject(message.change[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlsa.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.comboBonus != null && message.hasOwnProperty("comboBonus"))
                object.comboBonus = message.comboBonus;
            if (message.extraChange && message.extraChange.length) {
                object.extraChange = [];
                for (let j = 0; j < message.extraChange.length; ++j)
                    object.extraChange[j] = $root.jlsa.Cell.toObject(message.extraChange[j], options);
            }
            return object;
        };

        /**
         * Converts this Combo to JSON.
         * @function toJSON
         * @memberof jlsa.Combo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Combo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Combo
         * @function getTypeUrl
         * @memberof jlsa.Combo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Combo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.Combo";
        };

        return Combo;
    })();

    jlsa.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlsa
         * @interface IPlate
         * @property {Array.<jlsa.IColumnData>|null} [column] Plate column
         * @property {Array.<jlsa.ICombo>|null} [combo] Plate combo
         * @property {number|null} [win] Plate win
         * @property {number|null} [bonusWin] Plate bonusWin
         * @property {boolean|null} [isBonus] Plate isBonus
         * @property {number|null} [totalFreeSpin] Plate totalFreeSpin
         */

        /**
         * Constructs a new Plate.
         * @memberof jlsa
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlsa.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.combo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlsa.IColumnData>} column
         * @memberof jlsa.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate combo.
         * @member {Array.<jlsa.ICombo>} combo
         * @memberof jlsa.Plate
         * @instance
         */
        Plate.prototype.combo = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlsa.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate bonusWin.
         * @member {number} bonusWin
         * @memberof jlsa.Plate
         * @instance
         */
        Plate.prototype.bonusWin = 0;

        /**
         * Plate isBonus.
         * @member {boolean} isBonus
         * @memberof jlsa.Plate
         * @instance
         */
        Plate.prototype.isBonus = false;

        /**
         * Plate totalFreeSpin.
         * @member {number} totalFreeSpin
         * @memberof jlsa.Plate
         * @instance
         */
        Plate.prototype.totalFreeSpin = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlsa.Plate
         * @static
         * @param {jlsa.IPlate=} [properties] Properties to set
         * @returns {jlsa.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlsa.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlsa.Plate
         * @static
         * @param {jlsa.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlsa.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.combo != null && message.combo.length)
                for (let i = 0; i < message.combo.length; ++i)
                    $root.jlsa.Combo.encode(message.combo[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.win);
            if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
            if (message.isBonus != null && Object.hasOwnProperty.call(message, "isBonus"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isBonus);
            if (message.totalFreeSpin != null && Object.hasOwnProperty.call(message, "totalFreeSpin"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.totalFreeSpin);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlsa.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.Plate
         * @static
         * @param {jlsa.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlsa.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.combo && message.combo.length))
                            message.combo = [];
                        message.combo.push($root.jlsa.Combo.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.win = reader.double();
                        break;
                    }
                case 5: {
                        message.bonusWin = reader.double();
                        break;
                    }
                case 6: {
                        message.isBonus = reader.bool();
                        break;
                    }
                case 7: {
                        message.totalFreeSpin = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlsa.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlsa.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.combo != null && message.hasOwnProperty("combo")) {
                if (!Array.isArray(message.combo))
                    return "combo: array expected";
                for (let i = 0; i < message.combo.length; ++i) {
                    let error = $root.jlsa.Combo.verify(message.combo[i]);
                    if (error)
                        return "combo." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                if (typeof message.bonusWin !== "number")
                    return "bonusWin: number expected";
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                if (typeof message.isBonus !== "boolean")
                    return "isBonus: boolean expected";
            if (message.totalFreeSpin != null && message.hasOwnProperty("totalFreeSpin"))
                if (!$util.isInteger(message.totalFreeSpin))
                    return "totalFreeSpin: integer expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.Plate)
                return object;
            let message = new $root.jlsa.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlsa.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlsa.Plate.column: object expected");
                    message.column[i] = $root.jlsa.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.combo) {
                if (!Array.isArray(object.combo))
                    throw TypeError(".jlsa.Plate.combo: array expected");
                message.combo = [];
                for (let i = 0; i < object.combo.length; ++i) {
                    if (typeof object.combo[i] !== "object")
                        throw TypeError(".jlsa.Plate.combo: object expected");
                    message.combo[i] = $root.jlsa.Combo.fromObject(object.combo[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.bonusWin != null)
                message.bonusWin = Number(object.bonusWin);
            if (object.isBonus != null)
                message.isBonus = Boolean(object.isBonus);
            if (object.totalFreeSpin != null)
                message.totalFreeSpin = object.totalFreeSpin >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.Plate
         * @static
         * @param {jlsa.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.combo = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.bonusWin = 0;
                object.isBonus = false;
                object.totalFreeSpin = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlsa.ColumnData.toObject(message.column[j], options);
            }
            if (message.combo && message.combo.length) {
                object.combo = [];
                for (let j = 0; j < message.combo.length; ++j)
                    object.combo[j] = $root.jlsa.Combo.toObject(message.combo[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
            if (message.isBonus != null && message.hasOwnProperty("isBonus"))
                object.isBonus = message.isBonus;
            if (message.totalFreeSpin != null && message.hasOwnProperty("totalFreeSpin"))
                object.totalFreeSpin = message.totalFreeSpin;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlsa.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlsa.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.Plate";
        };

        return Plate;
    })();

    jlsa.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlsa
         * @interface IAllPlate
         * @property {Array.<jlsa.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlsa
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlsa.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlsa.IPlate>} plate
         * @memberof jlsa.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlsa.AllPlate
         * @static
         * @param {jlsa.IAllPlate=} [properties] Properties to set
         * @returns {jlsa.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlsa.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlsa.AllPlate
         * @static
         * @param {jlsa.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlsa.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlsa.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.AllPlate
         * @static
         * @param {jlsa.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlsa.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlsa.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlsa.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.AllPlate)
                return object;
            let message = new $root.jlsa.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlsa.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlsa.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlsa.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.AllPlate
         * @static
         * @param {jlsa.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlsa.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlsa.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlsa.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.AllPlate";
        };

        return AllPlate;
    })();

    jlsa.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlsa
         * @interface IColumnData
         * @property {Array.<number>|null} [row] ColumnData row
         * @property {Array.<number>|null} [isGold] ColumnData isGold
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlsa
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlsa.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            this.isGold = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<number>} row
         * @memberof jlsa.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * ColumnData isGold.
         * @member {Array.<number>} isGold
         * @memberof jlsa.ColumnData
         * @instance
         */
        ColumnData.prototype.isGold = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlsa.ColumnData
         * @static
         * @param {jlsa.IColumnData=} [properties] Properties to set
         * @returns {jlsa.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlsa.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlsa.ColumnData
         * @static
         * @param {jlsa.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            if (message.isGold != null && message.isGold.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.isGold.length; ++i)
                    writer.uint32(message.isGold[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlsa.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa.ColumnData
         * @static
         * @param {jlsa.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.isGold && message.isGold.length))
                            message.isGold = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.isGold.push(reader.uint32());
                        } else
                            message.isGold.push(reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlsa.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            if (message.isGold != null && message.hasOwnProperty("isGold")) {
                if (!Array.isArray(message.isGold))
                    return "isGold: array expected";
                for (let i = 0; i < message.isGold.length; ++i)
                    if (!$util.isInteger(message.isGold[i]))
                        return "isGold: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa.ColumnData)
                return object;
            let message = new $root.jlsa.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlsa.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            if (object.isGold) {
                if (!Array.isArray(object.isGold))
                    throw TypeError(".jlsa.ColumnData.isGold: array expected");
                message.isGold = [];
                for (let i = 0; i < object.isGold.length; ++i)
                    message.isGold[i] = object.isGold[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa.ColumnData
         * @static
         * @param {jlsa.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.row = [];
                object.isGold = [];
            }
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            if (message.isGold && message.isGold.length) {
                object.isGold = [];
                for (let j = 0; j < message.isGold.length; ++j)
                    object.isGold[j] = message.isGold[j];
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlsa.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlsa.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa.ColumnData";
        };

        return ColumnData;
    })();

    return jlsa;
})();

export const jlsa2 = $root.jlsa2 = (() => {

    /**
     * Namespace jlsa2.
     * @exports jlsa2
     * @namespace
     */
    const jlsa2 = {};

    /**
     * Symbol enum.
     * @name jlsa2.Symbol
     * @enum {number}
     * @property {number} SmallGhost=0 SmallGhost value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} BigGhost=10 BigGhost value
     */
    jlsa2.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SmallGhost"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "BigGhost"] = 10;
        return values;
    })();

    jlsa2.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlsa2
         * @interface IAwardReel
         * @property {jlsa2.Symbol|null} [symbol] AwardReel symbol
         * @property {number|null} [totalCount] AwardReel totalCount
         * @property {number|null} [maxColumn] AwardReel maxColumn
         * @property {number|null} [win] AwardReel win
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlsa2
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlsa2.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel symbol.
         * @member {jlsa2.Symbol} symbol
         * @memberof jlsa2.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * AwardReel totalCount.
         * @member {number} totalCount
         * @memberof jlsa2.AwardReel
         * @instance
         */
        AwardReel.prototype.totalCount = 0;

        /**
         * AwardReel maxColumn.
         * @member {number} maxColumn
         * @memberof jlsa2.AwardReel
         * @instance
         */
        AwardReel.prototype.maxColumn = 0;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlsa2.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlsa2.AwardReel
         * @static
         * @param {jlsa2.IAwardReel=} [properties] Properties to set
         * @returns {jlsa2.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlsa2.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlsa2.AwardReel
         * @static
         * @param {jlsa2.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.totalCount != null && Object.hasOwnProperty.call(message, "totalCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.totalCount);
            if (message.maxColumn != null && Object.hasOwnProperty.call(message, "maxColumn"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.maxColumn);
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlsa2.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa2.AwardReel
         * @static
         * @param {jlsa2.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa2.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa2.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa2.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.totalCount = reader.uint32();
                        break;
                    }
                case 3: {
                        message.maxColumn = reader.uint32();
                        break;
                    }
                case 4: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa2.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa2.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlsa2.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                switch (message.symbol) {
                default:
                    return "symbol: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                    break;
                }
            if (message.totalCount != null && message.hasOwnProperty("totalCount"))
                if (!$util.isInteger(message.totalCount))
                    return "totalCount: integer expected";
            if (message.maxColumn != null && message.hasOwnProperty("maxColumn"))
                if (!$util.isInteger(message.maxColumn))
                    return "maxColumn: integer expected";
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa2.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa2.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa2.AwardReel)
                return object;
            let message = new $root.jlsa2.AwardReel();
            switch (object.symbol) {
            default:
                if (typeof object.symbol === "number") {
                    message.symbol = object.symbol;
                    break;
                }
                break;
            case "SmallGhost":
            case 0:
                message.symbol = 0;
                break;
            case "Bonus":
            case 1:
                message.symbol = 1;
                break;
            case "Symbol1":
            case 2:
                message.symbol = 2;
                break;
            case "Symbol2":
            case 3:
                message.symbol = 3;
                break;
            case "Symbol3":
            case 4:
                message.symbol = 4;
                break;
            case "Symbol4":
            case 5:
                message.symbol = 5;
                break;
            case "Symbol5":
            case 6:
                message.symbol = 6;
                break;
            case "Symbol6":
            case 7:
                message.symbol = 7;
                break;
            case "Symbol7":
            case 8:
                message.symbol = 8;
                break;
            case "Symbol8":
            case 9:
                message.symbol = 9;
                break;
            case "BigGhost":
            case 10:
                message.symbol = 10;
                break;
            }
            if (object.totalCount != null)
                message.totalCount = object.totalCount >>> 0;
            if (object.maxColumn != null)
                message.maxColumn = object.maxColumn >>> 0;
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa2.AwardReel
         * @static
         * @param {jlsa2.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = options.enums === String ? "SmallGhost" : 0;
                object.totalCount = 0;
                object.maxColumn = 0;
                object.win = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = options.enums === String ? $root.jlsa2.Symbol[message.symbol] === undefined ? message.symbol : $root.jlsa2.Symbol[message.symbol] : message.symbol;
            if (message.totalCount != null && message.hasOwnProperty("totalCount"))
                object.totalCount = message.totalCount;
            if (message.maxColumn != null && message.hasOwnProperty("maxColumn"))
                object.maxColumn = message.maxColumn;
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlsa2.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlsa2.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa2.AwardReel";
        };

        return AwardReel;
    })();

    jlsa2.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlsa2
         * @interface ICell
         * @property {jlsa2.Symbol|null} [symbol] Cell symbol
         * @property {boolean|null} [isGold] Cell isGold
         * @property {number|null} [row] Cell row
         * @property {number|null} [column] Cell column
         * @property {number|null} [count] Cell count
         */

        /**
         * Constructs a new Cell.
         * @memberof jlsa2
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlsa2.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {jlsa2.Symbol} symbol
         * @memberof jlsa2.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell isGold.
         * @member {boolean} isGold
         * @memberof jlsa2.Cell
         * @instance
         */
        Cell.prototype.isGold = false;

        /**
         * Cell row.
         * @member {number} row
         * @memberof jlsa2.Cell
         * @instance
         */
        Cell.prototype.row = 0;

        /**
         * Cell column.
         * @member {number} column
         * @memberof jlsa2.Cell
         * @instance
         */
        Cell.prototype.column = 0;

        /**
         * Cell count.
         * @member {number} count
         * @memberof jlsa2.Cell
         * @instance
         */
        Cell.prototype.count = 0;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlsa2.Cell
         * @static
         * @param {jlsa2.ICell=} [properties] Properties to set
         * @returns {jlsa2.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlsa2.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlsa2.Cell
         * @static
         * @param {jlsa2.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.isGold != null && Object.hasOwnProperty.call(message, "isGold"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isGold);
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.column);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.count);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlsa2.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa2.Cell
         * @static
         * @param {jlsa2.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa2.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa2.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa2.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.isGold = reader.bool();
                        break;
                    }
                case 3: {
                        message.row = reader.int32();
                        break;
                    }
                case 4: {
                        message.column = reader.int32();
                        break;
                    }
                case 5: {
                        message.count = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa2.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa2.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlsa2.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                switch (message.symbol) {
                default:
                    return "symbol: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                    break;
                }
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                if (typeof message.isGold !== "boolean")
                    return "isGold: boolean expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa2.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa2.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa2.Cell)
                return object;
            let message = new $root.jlsa2.Cell();
            switch (object.symbol) {
            default:
                if (typeof object.symbol === "number") {
                    message.symbol = object.symbol;
                    break;
                }
                break;
            case "SmallGhost":
            case 0:
                message.symbol = 0;
                break;
            case "Bonus":
            case 1:
                message.symbol = 1;
                break;
            case "Symbol1":
            case 2:
                message.symbol = 2;
                break;
            case "Symbol2":
            case 3:
                message.symbol = 3;
                break;
            case "Symbol3":
            case 4:
                message.symbol = 4;
                break;
            case "Symbol4":
            case 5:
                message.symbol = 5;
                break;
            case "Symbol5":
            case 6:
                message.symbol = 6;
                break;
            case "Symbol6":
            case 7:
                message.symbol = 7;
                break;
            case "Symbol7":
            case 8:
                message.symbol = 8;
                break;
            case "Symbol8":
            case 9:
                message.symbol = 9;
                break;
            case "BigGhost":
            case 10:
                message.symbol = 10;
                break;
            }
            if (object.isGold != null)
                message.isGold = Boolean(object.isGold);
            if (object.row != null)
                message.row = object.row | 0;
            if (object.column != null)
                message.column = object.column | 0;
            if (object.count != null)
                message.count = object.count | 0;
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa2.Cell
         * @static
         * @param {jlsa2.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = options.enums === String ? "SmallGhost" : 0;
                object.isGold = false;
                object.row = 0;
                object.column = 0;
                object.count = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = options.enums === String ? $root.jlsa2.Symbol[message.symbol] === undefined ? message.symbol : $root.jlsa2.Symbol[message.symbol] : message.symbol;
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                object.isGold = message.isGold;
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlsa2.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlsa2.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa2.Cell";
        };

        return Cell;
    })();

    jlsa2.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlsa2
         * @interface IColumnData
         * @property {Array.<jlsa2.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlsa2
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlsa2.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jlsa2.ICell>} row
         * @memberof jlsa2.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlsa2.ColumnData
         * @static
         * @param {jlsa2.IColumnData=} [properties] Properties to set
         * @returns {jlsa2.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlsa2.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlsa2.ColumnData
         * @static
         * @param {jlsa2.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jlsa2.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlsa2.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa2.ColumnData
         * @static
         * @param {jlsa2.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa2.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa2.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa2.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jlsa2.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa2.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa2.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlsa2.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jlsa2.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa2.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa2.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa2.ColumnData)
                return object;
            let message = new $root.jlsa2.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlsa2.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jlsa2.ColumnData.row: object expected");
                    message.row[i] = $root.jlsa2.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa2.ColumnData
         * @static
         * @param {jlsa2.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jlsa2.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlsa2.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlsa2.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa2.ColumnData";
        };

        return ColumnData;
    })();

    jlsa2.Combo = (function() {

        /**
         * Properties of a Combo.
         * @memberof jlsa2
         * @interface ICombo
         * @property {Array.<jlsa2.ICell>|null} [change] Combo change
         * @property {Array.<jlsa2.ICell>|null} [ghostChange] Combo ghostChange
         * @property {Array.<jlsa2.IAwardReel>|null} [award] Combo award
         * @property {number|null} [win] Combo win
         */

        /**
         * Constructs a new Combo.
         * @memberof jlsa2
         * @classdesc Represents a Combo.
         * @implements ICombo
         * @constructor
         * @param {jlsa2.ICombo=} [properties] Properties to set
         */
        function Combo(properties) {
            this.change = [];
            this.ghostChange = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Combo change.
         * @member {Array.<jlsa2.ICell>} change
         * @memberof jlsa2.Combo
         * @instance
         */
        Combo.prototype.change = $util.emptyArray;

        /**
         * Combo ghostChange.
         * @member {Array.<jlsa2.ICell>} ghostChange
         * @memberof jlsa2.Combo
         * @instance
         */
        Combo.prototype.ghostChange = $util.emptyArray;

        /**
         * Combo award.
         * @member {Array.<jlsa2.IAwardReel>} award
         * @memberof jlsa2.Combo
         * @instance
         */
        Combo.prototype.award = $util.emptyArray;

        /**
         * Combo win.
         * @member {number} win
         * @memberof jlsa2.Combo
         * @instance
         */
        Combo.prototype.win = 0;

        /**
         * Creates a new Combo instance using the specified properties.
         * @function create
         * @memberof jlsa2.Combo
         * @static
         * @param {jlsa2.ICombo=} [properties] Properties to set
         * @returns {jlsa2.Combo} Combo instance
         */
        Combo.create = function create(properties) {
            return new Combo(properties);
        };

        /**
         * Encodes the specified Combo message. Does not implicitly {@link jlsa2.Combo.verify|verify} messages.
         * @function encode
         * @memberof jlsa2.Combo
         * @static
         * @param {jlsa2.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.change != null && message.change.length)
                for (let i = 0; i < message.change.length; ++i)
                    $root.jlsa2.Cell.encode(message.change[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ghostChange != null && message.ghostChange.length)
                for (let i = 0; i < message.ghostChange.length; ++i)
                    $root.jlsa2.Cell.encode(message.ghostChange[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlsa2.AwardReel.encode(message.award[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified Combo message, length delimited. Does not implicitly {@link jlsa2.Combo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa2.Combo
         * @static
         * @param {jlsa2.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Combo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa2.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa2.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa2.Combo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.change && message.change.length))
                            message.change = [];
                        message.change.push($root.jlsa2.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.ghostChange && message.ghostChange.length))
                            message.ghostChange = [];
                        message.ghostChange.push($root.jlsa2.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlsa2.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Combo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa2.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa2.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Combo message.
         * @function verify
         * @memberof jlsa2.Combo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Combo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.change != null && message.hasOwnProperty("change")) {
                if (!Array.isArray(message.change))
                    return "change: array expected";
                for (let i = 0; i < message.change.length; ++i) {
                    let error = $root.jlsa2.Cell.verify(message.change[i]);
                    if (error)
                        return "change." + error;
                }
            }
            if (message.ghostChange != null && message.hasOwnProperty("ghostChange")) {
                if (!Array.isArray(message.ghostChange))
                    return "ghostChange: array expected";
                for (let i = 0; i < message.ghostChange.length; ++i) {
                    let error = $root.jlsa2.Cell.verify(message.ghostChange[i]);
                    if (error)
                        return "ghostChange." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlsa2.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates a Combo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa2.Combo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa2.Combo} Combo
         */
        Combo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa2.Combo)
                return object;
            let message = new $root.jlsa2.Combo();
            if (object.change) {
                if (!Array.isArray(object.change))
                    throw TypeError(".jlsa2.Combo.change: array expected");
                message.change = [];
                for (let i = 0; i < object.change.length; ++i) {
                    if (typeof object.change[i] !== "object")
                        throw TypeError(".jlsa2.Combo.change: object expected");
                    message.change[i] = $root.jlsa2.Cell.fromObject(object.change[i]);
                }
            }
            if (object.ghostChange) {
                if (!Array.isArray(object.ghostChange))
                    throw TypeError(".jlsa2.Combo.ghostChange: array expected");
                message.ghostChange = [];
                for (let i = 0; i < object.ghostChange.length; ++i) {
                    if (typeof object.ghostChange[i] !== "object")
                        throw TypeError(".jlsa2.Combo.ghostChange: object expected");
                    message.ghostChange[i] = $root.jlsa2.Cell.fromObject(object.ghostChange[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlsa2.Combo.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlsa2.Combo.award: object expected");
                    message.award[i] = $root.jlsa2.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from a Combo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa2.Combo
         * @static
         * @param {jlsa2.Combo} message Combo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Combo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change = [];
                object.ghostChange = [];
                object.award = [];
            }
            if (options.defaults)
                object.win = 0;
            if (message.change && message.change.length) {
                object.change = [];
                for (let j = 0; j < message.change.length; ++j)
                    object.change[j] = $root.jlsa2.Cell.toObject(message.change[j], options);
            }
            if (message.ghostChange && message.ghostChange.length) {
                object.ghostChange = [];
                for (let j = 0; j < message.ghostChange.length; ++j)
                    object.ghostChange[j] = $root.jlsa2.Cell.toObject(message.ghostChange[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlsa2.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this Combo to JSON.
         * @function toJSON
         * @memberof jlsa2.Combo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Combo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Combo
         * @function getTypeUrl
         * @memberof jlsa2.Combo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Combo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa2.Combo";
        };

        return Combo;
    })();

    jlsa2.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlsa2
         * @interface IPlate
         * @property {Array.<jlsa2.IColumnData>|null} [column] Plate column
         * @property {Array.<jlsa2.ICombo>|null} [combo] Plate combo
         * @property {number|null} [win] Plate win
         */

        /**
         * Constructs a new Plate.
         * @memberof jlsa2
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlsa2.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.combo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlsa2.IColumnData>} column
         * @memberof jlsa2.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate combo.
         * @member {Array.<jlsa2.ICombo>} combo
         * @memberof jlsa2.Plate
         * @instance
         */
        Plate.prototype.combo = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlsa2.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlsa2.Plate
         * @static
         * @param {jlsa2.IPlate=} [properties] Properties to set
         * @returns {jlsa2.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlsa2.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlsa2.Plate
         * @static
         * @param {jlsa2.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlsa2.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.combo != null && message.combo.length)
                for (let i = 0; i < message.combo.length; ++i)
                    $root.jlsa2.Combo.encode(message.combo[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlsa2.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa2.Plate
         * @static
         * @param {jlsa2.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa2.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa2.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa2.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlsa2.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.combo && message.combo.length))
                            message.combo = [];
                        message.combo.push($root.jlsa2.Combo.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa2.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa2.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlsa2.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlsa2.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.combo != null && message.hasOwnProperty("combo")) {
                if (!Array.isArray(message.combo))
                    return "combo: array expected";
                for (let i = 0; i < message.combo.length; ++i) {
                    let error = $root.jlsa2.Combo.verify(message.combo[i]);
                    if (error)
                        return "combo." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa2.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa2.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa2.Plate)
                return object;
            let message = new $root.jlsa2.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlsa2.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlsa2.Plate.column: object expected");
                    message.column[i] = $root.jlsa2.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.combo) {
                if (!Array.isArray(object.combo))
                    throw TypeError(".jlsa2.Plate.combo: array expected");
                message.combo = [];
                for (let i = 0; i < object.combo.length; ++i) {
                    if (typeof object.combo[i] !== "object")
                        throw TypeError(".jlsa2.Plate.combo: object expected");
                    message.combo[i] = $root.jlsa2.Combo.fromObject(object.combo[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa2.Plate
         * @static
         * @param {jlsa2.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.combo = [];
            }
            if (options.defaults)
                object.win = 0;
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlsa2.ColumnData.toObject(message.column[j], options);
            }
            if (message.combo && message.combo.length) {
                object.combo = [];
                for (let j = 0; j < message.combo.length; ++j)
                    object.combo[j] = $root.jlsa2.Combo.toObject(message.combo[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlsa2.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlsa2.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa2.Plate";
        };

        return Plate;
    })();

    jlsa2.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlsa2
         * @interface IAllPlate
         * @property {Array.<jlsa2.IPlate>|null} [plate] AllPlate plate
         * @property {number|null} [win] AllPlate win
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlsa2
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlsa2.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlsa2.IPlate>} plate
         * @memberof jlsa2.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * AllPlate win.
         * @member {number} win
         * @memberof jlsa2.AllPlate
         * @instance
         */
        AllPlate.prototype.win = 0;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlsa2.AllPlate
         * @static
         * @param {jlsa2.IAllPlate=} [properties] Properties to set
         * @returns {jlsa2.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlsa2.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlsa2.AllPlate
         * @static
         * @param {jlsa2.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlsa2.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlsa2.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsa2.AllPlate
         * @static
         * @param {jlsa2.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlsa2.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsa2.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsa2.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlsa2.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsa2.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsa2.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlsa2.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlsa2.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsa2.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsa2.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsa2.AllPlate)
                return object;
            let message = new $root.jlsa2.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlsa2.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlsa2.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlsa2.Plate.fromObject(object.plate[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsa2.AllPlate
         * @static
         * @param {jlsa2.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (options.defaults)
                object.win = 0;
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlsa2.Plate.toObject(message.plate[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlsa2.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlsa2.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsa2.AllPlate";
        };

        return AllPlate;
    })();

    return jlsa2;
})();

export const jlsad = $root.jlsad = (() => {

    /**
     * Namespace jlsad.
     * @exports jlsad
     * @namespace
     */
    const jlsad = {};

    /**
     * Symbol enum.
     * @name jlsad.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} SymbolMax=10 SymbolMax value
     */
    jlsad.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "SymbolMax"] = 10;
        return values;
    })();

    jlsad.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlsad
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlsad
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlsad.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlsad.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlsad.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlsad.Block
         * @static
         * @param {jlsad.IBlock=} [properties] Properties to set
         * @returns {jlsad.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlsad.Block.verify|verify} messages.
         * @function encode
         * @memberof jlsad.Block
         * @static
         * @param {jlsad.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlsad.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.Block
         * @static
         * @param {jlsad.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlsad.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.Block)
                return object;
            let message = new $root.jlsad.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.Block
         * @static
         * @param {jlsad.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlsad.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlsad.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.Block";
        };

        return Block;
    })();

    jlsad.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlsad
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {number|null} [row] Cell row
         * @property {number|null} [column] Cell column
         * @property {number|null} [isGold] Cell isGold
         */

        /**
         * Constructs a new Cell.
         * @memberof jlsad
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlsad.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jlsad.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell row.
         * @member {number} row
         * @memberof jlsad.Cell
         * @instance
         */
        Cell.prototype.row = 0;

        /**
         * Cell column.
         * @member {number} column
         * @memberof jlsad.Cell
         * @instance
         */
        Cell.prototype.column = 0;

        /**
         * Cell isGold.
         * @member {number} isGold
         * @memberof jlsad.Cell
         * @instance
         */
        Cell.prototype.isGold = 0;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlsad.Cell
         * @static
         * @param {jlsad.ICell=} [properties] Properties to set
         * @returns {jlsad.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlsad.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlsad.Cell
         * @static
         * @param {jlsad.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.column);
            if (message.isGold != null && Object.hasOwnProperty.call(message, "isGold"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.isGold);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlsad.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.Cell
         * @static
         * @param {jlsad.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.row = reader.uint32();
                        break;
                    }
                case 3: {
                        message.column = reader.uint32();
                        break;
                    }
                case 4: {
                        message.isGold = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlsad.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                if (!$util.isInteger(message.isGold))
                    return "isGold: integer expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.Cell)
                return object;
            let message = new $root.jlsad.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            if (object.isGold != null)
                message.isGold = object.isGold >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.Cell
         * @static
         * @param {jlsad.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.row = 0;
                object.column = 0;
                object.isGold = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            if (message.isGold != null && message.hasOwnProperty("isGold"))
                object.isGold = message.isGold;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlsad.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlsad.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.Cell";
        };

        return Cell;
    })();

    jlsad.AwardReel = (function() {

        /**
         * Properties of an AwardReel.
         * @memberof jlsad
         * @interface IAwardReel
         * @property {number|null} [symbol] AwardReel symbol
         * @property {Array.<jlsad.IBlock>|null} [block] AwardReel block
         * @property {number|null} [win] AwardReel win
         */

        /**
         * Constructs a new AwardReel.
         * @memberof jlsad
         * @classdesc Represents an AwardReel.
         * @implements IAwardReel
         * @constructor
         * @param {jlsad.IAwardReel=} [properties] Properties to set
         */
        function AwardReel(properties) {
            this.block = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardReel symbol.
         * @member {number} symbol
         * @memberof jlsad.AwardReel
         * @instance
         */
        AwardReel.prototype.symbol = 0;

        /**
         * AwardReel block.
         * @member {Array.<jlsad.IBlock>} block
         * @memberof jlsad.AwardReel
         * @instance
         */
        AwardReel.prototype.block = $util.emptyArray;

        /**
         * AwardReel win.
         * @member {number} win
         * @memberof jlsad.AwardReel
         * @instance
         */
        AwardReel.prototype.win = 0;

        /**
         * Creates a new AwardReel instance using the specified properties.
         * @function create
         * @memberof jlsad.AwardReel
         * @static
         * @param {jlsad.IAwardReel=} [properties] Properties to set
         * @returns {jlsad.AwardReel} AwardReel instance
         */
        AwardReel.create = function create(properties) {
            return new AwardReel(properties);
        };

        /**
         * Encodes the specified AwardReel message. Does not implicitly {@link jlsad.AwardReel.verify|verify} messages.
         * @function encode
         * @memberof jlsad.AwardReel
         * @static
         * @param {jlsad.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.block != null && message.block.length)
                for (let i = 0; i < message.block.length; ++i)
                    $root.jlsad.Block.encode(message.block[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlsad.AwardReel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.AwardReel
         * @static
         * @param {jlsad.IAwardReel} message AwardReel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.AwardReel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.block && message.block.length))
                            message.block = [];
                        message.block.push($root.jlsad.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardReel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.AwardReel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.AwardReel} AwardReel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardReel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardReel message.
         * @function verify
         * @memberof jlsad.AwardReel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardReel.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                if (!Array.isArray(message.block))
                    return "block: array expected";
                for (let i = 0; i < message.block.length; ++i) {
                    let error = $root.jlsad.Block.verify(message.block[i]);
                    if (error)
                        return "block." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.AwardReel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.AwardReel} AwardReel
         */
        AwardReel.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.AwardReel)
                return object;
            let message = new $root.jlsad.AwardReel();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.block) {
                if (!Array.isArray(object.block))
                    throw TypeError(".jlsad.AwardReel.block: array expected");
                message.block = [];
                for (let i = 0; i < object.block.length; ++i) {
                    if (typeof object.block[i] !== "object")
                        throw TypeError(".jlsad.AwardReel.block: object expected");
                    message.block[i] = $root.jlsad.Block.fromObject(object.block[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.AwardReel
         * @static
         * @param {jlsad.AwardReel} message AwardReel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardReel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.block = [];
            if (options.defaults) {
                object.symbol = 0;
                object.win = 0;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.block && message.block.length) {
                object.block = [];
                for (let j = 0; j < message.block.length; ++j)
                    object.block[j] = $root.jlsad.Block.toObject(message.block[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this AwardReel to JSON.
         * @function toJSON
         * @memberof jlsad.AwardReel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardReel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardReel
         * @function getTypeUrl
         * @memberof jlsad.AwardReel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.AwardReel";
        };

        return AwardReel;
    })();

    jlsad.Combo = (function() {

        /**
         * Properties of a Combo.
         * @memberof jlsad
         * @interface ICombo
         * @property {Array.<jlsad.ICell>|null} [change] Combo change
         * @property {Array.<jlsad.IAwardReel>|null} [award] Combo award
         * @property {number|null} [win] Combo win
         * @property {number|null} [comboBonus] Combo comboBonus
         * @property {Array.<jlsad.IBlock>|null} [goldReplace] Combo goldReplace
         * @property {Array.<jlsad.ICell>|null} [extraChange] Combo extraChange
         * @property {Array.<number>|null} [bigGoldCount] Combo bigGoldCount
         */

        /**
         * Constructs a new Combo.
         * @memberof jlsad
         * @classdesc Represents a Combo.
         * @implements ICombo
         * @constructor
         * @param {jlsad.ICombo=} [properties] Properties to set
         */
        function Combo(properties) {
            this.change = [];
            this.award = [];
            this.goldReplace = [];
            this.extraChange = [];
            this.bigGoldCount = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Combo change.
         * @member {Array.<jlsad.ICell>} change
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.change = $util.emptyArray;

        /**
         * Combo award.
         * @member {Array.<jlsad.IAwardReel>} award
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.award = $util.emptyArray;

        /**
         * Combo win.
         * @member {number} win
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.win = 0;

        /**
         * Combo comboBonus.
         * @member {number} comboBonus
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.comboBonus = 0;

        /**
         * Combo goldReplace.
         * @member {Array.<jlsad.IBlock>} goldReplace
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.goldReplace = $util.emptyArray;

        /**
         * Combo extraChange.
         * @member {Array.<jlsad.ICell>} extraChange
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.extraChange = $util.emptyArray;

        /**
         * Combo bigGoldCount.
         * @member {Array.<number>} bigGoldCount
         * @memberof jlsad.Combo
         * @instance
         */
        Combo.prototype.bigGoldCount = $util.emptyArray;

        /**
         * Creates a new Combo instance using the specified properties.
         * @function create
         * @memberof jlsad.Combo
         * @static
         * @param {jlsad.ICombo=} [properties] Properties to set
         * @returns {jlsad.Combo} Combo instance
         */
        Combo.create = function create(properties) {
            return new Combo(properties);
        };

        /**
         * Encodes the specified Combo message. Does not implicitly {@link jlsad.Combo.verify|verify} messages.
         * @function encode
         * @memberof jlsad.Combo
         * @static
         * @param {jlsad.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.change != null && message.change.length)
                for (let i = 0; i < message.change.length; ++i)
                    $root.jlsad.Cell.encode(message.change[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlsad.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.comboBonus != null && Object.hasOwnProperty.call(message, "comboBonus"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.comboBonus);
            if (message.extraChange != null && message.extraChange.length)
                for (let i = 0; i < message.extraChange.length; ++i)
                    $root.jlsad.Cell.encode(message.extraChange[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.goldReplace != null && message.goldReplace.length)
                for (let i = 0; i < message.goldReplace.length; ++i)
                    $root.jlsad.Block.encode(message.goldReplace[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.bigGoldCount != null && message.bigGoldCount.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.bigGoldCount.length; ++i)
                    writer.int32(message.bigGoldCount[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Combo message, length delimited. Does not implicitly {@link jlsad.Combo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.Combo
         * @static
         * @param {jlsad.ICombo} message Combo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Combo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Combo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.Combo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.change && message.change.length))
                            message.change = [];
                        message.change.push($root.jlsad.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlsad.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.comboBonus = reader.uint32();
                        break;
                    }
                case 6: {
                        if (!(message.goldReplace && message.goldReplace.length))
                            message.goldReplace = [];
                        message.goldReplace.push($root.jlsad.Block.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.extraChange && message.extraChange.length))
                            message.extraChange = [];
                        message.extraChange.push($root.jlsad.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        if (!(message.bigGoldCount && message.bigGoldCount.length))
                            message.bigGoldCount = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.bigGoldCount.push(reader.int32());
                        } else
                            message.bigGoldCount.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Combo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.Combo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.Combo} Combo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Combo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Combo message.
         * @function verify
         * @memberof jlsad.Combo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Combo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.change != null && message.hasOwnProperty("change")) {
                if (!Array.isArray(message.change))
                    return "change: array expected";
                for (let i = 0; i < message.change.length; ++i) {
                    let error = $root.jlsad.Cell.verify(message.change[i]);
                    if (error)
                        return "change." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlsad.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.comboBonus != null && message.hasOwnProperty("comboBonus"))
                if (!$util.isInteger(message.comboBonus))
                    return "comboBonus: integer expected";
            if (message.goldReplace != null && message.hasOwnProperty("goldReplace")) {
                if (!Array.isArray(message.goldReplace))
                    return "goldReplace: array expected";
                for (let i = 0; i < message.goldReplace.length; ++i) {
                    let error = $root.jlsad.Block.verify(message.goldReplace[i]);
                    if (error)
                        return "goldReplace." + error;
                }
            }
            if (message.extraChange != null && message.hasOwnProperty("extraChange")) {
                if (!Array.isArray(message.extraChange))
                    return "extraChange: array expected";
                for (let i = 0; i < message.extraChange.length; ++i) {
                    let error = $root.jlsad.Cell.verify(message.extraChange[i]);
                    if (error)
                        return "extraChange." + error;
                }
            }
            if (message.bigGoldCount != null && message.hasOwnProperty("bigGoldCount")) {
                if (!Array.isArray(message.bigGoldCount))
                    return "bigGoldCount: array expected";
                for (let i = 0; i < message.bigGoldCount.length; ++i)
                    if (!$util.isInteger(message.bigGoldCount[i]))
                        return "bigGoldCount: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Combo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.Combo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.Combo} Combo
         */
        Combo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.Combo)
                return object;
            let message = new $root.jlsad.Combo();
            if (object.change) {
                if (!Array.isArray(object.change))
                    throw TypeError(".jlsad.Combo.change: array expected");
                message.change = [];
                for (let i = 0; i < object.change.length; ++i) {
                    if (typeof object.change[i] !== "object")
                        throw TypeError(".jlsad.Combo.change: object expected");
                    message.change[i] = $root.jlsad.Cell.fromObject(object.change[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlsad.Combo.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlsad.Combo.award: object expected");
                    message.award[i] = $root.jlsad.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.comboBonus != null)
                message.comboBonus = object.comboBonus >>> 0;
            if (object.goldReplace) {
                if (!Array.isArray(object.goldReplace))
                    throw TypeError(".jlsad.Combo.goldReplace: array expected");
                message.goldReplace = [];
                for (let i = 0; i < object.goldReplace.length; ++i) {
                    if (typeof object.goldReplace[i] !== "object")
                        throw TypeError(".jlsad.Combo.goldReplace: object expected");
                    message.goldReplace[i] = $root.jlsad.Block.fromObject(object.goldReplace[i]);
                }
            }
            if (object.extraChange) {
                if (!Array.isArray(object.extraChange))
                    throw TypeError(".jlsad.Combo.extraChange: array expected");
                message.extraChange = [];
                for (let i = 0; i < object.extraChange.length; ++i) {
                    if (typeof object.extraChange[i] !== "object")
                        throw TypeError(".jlsad.Combo.extraChange: object expected");
                    message.extraChange[i] = $root.jlsad.Cell.fromObject(object.extraChange[i]);
                }
            }
            if (object.bigGoldCount) {
                if (!Array.isArray(object.bigGoldCount))
                    throw TypeError(".jlsad.Combo.bigGoldCount: array expected");
                message.bigGoldCount = [];
                for (let i = 0; i < object.bigGoldCount.length; ++i)
                    message.bigGoldCount[i] = object.bigGoldCount[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Combo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.Combo
         * @static
         * @param {jlsad.Combo} message Combo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Combo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change = [];
                object.award = [];
                object.extraChange = [];
                object.goldReplace = [];
                object.bigGoldCount = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.comboBonus = 0;
            }
            if (message.change && message.change.length) {
                object.change = [];
                for (let j = 0; j < message.change.length; ++j)
                    object.change[j] = $root.jlsad.Cell.toObject(message.change[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlsad.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.comboBonus != null && message.hasOwnProperty("comboBonus"))
                object.comboBonus = message.comboBonus;
            if (message.extraChange && message.extraChange.length) {
                object.extraChange = [];
                for (let j = 0; j < message.extraChange.length; ++j)
                    object.extraChange[j] = $root.jlsad.Cell.toObject(message.extraChange[j], options);
            }
            if (message.goldReplace && message.goldReplace.length) {
                object.goldReplace = [];
                for (let j = 0; j < message.goldReplace.length; ++j)
                    object.goldReplace[j] = $root.jlsad.Block.toObject(message.goldReplace[j], options);
            }
            if (message.bigGoldCount && message.bigGoldCount.length) {
                object.bigGoldCount = [];
                for (let j = 0; j < message.bigGoldCount.length; ++j)
                    object.bigGoldCount[j] = message.bigGoldCount[j];
            }
            return object;
        };

        /**
         * Converts this Combo to JSON.
         * @function toJSON
         * @memberof jlsad.Combo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Combo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Combo
         * @function getTypeUrl
         * @memberof jlsad.Combo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Combo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.Combo";
        };

        return Combo;
    })();

    jlsad.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlsad
         * @interface IPlate
         * @property {Array.<jlsad.IColumnData>|null} [column] Plate column
         * @property {Array.<jlsad.ICombo>|null} [combo] Plate combo
         * @property {number|null} [win] Plate win
         * @property {boolean|null} [isBouns] Plate isBouns
         * @property {number|null} [totalFreeSpin] Plate totalFreeSpin
         */

        /**
         * Constructs a new Plate.
         * @memberof jlsad
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlsad.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.combo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlsad.IColumnData>} column
         * @memberof jlsad.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate combo.
         * @member {Array.<jlsad.ICombo>} combo
         * @memberof jlsad.Plate
         * @instance
         */
        Plate.prototype.combo = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlsad.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate isBouns.
         * @member {boolean} isBouns
         * @memberof jlsad.Plate
         * @instance
         */
        Plate.prototype.isBouns = false;

        /**
         * Plate totalFreeSpin.
         * @member {number} totalFreeSpin
         * @memberof jlsad.Plate
         * @instance
         */
        Plate.prototype.totalFreeSpin = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlsad.Plate
         * @static
         * @param {jlsad.IPlate=} [properties] Properties to set
         * @returns {jlsad.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlsad.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlsad.Plate
         * @static
         * @param {jlsad.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlsad.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.combo != null && message.combo.length)
                for (let i = 0; i < message.combo.length; ++i)
                    $root.jlsad.Combo.encode(message.combo[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.isBouns != null && Object.hasOwnProperty.call(message, "isBouns"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isBouns);
            if (message.totalFreeSpin != null && Object.hasOwnProperty.call(message, "totalFreeSpin"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.totalFreeSpin);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlsad.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.Plate
         * @static
         * @param {jlsad.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlsad.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.combo && message.combo.length))
                            message.combo = [];
                        message.combo.push($root.jlsad.Combo.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.isBouns = reader.bool();
                        break;
                    }
                case 5: {
                        message.totalFreeSpin = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlsad.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlsad.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.combo != null && message.hasOwnProperty("combo")) {
                if (!Array.isArray(message.combo))
                    return "combo: array expected";
                for (let i = 0; i < message.combo.length; ++i) {
                    let error = $root.jlsad.Combo.verify(message.combo[i]);
                    if (error)
                        return "combo." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.isBouns != null && message.hasOwnProperty("isBouns"))
                if (typeof message.isBouns !== "boolean")
                    return "isBouns: boolean expected";
            if (message.totalFreeSpin != null && message.hasOwnProperty("totalFreeSpin"))
                if (!$util.isInteger(message.totalFreeSpin))
                    return "totalFreeSpin: integer expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.Plate)
                return object;
            let message = new $root.jlsad.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlsad.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlsad.Plate.column: object expected");
                    message.column[i] = $root.jlsad.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.combo) {
                if (!Array.isArray(object.combo))
                    throw TypeError(".jlsad.Plate.combo: array expected");
                message.combo = [];
                for (let i = 0; i < object.combo.length; ++i) {
                    if (typeof object.combo[i] !== "object")
                        throw TypeError(".jlsad.Plate.combo: object expected");
                    message.combo[i] = $root.jlsad.Combo.fromObject(object.combo[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.isBouns != null)
                message.isBouns = Boolean(object.isBouns);
            if (object.totalFreeSpin != null)
                message.totalFreeSpin = object.totalFreeSpin >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.Plate
         * @static
         * @param {jlsad.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.combo = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.isBouns = false;
                object.totalFreeSpin = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlsad.ColumnData.toObject(message.column[j], options);
            }
            if (message.combo && message.combo.length) {
                object.combo = [];
                for (let j = 0; j < message.combo.length; ++j)
                    object.combo[j] = $root.jlsad.Combo.toObject(message.combo[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.isBouns != null && message.hasOwnProperty("isBouns"))
                object.isBouns = message.isBouns;
            if (message.totalFreeSpin != null && message.hasOwnProperty("totalFreeSpin"))
                object.totalFreeSpin = message.totalFreeSpin;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlsad.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlsad.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.Plate";
        };

        return Plate;
    })();

    jlsad.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlsad
         * @interface IAllPlate
         * @property {Array.<jlsad.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlsad
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlsad.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlsad.IPlate>} plate
         * @memberof jlsad.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlsad.AllPlate
         * @static
         * @param {jlsad.IAllPlate=} [properties] Properties to set
         * @returns {jlsad.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlsad.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlsad.AllPlate
         * @static
         * @param {jlsad.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlsad.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlsad.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.AllPlate
         * @static
         * @param {jlsad.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlsad.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlsad.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlsad.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.AllPlate)
                return object;
            let message = new $root.jlsad.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlsad.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlsad.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlsad.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.AllPlate
         * @static
         * @param {jlsad.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlsad.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlsad.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlsad.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.AllPlate";
        };

        return AllPlate;
    })();

    jlsad.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlsad
         * @interface IColumnData
         * @property {Array.<number>|null} [row] ColumnData row
         * @property {Array.<number>|null} [isGold] ColumnData isGold
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlsad
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlsad.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            this.isGold = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<number>} row
         * @memberof jlsad.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * ColumnData isGold.
         * @member {Array.<number>} isGold
         * @memberof jlsad.ColumnData
         * @instance
         */
        ColumnData.prototype.isGold = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlsad.ColumnData
         * @static
         * @param {jlsad.IColumnData=} [properties] Properties to set
         * @returns {jlsad.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlsad.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlsad.ColumnData
         * @static
         * @param {jlsad.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            if (message.isGold != null && message.isGold.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.isGold.length; ++i)
                    writer.uint32(message.isGold[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlsad.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsad.ColumnData
         * @static
         * @param {jlsad.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsad.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsad.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsad.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.isGold && message.isGold.length))
                            message.isGold = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.isGold.push(reader.uint32());
                        } else
                            message.isGold.push(reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsad.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsad.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlsad.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            if (message.isGold != null && message.hasOwnProperty("isGold")) {
                if (!Array.isArray(message.isGold))
                    return "isGold: array expected";
                for (let i = 0; i < message.isGold.length; ++i)
                    if (!$util.isInteger(message.isGold[i]))
                        return "isGold: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsad.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsad.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsad.ColumnData)
                return object;
            let message = new $root.jlsad.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlsad.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            if (object.isGold) {
                if (!Array.isArray(object.isGold))
                    throw TypeError(".jlsad.ColumnData.isGold: array expected");
                message.isGold = [];
                for (let i = 0; i < object.isGold.length; ++i)
                    message.isGold[i] = object.isGold[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsad.ColumnData
         * @static
         * @param {jlsad.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.row = [];
                object.isGold = [];
            }
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            if (message.isGold && message.isGold.length) {
                object.isGold = [];
                for (let j = 0; j < message.isGold.length; ++j)
                    object.isGold[j] = message.isGold[j];
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlsad.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlsad.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsad.ColumnData";
        };

        return ColumnData;
    })();

    return jlsad;
})();

export const jlsaj = $root.jlsaj = (() => {

    /**
     * Namespace jlsaj.
     * @exports jlsaj
     * @namespace
     */
    const jlsaj = {};

    jlsaj.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlsaj
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlsaj
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlsaj.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlsaj.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlsaj.Column
         * @static
         * @param {jlsaj.IColumn=} [properties] Properties to set
         * @returns {jlsaj.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlsaj.Column.verify|verify} messages.
         * @function encode
         * @memberof jlsaj.Column
         * @static
         * @param {jlsaj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlsaj.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsaj.Column
         * @static
         * @param {jlsaj.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlsaj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsaj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsaj.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsaj.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsaj.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlsaj.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsaj.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsaj.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsaj.Column)
                return object;
            let message = new $root.jlsaj.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlsaj.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsaj.Column
         * @static
         * @param {jlsaj.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlsaj.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlsaj.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsaj.Column";
        };

        return Column;
    })();

    jlsaj.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlsaj
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Connect] AwardData Connect
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlsaj
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlsaj.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlsaj.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Connect.
         * @member {number} Connect
         * @memberof jlsaj.AwardData
         * @instance
         */
        AwardData.prototype.Connect = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlsaj.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlsaj.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlsaj.AwardData
         * @static
         * @param {jlsaj.IAwardData=} [properties] Properties to set
         * @returns {jlsaj.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlsaj.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlsaj.AwardData
         * @static
         * @param {jlsaj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Connect != null && Object.hasOwnProperty.call(message, "Connect"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Connect);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlsaj.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsaj.AwardData
         * @static
         * @param {jlsaj.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsaj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsaj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsaj.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Connect = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsaj.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsaj.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlsaj.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Connect != null && message.hasOwnProperty("Connect"))
                if (!$util.isInteger(message.Connect))
                    return "Connect: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsaj.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsaj.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsaj.AwardData)
                return object;
            let message = new $root.jlsaj.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Connect != null)
                message.Connect = object.Connect | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsaj.AwardData
         * @static
         * @param {jlsaj.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Connect = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Connect != null && message.hasOwnProperty("Connect"))
                object.Connect = message.Connect;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlsaj.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlsaj.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsaj.AwardData";
        };

        return AwardData;
    })();

    jlsaj.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlsaj
         * @interface IComboStageInfo
         * @property {Array.<jlsaj.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {Array.<jlsaj.IColumn>|null} [ComboStageSymbolLog] ComboStageInfo ComboStageSymbolLog
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlsaj.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlsaj.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<jlsaj.IColumn>|null} [ChangeSymbols] ComboStageInfo ChangeSymbols
         * @property {number|null} [Mult] ComboStageInfo Mult
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         * @property {number|null} [MysteryResult] ComboStageInfo MysteryResult
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlsaj
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlsaj.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.ComboStageSymbolLog = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.ChangeSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlsaj.IColumn>} ComboStageSymbol
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageSymbolLog.
         * @member {Array.<jlsaj.IColumn>} ComboStageSymbolLog
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbolLog = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlsaj.IAwardData>} AwardDataVec
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlsaj.IColumn>} FillSymbols
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSymbols.
         * @member {Array.<jlsaj.IColumn>} ChangeSymbols
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSymbols = $util.emptyArray;

        /**
         * ComboStageInfo Mult.
         * @member {number} Mult
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Mult = 0;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * ComboStageInfo MysteryResult.
         * @member {number} MysteryResult
         * @memberof jlsaj.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MysteryResult = 0;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {jlsaj.IComboStageInfo=} [properties] Properties to set
         * @returns {jlsaj.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlsaj.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {jlsaj.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlsaj.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageSymbolLog != null && message.ComboStageSymbolLog.length)
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i)
                    $root.jlsaj.Column.encode(message.ComboStageSymbolLog[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlsaj.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlsaj.Column.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.ChangeSymbols != null && message.ChangeSymbols.length)
                for (let i = 0; i < message.ChangeSymbols.length; ++i)
                    $root.jlsaj.Column.encode(message.ChangeSymbols[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Mult);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            if (message.MysteryResult != null && Object.hasOwnProperty.call(message, "MysteryResult"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.MysteryResult);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlsaj.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {jlsaj.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsaj.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsaj.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlsaj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.ComboStageSymbolLog && message.ComboStageSymbolLog.length))
                            message.ComboStageSymbolLog = [];
                        message.ComboStageSymbolLog.push($root.jlsaj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlsaj.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlsaj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.ChangeSymbols && message.ChangeSymbols.length))
                            message.ChangeSymbols = [];
                        message.ChangeSymbols.push($root.jlsaj.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 7: {
                        message.Mult = reader.double();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 9: {
                        message.MysteryResult = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsaj.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlsaj.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageSymbolLog != null && message.hasOwnProperty("ComboStageSymbolLog")) {
                if (!Array.isArray(message.ComboStageSymbolLog))
                    return "ComboStageSymbolLog: array expected";
                for (let i = 0; i < message.ComboStageSymbolLog.length; ++i) {
                    let error = $root.jlsaj.Column.verify(message.ComboStageSymbolLog[i]);
                    if (error)
                        return "ComboStageSymbolLog." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlsaj.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlsaj.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSymbols != null && message.hasOwnProperty("ChangeSymbols")) {
                if (!Array.isArray(message.ChangeSymbols))
                    return "ChangeSymbols: array expected";
                for (let i = 0; i < message.ChangeSymbols.length; ++i) {
                    let error = $root.jlsaj.Column.verify(message.ChangeSymbols[i]);
                    if (error)
                        return "ChangeSymbols." + error;
                }
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.MysteryResult != null && message.hasOwnProperty("MysteryResult"))
                if (!$util.isInteger(message.MysteryResult))
                    return "MysteryResult: integer expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsaj.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsaj.ComboStageInfo)
                return object;
            let message = new $root.jlsaj.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlsaj.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlsaj.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlsaj.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageSymbolLog) {
                if (!Array.isArray(object.ComboStageSymbolLog))
                    throw TypeError(".jlsaj.ComboStageInfo.ComboStageSymbolLog: array expected");
                message.ComboStageSymbolLog = [];
                for (let i = 0; i < object.ComboStageSymbolLog.length; ++i) {
                    if (typeof object.ComboStageSymbolLog[i] !== "object")
                        throw TypeError(".jlsaj.ComboStageInfo.ComboStageSymbolLog: object expected");
                    message.ComboStageSymbolLog[i] = $root.jlsaj.Column.fromObject(object.ComboStageSymbolLog[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlsaj.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlsaj.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlsaj.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlsaj.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlsaj.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlsaj.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSymbols) {
                if (!Array.isArray(object.ChangeSymbols))
                    throw TypeError(".jlsaj.ComboStageInfo.ChangeSymbols: array expected");
                message.ChangeSymbols = [];
                for (let i = 0; i < object.ChangeSymbols.length; ++i) {
                    if (typeof object.ChangeSymbols[i] !== "object")
                        throw TypeError(".jlsaj.ComboStageInfo.ChangeSymbols: object expected");
                    message.ChangeSymbols[i] = $root.jlsaj.Column.fromObject(object.ChangeSymbols[i]);
                }
            }
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.MysteryResult != null)
                message.MysteryResult = object.MysteryResult | 0;
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {jlsaj.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.ComboStageSymbolLog = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.ChangeSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.Mult = 0;
                object.MaxFlag = false;
                object.MysteryResult = 0;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlsaj.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageSymbolLog && message.ComboStageSymbolLog.length) {
                object.ComboStageSymbolLog = [];
                for (let j = 0; j < message.ComboStageSymbolLog.length; ++j)
                    object.ComboStageSymbolLog[j] = $root.jlsaj.Column.toObject(message.ComboStageSymbolLog[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlsaj.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlsaj.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSymbols && message.ChangeSymbols.length) {
                object.ChangeSymbols = [];
                for (let j = 0; j < message.ChangeSymbols.length; ++j)
                    object.ChangeSymbols[j] = $root.jlsaj.Column.toObject(message.ChangeSymbols[j], options);
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.MysteryResult != null && message.hasOwnProperty("MysteryResult"))
                object.MysteryResult = message.MysteryResult;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlsaj.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlsaj.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsaj.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlsaj.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlsaj
         * @interface IRoundInfo
         * @property {Array.<jlsaj.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlsaj
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlsaj.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlsaj.IComboStageInfo>} ComboStageData
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsaj.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {jlsaj.IRoundInfo=} [properties] Properties to set
         * @returns {jlsaj.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlsaj.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {jlsaj.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlsaj.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AddRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlsaj.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {jlsaj.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsaj.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsaj.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlsaj.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsaj.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlsaj.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsaj.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsaj.RoundInfo)
                return object;
            let message = new $root.jlsaj.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlsaj.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlsaj.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlsaj.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {jlsaj.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.AddRound = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlsaj.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlsaj.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlsaj.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsaj.RoundInfo";
        };

        return RoundInfo;
    })();

    jlsaj.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsaj
         * @interface ISpinAck
         * @property {Array.<jlsaj.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [Bet] SpinAck Bet
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsaj
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsaj.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlsaj.IRoundInfo>} RoundQueue
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlsaj.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsaj.SpinAck
         * @static
         * @param {jlsaj.ISpinAck=} [properties] Properties to set
         * @returns {jlsaj.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsaj.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsaj.SpinAck
         * @static
         * @param {jlsaj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlsaj.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.Bet);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsaj.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsaj.SpinAck
         * @static
         * @param {jlsaj.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsaj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsaj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsaj.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlsaj.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.Bet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsaj.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsaj.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsaj.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlsaj.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsaj.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsaj.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsaj.SpinAck)
                return object;
            let message = new $root.jlsaj.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlsaj.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlsaj.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlsaj.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsaj.SpinAck
         * @static
         * @param {jlsaj.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.Bet = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlsaj.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsaj.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsaj.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsaj.SpinAck";
        };

        return SpinAck;
    })();

    jlsaj.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlsaj
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlsaj
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlsaj.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlsaj.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlsaj.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {jlsaj.IGameInfoData=} [properties] Properties to set
         * @returns {jlsaj.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlsaj.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {jlsaj.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlsaj.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {jlsaj.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsaj.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsaj.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsaj.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsaj.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsaj.GameInfoData)
                return object;
            let message = new $root.jlsaj.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlsaj.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {jlsaj.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlsaj.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlsaj.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsaj.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlsaj;
})();

export const jlsb = $root.jlsb = (() => {

    /**
     * Namespace jlsb.
     * @exports jlsb
     * @namespace
     */
    const jlsb = {};

    jlsb.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlsb
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlsb
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlsb.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlsb.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlsb.Column
         * @static
         * @param {jlsb.IColumn=} [properties] Properties to set
         * @returns {jlsb.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlsb.Column.verify|verify} messages.
         * @function encode
         * @memberof jlsb.Column
         * @static
         * @param {jlsb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlsb.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsb.Column
         * @static
         * @param {jlsb.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlsb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsb.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsb.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsb.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlsb.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsb.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsb.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsb.Column)
                return object;
            let message = new $root.jlsb.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlsb.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsb.Column
         * @static
         * @param {jlsb.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlsb.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlsb.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsb.Column";
        };

        return Column;
    })();

    jlsb.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlsb
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlsb
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlsb.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlsb.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlsb.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlsb.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlsb.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlsb.AwardData
         * @static
         * @param {jlsb.IAwardData=} [properties] Properties to set
         * @returns {jlsb.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlsb.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlsb.AwardData
         * @static
         * @param {jlsb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlsb.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsb.AwardData
         * @static
         * @param {jlsb.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsb.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsb.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsb.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlsb.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsb.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsb.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsb.AwardData)
                return object;
            let message = new $root.jlsb.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsb.AwardData
         * @static
         * @param {jlsb.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlsb.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlsb.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsb.AwardData";
        };

        return AwardData;
    })();

    jlsb.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlsb
         * @interface IRoundInfo
         * @property {Array.<jlsb.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlsb.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {number|null} [Change] RoundInfo Change
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<jlsb.IColumn>|null} [PlateSymbolReal] RoundInfo PlateSymbolReal
         * @property {number|null} [AddRound] RoundInfo AddRound
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlsb
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlsb.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.PlateSymbolReal = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlsb.IColumn>} PlateSymbol
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlsb.IAwardData>} AwardDataVec
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo Change.
         * @member {number} Change
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Change = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo PlateSymbolReal.
         * @member {Array.<jlsb.IColumn>} PlateSymbolReal
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolReal = $util.emptyArray;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlsb.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlsb.RoundInfo
         * @static
         * @param {jlsb.IRoundInfo=} [properties] Properties to set
         * @returns {jlsb.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlsb.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsb.RoundInfo
         * @static
         * @param {jlsb.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlsb.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlsb.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.RoundWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Mult);
            if (message.Change != null && Object.hasOwnProperty.call(message, "Change"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.Change);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            if (message.PlateSymbolReal != null && message.PlateSymbolReal.length)
                for (let i = 0; i < message.PlateSymbolReal.length; ++i)
                    $root.jlsb.Column.encode(message.PlateSymbolReal[i], writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.AddRound);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlsb.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsb.RoundInfo
         * @static
         * @param {jlsb.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsb.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsb.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsb.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlsb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlsb.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 6: {
                        message.Mult = reader.double();
                        break;
                    }
                case 7: {
                        message.Change = reader.int32();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 9: {
                        if (!(message.PlateSymbolReal && message.PlateSymbolReal.length))
                            message.PlateSymbolReal = [];
                        message.PlateSymbolReal.push($root.jlsb.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 10: {
                        message.AddRound = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsb.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsb.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlsb.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlsb.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlsb.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Change != null && message.hasOwnProperty("Change"))
                if (!$util.isInteger(message.Change))
                    return "Change: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.PlateSymbolReal != null && message.hasOwnProperty("PlateSymbolReal")) {
                if (!Array.isArray(message.PlateSymbolReal))
                    return "PlateSymbolReal: array expected";
                for (let i = 0; i < message.PlateSymbolReal.length; ++i) {
                    let error = $root.jlsb.Column.verify(message.PlateSymbolReal[i]);
                    if (error)
                        return "PlateSymbolReal." + error;
                }
            }
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsb.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsb.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsb.RoundInfo)
                return object;
            let message = new $root.jlsb.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlsb.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlsb.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlsb.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlsb.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlsb.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlsb.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Change != null)
                message.Change = object.Change | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.PlateSymbolReal) {
                if (!Array.isArray(object.PlateSymbolReal))
                    throw TypeError(".jlsb.RoundInfo.PlateSymbolReal: array expected");
                message.PlateSymbolReal = [];
                for (let i = 0; i < object.PlateSymbolReal.length; ++i) {
                    if (typeof object.PlateSymbolReal[i] !== "object")
                        throw TypeError(".jlsb.RoundInfo.PlateSymbolReal: object expected");
                    message.PlateSymbolReal[i] = $root.jlsb.Column.fromObject(object.PlateSymbolReal[i]);
                }
            }
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsb.RoundInfo
         * @static
         * @param {jlsb.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.PlateSymbolReal = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.Mult = 0;
                object.Change = 0;
                object.MaxFlag = false;
                object.AddRound = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlsb.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlsb.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Change != null && message.hasOwnProperty("Change"))
                object.Change = message.Change;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.PlateSymbolReal && message.PlateSymbolReal.length) {
                object.PlateSymbolReal = [];
                for (let j = 0; j < message.PlateSymbolReal.length; ++j)
                    object.PlateSymbolReal[j] = $root.jlsb.Column.toObject(message.PlateSymbolReal[j], options);
            }
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlsb.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlsb.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsb.RoundInfo";
        };

        return RoundInfo;
    })();

    jlsb.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsb
         * @interface ISpinAck
         * @property {Array.<jlsb.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsb
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsb.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlsb.IRoundInfo>} RoundQueue
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlsb.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsb.SpinAck
         * @static
         * @param {jlsb.ISpinAck=} [properties] Properties to set
         * @returns {jlsb.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsb.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsb.SpinAck
         * @static
         * @param {jlsb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlsb.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsb.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsb.SpinAck
         * @static
         * @param {jlsb.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsb.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlsb.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsb.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsb.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsb.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlsb.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsb.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsb.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsb.SpinAck)
                return object;
            let message = new $root.jlsb.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlsb.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlsb.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlsb.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsb.SpinAck
         * @static
         * @param {jlsb.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlsb.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsb.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsb.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsb.SpinAck";
        };

        return SpinAck;
    })();

    jlsb.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlsb
         * @interface IGameInfoAck
         * @property {jlsb.IVolInfo|null} [GameVolInfo] GameInfoAck GameVolInfo
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlsb
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlsb.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck GameVolInfo.
         * @member {jlsb.IVolInfo|null|undefined} GameVolInfo
         * @memberof jlsb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.GameVolInfo = null;

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlsb.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {jlsb.IGameInfoAck=} [properties] Properties to set
         * @returns {jlsb.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlsb.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {jlsb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.GameVolInfo != null && Object.hasOwnProperty.call(message, "GameVolInfo"))
                $root.jlsb.VolInfo.encode(message.GameVolInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlsb.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {jlsb.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsb.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameVolInfo = $root.jlsb.VolInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsb.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.GameVolInfo != null && message.hasOwnProperty("GameVolInfo")) {
                let error = $root.jlsb.VolInfo.verify(message.GameVolInfo);
                if (error)
                    return "GameVolInfo." + error;
            }
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsb.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsb.GameInfoAck)
                return object;
            let message = new $root.jlsb.GameInfoAck();
            if (object.GameVolInfo != null) {
                if (typeof object.GameVolInfo !== "object")
                    throw TypeError(".jlsb.GameInfoAck.GameVolInfo: object expected");
                message.GameVolInfo = $root.jlsb.VolInfo.fromObject(object.GameVolInfo);
            }
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlsb.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {jlsb.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.GameVolInfo = null;
            if (message.GameVolInfo != null && message.hasOwnProperty("GameVolInfo"))
                object.GameVolInfo = $root.jlsb.VolInfo.toObject(message.GameVolInfo, options);
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlsb.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlsb.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsb.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jlsb.VolInfo = (function() {

        /**
         * Properties of a VolInfo.
         * @memberof jlsb
         * @interface IVolInfo
         * @property {number|null} [VolPos] VolInfo VolPos
         * @property {Array.<number>|null} [VolList] VolInfo VolList
         */

        /**
         * Constructs a new VolInfo.
         * @memberof jlsb
         * @classdesc Represents a VolInfo.
         * @implements IVolInfo
         * @constructor
         * @param {jlsb.IVolInfo=} [properties] Properties to set
         */
        function VolInfo(properties) {
            this.VolList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VolInfo VolPos.
         * @member {number} VolPos
         * @memberof jlsb.VolInfo
         * @instance
         */
        VolInfo.prototype.VolPos = 0;

        /**
         * VolInfo VolList.
         * @member {Array.<number>} VolList
         * @memberof jlsb.VolInfo
         * @instance
         */
        VolInfo.prototype.VolList = $util.emptyArray;

        /**
         * Creates a new VolInfo instance using the specified properties.
         * @function create
         * @memberof jlsb.VolInfo
         * @static
         * @param {jlsb.IVolInfo=} [properties] Properties to set
         * @returns {jlsb.VolInfo} VolInfo instance
         */
        VolInfo.create = function create(properties) {
            return new VolInfo(properties);
        };

        /**
         * Encodes the specified VolInfo message. Does not implicitly {@link jlsb.VolInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsb.VolInfo
         * @static
         * @param {jlsb.IVolInfo} message VolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VolInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.VolPos != null && Object.hasOwnProperty.call(message, "VolPos"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.VolPos);
            if (message.VolList != null && message.VolList.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.VolList.length; ++i)
                    writer.int32(message.VolList[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified VolInfo message, length delimited. Does not implicitly {@link jlsb.VolInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsb.VolInfo
         * @static
         * @param {jlsb.IVolInfo} message VolInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VolInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VolInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsb.VolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsb.VolInfo} VolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VolInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsb.VolInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.VolPos = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.VolList && message.VolList.length))
                            message.VolList = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.VolList.push(reader.int32());
                        } else
                            message.VolList.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VolInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsb.VolInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsb.VolInfo} VolInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VolInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VolInfo message.
         * @function verify
         * @memberof jlsb.VolInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VolInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.VolPos != null && message.hasOwnProperty("VolPos"))
                if (!$util.isInteger(message.VolPos))
                    return "VolPos: integer expected";
            if (message.VolList != null && message.hasOwnProperty("VolList")) {
                if (!Array.isArray(message.VolList))
                    return "VolList: array expected";
                for (let i = 0; i < message.VolList.length; ++i)
                    if (!$util.isInteger(message.VolList[i]))
                        return "VolList: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a VolInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsb.VolInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsb.VolInfo} VolInfo
         */
        VolInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsb.VolInfo)
                return object;
            let message = new $root.jlsb.VolInfo();
            if (object.VolPos != null)
                message.VolPos = object.VolPos | 0;
            if (object.VolList) {
                if (!Array.isArray(object.VolList))
                    throw TypeError(".jlsb.VolInfo.VolList: array expected");
                message.VolList = [];
                for (let i = 0; i < object.VolList.length; ++i)
                    message.VolList[i] = object.VolList[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a VolInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsb.VolInfo
         * @static
         * @param {jlsb.VolInfo} message VolInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VolInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.VolList = [];
            if (options.defaults)
                object.VolPos = 0;
            if (message.VolPos != null && message.hasOwnProperty("VolPos"))
                object.VolPos = message.VolPos;
            if (message.VolList && message.VolList.length) {
                object.VolList = [];
                for (let j = 0; j < message.VolList.length; ++j)
                    object.VolList[j] = message.VolList[j];
            }
            return object;
        };

        /**
         * Converts this VolInfo to JSON.
         * @function toJSON
         * @memberof jlsb.VolInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VolInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VolInfo
         * @function getTypeUrl
         * @memberof jlsb.VolInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VolInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsb.VolInfo";
        };

        return VolInfo;
    })();

    return jlsb;
})();

export const jlsc = $root.jlsc = (() => {

    /**
     * Namespace jlsc.
     * @exports jlsc
     * @namespace
     */
    const jlsc = {};

    jlsc.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlsc
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlsc
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlsc.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlsc.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlsc.Column
         * @static
         * @param {jlsc.IColumn=} [properties] Properties to set
         * @returns {jlsc.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlsc.Column.verify|verify} messages.
         * @function encode
         * @memberof jlsc.Column
         * @static
         * @param {jlsc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlsc.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsc.Column
         * @static
         * @param {jlsc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlsc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsc.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlsc.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsc.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsc.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsc.Column)
                return object;
            let message = new $root.jlsc.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlsc.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsc.Column
         * @static
         * @param {jlsc.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlsc.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlsc.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsc.Column";
        };

        return Column;
    })();

    jlsc.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlsc
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Direction] AwardData Direction
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlsc
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlsc.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlsc.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlsc.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlsc.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Direction.
         * @member {number} Direction
         * @memberof jlsc.AwardData
         * @instance
         */
        AwardData.prototype.Direction = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlsc.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlsc.AwardData
         * @static
         * @param {jlsc.IAwardData=} [properties] Properties to set
         * @returns {jlsc.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlsc.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlsc.AwardData
         * @static
         * @param {jlsc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Direction != null && Object.hasOwnProperty.call(message, "Direction"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.Direction);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlsc.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsc.AwardData
         * @static
         * @param {jlsc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsc.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Direction = reader.int32();
                        break;
                    }
                case 5: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlsc.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Direction != null && message.hasOwnProperty("Direction"))
                if (!$util.isInteger(message.Direction))
                    return "Direction: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsc.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsc.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsc.AwardData)
                return object;
            let message = new $root.jlsc.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Direction != null)
                message.Direction = object.Direction | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsc.AwardData
         * @static
         * @param {jlsc.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Direction = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Direction != null && message.hasOwnProperty("Direction"))
                object.Direction = message.Direction;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlsc.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlsc.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsc.AwardData";
        };

        return AwardData;
    })();

    jlsc.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlsc
         * @interface IRoundInfo
         * @property {Array.<jlsc.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlsc.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {Array.<number>|null} [Bonus] RoundInfo Bonus
         * @property {Array.<number>|null} [B1WildPos] RoundInfo B1WildPos
         * @property {number|null} [B2TransTarget] RoundInfo B2TransTarget
         * @property {Array.<number>|null} [B5MysPos] RoundInfo B5MysPos
         * @property {number|null} [B5MysTarget] RoundInfo B5MysTarget
         * @property {Array.<jlsc.IColumn>|null} [PlateSymbolLog] RoundInfo PlateSymbolLog
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlsc
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlsc.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.Bonus = [];
            this.B1WildPos = [];
            this.B5MysPos = [];
            this.PlateSymbolLog = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlsc.IColumn>} PlateSymbol
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlsc.IAwardData>} AwardDataVec
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo Bonus.
         * @member {Array.<number>} Bonus
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Bonus = $util.emptyArray;

        /**
         * RoundInfo B1WildPos.
         * @member {Array.<number>} B1WildPos
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.B1WildPos = $util.emptyArray;

        /**
         * RoundInfo B2TransTarget.
         * @member {number} B2TransTarget
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.B2TransTarget = 0;

        /**
         * RoundInfo B5MysPos.
         * @member {Array.<number>} B5MysPos
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.B5MysPos = $util.emptyArray;

        /**
         * RoundInfo B5MysTarget.
         * @member {number} B5MysTarget
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.B5MysTarget = 0;

        /**
         * RoundInfo PlateSymbolLog.
         * @member {Array.<jlsc.IColumn>} PlateSymbolLog
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlsc.RoundInfo
         * @static
         * @param {jlsc.IRoundInfo=} [properties] Properties to set
         * @returns {jlsc.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlsc.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsc.RoundInfo
         * @static
         * @param {jlsc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlsc.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlsc.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RoundWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.Mult);
            if (message.Bonus != null && message.Bonus.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.Bonus.length; ++i)
                    writer.int32(message.Bonus[i]);
                writer.ldelim();
            }
            if (message.B1WildPos != null && message.B1WildPos.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.B1WildPos.length; ++i)
                    writer.int32(message.B1WildPos[i]);
                writer.ldelim();
            }
            if (message.B2TransTarget != null && Object.hasOwnProperty.call(message, "B2TransTarget"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.B2TransTarget);
            if (message.B5MysPos != null && message.B5MysPos.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (let i = 0; i < message.B5MysPos.length; ++i)
                    writer.int32(message.B5MysPos[i]);
                writer.ldelim();
            }
            if (message.B5MysTarget != null && Object.hasOwnProperty.call(message, "B5MysTarget"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.B5MysTarget);
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlsc.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlsc.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsc.RoundInfo
         * @static
         * @param {jlsc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsc.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlsc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlsc.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.Mult = reader.double();
                        break;
                    }
                case 7: {
                        if (!(message.Bonus && message.Bonus.length))
                            message.Bonus = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Bonus.push(reader.int32());
                        } else
                            message.Bonus.push(reader.int32());
                        break;
                    }
                case 8: {
                        if (!(message.B1WildPos && message.B1WildPos.length))
                            message.B1WildPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.B1WildPos.push(reader.int32());
                        } else
                            message.B1WildPos.push(reader.int32());
                        break;
                    }
                case 9: {
                        message.B2TransTarget = reader.int32();
                        break;
                    }
                case 10: {
                        if (!(message.B5MysPos && message.B5MysPos.length))
                            message.B5MysPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.B5MysPos.push(reader.int32());
                        } else
                            message.B5MysPos.push(reader.int32());
                        break;
                    }
                case 11: {
                        message.B5MysTarget = reader.int32();
                        break;
                    }
                case 12: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlsc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 13: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlsc.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlsc.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlsc.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.Bonus != null && message.hasOwnProperty("Bonus")) {
                if (!Array.isArray(message.Bonus))
                    return "Bonus: array expected";
                for (let i = 0; i < message.Bonus.length; ++i)
                    if (!$util.isInteger(message.Bonus[i]))
                        return "Bonus: integer[] expected";
            }
            if (message.B1WildPos != null && message.hasOwnProperty("B1WildPos")) {
                if (!Array.isArray(message.B1WildPos))
                    return "B1WildPos: array expected";
                for (let i = 0; i < message.B1WildPos.length; ++i)
                    if (!$util.isInteger(message.B1WildPos[i]))
                        return "B1WildPos: integer[] expected";
            }
            if (message.B2TransTarget != null && message.hasOwnProperty("B2TransTarget"))
                if (!$util.isInteger(message.B2TransTarget))
                    return "B2TransTarget: integer expected";
            if (message.B5MysPos != null && message.hasOwnProperty("B5MysPos")) {
                if (!Array.isArray(message.B5MysPos))
                    return "B5MysPos: array expected";
                for (let i = 0; i < message.B5MysPos.length; ++i)
                    if (!$util.isInteger(message.B5MysPos[i]))
                        return "B5MysPos: integer[] expected";
            }
            if (message.B5MysTarget != null && message.hasOwnProperty("B5MysTarget"))
                if (!$util.isInteger(message.B5MysTarget))
                    return "B5MysTarget: integer expected";
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlsc.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsc.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsc.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsc.RoundInfo)
                return object;
            let message = new $root.jlsc.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlsc.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlsc.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlsc.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlsc.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlsc.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlsc.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.Bonus) {
                if (!Array.isArray(object.Bonus))
                    throw TypeError(".jlsc.RoundInfo.Bonus: array expected");
                message.Bonus = [];
                for (let i = 0; i < object.Bonus.length; ++i)
                    message.Bonus[i] = object.Bonus[i] | 0;
            }
            if (object.B1WildPos) {
                if (!Array.isArray(object.B1WildPos))
                    throw TypeError(".jlsc.RoundInfo.B1WildPos: array expected");
                message.B1WildPos = [];
                for (let i = 0; i < object.B1WildPos.length; ++i)
                    message.B1WildPos[i] = object.B1WildPos[i] | 0;
            }
            if (object.B2TransTarget != null)
                message.B2TransTarget = object.B2TransTarget | 0;
            if (object.B5MysPos) {
                if (!Array.isArray(object.B5MysPos))
                    throw TypeError(".jlsc.RoundInfo.B5MysPos: array expected");
                message.B5MysPos = [];
                for (let i = 0; i < object.B5MysPos.length; ++i)
                    message.B5MysPos[i] = object.B5MysPos[i] | 0;
            }
            if (object.B5MysTarget != null)
                message.B5MysTarget = object.B5MysTarget | 0;
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlsc.RoundInfo.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlsc.RoundInfo.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlsc.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsc.RoundInfo
         * @static
         * @param {jlsc.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.Bonus = [];
                object.B1WildPos = [];
                object.B5MysPos = [];
                object.PlateSymbolLog = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.RoundWin = 0;
                object.FreeRemainRound = 0;
                object.Mult = 0;
                object.B2TransTarget = 0;
                object.B5MysTarget = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlsc.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlsc.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.Bonus && message.Bonus.length) {
                object.Bonus = [];
                for (let j = 0; j < message.Bonus.length; ++j)
                    object.Bonus[j] = message.Bonus[j];
            }
            if (message.B1WildPos && message.B1WildPos.length) {
                object.B1WildPos = [];
                for (let j = 0; j < message.B1WildPos.length; ++j)
                    object.B1WildPos[j] = message.B1WildPos[j];
            }
            if (message.B2TransTarget != null && message.hasOwnProperty("B2TransTarget"))
                object.B2TransTarget = message.B2TransTarget;
            if (message.B5MysPos && message.B5MysPos.length) {
                object.B5MysPos = [];
                for (let j = 0; j < message.B5MysPos.length; ++j)
                    object.B5MysPos[j] = message.B5MysPos[j];
            }
            if (message.B5MysTarget != null && message.hasOwnProperty("B5MysTarget"))
                object.B5MysTarget = message.B5MysTarget;
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlsc.Column.toObject(message.PlateSymbolLog[j], options);
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlsc.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlsc.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsc.RoundInfo";
        };

        return RoundInfo;
    })();

    jlsc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsc
         * @interface ISpinAck
         * @property {Array.<jlsc.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlsc.IRoundInfo>} RoundQueue
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsc.SpinAck
         * @static
         * @param {jlsc.ISpinAck=} [properties] Properties to set
         * @returns {jlsc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsc.SpinAck
         * @static
         * @param {jlsc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlsc.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsc.SpinAck
         * @static
         * @param {jlsc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlsc.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlsc.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsc.SpinAck)
                return object;
            let message = new $root.jlsc.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlsc.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlsc.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlsc.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsc.SpinAck
         * @static
         * @param {jlsc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlsc.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsc.SpinAck";
        };

        return SpinAck;
    })();

    jlsc.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlsc
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlsc
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlsc.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlsc.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlsc.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlsc.GameInfoData
         * @static
         * @param {jlsc.IGameInfoData=} [properties] Properties to set
         * @returns {jlsc.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlsc.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlsc.GameInfoData
         * @static
         * @param {jlsc.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlsc.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsc.GameInfoData
         * @static
         * @param {jlsc.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsc.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsc.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsc.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsc.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsc.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlsc.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsc.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsc.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsc.GameInfoData)
                return object;
            let message = new $root.jlsc.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlsc.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsc.GameInfoData
         * @static
         * @param {jlsc.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlsc.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlsc.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsc.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlsc;
})();

export const jlsg = $root.jlsg = (() => {

    /**
     * Namespace jlsg.
     * @exports jlsg
     * @namespace
     */
    const jlsg = {};

    jlsg.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlsg
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlsg
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlsg.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlsg.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlsg.Column
         * @static
         * @param {jlsg.IColumn=} [properties] Properties to set
         * @returns {jlsg.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlsg.Column.verify|verify} messages.
         * @function encode
         * @memberof jlsg.Column
         * @static
         * @param {jlsg.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlsg.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsg.Column
         * @static
         * @param {jlsg.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlsg.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsg.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsg.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsg.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsg.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlsg.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsg.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsg.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsg.Column)
                return object;
            let message = new $root.jlsg.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlsg.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsg.Column
         * @static
         * @param {jlsg.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlsg.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlsg.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsg.Column";
        };

        return Column;
    })();

    jlsg.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlsg
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Connect] AwardData Connect
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlsg
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlsg.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlsg.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlsg.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Connect.
         * @member {number} Connect
         * @memberof jlsg.AwardData
         * @instance
         */
        AwardData.prototype.Connect = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlsg.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlsg.AwardData
         * @static
         * @param {jlsg.IAwardData=} [properties] Properties to set
         * @returns {jlsg.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlsg.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlsg.AwardData
         * @static
         * @param {jlsg.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Connect != null && Object.hasOwnProperty.call(message, "Connect"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Connect);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlsg.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsg.AwardData
         * @static
         * @param {jlsg.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsg.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsg.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsg.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Connect = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsg.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsg.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlsg.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Connect != null && message.hasOwnProperty("Connect"))
                if (!$util.isInteger(message.Connect))
                    return "Connect: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsg.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsg.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsg.AwardData)
                return object;
            let message = new $root.jlsg.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Connect != null)
                message.Connect = object.Connect | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsg.AwardData
         * @static
         * @param {jlsg.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Connect = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Connect != null && message.hasOwnProperty("Connect"))
                object.Connect = message.Connect;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlsg.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlsg.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsg.AwardData";
        };

        return AwardData;
    })();

    jlsg.DisplayAwardVec = (function() {

        /**
         * Properties of a DisplayAwardVec.
         * @memberof jlsg
         * @interface IDisplayAwardVec
         * @property {Array.<jlsg.IAwardData>|null} [AwardDataVec] DisplayAwardVec AwardDataVec
         */

        /**
         * Constructs a new DisplayAwardVec.
         * @memberof jlsg
         * @classdesc Represents a DisplayAwardVec.
         * @implements IDisplayAwardVec
         * @constructor
         * @param {jlsg.IDisplayAwardVec=} [properties] Properties to set
         */
        function DisplayAwardVec(properties) {
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DisplayAwardVec AwardDataVec.
         * @member {Array.<jlsg.IAwardData>} AwardDataVec
         * @memberof jlsg.DisplayAwardVec
         * @instance
         */
        DisplayAwardVec.prototype.AwardDataVec = $util.emptyArray;

        /**
         * Creates a new DisplayAwardVec instance using the specified properties.
         * @function create
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {jlsg.IDisplayAwardVec=} [properties] Properties to set
         * @returns {jlsg.DisplayAwardVec} DisplayAwardVec instance
         */
        DisplayAwardVec.create = function create(properties) {
            return new DisplayAwardVec(properties);
        };

        /**
         * Encodes the specified DisplayAwardVec message. Does not implicitly {@link jlsg.DisplayAwardVec.verify|verify} messages.
         * @function encode
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {jlsg.IDisplayAwardVec} message DisplayAwardVec message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisplayAwardVec.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlsg.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified DisplayAwardVec message, length delimited. Does not implicitly {@link jlsg.DisplayAwardVec.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {jlsg.IDisplayAwardVec} message DisplayAwardVec message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisplayAwardVec.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DisplayAwardVec message from the specified reader or buffer.
         * @function decode
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsg.DisplayAwardVec} DisplayAwardVec
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisplayAwardVec.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsg.DisplayAwardVec();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlsg.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DisplayAwardVec message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsg.DisplayAwardVec} DisplayAwardVec
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisplayAwardVec.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisplayAwardVec message.
         * @function verify
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisplayAwardVec.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlsg.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DisplayAwardVec message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsg.DisplayAwardVec} DisplayAwardVec
         */
        DisplayAwardVec.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsg.DisplayAwardVec)
                return object;
            let message = new $root.jlsg.DisplayAwardVec();
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlsg.DisplayAwardVec.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlsg.DisplayAwardVec.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlsg.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a DisplayAwardVec message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {jlsg.DisplayAwardVec} message DisplayAwardVec
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisplayAwardVec.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AwardDataVec = [];
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlsg.AwardData.toObject(message.AwardDataVec[j], options);
            }
            return object;
        };

        /**
         * Converts this DisplayAwardVec to JSON.
         * @function toJSON
         * @memberof jlsg.DisplayAwardVec
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisplayAwardVec.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DisplayAwardVec
         * @function getTypeUrl
         * @memberof jlsg.DisplayAwardVec
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DisplayAwardVec.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsg.DisplayAwardVec";
        };

        return DisplayAwardVec;
    })();

    jlsg.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlsg
         * @interface IRoundInfo
         * @property {Array.<jlsg.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlsg.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {Array.<jlsg.IColumn>|null} [PlateSymbolLog] RoundInfo PlateSymbolLog
         * @property {Array.<number>|null} [DisplayWin] RoundInfo DisplayWin
         * @property {Array.<jlsg.IDisplayAwardVec>|null} [DisplayAward] RoundInfo DisplayAward
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlsg
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlsg.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.AwardDataVec = [];
            this.PlateSymbolLog = [];
            this.DisplayWin = [];
            this.DisplayAward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlsg.IColumn>} PlateSymbol
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlsg.IAwardData>} AwardDataVec
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo PlateSymbolLog.
         * @member {Array.<jlsg.IColumn>} PlateSymbolLog
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolLog = $util.emptyArray;

        /**
         * RoundInfo DisplayWin.
         * @member {Array.<number>} DisplayWin
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.DisplayWin = $util.emptyArray;

        /**
         * RoundInfo DisplayAward.
         * @member {Array.<jlsg.IDisplayAwardVec>} DisplayAward
         * @memberof jlsg.RoundInfo
         * @instance
         */
        RoundInfo.prototype.DisplayAward = $util.emptyArray;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlsg.RoundInfo
         * @static
         * @param {jlsg.IRoundInfo=} [properties] Properties to set
         * @returns {jlsg.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlsg.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsg.RoundInfo
         * @static
         * @param {jlsg.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlsg.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlsg.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeTotalRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AddRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            if (message.PlateSymbolLog != null && message.PlateSymbolLog.length)
                for (let i = 0; i < message.PlateSymbolLog.length; ++i)
                    $root.jlsg.Column.encode(message.PlateSymbolLog[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.DisplayWin != null && message.DisplayWin.length) {
                writer.uint32(/* id 11, wireType 2 =*/90).fork();
                for (let i = 0; i < message.DisplayWin.length; ++i)
                    writer.double(message.DisplayWin[i]);
                writer.ldelim();
            }
            if (message.DisplayAward != null && message.DisplayAward.length)
                for (let i = 0; i < message.DisplayAward.length; ++i)
                    $root.jlsg.DisplayAwardVec.encode(message.DisplayAward[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlsg.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsg.RoundInfo
         * @static
         * @param {jlsg.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsg.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsg.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsg.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlsg.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlsg.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 7: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 10: {
                        if (!(message.PlateSymbolLog && message.PlateSymbolLog.length))
                            message.PlateSymbolLog = [];
                        message.PlateSymbolLog.push($root.jlsg.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        if (!(message.DisplayWin && message.DisplayWin.length))
                            message.DisplayWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.DisplayWin.push(reader.double());
                        } else
                            message.DisplayWin.push(reader.double());
                        break;
                    }
                case 12: {
                        if (!(message.DisplayAward && message.DisplayAward.length))
                            message.DisplayAward = [];
                        message.DisplayAward.push($root.jlsg.DisplayAwardVec.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsg.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsg.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlsg.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlsg.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlsg.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.PlateSymbolLog != null && message.hasOwnProperty("PlateSymbolLog")) {
                if (!Array.isArray(message.PlateSymbolLog))
                    return "PlateSymbolLog: array expected";
                for (let i = 0; i < message.PlateSymbolLog.length; ++i) {
                    let error = $root.jlsg.Column.verify(message.PlateSymbolLog[i]);
                    if (error)
                        return "PlateSymbolLog." + error;
                }
            }
            if (message.DisplayWin != null && message.hasOwnProperty("DisplayWin")) {
                if (!Array.isArray(message.DisplayWin))
                    return "DisplayWin: array expected";
                for (let i = 0; i < message.DisplayWin.length; ++i)
                    if (typeof message.DisplayWin[i] !== "number")
                        return "DisplayWin: number[] expected";
            }
            if (message.DisplayAward != null && message.hasOwnProperty("DisplayAward")) {
                if (!Array.isArray(message.DisplayAward))
                    return "DisplayAward: array expected";
                for (let i = 0; i < message.DisplayAward.length; ++i) {
                    let error = $root.jlsg.DisplayAwardVec.verify(message.DisplayAward[i]);
                    if (error)
                        return "DisplayAward." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsg.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsg.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsg.RoundInfo)
                return object;
            let message = new $root.jlsg.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlsg.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlsg.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlsg.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlsg.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlsg.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlsg.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.PlateSymbolLog) {
                if (!Array.isArray(object.PlateSymbolLog))
                    throw TypeError(".jlsg.RoundInfo.PlateSymbolLog: array expected");
                message.PlateSymbolLog = [];
                for (let i = 0; i < object.PlateSymbolLog.length; ++i) {
                    if (typeof object.PlateSymbolLog[i] !== "object")
                        throw TypeError(".jlsg.RoundInfo.PlateSymbolLog: object expected");
                    message.PlateSymbolLog[i] = $root.jlsg.Column.fromObject(object.PlateSymbolLog[i]);
                }
            }
            if (object.DisplayWin) {
                if (!Array.isArray(object.DisplayWin))
                    throw TypeError(".jlsg.RoundInfo.DisplayWin: array expected");
                message.DisplayWin = [];
                for (let i = 0; i < object.DisplayWin.length; ++i)
                    message.DisplayWin[i] = Number(object.DisplayWin[i]);
            }
            if (object.DisplayAward) {
                if (!Array.isArray(object.DisplayAward))
                    throw TypeError(".jlsg.RoundInfo.DisplayAward: array expected");
                message.DisplayAward = [];
                for (let i = 0; i < object.DisplayAward.length; ++i) {
                    if (typeof object.DisplayAward[i] !== "object")
                        throw TypeError(".jlsg.RoundInfo.DisplayAward: object expected");
                    message.DisplayAward[i] = $root.jlsg.DisplayAwardVec.fromObject(object.DisplayAward[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsg.RoundInfo
         * @static
         * @param {jlsg.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.AwardDataVec = [];
                object.PlateSymbolLog = [];
                object.DisplayWin = [];
                object.DisplayAward = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.AddRound = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlsg.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlsg.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.PlateSymbolLog && message.PlateSymbolLog.length) {
                object.PlateSymbolLog = [];
                for (let j = 0; j < message.PlateSymbolLog.length; ++j)
                    object.PlateSymbolLog[j] = $root.jlsg.Column.toObject(message.PlateSymbolLog[j], options);
            }
            if (message.DisplayWin && message.DisplayWin.length) {
                object.DisplayWin = [];
                for (let j = 0; j < message.DisplayWin.length; ++j)
                    object.DisplayWin[j] = options.json && !isFinite(message.DisplayWin[j]) ? String(message.DisplayWin[j]) : message.DisplayWin[j];
            }
            if (message.DisplayAward && message.DisplayAward.length) {
                object.DisplayAward = [];
                for (let j = 0; j < message.DisplayAward.length; ++j)
                    object.DisplayAward[j] = $root.jlsg.DisplayAwardVec.toObject(message.DisplayAward[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlsg.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlsg.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsg.RoundInfo";
        };

        return RoundInfo;
    })();

    jlsg.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsg
         * @interface ISpinAck
         * @property {Array.<jlsg.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsg
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsg.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlsg.IRoundInfo>} RoundQueue
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsg.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsg.SpinAck
         * @static
         * @param {jlsg.ISpinAck=} [properties] Properties to set
         * @returns {jlsg.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsg.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsg.SpinAck
         * @static
         * @param {jlsg.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlsg.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Bet);
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsg.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsg.SpinAck
         * @static
         * @param {jlsg.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsg.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsg.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsg.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlsg.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Bet = reader.double();
                        break;
                    }
                case 3: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsg.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsg.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsg.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlsg.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsg.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsg.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsg.SpinAck)
                return object;
            let message = new $root.jlsg.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlsg.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlsg.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlsg.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsg.SpinAck
         * @static
         * @param {jlsg.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.Bet = 0;
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlsg.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsg.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsg.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsg.SpinAck";
        };

        return SpinAck;
    })();

    jlsg.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlsg
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {number|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlsg
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlsg.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlsg.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {number} Mul
         * @memberof jlsg.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = 0;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlsg.GameInfoData
         * @static
         * @param {jlsg.IGameInfoData=} [properties] Properties to set
         * @returns {jlsg.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlsg.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlsg.GameInfoData
         * @static
         * @param {jlsg.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && Object.hasOwnProperty.call(message, "Mul"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.Mul);
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlsg.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsg.GameInfoData
         * @static
         * @param {jlsg.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsg.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsg.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsg.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        message.Mul = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsg.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsg.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlsg.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                if (typeof message.Mul !== "number")
                    return "Mul: number expected";
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsg.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsg.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsg.GameInfoData)
                return object;
            let message = new $root.jlsg.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul != null)
                message.Mul = Number(object.Mul);
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsg.GameInfoData
         * @static
         * @param {jlsg.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ShowExtra = false;
                object.Mul = 0;
            }
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul != null && message.hasOwnProperty("Mul"))
                object.Mul = options.json && !isFinite(message.Mul) ? String(message.Mul) : message.Mul;
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlsg.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlsg.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsg.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlsg;
})();

export const jlshb = $root.jlshb = (() => {

    /**
     * Namespace jlshb.
     * @exports jlshb
     * @namespace
     */
    const jlshb = {};

    /**
     * Symbol enum.
     * @name jlshb.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     */
    jlshb.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        return values;
    })();

    jlshb.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlshb
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlshb
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlshb.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlshb.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlshb.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlshb.Block
         * @static
         * @param {jlshb.IBlock=} [properties] Properties to set
         * @returns {jlshb.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlshb.Block.verify|verify} messages.
         * @function encode
         * @memberof jlshb.Block
         * @static
         * @param {jlshb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlshb.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlshb.Block
         * @static
         * @param {jlshb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlshb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlshb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlshb.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlshb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlshb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlshb.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlshb.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlshb.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlshb.Block)
                return object;
            let message = new $root.jlshb.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlshb.Block
         * @static
         * @param {jlshb.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlshb.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlshb.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlshb.Block";
        };

        return Block;
    })();

    jlshb.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlshb
         * @interface IPlate
         * @property {Array.<jlshb.IColumnData>|null} [column] Plate column
         * @property {Array.<jlshb.Plate.IAwardReel>|null} [award] Plate award
         * @property {number|null} [win] Plate win
         * @property {number|null} [ballCount] Plate ballCount
         * @property {number|null} [bonusWin] Plate bonusWin
         */

        /**
         * Constructs a new Plate.
         * @memberof jlshb
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlshb.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.award = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlshb.IColumnData>} column
         * @memberof jlshb.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate award.
         * @member {Array.<jlshb.Plate.IAwardReel>} award
         * @memberof jlshb.Plate
         * @instance
         */
        Plate.prototype.award = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlshb.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate ballCount.
         * @member {number} ballCount
         * @memberof jlshb.Plate
         * @instance
         */
        Plate.prototype.ballCount = 0;

        /**
         * Plate bonusWin.
         * @member {number} bonusWin
         * @memberof jlshb.Plate
         * @instance
         */
        Plate.prototype.bonusWin = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlshb.Plate
         * @static
         * @param {jlshb.IPlate=} [properties] Properties to set
         * @returns {jlshb.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlshb.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlshb.Plate
         * @static
         * @param {jlshb.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlshb.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.award != null && message.award.length)
                for (let i = 0; i < message.award.length; ++i)
                    $root.jlshb.Plate.AwardReel.encode(message.award[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
            if (message.ballCount != null && Object.hasOwnProperty.call(message, "ballCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.ballCount);
            if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlshb.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlshb.Plate
         * @static
         * @param {jlshb.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlshb.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlshb.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlshb.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlshb.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.award && message.award.length))
                            message.award = [];
                        message.award.push($root.jlshb.Plate.AwardReel.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.win = reader.double();
                        break;
                    }
                case 4: {
                        message.ballCount = reader.uint32();
                        break;
                    }
                case 5: {
                        message.bonusWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlshb.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlshb.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlshb.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlshb.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.award != null && message.hasOwnProperty("award")) {
                if (!Array.isArray(message.award))
                    return "award: array expected";
                for (let i = 0; i < message.award.length; ++i) {
                    let error = $root.jlshb.Plate.AwardReel.verify(message.award[i]);
                    if (error)
                        return "award." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                if (!$util.isInteger(message.ballCount))
                    return "ballCount: integer expected";
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                if (typeof message.bonusWin !== "number")
                    return "bonusWin: number expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlshb.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlshb.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlshb.Plate)
                return object;
            let message = new $root.jlshb.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlshb.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlshb.Plate.column: object expected");
                    message.column[i] = $root.jlshb.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.award) {
                if (!Array.isArray(object.award))
                    throw TypeError(".jlshb.Plate.award: array expected");
                message.award = [];
                for (let i = 0; i < object.award.length; ++i) {
                    if (typeof object.award[i] !== "object")
                        throw TypeError(".jlshb.Plate.award: object expected");
                    message.award[i] = $root.jlshb.Plate.AwardReel.fromObject(object.award[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.ballCount != null)
                message.ballCount = object.ballCount >>> 0;
            if (object.bonusWin != null)
                message.bonusWin = Number(object.bonusWin);
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlshb.Plate
         * @static
         * @param {jlshb.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.award = [];
            }
            if (options.defaults) {
                object.win = 0;
                object.ballCount = 0;
                object.bonusWin = 0;
            }
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlshb.ColumnData.toObject(message.column[j], options);
            }
            if (message.award && message.award.length) {
                object.award = [];
                for (let j = 0; j < message.award.length; ++j)
                    object.award[j] = $root.jlshb.Plate.AwardReel.toObject(message.award[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                object.ballCount = message.ballCount;
            if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlshb.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlshb.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlshb.Plate";
        };

        Plate.AwardReel = (function() {

            /**
             * Properties of an AwardReel.
             * @memberof jlshb.Plate
             * @interface IAwardReel
             * @property {number|null} [line] AwardReel line
             * @property {jlshb.IBlock|null} [block] AwardReel block
             * @property {number|null} [win] AwardReel win
             * @property {number|null} [ballCount] AwardReel ballCount
             * @property {number|null} [bonusWin] AwardReel bonusWin
             */

            /**
             * Constructs a new AwardReel.
             * @memberof jlshb.Plate
             * @classdesc Represents an AwardReel.
             * @implements IAwardReel
             * @constructor
             * @param {jlshb.Plate.IAwardReel=} [properties] Properties to set
             */
            function AwardReel(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null)
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AwardReel line.
             * @member {number} line
             * @memberof jlshb.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.line = 0;

            /**
             * AwardReel block.
             * @member {jlshb.IBlock|null|undefined} block
             * @memberof jlshb.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.block = null;

            /**
             * AwardReel win.
             * @member {number} win
             * @memberof jlshb.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.win = 0;

            /**
             * AwardReel ballCount.
             * @member {number} ballCount
             * @memberof jlshb.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.ballCount = 0;

            /**
             * AwardReel bonusWin.
             * @member {number} bonusWin
             * @memberof jlshb.Plate.AwardReel
             * @instance
             */
            AwardReel.prototype.bonusWin = 0;

            /**
             * Creates a new AwardReel instance using the specified properties.
             * @function create
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {jlshb.Plate.IAwardReel=} [properties] Properties to set
             * @returns {jlshb.Plate.AwardReel} AwardReel instance
             */
            AwardReel.create = function create(properties) {
                return new AwardReel(properties);
            };

            /**
             * Encodes the specified AwardReel message. Does not implicitly {@link jlshb.Plate.AwardReel.verify|verify} messages.
             * @function encode
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {jlshb.Plate.IAwardReel} message AwardReel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AwardReel.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.line);
                if (message.block != null && Object.hasOwnProperty.call(message, "block"))
                    $root.jlshb.Block.encode(message.block, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
                if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.win);
                if (message.ballCount != null && Object.hasOwnProperty.call(message, "ballCount"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.ballCount);
                if (message.bonusWin != null && Object.hasOwnProperty.call(message, "bonusWin"))
                    writer.uint32(/* id 5, wireType 1 =*/41).double(message.bonusWin);
                return writer;
            };

            /**
             * Encodes the specified AwardReel message, length delimited. Does not implicitly {@link jlshb.Plate.AwardReel.verify|verify} messages.
             * @function encodeDelimited
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {jlshb.Plate.IAwardReel} message AwardReel message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AwardReel.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            /**
             * Decodes an AwardReel message from the specified reader or buffer.
             * @function decode
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {jlshb.Plate.AwardReel} AwardReel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AwardReel.decode = function decode(reader, length, error) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlshb.Plate.AwardReel();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.line = reader.uint32();
                            break;
                        }
                    case 2: {
                            message.block = $root.jlshb.Block.decode(reader, reader.uint32());
                            break;
                        }
                    case 3: {
                            message.win = reader.double();
                            break;
                        }
                    case 4: {
                            message.ballCount = reader.uint32();
                            break;
                        }
                    case 5: {
                            message.bonusWin = reader.double();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AwardReel message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {jlshb.Plate.AwardReel} AwardReel
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AwardReel.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AwardReel message.
             * @function verify
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AwardReel.verify = function verify(message) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (message.line != null && message.hasOwnProperty("line"))
                    if (!$util.isInteger(message.line))
                        return "line: integer expected";
                if (message.block != null && message.hasOwnProperty("block")) {
                    let error = $root.jlshb.Block.verify(message.block);
                    if (error)
                        return "block." + error;
                }
                if (message.win != null && message.hasOwnProperty("win"))
                    if (typeof message.win !== "number")
                        return "win: number expected";
                if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                    if (!$util.isInteger(message.ballCount))
                        return "ballCount: integer expected";
                if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                    if (typeof message.bonusWin !== "number")
                        return "bonusWin: number expected";
                return null;
            };

            /**
             * Creates an AwardReel message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {jlshb.Plate.AwardReel} AwardReel
             */
            AwardReel.fromObject = function fromObject(object) {
                if (object instanceof $root.jlshb.Plate.AwardReel)
                    return object;
                let message = new $root.jlshb.Plate.AwardReel();
                if (object.line != null)
                    message.line = object.line >>> 0;
                if (object.block != null) {
                    if (typeof object.block !== "object")
                        throw TypeError(".jlshb.Plate.AwardReel.block: object expected");
                    message.block = $root.jlshb.Block.fromObject(object.block);
                }
                if (object.win != null)
                    message.win = Number(object.win);
                if (object.ballCount != null)
                    message.ballCount = object.ballCount >>> 0;
                if (object.bonusWin != null)
                    message.bonusWin = Number(object.bonusWin);
                return message;
            };

            /**
             * Creates a plain object from an AwardReel message. Also converts values to other types if specified.
             * @function toObject
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {jlshb.Plate.AwardReel} message AwardReel
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AwardReel.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.line = 0;
                    object.block = null;
                    object.win = 0;
                    object.ballCount = 0;
                    object.bonusWin = 0;
                }
                if (message.line != null && message.hasOwnProperty("line"))
                    object.line = message.line;
                if (message.block != null && message.hasOwnProperty("block"))
                    object.block = $root.jlshb.Block.toObject(message.block, options);
                if (message.win != null && message.hasOwnProperty("win"))
                    object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
                if (message.ballCount != null && message.hasOwnProperty("ballCount"))
                    object.ballCount = message.ballCount;
                if (message.bonusWin != null && message.hasOwnProperty("bonusWin"))
                    object.bonusWin = options.json && !isFinite(message.bonusWin) ? String(message.bonusWin) : message.bonusWin;
                return object;
            };

            /**
             * Converts this AwardReel to JSON.
             * @function toJSON
             * @memberof jlshb.Plate.AwardReel
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AwardReel.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AwardReel
             * @function getTypeUrl
             * @memberof jlshb.Plate.AwardReel
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AwardReel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/jlshb.Plate.AwardReel";
            };

            return AwardReel;
        })();

        return Plate;
    })();

    jlshb.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlshb
         * @interface IAllPlate
         * @property {Array.<jlshb.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlshb
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlshb.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlshb.IPlate>} plate
         * @memberof jlshb.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlshb.AllPlate
         * @static
         * @param {jlshb.IAllPlate=} [properties] Properties to set
         * @returns {jlshb.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlshb.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlshb.AllPlate
         * @static
         * @param {jlshb.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlshb.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlshb.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlshb.AllPlate
         * @static
         * @param {jlshb.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlshb.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlshb.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlshb.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlshb.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlshb.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlshb.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlshb.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlshb.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlshb.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlshb.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlshb.AllPlate)
                return object;
            let message = new $root.jlshb.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlshb.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlshb.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlshb.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlshb.AllPlate
         * @static
         * @param {jlshb.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlshb.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlshb.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlshb.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlshb.AllPlate";
        };

        return AllPlate;
    })();

    jlshb.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlshb
         * @interface IColumnData
         * @property {Array.<number>|null} [row] ColumnData row
         * @property {Array.<number>|null} [bonus] ColumnData bonus
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlshb
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlshb.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            this.bonus = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<number>} row
         * @memberof jlshb.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * ColumnData bonus.
         * @member {Array.<number>} bonus
         * @memberof jlshb.ColumnData
         * @instance
         */
        ColumnData.prototype.bonus = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlshb.ColumnData
         * @static
         * @param {jlshb.IColumnData=} [properties] Properties to set
         * @returns {jlshb.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlshb.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlshb.ColumnData
         * @static
         * @param {jlshb.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.row.length; ++i)
                    writer.int32(message.row[i]);
                writer.ldelim();
            }
            if (message.bonus != null && message.bonus.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.bonus.length; ++i)
                    writer.double(message.bonus[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlshb.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlshb.ColumnData
         * @static
         * @param {jlshb.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlshb.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlshb.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlshb.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.row.push(reader.int32());
                        } else
                            message.row.push(reader.int32());
                        break;
                    }
                case 2: {
                        if (!(message.bonus && message.bonus.length))
                            message.bonus = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.bonus.push(reader.double());
                        } else
                            message.bonus.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlshb.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlshb.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlshb.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i)
                    if (!$util.isInteger(message.row[i]))
                        return "row: integer[] expected";
            }
            if (message.bonus != null && message.hasOwnProperty("bonus")) {
                if (!Array.isArray(message.bonus))
                    return "bonus: array expected";
                for (let i = 0; i < message.bonus.length; ++i)
                    if (typeof message.bonus[i] !== "number")
                        return "bonus: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlshb.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlshb.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlshb.ColumnData)
                return object;
            let message = new $root.jlshb.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlshb.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i)
                    message.row[i] = object.row[i] | 0;
            }
            if (object.bonus) {
                if (!Array.isArray(object.bonus))
                    throw TypeError(".jlshb.ColumnData.bonus: array expected");
                message.bonus = [];
                for (let i = 0; i < object.bonus.length; ++i)
                    message.bonus[i] = Number(object.bonus[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlshb.ColumnData
         * @static
         * @param {jlshb.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.row = [];
                object.bonus = [];
            }
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = message.row[j];
            }
            if (message.bonus && message.bonus.length) {
                object.bonus = [];
                for (let j = 0; j < message.bonus.length; ++j)
                    object.bonus[j] = options.json && !isFinite(message.bonus[j]) ? String(message.bonus[j]) : message.bonus[j];
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlshb.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlshb.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlshb.ColumnData";
        };

        return ColumnData;
    })();

    return jlshb;
})();

export const jlsl = $root.jlsl = (() => {

    /**
     * Namespace jlsl.
     * @exports jlsl
     * @namespace
     */
    const jlsl = {};

    jlsl.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlsl
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlsl
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlsl.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlsl.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlsl.Column
         * @static
         * @param {jlsl.IColumn=} [properties] Properties to set
         * @returns {jlsl.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlsl.Column.verify|verify} messages.
         * @function encode
         * @memberof jlsl.Column
         * @static
         * @param {jlsl.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlsl.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.Column
         * @static
         * @param {jlsl.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlsl.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.Column)
                return object;
            let message = new $root.jlsl.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlsl.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.Column
         * @static
         * @param {jlsl.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlsl.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlsl.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.Column";
        };

        return Column;
    })();

    jlsl.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlsl
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlsl
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlsl.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlsl.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {jlsl.IColumnFloat=} [properties] Properties to set
         * @returns {jlsl.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlsl.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {jlsl.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlsl.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {jlsl.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.ColumnFloat)
                return object;
            let message = new $root.jlsl.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlsl.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {jlsl.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlsl.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlsl.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlsl.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlsl
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlsl
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlsl.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlsl.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlsl.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlsl.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlsl.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlsl.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlsl.AwardData
         * @static
         * @param {jlsl.IAwardData=} [properties] Properties to set
         * @returns {jlsl.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlsl.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlsl.AwardData
         * @static
         * @param {jlsl.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Mult);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlsl.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.AwardData
         * @static
         * @param {jlsl.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.Mult = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlsl.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.AwardData)
                return object;
            let message = new $root.jlsl.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlsl.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.AwardData
         * @static
         * @param {jlsl.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlsl.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlsl.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.AwardData";
        };

        return AwardData;
    })();

    jlsl.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlsl
         * @interface IComboStageInfo
         * @property {Array.<jlsl.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {Array.<jlsl.IColumnFloat>|null} [ComboStageMult] ComboStageInfo ComboStageMult
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlsl.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlsl.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {number|null} [WildPos] ComboStageInfo WildPos
         * @property {Array.<number>|null} [BombWildPos] ComboStageInfo BombWildPos
         * @property {Array.<number>|null} [BombedPos] ComboStageInfo BombedPos
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlsl
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlsl.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.ComboStageMult = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.BombWildPos = [];
            this.BombedPos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlsl.IColumn>} ComboStageSymbol
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageMult.
         * @member {Array.<jlsl.IColumnFloat>} ComboStageMult
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageMult = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlsl.IAwardData>} AwardDataVec
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlsl.IColumn>} FillSymbols
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo WildPos.
         * @member {number} WildPos
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.WildPos = 0;

        /**
         * ComboStageInfo BombWildPos.
         * @member {Array.<number>} BombWildPos
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.BombWildPos = $util.emptyArray;

        /**
         * ComboStageInfo BombedPos.
         * @member {Array.<number>} BombedPos
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.BombedPos = $util.emptyArray;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsl.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {jlsl.IComboStageInfo=} [properties] Properties to set
         * @returns {jlsl.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlsl.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {jlsl.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlsl.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageMult != null && message.ComboStageMult.length)
                for (let i = 0; i < message.ComboStageMult.length; ++i)
                    $root.jlsl.ColumnFloat.encode(message.ComboStageMult[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlsl.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlsl.Column.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.WildPos != null && Object.hasOwnProperty.call(message, "WildPos"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.WildPos);
            if (message.BombWildPos != null && message.BombWildPos.length) {
                writer.uint32(/* id 7, wireType 2 =*/58).fork();
                for (let i = 0; i < message.BombWildPos.length; ++i)
                    writer.int32(message.BombWildPos[i]);
                writer.ldelim();
            }
            if (message.BombedPos != null && message.BombedPos.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.BombedPos.length; ++i)
                    writer.int32(message.BombedPos[i]);
                writer.ldelim();
            }
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlsl.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {jlsl.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlsl.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.ComboStageMult && message.ComboStageMult.length))
                            message.ComboStageMult = [];
                        message.ComboStageMult.push($root.jlsl.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlsl.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlsl.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        message.WildPos = reader.int32();
                        break;
                    }
                case 7: {
                        if (!(message.BombWildPos && message.BombWildPos.length))
                            message.BombWildPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BombWildPos.push(reader.int32());
                        } else
                            message.BombWildPos.push(reader.int32());
                        break;
                    }
                case 8: {
                        if (!(message.BombedPos && message.BombedPos.length))
                            message.BombedPos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BombedPos.push(reader.int32());
                        } else
                            message.BombedPos.push(reader.int32());
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlsl.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageMult != null && message.hasOwnProperty("ComboStageMult")) {
                if (!Array.isArray(message.ComboStageMult))
                    return "ComboStageMult: array expected";
                for (let i = 0; i < message.ComboStageMult.length; ++i) {
                    let error = $root.jlsl.ColumnFloat.verify(message.ComboStageMult[i]);
                    if (error)
                        return "ComboStageMult." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlsl.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlsl.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.WildPos != null && message.hasOwnProperty("WildPos"))
                if (!$util.isInteger(message.WildPos))
                    return "WildPos: integer expected";
            if (message.BombWildPos != null && message.hasOwnProperty("BombWildPos")) {
                if (!Array.isArray(message.BombWildPos))
                    return "BombWildPos: array expected";
                for (let i = 0; i < message.BombWildPos.length; ++i)
                    if (!$util.isInteger(message.BombWildPos[i]))
                        return "BombWildPos: integer[] expected";
            }
            if (message.BombedPos != null && message.hasOwnProperty("BombedPos")) {
                if (!Array.isArray(message.BombedPos))
                    return "BombedPos: array expected";
                for (let i = 0; i < message.BombedPos.length; ++i)
                    if (!$util.isInteger(message.BombedPos[i]))
                        return "BombedPos: integer[] expected";
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.ComboStageInfo)
                return object;
            let message = new $root.jlsl.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlsl.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlsl.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlsl.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageMult) {
                if (!Array.isArray(object.ComboStageMult))
                    throw TypeError(".jlsl.ComboStageInfo.ComboStageMult: array expected");
                message.ComboStageMult = [];
                for (let i = 0; i < object.ComboStageMult.length; ++i) {
                    if (typeof object.ComboStageMult[i] !== "object")
                        throw TypeError(".jlsl.ComboStageInfo.ComboStageMult: object expected");
                    message.ComboStageMult[i] = $root.jlsl.ColumnFloat.fromObject(object.ComboStageMult[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlsl.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlsl.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlsl.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlsl.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlsl.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlsl.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.WildPos != null)
                message.WildPos = object.WildPos | 0;
            if (object.BombWildPos) {
                if (!Array.isArray(object.BombWildPos))
                    throw TypeError(".jlsl.ComboStageInfo.BombWildPos: array expected");
                message.BombWildPos = [];
                for (let i = 0; i < object.BombWildPos.length; ++i)
                    message.BombWildPos[i] = object.BombWildPos[i] | 0;
            }
            if (object.BombedPos) {
                if (!Array.isArray(object.BombedPos))
                    throw TypeError(".jlsl.ComboStageInfo.BombedPos: array expected");
                message.BombedPos = [];
                for (let i = 0; i < object.BombedPos.length; ++i)
                    message.BombedPos[i] = object.BombedPos[i] | 0;
            }
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {jlsl.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.ComboStageMult = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.BombWildPos = [];
                object.BombedPos = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.WildPos = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlsl.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageMult && message.ComboStageMult.length) {
                object.ComboStageMult = [];
                for (let j = 0; j < message.ComboStageMult.length; ++j)
                    object.ComboStageMult[j] = $root.jlsl.ColumnFloat.toObject(message.ComboStageMult[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlsl.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlsl.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.WildPos != null && message.hasOwnProperty("WildPos"))
                object.WildPos = message.WildPos;
            if (message.BombWildPos && message.BombWildPos.length) {
                object.BombWildPos = [];
                for (let j = 0; j < message.BombWildPos.length; ++j)
                    object.BombWildPos[j] = message.BombWildPos[j];
            }
            if (message.BombedPos && message.BombedPos.length) {
                object.BombedPos = [];
                for (let j = 0; j < message.BombedPos.length; ++j)
                    object.BombedPos[j] = message.BombedPos[j];
            }
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlsl.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlsl.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlsl.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlsl
         * @interface IRoundInfo
         * @property {Array.<jlsl.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         * @property {number|null} [FreeNowRound] RoundInfo FreeNowRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlsl
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlsl.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlsl.IComboStageInfo>} ComboStageData
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * RoundInfo FreeNowRound.
         * @member {number} FreeNowRound
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeNowRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlsl.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlsl.RoundInfo
         * @static
         * @param {jlsl.IRoundInfo=} [properties] Properties to set
         * @returns {jlsl.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlsl.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsl.RoundInfo
         * @static
         * @param {jlsl.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlsl.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AddRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            if (message.FreeNowRound != null && Object.hasOwnProperty.call(message, "FreeNowRound"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeNowRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.FreeTotalRound);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.ScatterCount);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlsl.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.RoundInfo
         * @static
         * @param {jlsl.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlsl.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                case 7: {
                        message.FreeNowRound = reader.int32();
                        break;
                    }
                case 8: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 9: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlsl.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlsl.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                if (!$util.isInteger(message.FreeNowRound))
                    return "FreeNowRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.RoundInfo)
                return object;
            let message = new $root.jlsl.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlsl.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlsl.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlsl.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            if (object.FreeNowRound != null)
                message.FreeNowRound = object.FreeNowRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.RoundInfo
         * @static
         * @param {jlsl.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.AddRound = 0;
                object.MaxFlag = false;
                object.FreeNowRound = 0;
                object.FreeTotalRound = 0;
                object.ScatterCount = 0;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlsl.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            if (message.FreeNowRound != null && message.hasOwnProperty("FreeNowRound"))
                object.FreeNowRound = message.FreeNowRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlsl.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlsl.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.RoundInfo";
        };

        return RoundInfo;
    })();

    jlsl.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsl
         * @interface ISpinAck
         * @property {Array.<jlsl.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [Bet] SpinAck Bet
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsl
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsl.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlsl.IRoundInfo>} RoundQueue
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Bet.
         * @member {number} Bet
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.Bet = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsl.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsl.SpinAck
         * @static
         * @param {jlsl.ISpinAck=} [properties] Properties to set
         * @returns {jlsl.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsl.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsl.SpinAck
         * @static
         * @param {jlsl.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlsl.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Bet);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsl.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.SpinAck
         * @static
         * @param {jlsl.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlsl.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Bet = reader.double();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsl.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlsl.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.SpinAck)
                return object;
            let message = new $root.jlsl.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlsl.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlsl.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlsl.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.SpinAck
         * @static
         * @param {jlsl.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Bet = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlsl.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsl.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsl.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.SpinAck";
        };

        return SpinAck;
    })();

    jlsl.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlsl
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlsl
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlsl.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlsl.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlsl.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlsl.GameInfoData
         * @static
         * @param {jlsl.IGameInfoData=} [properties] Properties to set
         * @returns {jlsl.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlsl.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlsl.GameInfoData
         * @static
         * @param {jlsl.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlsl.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsl.GameInfoData
         * @static
         * @param {jlsl.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsl.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsl.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsl.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsl.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsl.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlsl.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsl.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsl.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsl.GameInfoData)
                return object;
            let message = new $root.jlsl.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlsl.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsl.GameInfoData
         * @static
         * @param {jlsl.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Mul = [];
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlsl.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlsl.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsl.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlsl;
})();

export const jlsr = $root.jlsr = (() => {

    /**
     * Namespace jlsr.
     * @exports jlsr
     * @namespace
     */
    const jlsr = {};

    jlsr.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsr
         * @interface ISpinAck
         * @property {number|null} [Award] SpinAck Award
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {Array.<number>|null} [PlateSymbol] SpinAck PlateSymbol
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsr
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsr.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.PlateSymbol = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck Award.
         * @member {number} Award
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.Award = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck PlateSymbol.
         * @member {Array.<number>} PlateSymbol
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateSymbol = $util.emptyArray;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsr.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsr.SpinAck
         * @static
         * @param {jlsr.ISpinAck=} [properties] Properties to set
         * @returns {jlsr.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsr.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsr.SpinAck
         * @static
         * @param {jlsr.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Award != null && Object.hasOwnProperty.call(message, "Award"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Award);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.PlateSymbol != null && message.PlateSymbol.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    writer.int32(message.PlateSymbol[i]);
                writer.ldelim();
            }
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsr.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsr.SpinAck
         * @static
         * @param {jlsr.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsr.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsr.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsr.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Award = reader.int32();
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PlateSymbol.push(reader.int32());
                        } else
                            message.PlateSymbol.push(reader.int32());
                        break;
                    }
                case 5: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 6: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 7: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsr.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsr.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsr.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Award != null && message.hasOwnProperty("Award"))
                if (!$util.isInteger(message.Award))
                    return "Award: integer expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    if (!$util.isInteger(message.PlateSymbol[i]))
                        return "PlateSymbol: integer[] expected";
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsr.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsr.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsr.SpinAck)
                return object;
            let message = new $root.jlsr.SpinAck();
            if (object.Award != null)
                message.Award = object.Award | 0;
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlsr.SpinAck.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i)
                    message.PlateSymbol[i] = object.PlateSymbol[i] | 0;
            }
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsr.SpinAck
         * @static
         * @param {jlsr.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PlateSymbol = [];
            if (options.defaults) {
                object.Award = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.Award != null && message.hasOwnProperty("Award"))
                object.Award = message.Award;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = message.PlateSymbol[j];
            }
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsr.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsr.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsr.SpinAck";
        };

        return SpinAck;
    })();

    return jlsr;
})();

export const jlsss = $root.jlsss = (() => {

    /**
     * Namespace jlsss.
     * @exports jlsss
     * @namespace
     */
    const jlsss = {};

    jlsss.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlsss
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlsss
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlsss.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlsss.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlsss.Column
         * @static
         * @param {jlsss.IColumn=} [properties] Properties to set
         * @returns {jlsss.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlsss.Column.verify|verify} messages.
         * @function encode
         * @memberof jlsss.Column
         * @static
         * @param {jlsss.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlsss.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.Column
         * @static
         * @param {jlsss.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlsss.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.Column)
                return object;
            let message = new $root.jlsss.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlsss.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.Column
         * @static
         * @param {jlsss.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlsss.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlsss.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.Column";
        };

        return Column;
    })();

    jlsss.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlsss
         * @interface IAwardData
         * @property {number|null} [AwardSymbol] AwardData AwardSymbol
         * @property {number|null} [AwardLine] AwardData AwardLine
         * @property {number|null} [AwardMoney] AwardData AwardMoney
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlsss
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlsss.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData AwardSymbol.
         * @member {number} AwardSymbol
         * @memberof jlsss.AwardData
         * @instance
         */
        AwardData.prototype.AwardSymbol = 0;

        /**
         * AwardData AwardLine.
         * @member {number} AwardLine
         * @memberof jlsss.AwardData
         * @instance
         */
        AwardData.prototype.AwardLine = 0;

        /**
         * AwardData AwardMoney.
         * @member {number} AwardMoney
         * @memberof jlsss.AwardData
         * @instance
         */
        AwardData.prototype.AwardMoney = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlsss.AwardData
         * @static
         * @param {jlsss.IAwardData=} [properties] Properties to set
         * @returns {jlsss.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlsss.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlsss.AwardData
         * @static
         * @param {jlsss.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.AwardSymbol != null && Object.hasOwnProperty.call(message, "AwardSymbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.AwardSymbol);
            if (message.AwardLine != null && Object.hasOwnProperty.call(message, "AwardLine"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardLine);
            if (message.AwardMoney != null && Object.hasOwnProperty.call(message, "AwardMoney"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.AwardMoney);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlsss.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.AwardData
         * @static
         * @param {jlsss.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.AwardSymbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.AwardLine = reader.int32();
                        break;
                    }
                case 3: {
                        message.AwardMoney = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlsss.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                if (!$util.isInteger(message.AwardSymbol))
                    return "AwardSymbol: integer expected";
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                if (!$util.isInteger(message.AwardLine))
                    return "AwardLine: integer expected";
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                if (typeof message.AwardMoney !== "number")
                    return "AwardMoney: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.AwardData)
                return object;
            let message = new $root.jlsss.AwardData();
            if (object.AwardSymbol != null)
                message.AwardSymbol = object.AwardSymbol | 0;
            if (object.AwardLine != null)
                message.AwardLine = object.AwardLine | 0;
            if (object.AwardMoney != null)
                message.AwardMoney = Number(object.AwardMoney);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.AwardData
         * @static
         * @param {jlsss.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.AwardSymbol = 0;
                object.AwardLine = 0;
                object.AwardMoney = 0;
            }
            if (message.AwardSymbol != null && message.hasOwnProperty("AwardSymbol"))
                object.AwardSymbol = message.AwardSymbol;
            if (message.AwardLine != null && message.hasOwnProperty("AwardLine"))
                object.AwardLine = message.AwardLine;
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                object.AwardMoney = options.json && !isFinite(message.AwardMoney) ? String(message.AwardMoney) : message.AwardMoney;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlsss.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlsss.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.AwardData";
        };

        return AwardData;
    })();

    jlsss.BonusRoundData = (function() {

        /**
         * Properties of a BonusRoundData.
         * @memberof jlsss
         * @interface IBonusRoundData
         * @property {Array.<jlsss.IColumn>|null} [Plate] BonusRoundData Plate
         * @property {Array.<jlsss.IAwardData>|null} [AwardDataList] BonusRoundData AwardDataList
         * @property {number|null} [RoundWin] BonusRoundData RoundWin
         * @property {boolean|null} [IsJP] BonusRoundData IsJP
         */

        /**
         * Constructs a new BonusRoundData.
         * @memberof jlsss
         * @classdesc Represents a BonusRoundData.
         * @implements IBonusRoundData
         * @constructor
         * @param {jlsss.IBonusRoundData=} [properties] Properties to set
         */
        function BonusRoundData(properties) {
            this.Plate = [];
            this.AwardDataList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusRoundData Plate.
         * @member {Array.<jlsss.IColumn>} Plate
         * @memberof jlsss.BonusRoundData
         * @instance
         */
        BonusRoundData.prototype.Plate = $util.emptyArray;

        /**
         * BonusRoundData AwardDataList.
         * @member {Array.<jlsss.IAwardData>} AwardDataList
         * @memberof jlsss.BonusRoundData
         * @instance
         */
        BonusRoundData.prototype.AwardDataList = $util.emptyArray;

        /**
         * BonusRoundData RoundWin.
         * @member {number} RoundWin
         * @memberof jlsss.BonusRoundData
         * @instance
         */
        BonusRoundData.prototype.RoundWin = 0;

        /**
         * BonusRoundData IsJP.
         * @member {boolean} IsJP
         * @memberof jlsss.BonusRoundData
         * @instance
         */
        BonusRoundData.prototype.IsJP = false;

        /**
         * Creates a new BonusRoundData instance using the specified properties.
         * @function create
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {jlsss.IBonusRoundData=} [properties] Properties to set
         * @returns {jlsss.BonusRoundData} BonusRoundData instance
         */
        BonusRoundData.create = function create(properties) {
            return new BonusRoundData(properties);
        };

        /**
         * Encodes the specified BonusRoundData message. Does not implicitly {@link jlsss.BonusRoundData.verify|verify} messages.
         * @function encode
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {jlsss.IBonusRoundData} message BonusRoundData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusRoundData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Plate != null && message.Plate.length)
                for (let i = 0; i < message.Plate.length; ++i)
                    $root.jlsss.Column.encode(message.Plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlsss.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.RoundWin);
            if (message.IsJP != null && Object.hasOwnProperty.call(message, "IsJP"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.IsJP);
            return writer;
        };

        /**
         * Encodes the specified BonusRoundData message, length delimited. Does not implicitly {@link jlsss.BonusRoundData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {jlsss.IBonusRoundData} message BonusRoundData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusRoundData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusRoundData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.BonusRoundData} BonusRoundData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusRoundData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.BonusRoundData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Plate && message.Plate.length))
                            message.Plate = [];
                        message.Plate.push($root.jlsss.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlsss.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 4: {
                        message.IsJP = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusRoundData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.BonusRoundData} BonusRoundData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusRoundData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusRoundData message.
         * @function verify
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusRoundData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Plate != null && message.hasOwnProperty("Plate")) {
                if (!Array.isArray(message.Plate))
                    return "Plate: array expected";
                for (let i = 0; i < message.Plate.length; ++i) {
                    let error = $root.jlsss.Column.verify(message.Plate[i]);
                    if (error)
                        return "Plate." + error;
                }
            }
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlsss.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.IsJP != null && message.hasOwnProperty("IsJP"))
                if (typeof message.IsJP !== "boolean")
                    return "IsJP: boolean expected";
            return null;
        };

        /**
         * Creates a BonusRoundData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.BonusRoundData} BonusRoundData
         */
        BonusRoundData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.BonusRoundData)
                return object;
            let message = new $root.jlsss.BonusRoundData();
            if (object.Plate) {
                if (!Array.isArray(object.Plate))
                    throw TypeError(".jlsss.BonusRoundData.Plate: array expected");
                message.Plate = [];
                for (let i = 0; i < object.Plate.length; ++i) {
                    if (typeof object.Plate[i] !== "object")
                        throw TypeError(".jlsss.BonusRoundData.Plate: object expected");
                    message.Plate[i] = $root.jlsss.Column.fromObject(object.Plate[i]);
                }
            }
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlsss.BonusRoundData.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlsss.BonusRoundData.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlsss.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.IsJP != null)
                message.IsJP = Boolean(object.IsJP);
            return message;
        };

        /**
         * Creates a plain object from a BonusRoundData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {jlsss.BonusRoundData} message BonusRoundData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusRoundData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Plate = [];
                object.AwardDataList = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.IsJP = false;
            }
            if (message.Plate && message.Plate.length) {
                object.Plate = [];
                for (let j = 0; j < message.Plate.length; ++j)
                    object.Plate[j] = $root.jlsss.Column.toObject(message.Plate[j], options);
            }
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlsss.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.IsJP != null && message.hasOwnProperty("IsJP"))
                object.IsJP = message.IsJP;
            return object;
        };

        /**
         * Converts this BonusRoundData to JSON.
         * @function toJSON
         * @memberof jlsss.BonusRoundData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusRoundData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusRoundData
         * @function getTypeUrl
         * @memberof jlsss.BonusRoundData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusRoundData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.BonusRoundData";
        };

        return BonusRoundData;
    })();

    jlsss.BonusAckData = (function() {

        /**
         * Properties of a BonusAckData.
         * @memberof jlsss
         * @interface IBonusAckData
         * @property {Array.<jlsss.IBonusRoundData>|null} [RoundData] BonusAckData RoundData
         * @property {number|null} [BonusWin] BonusAckData BonusWin
         */

        /**
         * Constructs a new BonusAckData.
         * @memberof jlsss
         * @classdesc Represents a BonusAckData.
         * @implements IBonusAckData
         * @constructor
         * @param {jlsss.IBonusAckData=} [properties] Properties to set
         */
        function BonusAckData(properties) {
            this.RoundData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusAckData RoundData.
         * @member {Array.<jlsss.IBonusRoundData>} RoundData
         * @memberof jlsss.BonusAckData
         * @instance
         */
        BonusAckData.prototype.RoundData = $util.emptyArray;

        /**
         * BonusAckData BonusWin.
         * @member {number} BonusWin
         * @memberof jlsss.BonusAckData
         * @instance
         */
        BonusAckData.prototype.BonusWin = 0;

        /**
         * Creates a new BonusAckData instance using the specified properties.
         * @function create
         * @memberof jlsss.BonusAckData
         * @static
         * @param {jlsss.IBonusAckData=} [properties] Properties to set
         * @returns {jlsss.BonusAckData} BonusAckData instance
         */
        BonusAckData.create = function create(properties) {
            return new BonusAckData(properties);
        };

        /**
         * Encodes the specified BonusAckData message. Does not implicitly {@link jlsss.BonusAckData.verify|verify} messages.
         * @function encode
         * @memberof jlsss.BonusAckData
         * @static
         * @param {jlsss.IBonusAckData} message BonusAckData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAckData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundData != null && message.RoundData.length)
                for (let i = 0; i < message.RoundData.length; ++i)
                    $root.jlsss.BonusRoundData.encode(message.RoundData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.BonusWin != null && Object.hasOwnProperty.call(message, "BonusWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.BonusWin);
            return writer;
        };

        /**
         * Encodes the specified BonusAckData message, length delimited. Does not implicitly {@link jlsss.BonusAckData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.BonusAckData
         * @static
         * @param {jlsss.IBonusAckData} message BonusAckData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusAckData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusAckData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.BonusAckData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.BonusAckData} BonusAckData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAckData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.BonusAckData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundData && message.RoundData.length))
                            message.RoundData = [];
                        message.RoundData.push($root.jlsss.BonusRoundData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.BonusWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusAckData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.BonusAckData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.BonusAckData} BonusAckData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusAckData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusAckData message.
         * @function verify
         * @memberof jlsss.BonusAckData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusAckData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundData != null && message.hasOwnProperty("RoundData")) {
                if (!Array.isArray(message.RoundData))
                    return "RoundData: array expected";
                for (let i = 0; i < message.RoundData.length; ++i) {
                    let error = $root.jlsss.BonusRoundData.verify(message.RoundData[i]);
                    if (error)
                        return "RoundData." + error;
                }
            }
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                if (typeof message.BonusWin !== "number")
                    return "BonusWin: number expected";
            return null;
        };

        /**
         * Creates a BonusAckData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.BonusAckData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.BonusAckData} BonusAckData
         */
        BonusAckData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.BonusAckData)
                return object;
            let message = new $root.jlsss.BonusAckData();
            if (object.RoundData) {
                if (!Array.isArray(object.RoundData))
                    throw TypeError(".jlsss.BonusAckData.RoundData: array expected");
                message.RoundData = [];
                for (let i = 0; i < object.RoundData.length; ++i) {
                    if (typeof object.RoundData[i] !== "object")
                        throw TypeError(".jlsss.BonusAckData.RoundData: object expected");
                    message.RoundData[i] = $root.jlsss.BonusRoundData.fromObject(object.RoundData[i]);
                }
            }
            if (object.BonusWin != null)
                message.BonusWin = Number(object.BonusWin);
            return message;
        };

        /**
         * Creates a plain object from a BonusAckData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.BonusAckData
         * @static
         * @param {jlsss.BonusAckData} message BonusAckData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusAckData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundData = [];
            if (options.defaults)
                object.BonusWin = 0;
            if (message.RoundData && message.RoundData.length) {
                object.RoundData = [];
                for (let j = 0; j < message.RoundData.length; ++j)
                    object.RoundData[j] = $root.jlsss.BonusRoundData.toObject(message.RoundData[j], options);
            }
            if (message.BonusWin != null && message.hasOwnProperty("BonusWin"))
                object.BonusWin = options.json && !isFinite(message.BonusWin) ? String(message.BonusWin) : message.BonusWin;
            return object;
        };

        /**
         * Converts this BonusAckData to JSON.
         * @function toJSON
         * @memberof jlsss.BonusAckData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusAckData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusAckData
         * @function getTypeUrl
         * @memberof jlsss.BonusAckData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusAckData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.BonusAckData";
        };

        return BonusAckData;
    })();

    jlsss.AckInfo = (function() {

        /**
         * Properties of an AckInfo.
         * @memberof jlsss
         * @interface IAckInfo
         * @property {Array.<jlsss.IColumn>|null} [MainPlate] AckInfo MainPlate
         * @property {Array.<jlsss.IAwardData>|null} [AwardDataList] AckInfo AwardDataList
         * @property {number|null} [AwardTypeFlag] AckInfo AwardTypeFlag
         * @property {Array.<jlsss.IBonusAckData>|null} [Bonus] AckInfo Bonus
         * @property {number|null} [MainWin] AckInfo MainWin
         * @property {number|null} [BonusTotalWin] AckInfo BonusTotalWin
         * @property {number|null} [TotalWin] AckInfo TotalWin
         * @property {Array.<number>|null} [JPWin] AckInfo JPWin
         * @property {jlsss.IJPInfo|null} [JPInfo] AckInfo JPInfo
         * @property {number|null} [PuzzleWeightNum] AckInfo PuzzleWeightNum
         */

        /**
         * Constructs a new AckInfo.
         * @memberof jlsss
         * @classdesc Represents an AckInfo.
         * @implements IAckInfo
         * @constructor
         * @param {jlsss.IAckInfo=} [properties] Properties to set
         */
        function AckInfo(properties) {
            this.MainPlate = [];
            this.AwardDataList = [];
            this.Bonus = [];
            this.JPWin = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AckInfo MainPlate.
         * @member {Array.<jlsss.IColumn>} MainPlate
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.MainPlate = $util.emptyArray;

        /**
         * AckInfo AwardDataList.
         * @member {Array.<jlsss.IAwardData>} AwardDataList
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.AwardDataList = $util.emptyArray;

        /**
         * AckInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.AwardTypeFlag = 0;

        /**
         * AckInfo Bonus.
         * @member {Array.<jlsss.IBonusAckData>} Bonus
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.Bonus = $util.emptyArray;

        /**
         * AckInfo MainWin.
         * @member {number} MainWin
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.MainWin = 0;

        /**
         * AckInfo BonusTotalWin.
         * @member {number} BonusTotalWin
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.BonusTotalWin = 0;

        /**
         * AckInfo TotalWin.
         * @member {number} TotalWin
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.TotalWin = 0;

        /**
         * AckInfo JPWin.
         * @member {Array.<number>} JPWin
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.JPWin = $util.emptyArray;

        /**
         * AckInfo JPInfo.
         * @member {jlsss.IJPInfo|null|undefined} JPInfo
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.JPInfo = null;

        /**
         * AckInfo PuzzleWeightNum.
         * @member {number} PuzzleWeightNum
         * @memberof jlsss.AckInfo
         * @instance
         */
        AckInfo.prototype.PuzzleWeightNum = 0;

        /**
         * Creates a new AckInfo instance using the specified properties.
         * @function create
         * @memberof jlsss.AckInfo
         * @static
         * @param {jlsss.IAckInfo=} [properties] Properties to set
         * @returns {jlsss.AckInfo} AckInfo instance
         */
        AckInfo.create = function create(properties) {
            return new AckInfo(properties);
        };

        /**
         * Encodes the specified AckInfo message. Does not implicitly {@link jlsss.AckInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsss.AckInfo
         * @static
         * @param {jlsss.IAckInfo} message AckInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AckInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainPlate != null && message.MainPlate.length)
                for (let i = 0; i < message.MainPlate.length; ++i)
                    $root.jlsss.Column.encode(message.MainPlate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlsss.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.Bonus != null && message.Bonus.length)
                for (let i = 0; i < message.Bonus.length; ++i)
                    $root.jlsss.BonusAckData.encode(message.Bonus[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.MainWin != null && Object.hasOwnProperty.call(message, "MainWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.MainWin);
            if (message.BonusTotalWin != null && Object.hasOwnProperty.call(message, "BonusTotalWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.BonusTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.TotalWin);
            if (message.JPWin != null && message.JPWin.length) {
                writer.uint32(/* id 8, wireType 2 =*/66).fork();
                for (let i = 0; i < message.JPWin.length; ++i)
                    writer.double(message.JPWin[i]);
                writer.ldelim();
            }
            if (message.JPInfo != null && Object.hasOwnProperty.call(message, "JPInfo"))
                $root.jlsss.JPInfo.encode(message.JPInfo, writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.PuzzleWeightNum != null && Object.hasOwnProperty.call(message, "PuzzleWeightNum"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.PuzzleWeightNum);
            return writer;
        };

        /**
         * Encodes the specified AckInfo message, length delimited. Does not implicitly {@link jlsss.AckInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.AckInfo
         * @static
         * @param {jlsss.IAckInfo} message AckInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AckInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AckInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.AckInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.AckInfo} AckInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AckInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.AckInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.MainPlate && message.MainPlate.length))
                            message.MainPlate = [];
                        message.MainPlate.push($root.jlsss.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlsss.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.Bonus && message.Bonus.length))
                            message.Bonus = [];
                        message.Bonus.push($root.jlsss.BonusAckData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.MainWin = reader.double();
                        break;
                    }
                case 6: {
                        message.BonusTotalWin = reader.double();
                        break;
                    }
                case 7: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 8: {
                        if (!(message.JPWin && message.JPWin.length))
                            message.JPWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPWin.push(reader.double());
                        } else
                            message.JPWin.push(reader.double());
                        break;
                    }
                case 9: {
                        message.JPInfo = $root.jlsss.JPInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 10: {
                        message.PuzzleWeightNum = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AckInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.AckInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.AckInfo} AckInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AckInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AckInfo message.
         * @function verify
         * @memberof jlsss.AckInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AckInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainPlate != null && message.hasOwnProperty("MainPlate")) {
                if (!Array.isArray(message.MainPlate))
                    return "MainPlate: array expected";
                for (let i = 0; i < message.MainPlate.length; ++i) {
                    let error = $root.jlsss.Column.verify(message.MainPlate[i]);
                    if (error)
                        return "MainPlate." + error;
                }
            }
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlsss.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.Bonus != null && message.hasOwnProperty("Bonus")) {
                if (!Array.isArray(message.Bonus))
                    return "Bonus: array expected";
                for (let i = 0; i < message.Bonus.length; ++i) {
                    let error = $root.jlsss.BonusAckData.verify(message.Bonus[i]);
                    if (error)
                        return "Bonus." + error;
                }
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                if (typeof message.MainWin !== "number")
                    return "MainWin: number expected";
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                if (typeof message.BonusTotalWin !== "number")
                    return "BonusTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.JPWin != null && message.hasOwnProperty("JPWin")) {
                if (!Array.isArray(message.JPWin))
                    return "JPWin: array expected";
                for (let i = 0; i < message.JPWin.length; ++i)
                    if (typeof message.JPWin[i] !== "number")
                        return "JPWin: number[] expected";
            }
            if (message.JPInfo != null && message.hasOwnProperty("JPInfo")) {
                let error = $root.jlsss.JPInfo.verify(message.JPInfo);
                if (error)
                    return "JPInfo." + error;
            }
            if (message.PuzzleWeightNum != null && message.hasOwnProperty("PuzzleWeightNum"))
                if (!$util.isInteger(message.PuzzleWeightNum))
                    return "PuzzleWeightNum: integer expected";
            return null;
        };

        /**
         * Creates an AckInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.AckInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.AckInfo} AckInfo
         */
        AckInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.AckInfo)
                return object;
            let message = new $root.jlsss.AckInfo();
            if (object.MainPlate) {
                if (!Array.isArray(object.MainPlate))
                    throw TypeError(".jlsss.AckInfo.MainPlate: array expected");
                message.MainPlate = [];
                for (let i = 0; i < object.MainPlate.length; ++i) {
                    if (typeof object.MainPlate[i] !== "object")
                        throw TypeError(".jlsss.AckInfo.MainPlate: object expected");
                    message.MainPlate[i] = $root.jlsss.Column.fromObject(object.MainPlate[i]);
                }
            }
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlsss.AckInfo.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlsss.AckInfo.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlsss.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.Bonus) {
                if (!Array.isArray(object.Bonus))
                    throw TypeError(".jlsss.AckInfo.Bonus: array expected");
                message.Bonus = [];
                for (let i = 0; i < object.Bonus.length; ++i) {
                    if (typeof object.Bonus[i] !== "object")
                        throw TypeError(".jlsss.AckInfo.Bonus: object expected");
                    message.Bonus[i] = $root.jlsss.BonusAckData.fromObject(object.Bonus[i]);
                }
            }
            if (object.MainWin != null)
                message.MainWin = Number(object.MainWin);
            if (object.BonusTotalWin != null)
                message.BonusTotalWin = Number(object.BonusTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.JPWin) {
                if (!Array.isArray(object.JPWin))
                    throw TypeError(".jlsss.AckInfo.JPWin: array expected");
                message.JPWin = [];
                for (let i = 0; i < object.JPWin.length; ++i)
                    message.JPWin[i] = Number(object.JPWin[i]);
            }
            if (object.JPInfo != null) {
                if (typeof object.JPInfo !== "object")
                    throw TypeError(".jlsss.AckInfo.JPInfo: object expected");
                message.JPInfo = $root.jlsss.JPInfo.fromObject(object.JPInfo);
            }
            if (object.PuzzleWeightNum != null)
                message.PuzzleWeightNum = object.PuzzleWeightNum | 0;
            return message;
        };

        /**
         * Creates a plain object from an AckInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.AckInfo
         * @static
         * @param {jlsss.AckInfo} message AckInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AckInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.MainPlate = [];
                object.AwardDataList = [];
                object.Bonus = [];
                object.JPWin = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.MainWin = 0;
                object.BonusTotalWin = 0;
                object.TotalWin = 0;
                object.JPInfo = null;
                object.PuzzleWeightNum = 0;
            }
            if (message.MainPlate && message.MainPlate.length) {
                object.MainPlate = [];
                for (let j = 0; j < message.MainPlate.length; ++j)
                    object.MainPlate[j] = $root.jlsss.Column.toObject(message.MainPlate[j], options);
            }
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlsss.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.Bonus && message.Bonus.length) {
                object.Bonus = [];
                for (let j = 0; j < message.Bonus.length; ++j)
                    object.Bonus[j] = $root.jlsss.BonusAckData.toObject(message.Bonus[j], options);
            }
            if (message.MainWin != null && message.hasOwnProperty("MainWin"))
                object.MainWin = options.json && !isFinite(message.MainWin) ? String(message.MainWin) : message.MainWin;
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin"))
                object.BonusTotalWin = options.json && !isFinite(message.BonusTotalWin) ? String(message.BonusTotalWin) : message.BonusTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.JPWin && message.JPWin.length) {
                object.JPWin = [];
                for (let j = 0; j < message.JPWin.length; ++j)
                    object.JPWin[j] = options.json && !isFinite(message.JPWin[j]) ? String(message.JPWin[j]) : message.JPWin[j];
            }
            if (message.JPInfo != null && message.hasOwnProperty("JPInfo"))
                object.JPInfo = $root.jlsss.JPInfo.toObject(message.JPInfo, options);
            if (message.PuzzleWeightNum != null && message.hasOwnProperty("PuzzleWeightNum"))
                object.PuzzleWeightNum = message.PuzzleWeightNum;
            return object;
        };

        /**
         * Converts this AckInfo to JSON.
         * @function toJSON
         * @memberof jlsss.AckInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AckInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AckInfo
         * @function getTypeUrl
         * @memberof jlsss.AckInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AckInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.AckInfo";
        };

        return AckInfo;
    })();

    jlsss.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlsss
         * @interface ISpinAck
         * @property {jlsss.IAckInfo|null} [PlateInfo] SpinAck PlateInfo
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlsss
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlsss.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateInfo.
         * @member {jlsss.IAckInfo|null|undefined} PlateInfo
         * @memberof jlsss.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateInfo = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlsss.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlsss.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlsss.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlsss.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlsss.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlsss.SpinAck
         * @static
         * @param {jlsss.ISpinAck=} [properties] Properties to set
         * @returns {jlsss.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlsss.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlsss.SpinAck
         * @static
         * @param {jlsss.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateInfo != null && Object.hasOwnProperty.call(message, "PlateInfo"))
                $root.jlsss.AckInfo.encode(message.PlateInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlsss.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.SpinAck
         * @static
         * @param {jlsss.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlateInfo = $root.jlsss.AckInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 4: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 5: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 6: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlsss.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo")) {
                let error = $root.jlsss.AckInfo.verify(message.PlateInfo);
                if (error)
                    return "PlateInfo." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.SpinAck)
                return object;
            let message = new $root.jlsss.SpinAck();
            if (object.PlateInfo != null) {
                if (typeof object.PlateInfo !== "object")
                    throw TypeError(".jlsss.SpinAck.PlateInfo: object expected");
                message.PlateInfo = $root.jlsss.AckInfo.fromObject(object.PlateInfo);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.SpinAck
         * @static
         * @param {jlsss.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.PlateInfo = null;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo"))
                object.PlateInfo = $root.jlsss.AckInfo.toObject(message.PlateInfo, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlsss.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlsss.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.SpinAck";
        };

        return SpinAck;
    })();

    jlsss.JPInfo = (function() {

        /**
         * Properties of a JPInfo.
         * @memberof jlsss
         * @interface IJPInfo
         * @property {Array.<number>|null} [Fronts] JPInfo Fronts
         */

        /**
         * Constructs a new JPInfo.
         * @memberof jlsss
         * @classdesc Represents a JPInfo.
         * @implements IJPInfo
         * @constructor
         * @param {jlsss.IJPInfo=} [properties] Properties to set
         */
        function JPInfo(properties) {
            this.Fronts = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPInfo Fronts.
         * @member {Array.<number>} Fronts
         * @memberof jlsss.JPInfo
         * @instance
         */
        JPInfo.prototype.Fronts = $util.emptyArray;

        /**
         * Creates a new JPInfo instance using the specified properties.
         * @function create
         * @memberof jlsss.JPInfo
         * @static
         * @param {jlsss.IJPInfo=} [properties] Properties to set
         * @returns {jlsss.JPInfo} JPInfo instance
         */
        JPInfo.create = function create(properties) {
            return new JPInfo(properties);
        };

        /**
         * Encodes the specified JPInfo message. Does not implicitly {@link jlsss.JPInfo.verify|verify} messages.
         * @function encode
         * @memberof jlsss.JPInfo
         * @static
         * @param {jlsss.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Fronts != null && message.Fronts.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Fronts.length; ++i)
                    writer.double(message.Fronts[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified JPInfo message, length delimited. Does not implicitly {@link jlsss.JPInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.JPInfo
         * @static
         * @param {jlsss.IJPInfo} message JPInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.JPInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Fronts && message.Fronts.length))
                            message.Fronts = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Fronts.push(reader.double());
                        } else
                            message.Fronts.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.JPInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.JPInfo} JPInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPInfo message.
         * @function verify
         * @memberof jlsss.JPInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Fronts != null && message.hasOwnProperty("Fronts")) {
                if (!Array.isArray(message.Fronts))
                    return "Fronts: array expected";
                for (let i = 0; i < message.Fronts.length; ++i)
                    if (typeof message.Fronts[i] !== "number")
                        return "Fronts: number[] expected";
            }
            return null;
        };

        /**
         * Creates a JPInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.JPInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.JPInfo} JPInfo
         */
        JPInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.JPInfo)
                return object;
            let message = new $root.jlsss.JPInfo();
            if (object.Fronts) {
                if (!Array.isArray(object.Fronts))
                    throw TypeError(".jlsss.JPInfo.Fronts: array expected");
                message.Fronts = [];
                for (let i = 0; i < object.Fronts.length; ++i)
                    message.Fronts[i] = Number(object.Fronts[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a JPInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.JPInfo
         * @static
         * @param {jlsss.JPInfo} message JPInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Fronts = [];
            if (message.Fronts && message.Fronts.length) {
                object.Fronts = [];
                for (let j = 0; j < message.Fronts.length; ++j)
                    object.Fronts[j] = options.json && !isFinite(message.Fronts[j]) ? String(message.Fronts[j]) : message.Fronts[j];
            }
            return object;
        };

        /**
         * Converts this JPInfo to JSON.
         * @function toJSON
         * @memberof jlsss.JPInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPInfo
         * @function getTypeUrl
         * @memberof jlsss.JPInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.JPInfo";
        };

        return JPInfo;
    })();

    jlsss.JPInfoReq = (function() {

        /**
         * Properties of a JPInfoReq.
         * @memberof jlsss
         * @interface IJPInfoReq
         * @property {number|null} [Currency] JPInfoReq Currency
         */

        /**
         * Constructs a new JPInfoReq.
         * @memberof jlsss
         * @classdesc Represents a JPInfoReq.
         * @implements IJPInfoReq
         * @constructor
         * @param {jlsss.IJPInfoReq=} [properties] Properties to set
         */
        function JPInfoReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JPInfoReq Currency.
         * @member {number} Currency
         * @memberof jlsss.JPInfoReq
         * @instance
         */
        JPInfoReq.prototype.Currency = 0;

        /**
         * Creates a new JPInfoReq instance using the specified properties.
         * @function create
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {jlsss.IJPInfoReq=} [properties] Properties to set
         * @returns {jlsss.JPInfoReq} JPInfoReq instance
         */
        JPInfoReq.create = function create(properties) {
            return new JPInfoReq(properties);
        };

        /**
         * Encodes the specified JPInfoReq message. Does not implicitly {@link jlsss.JPInfoReq.verify|verify} messages.
         * @function encode
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {jlsss.IJPInfoReq} message JPInfoReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfoReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Currency != null && Object.hasOwnProperty.call(message, "Currency"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Currency);
            return writer;
        };

        /**
         * Encodes the specified JPInfoReq message, length delimited. Does not implicitly {@link jlsss.JPInfoReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {jlsss.IJPInfoReq} message JPInfoReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JPInfoReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a JPInfoReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.JPInfoReq} JPInfoReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfoReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.JPInfoReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Currency = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JPInfoReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.JPInfoReq} JPInfoReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JPInfoReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JPInfoReq message.
         * @function verify
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JPInfoReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                if (!$util.isInteger(message.Currency))
                    return "Currency: integer expected";
            return null;
        };

        /**
         * Creates a JPInfoReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.JPInfoReq} JPInfoReq
         */
        JPInfoReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.JPInfoReq)
                return object;
            let message = new $root.jlsss.JPInfoReq();
            if (object.Currency != null)
                message.Currency = object.Currency | 0;
            return message;
        };

        /**
         * Creates a plain object from a JPInfoReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {jlsss.JPInfoReq} message JPInfoReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JPInfoReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.Currency = 0;
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                object.Currency = message.Currency;
            return object;
        };

        /**
         * Converts this JPInfoReq to JSON.
         * @function toJSON
         * @memberof jlsss.JPInfoReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JPInfoReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JPInfoReq
         * @function getTypeUrl
         * @memberof jlsss.JPInfoReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JPInfoReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.JPInfoReq";
        };

        return JPInfoReq;
    })();

    jlsss.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlsss
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<number>|null} [JPOdds] GameInfoData JPOdds
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlsss
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlsss.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.JPOdds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlsss.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlsss.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData JPOdds.
         * @member {Array.<number>} JPOdds
         * @memberof jlsss.GameInfoData
         * @instance
         */
        GameInfoData.prototype.JPOdds = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlsss.GameInfoData
         * @static
         * @param {jlsss.IGameInfoData=} [properties] Properties to set
         * @returns {jlsss.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlsss.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlsss.GameInfoData
         * @static
         * @param {jlsss.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.JPOdds != null && message.JPOdds.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.JPOdds.length; ++i)
                    writer.double(message.JPOdds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlsss.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlsss.GameInfoData
         * @static
         * @param {jlsss.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlsss.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlsss.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlsss.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.JPOdds && message.JPOdds.length))
                            message.JPOdds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.JPOdds.push(reader.double());
                        } else
                            message.JPOdds.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlsss.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlsss.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlsss.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.JPOdds != null && message.hasOwnProperty("JPOdds")) {
                if (!Array.isArray(message.JPOdds))
                    return "JPOdds: array expected";
                for (let i = 0; i < message.JPOdds.length; ++i)
                    if (typeof message.JPOdds[i] !== "number")
                        return "JPOdds: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlsss.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlsss.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlsss.GameInfoData)
                return object;
            let message = new $root.jlsss.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlsss.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.JPOdds) {
                if (!Array.isArray(object.JPOdds))
                    throw TypeError(".jlsss.GameInfoData.JPOdds: array expected");
                message.JPOdds = [];
                for (let i = 0; i < object.JPOdds.length; ++i)
                    message.JPOdds[i] = Number(object.JPOdds[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlsss.GameInfoData
         * @static
         * @param {jlsss.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.JPOdds = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.JPOdds && message.JPOdds.length) {
                object.JPOdds = [];
                for (let j = 0; j < message.JPOdds.length; ++j)
                    object.JPOdds[j] = options.json && !isFinite(message.JPOdds[j]) ? String(message.JPOdds[j]) : message.JPOdds[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlsss.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlsss.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlsss.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlsss;
})();

export const jltw = $root.jltw = (() => {

    /**
     * Namespace jltw.
     * @exports jltw
     * @namespace
     */
    const jltw = {};

    /**
     * Symbol enum.
     * @name jltw.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} Symbol9=10 Symbol9 value
     * @property {number} Symbol10=11 Symbol10 value
     * @property {number} Symbol11=12 Symbol11 value
     */
    jltw.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "Symbol9"] = 10;
        values[valuesById[11] = "Symbol10"] = 11;
        values[valuesById[12] = "Symbol11"] = 12;
        return values;
    })();

    jltw.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jltw
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jltw
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jltw.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jltw.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jltw.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jltw.Block
         * @static
         * @param {jltw.IBlock=} [properties] Properties to set
         * @returns {jltw.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jltw.Block.verify|verify} messages.
         * @function encode
         * @memberof jltw.Block
         * @static
         * @param {jltw.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jltw.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltw.Block
         * @static
         * @param {jltw.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jltw.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltw.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltw.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltw.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltw.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jltw.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltw.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltw.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jltw.Block)
                return object;
            let message = new $root.jltw.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltw.Block
         * @static
         * @param {jltw.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jltw.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jltw.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltw.Block";
        };

        return Block;
    })();

    jltw.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jltw
         * @interface IPlate
         * @property {Array.<jltw.IColumnData>|null} [column] Plate column
         * @property {number|null} [win] Plate win
         * @property {Array.<number>|null} [sameReel] Plate sameReel
         */

        /**
         * Constructs a new Plate.
         * @memberof jltw
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jltw.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            this.sameReel = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jltw.IColumnData>} column
         * @memberof jltw.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jltw.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Plate sameReel.
         * @member {Array.<number>} sameReel
         * @memberof jltw.Plate
         * @instance
         */
        Plate.prototype.sameReel = $util.emptyArray;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jltw.Plate
         * @static
         * @param {jltw.IPlate=} [properties] Properties to set
         * @returns {jltw.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jltw.Plate.verify|verify} messages.
         * @function encode
         * @memberof jltw.Plate
         * @static
         * @param {jltw.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jltw.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.win);
            if (message.sameReel != null && message.sameReel.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.sameReel.length; ++i)
                    writer.int32(message.sameReel[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jltw.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltw.Plate
         * @static
         * @param {jltw.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jltw.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltw.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltw.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jltw.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.win = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.sameReel && message.sameReel.length))
                            message.sameReel = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.sameReel.push(reader.int32());
                        } else
                            message.sameReel.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltw.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltw.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jltw.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jltw.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            if (message.sameReel != null && message.hasOwnProperty("sameReel")) {
                if (!Array.isArray(message.sameReel))
                    return "sameReel: array expected";
                for (let i = 0; i < message.sameReel.length; ++i)
                    if (!$util.isInteger(message.sameReel[i]))
                        return "sameReel: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltw.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltw.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jltw.Plate)
                return object;
            let message = new $root.jltw.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jltw.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jltw.Plate.column: object expected");
                    message.column[i] = $root.jltw.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            if (object.sameReel) {
                if (!Array.isArray(object.sameReel))
                    throw TypeError(".jltw.Plate.sameReel: array expected");
                message.sameReel = [];
                for (let i = 0; i < object.sameReel.length; ++i)
                    message.sameReel[i] = object.sameReel[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltw.Plate
         * @static
         * @param {jltw.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.column = [];
                object.sameReel = [];
            }
            if (options.defaults)
                object.win = 0;
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jltw.ColumnData.toObject(message.column[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            if (message.sameReel && message.sameReel.length) {
                object.sameReel = [];
                for (let j = 0; j < message.sameReel.length; ++j)
                    object.sameReel[j] = message.sameReel[j];
            }
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jltw.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jltw.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltw.Plate";
        };

        return Plate;
    })();

    jltw.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jltw
         * @interface IAllPlate
         * @property {Array.<jltw.IPlate>|null} [plate] AllPlate plate
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jltw
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jltw.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jltw.IPlate>} plate
         * @memberof jltw.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jltw.AllPlate
         * @static
         * @param {jltw.IAllPlate=} [properties] Properties to set
         * @returns {jltw.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jltw.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jltw.AllPlate
         * @static
         * @param {jltw.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jltw.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jltw.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltw.AllPlate
         * @static
         * @param {jltw.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jltw.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltw.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltw.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jltw.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltw.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltw.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jltw.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jltw.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltw.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltw.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jltw.AllPlate)
                return object;
            let message = new $root.jltw.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jltw.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jltw.AllPlate.plate: object expected");
                    message.plate[i] = $root.jltw.Plate.fromObject(object.plate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltw.AllPlate
         * @static
         * @param {jltw.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jltw.Plate.toObject(message.plate[j], options);
            }
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jltw.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jltw.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltw.AllPlate";
        };

        return AllPlate;
    })();

    jltw.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jltw
         * @interface IColumnData
         * @property {Array.<jltw.ICell>|null} [row] ColumnData row
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jltw
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jltw.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jltw.ICell>} row
         * @memberof jltw.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jltw.ColumnData
         * @static
         * @param {jltw.IColumnData=} [properties] Properties to set
         * @returns {jltw.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jltw.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jltw.ColumnData
         * @static
         * @param {jltw.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jltw.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jltw.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltw.ColumnData
         * @static
         * @param {jltw.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jltw.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltw.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltw.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jltw.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltw.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltw.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jltw.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jltw.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltw.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltw.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jltw.ColumnData)
                return object;
            let message = new $root.jltw.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jltw.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jltw.ColumnData.row: object expected");
                    message.row[i] = $root.jltw.Cell.fromObject(object.row[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltw.ColumnData
         * @static
         * @param {jltw.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jltw.Cell.toObject(message.row[j], options);
            }
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jltw.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jltw.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltw.ColumnData";
        };

        return ColumnData;
    })();

    jltw.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jltw
         * @interface ICell
         * @property {number|null} [symbol] Cell symbol
         * @property {boolean|null} [isConnect] Cell isConnect
         */

        /**
         * Constructs a new Cell.
         * @memberof jltw
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jltw.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {number} symbol
         * @memberof jltw.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell isConnect.
         * @member {boolean} isConnect
         * @memberof jltw.Cell
         * @instance
         */
        Cell.prototype.isConnect = false;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jltw.Cell
         * @static
         * @param {jltw.ICell=} [properties] Properties to set
         * @returns {jltw.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jltw.Cell.verify|verify} messages.
         * @function encode
         * @memberof jltw.Cell
         * @static
         * @param {jltw.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isConnect);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jltw.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltw.Cell
         * @static
         * @param {jltw.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jltw.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltw.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltw.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.isConnect = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltw.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltw.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jltw.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isInteger(message.symbol))
                    return "symbol: integer expected";
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltw.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltw.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jltw.Cell)
                return object;
            let message = new $root.jltw.Cell();
            if (object.symbol != null)
                message.symbol = object.symbol | 0;
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltw.Cell
         * @static
         * @param {jltw.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = 0;
                object.isConnect = false;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jltw.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jltw.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltw.Cell";
        };

        return Cell;
    })();

    return jltw;
})();

export const jltx = $root.jltx = (() => {

    /**
     * Namespace jltx.
     * @exports jltx
     * @namespace
     */
    const jltx = {};

    jltx.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jltx
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jltx
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jltx.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jltx.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jltx.Column
         * @static
         * @param {jltx.IColumn=} [properties] Properties to set
         * @returns {jltx.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jltx.Column.verify|verify} messages.
         * @function encode
         * @memberof jltx.Column
         * @static
         * @param {jltx.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jltx.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.Column
         * @static
         * @param {jltx.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jltx.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.Column)
                return object;
            let message = new $root.jltx.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jltx.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.Column
         * @static
         * @param {jltx.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jltx.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jltx.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.Column";
        };

        return Column;
    })();

    jltx.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jltx
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jltx
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jltx.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jltx.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jltx.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jltx.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jltx.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jltx.AwardData
         * @static
         * @param {jltx.IAwardData=} [properties] Properties to set
         * @returns {jltx.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jltx.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jltx.AwardData
         * @static
         * @param {jltx.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jltx.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.AwardData
         * @static
         * @param {jltx.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jltx.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.AwardData)
                return object;
            let message = new $root.jltx.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.AwardData
         * @static
         * @param {jltx.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jltx.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jltx.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.AwardData";
        };

        return AwardData;
    })();

    jltx.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jltx
         * @interface IComboStageInfo
         * @property {Array.<jltx.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jltx.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jltx.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {boolean|null} [BonusHit] ComboStageInfo BonusHit
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jltx
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jltx.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jltx.IColumn>} ComboStageSymbol
         * @memberof jltx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jltx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jltx.IAwardData>} AwardDataVec
         * @memberof jltx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jltx.IColumn>} FillSymbols
         * @memberof jltx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo BonusHit.
         * @member {boolean} BonusHit
         * @memberof jltx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.BonusHit = false;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jltx.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {jltx.IComboStageInfo=} [properties] Properties to set
         * @returns {jltx.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jltx.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {jltx.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jltx.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jltx.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jltx.Column.encode(message.FillSymbols[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.BonusHit != null && Object.hasOwnProperty.call(message, "BonusHit"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.BonusHit);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jltx.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {jltx.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jltx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jltx.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jltx.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.BonusHit = reader.bool();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jltx.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jltx.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jltx.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.BonusHit != null && message.hasOwnProperty("BonusHit"))
                if (typeof message.BonusHit !== "boolean")
                    return "BonusHit: boolean expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.ComboStageInfo)
                return object;
            let message = new $root.jltx.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jltx.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jltx.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jltx.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jltx.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jltx.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jltx.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jltx.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jltx.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jltx.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.BonusHit != null)
                message.BonusHit = Boolean(object.BonusHit);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {jltx.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.BonusHit = false;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jltx.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jltx.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jltx.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.BonusHit != null && message.hasOwnProperty("BonusHit"))
                object.BonusHit = message.BonusHit;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jltx.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jltx.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jltx.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jltx
         * @interface IRoundInfo
         * @property {Array.<jltx.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {boolean|null} [BonusFlag] RoundInfo BonusFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jltx
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jltx.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jltx.IComboStageInfo>} ComboStageData
         * @memberof jltx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jltx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jltx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jltx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo BonusFlag.
         * @member {boolean} BonusFlag
         * @memberof jltx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.BonusFlag = false;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jltx.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jltx.RoundInfo
         * @static
         * @param {jltx.IRoundInfo=} [properties] Properties to set
         * @returns {jltx.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jltx.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jltx.RoundInfo
         * @static
         * @param {jltx.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jltx.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.BonusFlag != null && Object.hasOwnProperty.call(message, "BonusFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.BonusFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jltx.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.RoundInfo
         * @static
         * @param {jltx.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jltx.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.BonusFlag = reader.bool();
                        break;
                    }
                case 6: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jltx.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jltx.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.BonusFlag != null && message.hasOwnProperty("BonusFlag"))
                if (typeof message.BonusFlag !== "boolean")
                    return "BonusFlag: boolean expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.RoundInfo)
                return object;
            let message = new $root.jltx.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jltx.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jltx.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jltx.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.BonusFlag != null)
                message.BonusFlag = Boolean(object.BonusFlag);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.RoundInfo
         * @static
         * @param {jltx.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.BonusFlag = false;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jltx.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.BonusFlag != null && message.hasOwnProperty("BonusFlag"))
                object.BonusFlag = message.BonusFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jltx.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jltx.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.RoundInfo";
        };

        return RoundInfo;
    })();

    jltx.BonusGameInfo = (function() {

        /**
         * Properties of a BonusGameInfo.
         * @memberof jltx
         * @interface IBonusGameInfo
         * @property {number|null} [Symbol] BonusGameInfo Symbol
         * @property {number|null} [Result] BonusGameInfo Result
         * @property {number|null} [Win] BonusGameInfo Win
         * @property {number|null} [HealthPoint] BonusGameInfo HealthPoint
         * @property {boolean|null} [MaxFlag] BonusGameInfo MaxFlag
         */

        /**
         * Constructs a new BonusGameInfo.
         * @memberof jltx
         * @classdesc Represents a BonusGameInfo.
         * @implements IBonusGameInfo
         * @constructor
         * @param {jltx.IBonusGameInfo=} [properties] Properties to set
         */
        function BonusGameInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusGameInfo Symbol.
         * @member {number} Symbol
         * @memberof jltx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Symbol = 0;

        /**
         * BonusGameInfo Result.
         * @member {number} Result
         * @memberof jltx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Result = 0;

        /**
         * BonusGameInfo Win.
         * @member {number} Win
         * @memberof jltx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.Win = 0;

        /**
         * BonusGameInfo HealthPoint.
         * @member {number} HealthPoint
         * @memberof jltx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.HealthPoint = 0;

        /**
         * BonusGameInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jltx.BonusGameInfo
         * @instance
         */
        BonusGameInfo.prototype.MaxFlag = false;

        /**
         * Creates a new BonusGameInfo instance using the specified properties.
         * @function create
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {jltx.IBonusGameInfo=} [properties] Properties to set
         * @returns {jltx.BonusGameInfo} BonusGameInfo instance
         */
        BonusGameInfo.create = function create(properties) {
            return new BonusGameInfo(properties);
        };

        /**
         * Encodes the specified BonusGameInfo message. Does not implicitly {@link jltx.BonusGameInfo.verify|verify} messages.
         * @function encode
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {jltx.IBonusGameInfo} message BonusGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Result != null && Object.hasOwnProperty.call(message, "Result"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Result);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            if (message.HealthPoint != null && Object.hasOwnProperty.call(message, "HealthPoint"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.HealthPoint);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified BonusGameInfo message, length delimited. Does not implicitly {@link jltx.BonusGameInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {jltx.IBonusGameInfo} message BonusGameInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusGameInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.BonusGameInfo} BonusGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.BonusGameInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Result = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                case 4: {
                        message.HealthPoint = reader.int32();
                        break;
                    }
                case 5: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusGameInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.BonusGameInfo} BonusGameInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusGameInfo message.
         * @function verify
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusGameInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Result != null && message.hasOwnProperty("Result"))
                if (!$util.isInteger(message.Result))
                    return "Result: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.HealthPoint != null && message.hasOwnProperty("HealthPoint"))
                if (!$util.isInteger(message.HealthPoint))
                    return "HealthPoint: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a BonusGameInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.BonusGameInfo} BonusGameInfo
         */
        BonusGameInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.BonusGameInfo)
                return object;
            let message = new $root.jltx.BonusGameInfo();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Result != null)
                message.Result = object.Result | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.HealthPoint != null)
                message.HealthPoint = object.HealthPoint | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a BonusGameInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {jltx.BonusGameInfo} message BonusGameInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusGameInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Result = 0;
                object.Win = 0;
                object.HealthPoint = 0;
                object.MaxFlag = false;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Result != null && message.hasOwnProperty("Result"))
                object.Result = message.Result;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.HealthPoint != null && message.hasOwnProperty("HealthPoint"))
                object.HealthPoint = message.HealthPoint;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this BonusGameInfo to JSON.
         * @function toJSON
         * @memberof jltx.BonusGameInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusGameInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusGameInfo
         * @function getTypeUrl
         * @memberof jltx.BonusGameInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusGameInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.BonusGameInfo";
        };

        return BonusGameInfo;
    })();

    jltx.BonusGameInfoArray = (function() {

        /**
         * Properties of a BonusGameInfoArray.
         * @memberof jltx
         * @interface IBonusGameInfoArray
         * @property {Array.<jltx.IBonusGameInfo>|null} [Array] BonusGameInfoArray Array
         */

        /**
         * Constructs a new BonusGameInfoArray.
         * @memberof jltx
         * @classdesc Represents a BonusGameInfoArray.
         * @implements IBonusGameInfoArray
         * @constructor
         * @param {jltx.IBonusGameInfoArray=} [properties] Properties to set
         */
        function BonusGameInfoArray(properties) {
            this.Array = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BonusGameInfoArray Array.
         * @member {Array.<jltx.IBonusGameInfo>} Array
         * @memberof jltx.BonusGameInfoArray
         * @instance
         */
        BonusGameInfoArray.prototype.Array = $util.emptyArray;

        /**
         * Creates a new BonusGameInfoArray instance using the specified properties.
         * @function create
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {jltx.IBonusGameInfoArray=} [properties] Properties to set
         * @returns {jltx.BonusGameInfoArray} BonusGameInfoArray instance
         */
        BonusGameInfoArray.create = function create(properties) {
            return new BonusGameInfoArray(properties);
        };

        /**
         * Encodes the specified BonusGameInfoArray message. Does not implicitly {@link jltx.BonusGameInfoArray.verify|verify} messages.
         * @function encode
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {jltx.IBonusGameInfoArray} message BonusGameInfoArray message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfoArray.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Array != null && message.Array.length)
                for (let i = 0; i < message.Array.length; ++i)
                    $root.jltx.BonusGameInfo.encode(message.Array[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BonusGameInfoArray message, length delimited. Does not implicitly {@link jltx.BonusGameInfoArray.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {jltx.IBonusGameInfoArray} message BonusGameInfoArray message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BonusGameInfoArray.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BonusGameInfoArray message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.BonusGameInfoArray} BonusGameInfoArray
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfoArray.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.BonusGameInfoArray();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Array && message.Array.length))
                            message.Array = [];
                        message.Array.push($root.jltx.BonusGameInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BonusGameInfoArray message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.BonusGameInfoArray} BonusGameInfoArray
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BonusGameInfoArray.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BonusGameInfoArray message.
         * @function verify
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BonusGameInfoArray.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Array != null && message.hasOwnProperty("Array")) {
                if (!Array.isArray(message.Array))
                    return "Array: array expected";
                for (let i = 0; i < message.Array.length; ++i) {
                    let error = $root.jltx.BonusGameInfo.verify(message.Array[i]);
                    if (error)
                        return "Array." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BonusGameInfoArray message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.BonusGameInfoArray} BonusGameInfoArray
         */
        BonusGameInfoArray.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.BonusGameInfoArray)
                return object;
            let message = new $root.jltx.BonusGameInfoArray();
            if (object.Array) {
                if (!Array.isArray(object.Array))
                    throw TypeError(".jltx.BonusGameInfoArray.Array: array expected");
                message.Array = [];
                for (let i = 0; i < object.Array.length; ++i) {
                    if (typeof object.Array[i] !== "object")
                        throw TypeError(".jltx.BonusGameInfoArray.Array: object expected");
                    message.Array[i] = $root.jltx.BonusGameInfo.fromObject(object.Array[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BonusGameInfoArray message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {jltx.BonusGameInfoArray} message BonusGameInfoArray
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BonusGameInfoArray.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Array = [];
            if (message.Array && message.Array.length) {
                object.Array = [];
                for (let j = 0; j < message.Array.length; ++j)
                    object.Array[j] = $root.jltx.BonusGameInfo.toObject(message.Array[j], options);
            }
            return object;
        };

        /**
         * Converts this BonusGameInfoArray to JSON.
         * @function toJSON
         * @memberof jltx.BonusGameInfoArray
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BonusGameInfoArray.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BonusGameInfoArray
         * @function getTypeUrl
         * @memberof jltx.BonusGameInfoArray
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BonusGameInfoArray.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.BonusGameInfoArray";
        };

        return BonusGameInfoArray;
    })();

    jltx.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jltx
         * @interface ISpinAck
         * @property {Array.<jltx.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {Array.<jltx.IBonusGameInfoArray>|null} [BonusInfo] SpinAck BonusInfo
         * @property {Array.<number>|null} [BonusTotalWin] SpinAck BonusTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jltx
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jltx.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            this.BonusInfo = [];
            this.BonusTotalWin = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jltx.IRoundInfo>} RoundQueue
         * @memberof jltx.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jltx.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck BonusInfo.
         * @member {Array.<jltx.IBonusGameInfoArray>} BonusInfo
         * @memberof jltx.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusInfo = $util.emptyArray;

        /**
         * SpinAck BonusTotalWin.
         * @member {Array.<number>} BonusTotalWin
         * @memberof jltx.SpinAck
         * @instance
         */
        SpinAck.prototype.BonusTotalWin = $util.emptyArray;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jltx.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jltx.SpinAck
         * @static
         * @param {jltx.ISpinAck=} [properties] Properties to set
         * @returns {jltx.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jltx.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jltx.SpinAck
         * @static
         * @param {jltx.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jltx.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.BonusInfo != null && message.BonusInfo.length)
                for (let i = 0; i < message.BonusInfo.length; ++i)
                    $root.jltx.BonusGameInfoArray.encode(message.BonusInfo[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.BonusTotalWin != null && message.BonusTotalWin.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (let i = 0; i < message.BonusTotalWin.length; ++i)
                    writer.double(message.BonusTotalWin[i]);
                writer.ldelim();
            }
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.TotalWin);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jltx.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jltx.SpinAck
         * @static
         * @param {jltx.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jltx.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jltx.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jltx.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jltx.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.BonusInfo && message.BonusInfo.length))
                            message.BonusInfo = [];
                        message.BonusInfo.push($root.jltx.BonusGameInfoArray.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.BonusTotalWin && message.BonusTotalWin.length))
                            message.BonusTotalWin = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BonusTotalWin.push(reader.double());
                        } else
                            message.BonusTotalWin.push(reader.double());
                        break;
                    }
                case 5: {
                        message.TotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jltx.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jltx.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jltx.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jltx.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.BonusInfo != null && message.hasOwnProperty("BonusInfo")) {
                if (!Array.isArray(message.BonusInfo))
                    return "BonusInfo: array expected";
                for (let i = 0; i < message.BonusInfo.length; ++i) {
                    let error = $root.jltx.BonusGameInfoArray.verify(message.BonusInfo[i]);
                    if (error)
                        return "BonusInfo." + error;
                }
            }
            if (message.BonusTotalWin != null && message.hasOwnProperty("BonusTotalWin")) {
                if (!Array.isArray(message.BonusTotalWin))
                    return "BonusTotalWin: array expected";
                for (let i = 0; i < message.BonusTotalWin.length; ++i)
                    if (typeof message.BonusTotalWin[i] !== "number")
                        return "BonusTotalWin: number[] expected";
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jltx.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jltx.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jltx.SpinAck)
                return object;
            let message = new $root.jltx.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jltx.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jltx.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jltx.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.BonusInfo) {
                if (!Array.isArray(object.BonusInfo))
                    throw TypeError(".jltx.SpinAck.BonusInfo: array expected");
                message.BonusInfo = [];
                for (let i = 0; i < object.BonusInfo.length; ++i) {
                    if (typeof object.BonusInfo[i] !== "object")
                        throw TypeError(".jltx.SpinAck.BonusInfo: object expected");
                    message.BonusInfo[i] = $root.jltx.BonusGameInfoArray.fromObject(object.BonusInfo[i]);
                }
            }
            if (object.BonusTotalWin) {
                if (!Array.isArray(object.BonusTotalWin))
                    throw TypeError(".jltx.SpinAck.BonusTotalWin: array expected");
                message.BonusTotalWin = [];
                for (let i = 0; i < object.BonusTotalWin.length; ++i)
                    message.BonusTotalWin[i] = Number(object.BonusTotalWin[i]);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jltx.SpinAck
         * @static
         * @param {jltx.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.RoundQueue = [];
                object.BonusInfo = [];
                object.BonusTotalWin = [];
            }
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jltx.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.BonusInfo && message.BonusInfo.length) {
                object.BonusInfo = [];
                for (let j = 0; j < message.BonusInfo.length; ++j)
                    object.BonusInfo[j] = $root.jltx.BonusGameInfoArray.toObject(message.BonusInfo[j], options);
            }
            if (message.BonusTotalWin && message.BonusTotalWin.length) {
                object.BonusTotalWin = [];
                for (let j = 0; j < message.BonusTotalWin.length; ++j)
                    object.BonusTotalWin[j] = options.json && !isFinite(message.BonusTotalWin[j]) ? String(message.BonusTotalWin[j]) : message.BonusTotalWin[j];
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jltx.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jltx.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jltx.SpinAck";
        };

        return SpinAck;
    })();

    return jltx;
})();

export const jlwa = $root.jlwa = (() => {

    /**
     * Namespace jlwa.
     * @exports jlwa
     * @namespace
     */
    const jlwa = {};

    jlwa.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlwa
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlwa
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlwa.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlwa.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlwa.Column
         * @static
         * @param {jlwa.IColumn=} [properties] Properties to set
         * @returns {jlwa.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlwa.Column.verify|verify} messages.
         * @function encode
         * @memberof jlwa.Column
         * @static
         * @param {jlwa.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlwa.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwa.Column
         * @static
         * @param {jlwa.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlwa.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwa.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwa.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwa.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwa.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlwa.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwa.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwa.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwa.Column)
                return object;
            let message = new $root.jlwa.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlwa.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwa.Column
         * @static
         * @param {jlwa.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlwa.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlwa.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwa.Column";
        };

        return Column;
    })();

    jlwa.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlwa
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         * @property {Array.<number>|null} [PosVec] AwardData PosVec
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlwa
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlwa.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.PosVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlwa.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlwa.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlwa.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlwa.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlwa.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * AwardData PosVec.
         * @member {Array.<number>} PosVec
         * @memberof jlwa.AwardData
         * @instance
         */
        AwardData.prototype.PosVec = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlwa.AwardData
         * @static
         * @param {jlwa.IAwardData=} [properties] Properties to set
         * @returns {jlwa.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlwa.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlwa.AwardData
         * @static
         * @param {jlwa.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mult);
            if (message.PosVec != null && message.PosVec.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.PosVec.length; ++i)
                    writer.int32(message.PosVec[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlwa.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwa.AwardData
         * @static
         * @param {jlwa.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwa.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwa.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwa.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        message.Mult = reader.double();
                        break;
                    }
                case 6: {
                        if (!(message.PosVec && message.PosVec.length))
                            message.PosVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.PosVec.push(reader.int32());
                        } else
                            message.PosVec.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwa.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwa.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlwa.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.PosVec != null && message.hasOwnProperty("PosVec")) {
                if (!Array.isArray(message.PosVec))
                    return "PosVec: array expected";
                for (let i = 0; i < message.PosVec.length; ++i)
                    if (!$util.isInteger(message.PosVec[i]))
                        return "PosVec: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwa.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwa.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwa.AwardData)
                return object;
            let message = new $root.jlwa.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.PosVec) {
                if (!Array.isArray(object.PosVec))
                    throw TypeError(".jlwa.AwardData.PosVec: array expected");
                message.PosVec = [];
                for (let i = 0; i < object.PosVec.length; ++i)
                    message.PosVec[i] = object.PosVec[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwa.AwardData
         * @static
         * @param {jlwa.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.PosVec = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.PosVec && message.PosVec.length) {
                object.PosVec = [];
                for (let j = 0; j < message.PosVec.length; ++j)
                    object.PosVec[j] = message.PosVec[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlwa.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlwa.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwa.AwardData";
        };

        return AwardData;
    })();

    jlwa.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlwa
         * @interface IComboStageInfo
         * @property {Array.<jlwa.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {number|null} [ComboLevel] ComboStageInfo ComboLevel
         * @property {Array.<jlwa.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlwa.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {Array.<number>|null} [UpgradeVec] ComboStageInfo UpgradeVec
         * @property {number|null} [Mult] ComboStageInfo Mult
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlwa
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlwa.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.UpgradeVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlwa.IColumn>} ComboStageSymbol
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo ComboLevel.
         * @member {number} ComboLevel
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboLevel = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlwa.IAwardData>} AwardDataVec
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlwa.IColumn>} FillSymbols
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo UpgradeVec.
         * @member {Array.<number>} UpgradeVec
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.UpgradeVec = $util.emptyArray;

        /**
         * ComboStageInfo Mult.
         * @member {number} Mult
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Mult = 0;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlwa.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {jlwa.IComboStageInfo=} [properties] Properties to set
         * @returns {jlwa.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlwa.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {jlwa.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlwa.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.ComboLevel != null && Object.hasOwnProperty.call(message, "ComboLevel"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ComboLevel);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlwa.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlwa.Column.encode(message.FillSymbols[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.UpgradeVec != null && message.UpgradeVec.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.UpgradeVec.length; ++i)
                    writer.int32(message.UpgradeVec[i]);
                writer.ldelim();
            }
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Mult);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlwa.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {jlwa.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwa.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwa.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlwa.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        message.ComboLevel = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlwa.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlwa.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 6: {
                        if (!(message.UpgradeVec && message.UpgradeVec.length))
                            message.UpgradeVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.UpgradeVec.push(reader.int32());
                        } else
                            message.UpgradeVec.push(reader.int32());
                        break;
                    }
                case 7: {
                        message.Mult = reader.double();
                        break;
                    }
                case 8: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwa.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlwa.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.ComboLevel != null && message.hasOwnProperty("ComboLevel"))
                if (!$util.isInteger(message.ComboLevel))
                    return "ComboLevel: integer expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlwa.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlwa.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.UpgradeVec != null && message.hasOwnProperty("UpgradeVec")) {
                if (!Array.isArray(message.UpgradeVec))
                    return "UpgradeVec: array expected";
                for (let i = 0; i < message.UpgradeVec.length; ++i)
                    if (!$util.isInteger(message.UpgradeVec[i]))
                        return "UpgradeVec: integer[] expected";
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwa.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwa.ComboStageInfo)
                return object;
            let message = new $root.jlwa.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlwa.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlwa.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlwa.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.ComboLevel != null)
                message.ComboLevel = object.ComboLevel | 0;
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlwa.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlwa.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlwa.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlwa.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlwa.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlwa.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.UpgradeVec) {
                if (!Array.isArray(object.UpgradeVec))
                    throw TypeError(".jlwa.ComboStageInfo.UpgradeVec: array expected");
                message.UpgradeVec = [];
                for (let i = 0; i < object.UpgradeVec.length; ++i)
                    message.UpgradeVec[i] = object.UpgradeVec[i] | 0;
            }
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {jlwa.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.UpgradeVec = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ComboLevel = 0;
                object.Mult = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlwa.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.ComboLevel != null && message.hasOwnProperty("ComboLevel"))
                object.ComboLevel = message.ComboLevel;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlwa.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlwa.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.UpgradeVec && message.UpgradeVec.length) {
                object.UpgradeVec = [];
                for (let j = 0; j < message.UpgradeVec.length; ++j)
                    object.UpgradeVec[j] = message.UpgradeVec[j];
            }
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlwa.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlwa.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwa.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlwa.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlwa
         * @interface IRoundInfo
         * @property {Array.<jlwa.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [AddRound] RoundInfo AddRound
         * @property {boolean|null} [Plus] RoundInfo Plus
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlwa
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlwa.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlwa.IComboStageInfo>} ComboStageData
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo AddRound.
         * @member {number} AddRound
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRound = 0;

        /**
         * RoundInfo Plus.
         * @member {boolean} Plus
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Plus = false;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlwa.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlwa.RoundInfo
         * @static
         * @param {jlwa.IRoundInfo=} [properties] Properties to set
         * @returns {jlwa.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlwa.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwa.RoundInfo
         * @static
         * @param {jlwa.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlwa.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRemainRound);
            if (message.AddRound != null && Object.hasOwnProperty.call(message, "AddRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AddRound);
            if (message.Plus != null && Object.hasOwnProperty.call(message, "Plus"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.Plus);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlwa.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwa.RoundInfo
         * @static
         * @param {jlwa.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwa.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwa.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwa.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlwa.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.AddRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.Plus = reader.bool();
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwa.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwa.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlwa.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlwa.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                if (!$util.isInteger(message.AddRound))
                    return "AddRound: integer expected";
            if (message.Plus != null && message.hasOwnProperty("Plus"))
                if (typeof message.Plus !== "boolean")
                    return "Plus: boolean expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwa.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwa.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwa.RoundInfo)
                return object;
            let message = new $root.jlwa.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlwa.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlwa.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlwa.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.AddRound != null)
                message.AddRound = object.AddRound | 0;
            if (object.Plus != null)
                message.Plus = Boolean(object.Plus);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwa.RoundInfo
         * @static
         * @param {jlwa.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.AddRound = 0;
                object.Plus = false;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlwa.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.AddRound != null && message.hasOwnProperty("AddRound"))
                object.AddRound = message.AddRound;
            if (message.Plus != null && message.hasOwnProperty("Plus"))
                object.Plus = message.Plus;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlwa.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlwa.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwa.RoundInfo";
        };

        return RoundInfo;
    })();

    jlwa.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlwa
         * @interface ISpinAck
         * @property {Array.<jlwa.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlwa
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlwa.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlwa.IRoundInfo>} RoundQueue
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlwa.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlwa.SpinAck
         * @static
         * @param {jlwa.ISpinAck=} [properties] Properties to set
         * @returns {jlwa.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlwa.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlwa.SpinAck
         * @static
         * @param {jlwa.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlwa.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlwa.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwa.SpinAck
         * @static
         * @param {jlwa.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwa.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwa.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwa.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlwa.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwa.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwa.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlwa.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlwa.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwa.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwa.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwa.SpinAck)
                return object;
            let message = new $root.jlwa.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlwa.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlwa.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlwa.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwa.SpinAck
         * @static
         * @param {jlwa.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlwa.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlwa.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlwa.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwa.SpinAck";
        };

        return SpinAck;
    })();

    jlwa.GameInfoData = (function() {

        /**
         * Properties of a GameInfoData.
         * @memberof jlwa
         * @interface IGameInfoData
         * @property {boolean|null} [ShowExtra] GameInfoData ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoData Mul
         * @property {Array.<jlwa.IColumn>|null} [ComboSetting] GameInfoData ComboSetting
         */

        /**
         * Constructs a new GameInfoData.
         * @memberof jlwa
         * @classdesc Represents a GameInfoData.
         * @implements IGameInfoData
         * @constructor
         * @param {jlwa.IGameInfoData=} [properties] Properties to set
         */
        function GameInfoData(properties) {
            this.Mul = [];
            this.ComboSetting = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoData ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlwa.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ShowExtra = false;

        /**
         * GameInfoData Mul.
         * @member {Array.<number>} Mul
         * @memberof jlwa.GameInfoData
         * @instance
         */
        GameInfoData.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoData ComboSetting.
         * @member {Array.<jlwa.IColumn>} ComboSetting
         * @memberof jlwa.GameInfoData
         * @instance
         */
        GameInfoData.prototype.ComboSetting = $util.emptyArray;

        /**
         * Creates a new GameInfoData instance using the specified properties.
         * @function create
         * @memberof jlwa.GameInfoData
         * @static
         * @param {jlwa.IGameInfoData=} [properties] Properties to set
         * @returns {jlwa.GameInfoData} GameInfoData instance
         */
        GameInfoData.create = function create(properties) {
            return new GameInfoData(properties);
        };

        /**
         * Encodes the specified GameInfoData message. Does not implicitly {@link jlwa.GameInfoData.verify|verify} messages.
         * @function encode
         * @memberof jlwa.GameInfoData
         * @static
         * @param {jlwa.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.ComboSetting != null && message.ComboSetting.length)
                for (let i = 0; i < message.ComboSetting.length; ++i)
                    $root.jlwa.Column.encode(message.ComboSetting[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameInfoData message, length delimited. Does not implicitly {@link jlwa.GameInfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwa.GameInfoData
         * @static
         * @param {jlwa.IGameInfoData} message GameInfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwa.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwa.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwa.GameInfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.ComboSetting && message.ComboSetting.length))
                            message.ComboSetting = [];
                        message.ComboSetting.push($root.jlwa.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwa.GameInfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwa.GameInfoData} GameInfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoData message.
         * @function verify
         * @memberof jlwa.GameInfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.ComboSetting != null && message.hasOwnProperty("ComboSetting")) {
                if (!Array.isArray(message.ComboSetting))
                    return "ComboSetting: array expected";
                for (let i = 0; i < message.ComboSetting.length; ++i) {
                    let error = $root.jlwa.Column.verify(message.ComboSetting[i]);
                    if (error)
                        return "ComboSetting." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GameInfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwa.GameInfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwa.GameInfoData} GameInfoData
         */
        GameInfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwa.GameInfoData)
                return object;
            let message = new $root.jlwa.GameInfoData();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlwa.GameInfoData.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.ComboSetting) {
                if (!Array.isArray(object.ComboSetting))
                    throw TypeError(".jlwa.GameInfoData.ComboSetting: array expected");
                message.ComboSetting = [];
                for (let i = 0; i < object.ComboSetting.length; ++i) {
                    if (typeof object.ComboSetting[i] !== "object")
                        throw TypeError(".jlwa.GameInfoData.ComboSetting: object expected");
                    message.ComboSetting[i] = $root.jlwa.Column.fromObject(object.ComboSetting[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwa.GameInfoData
         * @static
         * @param {jlwa.GameInfoData} message GameInfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.ComboSetting = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.ComboSetting && message.ComboSetting.length) {
                object.ComboSetting = [];
                for (let j = 0; j < message.ComboSetting.length; ++j)
                    object.ComboSetting[j] = $root.jlwa.Column.toObject(message.ComboSetting[j], options);
            }
            return object;
        };

        /**
         * Converts this GameInfoData to JSON.
         * @function toJSON
         * @memberof jlwa.GameInfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoData
         * @function getTypeUrl
         * @memberof jlwa.GameInfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwa.GameInfoData";
        };

        return GameInfoData;
    })();

    return jlwa;
})();

export const jlwc = $root.jlwc = (() => {

    /**
     * Namespace jlwc.
     * @exports jlwc
     * @namespace
     */
    const jlwc = {};

    jlwc.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlwc
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlwc
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlwc.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlwc.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlwc.Column
         * @static
         * @param {jlwc.IColumn=} [properties] Properties to set
         * @returns {jlwc.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlwc.Column.verify|verify} messages.
         * @function encode
         * @memberof jlwc.Column
         * @static
         * @param {jlwc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlwc.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwc.Column
         * @static
         * @param {jlwc.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlwc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwc.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwc.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwc.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlwc.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwc.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwc.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwc.Column)
                return object;
            let message = new $root.jlwc.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlwc.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwc.Column
         * @static
         * @param {jlwc.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlwc.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlwc.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwc.Column";
        };

        return Column;
    })();

    jlwc.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlwc
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlwc
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlwc.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlwc.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlwc.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlwc.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlwc.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlwc.AwardData
         * @static
         * @param {jlwc.IAwardData=} [properties] Properties to set
         * @returns {jlwc.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlwc.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlwc.AwardData
         * @static
         * @param {jlwc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlwc.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwc.AwardData
         * @static
         * @param {jlwc.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwc.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwc.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwc.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlwc.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwc.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwc.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwc.AwardData)
                return object;
            let message = new $root.jlwc.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwc.AwardData
         * @static
         * @param {jlwc.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlwc.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlwc.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwc.AwardData";
        };

        return AwardData;
    })();

    jlwc.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlwc
         * @interface IRoundInfo
         * @property {Array.<jlwc.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlwc.IColumn>|null} [PlateSymbolReal] RoundInfo PlateSymbolReal
         * @property {Array.<jlwc.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [Mult] RoundInfo Mult
         * @property {number|null} [TransSymbol] RoundInfo TransSymbol
         * @property {number|null} [TransSymbolPos] RoundInfo TransSymbolPos
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlwc
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlwc.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolReal = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlwc.IColumn>} PlateSymbol
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolReal.
         * @member {Array.<jlwc.IColumn>} PlateSymbolReal
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolReal = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlwc.IAwardData>} AwardDataVec
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo Mult.
         * @member {number} Mult
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.Mult = 0;

        /**
         * RoundInfo TransSymbol.
         * @member {number} TransSymbol
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.TransSymbol = 0;

        /**
         * RoundInfo TransSymbolPos.
         * @member {number} TransSymbolPos
         * @memberof jlwc.RoundInfo
         * @instance
         */
        RoundInfo.prototype.TransSymbolPos = 0;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlwc.RoundInfo
         * @static
         * @param {jlwc.IRoundInfo=} [properties] Properties to set
         * @returns {jlwc.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlwc.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwc.RoundInfo
         * @static
         * @param {jlwc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlwc.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolReal != null && message.PlateSymbolReal.length)
                for (let i = 0; i < message.PlateSymbolReal.length; ++i)
                    $root.jlwc.Column.encode(message.PlateSymbolReal[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlwc.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.RoundWin);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.Mult);
            if (message.TransSymbol != null && Object.hasOwnProperty.call(message, "TransSymbol"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.TransSymbol);
            if (message.TransSymbolPos != null && Object.hasOwnProperty.call(message, "TransSymbolPos"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.TransSymbolPos);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlwc.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwc.RoundInfo
         * @static
         * @param {jlwc.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwc.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlwc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolReal && message.PlateSymbolReal.length))
                            message.PlateSymbolReal = [];
                        message.PlateSymbolReal.push($root.jlwc.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlwc.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 7: {
                        message.Mult = reader.double();
                        break;
                    }
                case 8: {
                        message.TransSymbol = reader.int32();
                        break;
                    }
                case 9: {
                        message.TransSymbolPos = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwc.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwc.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlwc.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlwc.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolReal != null && message.hasOwnProperty("PlateSymbolReal")) {
                if (!Array.isArray(message.PlateSymbolReal))
                    return "PlateSymbolReal: array expected";
                for (let i = 0; i < message.PlateSymbolReal.length; ++i) {
                    let error = $root.jlwc.Column.verify(message.PlateSymbolReal[i]);
                    if (error)
                        return "PlateSymbolReal." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlwc.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            if (message.TransSymbol != null && message.hasOwnProperty("TransSymbol"))
                if (!$util.isInteger(message.TransSymbol))
                    return "TransSymbol: integer expected";
            if (message.TransSymbolPos != null && message.hasOwnProperty("TransSymbolPos"))
                if (!$util.isInteger(message.TransSymbolPos))
                    return "TransSymbolPos: integer expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwc.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwc.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwc.RoundInfo)
                return object;
            let message = new $root.jlwc.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlwc.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlwc.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlwc.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolReal) {
                if (!Array.isArray(object.PlateSymbolReal))
                    throw TypeError(".jlwc.RoundInfo.PlateSymbolReal: array expected");
                message.PlateSymbolReal = [];
                for (let i = 0; i < object.PlateSymbolReal.length; ++i) {
                    if (typeof object.PlateSymbolReal[i] !== "object")
                        throw TypeError(".jlwc.RoundInfo.PlateSymbolReal: object expected");
                    message.PlateSymbolReal[i] = $root.jlwc.Column.fromObject(object.PlateSymbolReal[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlwc.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlwc.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlwc.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            if (object.TransSymbol != null)
                message.TransSymbol = object.TransSymbol | 0;
            if (object.TransSymbolPos != null)
                message.TransSymbolPos = object.TransSymbolPos | 0;
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwc.RoundInfo
         * @static
         * @param {jlwc.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolReal = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.RoundWin = 0;
                object.Mult = 0;
                object.TransSymbol = 0;
                object.TransSymbolPos = 0;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlwc.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolReal && message.PlateSymbolReal.length) {
                object.PlateSymbolReal = [];
                for (let j = 0; j < message.PlateSymbolReal.length; ++j)
                    object.PlateSymbolReal[j] = $root.jlwc.Column.toObject(message.PlateSymbolReal[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlwc.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            if (message.TransSymbol != null && message.hasOwnProperty("TransSymbol"))
                object.TransSymbol = message.TransSymbol;
            if (message.TransSymbolPos != null && message.hasOwnProperty("TransSymbolPos"))
                object.TransSymbolPos = message.TransSymbolPos;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlwc.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlwc.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwc.RoundInfo";
        };

        return RoundInfo;
    })();

    jlwc.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlwc
         * @interface ISpinAck
         * @property {Array.<jlwc.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {boolean|null} [Extra] SpinAck Extra
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlwc
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlwc.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlwc.IRoundInfo>} RoundQueue
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck Extra.
         * @member {boolean} Extra
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.Extra = false;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlwc.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlwc.SpinAck
         * @static
         * @param {jlwc.ISpinAck=} [properties] Properties to set
         * @returns {jlwc.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlwc.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlwc.SpinAck
         * @static
         * @param {jlwc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlwc.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.Extra);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlwc.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwc.SpinAck
         * @static
         * @param {jlwc.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwc.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlwc.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.Extra = reader.bool();
                        break;
                    }
                case 8: {
                        message.RTP = reader.double();
                        break;
                    }
                case 9: {
                        message.LID = reader.int32();
                        break;
                    }
                case 10: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwc.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwc.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlwc.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlwc.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (typeof message.Extra !== "boolean")
                    return "Extra: boolean expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwc.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwc.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwc.SpinAck)
                return object;
            let message = new $root.jlwc.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlwc.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlwc.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlwc.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.Extra != null)
                message.Extra = Boolean(object.Extra);
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwc.SpinAck
         * @static
         * @param {jlwc.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.Extra = false;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlwc.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlwc.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlwc.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwc.SpinAck";
        };

        return SpinAck;
    })();

    jlwc.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlwc
         * @interface IGameInfoAck
         * @property {boolean|null} [ShowExtra] GameInfoAck ShowExtra
         * @property {Array.<number>|null} [Mul] GameInfoAck Mul
         * @property {Array.<number>|null} [Football] GameInfoAck Football
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlwc
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlwc.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.Mul = [];
            this.Football = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck ShowExtra.
         * @member {boolean} ShowExtra
         * @memberof jlwc.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.ShowExtra = false;

        /**
         * GameInfoAck Mul.
         * @member {Array.<number>} Mul
         * @memberof jlwc.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Mul = $util.emptyArray;

        /**
         * GameInfoAck Football.
         * @member {Array.<number>} Football
         * @memberof jlwc.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Football = $util.emptyArray;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {jlwc.IGameInfoAck=} [properties] Properties to set
         * @returns {jlwc.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlwc.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {jlwc.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ShowExtra != null && Object.hasOwnProperty.call(message, "ShowExtra"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ShowExtra);
            if (message.Mul != null && message.Mul.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.Mul.length; ++i)
                    writer.double(message.Mul[i]);
                writer.ldelim();
            }
            if (message.Football != null && message.Football.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.Football.length; ++i)
                    writer.double(message.Football[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlwc.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {jlwc.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwc.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwc.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ShowExtra = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.Mul && message.Mul.length))
                            message.Mul = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Mul.push(reader.double());
                        } else
                            message.Mul.push(reader.double());
                        break;
                    }
                case 3: {
                        if (!(message.Football && message.Football.length))
                            message.Football = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Football.push(reader.double());
                        } else
                            message.Football.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwc.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                if (typeof message.ShowExtra !== "boolean")
                    return "ShowExtra: boolean expected";
            if (message.Mul != null && message.hasOwnProperty("Mul")) {
                if (!Array.isArray(message.Mul))
                    return "Mul: array expected";
                for (let i = 0; i < message.Mul.length; ++i)
                    if (typeof message.Mul[i] !== "number")
                        return "Mul: number[] expected";
            }
            if (message.Football != null && message.hasOwnProperty("Football")) {
                if (!Array.isArray(message.Football))
                    return "Football: array expected";
                for (let i = 0; i < message.Football.length; ++i)
                    if (typeof message.Football[i] !== "number")
                        return "Football: number[] expected";
            }
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwc.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwc.GameInfoAck)
                return object;
            let message = new $root.jlwc.GameInfoAck();
            if (object.ShowExtra != null)
                message.ShowExtra = Boolean(object.ShowExtra);
            if (object.Mul) {
                if (!Array.isArray(object.Mul))
                    throw TypeError(".jlwc.GameInfoAck.Mul: array expected");
                message.Mul = [];
                for (let i = 0; i < object.Mul.length; ++i)
                    message.Mul[i] = Number(object.Mul[i]);
            }
            if (object.Football) {
                if (!Array.isArray(object.Football))
                    throw TypeError(".jlwc.GameInfoAck.Football: array expected");
                message.Football = [];
                for (let i = 0; i < object.Football.length; ++i)
                    message.Football[i] = Number(object.Football[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {jlwc.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Mul = [];
                object.Football = [];
            }
            if (options.defaults)
                object.ShowExtra = false;
            if (message.ShowExtra != null && message.hasOwnProperty("ShowExtra"))
                object.ShowExtra = message.ShowExtra;
            if (message.Mul && message.Mul.length) {
                object.Mul = [];
                for (let j = 0; j < message.Mul.length; ++j)
                    object.Mul[j] = options.json && !isFinite(message.Mul[j]) ? String(message.Mul[j]) : message.Mul[j];
            }
            if (message.Football && message.Football.length) {
                object.Football = [];
                for (let j = 0; j < message.Football.length; ++j)
                    object.Football[j] = options.json && !isFinite(message.Football[j]) ? String(message.Football[j]) : message.Football[j];
            }
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlwc.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlwc.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwc.GameInfoAck";
        };

        return GameInfoAck;
    })();

    return jlwc;
})();

export const jlwn = $root.jlwn = (() => {

    /**
     * Namespace jlwn.
     * @exports jlwn
     * @namespace
     */
    const jlwn = {};

    jlwn.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlwn
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlwn
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlwn.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlwn.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlwn.Column
         * @static
         * @param {jlwn.IColumn=} [properties] Properties to set
         * @returns {jlwn.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlwn.Column.verify|verify} messages.
         * @function encode
         * @memberof jlwn.Column
         * @static
         * @param {jlwn.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlwn.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwn.Column
         * @static
         * @param {jlwn.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlwn.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwn.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwn.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwn.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwn.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlwn.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwn.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwn.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwn.Column)
                return object;
            let message = new $root.jlwn.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlwn.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwn.Column
         * @static
         * @param {jlwn.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlwn.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlwn.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwn.Column";
        };

        return Column;
    })();

    jlwn.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlwn
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Win] AwardData Win
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlwn
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlwn.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlwn.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlwn.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlwn.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlwn.AwardData
         * @static
         * @param {jlwn.IAwardData=} [properties] Properties to set
         * @returns {jlwn.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlwn.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlwn.AwardData
         * @static
         * @param {jlwn.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.Win);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlwn.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwn.AwardData
         * @static
         * @param {jlwn.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwn.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwn.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwn.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwn.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwn.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlwn.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwn.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwn.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwn.AwardData)
                return object;
            let message = new $root.jlwn.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwn.AwardData
         * @static
         * @param {jlwn.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlwn.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlwn.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwn.AwardData";
        };

        return AwardData;
    })();

    jlwn.SinglePlate = (function() {

        /**
         * Properties of a SinglePlate.
         * @memberof jlwn
         * @interface ISinglePlate
         * @property {Array.<jlwn.IColumn>|null} [Symbol] SinglePlate Symbol
         * @property {number|null} [SingleWin] SinglePlate SingleWin
         * @property {Array.<jlwn.IAwardData>|null} [AwardDataVec] SinglePlate AwardDataVec
         * @property {number|null} [CrystalType] SinglePlate CrystalType
         * @property {boolean|null} [NeedEliminate] SinglePlate NeedEliminate
         * @property {Array.<jlwn.IColumn>|null} [Eliminate] SinglePlate Eliminate
         */

        /**
         * Constructs a new SinglePlate.
         * @memberof jlwn
         * @classdesc Represents a SinglePlate.
         * @implements ISinglePlate
         * @constructor
         * @param {jlwn.ISinglePlate=} [properties] Properties to set
         */
        function SinglePlate(properties) {
            this.Symbol = [];
            this.AwardDataVec = [];
            this.Eliminate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SinglePlate Symbol.
         * @member {Array.<jlwn.IColumn>} Symbol
         * @memberof jlwn.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Symbol = $util.emptyArray;

        /**
         * SinglePlate SingleWin.
         * @member {number} SingleWin
         * @memberof jlwn.SinglePlate
         * @instance
         */
        SinglePlate.prototype.SingleWin = 0;

        /**
         * SinglePlate AwardDataVec.
         * @member {Array.<jlwn.IAwardData>} AwardDataVec
         * @memberof jlwn.SinglePlate
         * @instance
         */
        SinglePlate.prototype.AwardDataVec = $util.emptyArray;

        /**
         * SinglePlate CrystalType.
         * @member {number} CrystalType
         * @memberof jlwn.SinglePlate
         * @instance
         */
        SinglePlate.prototype.CrystalType = 0;

        /**
         * SinglePlate NeedEliminate.
         * @member {boolean} NeedEliminate
         * @memberof jlwn.SinglePlate
         * @instance
         */
        SinglePlate.prototype.NeedEliminate = false;

        /**
         * SinglePlate Eliminate.
         * @member {Array.<jlwn.IColumn>} Eliminate
         * @memberof jlwn.SinglePlate
         * @instance
         */
        SinglePlate.prototype.Eliminate = $util.emptyArray;

        /**
         * Creates a new SinglePlate instance using the specified properties.
         * @function create
         * @memberof jlwn.SinglePlate
         * @static
         * @param {jlwn.ISinglePlate=} [properties] Properties to set
         * @returns {jlwn.SinglePlate} SinglePlate instance
         */
        SinglePlate.create = function create(properties) {
            return new SinglePlate(properties);
        };

        /**
         * Encodes the specified SinglePlate message. Does not implicitly {@link jlwn.SinglePlate.verify|verify} messages.
         * @function encode
         * @memberof jlwn.SinglePlate
         * @static
         * @param {jlwn.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && message.Symbol.length)
                for (let i = 0; i < message.Symbol.length; ++i)
                    $root.jlwn.Column.encode(message.Symbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.SingleWin != null && Object.hasOwnProperty.call(message, "SingleWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.SingleWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlwn.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.CrystalType != null && Object.hasOwnProperty.call(message, "CrystalType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.CrystalType);
            if (message.NeedEliminate != null && Object.hasOwnProperty.call(message, "NeedEliminate"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.NeedEliminate);
            if (message.Eliminate != null && message.Eliminate.length)
                for (let i = 0; i < message.Eliminate.length; ++i)
                    $root.jlwn.Column.encode(message.Eliminate[i], writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified SinglePlate message, length delimited. Does not implicitly {@link jlwn.SinglePlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwn.SinglePlate
         * @static
         * @param {jlwn.ISinglePlate} message SinglePlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SinglePlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlwn.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwn.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwn.SinglePlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Symbol && message.Symbol.length))
                            message.Symbol = [];
                        message.Symbol.push($root.jlwn.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.SingleWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlwn.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.CrystalType = reader.int32();
                        break;
                    }
                case 5: {
                        message.NeedEliminate = reader.bool();
                        break;
                    }
                case 6: {
                        if (!(message.Eliminate && message.Eliminate.length))
                            message.Eliminate = [];
                        message.Eliminate.push($root.jlwn.Column.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SinglePlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwn.SinglePlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwn.SinglePlate} SinglePlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SinglePlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SinglePlate message.
         * @function verify
         * @memberof jlwn.SinglePlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SinglePlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol")) {
                if (!Array.isArray(message.Symbol))
                    return "Symbol: array expected";
                for (let i = 0; i < message.Symbol.length; ++i) {
                    let error = $root.jlwn.Column.verify(message.Symbol[i]);
                    if (error)
                        return "Symbol." + error;
                }
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                if (typeof message.SingleWin !== "number")
                    return "SingleWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlwn.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.CrystalType != null && message.hasOwnProperty("CrystalType"))
                if (!$util.isInteger(message.CrystalType))
                    return "CrystalType: integer expected";
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                if (typeof message.NeedEliminate !== "boolean")
                    return "NeedEliminate: boolean expected";
            if (message.Eliminate != null && message.hasOwnProperty("Eliminate")) {
                if (!Array.isArray(message.Eliminate))
                    return "Eliminate: array expected";
                for (let i = 0; i < message.Eliminate.length; ++i) {
                    let error = $root.jlwn.Column.verify(message.Eliminate[i]);
                    if (error)
                        return "Eliminate." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SinglePlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwn.SinglePlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwn.SinglePlate} SinglePlate
         */
        SinglePlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwn.SinglePlate)
                return object;
            let message = new $root.jlwn.SinglePlate();
            if (object.Symbol) {
                if (!Array.isArray(object.Symbol))
                    throw TypeError(".jlwn.SinglePlate.Symbol: array expected");
                message.Symbol = [];
                for (let i = 0; i < object.Symbol.length; ++i) {
                    if (typeof object.Symbol[i] !== "object")
                        throw TypeError(".jlwn.SinglePlate.Symbol: object expected");
                    message.Symbol[i] = $root.jlwn.Column.fromObject(object.Symbol[i]);
                }
            }
            if (object.SingleWin != null)
                message.SingleWin = Number(object.SingleWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlwn.SinglePlate.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlwn.SinglePlate.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlwn.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.CrystalType != null)
                message.CrystalType = object.CrystalType | 0;
            if (object.NeedEliminate != null)
                message.NeedEliminate = Boolean(object.NeedEliminate);
            if (object.Eliminate) {
                if (!Array.isArray(object.Eliminate))
                    throw TypeError(".jlwn.SinglePlate.Eliminate: array expected");
                message.Eliminate = [];
                for (let i = 0; i < object.Eliminate.length; ++i) {
                    if (typeof object.Eliminate[i] !== "object")
                        throw TypeError(".jlwn.SinglePlate.Eliminate: object expected");
                    message.Eliminate[i] = $root.jlwn.Column.fromObject(object.Eliminate[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SinglePlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwn.SinglePlate
         * @static
         * @param {jlwn.SinglePlate} message SinglePlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SinglePlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.Symbol = [];
                object.AwardDataVec = [];
                object.Eliminate = [];
            }
            if (options.defaults) {
                object.SingleWin = 0;
                object.CrystalType = 0;
                object.NeedEliminate = false;
            }
            if (message.Symbol && message.Symbol.length) {
                object.Symbol = [];
                for (let j = 0; j < message.Symbol.length; ++j)
                    object.Symbol[j] = $root.jlwn.Column.toObject(message.Symbol[j], options);
            }
            if (message.SingleWin != null && message.hasOwnProperty("SingleWin"))
                object.SingleWin = options.json && !isFinite(message.SingleWin) ? String(message.SingleWin) : message.SingleWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlwn.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.CrystalType != null && message.hasOwnProperty("CrystalType"))
                object.CrystalType = message.CrystalType;
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                object.NeedEliminate = message.NeedEliminate;
            if (message.Eliminate && message.Eliminate.length) {
                object.Eliminate = [];
                for (let j = 0; j < message.Eliminate.length; ++j)
                    object.Eliminate[j] = $root.jlwn.Column.toObject(message.Eliminate[j], options);
            }
            return object;
        };

        /**
         * Converts this SinglePlate to JSON.
         * @function toJSON
         * @memberof jlwn.SinglePlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SinglePlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SinglePlate
         * @function getTypeUrl
         * @memberof jlwn.SinglePlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SinglePlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwn.SinglePlate";
        };

        return SinglePlate;
    })();

    jlwn.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlwn
         * @interface IRoundInfo
         * @property {Array.<jlwn.ISinglePlate>|null} [PlateQueue] RoundInfo PlateQueue
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {Array.<number>|null} [RoundMulShow] RoundInfo RoundMulShow
         * @property {number|null} [RoundMul] RoundInfo RoundMul
         * @property {number|null} [EliminateCount] RoundInfo EliminateCount
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [ScatterCount] RoundInfo ScatterCount
         * @property {number|null} [ScatterWin] RoundInfo ScatterWin
         * @property {boolean|null} [GetFree] RoundInfo GetFree
         * @property {number|null} [GetFreeRound] RoundInfo GetFreeRound
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlwn
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlwn.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateQueue = [];
            this.RoundMulShow = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateQueue.
         * @member {Array.<jlwn.ISinglePlate>} PlateQueue
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateQueue = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo RoundMulShow.
         * @member {Array.<number>} RoundMulShow
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundMulShow = $util.emptyArray;

        /**
         * RoundInfo RoundMul.
         * @member {number} RoundMul
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundMul = 0;

        /**
         * RoundInfo EliminateCount.
         * @member {number} EliminateCount
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.EliminateCount = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterCount = 0;

        /**
         * RoundInfo ScatterWin.
         * @member {number} ScatterWin
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ScatterWin = 0;

        /**
         * RoundInfo GetFree.
         * @member {boolean} GetFree
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetFree = false;

        /**
         * RoundInfo GetFreeRound.
         * @member {number} GetFreeRound
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.GetFreeRound = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlwn.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlwn.RoundInfo
         * @static
         * @param {jlwn.IRoundInfo=} [properties] Properties to set
         * @returns {jlwn.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlwn.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwn.RoundInfo
         * @static
         * @param {jlwn.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateQueue != null && message.PlateQueue.length)
                for (let i = 0; i < message.PlateQueue.length; ++i)
                    $root.jlwn.SinglePlate.encode(message.PlateQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.RoundMulShow != null && message.RoundMulShow.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.RoundMulShow.length; ++i)
                    writer.double(message.RoundMulShow[i]);
                writer.ldelim();
            }
            if (message.RoundMul != null && Object.hasOwnProperty.call(message, "RoundMul"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.RoundMul);
            if (message.EliminateCount != null && Object.hasOwnProperty.call(message, "EliminateCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.EliminateCount);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.ScatterCount);
            if (message.ScatterWin != null && Object.hasOwnProperty.call(message, "ScatterWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.ScatterWin);
            if (message.GetFree != null && Object.hasOwnProperty.call(message, "GetFree"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.GetFree);
            if (message.GetFreeRound != null && Object.hasOwnProperty.call(message, "GetFreeRound"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.GetFreeRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlwn.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwn.RoundInfo
         * @static
         * @param {jlwn.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwn.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwn.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwn.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateQueue && message.PlateQueue.length))
                            message.PlateQueue = [];
                        message.PlateQueue.push($root.jlwn.SinglePlate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.RoundMulShow && message.RoundMulShow.length))
                            message.RoundMulShow = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.RoundMulShow.push(reader.double());
                        } else
                            message.RoundMulShow.push(reader.double());
                        break;
                    }
                case 4: {
                        message.RoundMul = reader.double();
                        break;
                    }
                case 5: {
                        message.EliminateCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 8: {
                        message.ScatterWin = reader.double();
                        break;
                    }
                case 9: {
                        message.GetFree = reader.bool();
                        break;
                    }
                case 10: {
                        message.GetFreeRound = reader.int32();
                        break;
                    }
                case 11: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwn.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwn.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlwn.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateQueue != null && message.hasOwnProperty("PlateQueue")) {
                if (!Array.isArray(message.PlateQueue))
                    return "PlateQueue: array expected";
                for (let i = 0; i < message.PlateQueue.length; ++i) {
                    let error = $root.jlwn.SinglePlate.verify(message.PlateQueue[i]);
                    if (error)
                        return "PlateQueue." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.RoundMulShow != null && message.hasOwnProperty("RoundMulShow")) {
                if (!Array.isArray(message.RoundMulShow))
                    return "RoundMulShow: array expected";
                for (let i = 0; i < message.RoundMulShow.length; ++i)
                    if (typeof message.RoundMulShow[i] !== "number")
                        return "RoundMulShow: number[] expected";
            }
            if (message.RoundMul != null && message.hasOwnProperty("RoundMul"))
                if (typeof message.RoundMul !== "number")
                    return "RoundMul: number expected";
            if (message.EliminateCount != null && message.hasOwnProperty("EliminateCount"))
                if (!$util.isInteger(message.EliminateCount))
                    return "EliminateCount: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                if (typeof message.ScatterWin !== "number")
                    return "ScatterWin: number expected";
            if (message.GetFree != null && message.hasOwnProperty("GetFree"))
                if (typeof message.GetFree !== "boolean")
                    return "GetFree: boolean expected";
            if (message.GetFreeRound != null && message.hasOwnProperty("GetFreeRound"))
                if (!$util.isInteger(message.GetFreeRound))
                    return "GetFreeRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwn.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwn.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwn.RoundInfo)
                return object;
            let message = new $root.jlwn.RoundInfo();
            if (object.PlateQueue) {
                if (!Array.isArray(object.PlateQueue))
                    throw TypeError(".jlwn.RoundInfo.PlateQueue: array expected");
                message.PlateQueue = [];
                for (let i = 0; i < object.PlateQueue.length; ++i) {
                    if (typeof object.PlateQueue[i] !== "object")
                        throw TypeError(".jlwn.RoundInfo.PlateQueue: object expected");
                    message.PlateQueue[i] = $root.jlwn.SinglePlate.fromObject(object.PlateQueue[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.RoundMulShow) {
                if (!Array.isArray(object.RoundMulShow))
                    throw TypeError(".jlwn.RoundInfo.RoundMulShow: array expected");
                message.RoundMulShow = [];
                for (let i = 0; i < object.RoundMulShow.length; ++i)
                    message.RoundMulShow[i] = Number(object.RoundMulShow[i]);
            }
            if (object.RoundMul != null)
                message.RoundMul = Number(object.RoundMul);
            if (object.EliminateCount != null)
                message.EliminateCount = object.EliminateCount | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.ScatterWin != null)
                message.ScatterWin = Number(object.ScatterWin);
            if (object.GetFree != null)
                message.GetFree = Boolean(object.GetFree);
            if (object.GetFreeRound != null)
                message.GetFreeRound = object.GetFreeRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwn.RoundInfo
         * @static
         * @param {jlwn.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateQueue = [];
                object.RoundMulShow = [];
            }
            if (options.defaults) {
                object.RoundWin = 0;
                object.RoundMul = 0;
                object.EliminateCount = 0;
                object.AwardTypeFlag = 0;
                object.ScatterCount = 0;
                object.ScatterWin = 0;
                object.GetFree = false;
                object.GetFreeRound = 0;
                object.MaxFlag = false;
            }
            if (message.PlateQueue && message.PlateQueue.length) {
                object.PlateQueue = [];
                for (let j = 0; j < message.PlateQueue.length; ++j)
                    object.PlateQueue[j] = $root.jlwn.SinglePlate.toObject(message.PlateQueue[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.RoundMulShow && message.RoundMulShow.length) {
                object.RoundMulShow = [];
                for (let j = 0; j < message.RoundMulShow.length; ++j)
                    object.RoundMulShow[j] = options.json && !isFinite(message.RoundMulShow[j]) ? String(message.RoundMulShow[j]) : message.RoundMulShow[j];
            }
            if (message.RoundMul != null && message.hasOwnProperty("RoundMul"))
                object.RoundMul = options.json && !isFinite(message.RoundMul) ? String(message.RoundMul) : message.RoundMul;
            if (message.EliminateCount != null && message.hasOwnProperty("EliminateCount"))
                object.EliminateCount = message.EliminateCount;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.ScatterWin != null && message.hasOwnProperty("ScatterWin"))
                object.ScatterWin = options.json && !isFinite(message.ScatterWin) ? String(message.ScatterWin) : message.ScatterWin;
            if (message.GetFree != null && message.hasOwnProperty("GetFree"))
                object.GetFree = message.GetFree;
            if (message.GetFreeRound != null && message.hasOwnProperty("GetFreeRound"))
                object.GetFreeRound = message.GetFreeRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlwn.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlwn.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwn.RoundInfo";
        };

        return RoundInfo;
    })();

    jlwn.FreeData = (function() {

        /**
         * Properties of a FreeData.
         * @memberof jlwn
         * @interface IFreeData
         * @property {Array.<jlwn.IRoundInfo>|null} [FreeQueue] FreeData FreeQueue
         * @property {number|null} [FreeTotalWin] FreeData FreeTotalWin
         */

        /**
         * Constructs a new FreeData.
         * @memberof jlwn
         * @classdesc Represents a FreeData.
         * @implements IFreeData
         * @constructor
         * @param {jlwn.IFreeData=} [properties] Properties to set
         */
        function FreeData(properties) {
            this.FreeQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeData FreeQueue.
         * @member {Array.<jlwn.IRoundInfo>} FreeQueue
         * @memberof jlwn.FreeData
         * @instance
         */
        FreeData.prototype.FreeQueue = $util.emptyArray;

        /**
         * FreeData FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlwn.FreeData
         * @instance
         */
        FreeData.prototype.FreeTotalWin = 0;

        /**
         * Creates a new FreeData instance using the specified properties.
         * @function create
         * @memberof jlwn.FreeData
         * @static
         * @param {jlwn.IFreeData=} [properties] Properties to set
         * @returns {jlwn.FreeData} FreeData instance
         */
        FreeData.create = function create(properties) {
            return new FreeData(properties);
        };

        /**
         * Encodes the specified FreeData message. Does not implicitly {@link jlwn.FreeData.verify|verify} messages.
         * @function encode
         * @memberof jlwn.FreeData
         * @static
         * @param {jlwn.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeQueue != null && message.FreeQueue.length)
                for (let i = 0; i < message.FreeQueue.length; ++i)
                    $root.jlwn.RoundInfo.encode(message.FreeQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            return writer;
        };

        /**
         * Encodes the specified FreeData message, length delimited. Does not implicitly {@link jlwn.FreeData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwn.FreeData
         * @static
         * @param {jlwn.IFreeData} message FreeData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwn.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwn.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwn.FreeData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.FreeQueue && message.FreeQueue.length))
                            message.FreeQueue = [];
                        message.FreeQueue.push($root.jlwn.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwn.FreeData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwn.FreeData} FreeData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeData message.
         * @function verify
         * @memberof jlwn.FreeData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeQueue != null && message.hasOwnProperty("FreeQueue")) {
                if (!Array.isArray(message.FreeQueue))
                    return "FreeQueue: array expected";
                for (let i = 0; i < message.FreeQueue.length; ++i) {
                    let error = $root.jlwn.RoundInfo.verify(message.FreeQueue[i]);
                    if (error)
                        return "FreeQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            return null;
        };

        /**
         * Creates a FreeData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwn.FreeData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwn.FreeData} FreeData
         */
        FreeData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwn.FreeData)
                return object;
            let message = new $root.jlwn.FreeData();
            if (object.FreeQueue) {
                if (!Array.isArray(object.FreeQueue))
                    throw TypeError(".jlwn.FreeData.FreeQueue: array expected");
                message.FreeQueue = [];
                for (let i = 0; i < object.FreeQueue.length; ++i) {
                    if (typeof object.FreeQueue[i] !== "object")
                        throw TypeError(".jlwn.FreeData.FreeQueue: object expected");
                    message.FreeQueue[i] = $root.jlwn.RoundInfo.fromObject(object.FreeQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            return message;
        };

        /**
         * Creates a plain object from a FreeData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwn.FreeData
         * @static
         * @param {jlwn.FreeData} message FreeData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreeQueue = [];
            if (options.defaults)
                object.FreeTotalWin = 0;
            if (message.FreeQueue && message.FreeQueue.length) {
                object.FreeQueue = [];
                for (let j = 0; j < message.FreeQueue.length; ++j)
                    object.FreeQueue[j] = $root.jlwn.RoundInfo.toObject(message.FreeQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            return object;
        };

        /**
         * Converts this FreeData to JSON.
         * @function toJSON
         * @memberof jlwn.FreeData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeData
         * @function getTypeUrl
         * @memberof jlwn.FreeData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwn.FreeData";
        };

        return FreeData;
    })();

    jlwn.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlwn
         * @interface ISpinAck
         * @property {jlwn.IRoundInfo|null} [MainGame] SpinAck MainGame
         * @property {jlwn.IFreeData|null} [FreeGame] SpinAck FreeGame
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlwn
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlwn.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck MainGame.
         * @member {jlwn.IRoundInfo|null|undefined} MainGame
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.MainGame = null;

        /**
         * SpinAck FreeGame.
         * @member {jlwn.IFreeData|null|undefined} FreeGame
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeGame = null;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlwn.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlwn.SpinAck
         * @static
         * @param {jlwn.ISpinAck=} [properties] Properties to set
         * @returns {jlwn.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlwn.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlwn.SpinAck
         * @static
         * @param {jlwn.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.MainGame != null && Object.hasOwnProperty.call(message, "MainGame"))
                $root.jlwn.RoundInfo.encode(message.MainGame, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeGame != null && Object.hasOwnProperty.call(message, "FreeGame"))
                $root.jlwn.FreeData.encode(message.FreeGame, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlwn.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwn.SpinAck
         * @static
         * @param {jlwn.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwn.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwn.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwn.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.MainGame = $root.jlwn.RoundInfo.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.FreeGame = $root.jlwn.FreeData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwn.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwn.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlwn.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.MainGame != null && message.hasOwnProperty("MainGame")) {
                let error = $root.jlwn.RoundInfo.verify(message.MainGame);
                if (error)
                    return "MainGame." + error;
            }
            if (message.FreeGame != null && message.hasOwnProperty("FreeGame")) {
                let error = $root.jlwn.FreeData.verify(message.FreeGame);
                if (error)
                    return "FreeGame." + error;
            }
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwn.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwn.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwn.SpinAck)
                return object;
            let message = new $root.jlwn.SpinAck();
            if (object.MainGame != null) {
                if (typeof object.MainGame !== "object")
                    throw TypeError(".jlwn.SpinAck.MainGame: object expected");
                message.MainGame = $root.jlwn.RoundInfo.fromObject(object.MainGame);
            }
            if (object.FreeGame != null) {
                if (typeof object.FreeGame !== "object")
                    throw TypeError(".jlwn.SpinAck.FreeGame: object expected");
                message.FreeGame = $root.jlwn.FreeData.fromObject(object.FreeGame);
            }
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwn.SpinAck
         * @static
         * @param {jlwn.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.MainGame = null;
                object.FreeGame = null;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.MainGame != null && message.hasOwnProperty("MainGame"))
                object.MainGame = $root.jlwn.RoundInfo.toObject(message.MainGame, options);
            if (message.FreeGame != null && message.hasOwnProperty("FreeGame"))
                object.FreeGame = $root.jlwn.FreeData.toObject(message.FreeGame, options);
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlwn.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlwn.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwn.SpinAck";
        };

        return SpinAck;
    })();

    return jlwn;
})();

export const jlwod = $root.jlwod = (() => {

    /**
     * Namespace jlwod.
     * @exports jlwod
     * @namespace
     */
    const jlwod = {};

    jlwod.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlwod
         * @interface IColumn
         * @property {Array.<number>|null} [Row] Column Row
         */

        /**
         * Constructs a new Column.
         * @memberof jlwod
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlwod.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Row.
         * @member {Array.<number>} Row
         * @memberof jlwod.Column
         * @instance
         */
        Column.prototype.Row = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlwod.Column
         * @static
         * @param {jlwod.IColumn=} [properties] Properties to set
         * @returns {jlwod.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlwod.Column.verify|verify} messages.
         * @function encode
         * @memberof jlwod.Column
         * @static
         * @param {jlwod.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Row != null && message.Row.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Row.length; ++i)
                    writer.int32(message.Row[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlwod.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.Column
         * @static
         * @param {jlwod.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Row && message.Row.length))
                            message.Row = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Row.push(reader.int32());
                        } else
                            message.Row.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlwod.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Row != null && message.hasOwnProperty("Row")) {
                if (!Array.isArray(message.Row))
                    return "Row: array expected";
                for (let i = 0; i < message.Row.length; ++i)
                    if (!$util.isInteger(message.Row[i]))
                        return "Row: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.Column)
                return object;
            let message = new $root.jlwod.Column();
            if (object.Row) {
                if (!Array.isArray(object.Row))
                    throw TypeError(".jlwod.Column.Row: array expected");
                message.Row = [];
                for (let i = 0; i < object.Row.length; ++i)
                    message.Row[i] = object.Row[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.Column
         * @static
         * @param {jlwod.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Row = [];
            if (message.Row && message.Row.length) {
                object.Row = [];
                for (let j = 0; j < message.Row.length; ++j)
                    object.Row[j] = message.Row[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlwod.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlwod.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.Column";
        };

        return Column;
    })();

    jlwod.Grid = (function() {

        /**
         * Properties of a Grid.
         * @memberof jlwod
         * @interface IGrid
         * @property {number|null} [Column] Grid Column
         * @property {number|null} [Row] Grid Row
         */

        /**
         * Constructs a new Grid.
         * @memberof jlwod
         * @classdesc Represents a Grid.
         * @implements IGrid
         * @constructor
         * @param {jlwod.IGrid=} [properties] Properties to set
         */
        function Grid(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Grid Column.
         * @member {number} Column
         * @memberof jlwod.Grid
         * @instance
         */
        Grid.prototype.Column = 0;

        /**
         * Grid Row.
         * @member {number} Row
         * @memberof jlwod.Grid
         * @instance
         */
        Grid.prototype.Row = 0;

        /**
         * Creates a new Grid instance using the specified properties.
         * @function create
         * @memberof jlwod.Grid
         * @static
         * @param {jlwod.IGrid=} [properties] Properties to set
         * @returns {jlwod.Grid} Grid instance
         */
        Grid.create = function create(properties) {
            return new Grid(properties);
        };

        /**
         * Encodes the specified Grid message. Does not implicitly {@link jlwod.Grid.verify|verify} messages.
         * @function encode
         * @memberof jlwod.Grid
         * @static
         * @param {jlwod.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Column != null && Object.hasOwnProperty.call(message, "Column"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Column);
            if (message.Row != null && Object.hasOwnProperty.call(message, "Row"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Row);
            return writer;
        };

        /**
         * Encodes the specified Grid message, length delimited. Does not implicitly {@link jlwod.Grid.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.Grid
         * @static
         * @param {jlwod.IGrid} message Grid message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Grid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Grid message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.Grid();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Column = reader.int32();
                        break;
                    }
                case 2: {
                        message.Row = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Grid message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.Grid
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.Grid} Grid
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Grid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Grid message.
         * @function verify
         * @memberof jlwod.Grid
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Grid.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Column != null && message.hasOwnProperty("Column"))
                if (!$util.isInteger(message.Column))
                    return "Column: integer expected";
            if (message.Row != null && message.hasOwnProperty("Row"))
                if (!$util.isInteger(message.Row))
                    return "Row: integer expected";
            return null;
        };

        /**
         * Creates a Grid message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.Grid
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.Grid} Grid
         */
        Grid.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.Grid)
                return object;
            let message = new $root.jlwod.Grid();
            if (object.Column != null)
                message.Column = object.Column | 0;
            if (object.Row != null)
                message.Row = object.Row | 0;
            return message;
        };

        /**
         * Creates a plain object from a Grid message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.Grid
         * @static
         * @param {jlwod.Grid} message Grid
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Grid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Column = 0;
                object.Row = 0;
            }
            if (message.Column != null && message.hasOwnProperty("Column"))
                object.Column = message.Column;
            if (message.Row != null && message.hasOwnProperty("Row"))
                object.Row = message.Row;
            return object;
        };

        /**
         * Converts this Grid to JSON.
         * @function toJSON
         * @memberof jlwod.Grid
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Grid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Grid
         * @function getTypeUrl
         * @memberof jlwod.Grid
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Grid.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.Grid";
        };

        return Grid;
    })();

    jlwod.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlwod
         * @interface IAwardData
         * @property {boolean|null} [IsLine] AwardData IsLine
         * @property {boolean|null} [IsWild] AwardData IsWild
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {Array.<jlwod.IGrid>|null} [AwardGrid] AwardData AwardGrid
         * @property {number|null} [AwardMoney] AwardData AwardMoney
         * @property {number|null} [AwardCombo] AwardData AwardCombo
         * @property {number|null} [AwardCount] AwardData AwardCount
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlwod
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlwod.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.AwardGrid = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData IsLine.
         * @member {boolean} IsLine
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.IsLine = false;

        /**
         * AwardData IsWild.
         * @member {boolean} IsWild
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.IsWild = false;

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData AwardGrid.
         * @member {Array.<jlwod.IGrid>} AwardGrid
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.AwardGrid = $util.emptyArray;

        /**
         * AwardData AwardMoney.
         * @member {number} AwardMoney
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.AwardMoney = 0;

        /**
         * AwardData AwardCombo.
         * @member {number} AwardCombo
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.AwardCombo = 0;

        /**
         * AwardData AwardCount.
         * @member {number} AwardCount
         * @memberof jlwod.AwardData
         * @instance
         */
        AwardData.prototype.AwardCount = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlwod.AwardData
         * @static
         * @param {jlwod.IAwardData=} [properties] Properties to set
         * @returns {jlwod.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlwod.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlwod.AwardData
         * @static
         * @param {jlwod.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.IsLine != null && Object.hasOwnProperty.call(message, "IsLine"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.IsLine);
            if (message.IsWild != null && Object.hasOwnProperty.call(message, "IsWild"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.IsWild);
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Symbol);
            if (message.AwardGrid != null && message.AwardGrid.length)
                for (let i = 0; i < message.AwardGrid.length; ++i)
                    $root.jlwod.Grid.encode(message.AwardGrid[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.AwardMoney != null && Object.hasOwnProperty.call(message, "AwardMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.AwardMoney);
            if (message.AwardCombo != null && Object.hasOwnProperty.call(message, "AwardCombo"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.AwardCombo);
            if (message.AwardCount != null && Object.hasOwnProperty.call(message, "AwardCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.AwardCount);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlwod.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.AwardData
         * @static
         * @param {jlwod.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.IsLine = reader.bool();
                        break;
                    }
                case 2: {
                        message.IsWild = reader.bool();
                        break;
                    }
                case 3: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.AwardGrid && message.AwardGrid.length))
                            message.AwardGrid = [];
                        message.AwardGrid.push($root.jlwod.Grid.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.AwardMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AwardCombo = reader.double();
                        break;
                    }
                case 7: {
                        message.AwardCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlwod.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.IsLine != null && message.hasOwnProperty("IsLine"))
                if (typeof message.IsLine !== "boolean")
                    return "IsLine: boolean expected";
            if (message.IsWild != null && message.hasOwnProperty("IsWild"))
                if (typeof message.IsWild !== "boolean")
                    return "IsWild: boolean expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.AwardGrid != null && message.hasOwnProperty("AwardGrid")) {
                if (!Array.isArray(message.AwardGrid))
                    return "AwardGrid: array expected";
                for (let i = 0; i < message.AwardGrid.length; ++i) {
                    let error = $root.jlwod.Grid.verify(message.AwardGrid[i]);
                    if (error)
                        return "AwardGrid." + error;
                }
            }
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                if (typeof message.AwardMoney !== "number")
                    return "AwardMoney: number expected";
            if (message.AwardCombo != null && message.hasOwnProperty("AwardCombo"))
                if (typeof message.AwardCombo !== "number")
                    return "AwardCombo: number expected";
            if (message.AwardCount != null && message.hasOwnProperty("AwardCount"))
                if (!$util.isInteger(message.AwardCount))
                    return "AwardCount: integer expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.AwardData)
                return object;
            let message = new $root.jlwod.AwardData();
            if (object.IsLine != null)
                message.IsLine = Boolean(object.IsLine);
            if (object.IsWild != null)
                message.IsWild = Boolean(object.IsWild);
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.AwardGrid) {
                if (!Array.isArray(object.AwardGrid))
                    throw TypeError(".jlwod.AwardData.AwardGrid: array expected");
                message.AwardGrid = [];
                for (let i = 0; i < object.AwardGrid.length; ++i) {
                    if (typeof object.AwardGrid[i] !== "object")
                        throw TypeError(".jlwod.AwardData.AwardGrid: object expected");
                    message.AwardGrid[i] = $root.jlwod.Grid.fromObject(object.AwardGrid[i]);
                }
            }
            if (object.AwardMoney != null)
                message.AwardMoney = Number(object.AwardMoney);
            if (object.AwardCombo != null)
                message.AwardCombo = Number(object.AwardCombo);
            if (object.AwardCount != null)
                message.AwardCount = object.AwardCount | 0;
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.AwardData
         * @static
         * @param {jlwod.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.AwardGrid = [];
            if (options.defaults) {
                object.IsLine = false;
                object.IsWild = false;
                object.Symbol = 0;
                object.AwardMoney = 0;
                object.AwardCombo = 0;
                object.AwardCount = 0;
            }
            if (message.IsLine != null && message.hasOwnProperty("IsLine"))
                object.IsLine = message.IsLine;
            if (message.IsWild != null && message.hasOwnProperty("IsWild"))
                object.IsWild = message.IsWild;
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.AwardGrid && message.AwardGrid.length) {
                object.AwardGrid = [];
                for (let j = 0; j < message.AwardGrid.length; ++j)
                    object.AwardGrid[j] = $root.jlwod.Grid.toObject(message.AwardGrid[j], options);
            }
            if (message.AwardMoney != null && message.hasOwnProperty("AwardMoney"))
                object.AwardMoney = options.json && !isFinite(message.AwardMoney) ? String(message.AwardMoney) : message.AwardMoney;
            if (message.AwardCombo != null && message.hasOwnProperty("AwardCombo"))
                object.AwardCombo = options.json && !isFinite(message.AwardCombo) ? String(message.AwardCombo) : message.AwardCombo;
            if (message.AwardCount != null && message.hasOwnProperty("AwardCount"))
                object.AwardCount = message.AwardCount;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlwod.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlwod.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.AwardData";
        };

        return AwardData;
    })();

    jlwod.PData = (function() {

        /**
         * Properties of a PData.
         * @memberof jlwod
         * @interface IPData
         * @property {Array.<jlwod.IColumn>|null} [PlateShow] PData PlateShow
         * @property {number|null} [AwardTypeFlag] PData AwardTypeFlag
         * @property {Array.<jlwod.IAwardData>|null} [AwardDataList] PData AwardDataList
         * @property {number|null} [WinMoney] PData WinMoney
         * @property {number|null} [FreeTotalAward] PData FreeTotalAward
         * @property {number|null} [FreeCount] PData FreeCount
         */

        /**
         * Constructs a new PData.
         * @memberof jlwod
         * @classdesc Represents a PData.
         * @implements IPData
         * @constructor
         * @param {jlwod.IPData=} [properties] Properties to set
         */
        function PData(properties) {
            this.PlateShow = [];
            this.AwardDataList = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PData PlateShow.
         * @member {Array.<jlwod.IColumn>} PlateShow
         * @memberof jlwod.PData
         * @instance
         */
        PData.prototype.PlateShow = $util.emptyArray;

        /**
         * PData AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlwod.PData
         * @instance
         */
        PData.prototype.AwardTypeFlag = 0;

        /**
         * PData AwardDataList.
         * @member {Array.<jlwod.IAwardData>} AwardDataList
         * @memberof jlwod.PData
         * @instance
         */
        PData.prototype.AwardDataList = $util.emptyArray;

        /**
         * PData WinMoney.
         * @member {number} WinMoney
         * @memberof jlwod.PData
         * @instance
         */
        PData.prototype.WinMoney = 0;

        /**
         * PData FreeTotalAward.
         * @member {number} FreeTotalAward
         * @memberof jlwod.PData
         * @instance
         */
        PData.prototype.FreeTotalAward = 0;

        /**
         * PData FreeCount.
         * @member {number} FreeCount
         * @memberof jlwod.PData
         * @instance
         */
        PData.prototype.FreeCount = 0;

        /**
         * Creates a new PData instance using the specified properties.
         * @function create
         * @memberof jlwod.PData
         * @static
         * @param {jlwod.IPData=} [properties] Properties to set
         * @returns {jlwod.PData} PData instance
         */
        PData.create = function create(properties) {
            return new PData(properties);
        };

        /**
         * Encodes the specified PData message. Does not implicitly {@link jlwod.PData.verify|verify} messages.
         * @function encode
         * @memberof jlwod.PData
         * @static
         * @param {jlwod.IPData} message PData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateShow != null && message.PlateShow.length)
                for (let i = 0; i < message.PlateShow.length; ++i)
                    $root.jlwod.Column.encode(message.PlateShow[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.AwardTypeFlag);
            if (message.AwardDataList != null && message.AwardDataList.length)
                for (let i = 0; i < message.AwardDataList.length; ++i)
                    $root.jlwod.AwardData.encode(message.AwardDataList[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.WinMoney != null && Object.hasOwnProperty.call(message, "WinMoney"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.WinMoney);
            if (message.FreeTotalAward != null && Object.hasOwnProperty.call(message, "FreeTotalAward"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.FreeTotalAward);
            if (message.FreeCount != null && Object.hasOwnProperty.call(message, "FreeCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeCount);
            return writer;
        };

        /**
         * Encodes the specified PData message, length delimited. Does not implicitly {@link jlwod.PData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.PData
         * @static
         * @param {jlwod.IPData} message PData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.PData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.PData} PData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.PData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateShow && message.PlateShow.length))
                            message.PlateShow = [];
                        message.PlateShow.push($root.jlwod.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataList && message.AwardDataList.length))
                            message.AwardDataList = [];
                        message.AwardDataList.push($root.jlwod.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.WinMoney = reader.double();
                        break;
                    }
                case 5: {
                        message.FreeTotalAward = reader.double();
                        break;
                    }
                case 6: {
                        message.FreeCount = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.PData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.PData} PData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PData message.
         * @function verify
         * @memberof jlwod.PData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateShow != null && message.hasOwnProperty("PlateShow")) {
                if (!Array.isArray(message.PlateShow))
                    return "PlateShow: array expected";
                for (let i = 0; i < message.PlateShow.length; ++i) {
                    let error = $root.jlwod.Column.verify(message.PlateShow[i]);
                    if (error)
                        return "PlateShow." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.AwardDataList != null && message.hasOwnProperty("AwardDataList")) {
                if (!Array.isArray(message.AwardDataList))
                    return "AwardDataList: array expected";
                for (let i = 0; i < message.AwardDataList.length; ++i) {
                    let error = $root.jlwod.AwardData.verify(message.AwardDataList[i]);
                    if (error)
                        return "AwardDataList." + error;
                }
            }
            if (message.WinMoney != null && message.hasOwnProperty("WinMoney"))
                if (typeof message.WinMoney !== "number")
                    return "WinMoney: number expected";
            if (message.FreeTotalAward != null && message.hasOwnProperty("FreeTotalAward"))
                if (typeof message.FreeTotalAward !== "number")
                    return "FreeTotalAward: number expected";
            if (message.FreeCount != null && message.hasOwnProperty("FreeCount"))
                if (!$util.isInteger(message.FreeCount))
                    return "FreeCount: integer expected";
            return null;
        };

        /**
         * Creates a PData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.PData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.PData} PData
         */
        PData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.PData)
                return object;
            let message = new $root.jlwod.PData();
            if (object.PlateShow) {
                if (!Array.isArray(object.PlateShow))
                    throw TypeError(".jlwod.PData.PlateShow: array expected");
                message.PlateShow = [];
                for (let i = 0; i < object.PlateShow.length; ++i) {
                    if (typeof object.PlateShow[i] !== "object")
                        throw TypeError(".jlwod.PData.PlateShow: object expected");
                    message.PlateShow[i] = $root.jlwod.Column.fromObject(object.PlateShow[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.AwardDataList) {
                if (!Array.isArray(object.AwardDataList))
                    throw TypeError(".jlwod.PData.AwardDataList: array expected");
                message.AwardDataList = [];
                for (let i = 0; i < object.AwardDataList.length; ++i) {
                    if (typeof object.AwardDataList[i] !== "object")
                        throw TypeError(".jlwod.PData.AwardDataList: object expected");
                    message.AwardDataList[i] = $root.jlwod.AwardData.fromObject(object.AwardDataList[i]);
                }
            }
            if (object.WinMoney != null)
                message.WinMoney = Number(object.WinMoney);
            if (object.FreeTotalAward != null)
                message.FreeTotalAward = Number(object.FreeTotalAward);
            if (object.FreeCount != null)
                message.FreeCount = object.FreeCount | 0;
            return message;
        };

        /**
         * Creates a plain object from a PData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.PData
         * @static
         * @param {jlwod.PData} message PData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateShow = [];
                object.AwardDataList = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.WinMoney = 0;
                object.FreeTotalAward = 0;
                object.FreeCount = 0;
            }
            if (message.PlateShow && message.PlateShow.length) {
                object.PlateShow = [];
                for (let j = 0; j < message.PlateShow.length; ++j)
                    object.PlateShow[j] = $root.jlwod.Column.toObject(message.PlateShow[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.AwardDataList && message.AwardDataList.length) {
                object.AwardDataList = [];
                for (let j = 0; j < message.AwardDataList.length; ++j)
                    object.AwardDataList[j] = $root.jlwod.AwardData.toObject(message.AwardDataList[j], options);
            }
            if (message.WinMoney != null && message.hasOwnProperty("WinMoney"))
                object.WinMoney = options.json && !isFinite(message.WinMoney) ? String(message.WinMoney) : message.WinMoney;
            if (message.FreeTotalAward != null && message.hasOwnProperty("FreeTotalAward"))
                object.FreeTotalAward = options.json && !isFinite(message.FreeTotalAward) ? String(message.FreeTotalAward) : message.FreeTotalAward;
            if (message.FreeCount != null && message.hasOwnProperty("FreeCount"))
                object.FreeCount = message.FreeCount;
            return object;
        };

        /**
         * Converts this PData to JSON.
         * @function toJSON
         * @memberof jlwod.PData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PData
         * @function getTypeUrl
         * @memberof jlwod.PData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.PData";
        };

        return PData;
    })();

    jlwod.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlwod
         * @interface ISpinAck
         * @property {jlwod.IPData|null} [PlateInfo] SpinAck PlateInfo
         * @property {Array.<jlwod.IPData>|null} [FreePlateInfo] SpinAck FreePlateInfo
         * @property {boolean|null} [IsFree] SpinAck IsFree
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {number|null} [AwardType] SpinAck AwardType
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlwod
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlwod.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.FreePlateInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck PlateInfo.
         * @member {jlwod.IPData|null|undefined} PlateInfo
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.PlateInfo = null;

        /**
         * SpinAck FreePlateInfo.
         * @member {Array.<jlwod.IPData>} FreePlateInfo
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.FreePlateInfo = $util.emptyArray;

        /**
         * SpinAck IsFree.
         * @member {boolean} IsFree
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.IsFree = false;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck AwardType.
         * @member {number} AwardType
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.AwardType = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlwod.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlwod.SpinAck
         * @static
         * @param {jlwod.ISpinAck=} [properties] Properties to set
         * @returns {jlwod.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlwod.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlwod.SpinAck
         * @static
         * @param {jlwod.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateInfo != null && Object.hasOwnProperty.call(message, "PlateInfo"))
                $root.jlwod.PData.encode(message.PlateInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreePlateInfo != null && message.FreePlateInfo.length)
                for (let i = 0; i < message.FreePlateInfo.length; ++i)
                    $root.jlwod.PData.encode(message.FreePlateInfo[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.IsFree != null && Object.hasOwnProperty.call(message, "IsFree"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.IsFree);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.TotalWin);
            if (message.AwardType != null && Object.hasOwnProperty.call(message, "AwardType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AwardType);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlwod.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.SpinAck
         * @static
         * @param {jlwod.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.PlateInfo = $root.jlwod.PData.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        if (!(message.FreePlateInfo && message.FreePlateInfo.length))
                            message.FreePlateInfo = [];
                        message.FreePlateInfo.push($root.jlwod.PData.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        message.IsFree = reader.bool();
                        break;
                    }
                case 4: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 5: {
                        message.AwardType = reader.int32();
                        break;
                    }
                case 6: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 7: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 8: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 9: {
                        message.RTP = reader.double();
                        break;
                    }
                case 10: {
                        message.LID = reader.int32();
                        break;
                    }
                case 11: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlwod.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo")) {
                let error = $root.jlwod.PData.verify(message.PlateInfo);
                if (error)
                    return "PlateInfo." + error;
            }
            if (message.FreePlateInfo != null && message.hasOwnProperty("FreePlateInfo")) {
                if (!Array.isArray(message.FreePlateInfo))
                    return "FreePlateInfo: array expected";
                for (let i = 0; i < message.FreePlateInfo.length; ++i) {
                    let error = $root.jlwod.PData.verify(message.FreePlateInfo[i]);
                    if (error)
                        return "FreePlateInfo." + error;
                }
            }
            if (message.IsFree != null && message.hasOwnProperty("IsFree"))
                if (typeof message.IsFree !== "boolean")
                    return "IsFree: boolean expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                if (!$util.isInteger(message.AwardType))
                    return "AwardType: integer expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.SpinAck)
                return object;
            let message = new $root.jlwod.SpinAck();
            if (object.PlateInfo != null) {
                if (typeof object.PlateInfo !== "object")
                    throw TypeError(".jlwod.SpinAck.PlateInfo: object expected");
                message.PlateInfo = $root.jlwod.PData.fromObject(object.PlateInfo);
            }
            if (object.FreePlateInfo) {
                if (!Array.isArray(object.FreePlateInfo))
                    throw TypeError(".jlwod.SpinAck.FreePlateInfo: array expected");
                message.FreePlateInfo = [];
                for (let i = 0; i < object.FreePlateInfo.length; ++i) {
                    if (typeof object.FreePlateInfo[i] !== "object")
                        throw TypeError(".jlwod.SpinAck.FreePlateInfo: object expected");
                    message.FreePlateInfo[i] = $root.jlwod.PData.fromObject(object.FreePlateInfo[i]);
                }
            }
            if (object.IsFree != null)
                message.IsFree = Boolean(object.IsFree);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.AwardType != null)
                message.AwardType = object.AwardType | 0;
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.SpinAck
         * @static
         * @param {jlwod.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.FreePlateInfo = [];
            if (options.defaults) {
                object.PlateInfo = null;
                object.IsFree = false;
                object.TotalWin = 0;
                object.AwardType = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.PlateInfo != null && message.hasOwnProperty("PlateInfo"))
                object.PlateInfo = $root.jlwod.PData.toObject(message.PlateInfo, options);
            if (message.FreePlateInfo && message.FreePlateInfo.length) {
                object.FreePlateInfo = [];
                for (let j = 0; j < message.FreePlateInfo.length; ++j)
                    object.FreePlateInfo[j] = $root.jlwod.PData.toObject(message.FreePlateInfo[j], options);
            }
            if (message.IsFree != null && message.hasOwnProperty("IsFree"))
                object.IsFree = message.IsFree;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.AwardType != null && message.hasOwnProperty("AwardType"))
                object.AwardType = message.AwardType;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlwod.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlwod.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.SpinAck";
        };

        return SpinAck;
    })();

    jlwod.FreeReq = (function() {

        /**
         * Properties of a FreeReq.
         * @memberof jlwod
         * @interface IFreeReq
         * @property {number|null} [FreeType] FreeReq FreeType
         */

        /**
         * Constructs a new FreeReq.
         * @memberof jlwod
         * @classdesc Represents a FreeReq.
         * @implements IFreeReq
         * @constructor
         * @param {jlwod.IFreeReq=} [properties] Properties to set
         */
        function FreeReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreeReq FreeType.
         * @member {number} FreeType
         * @memberof jlwod.FreeReq
         * @instance
         */
        FreeReq.prototype.FreeType = 0;

        /**
         * Creates a new FreeReq instance using the specified properties.
         * @function create
         * @memberof jlwod.FreeReq
         * @static
         * @param {jlwod.IFreeReq=} [properties] Properties to set
         * @returns {jlwod.FreeReq} FreeReq instance
         */
        FreeReq.create = function create(properties) {
            return new FreeReq(properties);
        };

        /**
         * Encodes the specified FreeReq message. Does not implicitly {@link jlwod.FreeReq.verify|verify} messages.
         * @function encode
         * @memberof jlwod.FreeReq
         * @static
         * @param {jlwod.IFreeReq} message FreeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.FreeType != null && Object.hasOwnProperty.call(message, "FreeType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.FreeType);
            return writer;
        };

        /**
         * Encodes the specified FreeReq message, length delimited. Does not implicitly {@link jlwod.FreeReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.FreeReq
         * @static
         * @param {jlwod.IFreeReq} message FreeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreeReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreeReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.FreeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.FreeReq} FreeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.FreeReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.FreeType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreeReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.FreeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.FreeReq} FreeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreeReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreeReq message.
         * @function verify
         * @memberof jlwod.FreeReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreeReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                if (!$util.isInteger(message.FreeType))
                    return "FreeType: integer expected";
            return null;
        };

        /**
         * Creates a FreeReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.FreeReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.FreeReq} FreeReq
         */
        FreeReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.FreeReq)
                return object;
            let message = new $root.jlwod.FreeReq();
            if (object.FreeType != null)
                message.FreeType = object.FreeType | 0;
            return message;
        };

        /**
         * Creates a plain object from a FreeReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.FreeReq
         * @static
         * @param {jlwod.FreeReq} message FreeReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreeReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.FreeType = 0;
            if (message.FreeType != null && message.hasOwnProperty("FreeType"))
                object.FreeType = message.FreeType;
            return object;
        };

        /**
         * Converts this FreeReq to JSON.
         * @function toJSON
         * @memberof jlwod.FreeReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreeReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreeReq
         * @function getTypeUrl
         * @memberof jlwod.FreeReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.FreeReq";
        };

        return FreeReq;
    })();

    jlwod.StateInfo = (function() {

        /**
         * Properties of a StateInfo.
         * @memberof jlwod
         * @interface IStateInfo
         * @property {boolean|null} [IsinFree] StateInfo IsinFree
         * @property {boolean|null} [NeedchooseFree] StateInfo NeedchooseFree
         * @property {number|null} [FreeGameType] StateInfo FreeGameType
         * @property {number|null} [FreeRoundCount] StateInfo FreeRoundCount
         * @property {number|null} [FreeGameCount] StateInfo FreeGameCount
         * @property {number|null} [FreeTotalAward] StateInfo FreeTotalAward
         */

        /**
         * Constructs a new StateInfo.
         * @memberof jlwod
         * @classdesc Represents a StateInfo.
         * @implements IStateInfo
         * @constructor
         * @param {jlwod.IStateInfo=} [properties] Properties to set
         */
        function StateInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StateInfo IsinFree.
         * @member {boolean} IsinFree
         * @memberof jlwod.StateInfo
         * @instance
         */
        StateInfo.prototype.IsinFree = false;

        /**
         * StateInfo NeedchooseFree.
         * @member {boolean} NeedchooseFree
         * @memberof jlwod.StateInfo
         * @instance
         */
        StateInfo.prototype.NeedchooseFree = false;

        /**
         * StateInfo FreeGameType.
         * @member {number} FreeGameType
         * @memberof jlwod.StateInfo
         * @instance
         */
        StateInfo.prototype.FreeGameType = 0;

        /**
         * StateInfo FreeRoundCount.
         * @member {number} FreeRoundCount
         * @memberof jlwod.StateInfo
         * @instance
         */
        StateInfo.prototype.FreeRoundCount = 0;

        /**
         * StateInfo FreeGameCount.
         * @member {number} FreeGameCount
         * @memberof jlwod.StateInfo
         * @instance
         */
        StateInfo.prototype.FreeGameCount = 0;

        /**
         * StateInfo FreeTotalAward.
         * @member {number} FreeTotalAward
         * @memberof jlwod.StateInfo
         * @instance
         */
        StateInfo.prototype.FreeTotalAward = 0;

        /**
         * Creates a new StateInfo instance using the specified properties.
         * @function create
         * @memberof jlwod.StateInfo
         * @static
         * @param {jlwod.IStateInfo=} [properties] Properties to set
         * @returns {jlwod.StateInfo} StateInfo instance
         */
        StateInfo.create = function create(properties) {
            return new StateInfo(properties);
        };

        /**
         * Encodes the specified StateInfo message. Does not implicitly {@link jlwod.StateInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwod.StateInfo
         * @static
         * @param {jlwod.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.IsinFree != null && Object.hasOwnProperty.call(message, "IsinFree"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.IsinFree);
            if (message.NeedchooseFree != null && Object.hasOwnProperty.call(message, "NeedchooseFree"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.NeedchooseFree);
            if (message.FreeGameType != null && Object.hasOwnProperty.call(message, "FreeGameType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.FreeGameType);
            if (message.FreeRoundCount != null && Object.hasOwnProperty.call(message, "FreeRoundCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeRoundCount);
            if (message.FreeGameCount != null && Object.hasOwnProperty.call(message, "FreeGameCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeGameCount);
            if (message.FreeTotalAward != null && Object.hasOwnProperty.call(message, "FreeTotalAward"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.FreeTotalAward);
            return writer;
        };

        /**
         * Encodes the specified StateInfo message, length delimited. Does not implicitly {@link jlwod.StateInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.StateInfo
         * @static
         * @param {jlwod.IStateInfo} message StateInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StateInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.StateInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.IsinFree = reader.bool();
                        break;
                    }
                case 2: {
                        message.NeedchooseFree = reader.bool();
                        break;
                    }
                case 3: {
                        message.FreeGameType = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeRoundCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeGameCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeTotalAward = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StateInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.StateInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.StateInfo} StateInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StateInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StateInfo message.
         * @function verify
         * @memberof jlwod.StateInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StateInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.IsinFree != null && message.hasOwnProperty("IsinFree"))
                if (typeof message.IsinFree !== "boolean")
                    return "IsinFree: boolean expected";
            if (message.NeedchooseFree != null && message.hasOwnProperty("NeedchooseFree"))
                if (typeof message.NeedchooseFree !== "boolean")
                    return "NeedchooseFree: boolean expected";
            if (message.FreeGameType != null && message.hasOwnProperty("FreeGameType"))
                if (!$util.isInteger(message.FreeGameType))
                    return "FreeGameType: integer expected";
            if (message.FreeRoundCount != null && message.hasOwnProperty("FreeRoundCount"))
                if (!$util.isInteger(message.FreeRoundCount))
                    return "FreeRoundCount: integer expected";
            if (message.FreeGameCount != null && message.hasOwnProperty("FreeGameCount"))
                if (!$util.isInteger(message.FreeGameCount))
                    return "FreeGameCount: integer expected";
            if (message.FreeTotalAward != null && message.hasOwnProperty("FreeTotalAward"))
                if (typeof message.FreeTotalAward !== "number")
                    return "FreeTotalAward: number expected";
            return null;
        };

        /**
         * Creates a StateInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.StateInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.StateInfo} StateInfo
         */
        StateInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.StateInfo)
                return object;
            let message = new $root.jlwod.StateInfo();
            if (object.IsinFree != null)
                message.IsinFree = Boolean(object.IsinFree);
            if (object.NeedchooseFree != null)
                message.NeedchooseFree = Boolean(object.NeedchooseFree);
            if (object.FreeGameType != null)
                message.FreeGameType = object.FreeGameType | 0;
            if (object.FreeRoundCount != null)
                message.FreeRoundCount = object.FreeRoundCount | 0;
            if (object.FreeGameCount != null)
                message.FreeGameCount = object.FreeGameCount | 0;
            if (object.FreeTotalAward != null)
                message.FreeTotalAward = Number(object.FreeTotalAward);
            return message;
        };

        /**
         * Creates a plain object from a StateInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.StateInfo
         * @static
         * @param {jlwod.StateInfo} message StateInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StateInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.IsinFree = false;
                object.NeedchooseFree = false;
                object.FreeGameType = 0;
                object.FreeRoundCount = 0;
                object.FreeGameCount = 0;
                object.FreeTotalAward = 0;
            }
            if (message.IsinFree != null && message.hasOwnProperty("IsinFree"))
                object.IsinFree = message.IsinFree;
            if (message.NeedchooseFree != null && message.hasOwnProperty("NeedchooseFree"))
                object.NeedchooseFree = message.NeedchooseFree;
            if (message.FreeGameType != null && message.hasOwnProperty("FreeGameType"))
                object.FreeGameType = message.FreeGameType;
            if (message.FreeRoundCount != null && message.hasOwnProperty("FreeRoundCount"))
                object.FreeRoundCount = message.FreeRoundCount;
            if (message.FreeGameCount != null && message.hasOwnProperty("FreeGameCount"))
                object.FreeGameCount = message.FreeGameCount;
            if (message.FreeTotalAward != null && message.hasOwnProperty("FreeTotalAward"))
                object.FreeTotalAward = options.json && !isFinite(message.FreeTotalAward) ? String(message.FreeTotalAward) : message.FreeTotalAward;
            return object;
        };

        /**
         * Converts this StateInfo to JSON.
         * @function toJSON
         * @memberof jlwod.StateInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StateInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StateInfo
         * @function getTypeUrl
         * @memberof jlwod.StateInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StateInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.StateInfo";
        };

        return StateInfo;
    })();

    jlwod.RoundRecord = (function() {

        /**
         * Properties of a RoundRecord.
         * @memberof jlwod
         * @interface IRoundRecord
         * @property {number|Long|null} [RoundIndex] RoundRecord RoundIndex
         * @property {number|null} [MGBet] RoundRecord MGBet
         */

        /**
         * Constructs a new RoundRecord.
         * @memberof jlwod
         * @classdesc Represents a RoundRecord.
         * @implements IRoundRecord
         * @constructor
         * @param {jlwod.IRoundRecord=} [properties] Properties to set
         */
        function RoundRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundRecord RoundIndex.
         * @member {number|Long} RoundIndex
         * @memberof jlwod.RoundRecord
         * @instance
         */
        RoundRecord.prototype.RoundIndex = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * RoundRecord MGBet.
         * @member {number} MGBet
         * @memberof jlwod.RoundRecord
         * @instance
         */
        RoundRecord.prototype.MGBet = 0;

        /**
         * Creates a new RoundRecord instance using the specified properties.
         * @function create
         * @memberof jlwod.RoundRecord
         * @static
         * @param {jlwod.IRoundRecord=} [properties] Properties to set
         * @returns {jlwod.RoundRecord} RoundRecord instance
         */
        RoundRecord.create = function create(properties) {
            return new RoundRecord(properties);
        };

        /**
         * Encodes the specified RoundRecord message. Does not implicitly {@link jlwod.RoundRecord.verify|verify} messages.
         * @function encode
         * @memberof jlwod.RoundRecord
         * @static
         * @param {jlwod.IRoundRecord} message RoundRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundIndex != null && Object.hasOwnProperty.call(message, "RoundIndex"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.RoundIndex);
            if (message.MGBet != null && Object.hasOwnProperty.call(message, "MGBet"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.MGBet);
            return writer;
        };

        /**
         * Encodes the specified RoundRecord message, length delimited. Does not implicitly {@link jlwod.RoundRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwod.RoundRecord
         * @static
         * @param {jlwod.IRoundRecord} message RoundRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundRecord message from the specified reader or buffer.
         * @function decode
         * @memberof jlwod.RoundRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwod.RoundRecord} RoundRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwod.RoundRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.RoundIndex = reader.int64();
                        break;
                    }
                case 2: {
                        message.MGBet = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwod.RoundRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwod.RoundRecord} RoundRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundRecord message.
         * @function verify
         * @memberof jlwod.RoundRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundIndex != null && message.hasOwnProperty("RoundIndex"))
                if (!$util.isInteger(message.RoundIndex) && !(message.RoundIndex && $util.isInteger(message.RoundIndex.low) && $util.isInteger(message.RoundIndex.high)))
                    return "RoundIndex: integer|Long expected";
            if (message.MGBet != null && message.hasOwnProperty("MGBet"))
                if (typeof message.MGBet !== "number")
                    return "MGBet: number expected";
            return null;
        };

        /**
         * Creates a RoundRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwod.RoundRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwod.RoundRecord} RoundRecord
         */
        RoundRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwod.RoundRecord)
                return object;
            let message = new $root.jlwod.RoundRecord();
            if (object.RoundIndex != null)
                if ($util.Long)
                    (message.RoundIndex = $util.Long.fromValue(object.RoundIndex)).unsigned = false;
                else if (typeof object.RoundIndex === "string")
                    message.RoundIndex = parseInt(object.RoundIndex, 10);
                else if (typeof object.RoundIndex === "number")
                    message.RoundIndex = object.RoundIndex;
                else if (typeof object.RoundIndex === "object")
                    message.RoundIndex = new $util.LongBits(object.RoundIndex.low >>> 0, object.RoundIndex.high >>> 0).toNumber();
            if (object.MGBet != null)
                message.MGBet = Number(object.MGBet);
            return message;
        };

        /**
         * Creates a plain object from a RoundRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwod.RoundRecord
         * @static
         * @param {jlwod.RoundRecord} message RoundRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.RoundIndex = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.RoundIndex = options.longs === String ? "0" : 0;
                object.MGBet = 0;
            }
            if (message.RoundIndex != null && message.hasOwnProperty("RoundIndex"))
                if (typeof message.RoundIndex === "number")
                    object.RoundIndex = options.longs === String ? String(message.RoundIndex) : message.RoundIndex;
                else
                    object.RoundIndex = options.longs === String ? $util.Long.prototype.toString.call(message.RoundIndex) : options.longs === Number ? new $util.LongBits(message.RoundIndex.low >>> 0, message.RoundIndex.high >>> 0).toNumber() : message.RoundIndex;
            if (message.MGBet != null && message.hasOwnProperty("MGBet"))
                object.MGBet = options.json && !isFinite(message.MGBet) ? String(message.MGBet) : message.MGBet;
            return object;
        };

        /**
         * Converts this RoundRecord to JSON.
         * @function toJSON
         * @memberof jlwod.RoundRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundRecord
         * @function getTypeUrl
         * @memberof jlwod.RoundRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwod.RoundRecord";
        };

        return RoundRecord;
    })();

    return jlwod;
})();

export const jlwr = $root.jlwr = (() => {

    /**
     * Namespace jlwr.
     * @exports jlwr
     * @namespace
     */
    const jlwr = {};

    jlwr.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlwr
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlwr
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlwr.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlwr.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlwr.Column
         * @static
         * @param {jlwr.IColumn=} [properties] Properties to set
         * @returns {jlwr.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlwr.Column.verify|verify} messages.
         * @function encode
         * @memberof jlwr.Column
         * @static
         * @param {jlwr.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlwr.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.Column
         * @static
         * @param {jlwr.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlwr.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.Column)
                return object;
            let message = new $root.jlwr.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlwr.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.Column
         * @static
         * @param {jlwr.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlwr.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlwr.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.Column";
        };

        return Column;
    })();

    jlwr.ColumnFloat = (function() {

        /**
         * Properties of a ColumnFloat.
         * @memberof jlwr
         * @interface IColumnFloat
         * @property {Array.<number>|null} [Col] ColumnFloat Col
         */

        /**
         * Constructs a new ColumnFloat.
         * @memberof jlwr
         * @classdesc Represents a ColumnFloat.
         * @implements IColumnFloat
         * @constructor
         * @param {jlwr.IColumnFloat=} [properties] Properties to set
         */
        function ColumnFloat(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnFloat Col.
         * @member {Array.<number>} Col
         * @memberof jlwr.ColumnFloat
         * @instance
         */
        ColumnFloat.prototype.Col = $util.emptyArray;

        /**
         * Creates a new ColumnFloat instance using the specified properties.
         * @function create
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {jlwr.IColumnFloat=} [properties] Properties to set
         * @returns {jlwr.ColumnFloat} ColumnFloat instance
         */
        ColumnFloat.create = function create(properties) {
            return new ColumnFloat(properties);
        };

        /**
         * Encodes the specified ColumnFloat message. Does not implicitly {@link jlwr.ColumnFloat.verify|verify} messages.
         * @function encode
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {jlwr.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.double(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified ColumnFloat message, length delimited. Does not implicitly {@link jlwr.ColumnFloat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {jlwr.IColumnFloat} message ColumnFloat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnFloat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.ColumnFloat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.double());
                        } else
                            message.Col.push(reader.double());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnFloat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.ColumnFloat} ColumnFloat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnFloat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnFloat message.
         * @function verify
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnFloat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (typeof message.Col[i] !== "number")
                        return "Col: number[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnFloat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.ColumnFloat} ColumnFloat
         */
        ColumnFloat.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.ColumnFloat)
                return object;
            let message = new $root.jlwr.ColumnFloat();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlwr.ColumnFloat.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = Number(object.Col[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnFloat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {jlwr.ColumnFloat} message ColumnFloat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnFloat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = options.json && !isFinite(message.Col[j]) ? String(message.Col[j]) : message.Col[j];
            }
            return object;
        };

        /**
         * Converts this ColumnFloat to JSON.
         * @function toJSON
         * @memberof jlwr.ColumnFloat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnFloat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnFloat
         * @function getTypeUrl
         * @memberof jlwr.ColumnFloat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnFloat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.ColumnFloat";
        };

        return ColumnFloat;
    })();

    jlwr.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlwr
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [Line] AwardData Line
         * @property {number|null} [Win] AwardData Win
         * @property {number|null} [Mult] AwardData Mult
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlwr
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlwr.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlwr.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlwr.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData Line.
         * @member {number} Line
         * @memberof jlwr.AwardData
         * @instance
         */
        AwardData.prototype.Line = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlwr.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Mult.
         * @member {number} Mult
         * @memberof jlwr.AwardData
         * @instance
         */
        AwardData.prototype.Mult = 0;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlwr.AwardData
         * @static
         * @param {jlwr.IAwardData=} [properties] Properties to set
         * @returns {jlwr.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlwr.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlwr.AwardData
         * @static
         * @param {jlwr.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.Line != null && Object.hasOwnProperty.call(message, "Line"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Line);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Mult != null && Object.hasOwnProperty.call(message, "Mult"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.Mult);
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlwr.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.AwardData
         * @static
         * @param {jlwr.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.Line = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        message.Mult = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlwr.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.Line != null && message.hasOwnProperty("Line"))
                if (!$util.isInteger(message.Line))
                    return "Line: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                if (typeof message.Mult !== "number")
                    return "Mult: number expected";
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.AwardData)
                return object;
            let message = new $root.jlwr.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.Line != null)
                message.Line = object.Line | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Mult != null)
                message.Mult = Number(object.Mult);
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.AwardData
         * @static
         * @param {jlwr.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.Line = 0;
                object.Win = 0;
                object.Mult = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.Line != null && message.hasOwnProperty("Line"))
                object.Line = message.Line;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Mult != null && message.hasOwnProperty("Mult"))
                object.Mult = options.json && !isFinite(message.Mult) ? String(message.Mult) : message.Mult;
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlwr.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlwr.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.AwardData";
        };

        return AwardData;
    })();

    jlwr.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlwr
         * @interface IRoundInfo
         * @property {Array.<jlwr.IColumn>|null} [PlateSymbol] RoundInfo PlateSymbol
         * @property {Array.<jlwr.IColumnFloat>|null} [PlateSymbolNum] RoundInfo PlateSymbolNum
         * @property {Array.<jlwr.IAwardData>|null} [AwardDataVec] RoundInfo AwardDataVec
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeLevel] RoundInfo FreeLevel
         * @property {number|null} [FreeWildCount] RoundInfo FreeWildCount
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlwr
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlwr.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.PlateSymbol = [];
            this.PlateSymbolNum = [];
            this.AwardDataVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo PlateSymbol.
         * @member {Array.<jlwr.IColumn>} PlateSymbol
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbol = $util.emptyArray;

        /**
         * RoundInfo PlateSymbolNum.
         * @member {Array.<jlwr.IColumnFloat>} PlateSymbolNum
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.PlateSymbolNum = $util.emptyArray;

        /**
         * RoundInfo AwardDataVec.
         * @member {Array.<jlwr.IAwardData>} AwardDataVec
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeLevel.
         * @member {number} FreeLevel
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeLevel = 0;

        /**
         * RoundInfo FreeWildCount.
         * @member {number} FreeWildCount
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeWildCount = 0;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlwr.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlwr.RoundInfo
         * @static
         * @param {jlwr.IRoundInfo=} [properties] Properties to set
         * @returns {jlwr.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlwr.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwr.RoundInfo
         * @static
         * @param {jlwr.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.PlateSymbol != null && message.PlateSymbol.length)
                for (let i = 0; i < message.PlateSymbol.length; ++i)
                    $root.jlwr.Column.encode(message.PlateSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.PlateSymbolNum != null && message.PlateSymbolNum.length)
                for (let i = 0; i < message.PlateSymbolNum.length; ++i)
                    $root.jlwr.ColumnFloat.encode(message.PlateSymbolNum[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlwr.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.AwardTypeFlag);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.FreeRemainRound);
            if (message.FreeLevel != null && Object.hasOwnProperty.call(message, "FreeLevel"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeLevel);
            if (message.FreeWildCount != null && Object.hasOwnProperty.call(message, "FreeWildCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.FreeWildCount);
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.RoundWin);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlwr.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.RoundInfo
         * @static
         * @param {jlwr.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.PlateSymbol && message.PlateSymbol.length))
                            message.PlateSymbol = [];
                        message.PlateSymbol.push($root.jlwr.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.PlateSymbolNum && message.PlateSymbolNum.length))
                            message.PlateSymbolNum = [];
                        message.PlateSymbolNum.push($root.jlwr.ColumnFloat.decode(reader, reader.uint32()));
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlwr.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 5: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 6: {
                        message.FreeLevel = reader.int32();
                        break;
                    }
                case 7: {
                        message.FreeWildCount = reader.int32();
                        break;
                    }
                case 8: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 9: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlwr.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.PlateSymbol != null && message.hasOwnProperty("PlateSymbol")) {
                if (!Array.isArray(message.PlateSymbol))
                    return "PlateSymbol: array expected";
                for (let i = 0; i < message.PlateSymbol.length; ++i) {
                    let error = $root.jlwr.Column.verify(message.PlateSymbol[i]);
                    if (error)
                        return "PlateSymbol." + error;
                }
            }
            if (message.PlateSymbolNum != null && message.hasOwnProperty("PlateSymbolNum")) {
                if (!Array.isArray(message.PlateSymbolNum))
                    return "PlateSymbolNum: array expected";
                for (let i = 0; i < message.PlateSymbolNum.length; ++i) {
                    let error = $root.jlwr.ColumnFloat.verify(message.PlateSymbolNum[i]);
                    if (error)
                        return "PlateSymbolNum." + error;
                }
            }
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlwr.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeLevel != null && message.hasOwnProperty("FreeLevel"))
                if (!$util.isInteger(message.FreeLevel))
                    return "FreeLevel: integer expected";
            if (message.FreeWildCount != null && message.hasOwnProperty("FreeWildCount"))
                if (!$util.isInteger(message.FreeWildCount))
                    return "FreeWildCount: integer expected";
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.RoundInfo)
                return object;
            let message = new $root.jlwr.RoundInfo();
            if (object.PlateSymbol) {
                if (!Array.isArray(object.PlateSymbol))
                    throw TypeError(".jlwr.RoundInfo.PlateSymbol: array expected");
                message.PlateSymbol = [];
                for (let i = 0; i < object.PlateSymbol.length; ++i) {
                    if (typeof object.PlateSymbol[i] !== "object")
                        throw TypeError(".jlwr.RoundInfo.PlateSymbol: object expected");
                    message.PlateSymbol[i] = $root.jlwr.Column.fromObject(object.PlateSymbol[i]);
                }
            }
            if (object.PlateSymbolNum) {
                if (!Array.isArray(object.PlateSymbolNum))
                    throw TypeError(".jlwr.RoundInfo.PlateSymbolNum: array expected");
                message.PlateSymbolNum = [];
                for (let i = 0; i < object.PlateSymbolNum.length; ++i) {
                    if (typeof object.PlateSymbolNum[i] !== "object")
                        throw TypeError(".jlwr.RoundInfo.PlateSymbolNum: object expected");
                    message.PlateSymbolNum[i] = $root.jlwr.ColumnFloat.fromObject(object.PlateSymbolNum[i]);
                }
            }
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlwr.RoundInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlwr.RoundInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlwr.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeLevel != null)
                message.FreeLevel = object.FreeLevel | 0;
            if (object.FreeWildCount != null)
                message.FreeWildCount = object.FreeWildCount | 0;
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.RoundInfo
         * @static
         * @param {jlwr.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.PlateSymbol = [];
                object.PlateSymbolNum = [];
                object.AwardDataVec = [];
            }
            if (options.defaults) {
                object.AwardTypeFlag = 0;
                object.FreeRemainRound = 0;
                object.FreeLevel = 0;
                object.FreeWildCount = 0;
                object.RoundWin = 0;
                object.MaxFlag = false;
            }
            if (message.PlateSymbol && message.PlateSymbol.length) {
                object.PlateSymbol = [];
                for (let j = 0; j < message.PlateSymbol.length; ++j)
                    object.PlateSymbol[j] = $root.jlwr.Column.toObject(message.PlateSymbol[j], options);
            }
            if (message.PlateSymbolNum && message.PlateSymbolNum.length) {
                object.PlateSymbolNum = [];
                for (let j = 0; j < message.PlateSymbolNum.length; ++j)
                    object.PlateSymbolNum[j] = $root.jlwr.ColumnFloat.toObject(message.PlateSymbolNum[j], options);
            }
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlwr.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeLevel != null && message.hasOwnProperty("FreeLevel"))
                object.FreeLevel = message.FreeLevel;
            if (message.FreeWildCount != null && message.hasOwnProperty("FreeWildCount"))
                object.FreeWildCount = message.FreeWildCount;
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlwr.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlwr.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.RoundInfo";
        };

        return RoundInfo;
    })();

    jlwr.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlwr
         * @interface ISpinAck
         * @property {Array.<jlwr.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlwr
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlwr.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlwr.IRoundInfo>} RoundQueue
         * @memberof jlwr.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlwr.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlwr.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlwr.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlwr.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlwr.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlwr.SpinAck
         * @static
         * @param {jlwr.ISpinAck=} [properties] Properties to set
         * @returns {jlwr.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlwr.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.SpinAck
         * @static
         * @param {jlwr.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlwr.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlwr.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.SpinAck
         * @static
         * @param {jlwr.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlwr.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlwr.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlwr.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.SpinAck)
                return object;
            let message = new $root.jlwr.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlwr.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlwr.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlwr.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.SpinAck
         * @static
         * @param {jlwr.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlwr.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlwr.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlwr.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.SpinAck";
        };

        return SpinAck;
    })();

    jlwr.GameInfoAck = (function() {

        /**
         * Properties of a GameInfoAck.
         * @memberof jlwr
         * @interface IGameInfoAck
         * @property {Array.<jlwr.IWalletInfo>|null} [WalletInfo] GameInfoAck WalletInfo
         * @property {number|null} [ArcadeNo] GameInfoAck ArcadeNo
         * @property {boolean|null} [CanIntoArcade] GameInfoAck CanIntoArcade
         * @property {boolean|null} [Test] GameInfoAck Test
         * @property {jlwr.IPreferRoundShow|null} [Prefer] GameInfoAck Prefer
         * @property {number|null} [FreeSpinRemain] GameInfoAck FreeSpinRemain
         */

        /**
         * Constructs a new GameInfoAck.
         * @memberof jlwr
         * @classdesc Represents a GameInfoAck.
         * @implements IGameInfoAck
         * @constructor
         * @param {jlwr.IGameInfoAck=} [properties] Properties to set
         */
        function GameInfoAck(properties) {
            this.WalletInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameInfoAck WalletInfo.
         * @member {Array.<jlwr.IWalletInfo>} WalletInfo
         * @memberof jlwr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.WalletInfo = $util.emptyArray;

        /**
         * GameInfoAck ArcadeNo.
         * @member {number} ArcadeNo
         * @memberof jlwr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.ArcadeNo = 0;

        /**
         * GameInfoAck CanIntoArcade.
         * @member {boolean} CanIntoArcade
         * @memberof jlwr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.CanIntoArcade = false;

        /**
         * GameInfoAck Test.
         * @member {boolean} Test
         * @memberof jlwr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Test = false;

        /**
         * GameInfoAck Prefer.
         * @member {jlwr.IPreferRoundShow|null|undefined} Prefer
         * @memberof jlwr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.Prefer = null;

        /**
         * GameInfoAck FreeSpinRemain.
         * @member {number} FreeSpinRemain
         * @memberof jlwr.GameInfoAck
         * @instance
         */
        GameInfoAck.prototype.FreeSpinRemain = 0;

        /**
         * Creates a new GameInfoAck instance using the specified properties.
         * @function create
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {jlwr.IGameInfoAck=} [properties] Properties to set
         * @returns {jlwr.GameInfoAck} GameInfoAck instance
         */
        GameInfoAck.create = function create(properties) {
            return new GameInfoAck(properties);
        };

        /**
         * Encodes the specified GameInfoAck message. Does not implicitly {@link jlwr.GameInfoAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {jlwr.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.WalletInfo != null && message.WalletInfo.length)
                for (let i = 0; i < message.WalletInfo.length; ++i)
                    $root.jlwr.WalletInfo.encode(message.WalletInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ArcadeNo != null && Object.hasOwnProperty.call(message, "ArcadeNo"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ArcadeNo);
            if (message.CanIntoArcade != null && Object.hasOwnProperty.call(message, "CanIntoArcade"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.CanIntoArcade);
            if (message.Test != null && Object.hasOwnProperty.call(message, "Test"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.Test);
            if (message.Prefer != null && Object.hasOwnProperty.call(message, "Prefer"))
                $root.jlwr.PreferRoundShow.encode(message.Prefer, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.FreeSpinRemain != null && Object.hasOwnProperty.call(message, "FreeSpinRemain"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.FreeSpinRemain);
            return writer;
        };

        /**
         * Encodes the specified GameInfoAck message, length delimited. Does not implicitly {@link jlwr.GameInfoAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {jlwr.IGameInfoAck} message GameInfoAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameInfoAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.GameInfoAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.WalletInfo && message.WalletInfo.length))
                            message.WalletInfo = [];
                        message.WalletInfo.push($root.jlwr.WalletInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ArcadeNo = reader.int32();
                        break;
                    }
                case 3: {
                        message.CanIntoArcade = reader.bool();
                        break;
                    }
                case 4: {
                        message.Test = reader.bool();
                        break;
                    }
                case 5: {
                        message.Prefer = $root.jlwr.PreferRoundShow.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.FreeSpinRemain = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameInfoAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.GameInfoAck} GameInfoAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameInfoAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameInfoAck message.
         * @function verify
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameInfoAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.WalletInfo != null && message.hasOwnProperty("WalletInfo")) {
                if (!Array.isArray(message.WalletInfo))
                    return "WalletInfo: array expected";
                for (let i = 0; i < message.WalletInfo.length; ++i) {
                    let error = $root.jlwr.WalletInfo.verify(message.WalletInfo[i]);
                    if (error)
                        return "WalletInfo." + error;
                }
            }
            if (message.ArcadeNo != null && message.hasOwnProperty("ArcadeNo"))
                if (!$util.isInteger(message.ArcadeNo))
                    return "ArcadeNo: integer expected";
            if (message.CanIntoArcade != null && message.hasOwnProperty("CanIntoArcade"))
                if (typeof message.CanIntoArcade !== "boolean")
                    return "CanIntoArcade: boolean expected";
            if (message.Test != null && message.hasOwnProperty("Test"))
                if (typeof message.Test !== "boolean")
                    return "Test: boolean expected";
            if (message.Prefer != null && message.hasOwnProperty("Prefer")) {
                let error = $root.jlwr.PreferRoundShow.verify(message.Prefer);
                if (error)
                    return "Prefer." + error;
            }
            if (message.FreeSpinRemain != null && message.hasOwnProperty("FreeSpinRemain"))
                if (!$util.isInteger(message.FreeSpinRemain))
                    return "FreeSpinRemain: integer expected";
            return null;
        };

        /**
         * Creates a GameInfoAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.GameInfoAck} GameInfoAck
         */
        GameInfoAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.GameInfoAck)
                return object;
            let message = new $root.jlwr.GameInfoAck();
            if (object.WalletInfo) {
                if (!Array.isArray(object.WalletInfo))
                    throw TypeError(".jlwr.GameInfoAck.WalletInfo: array expected");
                message.WalletInfo = [];
                for (let i = 0; i < object.WalletInfo.length; ++i) {
                    if (typeof object.WalletInfo[i] !== "object")
                        throw TypeError(".jlwr.GameInfoAck.WalletInfo: object expected");
                    message.WalletInfo[i] = $root.jlwr.WalletInfo.fromObject(object.WalletInfo[i]);
                }
            }
            if (object.ArcadeNo != null)
                message.ArcadeNo = object.ArcadeNo | 0;
            if (object.CanIntoArcade != null)
                message.CanIntoArcade = Boolean(object.CanIntoArcade);
            if (object.Test != null)
                message.Test = Boolean(object.Test);
            if (object.Prefer != null) {
                if (typeof object.Prefer !== "object")
                    throw TypeError(".jlwr.GameInfoAck.Prefer: object expected");
                message.Prefer = $root.jlwr.PreferRoundShow.fromObject(object.Prefer);
            }
            if (object.FreeSpinRemain != null)
                message.FreeSpinRemain = object.FreeSpinRemain | 0;
            return message;
        };

        /**
         * Creates a plain object from a GameInfoAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {jlwr.GameInfoAck} message GameInfoAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameInfoAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.WalletInfo = [];
            if (options.defaults) {
                object.ArcadeNo = 0;
                object.CanIntoArcade = false;
                object.Test = false;
                object.Prefer = null;
                object.FreeSpinRemain = 0;
            }
            if (message.WalletInfo && message.WalletInfo.length) {
                object.WalletInfo = [];
                for (let j = 0; j < message.WalletInfo.length; ++j)
                    object.WalletInfo[j] = $root.jlwr.WalletInfo.toObject(message.WalletInfo[j], options);
            }
            if (message.ArcadeNo != null && message.hasOwnProperty("ArcadeNo"))
                object.ArcadeNo = message.ArcadeNo;
            if (message.CanIntoArcade != null && message.hasOwnProperty("CanIntoArcade"))
                object.CanIntoArcade = message.CanIntoArcade;
            if (message.Test != null && message.hasOwnProperty("Test"))
                object.Test = message.Test;
            if (message.Prefer != null && message.hasOwnProperty("Prefer"))
                object.Prefer = $root.jlwr.PreferRoundShow.toObject(message.Prefer, options);
            if (message.FreeSpinRemain != null && message.hasOwnProperty("FreeSpinRemain"))
                object.FreeSpinRemain = message.FreeSpinRemain;
            return object;
        };

        /**
         * Converts this GameInfoAck to JSON.
         * @function toJSON
         * @memberof jlwr.GameInfoAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameInfoAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameInfoAck
         * @function getTypeUrl
         * @memberof jlwr.GameInfoAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameInfoAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.GameInfoAck";
        };

        return GameInfoAck;
    })();

    jlwr.GameReqData = (function() {

        /**
         * Properties of a GameReqData.
         * @memberof jlwr
         * @interface IGameReqData
         * @property {number|null} [aid] GameReqData aid
         * @property {number|null} [apiid] GameReqData apiid
         * @property {string|null} [token] GameReqData token
         * @property {Uint8Array|null} [encode] GameReqData encode
         * @property {jlwr.ISpinReq|null} [spin] GameReqData spin
         * @property {jlwr.ICheckMallReq|null} [checkMall] GameReqData checkMall
         */

        /**
         * Constructs a new GameReqData.
         * @memberof jlwr
         * @classdesc Represents a GameReqData.
         * @implements IGameReqData
         * @constructor
         * @param {jlwr.IGameReqData=} [properties] Properties to set
         */
        function GameReqData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameReqData aid.
         * @member {number} aid
         * @memberof jlwr.GameReqData
         * @instance
         */
        GameReqData.prototype.aid = 0;

        /**
         * GameReqData apiid.
         * @member {number} apiid
         * @memberof jlwr.GameReqData
         * @instance
         */
        GameReqData.prototype.apiid = 0;

        /**
         * GameReqData token.
         * @member {string} token
         * @memberof jlwr.GameReqData
         * @instance
         */
        GameReqData.prototype.token = "";

        /**
         * GameReqData encode.
         * @member {Uint8Array} encode
         * @memberof jlwr.GameReqData
         * @instance
         */
        GameReqData.prototype.encode = $util.newBuffer([]);

        /**
         * GameReqData spin.
         * @member {jlwr.ISpinReq|null|undefined} spin
         * @memberof jlwr.GameReqData
         * @instance
         */
        GameReqData.prototype.spin = null;

        /**
         * GameReqData checkMall.
         * @member {jlwr.ICheckMallReq|null|undefined} checkMall
         * @memberof jlwr.GameReqData
         * @instance
         */
        GameReqData.prototype.checkMall = null;

        /**
         * Creates a new GameReqData instance using the specified properties.
         * @function create
         * @memberof jlwr.GameReqData
         * @static
         * @param {jlwr.IGameReqData=} [properties] Properties to set
         * @returns {jlwr.GameReqData} GameReqData instance
         */
        GameReqData.create = function create(properties) {
            return new GameReqData(properties);
        };

        /**
         * Encodes the specified GameReqData message. Does not implicitly {@link jlwr.GameReqData.verify|verify} messages.
         * @function encode
         * @memberof jlwr.GameReqData
         * @static
         * @param {jlwr.IGameReqData} message GameReqData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameReqData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            if (message.apiid != null && Object.hasOwnProperty.call(message, "apiid"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.apiid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
            if (message.encode != null && Object.hasOwnProperty.call(message, "encode"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encode);
            if (message.spin != null && Object.hasOwnProperty.call(message, "spin"))
                $root.jlwr.SpinReq.encode(message.spin, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.checkMall != null && Object.hasOwnProperty.call(message, "checkMall"))
                $root.jlwr.CheckMallReq.encode(message.checkMall, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GameReqData message, length delimited. Does not implicitly {@link jlwr.GameReqData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.GameReqData
         * @static
         * @param {jlwr.IGameReqData} message GameReqData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameReqData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameReqData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.GameReqData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.GameReqData} GameReqData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameReqData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.GameReqData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                case 2: {
                        message.apiid = reader.int32();
                        break;
                    }
                case 3: {
                        message.token = reader.string();
                        break;
                    }
                case 4: {
                        message.encode = reader.bytes();
                        break;
                    }
                case 5: {
                        message.spin = $root.jlwr.SpinReq.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.checkMall = $root.jlwr.CheckMallReq.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameReqData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.GameReqData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.GameReqData} GameReqData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameReqData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameReqData message.
         * @function verify
         * @memberof jlwr.GameReqData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameReqData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            if (message.apiid != null && message.hasOwnProperty("apiid"))
                if (!$util.isInteger(message.apiid))
                    return "apiid: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.encode != null && message.hasOwnProperty("encode"))
                if (!(message.encode && typeof message.encode.length === "number" || $util.isString(message.encode)))
                    return "encode: buffer expected";
            if (message.spin != null && message.hasOwnProperty("spin")) {
                let error = $root.jlwr.SpinReq.verify(message.spin);
                if (error)
                    return "spin." + error;
            }
            if (message.checkMall != null && message.hasOwnProperty("checkMall")) {
                let error = $root.jlwr.CheckMallReq.verify(message.checkMall);
                if (error)
                    return "checkMall." + error;
            }
            return null;
        };

        /**
         * Creates a GameReqData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.GameReqData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.GameReqData} GameReqData
         */
        GameReqData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.GameReqData)
                return object;
            let message = new $root.jlwr.GameReqData();
            if (object.aid != null)
                message.aid = object.aid | 0;
            if (object.apiid != null)
                message.apiid = object.apiid | 0;
            if (object.token != null)
                message.token = String(object.token);
            if (object.encode != null)
                if (typeof object.encode === "string")
                    $util.base64.decode(object.encode, message.encode = $util.newBuffer($util.base64.length(object.encode)), 0);
                else if (object.encode.length >= 0)
                    message.encode = object.encode;
            if (object.spin != null) {
                if (typeof object.spin !== "object")
                    throw TypeError(".jlwr.GameReqData.spin: object expected");
                message.spin = $root.jlwr.SpinReq.fromObject(object.spin);
            }
            if (object.checkMall != null) {
                if (typeof object.checkMall !== "object")
                    throw TypeError(".jlwr.GameReqData.checkMall: object expected");
                message.checkMall = $root.jlwr.CheckMallReq.fromObject(object.checkMall);
            }
            return message;
        };

        /**
         * Creates a plain object from a GameReqData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.GameReqData
         * @static
         * @param {jlwr.GameReqData} message GameReqData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameReqData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.aid = 0;
                object.apiid = 0;
                object.token = "";
                if (options.bytes === String)
                    object.encode = "";
                else {
                    object.encode = [];
                    if (options.bytes !== Array)
                        object.encode = $util.newBuffer(object.encode);
                }
                object.spin = null;
                object.checkMall = null;
            }
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            if (message.apiid != null && message.hasOwnProperty("apiid"))
                object.apiid = message.apiid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.encode != null && message.hasOwnProperty("encode"))
                object.encode = options.bytes === String ? $util.base64.encode(message.encode, 0, message.encode.length) : options.bytes === Array ? Array.prototype.slice.call(message.encode) : message.encode;
            if (message.spin != null && message.hasOwnProperty("spin"))
                object.spin = $root.jlwr.SpinReq.toObject(message.spin, options);
            if (message.checkMall != null && message.hasOwnProperty("checkMall"))
                object.checkMall = $root.jlwr.CheckMallReq.toObject(message.checkMall, options);
            return object;
        };

        /**
         * Converts this GameReqData to JSON.
         * @function toJSON
         * @memberof jlwr.GameReqData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameReqData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameReqData
         * @function getTypeUrl
         * @memberof jlwr.GameReqData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameReqData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.GameReqData";
        };

        return GameReqData;
    })();

    jlwr.SpinReq = (function() {

        /**
         * Properties of a SpinReq.
         * @memberof jlwr
         * @interface ISpinReq
         * @property {number|null} [Bet] SpinReq Bet
         * @property {number|null} [Extra] SpinReq Extra
         * @property {number|null} [Currency] SpinReq Currency
         * @property {number|null} [TestMode] SpinReq TestMode
         * @property {number|null} [MallID] SpinReq MallID
         * @property {number|null} [MallBet] SpinReq MallBet
         * @property {number|null} [VipLv] SpinReq VipLv
         * @property {number|null} [Free] SpinReq Free
         * @property {number|null} [Trial] SpinReq Trial
         * @property {number|null} [Level] SpinReq Level
         */

        /**
         * Constructs a new SpinReq.
         * @memberof jlwr
         * @classdesc Represents a SpinReq.
         * @implements ISpinReq
         * @constructor
         * @param {jlwr.ISpinReq=} [properties] Properties to set
         */
        function SpinReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinReq Bet.
         * @member {number} Bet
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.Bet = 0;

        /**
         * SpinReq Extra.
         * @member {number} Extra
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.Extra = 0;

        /**
         * SpinReq Currency.
         * @member {number} Currency
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.Currency = 0;

        /**
         * SpinReq TestMode.
         * @member {number} TestMode
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.TestMode = 0;

        /**
         * SpinReq MallID.
         * @member {number} MallID
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.MallID = 0;

        /**
         * SpinReq MallBet.
         * @member {number} MallBet
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.MallBet = 0;

        /**
         * SpinReq VipLv.
         * @member {number} VipLv
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.VipLv = 0;

        /**
         * SpinReq Free.
         * @member {number} Free
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.Free = 0;

        /**
         * SpinReq Trial.
         * @member {number} Trial
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.Trial = 0;

        /**
         * SpinReq Level.
         * @member {number} Level
         * @memberof jlwr.SpinReq
         * @instance
         */
        SpinReq.prototype.Level = 0;

        /**
         * Creates a new SpinReq instance using the specified properties.
         * @function create
         * @memberof jlwr.SpinReq
         * @static
         * @param {jlwr.ISpinReq=} [properties] Properties to set
         * @returns {jlwr.SpinReq} SpinReq instance
         */
        SpinReq.create = function create(properties) {
            return new SpinReq(properties);
        };

        /**
         * Encodes the specified SpinReq message. Does not implicitly {@link jlwr.SpinReq.verify|verify} messages.
         * @function encode
         * @memberof jlwr.SpinReq
         * @static
         * @param {jlwr.ISpinReq} message SpinReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Bet != null && Object.hasOwnProperty.call(message, "Bet"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.Bet);
            if (message.Extra != null && Object.hasOwnProperty.call(message, "Extra"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Extra);
            if (message.Currency != null && Object.hasOwnProperty.call(message, "Currency"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.Currency);
            if (message.TestMode != null && Object.hasOwnProperty.call(message, "TestMode"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.TestMode);
            if (message.MallID != null && Object.hasOwnProperty.call(message, "MallID"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.MallID);
            if (message.MallBet != null && Object.hasOwnProperty.call(message, "MallBet"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.MallBet);
            if (message.VipLv != null && Object.hasOwnProperty.call(message, "VipLv"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.VipLv);
            if (message.Free != null && Object.hasOwnProperty.call(message, "Free"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.Free);
            if (message.Trial != null && Object.hasOwnProperty.call(message, "Trial"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.Trial);
            if (message.Level != null && Object.hasOwnProperty.call(message, "Level"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.Level);
            return writer;
        };

        /**
         * Encodes the specified SpinReq message, length delimited. Does not implicitly {@link jlwr.SpinReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.SpinReq
         * @static
         * @param {jlwr.ISpinReq} message SpinReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.SpinReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.SpinReq} SpinReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.SpinReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Bet = reader.double();
                        break;
                    }
                case 2: {
                        message.Extra = reader.int32();
                        break;
                    }
                case 3: {
                        message.Currency = reader.int32();
                        break;
                    }
                case 4: {
                        message.TestMode = reader.int32();
                        break;
                    }
                case 5: {
                        message.MallID = reader.int32();
                        break;
                    }
                case 6: {
                        message.MallBet = reader.double();
                        break;
                    }
                case 7: {
                        message.VipLv = reader.int32();
                        break;
                    }
                case 8: {
                        message.Free = reader.int32();
                        break;
                    }
                case 9: {
                        message.Trial = reader.int32();
                        break;
                    }
                case 10: {
                        message.Level = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.SpinReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.SpinReq} SpinReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinReq message.
         * @function verify
         * @memberof jlwr.SpinReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                if (typeof message.Bet !== "number")
                    return "Bet: number expected";
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                if (!$util.isInteger(message.Extra))
                    return "Extra: integer expected";
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                if (!$util.isInteger(message.Currency))
                    return "Currency: integer expected";
            if (message.TestMode != null && message.hasOwnProperty("TestMode"))
                if (!$util.isInteger(message.TestMode))
                    return "TestMode: integer expected";
            if (message.MallID != null && message.hasOwnProperty("MallID"))
                if (!$util.isInteger(message.MallID))
                    return "MallID: integer expected";
            if (message.MallBet != null && message.hasOwnProperty("MallBet"))
                if (typeof message.MallBet !== "number")
                    return "MallBet: number expected";
            if (message.VipLv != null && message.hasOwnProperty("VipLv"))
                if (!$util.isInteger(message.VipLv))
                    return "VipLv: integer expected";
            if (message.Free != null && message.hasOwnProperty("Free"))
                if (!$util.isInteger(message.Free))
                    return "Free: integer expected";
            if (message.Trial != null && message.hasOwnProperty("Trial"))
                if (!$util.isInteger(message.Trial))
                    return "Trial: integer expected";
            if (message.Level != null && message.hasOwnProperty("Level"))
                if (!$util.isInteger(message.Level))
                    return "Level: integer expected";
            return null;
        };

        /**
         * Creates a SpinReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.SpinReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.SpinReq} SpinReq
         */
        SpinReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.SpinReq)
                return object;
            let message = new $root.jlwr.SpinReq();
            if (object.Bet != null)
                message.Bet = Number(object.Bet);
            if (object.Extra != null)
                message.Extra = object.Extra | 0;
            if (object.Currency != null)
                message.Currency = object.Currency | 0;
            if (object.TestMode != null)
                message.TestMode = object.TestMode | 0;
            if (object.MallID != null)
                message.MallID = object.MallID | 0;
            if (object.MallBet != null)
                message.MallBet = Number(object.MallBet);
            if (object.VipLv != null)
                message.VipLv = object.VipLv | 0;
            if (object.Free != null)
                message.Free = object.Free | 0;
            if (object.Trial != null)
                message.Trial = object.Trial | 0;
            if (object.Level != null)
                message.Level = object.Level | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.SpinReq
         * @static
         * @param {jlwr.SpinReq} message SpinReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.Bet = 0;
                object.Extra = 0;
                object.Currency = 0;
                object.TestMode = 0;
                object.MallID = 0;
                object.MallBet = 0;
                object.VipLv = 0;
                object.Free = 0;
                object.Trial = 0;
                object.Level = 0;
            }
            if (message.Bet != null && message.hasOwnProperty("Bet"))
                object.Bet = options.json && !isFinite(message.Bet) ? String(message.Bet) : message.Bet;
            if (message.Extra != null && message.hasOwnProperty("Extra"))
                object.Extra = message.Extra;
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                object.Currency = message.Currency;
            if (message.TestMode != null && message.hasOwnProperty("TestMode"))
                object.TestMode = message.TestMode;
            if (message.MallID != null && message.hasOwnProperty("MallID"))
                object.MallID = message.MallID;
            if (message.MallBet != null && message.hasOwnProperty("MallBet"))
                object.MallBet = options.json && !isFinite(message.MallBet) ? String(message.MallBet) : message.MallBet;
            if (message.VipLv != null && message.hasOwnProperty("VipLv"))
                object.VipLv = message.VipLv;
            if (message.Free != null && message.hasOwnProperty("Free"))
                object.Free = message.Free;
            if (message.Trial != null && message.hasOwnProperty("Trial"))
                object.Trial = message.Trial;
            if (message.Level != null && message.hasOwnProperty("Level"))
                object.Level = message.Level;
            return object;
        };

        /**
         * Converts this SpinReq to JSON.
         * @function toJSON
         * @memberof jlwr.SpinReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinReq
         * @function getTypeUrl
         * @memberof jlwr.SpinReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.SpinReq";
        };

        return SpinReq;
    })();

    jlwr.CheckMallReq = (function() {

        /**
         * Properties of a CheckMallReq.
         * @memberof jlwr
         * @interface ICheckMallReq
         * @property {number|null} [Currency] CheckMallReq Currency
         */

        /**
         * Constructs a new CheckMallReq.
         * @memberof jlwr
         * @classdesc Represents a CheckMallReq.
         * @implements ICheckMallReq
         * @constructor
         * @param {jlwr.ICheckMallReq=} [properties] Properties to set
         */
        function CheckMallReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CheckMallReq Currency.
         * @member {number} Currency
         * @memberof jlwr.CheckMallReq
         * @instance
         */
        CheckMallReq.prototype.Currency = 0;

        /**
         * Creates a new CheckMallReq instance using the specified properties.
         * @function create
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {jlwr.ICheckMallReq=} [properties] Properties to set
         * @returns {jlwr.CheckMallReq} CheckMallReq instance
         */
        CheckMallReq.create = function create(properties) {
            return new CheckMallReq(properties);
        };

        /**
         * Encodes the specified CheckMallReq message. Does not implicitly {@link jlwr.CheckMallReq.verify|verify} messages.
         * @function encode
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {jlwr.ICheckMallReq} message CheckMallReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Currency != null && Object.hasOwnProperty.call(message, "Currency"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Currency);
            return writer;
        };

        /**
         * Encodes the specified CheckMallReq message, length delimited. Does not implicitly {@link jlwr.CheckMallReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {jlwr.ICheckMallReq} message CheckMallReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CheckMallReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.CheckMallReq} CheckMallReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.CheckMallReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Currency = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CheckMallReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.CheckMallReq} CheckMallReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckMallReq message.
         * @function verify
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckMallReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                if (!$util.isInteger(message.Currency))
                    return "Currency: integer expected";
            return null;
        };

        /**
         * Creates a CheckMallReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.CheckMallReq} CheckMallReq
         */
        CheckMallReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.CheckMallReq)
                return object;
            let message = new $root.jlwr.CheckMallReq();
            if (object.Currency != null)
                message.Currency = object.Currency | 0;
            return message;
        };

        /**
         * Creates a plain object from a CheckMallReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {jlwr.CheckMallReq} message CheckMallReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckMallReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.Currency = 0;
            if (message.Currency != null && message.hasOwnProperty("Currency"))
                object.Currency = message.Currency;
            return object;
        };

        /**
         * Converts this CheckMallReq to JSON.
         * @function toJSON
         * @memberof jlwr.CheckMallReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckMallReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CheckMallReq
         * @function getTypeUrl
         * @memberof jlwr.CheckMallReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CheckMallReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.CheckMallReq";
        };

        return CheckMallReq;
    })();

    jlwr.ResData = (function() {

        /**
         * Properties of a ResData.
         * @memberof jlwr
         * @interface IResData
         * @property {number|null} [ret] ResData ret
         * @property {number|null} [type] ResData type
         * @property {string|null} [token] ResData token
         * @property {string|null} [msg] ResData msg
         * @property {jlwr.IInfoData|null} [data] ResData data
         */

        /**
         * Constructs a new ResData.
         * @memberof jlwr
         * @classdesc Represents a ResData.
         * @implements IResData
         * @constructor
         * @param {jlwr.IResData=} [properties] Properties to set
         */
        function ResData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ResData ret.
         * @member {number} ret
         * @memberof jlwr.ResData
         * @instance
         */
        ResData.prototype.ret = 0;

        /**
         * ResData type.
         * @member {number} type
         * @memberof jlwr.ResData
         * @instance
         */
        ResData.prototype.type = 0;

        /**
         * ResData token.
         * @member {string} token
         * @memberof jlwr.ResData
         * @instance
         */
        ResData.prototype.token = "";

        /**
         * ResData msg.
         * @member {string} msg
         * @memberof jlwr.ResData
         * @instance
         */
        ResData.prototype.msg = "";

        /**
         * ResData data.
         * @member {jlwr.IInfoData|null|undefined} data
         * @memberof jlwr.ResData
         * @instance
         */
        ResData.prototype.data = null;

        /**
         * Creates a new ResData instance using the specified properties.
         * @function create
         * @memberof jlwr.ResData
         * @static
         * @param {jlwr.IResData=} [properties] Properties to set
         * @returns {jlwr.ResData} ResData instance
         */
        ResData.create = function create(properties) {
            return new ResData(properties);
        };

        /**
         * Encodes the specified ResData message. Does not implicitly {@link jlwr.ResData.verify|verify} messages.
         * @function encode
         * @memberof jlwr.ResData
         * @static
         * @param {jlwr.IResData} message ResData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ret != null && Object.hasOwnProperty.call(message, "ret"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ret);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
            if (message.msg != null && Object.hasOwnProperty.call(message, "msg"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.msg);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.jlwr.InfoData.encode(message.data, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ResData message, length delimited. Does not implicitly {@link jlwr.ResData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.ResData
         * @static
         * @param {jlwr.IResData} message ResData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ResData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ResData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.ResData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.ResData} ResData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.ResData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ret = reader.int32();
                        break;
                    }
                case 2: {
                        message.type = reader.int32();
                        break;
                    }
                case 3: {
                        message.token = reader.string();
                        break;
                    }
                case 4: {
                        message.msg = reader.string();
                        break;
                    }
                case 5: {
                        message.data = $root.jlwr.InfoData.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ResData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.ResData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.ResData} ResData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ResData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ResData message.
         * @function verify
         * @memberof jlwr.ResData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ResData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ret != null && message.hasOwnProperty("ret"))
                if (!$util.isInteger(message.ret))
                    return "ret: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.msg != null && message.hasOwnProperty("msg"))
                if (!$util.isString(message.msg))
                    return "msg: string expected";
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.jlwr.InfoData.verify(message.data);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        /**
         * Creates a ResData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.ResData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.ResData} ResData
         */
        ResData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.ResData)
                return object;
            let message = new $root.jlwr.ResData();
            if (object.ret != null)
                message.ret = object.ret | 0;
            if (object.type != null)
                message.type = object.type | 0;
            if (object.token != null)
                message.token = String(object.token);
            if (object.msg != null)
                message.msg = String(object.msg);
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".jlwr.ResData.data: object expected");
                message.data = $root.jlwr.InfoData.fromObject(object.data);
            }
            return message;
        };

        /**
         * Creates a plain object from a ResData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.ResData
         * @static
         * @param {jlwr.ResData} message ResData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ResData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ret = 0;
                object.type = 0;
                object.token = "";
                object.msg = "";
                object.data = null;
            }
            if (message.ret != null && message.hasOwnProperty("ret"))
                object.ret = message.ret;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.msg != null && message.hasOwnProperty("msg"))
                object.msg = message.msg;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.jlwr.InfoData.toObject(message.data, options);
            return object;
        };

        /**
         * Converts this ResData to JSON.
         * @function toJSON
         * @memberof jlwr.ResData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ResData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ResData
         * @function getTypeUrl
         * @memberof jlwr.ResData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ResData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.ResData";
        };

        return ResData;
    })();

    jlwr.InfoData = (function() {

        /**
         * Properties of an InfoData.
         * @memberof jlwr
         * @interface IInfoData
         * @property {Uint8Array|null} [encode] InfoData encode
         * @property {jlwr.ISpinAllData|null} [spinAll] InfoData spinAll
         * @property {jlwr.IGameInfoAck|null} [gameInfo] InfoData gameInfo
         * @property {jlwr.ICheckMallAck|null} [checkMall] InfoData checkMall
         * @property {jlwr.ILoginDataAck|null} [loginData] InfoData loginData
         * @property {jlwr.IHeartAck|null} [heart] InfoData heart
         * @property {jlwr.IExchangeAck|null} [exchange] InfoData exchange
         * @property {jlwr.ILastSpinAck|null} [lastSpin] InfoData lastSpin
         */

        /**
         * Constructs a new InfoData.
         * @memberof jlwr
         * @classdesc Represents an InfoData.
         * @implements IInfoData
         * @constructor
         * @param {jlwr.IInfoData=} [properties] Properties to set
         */
        function InfoData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InfoData encode.
         * @member {Uint8Array} encode
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.encode = $util.newBuffer([]);

        /**
         * InfoData spinAll.
         * @member {jlwr.ISpinAllData|null|undefined} spinAll
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.spinAll = null;

        /**
         * InfoData gameInfo.
         * @member {jlwr.IGameInfoAck|null|undefined} gameInfo
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.gameInfo = null;

        /**
         * InfoData checkMall.
         * @member {jlwr.ICheckMallAck|null|undefined} checkMall
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.checkMall = null;

        /**
         * InfoData loginData.
         * @member {jlwr.ILoginDataAck|null|undefined} loginData
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.loginData = null;

        /**
         * InfoData heart.
         * @member {jlwr.IHeartAck|null|undefined} heart
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.heart = null;

        /**
         * InfoData exchange.
         * @member {jlwr.IExchangeAck|null|undefined} exchange
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.exchange = null;

        /**
         * InfoData lastSpin.
         * @member {jlwr.ILastSpinAck|null|undefined} lastSpin
         * @memberof jlwr.InfoData
         * @instance
         */
        InfoData.prototype.lastSpin = null;

        /**
         * Creates a new InfoData instance using the specified properties.
         * @function create
         * @memberof jlwr.InfoData
         * @static
         * @param {jlwr.IInfoData=} [properties] Properties to set
         * @returns {jlwr.InfoData} InfoData instance
         */
        InfoData.create = function create(properties) {
            return new InfoData(properties);
        };

        /**
         * Encodes the specified InfoData message. Does not implicitly {@link jlwr.InfoData.verify|verify} messages.
         * @function encode
         * @memberof jlwr.InfoData
         * @static
         * @param {jlwr.IInfoData} message InfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.encode != null && Object.hasOwnProperty.call(message, "encode"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encode);
            if (message.spinAll != null && Object.hasOwnProperty.call(message, "spinAll"))
                $root.jlwr.SpinAllData.encode(message.spinAll, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.gameInfo != null && Object.hasOwnProperty.call(message, "gameInfo"))
                $root.jlwr.GameInfoAck.encode(message.gameInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.checkMall != null && Object.hasOwnProperty.call(message, "checkMall"))
                $root.jlwr.CheckMallAck.encode(message.checkMall, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.loginData != null && Object.hasOwnProperty.call(message, "loginData"))
                $root.jlwr.LoginDataAck.encode(message.loginData, writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            if (message.heart != null && Object.hasOwnProperty.call(message, "heart"))
                $root.jlwr.HeartAck.encode(message.heart, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.exchange != null && Object.hasOwnProperty.call(message, "exchange"))
                $root.jlwr.ExchangeAck.encode(message.exchange, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.lastSpin != null && Object.hasOwnProperty.call(message, "lastSpin"))
                $root.jlwr.LastSpinAck.encode(message.lastSpin, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified InfoData message, length delimited. Does not implicitly {@link jlwr.InfoData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.InfoData
         * @static
         * @param {jlwr.IInfoData} message InfoData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an InfoData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.InfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.InfoData} InfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InfoData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.InfoData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encode = reader.bytes();
                        break;
                    }
                case 2: {
                        message.spinAll = $root.jlwr.SpinAllData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.gameInfo = $root.jlwr.GameInfoAck.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.checkMall = $root.jlwr.CheckMallAck.decode(reader, reader.uint32());
                        break;
                    }
                case 5: {
                        message.loginData = $root.jlwr.LoginDataAck.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.heart = $root.jlwr.HeartAck.decode(reader, reader.uint32());
                        break;
                    }
                case 7: {
                        message.exchange = $root.jlwr.ExchangeAck.decode(reader, reader.uint32());
                        break;
                    }
                case 8: {
                        message.lastSpin = $root.jlwr.LastSpinAck.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an InfoData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.InfoData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.InfoData} InfoData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InfoData message.
         * @function verify
         * @memberof jlwr.InfoData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InfoData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.encode != null && message.hasOwnProperty("encode"))
                if (!(message.encode && typeof message.encode.length === "number" || $util.isString(message.encode)))
                    return "encode: buffer expected";
            if (message.spinAll != null && message.hasOwnProperty("spinAll")) {
                let error = $root.jlwr.SpinAllData.verify(message.spinAll);
                if (error)
                    return "spinAll." + error;
            }
            if (message.gameInfo != null && message.hasOwnProperty("gameInfo")) {
                let error = $root.jlwr.GameInfoAck.verify(message.gameInfo);
                if (error)
                    return "gameInfo." + error;
            }
            if (message.checkMall != null && message.hasOwnProperty("checkMall")) {
                let error = $root.jlwr.CheckMallAck.verify(message.checkMall);
                if (error)
                    return "checkMall." + error;
            }
            if (message.loginData != null && message.hasOwnProperty("loginData")) {
                let error = $root.jlwr.LoginDataAck.verify(message.loginData);
                if (error)
                    return "loginData." + error;
            }
            if (message.heart != null && message.hasOwnProperty("heart")) {
                let error = $root.jlwr.HeartAck.verify(message.heart);
                if (error)
                    return "heart." + error;
            }
            if (message.exchange != null && message.hasOwnProperty("exchange")) {
                let error = $root.jlwr.ExchangeAck.verify(message.exchange);
                if (error)
                    return "exchange." + error;
            }
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin")) {
                let error = $root.jlwr.LastSpinAck.verify(message.lastSpin);
                if (error)
                    return "lastSpin." + error;
            }
            return null;
        };

        /**
         * Creates an InfoData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.InfoData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.InfoData} InfoData
         */
        InfoData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.InfoData)
                return object;
            let message = new $root.jlwr.InfoData();
            if (object.encode != null)
                if (typeof object.encode === "string")
                    $util.base64.decode(object.encode, message.encode = $util.newBuffer($util.base64.length(object.encode)), 0);
                else if (object.encode.length >= 0)
                    message.encode = object.encode;
            if (object.spinAll != null) {
                if (typeof object.spinAll !== "object")
                    throw TypeError(".jlwr.InfoData.spinAll: object expected");
                message.spinAll = $root.jlwr.SpinAllData.fromObject(object.spinAll);
            }
            if (object.gameInfo != null) {
                if (typeof object.gameInfo !== "object")
                    throw TypeError(".jlwr.InfoData.gameInfo: object expected");
                message.gameInfo = $root.jlwr.GameInfoAck.fromObject(object.gameInfo);
            }
            if (object.checkMall != null) {
                if (typeof object.checkMall !== "object")
                    throw TypeError(".jlwr.InfoData.checkMall: object expected");
                message.checkMall = $root.jlwr.CheckMallAck.fromObject(object.checkMall);
            }
            if (object.loginData != null) {
                if (typeof object.loginData !== "object")
                    throw TypeError(".jlwr.InfoData.loginData: object expected");
                message.loginData = $root.jlwr.LoginDataAck.fromObject(object.loginData);
            }
            if (object.heart != null) {
                if (typeof object.heart !== "object")
                    throw TypeError(".jlwr.InfoData.heart: object expected");
                message.heart = $root.jlwr.HeartAck.fromObject(object.heart);
            }
            if (object.exchange != null) {
                if (typeof object.exchange !== "object")
                    throw TypeError(".jlwr.InfoData.exchange: object expected");
                message.exchange = $root.jlwr.ExchangeAck.fromObject(object.exchange);
            }
            if (object.lastSpin != null) {
                if (typeof object.lastSpin !== "object")
                    throw TypeError(".jlwr.InfoData.lastSpin: object expected");
                message.lastSpin = $root.jlwr.LastSpinAck.fromObject(object.lastSpin);
            }
            return message;
        };

        /**
         * Creates a plain object from an InfoData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.InfoData
         * @static
         * @param {jlwr.InfoData} message InfoData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encode = "";
                else {
                    object.encode = [];
                    if (options.bytes !== Array)
                        object.encode = $util.newBuffer(object.encode);
                }
                object.spinAll = null;
                object.gameInfo = null;
                object.checkMall = null;
                object.loginData = null;
                object.heart = null;
                object.exchange = null;
                object.lastSpin = null;
            }
            if (message.encode != null && message.hasOwnProperty("encode"))
                object.encode = options.bytes === String ? $util.base64.encode(message.encode, 0, message.encode.length) : options.bytes === Array ? Array.prototype.slice.call(message.encode) : message.encode;
            if (message.spinAll != null && message.hasOwnProperty("spinAll"))
                object.spinAll = $root.jlwr.SpinAllData.toObject(message.spinAll, options);
            if (message.gameInfo != null && message.hasOwnProperty("gameInfo"))
                object.gameInfo = $root.jlwr.GameInfoAck.toObject(message.gameInfo, options);
            if (message.checkMall != null && message.hasOwnProperty("checkMall"))
                object.checkMall = $root.jlwr.CheckMallAck.toObject(message.checkMall, options);
            if (message.loginData != null && message.hasOwnProperty("loginData"))
                object.loginData = $root.jlwr.LoginDataAck.toObject(message.loginData, options);
            if (message.heart != null && message.hasOwnProperty("heart"))
                object.heart = $root.jlwr.HeartAck.toObject(message.heart, options);
            if (message.exchange != null && message.hasOwnProperty("exchange"))
                object.exchange = $root.jlwr.ExchangeAck.toObject(message.exchange, options);
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin"))
                object.lastSpin = $root.jlwr.LastSpinAck.toObject(message.lastSpin, options);
            return object;
        };

        /**
         * Converts this InfoData to JSON.
         * @function toJSON
         * @memberof jlwr.InfoData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InfoData
         * @function getTypeUrl
         * @memberof jlwr.InfoData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InfoData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.InfoData";
        };

        return InfoData;
    })();

    jlwr.SpinAllData = (function() {

        /**
         * Properties of a SpinAllData.
         * @memberof jlwr
         * @interface ISpinAllData
         * @property {jlwr.ISpinAck|null} [info] SpinAllData info
         * @property {jlwr.IServiceData|null} [serviceInfo] SpinAllData serviceInfo
         * @property {Uint8Array|null} [serviceProto] SpinAllData serviceProto
         */

        /**
         * Constructs a new SpinAllData.
         * @memberof jlwr
         * @classdesc Represents a SpinAllData.
         * @implements ISpinAllData
         * @constructor
         * @param {jlwr.ISpinAllData=} [properties] Properties to set
         */
        function SpinAllData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAllData info.
         * @member {jlwr.ISpinAck|null|undefined} info
         * @memberof jlwr.SpinAllData
         * @instance
         */
        SpinAllData.prototype.info = null;

        /**
         * SpinAllData serviceInfo.
         * @member {jlwr.IServiceData|null|undefined} serviceInfo
         * @memberof jlwr.SpinAllData
         * @instance
         */
        SpinAllData.prototype.serviceInfo = null;

        /**
         * SpinAllData serviceProto.
         * @member {Uint8Array} serviceProto
         * @memberof jlwr.SpinAllData
         * @instance
         */
        SpinAllData.prototype.serviceProto = $util.newBuffer([]);

        /**
         * Creates a new SpinAllData instance using the specified properties.
         * @function create
         * @memberof jlwr.SpinAllData
         * @static
         * @param {jlwr.ISpinAllData=} [properties] Properties to set
         * @returns {jlwr.SpinAllData} SpinAllData instance
         */
        SpinAllData.create = function create(properties) {
            return new SpinAllData(properties);
        };

        /**
         * Encodes the specified SpinAllData message. Does not implicitly {@link jlwr.SpinAllData.verify|verify} messages.
         * @function encode
         * @memberof jlwr.SpinAllData
         * @static
         * @param {jlwr.ISpinAllData} message SpinAllData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAllData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.jlwr.SpinAck.encode(message.info, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.serviceInfo != null && Object.hasOwnProperty.call(message, "serviceInfo"))
                $root.jlwr.ServiceData.encode(message.serviceInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.serviceProto != null && Object.hasOwnProperty.call(message, "serviceProto"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.serviceProto);
            return writer;
        };

        /**
         * Encodes the specified SpinAllData message, length delimited. Does not implicitly {@link jlwr.SpinAllData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.SpinAllData
         * @static
         * @param {jlwr.ISpinAllData} message SpinAllData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAllData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAllData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.SpinAllData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.SpinAllData} SpinAllData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAllData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.SpinAllData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.info = $root.jlwr.SpinAck.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.serviceInfo = $root.jlwr.ServiceData.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.serviceProto = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAllData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.SpinAllData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.SpinAllData} SpinAllData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAllData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAllData message.
         * @function verify
         * @memberof jlwr.SpinAllData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAllData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                let error = $root.jlwr.SpinAck.verify(message.info);
                if (error)
                    return "info." + error;
            }
            if (message.serviceInfo != null && message.hasOwnProperty("serviceInfo")) {
                let error = $root.jlwr.ServiceData.verify(message.serviceInfo);
                if (error)
                    return "serviceInfo." + error;
            }
            if (message.serviceProto != null && message.hasOwnProperty("serviceProto"))
                if (!(message.serviceProto && typeof message.serviceProto.length === "number" || $util.isString(message.serviceProto)))
                    return "serviceProto: buffer expected";
            return null;
        };

        /**
         * Creates a SpinAllData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.SpinAllData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.SpinAllData} SpinAllData
         */
        SpinAllData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.SpinAllData)
                return object;
            let message = new $root.jlwr.SpinAllData();
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".jlwr.SpinAllData.info: object expected");
                message.info = $root.jlwr.SpinAck.fromObject(object.info);
            }
            if (object.serviceInfo != null) {
                if (typeof object.serviceInfo !== "object")
                    throw TypeError(".jlwr.SpinAllData.serviceInfo: object expected");
                message.serviceInfo = $root.jlwr.ServiceData.fromObject(object.serviceInfo);
            }
            if (object.serviceProto != null)
                if (typeof object.serviceProto === "string")
                    $util.base64.decode(object.serviceProto, message.serviceProto = $util.newBuffer($util.base64.length(object.serviceProto)), 0);
                else if (object.serviceProto.length >= 0)
                    message.serviceProto = object.serviceProto;
            return message;
        };

        /**
         * Creates a plain object from a SpinAllData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.SpinAllData
         * @static
         * @param {jlwr.SpinAllData} message SpinAllData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAllData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.info = null;
                object.serviceInfo = null;
                if (options.bytes === String)
                    object.serviceProto = "";
                else {
                    object.serviceProto = [];
                    if (options.bytes !== Array)
                        object.serviceProto = $util.newBuffer(object.serviceProto);
                }
            }
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.jlwr.SpinAck.toObject(message.info, options);
            if (message.serviceInfo != null && message.hasOwnProperty("serviceInfo"))
                object.serviceInfo = $root.jlwr.ServiceData.toObject(message.serviceInfo, options);
            if (message.serviceProto != null && message.hasOwnProperty("serviceProto"))
                object.serviceProto = options.bytes === String ? $util.base64.encode(message.serviceProto, 0, message.serviceProto.length) : options.bytes === Array ? Array.prototype.slice.call(message.serviceProto) : message.serviceProto;
            return object;
        };

        /**
         * Converts this SpinAllData to JSON.
         * @function toJSON
         * @memberof jlwr.SpinAllData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAllData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAllData
         * @function getTypeUrl
         * @memberof jlwr.SpinAllData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAllData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.SpinAllData";
        };

        return SpinAllData;
    })();

    jlwr.ServiceData = (function() {

        /**
         * Properties of a ServiceData.
         * @memberof jlwr
         * @interface IServiceData
         * @property {number|null} [missionReward] ServiceData missionReward
         * @property {number|null} [jpType] ServiceData jpType
         * @property {number|null} [jpWin] ServiceData jpWin
         * @property {number|null} [fullJpWin] ServiceData fullJpWin
         * @property {number|null} [trialfinish] ServiceData trialfinish
         * @property {number|null} [trialaward] ServiceData trialaward
         * @property {number|null} [freeremain] ServiceData freeremain
         */

        /**
         * Constructs a new ServiceData.
         * @memberof jlwr
         * @classdesc Represents a ServiceData.
         * @implements IServiceData
         * @constructor
         * @param {jlwr.IServiceData=} [properties] Properties to set
         */
        function ServiceData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ServiceData missionReward.
         * @member {number} missionReward
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.missionReward = 0;

        /**
         * ServiceData jpType.
         * @member {number} jpType
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.jpType = 0;

        /**
         * ServiceData jpWin.
         * @member {number} jpWin
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.jpWin = 0;

        /**
         * ServiceData fullJpWin.
         * @member {number} fullJpWin
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.fullJpWin = 0;

        /**
         * ServiceData trialfinish.
         * @member {number} trialfinish
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.trialfinish = 0;

        /**
         * ServiceData trialaward.
         * @member {number} trialaward
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.trialaward = 0;

        /**
         * ServiceData freeremain.
         * @member {number} freeremain
         * @memberof jlwr.ServiceData
         * @instance
         */
        ServiceData.prototype.freeremain = 0;

        /**
         * Creates a new ServiceData instance using the specified properties.
         * @function create
         * @memberof jlwr.ServiceData
         * @static
         * @param {jlwr.IServiceData=} [properties] Properties to set
         * @returns {jlwr.ServiceData} ServiceData instance
         */
        ServiceData.create = function create(properties) {
            return new ServiceData(properties);
        };

        /**
         * Encodes the specified ServiceData message. Does not implicitly {@link jlwr.ServiceData.verify|verify} messages.
         * @function encode
         * @memberof jlwr.ServiceData
         * @static
         * @param {jlwr.IServiceData} message ServiceData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServiceData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.missionReward != null && Object.hasOwnProperty.call(message, "missionReward"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.missionReward);
            if (message.jpType != null && Object.hasOwnProperty.call(message, "jpType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.jpType);
            if (message.jpWin != null && Object.hasOwnProperty.call(message, "jpWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.jpWin);
            if (message.fullJpWin != null && Object.hasOwnProperty.call(message, "fullJpWin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.fullJpWin);
            if (message.trialfinish != null && Object.hasOwnProperty.call(message, "trialfinish"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.trialfinish);
            if (message.trialaward != null && Object.hasOwnProperty.call(message, "trialaward"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.trialaward);
            if (message.freeremain != null && Object.hasOwnProperty.call(message, "freeremain"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.freeremain);
            return writer;
        };

        /**
         * Encodes the specified ServiceData message, length delimited. Does not implicitly {@link jlwr.ServiceData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.ServiceData
         * @static
         * @param {jlwr.IServiceData} message ServiceData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServiceData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ServiceData message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.ServiceData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.ServiceData} ServiceData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServiceData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.ServiceData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.missionReward = reader.uint32();
                        break;
                    }
                case 2: {
                        message.jpType = reader.int32();
                        break;
                    }
                case 3: {
                        message.jpWin = reader.double();
                        break;
                    }
                case 4: {
                        message.fullJpWin = reader.double();
                        break;
                    }
                case 5: {
                        message.trialfinish = reader.int32();
                        break;
                    }
                case 6: {
                        message.trialaward = reader.int32();
                        break;
                    }
                case 7: {
                        message.freeremain = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ServiceData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.ServiceData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.ServiceData} ServiceData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServiceData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServiceData message.
         * @function verify
         * @memberof jlwr.ServiceData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServiceData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.missionReward != null && message.hasOwnProperty("missionReward"))
                if (!$util.isInteger(message.missionReward))
                    return "missionReward: integer expected";
            if (message.jpType != null && message.hasOwnProperty("jpType"))
                if (!$util.isInteger(message.jpType))
                    return "jpType: integer expected";
            if (message.jpWin != null && message.hasOwnProperty("jpWin"))
                if (typeof message.jpWin !== "number")
                    return "jpWin: number expected";
            if (message.fullJpWin != null && message.hasOwnProperty("fullJpWin"))
                if (typeof message.fullJpWin !== "number")
                    return "fullJpWin: number expected";
            if (message.trialfinish != null && message.hasOwnProperty("trialfinish"))
                if (!$util.isInteger(message.trialfinish))
                    return "trialfinish: integer expected";
            if (message.trialaward != null && message.hasOwnProperty("trialaward"))
                if (!$util.isInteger(message.trialaward))
                    return "trialaward: integer expected";
            if (message.freeremain != null && message.hasOwnProperty("freeremain"))
                if (!$util.isInteger(message.freeremain))
                    return "freeremain: integer expected";
            return null;
        };

        /**
         * Creates a ServiceData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.ServiceData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.ServiceData} ServiceData
         */
        ServiceData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.ServiceData)
                return object;
            let message = new $root.jlwr.ServiceData();
            if (object.missionReward != null)
                message.missionReward = object.missionReward >>> 0;
            if (object.jpType != null)
                message.jpType = object.jpType | 0;
            if (object.jpWin != null)
                message.jpWin = Number(object.jpWin);
            if (object.fullJpWin != null)
                message.fullJpWin = Number(object.fullJpWin);
            if (object.trialfinish != null)
                message.trialfinish = object.trialfinish | 0;
            if (object.trialaward != null)
                message.trialaward = object.trialaward | 0;
            if (object.freeremain != null)
                message.freeremain = object.freeremain | 0;
            return message;
        };

        /**
         * Creates a plain object from a ServiceData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.ServiceData
         * @static
         * @param {jlwr.ServiceData} message ServiceData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServiceData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.missionReward = 0;
                object.jpType = 0;
                object.jpWin = 0;
                object.fullJpWin = 0;
                object.trialfinish = 0;
                object.trialaward = 0;
                object.freeremain = 0;
            }
            if (message.missionReward != null && message.hasOwnProperty("missionReward"))
                object.missionReward = message.missionReward;
            if (message.jpType != null && message.hasOwnProperty("jpType"))
                object.jpType = message.jpType;
            if (message.jpWin != null && message.hasOwnProperty("jpWin"))
                object.jpWin = options.json && !isFinite(message.jpWin) ? String(message.jpWin) : message.jpWin;
            if (message.fullJpWin != null && message.hasOwnProperty("fullJpWin"))
                object.fullJpWin = options.json && !isFinite(message.fullJpWin) ? String(message.fullJpWin) : message.fullJpWin;
            if (message.trialfinish != null && message.hasOwnProperty("trialfinish"))
                object.trialfinish = message.trialfinish;
            if (message.trialaward != null && message.hasOwnProperty("trialaward"))
                object.trialaward = message.trialaward;
            if (message.freeremain != null && message.hasOwnProperty("freeremain"))
                object.freeremain = message.freeremain;
            return object;
        };

        /**
         * Converts this ServiceData to JSON.
         * @function toJSON
         * @memberof jlwr.ServiceData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServiceData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ServiceData
         * @function getTypeUrl
         * @memberof jlwr.ServiceData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ServiceData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.ServiceData";
        };

        return ServiceData;
    })();

    jlwr.WalletInfo = (function() {

        /**
         * Properties of a WalletInfo.
         * @memberof jlwr
         * @interface IWalletInfo
         * @property {number|null} [currencyNumber] WalletInfo currencyNumber
         * @property {string|null} [currencyName] WalletInfo currencyName
         * @property {string|null} [currencySymbol] WalletInfo currencySymbol
         * @property {number|null} [coin] WalletInfo coin
         * @property {number|null} [bet] WalletInfo bet
         * @property {number|null} [unit] WalletInfo unit
         * @property {number|null} [ratio] WalletInfo ratio
         * @property {number|null} [rate] WalletInfo rate
         * @property {number|null} [upper] WalletInfo upper
         * @property {number|null} [lower] WalletInfo lower
         * @property {number|null} [cycle] WalletInfo cycle
         * @property {number|null} [decimal] WalletInfo decimal
         */

        /**
         * Constructs a new WalletInfo.
         * @memberof jlwr
         * @classdesc Represents a WalletInfo.
         * @implements IWalletInfo
         * @constructor
         * @param {jlwr.IWalletInfo=} [properties] Properties to set
         */
        function WalletInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WalletInfo currencyNumber.
         * @member {number} currencyNumber
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.currencyNumber = 0;

        /**
         * WalletInfo currencyName.
         * @member {string} currencyName
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.currencyName = "";

        /**
         * WalletInfo currencySymbol.
         * @member {string} currencySymbol
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.currencySymbol = "";

        /**
         * WalletInfo coin.
         * @member {number} coin
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.coin = 0;

        /**
         * WalletInfo bet.
         * @member {number} bet
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.bet = 0;

        /**
         * WalletInfo unit.
         * @member {number} unit
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.unit = 0;

        /**
         * WalletInfo ratio.
         * @member {number} ratio
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.ratio = 0;

        /**
         * WalletInfo rate.
         * @member {number} rate
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.rate = 0;

        /**
         * WalletInfo upper.
         * @member {number} upper
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.upper = 0;

        /**
         * WalletInfo lower.
         * @member {number} lower
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.lower = 0;

        /**
         * WalletInfo cycle.
         * @member {number} cycle
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.cycle = 0;

        /**
         * WalletInfo decimal.
         * @member {number} decimal
         * @memberof jlwr.WalletInfo
         * @instance
         */
        WalletInfo.prototype.decimal = 0;

        /**
         * Creates a new WalletInfo instance using the specified properties.
         * @function create
         * @memberof jlwr.WalletInfo
         * @static
         * @param {jlwr.IWalletInfo=} [properties] Properties to set
         * @returns {jlwr.WalletInfo} WalletInfo instance
         */
        WalletInfo.create = function create(properties) {
            return new WalletInfo(properties);
        };

        /**
         * Encodes the specified WalletInfo message. Does not implicitly {@link jlwr.WalletInfo.verify|verify} messages.
         * @function encode
         * @memberof jlwr.WalletInfo
         * @static
         * @param {jlwr.IWalletInfo} message WalletInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.currencyNumber != null && Object.hasOwnProperty.call(message, "currencyNumber"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.currencyNumber);
            if (message.currencyName != null && Object.hasOwnProperty.call(message, "currencyName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.currencyName);
            if (message.currencySymbol != null && Object.hasOwnProperty.call(message, "currencySymbol"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.currencySymbol);
            if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.coin);
            if (message.bet != null && Object.hasOwnProperty.call(message, "bet"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.bet);
            if (message.unit != null && Object.hasOwnProperty.call(message, "unit"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.unit);
            if (message.ratio != null && Object.hasOwnProperty.call(message, "ratio"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.ratio);
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 8, wireType 1 =*/65).double(message.rate);
            if (message.upper != null && Object.hasOwnProperty.call(message, "upper"))
                writer.uint32(/* id 9, wireType 1 =*/73).double(message.upper);
            if (message.lower != null && Object.hasOwnProperty.call(message, "lower"))
                writer.uint32(/* id 10, wireType 1 =*/81).double(message.lower);
            if (message.cycle != null && Object.hasOwnProperty.call(message, "cycle"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.cycle);
            if (message.decimal != null && Object.hasOwnProperty.call(message, "decimal"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.decimal);
            return writer;
        };

        /**
         * Encodes the specified WalletInfo message, length delimited. Does not implicitly {@link jlwr.WalletInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.WalletInfo
         * @static
         * @param {jlwr.IWalletInfo} message WalletInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WalletInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WalletInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.WalletInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.WalletInfo} WalletInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.WalletInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.currencyNumber = reader.int32();
                        break;
                    }
                case 2: {
                        message.currencyName = reader.string();
                        break;
                    }
                case 3: {
                        message.currencySymbol = reader.string();
                        break;
                    }
                case 4: {
                        message.coin = reader.double();
                        break;
                    }
                case 5: {
                        message.bet = reader.double();
                        break;
                    }
                case 6: {
                        message.unit = reader.double();
                        break;
                    }
                case 7: {
                        message.ratio = reader.double();
                        break;
                    }
                case 8: {
                        message.rate = reader.double();
                        break;
                    }
                case 9: {
                        message.upper = reader.double();
                        break;
                    }
                case 10: {
                        message.lower = reader.double();
                        break;
                    }
                case 11: {
                        message.cycle = reader.int32();
                        break;
                    }
                case 12: {
                        message.decimal = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WalletInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.WalletInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.WalletInfo} WalletInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WalletInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WalletInfo message.
         * @function verify
         * @memberof jlwr.WalletInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WalletInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                if (!$util.isInteger(message.currencyNumber))
                    return "currencyNumber: integer expected";
            if (message.currencyName != null && message.hasOwnProperty("currencyName"))
                if (!$util.isString(message.currencyName))
                    return "currencyName: string expected";
            if (message.currencySymbol != null && message.hasOwnProperty("currencySymbol"))
                if (!$util.isString(message.currencySymbol))
                    return "currencySymbol: string expected";
            if (message.coin != null && message.hasOwnProperty("coin"))
                if (typeof message.coin !== "number")
                    return "coin: number expected";
            if (message.bet != null && message.hasOwnProperty("bet"))
                if (typeof message.bet !== "number")
                    return "bet: number expected";
            if (message.unit != null && message.hasOwnProperty("unit"))
                if (typeof message.unit !== "number")
                    return "unit: number expected";
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                if (typeof message.ratio !== "number")
                    return "ratio: number expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.upper != null && message.hasOwnProperty("upper"))
                if (typeof message.upper !== "number")
                    return "upper: number expected";
            if (message.lower != null && message.hasOwnProperty("lower"))
                if (typeof message.lower !== "number")
                    return "lower: number expected";
            if (message.cycle != null && message.hasOwnProperty("cycle"))
                if (!$util.isInteger(message.cycle))
                    return "cycle: integer expected";
            if (message.decimal != null && message.hasOwnProperty("decimal"))
                if (!$util.isInteger(message.decimal))
                    return "decimal: integer expected";
            return null;
        };

        /**
         * Creates a WalletInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.WalletInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.WalletInfo} WalletInfo
         */
        WalletInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.WalletInfo)
                return object;
            let message = new $root.jlwr.WalletInfo();
            if (object.currencyNumber != null)
                message.currencyNumber = object.currencyNumber | 0;
            if (object.currencyName != null)
                message.currencyName = String(object.currencyName);
            if (object.currencySymbol != null)
                message.currencySymbol = String(object.currencySymbol);
            if (object.coin != null)
                message.coin = Number(object.coin);
            if (object.bet != null)
                message.bet = Number(object.bet);
            if (object.unit != null)
                message.unit = Number(object.unit);
            if (object.ratio != null)
                message.ratio = Number(object.ratio);
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.upper != null)
                message.upper = Number(object.upper);
            if (object.lower != null)
                message.lower = Number(object.lower);
            if (object.cycle != null)
                message.cycle = object.cycle | 0;
            if (object.decimal != null)
                message.decimal = object.decimal | 0;
            return message;
        };

        /**
         * Creates a plain object from a WalletInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.WalletInfo
         * @static
         * @param {jlwr.WalletInfo} message WalletInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WalletInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.currencyNumber = 0;
                object.currencyName = "";
                object.currencySymbol = "";
                object.coin = 0;
                object.bet = 0;
                object.unit = 0;
                object.ratio = 0;
                object.rate = 0;
                object.upper = 0;
                object.lower = 0;
                object.cycle = 0;
                object.decimal = 0;
            }
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                object.currencyNumber = message.currencyNumber;
            if (message.currencyName != null && message.hasOwnProperty("currencyName"))
                object.currencyName = message.currencyName;
            if (message.currencySymbol != null && message.hasOwnProperty("currencySymbol"))
                object.currencySymbol = message.currencySymbol;
            if (message.coin != null && message.hasOwnProperty("coin"))
                object.coin = options.json && !isFinite(message.coin) ? String(message.coin) : message.coin;
            if (message.bet != null && message.hasOwnProperty("bet"))
                object.bet = options.json && !isFinite(message.bet) ? String(message.bet) : message.bet;
            if (message.unit != null && message.hasOwnProperty("unit"))
                object.unit = options.json && !isFinite(message.unit) ? String(message.unit) : message.unit;
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                object.ratio = options.json && !isFinite(message.ratio) ? String(message.ratio) : message.ratio;
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.upper != null && message.hasOwnProperty("upper"))
                object.upper = options.json && !isFinite(message.upper) ? String(message.upper) : message.upper;
            if (message.lower != null && message.hasOwnProperty("lower"))
                object.lower = options.json && !isFinite(message.lower) ? String(message.lower) : message.lower;
            if (message.cycle != null && message.hasOwnProperty("cycle"))
                object.cycle = message.cycle;
            if (message.decimal != null && message.hasOwnProperty("decimal"))
                object.decimal = message.decimal;
            return object;
        };

        /**
         * Converts this WalletInfo to JSON.
         * @function toJSON
         * @memberof jlwr.WalletInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WalletInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WalletInfo
         * @function getTypeUrl
         * @memberof jlwr.WalletInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WalletInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.WalletInfo";
        };

        return WalletInfo;
    })();

    jlwr.PreferRoundShow = (function() {

        /**
         * Properties of a PreferRoundShow.
         * @memberof jlwr
         * @interface IPreferRoundShow
         * @property {number|null} [BaseRound] PreferRoundShow BaseRound
         * @property {number|null} [SigmaRound] PreferRoundShow SigmaRound
         * @property {number|null} [CV] PreferRoundShow CV
         * @property {number|null} [HR] PreferRoundShow HR
         * @property {number|null} [MGR] PreferRoundShow MGR
         * @property {number|null} [Range] PreferRoundShow Range
         * @property {number|null} [Show] PreferRoundShow Show
         */

        /**
         * Constructs a new PreferRoundShow.
         * @memberof jlwr
         * @classdesc Represents a PreferRoundShow.
         * @implements IPreferRoundShow
         * @constructor
         * @param {jlwr.IPreferRoundShow=} [properties] Properties to set
         */
        function PreferRoundShow(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PreferRoundShow BaseRound.
         * @member {number} BaseRound
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.BaseRound = 0;

        /**
         * PreferRoundShow SigmaRound.
         * @member {number} SigmaRound
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.SigmaRound = 0;

        /**
         * PreferRoundShow CV.
         * @member {number} CV
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.CV = 0;

        /**
         * PreferRoundShow HR.
         * @member {number} HR
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.HR = 0;

        /**
         * PreferRoundShow MGR.
         * @member {number} MGR
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.MGR = 0;

        /**
         * PreferRoundShow Range.
         * @member {number} Range
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.Range = 0;

        /**
         * PreferRoundShow Show.
         * @member {number} Show
         * @memberof jlwr.PreferRoundShow
         * @instance
         */
        PreferRoundShow.prototype.Show = 0;

        /**
         * Creates a new PreferRoundShow instance using the specified properties.
         * @function create
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {jlwr.IPreferRoundShow=} [properties] Properties to set
         * @returns {jlwr.PreferRoundShow} PreferRoundShow instance
         */
        PreferRoundShow.create = function create(properties) {
            return new PreferRoundShow(properties);
        };

        /**
         * Encodes the specified PreferRoundShow message. Does not implicitly {@link jlwr.PreferRoundShow.verify|verify} messages.
         * @function encode
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {jlwr.IPreferRoundShow} message PreferRoundShow message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreferRoundShow.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.BaseRound != null && Object.hasOwnProperty.call(message, "BaseRound"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.BaseRound);
            if (message.SigmaRound != null && Object.hasOwnProperty.call(message, "SigmaRound"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.SigmaRound);
            if (message.CV != null && Object.hasOwnProperty.call(message, "CV"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.CV);
            if (message.HR != null && Object.hasOwnProperty.call(message, "HR"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.HR);
            if (message.MGR != null && Object.hasOwnProperty.call(message, "MGR"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.MGR);
            if (message.Range != null && Object.hasOwnProperty.call(message, "Range"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.Range);
            if (message.Show != null && Object.hasOwnProperty.call(message, "Show"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.Show);
            return writer;
        };

        /**
         * Encodes the specified PreferRoundShow message, length delimited. Does not implicitly {@link jlwr.PreferRoundShow.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {jlwr.IPreferRoundShow} message PreferRoundShow message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreferRoundShow.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PreferRoundShow message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.PreferRoundShow} PreferRoundShow
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreferRoundShow.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.PreferRoundShow();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.BaseRound = reader.int32();
                        break;
                    }
                case 2: {
                        message.SigmaRound = reader.int32();
                        break;
                    }
                case 3: {
                        message.CV = reader.double();
                        break;
                    }
                case 4: {
                        message.HR = reader.double();
                        break;
                    }
                case 5: {
                        message.MGR = reader.double();
                        break;
                    }
                case 6: {
                        message.Range = reader.int32();
                        break;
                    }
                case 7: {
                        message.Show = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PreferRoundShow message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.PreferRoundShow} PreferRoundShow
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreferRoundShow.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PreferRoundShow message.
         * @function verify
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PreferRoundShow.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.BaseRound != null && message.hasOwnProperty("BaseRound"))
                if (!$util.isInteger(message.BaseRound))
                    return "BaseRound: integer expected";
            if (message.SigmaRound != null && message.hasOwnProperty("SigmaRound"))
                if (!$util.isInteger(message.SigmaRound))
                    return "SigmaRound: integer expected";
            if (message.CV != null && message.hasOwnProperty("CV"))
                if (typeof message.CV !== "number")
                    return "CV: number expected";
            if (message.HR != null && message.hasOwnProperty("HR"))
                if (typeof message.HR !== "number")
                    return "HR: number expected";
            if (message.MGR != null && message.hasOwnProperty("MGR"))
                if (typeof message.MGR !== "number")
                    return "MGR: number expected";
            if (message.Range != null && message.hasOwnProperty("Range"))
                if (!$util.isInteger(message.Range))
                    return "Range: integer expected";
            if (message.Show != null && message.hasOwnProperty("Show"))
                if (!$util.isInteger(message.Show))
                    return "Show: integer expected";
            return null;
        };

        /**
         * Creates a PreferRoundShow message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.PreferRoundShow} PreferRoundShow
         */
        PreferRoundShow.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.PreferRoundShow)
                return object;
            let message = new $root.jlwr.PreferRoundShow();
            if (object.BaseRound != null)
                message.BaseRound = object.BaseRound | 0;
            if (object.SigmaRound != null)
                message.SigmaRound = object.SigmaRound | 0;
            if (object.CV != null)
                message.CV = Number(object.CV);
            if (object.HR != null)
                message.HR = Number(object.HR);
            if (object.MGR != null)
                message.MGR = Number(object.MGR);
            if (object.Range != null)
                message.Range = object.Range | 0;
            if (object.Show != null)
                message.Show = object.Show | 0;
            return message;
        };

        /**
         * Creates a plain object from a PreferRoundShow message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {jlwr.PreferRoundShow} message PreferRoundShow
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreferRoundShow.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.BaseRound = 0;
                object.SigmaRound = 0;
                object.CV = 0;
                object.HR = 0;
                object.MGR = 0;
                object.Range = 0;
                object.Show = 0;
            }
            if (message.BaseRound != null && message.hasOwnProperty("BaseRound"))
                object.BaseRound = message.BaseRound;
            if (message.SigmaRound != null && message.hasOwnProperty("SigmaRound"))
                object.SigmaRound = message.SigmaRound;
            if (message.CV != null && message.hasOwnProperty("CV"))
                object.CV = options.json && !isFinite(message.CV) ? String(message.CV) : message.CV;
            if (message.HR != null && message.hasOwnProperty("HR"))
                object.HR = options.json && !isFinite(message.HR) ? String(message.HR) : message.HR;
            if (message.MGR != null && message.hasOwnProperty("MGR"))
                object.MGR = options.json && !isFinite(message.MGR) ? String(message.MGR) : message.MGR;
            if (message.Range != null && message.hasOwnProperty("Range"))
                object.Range = message.Range;
            if (message.Show != null && message.hasOwnProperty("Show"))
                object.Show = message.Show;
            return object;
        };

        /**
         * Converts this PreferRoundShow to JSON.
         * @function toJSON
         * @memberof jlwr.PreferRoundShow
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreferRoundShow.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PreferRoundShow
         * @function getTypeUrl
         * @memberof jlwr.PreferRoundShow
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PreferRoundShow.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.PreferRoundShow";
        };

        return PreferRoundShow;
    })();

    jlwr.CheckMallAck = (function() {

        /**
         * Properties of a CheckMallAck.
         * @memberof jlwr
         * @interface ICheckMallAck
         * @property {Array.<jlwr.IGameMallSetting>|null} [Settings] CheckMallAck Settings
         * @property {number|null} [Show] CheckMallAck Show
         */

        /**
         * Constructs a new CheckMallAck.
         * @memberof jlwr
         * @classdesc Represents a CheckMallAck.
         * @implements ICheckMallAck
         * @constructor
         * @param {jlwr.ICheckMallAck=} [properties] Properties to set
         */
        function CheckMallAck(properties) {
            this.Settings = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CheckMallAck Settings.
         * @member {Array.<jlwr.IGameMallSetting>} Settings
         * @memberof jlwr.CheckMallAck
         * @instance
         */
        CheckMallAck.prototype.Settings = $util.emptyArray;

        /**
         * CheckMallAck Show.
         * @member {number} Show
         * @memberof jlwr.CheckMallAck
         * @instance
         */
        CheckMallAck.prototype.Show = 0;

        /**
         * Creates a new CheckMallAck instance using the specified properties.
         * @function create
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {jlwr.ICheckMallAck=} [properties] Properties to set
         * @returns {jlwr.CheckMallAck} CheckMallAck instance
         */
        CheckMallAck.create = function create(properties) {
            return new CheckMallAck(properties);
        };

        /**
         * Encodes the specified CheckMallAck message. Does not implicitly {@link jlwr.CheckMallAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {jlwr.ICheckMallAck} message CheckMallAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Settings != null && message.Settings.length)
                for (let i = 0; i < message.Settings.length; ++i)
                    $root.jlwr.GameMallSetting.encode(message.Settings[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.Show != null && Object.hasOwnProperty.call(message, "Show"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Show);
            return writer;
        };

        /**
         * Encodes the specified CheckMallAck message, length delimited. Does not implicitly {@link jlwr.CheckMallAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {jlwr.ICheckMallAck} message CheckMallAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckMallAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CheckMallAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.CheckMallAck} CheckMallAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.CheckMallAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Settings && message.Settings.length))
                            message.Settings = [];
                        message.Settings.push($root.jlwr.GameMallSetting.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.Show = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CheckMallAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.CheckMallAck} CheckMallAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckMallAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckMallAck message.
         * @function verify
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckMallAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Settings != null && message.hasOwnProperty("Settings")) {
                if (!Array.isArray(message.Settings))
                    return "Settings: array expected";
                for (let i = 0; i < message.Settings.length; ++i) {
                    let error = $root.jlwr.GameMallSetting.verify(message.Settings[i]);
                    if (error)
                        return "Settings." + error;
                }
            }
            if (message.Show != null && message.hasOwnProperty("Show"))
                if (!$util.isInteger(message.Show))
                    return "Show: integer expected";
            return null;
        };

        /**
         * Creates a CheckMallAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.CheckMallAck} CheckMallAck
         */
        CheckMallAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.CheckMallAck)
                return object;
            let message = new $root.jlwr.CheckMallAck();
            if (object.Settings) {
                if (!Array.isArray(object.Settings))
                    throw TypeError(".jlwr.CheckMallAck.Settings: array expected");
                message.Settings = [];
                for (let i = 0; i < object.Settings.length; ++i) {
                    if (typeof object.Settings[i] !== "object")
                        throw TypeError(".jlwr.CheckMallAck.Settings: object expected");
                    message.Settings[i] = $root.jlwr.GameMallSetting.fromObject(object.Settings[i]);
                }
            }
            if (object.Show != null)
                message.Show = object.Show | 0;
            return message;
        };

        /**
         * Creates a plain object from a CheckMallAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {jlwr.CheckMallAck} message CheckMallAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckMallAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Settings = [];
            if (options.defaults)
                object.Show = 0;
            if (message.Settings && message.Settings.length) {
                object.Settings = [];
                for (let j = 0; j < message.Settings.length; ++j)
                    object.Settings[j] = $root.jlwr.GameMallSetting.toObject(message.Settings[j], options);
            }
            if (message.Show != null && message.hasOwnProperty("Show"))
                object.Show = message.Show;
            return object;
        };

        /**
         * Converts this CheckMallAck to JSON.
         * @function toJSON
         * @memberof jlwr.CheckMallAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckMallAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CheckMallAck
         * @function getTypeUrl
         * @memberof jlwr.CheckMallAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CheckMallAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.CheckMallAck";
        };

        return CheckMallAck;
    })();

    jlwr.GameMallSetting = (function() {

        /**
         * Properties of a GameMallSetting.
         * @memberof jlwr
         * @interface IGameMallSetting
         * @property {number|null} [GameID] GameMallSetting GameID
         * @property {number|null} [APIID] GameMallSetting APIID
         * @property {number|null} [AlterID] GameMallSetting AlterID
         * @property {number|null} [DescType] GameMallSetting DescType
         * @property {number|null} [DescRounds] GameMallSetting DescRounds
         * @property {number|null} [MaxBet] GameMallSetting MaxBet
         * @property {number|null} [PriceOdd] GameMallSetting PriceOdd
         * @property {boolean|null} [ForSale] GameMallSetting ForSale
         * @property {Array.<number>|null} [BetVec] GameMallSetting BetVec
         * @property {number|null} [ReelNo] GameMallSetting ReelNo
         * @property {number|null} [BetNo] GameMallSetting BetNo
         * @property {boolean|null} [Event] GameMallSetting Event
         */

        /**
         * Constructs a new GameMallSetting.
         * @memberof jlwr
         * @classdesc Represents a GameMallSetting.
         * @implements IGameMallSetting
         * @constructor
         * @param {jlwr.IGameMallSetting=} [properties] Properties to set
         */
        function GameMallSetting(properties) {
            this.BetVec = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GameMallSetting GameID.
         * @member {number} GameID
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.GameID = 0;

        /**
         * GameMallSetting APIID.
         * @member {number} APIID
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.APIID = 0;

        /**
         * GameMallSetting AlterID.
         * @member {number} AlterID
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.AlterID = 0;

        /**
         * GameMallSetting DescType.
         * @member {number} DescType
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.DescType = 0;

        /**
         * GameMallSetting DescRounds.
         * @member {number} DescRounds
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.DescRounds = 0;

        /**
         * GameMallSetting MaxBet.
         * @member {number} MaxBet
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.MaxBet = 0;

        /**
         * GameMallSetting PriceOdd.
         * @member {number} PriceOdd
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.PriceOdd = 0;

        /**
         * GameMallSetting ForSale.
         * @member {boolean} ForSale
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.ForSale = false;

        /**
         * GameMallSetting BetVec.
         * @member {Array.<number>} BetVec
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.BetVec = $util.emptyArray;

        /**
         * GameMallSetting ReelNo.
         * @member {number} ReelNo
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.ReelNo = 0;

        /**
         * GameMallSetting BetNo.
         * @member {number} BetNo
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.BetNo = 0;

        /**
         * GameMallSetting Event.
         * @member {boolean} Event
         * @memberof jlwr.GameMallSetting
         * @instance
         */
        GameMallSetting.prototype.Event = false;

        /**
         * Creates a new GameMallSetting instance using the specified properties.
         * @function create
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {jlwr.IGameMallSetting=} [properties] Properties to set
         * @returns {jlwr.GameMallSetting} GameMallSetting instance
         */
        GameMallSetting.create = function create(properties) {
            return new GameMallSetting(properties);
        };

        /**
         * Encodes the specified GameMallSetting message. Does not implicitly {@link jlwr.GameMallSetting.verify|verify} messages.
         * @function encode
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {jlwr.IGameMallSetting} message GameMallSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameMallSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.GameID != null && Object.hasOwnProperty.call(message, "GameID"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.GameID);
            if (message.APIID != null && Object.hasOwnProperty.call(message, "APIID"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.APIID);
            if (message.AlterID != null && Object.hasOwnProperty.call(message, "AlterID"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.AlterID);
            if (message.DescType != null && Object.hasOwnProperty.call(message, "DescType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.DescType);
            if (message.DescRounds != null && Object.hasOwnProperty.call(message, "DescRounds"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.DescRounds);
            if (message.MaxBet != null && Object.hasOwnProperty.call(message, "MaxBet"))
                writer.uint32(/* id 6, wireType 1 =*/49).double(message.MaxBet);
            if (message.PriceOdd != null && Object.hasOwnProperty.call(message, "PriceOdd"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.PriceOdd);
            if (message.ForSale != null && Object.hasOwnProperty.call(message, "ForSale"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.ForSale);
            if (message.BetVec != null && message.BetVec.length) {
                writer.uint32(/* id 9, wireType 2 =*/74).fork();
                for (let i = 0; i < message.BetVec.length; ++i)
                    writer.double(message.BetVec[i]);
                writer.ldelim();
            }
            if (message.ReelNo != null && Object.hasOwnProperty.call(message, "ReelNo"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.ReelNo);
            if (message.BetNo != null && Object.hasOwnProperty.call(message, "BetNo"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.BetNo);
            if (message.Event != null && Object.hasOwnProperty.call(message, "Event"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.Event);
            return writer;
        };

        /**
         * Encodes the specified GameMallSetting message, length delimited. Does not implicitly {@link jlwr.GameMallSetting.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {jlwr.IGameMallSetting} message GameMallSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GameMallSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GameMallSetting message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.GameMallSetting} GameMallSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameMallSetting.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.GameMallSetting();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.GameID = reader.int32();
                        break;
                    }
                case 2: {
                        message.APIID = reader.int32();
                        break;
                    }
                case 3: {
                        message.AlterID = reader.int32();
                        break;
                    }
                case 4: {
                        message.DescType = reader.int32();
                        break;
                    }
                case 5: {
                        message.DescRounds = reader.int32();
                        break;
                    }
                case 6: {
                        message.MaxBet = reader.double();
                        break;
                    }
                case 7: {
                        message.PriceOdd = reader.double();
                        break;
                    }
                case 8: {
                        message.ForSale = reader.bool();
                        break;
                    }
                case 9: {
                        if (!(message.BetVec && message.BetVec.length))
                            message.BetVec = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.BetVec.push(reader.double());
                        } else
                            message.BetVec.push(reader.double());
                        break;
                    }
                case 10: {
                        message.ReelNo = reader.int32();
                        break;
                    }
                case 11: {
                        message.BetNo = reader.int32();
                        break;
                    }
                case 12: {
                        message.Event = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GameMallSetting message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.GameMallSetting} GameMallSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GameMallSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GameMallSetting message.
         * @function verify
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GameMallSetting.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.GameID != null && message.hasOwnProperty("GameID"))
                if (!$util.isInteger(message.GameID))
                    return "GameID: integer expected";
            if (message.APIID != null && message.hasOwnProperty("APIID"))
                if (!$util.isInteger(message.APIID))
                    return "APIID: integer expected";
            if (message.AlterID != null && message.hasOwnProperty("AlterID"))
                if (!$util.isInteger(message.AlterID))
                    return "AlterID: integer expected";
            if (message.DescType != null && message.hasOwnProperty("DescType"))
                if (!$util.isInteger(message.DescType))
                    return "DescType: integer expected";
            if (message.DescRounds != null && message.hasOwnProperty("DescRounds"))
                if (!$util.isInteger(message.DescRounds))
                    return "DescRounds: integer expected";
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                if (typeof message.MaxBet !== "number")
                    return "MaxBet: number expected";
            if (message.PriceOdd != null && message.hasOwnProperty("PriceOdd"))
                if (typeof message.PriceOdd !== "number")
                    return "PriceOdd: number expected";
            if (message.ForSale != null && message.hasOwnProperty("ForSale"))
                if (typeof message.ForSale !== "boolean")
                    return "ForSale: boolean expected";
            if (message.BetVec != null && message.hasOwnProperty("BetVec")) {
                if (!Array.isArray(message.BetVec))
                    return "BetVec: array expected";
                for (let i = 0; i < message.BetVec.length; ++i)
                    if (typeof message.BetVec[i] !== "number")
                        return "BetVec: number[] expected";
            }
            if (message.ReelNo != null && message.hasOwnProperty("ReelNo"))
                if (!$util.isInteger(message.ReelNo))
                    return "ReelNo: integer expected";
            if (message.BetNo != null && message.hasOwnProperty("BetNo"))
                if (!$util.isInteger(message.BetNo))
                    return "BetNo: integer expected";
            if (message.Event != null && message.hasOwnProperty("Event"))
                if (typeof message.Event !== "boolean")
                    return "Event: boolean expected";
            return null;
        };

        /**
         * Creates a GameMallSetting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.GameMallSetting} GameMallSetting
         */
        GameMallSetting.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.GameMallSetting)
                return object;
            let message = new $root.jlwr.GameMallSetting();
            if (object.GameID != null)
                message.GameID = object.GameID | 0;
            if (object.APIID != null)
                message.APIID = object.APIID | 0;
            if (object.AlterID != null)
                message.AlterID = object.AlterID | 0;
            if (object.DescType != null)
                message.DescType = object.DescType | 0;
            if (object.DescRounds != null)
                message.DescRounds = object.DescRounds | 0;
            if (object.MaxBet != null)
                message.MaxBet = Number(object.MaxBet);
            if (object.PriceOdd != null)
                message.PriceOdd = Number(object.PriceOdd);
            if (object.ForSale != null)
                message.ForSale = Boolean(object.ForSale);
            if (object.BetVec) {
                if (!Array.isArray(object.BetVec))
                    throw TypeError(".jlwr.GameMallSetting.BetVec: array expected");
                message.BetVec = [];
                for (let i = 0; i < object.BetVec.length; ++i)
                    message.BetVec[i] = Number(object.BetVec[i]);
            }
            if (object.ReelNo != null)
                message.ReelNo = object.ReelNo | 0;
            if (object.BetNo != null)
                message.BetNo = object.BetNo | 0;
            if (object.Event != null)
                message.Event = Boolean(object.Event);
            return message;
        };

        /**
         * Creates a plain object from a GameMallSetting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {jlwr.GameMallSetting} message GameMallSetting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GameMallSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.BetVec = [];
            if (options.defaults) {
                object.GameID = 0;
                object.APIID = 0;
                object.AlterID = 0;
                object.DescType = 0;
                object.DescRounds = 0;
                object.MaxBet = 0;
                object.PriceOdd = 0;
                object.ForSale = false;
                object.ReelNo = 0;
                object.BetNo = 0;
                object.Event = false;
            }
            if (message.GameID != null && message.hasOwnProperty("GameID"))
                object.GameID = message.GameID;
            if (message.APIID != null && message.hasOwnProperty("APIID"))
                object.APIID = message.APIID;
            if (message.AlterID != null && message.hasOwnProperty("AlterID"))
                object.AlterID = message.AlterID;
            if (message.DescType != null && message.hasOwnProperty("DescType"))
                object.DescType = message.DescType;
            if (message.DescRounds != null && message.hasOwnProperty("DescRounds"))
                object.DescRounds = message.DescRounds;
            if (message.MaxBet != null && message.hasOwnProperty("MaxBet"))
                object.MaxBet = options.json && !isFinite(message.MaxBet) ? String(message.MaxBet) : message.MaxBet;
            if (message.PriceOdd != null && message.hasOwnProperty("PriceOdd"))
                object.PriceOdd = options.json && !isFinite(message.PriceOdd) ? String(message.PriceOdd) : message.PriceOdd;
            if (message.ForSale != null && message.hasOwnProperty("ForSale"))
                object.ForSale = message.ForSale;
            if (message.BetVec && message.BetVec.length) {
                object.BetVec = [];
                for (let j = 0; j < message.BetVec.length; ++j)
                    object.BetVec[j] = options.json && !isFinite(message.BetVec[j]) ? String(message.BetVec[j]) : message.BetVec[j];
            }
            if (message.ReelNo != null && message.hasOwnProperty("ReelNo"))
                object.ReelNo = message.ReelNo;
            if (message.BetNo != null && message.hasOwnProperty("BetNo"))
                object.BetNo = message.BetNo;
            if (message.Event != null && message.hasOwnProperty("Event"))
                object.Event = message.Event;
            return object;
        };

        /**
         * Converts this GameMallSetting to JSON.
         * @function toJSON
         * @memberof jlwr.GameMallSetting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GameMallSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GameMallSetting
         * @function getTypeUrl
         * @memberof jlwr.GameMallSetting
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GameMallSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.GameMallSetting";
        };

        return GameMallSetting;
    })();

    jlwr.LoginDataAck = (function() {

        /**
         * Properties of a LoginDataAck.
         * @memberof jlwr
         * @interface ILoginDataAck
         * @property {number|null} [result] LoginDataAck result
         * @property {number|null} [aid] LoginDataAck aid
         */

        /**
         * Constructs a new LoginDataAck.
         * @memberof jlwr
         * @classdesc Represents a LoginDataAck.
         * @implements ILoginDataAck
         * @constructor
         * @param {jlwr.ILoginDataAck=} [properties] Properties to set
         */
        function LoginDataAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginDataAck result.
         * @member {number} result
         * @memberof jlwr.LoginDataAck
         * @instance
         */
        LoginDataAck.prototype.result = 0;

        /**
         * LoginDataAck aid.
         * @member {number} aid
         * @memberof jlwr.LoginDataAck
         * @instance
         */
        LoginDataAck.prototype.aid = 0;

        /**
         * Creates a new LoginDataAck instance using the specified properties.
         * @function create
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {jlwr.ILoginDataAck=} [properties] Properties to set
         * @returns {jlwr.LoginDataAck} LoginDataAck instance
         */
        LoginDataAck.create = function create(properties) {
            return new LoginDataAck(properties);
        };

        /**
         * Encodes the specified LoginDataAck message. Does not implicitly {@link jlwr.LoginDataAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {jlwr.ILoginDataAck} message LoginDataAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.result);
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.aid);
            return writer;
        };

        /**
         * Encodes the specified LoginDataAck message, length delimited. Does not implicitly {@link jlwr.LoginDataAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {jlwr.ILoginDataAck} message LoginDataAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LoginDataAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.LoginDataAck} LoginDataAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.LoginDataAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.result = reader.int32();
                        break;
                    }
                case 2: {
                        message.aid = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoginDataAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.LoginDataAck} LoginDataAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoginDataAck message.
         * @function verify
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoginDataAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            return null;
        };

        /**
         * Creates a LoginDataAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.LoginDataAck} LoginDataAck
         */
        LoginDataAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.LoginDataAck)
                return object;
            let message = new $root.jlwr.LoginDataAck();
            if (object.result != null)
                message.result = object.result | 0;
            if (object.aid != null)
                message.aid = object.aid | 0;
            return message;
        };

        /**
         * Creates a plain object from a LoginDataAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {jlwr.LoginDataAck} message LoginDataAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginDataAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.result = 0;
                object.aid = 0;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            return object;
        };

        /**
         * Converts this LoginDataAck to JSON.
         * @function toJSON
         * @memberof jlwr.LoginDataAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginDataAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginDataAck
         * @function getTypeUrl
         * @memberof jlwr.LoginDataAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginDataAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.LoginDataAck";
        };

        return LoginDataAck;
    })();

    jlwr.LoginDataReq = (function() {

        /**
         * Properties of a LoginDataReq.
         * @memberof jlwr
         * @interface ILoginDataReq
         * @property {string|null} [result] LoginDataReq result
         * @property {number|null} [Source] LoginDataReq Source
         * @property {string|null} [OSType] LoginDataReq OSType
         * @property {string|null} [OSVersion] LoginDataReq OSVersion
         * @property {string|null} [Browser] LoginDataReq Browser
         * @property {string|null} [BrowserVersion] LoginDataReq BrowserVersion
         * @property {number|null} [BrowserTag] LoginDataReq BrowserTag
         * @property {string|null} [Machine] LoginDataReq Machine
         * @property {number|null} [Width] LoginDataReq Width
         * @property {number|null} [Height] LoginDataReq Height
         * @property {number|null} [Ratio] LoginDataReq Ratio
         * @property {string|null} [Language] LoginDataReq Language
         * @property {number|null} [Cheat] LoginDataReq Cheat
         */

        /**
         * Constructs a new LoginDataReq.
         * @memberof jlwr
         * @classdesc Represents a LoginDataReq.
         * @implements ILoginDataReq
         * @constructor
         * @param {jlwr.ILoginDataReq=} [properties] Properties to set
         */
        function LoginDataReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginDataReq result.
         * @member {string} result
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.result = "";

        /**
         * LoginDataReq Source.
         * @member {number} Source
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Source = 0;

        /**
         * LoginDataReq OSType.
         * @member {string} OSType
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.OSType = "";

        /**
         * LoginDataReq OSVersion.
         * @member {string} OSVersion
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.OSVersion = "";

        /**
         * LoginDataReq Browser.
         * @member {string} Browser
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Browser = "";

        /**
         * LoginDataReq BrowserVersion.
         * @member {string} BrowserVersion
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.BrowserVersion = "";

        /**
         * LoginDataReq BrowserTag.
         * @member {number} BrowserTag
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.BrowserTag = 0;

        /**
         * LoginDataReq Machine.
         * @member {string} Machine
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Machine = "";

        /**
         * LoginDataReq Width.
         * @member {number} Width
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Width = 0;

        /**
         * LoginDataReq Height.
         * @member {number} Height
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Height = 0;

        /**
         * LoginDataReq Ratio.
         * @member {number} Ratio
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Ratio = 0;

        /**
         * LoginDataReq Language.
         * @member {string} Language
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Language = "";

        /**
         * LoginDataReq Cheat.
         * @member {number} Cheat
         * @memberof jlwr.LoginDataReq
         * @instance
         */
        LoginDataReq.prototype.Cheat = 0;

        /**
         * Creates a new LoginDataReq instance using the specified properties.
         * @function create
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {jlwr.ILoginDataReq=} [properties] Properties to set
         * @returns {jlwr.LoginDataReq} LoginDataReq instance
         */
        LoginDataReq.create = function create(properties) {
            return new LoginDataReq(properties);
        };

        /**
         * Encodes the specified LoginDataReq message. Does not implicitly {@link jlwr.LoginDataReq.verify|verify} messages.
         * @function encode
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {jlwr.ILoginDataReq} message LoginDataReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.result);
            if (message.Source != null && Object.hasOwnProperty.call(message, "Source"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Source);
            if (message.OSType != null && Object.hasOwnProperty.call(message, "OSType"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.OSType);
            if (message.OSVersion != null && Object.hasOwnProperty.call(message, "OSVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.OSVersion);
            if (message.Browser != null && Object.hasOwnProperty.call(message, "Browser"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.Browser);
            if (message.BrowserVersion != null && Object.hasOwnProperty.call(message, "BrowserVersion"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.BrowserVersion);
            if (message.BrowserTag != null && Object.hasOwnProperty.call(message, "BrowserTag"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.BrowserTag);
            if (message.Machine != null && Object.hasOwnProperty.call(message, "Machine"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.Machine);
            if (message.Width != null && Object.hasOwnProperty.call(message, "Width"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.Width);
            if (message.Height != null && Object.hasOwnProperty.call(message, "Height"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.Height);
            if (message.Ratio != null && Object.hasOwnProperty.call(message, "Ratio"))
                writer.uint32(/* id 11, wireType 1 =*/89).double(message.Ratio);
            if (message.Language != null && Object.hasOwnProperty.call(message, "Language"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.Language);
            if (message.Cheat != null && Object.hasOwnProperty.call(message, "Cheat"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.Cheat);
            return writer;
        };

        /**
         * Encodes the specified LoginDataReq message, length delimited. Does not implicitly {@link jlwr.LoginDataReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {jlwr.ILoginDataReq} message LoginDataReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginDataReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LoginDataReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.LoginDataReq} LoginDataReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.LoginDataReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.result = reader.string();
                        break;
                    }
                case 2: {
                        message.Source = reader.int32();
                        break;
                    }
                case 3: {
                        message.OSType = reader.string();
                        break;
                    }
                case 4: {
                        message.OSVersion = reader.string();
                        break;
                    }
                case 5: {
                        message.Browser = reader.string();
                        break;
                    }
                case 6: {
                        message.BrowserVersion = reader.string();
                        break;
                    }
                case 7: {
                        message.BrowserTag = reader.int32();
                        break;
                    }
                case 8: {
                        message.Machine = reader.string();
                        break;
                    }
                case 9: {
                        message.Width = reader.int32();
                        break;
                    }
                case 10: {
                        message.Height = reader.int32();
                        break;
                    }
                case 11: {
                        message.Ratio = reader.double();
                        break;
                    }
                case 12: {
                        message.Language = reader.string();
                        break;
                    }
                case 13: {
                        message.Cheat = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoginDataReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.LoginDataReq} LoginDataReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginDataReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoginDataReq message.
         * @function verify
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoginDataReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isString(message.result))
                    return "result: string expected";
            if (message.Source != null && message.hasOwnProperty("Source"))
                if (!$util.isInteger(message.Source))
                    return "Source: integer expected";
            if (message.OSType != null && message.hasOwnProperty("OSType"))
                if (!$util.isString(message.OSType))
                    return "OSType: string expected";
            if (message.OSVersion != null && message.hasOwnProperty("OSVersion"))
                if (!$util.isString(message.OSVersion))
                    return "OSVersion: string expected";
            if (message.Browser != null && message.hasOwnProperty("Browser"))
                if (!$util.isString(message.Browser))
                    return "Browser: string expected";
            if (message.BrowserVersion != null && message.hasOwnProperty("BrowserVersion"))
                if (!$util.isString(message.BrowserVersion))
                    return "BrowserVersion: string expected";
            if (message.BrowserTag != null && message.hasOwnProperty("BrowserTag"))
                if (!$util.isInteger(message.BrowserTag))
                    return "BrowserTag: integer expected";
            if (message.Machine != null && message.hasOwnProperty("Machine"))
                if (!$util.isString(message.Machine))
                    return "Machine: string expected";
            if (message.Width != null && message.hasOwnProperty("Width"))
                if (!$util.isInteger(message.Width))
                    return "Width: integer expected";
            if (message.Height != null && message.hasOwnProperty("Height"))
                if (!$util.isInteger(message.Height))
                    return "Height: integer expected";
            if (message.Ratio != null && message.hasOwnProperty("Ratio"))
                if (typeof message.Ratio !== "number")
                    return "Ratio: number expected";
            if (message.Language != null && message.hasOwnProperty("Language"))
                if (!$util.isString(message.Language))
                    return "Language: string expected";
            if (message.Cheat != null && message.hasOwnProperty("Cheat"))
                if (!$util.isInteger(message.Cheat))
                    return "Cheat: integer expected";
            return null;
        };

        /**
         * Creates a LoginDataReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.LoginDataReq} LoginDataReq
         */
        LoginDataReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.LoginDataReq)
                return object;
            let message = new $root.jlwr.LoginDataReq();
            if (object.result != null)
                message.result = String(object.result);
            if (object.Source != null)
                message.Source = object.Source | 0;
            if (object.OSType != null)
                message.OSType = String(object.OSType);
            if (object.OSVersion != null)
                message.OSVersion = String(object.OSVersion);
            if (object.Browser != null)
                message.Browser = String(object.Browser);
            if (object.BrowserVersion != null)
                message.BrowserVersion = String(object.BrowserVersion);
            if (object.BrowserTag != null)
                message.BrowserTag = object.BrowserTag | 0;
            if (object.Machine != null)
                message.Machine = String(object.Machine);
            if (object.Width != null)
                message.Width = object.Width | 0;
            if (object.Height != null)
                message.Height = object.Height | 0;
            if (object.Ratio != null)
                message.Ratio = Number(object.Ratio);
            if (object.Language != null)
                message.Language = String(object.Language);
            if (object.Cheat != null)
                message.Cheat = object.Cheat | 0;
            return message;
        };

        /**
         * Creates a plain object from a LoginDataReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {jlwr.LoginDataReq} message LoginDataReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginDataReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.result = "";
                object.Source = 0;
                object.OSType = "";
                object.OSVersion = "";
                object.Browser = "";
                object.BrowserVersion = "";
                object.BrowserTag = 0;
                object.Machine = "";
                object.Width = 0;
                object.Height = 0;
                object.Ratio = 0;
                object.Language = "";
                object.Cheat = 0;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.Source != null && message.hasOwnProperty("Source"))
                object.Source = message.Source;
            if (message.OSType != null && message.hasOwnProperty("OSType"))
                object.OSType = message.OSType;
            if (message.OSVersion != null && message.hasOwnProperty("OSVersion"))
                object.OSVersion = message.OSVersion;
            if (message.Browser != null && message.hasOwnProperty("Browser"))
                object.Browser = message.Browser;
            if (message.BrowserVersion != null && message.hasOwnProperty("BrowserVersion"))
                object.BrowserVersion = message.BrowserVersion;
            if (message.BrowserTag != null && message.hasOwnProperty("BrowserTag"))
                object.BrowserTag = message.BrowserTag;
            if (message.Machine != null && message.hasOwnProperty("Machine"))
                object.Machine = message.Machine;
            if (message.Width != null && message.hasOwnProperty("Width"))
                object.Width = message.Width;
            if (message.Height != null && message.hasOwnProperty("Height"))
                object.Height = message.Height;
            if (message.Ratio != null && message.hasOwnProperty("Ratio"))
                object.Ratio = options.json && !isFinite(message.Ratio) ? String(message.Ratio) : message.Ratio;
            if (message.Language != null && message.hasOwnProperty("Language"))
                object.Language = message.Language;
            if (message.Cheat != null && message.hasOwnProperty("Cheat"))
                object.Cheat = message.Cheat;
            return object;
        };

        /**
         * Converts this LoginDataReq to JSON.
         * @function toJSON
         * @memberof jlwr.LoginDataReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginDataReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginDataReq
         * @function getTypeUrl
         * @memberof jlwr.LoginDataReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginDataReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.LoginDataReq";
        };

        return LoginDataReq;
    })();

    jlwr.LogoutReq = (function() {

        /**
         * Properties of a LogoutReq.
         * @memberof jlwr
         * @interface ILogoutReq
         * @property {number|null} [aid] LogoutReq aid
         */

        /**
         * Constructs a new LogoutReq.
         * @memberof jlwr
         * @classdesc Represents a LogoutReq.
         * @implements ILogoutReq
         * @constructor
         * @param {jlwr.ILogoutReq=} [properties] Properties to set
         */
        function LogoutReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LogoutReq aid.
         * @member {number} aid
         * @memberof jlwr.LogoutReq
         * @instance
         */
        LogoutReq.prototype.aid = 0;

        /**
         * Creates a new LogoutReq instance using the specified properties.
         * @function create
         * @memberof jlwr.LogoutReq
         * @static
         * @param {jlwr.ILogoutReq=} [properties] Properties to set
         * @returns {jlwr.LogoutReq} LogoutReq instance
         */
        LogoutReq.create = function create(properties) {
            return new LogoutReq(properties);
        };

        /**
         * Encodes the specified LogoutReq message. Does not implicitly {@link jlwr.LogoutReq.verify|verify} messages.
         * @function encode
         * @memberof jlwr.LogoutReq
         * @static
         * @param {jlwr.ILogoutReq} message LogoutReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogoutReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            return writer;
        };

        /**
         * Encodes the specified LogoutReq message, length delimited. Does not implicitly {@link jlwr.LogoutReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.LogoutReq
         * @static
         * @param {jlwr.ILogoutReq} message LogoutReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogoutReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LogoutReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.LogoutReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.LogoutReq} LogoutReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogoutReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.LogoutReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LogoutReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.LogoutReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.LogoutReq} LogoutReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogoutReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LogoutReq message.
         * @function verify
         * @memberof jlwr.LogoutReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LogoutReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            return null;
        };

        /**
         * Creates a LogoutReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.LogoutReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.LogoutReq} LogoutReq
         */
        LogoutReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.LogoutReq)
                return object;
            let message = new $root.jlwr.LogoutReq();
            if (object.aid != null)
                message.aid = object.aid | 0;
            return message;
        };

        /**
         * Creates a plain object from a LogoutReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.LogoutReq
         * @static
         * @param {jlwr.LogoutReq} message LogoutReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LogoutReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.aid = 0;
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            return object;
        };

        /**
         * Converts this LogoutReq to JSON.
         * @function toJSON
         * @memberof jlwr.LogoutReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LogoutReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LogoutReq
         * @function getTypeUrl
         * @memberof jlwr.LogoutReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LogoutReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.LogoutReq";
        };

        return LogoutReq;
    })();

    jlwr.HeartAck = (function() {

        /**
         * Properties of a HeartAck.
         * @memberof jlwr
         * @interface IHeartAck
         * @property {number|null} [state] HeartAck state
         * @property {string|null} [message] HeartAck message
         */

        /**
         * Constructs a new HeartAck.
         * @memberof jlwr
         * @classdesc Represents a HeartAck.
         * @implements IHeartAck
         * @constructor
         * @param {jlwr.IHeartAck=} [properties] Properties to set
         */
        function HeartAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HeartAck state.
         * @member {number} state
         * @memberof jlwr.HeartAck
         * @instance
         */
        HeartAck.prototype.state = 0;

        /**
         * HeartAck message.
         * @member {string} message
         * @memberof jlwr.HeartAck
         * @instance
         */
        HeartAck.prototype.message = "";

        /**
         * Creates a new HeartAck instance using the specified properties.
         * @function create
         * @memberof jlwr.HeartAck
         * @static
         * @param {jlwr.IHeartAck=} [properties] Properties to set
         * @returns {jlwr.HeartAck} HeartAck instance
         */
        HeartAck.create = function create(properties) {
            return new HeartAck(properties);
        };

        /**
         * Encodes the specified HeartAck message. Does not implicitly {@link jlwr.HeartAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.HeartAck
         * @static
         * @param {jlwr.IHeartAck} message HeartAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeartAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.state);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            return writer;
        };

        /**
         * Encodes the specified HeartAck message, length delimited. Does not implicitly {@link jlwr.HeartAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.HeartAck
         * @static
         * @param {jlwr.IHeartAck} message HeartAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeartAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HeartAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.HeartAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.HeartAck} HeartAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeartAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.HeartAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.state = reader.int32();
                        break;
                    }
                case 2: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HeartAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.HeartAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.HeartAck} HeartAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeartAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HeartAck message.
         * @function verify
         * @memberof jlwr.HeartAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HeartAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a HeartAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.HeartAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.HeartAck} HeartAck
         */
        HeartAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.HeartAck)
                return object;
            let message = new $root.jlwr.HeartAck();
            if (object.state != null)
                message.state = object.state | 0;
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a HeartAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.HeartAck
         * @static
         * @param {jlwr.HeartAck} message HeartAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HeartAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.state = 0;
                object.message = "";
            }
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this HeartAck to JSON.
         * @function toJSON
         * @memberof jlwr.HeartAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HeartAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HeartAck
         * @function getTypeUrl
         * @memberof jlwr.HeartAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HeartAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.HeartAck";
        };

        return HeartAck;
    })();

    jlwr.ExchangeReq = (function() {

        /**
         * Properties of an ExchangeReq.
         * @memberof jlwr
         * @interface IExchangeReq
         * @property {number|null} [aid] ExchangeReq aid
         * @property {string|null} [token] ExchangeReq token
         * @property {string|null} [stamp] ExchangeReq stamp
         */

        /**
         * Constructs a new ExchangeReq.
         * @memberof jlwr
         * @classdesc Represents an ExchangeReq.
         * @implements IExchangeReq
         * @constructor
         * @param {jlwr.IExchangeReq=} [properties] Properties to set
         */
        function ExchangeReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExchangeReq aid.
         * @member {number} aid
         * @memberof jlwr.ExchangeReq
         * @instance
         */
        ExchangeReq.prototype.aid = 0;

        /**
         * ExchangeReq token.
         * @member {string} token
         * @memberof jlwr.ExchangeReq
         * @instance
         */
        ExchangeReq.prototype.token = "";

        /**
         * ExchangeReq stamp.
         * @member {string} stamp
         * @memberof jlwr.ExchangeReq
         * @instance
         */
        ExchangeReq.prototype.stamp = "";

        /**
         * Creates a new ExchangeReq instance using the specified properties.
         * @function create
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {jlwr.IExchangeReq=} [properties] Properties to set
         * @returns {jlwr.ExchangeReq} ExchangeReq instance
         */
        ExchangeReq.create = function create(properties) {
            return new ExchangeReq(properties);
        };

        /**
         * Encodes the specified ExchangeReq message. Does not implicitly {@link jlwr.ExchangeReq.verify|verify} messages.
         * @function encode
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {jlwr.IExchangeReq} message ExchangeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
            if (message.stamp != null && Object.hasOwnProperty.call(message, "stamp"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.stamp);
            return writer;
        };

        /**
         * Encodes the specified ExchangeReq message, length delimited. Does not implicitly {@link jlwr.ExchangeReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {jlwr.IExchangeReq} message ExchangeReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ExchangeReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.ExchangeReq} ExchangeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.ExchangeReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                case 2: {
                        message.token = reader.string();
                        break;
                    }
                case 3: {
                        message.stamp = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExchangeReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.ExchangeReq} ExchangeReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExchangeReq message.
         * @function verify
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExchangeReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                if (!$util.isString(message.stamp))
                    return "stamp: string expected";
            return null;
        };

        /**
         * Creates an ExchangeReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.ExchangeReq} ExchangeReq
         */
        ExchangeReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.ExchangeReq)
                return object;
            let message = new $root.jlwr.ExchangeReq();
            if (object.aid != null)
                message.aid = object.aid | 0;
            if (object.token != null)
                message.token = String(object.token);
            if (object.stamp != null)
                message.stamp = String(object.stamp);
            return message;
        };

        /**
         * Creates a plain object from an ExchangeReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {jlwr.ExchangeReq} message ExchangeReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExchangeReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.aid = 0;
                object.token = "";
                object.stamp = "";
            }
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.stamp != null && message.hasOwnProperty("stamp"))
                object.stamp = message.stamp;
            return object;
        };

        /**
         * Converts this ExchangeReq to JSON.
         * @function toJSON
         * @memberof jlwr.ExchangeReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExchangeReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExchangeReq
         * @function getTypeUrl
         * @memberof jlwr.ExchangeReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExchangeReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.ExchangeReq";
        };

        return ExchangeReq;
    })();

    jlwr.ExchangeAck = (function() {

        /**
         * Properties of an ExchangeAck.
         * @memberof jlwr
         * @interface IExchangeAck
         * @property {Array.<jlwr.IWallet>|null} [wallet] ExchangeAck wallet
         */

        /**
         * Constructs a new ExchangeAck.
         * @memberof jlwr
         * @classdesc Represents an ExchangeAck.
         * @implements IExchangeAck
         * @constructor
         * @param {jlwr.IExchangeAck=} [properties] Properties to set
         */
        function ExchangeAck(properties) {
            this.wallet = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExchangeAck wallet.
         * @member {Array.<jlwr.IWallet>} wallet
         * @memberof jlwr.ExchangeAck
         * @instance
         */
        ExchangeAck.prototype.wallet = $util.emptyArray;

        /**
         * Creates a new ExchangeAck instance using the specified properties.
         * @function create
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {jlwr.IExchangeAck=} [properties] Properties to set
         * @returns {jlwr.ExchangeAck} ExchangeAck instance
         */
        ExchangeAck.create = function create(properties) {
            return new ExchangeAck(properties);
        };

        /**
         * Encodes the specified ExchangeAck message. Does not implicitly {@link jlwr.ExchangeAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {jlwr.IExchangeAck} message ExchangeAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.wallet != null && message.wallet.length)
                for (let i = 0; i < message.wallet.length; ++i)
                    $root.jlwr.Wallet.encode(message.wallet[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ExchangeAck message, length delimited. Does not implicitly {@link jlwr.ExchangeAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {jlwr.IExchangeAck} message ExchangeAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExchangeAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ExchangeAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.ExchangeAck} ExchangeAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.ExchangeAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.wallet && message.wallet.length))
                            message.wallet = [];
                        message.wallet.push($root.jlwr.Wallet.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExchangeAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.ExchangeAck} ExchangeAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExchangeAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExchangeAck message.
         * @function verify
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExchangeAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.wallet != null && message.hasOwnProperty("wallet")) {
                if (!Array.isArray(message.wallet))
                    return "wallet: array expected";
                for (let i = 0; i < message.wallet.length; ++i) {
                    let error = $root.jlwr.Wallet.verify(message.wallet[i]);
                    if (error)
                        return "wallet." + error;
                }
            }
            return null;
        };

        /**
         * Creates an ExchangeAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.ExchangeAck} ExchangeAck
         */
        ExchangeAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.ExchangeAck)
                return object;
            let message = new $root.jlwr.ExchangeAck();
            if (object.wallet) {
                if (!Array.isArray(object.wallet))
                    throw TypeError(".jlwr.ExchangeAck.wallet: array expected");
                message.wallet = [];
                for (let i = 0; i < object.wallet.length; ++i) {
                    if (typeof object.wallet[i] !== "object")
                        throw TypeError(".jlwr.ExchangeAck.wallet: object expected");
                    message.wallet[i] = $root.jlwr.Wallet.fromObject(object.wallet[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an ExchangeAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {jlwr.ExchangeAck} message ExchangeAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExchangeAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.wallet = [];
            if (message.wallet && message.wallet.length) {
                object.wallet = [];
                for (let j = 0; j < message.wallet.length; ++j)
                    object.wallet[j] = $root.jlwr.Wallet.toObject(message.wallet[j], options);
            }
            return object;
        };

        /**
         * Converts this ExchangeAck to JSON.
         * @function toJSON
         * @memberof jlwr.ExchangeAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExchangeAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExchangeAck
         * @function getTypeUrl
         * @memberof jlwr.ExchangeAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExchangeAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.ExchangeAck";
        };

        return ExchangeAck;
    })();

    jlwr.Wallet = (function() {

        /**
         * Properties of a Wallet.
         * @memberof jlwr
         * @interface IWallet
         * @property {number|null} [coin] Wallet coin
         * @property {number|null} [currencyNumber] Wallet currencyNumber
         * @property {number|null} [ratio] Wallet ratio
         * @property {number|null} [rate] Wallet rate
         * @property {number|null} [unit] Wallet unit
         */

        /**
         * Constructs a new Wallet.
         * @memberof jlwr
         * @classdesc Represents a Wallet.
         * @implements IWallet
         * @constructor
         * @param {jlwr.IWallet=} [properties] Properties to set
         */
        function Wallet(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Wallet coin.
         * @member {number} coin
         * @memberof jlwr.Wallet
         * @instance
         */
        Wallet.prototype.coin = 0;

        /**
         * Wallet currencyNumber.
         * @member {number} currencyNumber
         * @memberof jlwr.Wallet
         * @instance
         */
        Wallet.prototype.currencyNumber = 0;

        /**
         * Wallet ratio.
         * @member {number} ratio
         * @memberof jlwr.Wallet
         * @instance
         */
        Wallet.prototype.ratio = 0;

        /**
         * Wallet rate.
         * @member {number} rate
         * @memberof jlwr.Wallet
         * @instance
         */
        Wallet.prototype.rate = 0;

        /**
         * Wallet unit.
         * @member {number} unit
         * @memberof jlwr.Wallet
         * @instance
         */
        Wallet.prototype.unit = 0;

        /**
         * Creates a new Wallet instance using the specified properties.
         * @function create
         * @memberof jlwr.Wallet
         * @static
         * @param {jlwr.IWallet=} [properties] Properties to set
         * @returns {jlwr.Wallet} Wallet instance
         */
        Wallet.create = function create(properties) {
            return new Wallet(properties);
        };

        /**
         * Encodes the specified Wallet message. Does not implicitly {@link jlwr.Wallet.verify|verify} messages.
         * @function encode
         * @memberof jlwr.Wallet
         * @static
         * @param {jlwr.IWallet} message Wallet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wallet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.coin);
            if (message.currencyNumber != null && Object.hasOwnProperty.call(message, "currencyNumber"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.currencyNumber);
            if (message.ratio != null && Object.hasOwnProperty.call(message, "ratio"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.ratio);
            if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.rate);
            if (message.unit != null && Object.hasOwnProperty.call(message, "unit"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.unit);
            return writer;
        };

        /**
         * Encodes the specified Wallet message, length delimited. Does not implicitly {@link jlwr.Wallet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.Wallet
         * @static
         * @param {jlwr.IWallet} message Wallet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Wallet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Wallet message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.Wallet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.Wallet} Wallet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wallet.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.Wallet();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.coin = reader.double();
                        break;
                    }
                case 2: {
                        message.currencyNumber = reader.int32();
                        break;
                    }
                case 3: {
                        message.ratio = reader.double();
                        break;
                    }
                case 4: {
                        message.rate = reader.double();
                        break;
                    }
                case 5: {
                        message.unit = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Wallet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.Wallet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.Wallet} Wallet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Wallet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Wallet message.
         * @function verify
         * @memberof jlwr.Wallet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Wallet.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.coin != null && message.hasOwnProperty("coin"))
                if (typeof message.coin !== "number")
                    return "coin: number expected";
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                if (!$util.isInteger(message.currencyNumber))
                    return "currencyNumber: integer expected";
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                if (typeof message.ratio !== "number")
                    return "ratio: number expected";
            if (message.rate != null && message.hasOwnProperty("rate"))
                if (typeof message.rate !== "number")
                    return "rate: number expected";
            if (message.unit != null && message.hasOwnProperty("unit"))
                if (typeof message.unit !== "number")
                    return "unit: number expected";
            return null;
        };

        /**
         * Creates a Wallet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.Wallet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.Wallet} Wallet
         */
        Wallet.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.Wallet)
                return object;
            let message = new $root.jlwr.Wallet();
            if (object.coin != null)
                message.coin = Number(object.coin);
            if (object.currencyNumber != null)
                message.currencyNumber = object.currencyNumber | 0;
            if (object.ratio != null)
                message.ratio = Number(object.ratio);
            if (object.rate != null)
                message.rate = Number(object.rate);
            if (object.unit != null)
                message.unit = Number(object.unit);
            return message;
        };

        /**
         * Creates a plain object from a Wallet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.Wallet
         * @static
         * @param {jlwr.Wallet} message Wallet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Wallet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.coin = 0;
                object.currencyNumber = 0;
                object.ratio = 0;
                object.rate = 0;
                object.unit = 0;
            }
            if (message.coin != null && message.hasOwnProperty("coin"))
                object.coin = options.json && !isFinite(message.coin) ? String(message.coin) : message.coin;
            if (message.currencyNumber != null && message.hasOwnProperty("currencyNumber"))
                object.currencyNumber = message.currencyNumber;
            if (message.ratio != null && message.hasOwnProperty("ratio"))
                object.ratio = options.json && !isFinite(message.ratio) ? String(message.ratio) : message.ratio;
            if (message.rate != null && message.hasOwnProperty("rate"))
                object.rate = options.json && !isFinite(message.rate) ? String(message.rate) : message.rate;
            if (message.unit != null && message.hasOwnProperty("unit"))
                object.unit = options.json && !isFinite(message.unit) ? String(message.unit) : message.unit;
            return object;
        };

        /**
         * Converts this Wallet to JSON.
         * @function toJSON
         * @memberof jlwr.Wallet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Wallet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Wallet
         * @function getTypeUrl
         * @memberof jlwr.Wallet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Wallet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.Wallet";
        };

        return Wallet;
    })();

    jlwr.LastReq = (function() {

        /**
         * Properties of a LastReq.
         * @memberof jlwr
         * @interface ILastReq
         * @property {number|null} [aid] LastReq aid
         * @property {string|null} [token] LastReq token
         */

        /**
         * Constructs a new LastReq.
         * @memberof jlwr
         * @classdesc Represents a LastReq.
         * @implements ILastReq
         * @constructor
         * @param {jlwr.ILastReq=} [properties] Properties to set
         */
        function LastReq(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LastReq aid.
         * @member {number} aid
         * @memberof jlwr.LastReq
         * @instance
         */
        LastReq.prototype.aid = 0;

        /**
         * LastReq token.
         * @member {string} token
         * @memberof jlwr.LastReq
         * @instance
         */
        LastReq.prototype.token = "";

        /**
         * Creates a new LastReq instance using the specified properties.
         * @function create
         * @memberof jlwr.LastReq
         * @static
         * @param {jlwr.ILastReq=} [properties] Properties to set
         * @returns {jlwr.LastReq} LastReq instance
         */
        LastReq.create = function create(properties) {
            return new LastReq(properties);
        };

        /**
         * Encodes the specified LastReq message. Does not implicitly {@link jlwr.LastReq.verify|verify} messages.
         * @function encode
         * @memberof jlwr.LastReq
         * @static
         * @param {jlwr.ILastReq} message LastReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastReq.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.aid != null && Object.hasOwnProperty.call(message, "aid"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aid);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
            return writer;
        };

        /**
         * Encodes the specified LastReq message, length delimited. Does not implicitly {@link jlwr.LastReq.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.LastReq
         * @static
         * @param {jlwr.ILastReq} message LastReq message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastReq.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LastReq message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.LastReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.LastReq} LastReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastReq.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.LastReq();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.aid = reader.int32();
                        break;
                    }
                case 2: {
                        message.token = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LastReq message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.LastReq
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.LastReq} LastReq
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastReq.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LastReq message.
         * @function verify
         * @memberof jlwr.LastReq
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LastReq.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.aid != null && message.hasOwnProperty("aid"))
                if (!$util.isInteger(message.aid))
                    return "aid: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        /**
         * Creates a LastReq message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.LastReq
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.LastReq} LastReq
         */
        LastReq.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.LastReq)
                return object;
            let message = new $root.jlwr.LastReq();
            if (object.aid != null)
                message.aid = object.aid | 0;
            if (object.token != null)
                message.token = String(object.token);
            return message;
        };

        /**
         * Creates a plain object from a LastReq message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.LastReq
         * @static
         * @param {jlwr.LastReq} message LastReq
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LastReq.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.aid = 0;
                object.token = "";
            }
            if (message.aid != null && message.hasOwnProperty("aid"))
                object.aid = message.aid;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        /**
         * Converts this LastReq to JSON.
         * @function toJSON
         * @memberof jlwr.LastReq
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LastReq.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LastReq
         * @function getTypeUrl
         * @memberof jlwr.LastReq
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LastReq.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.LastReq";
        };

        return LastReq;
    })();

    jlwr.LastSpinAck = (function() {

        /**
         * Properties of a LastSpinAck.
         * @memberof jlwr
         * @interface ILastSpinAck
         * @property {number|null} [result] LastSpinAck result
         * @property {jlwr.ISpinAck|null} [lastSpin] LastSpinAck lastSpin
         */

        /**
         * Constructs a new LastSpinAck.
         * @memberof jlwr
         * @classdesc Represents a LastSpinAck.
         * @implements ILastSpinAck
         * @constructor
         * @param {jlwr.ILastSpinAck=} [properties] Properties to set
         */
        function LastSpinAck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LastSpinAck result.
         * @member {number} result
         * @memberof jlwr.LastSpinAck
         * @instance
         */
        LastSpinAck.prototype.result = 0;

        /**
         * LastSpinAck lastSpin.
         * @member {jlwr.ISpinAck|null|undefined} lastSpin
         * @memberof jlwr.LastSpinAck
         * @instance
         */
        LastSpinAck.prototype.lastSpin = null;

        /**
         * Creates a new LastSpinAck instance using the specified properties.
         * @function create
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {jlwr.ILastSpinAck=} [properties] Properties to set
         * @returns {jlwr.LastSpinAck} LastSpinAck instance
         */
        LastSpinAck.create = function create(properties) {
            return new LastSpinAck(properties);
        };

        /**
         * Encodes the specified LastSpinAck message. Does not implicitly {@link jlwr.LastSpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {jlwr.ILastSpinAck} message LastSpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastSpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.result);
            if (message.lastSpin != null && Object.hasOwnProperty.call(message, "lastSpin"))
                $root.jlwr.SpinAck.encode(message.lastSpin, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified LastSpinAck message, length delimited. Does not implicitly {@link jlwr.LastSpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {jlwr.ILastSpinAck} message LastSpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LastSpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LastSpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlwr.LastSpinAck} LastSpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastSpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlwr.LastSpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.result = reader.int32();
                        break;
                    }
                case 2: {
                        message.lastSpin = $root.jlwr.SpinAck.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LastSpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlwr.LastSpinAck} LastSpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LastSpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LastSpinAck message.
         * @function verify
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LastSpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin")) {
                let error = $root.jlwr.SpinAck.verify(message.lastSpin);
                if (error)
                    return "lastSpin." + error;
            }
            return null;
        };

        /**
         * Creates a LastSpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlwr.LastSpinAck} LastSpinAck
         */
        LastSpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlwr.LastSpinAck)
                return object;
            let message = new $root.jlwr.LastSpinAck();
            if (object.result != null)
                message.result = object.result | 0;
            if (object.lastSpin != null) {
                if (typeof object.lastSpin !== "object")
                    throw TypeError(".jlwr.LastSpinAck.lastSpin: object expected");
                message.lastSpin = $root.jlwr.SpinAck.fromObject(object.lastSpin);
            }
            return message;
        };

        /**
         * Creates a plain object from a LastSpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {jlwr.LastSpinAck} message LastSpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LastSpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.result = 0;
                object.lastSpin = null;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.lastSpin != null && message.hasOwnProperty("lastSpin"))
                object.lastSpin = $root.jlwr.SpinAck.toObject(message.lastSpin, options);
            return object;
        };

        /**
         * Converts this LastSpinAck to JSON.
         * @function toJSON
         * @memberof jlwr.LastSpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LastSpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LastSpinAck
         * @function getTypeUrl
         * @memberof jlwr.LastSpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LastSpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlwr.LastSpinAck";
        };

        return LastSpinAck;
    })();

    return jlwr;
})();

export const jlxyy = $root.jlxyy = (() => {

    /**
     * Namespace jlxyy.
     * @exports jlxyy
     * @namespace
     */
    const jlxyy = {};

    /**
     * Symbol enum.
     * @name jlxyy.Symbol
     * @enum {number}
     * @property {number} Wild=0 Wild value
     * @property {number} Bonus=1 Bonus value
     * @property {number} Symbol1=2 Symbol1 value
     * @property {number} Symbol2=3 Symbol2 value
     * @property {number} Symbol3=4 Symbol3 value
     * @property {number} Symbol4=5 Symbol4 value
     * @property {number} Symbol5=6 Symbol5 value
     * @property {number} Symbol6=7 Symbol6 value
     * @property {number} Symbol7=8 Symbol7 value
     * @property {number} Symbol8=9 Symbol8 value
     * @property {number} SymbolLittle=10 SymbolLittle value
     * @property {number} SymbolLittle2=11 SymbolLittle2 value
     */
    jlxyy.Symbol = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "Wild"] = 0;
        values[valuesById[1] = "Bonus"] = 1;
        values[valuesById[2] = "Symbol1"] = 2;
        values[valuesById[3] = "Symbol2"] = 3;
        values[valuesById[4] = "Symbol3"] = 4;
        values[valuesById[5] = "Symbol4"] = 5;
        values[valuesById[6] = "Symbol5"] = 6;
        values[valuesById[7] = "Symbol6"] = 7;
        values[valuesById[8] = "Symbol7"] = 8;
        values[valuesById[9] = "Symbol8"] = 9;
        values[valuesById[10] = "SymbolLittle"] = 10;
        values[valuesById[11] = "SymbolLittle2"] = 11;
        return values;
    })();

    /**
     * BonusType enum.
     * @name jlxyy.BonusType
     * @enum {number}
     * @property {number} None=0 None value
     * @property {number} RemoveSmall=1 RemoveSmall value
     * @property {number} WildReel=2 WildReel value
     * @property {number} SmallToBig=3 SmallToBig value
     * @property {number} MoveReel=4 MoveReel value
     * @property {number} Fake=5 Fake value
     */
    jlxyy.BonusType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "None"] = 0;
        values[valuesById[1] = "RemoveSmall"] = 1;
        values[valuesById[2] = "WildReel"] = 2;
        values[valuesById[3] = "SmallToBig"] = 3;
        values[valuesById[4] = "MoveReel"] = 4;
        values[valuesById[5] = "Fake"] = 5;
        return values;
    })();

    jlxyy.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof jlxyy
         * @interface IBlock
         * @property {number|null} [row] Block row
         * @property {number|null} [column] Block column
         */

        /**
         * Constructs a new Block.
         * @memberof jlxyy
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {jlxyy.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block row.
         * @member {number} row
         * @memberof jlxyy.Block
         * @instance
         */
        Block.prototype.row = 0;

        /**
         * Block column.
         * @member {number} column
         * @memberof jlxyy.Block
         * @instance
         */
        Block.prototype.column = 0;

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof jlxyy.Block
         * @static
         * @param {jlxyy.IBlock=} [properties] Properties to set
         * @returns {jlxyy.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link jlxyy.Block.verify|verify} messages.
         * @function encode
         * @memberof jlxyy.Block
         * @static
         * @param {jlxyy.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && Object.hasOwnProperty.call(message, "row"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.row);
            if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.column);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link jlxyy.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlxyy.Block
         * @static
         * @param {jlxyy.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof jlxyy.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlxyy.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlxyy.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.row = reader.uint32();
                        break;
                    }
                case 2: {
                        message.column = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlxyy.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlxyy.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof jlxyy.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row"))
                if (!$util.isInteger(message.row))
                    return "row: integer expected";
            if (message.column != null && message.hasOwnProperty("column"))
                if (!$util.isInteger(message.column))
                    return "column: integer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlxyy.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlxyy.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.jlxyy.Block)
                return object;
            let message = new $root.jlxyy.Block();
            if (object.row != null)
                message.row = object.row >>> 0;
            if (object.column != null)
                message.column = object.column >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlxyy.Block
         * @static
         * @param {jlxyy.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.row = 0;
                object.column = 0;
            }
            if (message.row != null && message.hasOwnProperty("row"))
                object.row = message.row;
            if (message.column != null && message.hasOwnProperty("column"))
                object.column = message.column;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof jlxyy.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof jlxyy.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlxyy.Block";
        };

        return Block;
    })();

    jlxyy.Plate = (function() {

        /**
         * Properties of a Plate.
         * @memberof jlxyy
         * @interface IPlate
         * @property {Array.<jlxyy.IColumnData>|null} [column] Plate column
         * @property {number|null} [win] Plate win
         */

        /**
         * Constructs a new Plate.
         * @memberof jlxyy
         * @classdesc Represents a Plate.
         * @implements IPlate
         * @constructor
         * @param {jlxyy.IPlate=} [properties] Properties to set
         */
        function Plate(properties) {
            this.column = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Plate column.
         * @member {Array.<jlxyy.IColumnData>} column
         * @memberof jlxyy.Plate
         * @instance
         */
        Plate.prototype.column = $util.emptyArray;

        /**
         * Plate win.
         * @member {number} win
         * @memberof jlxyy.Plate
         * @instance
         */
        Plate.prototype.win = 0;

        /**
         * Creates a new Plate instance using the specified properties.
         * @function create
         * @memberof jlxyy.Plate
         * @static
         * @param {jlxyy.IPlate=} [properties] Properties to set
         * @returns {jlxyy.Plate} Plate instance
         */
        Plate.create = function create(properties) {
            return new Plate(properties);
        };

        /**
         * Encodes the specified Plate message. Does not implicitly {@link jlxyy.Plate.verify|verify} messages.
         * @function encode
         * @memberof jlxyy.Plate
         * @static
         * @param {jlxyy.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.column != null && message.column.length)
                for (let i = 0; i < message.column.length; ++i)
                    $root.jlxyy.ColumnData.encode(message.column[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.win != null && Object.hasOwnProperty.call(message, "win"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.win);
            return writer;
        };

        /**
         * Encodes the specified Plate message, length delimited. Does not implicitly {@link jlxyy.Plate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlxyy.Plate
         * @static
         * @param {jlxyy.IPlate} message Plate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Plate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Plate message from the specified reader or buffer.
         * @function decode
         * @memberof jlxyy.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlxyy.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlxyy.Plate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.column && message.column.length))
                            message.column = [];
                        message.column.push($root.jlxyy.ColumnData.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.win = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Plate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlxyy.Plate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlxyy.Plate} Plate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Plate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Plate message.
         * @function verify
         * @memberof jlxyy.Plate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Plate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.column != null && message.hasOwnProperty("column")) {
                if (!Array.isArray(message.column))
                    return "column: array expected";
                for (let i = 0; i < message.column.length; ++i) {
                    let error = $root.jlxyy.ColumnData.verify(message.column[i]);
                    if (error)
                        return "column." + error;
                }
            }
            if (message.win != null && message.hasOwnProperty("win"))
                if (typeof message.win !== "number")
                    return "win: number expected";
            return null;
        };

        /**
         * Creates a Plate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlxyy.Plate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlxyy.Plate} Plate
         */
        Plate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlxyy.Plate)
                return object;
            let message = new $root.jlxyy.Plate();
            if (object.column) {
                if (!Array.isArray(object.column))
                    throw TypeError(".jlxyy.Plate.column: array expected");
                message.column = [];
                for (let i = 0; i < object.column.length; ++i) {
                    if (typeof object.column[i] !== "object")
                        throw TypeError(".jlxyy.Plate.column: object expected");
                    message.column[i] = $root.jlxyy.ColumnData.fromObject(object.column[i]);
                }
            }
            if (object.win != null)
                message.win = Number(object.win);
            return message;
        };

        /**
         * Creates a plain object from a Plate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlxyy.Plate
         * @static
         * @param {jlxyy.Plate} message Plate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Plate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.column = [];
            if (options.defaults)
                object.win = 0;
            if (message.column && message.column.length) {
                object.column = [];
                for (let j = 0; j < message.column.length; ++j)
                    object.column[j] = $root.jlxyy.ColumnData.toObject(message.column[j], options);
            }
            if (message.win != null && message.hasOwnProperty("win"))
                object.win = options.json && !isFinite(message.win) ? String(message.win) : message.win;
            return object;
        };

        /**
         * Converts this Plate to JSON.
         * @function toJSON
         * @memberof jlxyy.Plate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Plate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Plate
         * @function getTypeUrl
         * @memberof jlxyy.Plate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Plate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlxyy.Plate";
        };

        return Plate;
    })();

    jlxyy.AllPlate = (function() {

        /**
         * Properties of an AllPlate.
         * @memberof jlxyy
         * @interface IAllPlate
         * @property {Array.<jlxyy.IPlate>|null} [plate] AllPlate plate
         * @property {jlxyy.BonusType|null} [bonusType] AllPlate bonusType
         */

        /**
         * Constructs a new AllPlate.
         * @memberof jlxyy
         * @classdesc Represents an AllPlate.
         * @implements IAllPlate
         * @constructor
         * @param {jlxyy.IAllPlate=} [properties] Properties to set
         */
        function AllPlate(properties) {
            this.plate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllPlate plate.
         * @member {Array.<jlxyy.IPlate>} plate
         * @memberof jlxyy.AllPlate
         * @instance
         */
        AllPlate.prototype.plate = $util.emptyArray;

        /**
         * AllPlate bonusType.
         * @member {jlxyy.BonusType} bonusType
         * @memberof jlxyy.AllPlate
         * @instance
         */
        AllPlate.prototype.bonusType = 0;

        /**
         * Creates a new AllPlate instance using the specified properties.
         * @function create
         * @memberof jlxyy.AllPlate
         * @static
         * @param {jlxyy.IAllPlate=} [properties] Properties to set
         * @returns {jlxyy.AllPlate} AllPlate instance
         */
        AllPlate.create = function create(properties) {
            return new AllPlate(properties);
        };

        /**
         * Encodes the specified AllPlate message. Does not implicitly {@link jlxyy.AllPlate.verify|verify} messages.
         * @function encode
         * @memberof jlxyy.AllPlate
         * @static
         * @param {jlxyy.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.plate != null && message.plate.length)
                for (let i = 0; i < message.plate.length; ++i)
                    $root.jlxyy.Plate.encode(message.plate[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.bonusType != null && Object.hasOwnProperty.call(message, "bonusType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.bonusType);
            return writer;
        };

        /**
         * Encodes the specified AllPlate message, length delimited. Does not implicitly {@link jlxyy.AllPlate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlxyy.AllPlate
         * @static
         * @param {jlxyy.IAllPlate} message AllPlate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllPlate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer.
         * @function decode
         * @memberof jlxyy.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlxyy.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlxyy.AllPlate();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.plate && message.plate.length))
                            message.plate = [];
                        message.plate.push($root.jlxyy.Plate.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.bonusType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AllPlate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlxyy.AllPlate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlxyy.AllPlate} AllPlate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllPlate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AllPlate message.
         * @function verify
         * @memberof jlxyy.AllPlate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AllPlate.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.plate != null && message.hasOwnProperty("plate")) {
                if (!Array.isArray(message.plate))
                    return "plate: array expected";
                for (let i = 0; i < message.plate.length; ++i) {
                    let error = $root.jlxyy.Plate.verify(message.plate[i]);
                    if (error)
                        return "plate." + error;
                }
            }
            if (message.bonusType != null && message.hasOwnProperty("bonusType"))
                switch (message.bonusType) {
                default:
                    return "bonusType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                    break;
                }
            return null;
        };

        /**
         * Creates an AllPlate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlxyy.AllPlate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlxyy.AllPlate} AllPlate
         */
        AllPlate.fromObject = function fromObject(object) {
            if (object instanceof $root.jlxyy.AllPlate)
                return object;
            let message = new $root.jlxyy.AllPlate();
            if (object.plate) {
                if (!Array.isArray(object.plate))
                    throw TypeError(".jlxyy.AllPlate.plate: array expected");
                message.plate = [];
                for (let i = 0; i < object.plate.length; ++i) {
                    if (typeof object.plate[i] !== "object")
                        throw TypeError(".jlxyy.AllPlate.plate: object expected");
                    message.plate[i] = $root.jlxyy.Plate.fromObject(object.plate[i]);
                }
            }
            switch (object.bonusType) {
            default:
                if (typeof object.bonusType === "number") {
                    message.bonusType = object.bonusType;
                    break;
                }
                break;
            case "None":
            case 0:
                message.bonusType = 0;
                break;
            case "RemoveSmall":
            case 1:
                message.bonusType = 1;
                break;
            case "WildReel":
            case 2:
                message.bonusType = 2;
                break;
            case "SmallToBig":
            case 3:
                message.bonusType = 3;
                break;
            case "MoveReel":
            case 4:
                message.bonusType = 4;
                break;
            case "Fake":
            case 5:
                message.bonusType = 5;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an AllPlate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlxyy.AllPlate
         * @static
         * @param {jlxyy.AllPlate} message AllPlate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllPlate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.plate = [];
            if (options.defaults)
                object.bonusType = options.enums === String ? "None" : 0;
            if (message.plate && message.plate.length) {
                object.plate = [];
                for (let j = 0; j < message.plate.length; ++j)
                    object.plate[j] = $root.jlxyy.Plate.toObject(message.plate[j], options);
            }
            if (message.bonusType != null && message.hasOwnProperty("bonusType"))
                object.bonusType = options.enums === String ? $root.jlxyy.BonusType[message.bonusType] === undefined ? message.bonusType : $root.jlxyy.BonusType[message.bonusType] : message.bonusType;
            return object;
        };

        /**
         * Converts this AllPlate to JSON.
         * @function toJSON
         * @memberof jlxyy.AllPlate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllPlate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllPlate
         * @function getTypeUrl
         * @memberof jlxyy.AllPlate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllPlate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlxyy.AllPlate";
        };

        return AllPlate;
    })();

    jlxyy.ColumnData = (function() {

        /**
         * Properties of a ColumnData.
         * @memberof jlxyy
         * @interface IColumnData
         * @property {Array.<jlxyy.ICell>|null} [row] ColumnData row
         * @property {boolean|null} [isChange] ColumnData isChange
         */

        /**
         * Constructs a new ColumnData.
         * @memberof jlxyy
         * @classdesc Represents a ColumnData.
         * @implements IColumnData
         * @constructor
         * @param {jlxyy.IColumnData=} [properties] Properties to set
         */
        function ColumnData(properties) {
            this.row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnData row.
         * @member {Array.<jlxyy.ICell>} row
         * @memberof jlxyy.ColumnData
         * @instance
         */
        ColumnData.prototype.row = $util.emptyArray;

        /**
         * ColumnData isChange.
         * @member {boolean} isChange
         * @memberof jlxyy.ColumnData
         * @instance
         */
        ColumnData.prototype.isChange = false;

        /**
         * Creates a new ColumnData instance using the specified properties.
         * @function create
         * @memberof jlxyy.ColumnData
         * @static
         * @param {jlxyy.IColumnData=} [properties] Properties to set
         * @returns {jlxyy.ColumnData} ColumnData instance
         */
        ColumnData.create = function create(properties) {
            return new ColumnData(properties);
        };

        /**
         * Encodes the specified ColumnData message. Does not implicitly {@link jlxyy.ColumnData.verify|verify} messages.
         * @function encode
         * @memberof jlxyy.ColumnData
         * @static
         * @param {jlxyy.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.row != null && message.row.length)
                for (let i = 0; i < message.row.length; ++i)
                    $root.jlxyy.Cell.encode(message.row[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.isChange != null && Object.hasOwnProperty.call(message, "isChange"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isChange);
            return writer;
        };

        /**
         * Encodes the specified ColumnData message, length delimited. Does not implicitly {@link jlxyy.ColumnData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlxyy.ColumnData
         * @static
         * @param {jlxyy.IColumnData} message ColumnData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer.
         * @function decode
         * @memberof jlxyy.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlxyy.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlxyy.ColumnData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.row && message.row.length))
                            message.row = [];
                        message.row.push($root.jlxyy.Cell.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.isChange = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlxyy.ColumnData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlxyy.ColumnData} ColumnData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnData message.
         * @function verify
         * @memberof jlxyy.ColumnData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.row != null && message.hasOwnProperty("row")) {
                if (!Array.isArray(message.row))
                    return "row: array expected";
                for (let i = 0; i < message.row.length; ++i) {
                    let error = $root.jlxyy.Cell.verify(message.row[i]);
                    if (error)
                        return "row." + error;
                }
            }
            if (message.isChange != null && message.hasOwnProperty("isChange"))
                if (typeof message.isChange !== "boolean")
                    return "isChange: boolean expected";
            return null;
        };

        /**
         * Creates a ColumnData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlxyy.ColumnData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlxyy.ColumnData} ColumnData
         */
        ColumnData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlxyy.ColumnData)
                return object;
            let message = new $root.jlxyy.ColumnData();
            if (object.row) {
                if (!Array.isArray(object.row))
                    throw TypeError(".jlxyy.ColumnData.row: array expected");
                message.row = [];
                for (let i = 0; i < object.row.length; ++i) {
                    if (typeof object.row[i] !== "object")
                        throw TypeError(".jlxyy.ColumnData.row: object expected");
                    message.row[i] = $root.jlxyy.Cell.fromObject(object.row[i]);
                }
            }
            if (object.isChange != null)
                message.isChange = Boolean(object.isChange);
            return message;
        };

        /**
         * Creates a plain object from a ColumnData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlxyy.ColumnData
         * @static
         * @param {jlxyy.ColumnData} message ColumnData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.row = [];
            if (options.defaults)
                object.isChange = false;
            if (message.row && message.row.length) {
                object.row = [];
                for (let j = 0; j < message.row.length; ++j)
                    object.row[j] = $root.jlxyy.Cell.toObject(message.row[j], options);
            }
            if (message.isChange != null && message.hasOwnProperty("isChange"))
                object.isChange = message.isChange;
            return object;
        };

        /**
         * Converts this ColumnData to JSON.
         * @function toJSON
         * @memberof jlxyy.ColumnData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnData
         * @function getTypeUrl
         * @memberof jlxyy.ColumnData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlxyy.ColumnData";
        };

        return ColumnData;
    })();

    jlxyy.Cell = (function() {

        /**
         * Properties of a Cell.
         * @memberof jlxyy
         * @interface ICell
         * @property {jlxyy.Symbol|null} [symbol] Cell symbol
         * @property {boolean|null} [isConnect] Cell isConnect
         * @property {boolean|null} [isChange] Cell isChange
         */

        /**
         * Constructs a new Cell.
         * @memberof jlxyy
         * @classdesc Represents a Cell.
         * @implements ICell
         * @constructor
         * @param {jlxyy.ICell=} [properties] Properties to set
         */
        function Cell(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Cell symbol.
         * @member {jlxyy.Symbol} symbol
         * @memberof jlxyy.Cell
         * @instance
         */
        Cell.prototype.symbol = 0;

        /**
         * Cell isConnect.
         * @member {boolean} isConnect
         * @memberof jlxyy.Cell
         * @instance
         */
        Cell.prototype.isConnect = false;

        /**
         * Cell isChange.
         * @member {boolean} isChange
         * @memberof jlxyy.Cell
         * @instance
         */
        Cell.prototype.isChange = false;

        /**
         * Creates a new Cell instance using the specified properties.
         * @function create
         * @memberof jlxyy.Cell
         * @static
         * @param {jlxyy.ICell=} [properties] Properties to set
         * @returns {jlxyy.Cell} Cell instance
         */
        Cell.create = function create(properties) {
            return new Cell(properties);
        };

        /**
         * Encodes the specified Cell message. Does not implicitly {@link jlxyy.Cell.verify|verify} messages.
         * @function encode
         * @memberof jlxyy.Cell
         * @static
         * @param {jlxyy.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.symbol);
            if (message.isConnect != null && Object.hasOwnProperty.call(message, "isConnect"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isConnect);
            if (message.isChange != null && Object.hasOwnProperty.call(message, "isChange"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isChange);
            return writer;
        };

        /**
         * Encodes the specified Cell message, length delimited. Does not implicitly {@link jlxyy.Cell.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlxyy.Cell
         * @static
         * @param {jlxyy.ICell} message Cell message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Cell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Cell message from the specified reader or buffer.
         * @function decode
         * @memberof jlxyy.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlxyy.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlxyy.Cell();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.isConnect = reader.bool();
                        break;
                    }
                case 3: {
                        message.isChange = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Cell message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlxyy.Cell
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlxyy.Cell} Cell
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Cell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Cell message.
         * @function verify
         * @memberof jlxyy.Cell
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Cell.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                switch (message.symbol) {
                default:
                    return "symbol: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                    break;
                }
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                if (typeof message.isConnect !== "boolean")
                    return "isConnect: boolean expected";
            if (message.isChange != null && message.hasOwnProperty("isChange"))
                if (typeof message.isChange !== "boolean")
                    return "isChange: boolean expected";
            return null;
        };

        /**
         * Creates a Cell message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlxyy.Cell
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlxyy.Cell} Cell
         */
        Cell.fromObject = function fromObject(object) {
            if (object instanceof $root.jlxyy.Cell)
                return object;
            let message = new $root.jlxyy.Cell();
            switch (object.symbol) {
            default:
                if (typeof object.symbol === "number") {
                    message.symbol = object.symbol;
                    break;
                }
                break;
            case "Wild":
            case 0:
                message.symbol = 0;
                break;
            case "Bonus":
            case 1:
                message.symbol = 1;
                break;
            case "Symbol1":
            case 2:
                message.symbol = 2;
                break;
            case "Symbol2":
            case 3:
                message.symbol = 3;
                break;
            case "Symbol3":
            case 4:
                message.symbol = 4;
                break;
            case "Symbol4":
            case 5:
                message.symbol = 5;
                break;
            case "Symbol5":
            case 6:
                message.symbol = 6;
                break;
            case "Symbol6":
            case 7:
                message.symbol = 7;
                break;
            case "Symbol7":
            case 8:
                message.symbol = 8;
                break;
            case "Symbol8":
            case 9:
                message.symbol = 9;
                break;
            case "SymbolLittle":
            case 10:
                message.symbol = 10;
                break;
            case "SymbolLittle2":
            case 11:
                message.symbol = 11;
                break;
            }
            if (object.isConnect != null)
                message.isConnect = Boolean(object.isConnect);
            if (object.isChange != null)
                message.isChange = Boolean(object.isChange);
            return message;
        };

        /**
         * Creates a plain object from a Cell message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlxyy.Cell
         * @static
         * @param {jlxyy.Cell} message Cell
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Cell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.symbol = options.enums === String ? "Wild" : 0;
                object.isConnect = false;
                object.isChange = false;
            }
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = options.enums === String ? $root.jlxyy.Symbol[message.symbol] === undefined ? message.symbol : $root.jlxyy.Symbol[message.symbol] : message.symbol;
            if (message.isConnect != null && message.hasOwnProperty("isConnect"))
                object.isConnect = message.isConnect;
            if (message.isChange != null && message.hasOwnProperty("isChange"))
                object.isChange = message.isChange;
            return object;
        };

        /**
         * Converts this Cell to JSON.
         * @function toJSON
         * @memberof jlxyy.Cell
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Cell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Cell
         * @function getTypeUrl
         * @memberof jlxyy.Cell
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Cell.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlxyy.Cell";
        };

        return Cell;
    })();

    return jlxyy;
})();

export const jlzs = $root.jlzs = (() => {

    /**
     * Namespace jlzs.
     * @exports jlzs
     * @namespace
     */
    const jlzs = {};

    jlzs.Column = (function() {

        /**
         * Properties of a Column.
         * @memberof jlzs
         * @interface IColumn
         * @property {Array.<number>|null} [Col] Column Col
         */

        /**
         * Constructs a new Column.
         * @memberof jlzs
         * @classdesc Represents a Column.
         * @implements IColumn
         * @constructor
         * @param {jlzs.IColumn=} [properties] Properties to set
         */
        function Column(properties) {
            this.Col = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Column Col.
         * @member {Array.<number>} Col
         * @memberof jlzs.Column
         * @instance
         */
        Column.prototype.Col = $util.emptyArray;

        /**
         * Creates a new Column instance using the specified properties.
         * @function create
         * @memberof jlzs.Column
         * @static
         * @param {jlzs.IColumn=} [properties] Properties to set
         * @returns {jlzs.Column} Column instance
         */
        Column.create = function create(properties) {
            return new Column(properties);
        };

        /**
         * Encodes the specified Column message. Does not implicitly {@link jlzs.Column.verify|verify} messages.
         * @function encode
         * @memberof jlzs.Column
         * @static
         * @param {jlzs.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Col != null && message.Col.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.Col.length; ++i)
                    writer.int32(message.Col[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified Column message, length delimited. Does not implicitly {@link jlzs.Column.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlzs.Column
         * @static
         * @param {jlzs.IColumn} message Column message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Column.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Column message from the specified reader or buffer.
         * @function decode
         * @memberof jlzs.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlzs.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlzs.Column();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.Col && message.Col.length))
                            message.Col = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Col.push(reader.int32());
                        } else
                            message.Col.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Column message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlzs.Column
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlzs.Column} Column
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Column.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Column message.
         * @function verify
         * @memberof jlzs.Column
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Column.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Col != null && message.hasOwnProperty("Col")) {
                if (!Array.isArray(message.Col))
                    return "Col: array expected";
                for (let i = 0; i < message.Col.length; ++i)
                    if (!$util.isInteger(message.Col[i]))
                        return "Col: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a Column message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlzs.Column
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlzs.Column} Column
         */
        Column.fromObject = function fromObject(object) {
            if (object instanceof $root.jlzs.Column)
                return object;
            let message = new $root.jlzs.Column();
            if (object.Col) {
                if (!Array.isArray(object.Col))
                    throw TypeError(".jlzs.Column.Col: array expected");
                message.Col = [];
                for (let i = 0; i < object.Col.length; ++i)
                    message.Col[i] = object.Col[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a Column message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlzs.Column
         * @static
         * @param {jlzs.Column} message Column
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Column.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Col = [];
            if (message.Col && message.Col.length) {
                object.Col = [];
                for (let j = 0; j < message.Col.length; ++j)
                    object.Col[j] = message.Col[j];
            }
            return object;
        };

        /**
         * Converts this Column to JSON.
         * @function toJSON
         * @memberof jlzs.Column
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Column.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Column
         * @function getTypeUrl
         * @memberof jlzs.Column
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Column.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlzs.Column";
        };

        return Column;
    })();

    jlzs.AwardData = (function() {

        /**
         * Properties of an AwardData.
         * @memberof jlzs
         * @interface IAwardData
         * @property {number|null} [Symbol] AwardData Symbol
         * @property {number|null} [Count] AwardData Count
         * @property {number|null} [LineNum] AwardData LineNum
         * @property {number|null} [Win] AwardData Win
         * @property {Array.<number>|null} [Pos] AwardData Pos
         */

        /**
         * Constructs a new AwardData.
         * @memberof jlzs
         * @classdesc Represents an AwardData.
         * @implements IAwardData
         * @constructor
         * @param {jlzs.IAwardData=} [properties] Properties to set
         */
        function AwardData(properties) {
            this.Pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AwardData Symbol.
         * @member {number} Symbol
         * @memberof jlzs.AwardData
         * @instance
         */
        AwardData.prototype.Symbol = 0;

        /**
         * AwardData Count.
         * @member {number} Count
         * @memberof jlzs.AwardData
         * @instance
         */
        AwardData.prototype.Count = 0;

        /**
         * AwardData LineNum.
         * @member {number} LineNum
         * @memberof jlzs.AwardData
         * @instance
         */
        AwardData.prototype.LineNum = 0;

        /**
         * AwardData Win.
         * @member {number} Win
         * @memberof jlzs.AwardData
         * @instance
         */
        AwardData.prototype.Win = 0;

        /**
         * AwardData Pos.
         * @member {Array.<number>} Pos
         * @memberof jlzs.AwardData
         * @instance
         */
        AwardData.prototype.Pos = $util.emptyArray;

        /**
         * Creates a new AwardData instance using the specified properties.
         * @function create
         * @memberof jlzs.AwardData
         * @static
         * @param {jlzs.IAwardData=} [properties] Properties to set
         * @returns {jlzs.AwardData} AwardData instance
         */
        AwardData.create = function create(properties) {
            return new AwardData(properties);
        };

        /**
         * Encodes the specified AwardData message. Does not implicitly {@link jlzs.AwardData.verify|verify} messages.
         * @function encode
         * @memberof jlzs.AwardData
         * @static
         * @param {jlzs.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.Symbol != null && Object.hasOwnProperty.call(message, "Symbol"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.Symbol);
            if (message.Count != null && Object.hasOwnProperty.call(message, "Count"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.Count);
            if (message.LineNum != null && Object.hasOwnProperty.call(message, "LineNum"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.LineNum);
            if (message.Win != null && Object.hasOwnProperty.call(message, "Win"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.Win);
            if (message.Pos != null && message.Pos.length) {
                writer.uint32(/* id 5, wireType 2 =*/42).fork();
                for (let i = 0; i < message.Pos.length; ++i)
                    writer.int32(message.Pos[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Encodes the specified AwardData message, length delimited. Does not implicitly {@link jlzs.AwardData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlzs.AwardData
         * @static
         * @param {jlzs.IAwardData} message AwardData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AwardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer.
         * @function decode
         * @memberof jlzs.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlzs.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlzs.AwardData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.Symbol = reader.int32();
                        break;
                    }
                case 2: {
                        message.Count = reader.int32();
                        break;
                    }
                case 3: {
                        message.LineNum = reader.int32();
                        break;
                    }
                case 4: {
                        message.Win = reader.double();
                        break;
                    }
                case 5: {
                        if (!(message.Pos && message.Pos.length))
                            message.Pos = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.Pos.push(reader.int32());
                        } else
                            message.Pos.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AwardData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlzs.AwardData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlzs.AwardData} AwardData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AwardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AwardData message.
         * @function verify
         * @memberof jlzs.AwardData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AwardData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                if (!$util.isInteger(message.Symbol))
                    return "Symbol: integer expected";
            if (message.Count != null && message.hasOwnProperty("Count"))
                if (!$util.isInteger(message.Count))
                    return "Count: integer expected";
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                if (!$util.isInteger(message.LineNum))
                    return "LineNum: integer expected";
            if (message.Win != null && message.hasOwnProperty("Win"))
                if (typeof message.Win !== "number")
                    return "Win: number expected";
            if (message.Pos != null && message.hasOwnProperty("Pos")) {
                if (!Array.isArray(message.Pos))
                    return "Pos: array expected";
                for (let i = 0; i < message.Pos.length; ++i)
                    if (!$util.isInteger(message.Pos[i]))
                        return "Pos: integer[] expected";
            }
            return null;
        };

        /**
         * Creates an AwardData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlzs.AwardData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlzs.AwardData} AwardData
         */
        AwardData.fromObject = function fromObject(object) {
            if (object instanceof $root.jlzs.AwardData)
                return object;
            let message = new $root.jlzs.AwardData();
            if (object.Symbol != null)
                message.Symbol = object.Symbol | 0;
            if (object.Count != null)
                message.Count = object.Count | 0;
            if (object.LineNum != null)
                message.LineNum = object.LineNum | 0;
            if (object.Win != null)
                message.Win = Number(object.Win);
            if (object.Pos) {
                if (!Array.isArray(object.Pos))
                    throw TypeError(".jlzs.AwardData.Pos: array expected");
                message.Pos = [];
                for (let i = 0; i < object.Pos.length; ++i)
                    message.Pos[i] = object.Pos[i] | 0;
            }
            return message;
        };

        /**
         * Creates a plain object from an AwardData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlzs.AwardData
         * @static
         * @param {jlzs.AwardData} message AwardData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AwardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.Pos = [];
            if (options.defaults) {
                object.Symbol = 0;
                object.Count = 0;
                object.LineNum = 0;
                object.Win = 0;
            }
            if (message.Symbol != null && message.hasOwnProperty("Symbol"))
                object.Symbol = message.Symbol;
            if (message.Count != null && message.hasOwnProperty("Count"))
                object.Count = message.Count;
            if (message.LineNum != null && message.hasOwnProperty("LineNum"))
                object.LineNum = message.LineNum;
            if (message.Win != null && message.hasOwnProperty("Win"))
                object.Win = options.json && !isFinite(message.Win) ? String(message.Win) : message.Win;
            if (message.Pos && message.Pos.length) {
                object.Pos = [];
                for (let j = 0; j < message.Pos.length; ++j)
                    object.Pos[j] = message.Pos[j];
            }
            return object;
        };

        /**
         * Converts this AwardData to JSON.
         * @function toJSON
         * @memberof jlzs.AwardData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AwardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AwardData
         * @function getTypeUrl
         * @memberof jlzs.AwardData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AwardData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlzs.AwardData";
        };

        return AwardData;
    })();

    jlzs.ComboStageInfo = (function() {

        /**
         * Properties of a ComboStageInfo.
         * @memberof jlzs
         * @interface IComboStageInfo
         * @property {Array.<jlzs.IColumn>|null} [ComboStageSymbol] ComboStageInfo ComboStageSymbol
         * @property {number|null} [ComboStageWin] ComboStageInfo ComboStageWin
         * @property {Array.<jlzs.IAwardData>|null} [AwardDataVec] ComboStageInfo AwardDataVec
         * @property {Array.<jlzs.IColumn>|null} [FillSymbols] ComboStageInfo FillSymbols
         * @property {number|null} [ChangeSpecial] ComboStageInfo ChangeSpecial
         * @property {boolean|null} [NeedEliminate] ComboStageInfo NeedEliminate
         * @property {Array.<jlzs.IColumn>|null} [Eliminate] ComboStageInfo Eliminate
         * @property {number|null} [ScatterCount] ComboStageInfo ScatterCount
         * @property {number|null} [NowExtend] ComboStageInfo NowExtend
         * @property {number|null} [GetExtend] ComboStageInfo GetExtend
         * @property {number|null} [GetPlusRound] ComboStageInfo GetPlusRound
         * @property {boolean|null} [MaxFlag] ComboStageInfo MaxFlag
         */

        /**
         * Constructs a new ComboStageInfo.
         * @memberof jlzs
         * @classdesc Represents a ComboStageInfo.
         * @implements IComboStageInfo
         * @constructor
         * @param {jlzs.IComboStageInfo=} [properties] Properties to set
         */
        function ComboStageInfo(properties) {
            this.ComboStageSymbol = [];
            this.AwardDataVec = [];
            this.FillSymbols = [];
            this.Eliminate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComboStageInfo ComboStageSymbol.
         * @member {Array.<jlzs.IColumn>} ComboStageSymbol
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageSymbol = $util.emptyArray;

        /**
         * ComboStageInfo ComboStageWin.
         * @member {number} ComboStageWin
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ComboStageWin = 0;

        /**
         * ComboStageInfo AwardDataVec.
         * @member {Array.<jlzs.IAwardData>} AwardDataVec
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.AwardDataVec = $util.emptyArray;

        /**
         * ComboStageInfo FillSymbols.
         * @member {Array.<jlzs.IColumn>} FillSymbols
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.FillSymbols = $util.emptyArray;

        /**
         * ComboStageInfo ChangeSpecial.
         * @member {number} ChangeSpecial
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ChangeSpecial = 0;

        /**
         * ComboStageInfo NeedEliminate.
         * @member {boolean} NeedEliminate
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NeedEliminate = false;

        /**
         * ComboStageInfo Eliminate.
         * @member {Array.<jlzs.IColumn>} Eliminate
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.Eliminate = $util.emptyArray;

        /**
         * ComboStageInfo ScatterCount.
         * @member {number} ScatterCount
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.ScatterCount = 0;

        /**
         * ComboStageInfo NowExtend.
         * @member {number} NowExtend
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.NowExtend = 0;

        /**
         * ComboStageInfo GetExtend.
         * @member {number} GetExtend
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.GetExtend = 0;

        /**
         * ComboStageInfo GetPlusRound.
         * @member {number} GetPlusRound
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.GetPlusRound = 0;

        /**
         * ComboStageInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlzs.ComboStageInfo
         * @instance
         */
        ComboStageInfo.prototype.MaxFlag = false;

        /**
         * Creates a new ComboStageInfo instance using the specified properties.
         * @function create
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {jlzs.IComboStageInfo=} [properties] Properties to set
         * @returns {jlzs.ComboStageInfo} ComboStageInfo instance
         */
        ComboStageInfo.create = function create(properties) {
            return new ComboStageInfo(properties);
        };

        /**
         * Encodes the specified ComboStageInfo message. Does not implicitly {@link jlzs.ComboStageInfo.verify|verify} messages.
         * @function encode
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {jlzs.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageSymbol != null && message.ComboStageSymbol.length)
                for (let i = 0; i < message.ComboStageSymbol.length; ++i)
                    $root.jlzs.Column.encode(message.ComboStageSymbol[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ComboStageWin != null && Object.hasOwnProperty.call(message, "ComboStageWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.ComboStageWin);
            if (message.AwardDataVec != null && message.AwardDataVec.length)
                for (let i = 0; i < message.AwardDataVec.length; ++i)
                    $root.jlzs.AwardData.encode(message.AwardDataVec[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.FillSymbols != null && message.FillSymbols.length)
                for (let i = 0; i < message.FillSymbols.length; ++i)
                    $root.jlzs.Column.encode(message.FillSymbols[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.ChangeSpecial != null && Object.hasOwnProperty.call(message, "ChangeSpecial"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ChangeSpecial);
            if (message.NeedEliminate != null && Object.hasOwnProperty.call(message, "NeedEliminate"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.NeedEliminate);
            if (message.Eliminate != null && message.Eliminate.length)
                for (let i = 0; i < message.Eliminate.length; ++i)
                    $root.jlzs.Column.encode(message.Eliminate[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.ScatterCount != null && Object.hasOwnProperty.call(message, "ScatterCount"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.ScatterCount);
            if (message.NowExtend != null && Object.hasOwnProperty.call(message, "NowExtend"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.NowExtend);
            if (message.GetExtend != null && Object.hasOwnProperty.call(message, "GetExtend"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.GetExtend);
            if (message.GetPlusRound != null && Object.hasOwnProperty.call(message, "GetPlusRound"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.GetPlusRound);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified ComboStageInfo message, length delimited. Does not implicitly {@link jlzs.ComboStageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {jlzs.IComboStageInfo} message ComboStageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComboStageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlzs.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlzs.ComboStageInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageSymbol && message.ComboStageSymbol.length))
                            message.ComboStageSymbol = [];
                        message.ComboStageSymbol.push($root.jlzs.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.ComboStageWin = reader.double();
                        break;
                    }
                case 3: {
                        if (!(message.AwardDataVec && message.AwardDataVec.length))
                            message.AwardDataVec = [];
                        message.AwardDataVec.push($root.jlzs.AwardData.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        if (!(message.FillSymbols && message.FillSymbols.length))
                            message.FillSymbols = [];
                        message.FillSymbols.push($root.jlzs.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.ChangeSpecial = reader.int32();
                        break;
                    }
                case 6: {
                        message.NeedEliminate = reader.bool();
                        break;
                    }
                case 7: {
                        if (!(message.Eliminate && message.Eliminate.length))
                            message.Eliminate = [];
                        message.Eliminate.push($root.jlzs.Column.decode(reader, reader.uint32()));
                        break;
                    }
                case 8: {
                        message.ScatterCount = reader.int32();
                        break;
                    }
                case 9: {
                        message.NowExtend = reader.int32();
                        break;
                    }
                case 10: {
                        message.GetExtend = reader.int32();
                        break;
                    }
                case 11: {
                        message.GetPlusRound = reader.int32();
                        break;
                    }
                case 12: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComboStageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlzs.ComboStageInfo} ComboStageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComboStageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComboStageInfo message.
         * @function verify
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComboStageInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageSymbol != null && message.hasOwnProperty("ComboStageSymbol")) {
                if (!Array.isArray(message.ComboStageSymbol))
                    return "ComboStageSymbol: array expected";
                for (let i = 0; i < message.ComboStageSymbol.length; ++i) {
                    let error = $root.jlzs.Column.verify(message.ComboStageSymbol[i]);
                    if (error)
                        return "ComboStageSymbol." + error;
                }
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                if (typeof message.ComboStageWin !== "number")
                    return "ComboStageWin: number expected";
            if (message.AwardDataVec != null && message.hasOwnProperty("AwardDataVec")) {
                if (!Array.isArray(message.AwardDataVec))
                    return "AwardDataVec: array expected";
                for (let i = 0; i < message.AwardDataVec.length; ++i) {
                    let error = $root.jlzs.AwardData.verify(message.AwardDataVec[i]);
                    if (error)
                        return "AwardDataVec." + error;
                }
            }
            if (message.FillSymbols != null && message.hasOwnProperty("FillSymbols")) {
                if (!Array.isArray(message.FillSymbols))
                    return "FillSymbols: array expected";
                for (let i = 0; i < message.FillSymbols.length; ++i) {
                    let error = $root.jlzs.Column.verify(message.FillSymbols[i]);
                    if (error)
                        return "FillSymbols." + error;
                }
            }
            if (message.ChangeSpecial != null && message.hasOwnProperty("ChangeSpecial"))
                if (!$util.isInteger(message.ChangeSpecial))
                    return "ChangeSpecial: integer expected";
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                if (typeof message.NeedEliminate !== "boolean")
                    return "NeedEliminate: boolean expected";
            if (message.Eliminate != null && message.hasOwnProperty("Eliminate")) {
                if (!Array.isArray(message.Eliminate))
                    return "Eliminate: array expected";
                for (let i = 0; i < message.Eliminate.length; ++i) {
                    let error = $root.jlzs.Column.verify(message.Eliminate[i]);
                    if (error)
                        return "Eliminate." + error;
                }
            }
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                if (!$util.isInteger(message.ScatterCount))
                    return "ScatterCount: integer expected";
            if (message.NowExtend != null && message.hasOwnProperty("NowExtend"))
                if (!$util.isInteger(message.NowExtend))
                    return "NowExtend: integer expected";
            if (message.GetExtend != null && message.hasOwnProperty("GetExtend"))
                if (!$util.isInteger(message.GetExtend))
                    return "GetExtend: integer expected";
            if (message.GetPlusRound != null && message.hasOwnProperty("GetPlusRound"))
                if (!$util.isInteger(message.GetPlusRound))
                    return "GetPlusRound: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a ComboStageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlzs.ComboStageInfo} ComboStageInfo
         */
        ComboStageInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlzs.ComboStageInfo)
                return object;
            let message = new $root.jlzs.ComboStageInfo();
            if (object.ComboStageSymbol) {
                if (!Array.isArray(object.ComboStageSymbol))
                    throw TypeError(".jlzs.ComboStageInfo.ComboStageSymbol: array expected");
                message.ComboStageSymbol = [];
                for (let i = 0; i < object.ComboStageSymbol.length; ++i) {
                    if (typeof object.ComboStageSymbol[i] !== "object")
                        throw TypeError(".jlzs.ComboStageInfo.ComboStageSymbol: object expected");
                    message.ComboStageSymbol[i] = $root.jlzs.Column.fromObject(object.ComboStageSymbol[i]);
                }
            }
            if (object.ComboStageWin != null)
                message.ComboStageWin = Number(object.ComboStageWin);
            if (object.AwardDataVec) {
                if (!Array.isArray(object.AwardDataVec))
                    throw TypeError(".jlzs.ComboStageInfo.AwardDataVec: array expected");
                message.AwardDataVec = [];
                for (let i = 0; i < object.AwardDataVec.length; ++i) {
                    if (typeof object.AwardDataVec[i] !== "object")
                        throw TypeError(".jlzs.ComboStageInfo.AwardDataVec: object expected");
                    message.AwardDataVec[i] = $root.jlzs.AwardData.fromObject(object.AwardDataVec[i]);
                }
            }
            if (object.FillSymbols) {
                if (!Array.isArray(object.FillSymbols))
                    throw TypeError(".jlzs.ComboStageInfo.FillSymbols: array expected");
                message.FillSymbols = [];
                for (let i = 0; i < object.FillSymbols.length; ++i) {
                    if (typeof object.FillSymbols[i] !== "object")
                        throw TypeError(".jlzs.ComboStageInfo.FillSymbols: object expected");
                    message.FillSymbols[i] = $root.jlzs.Column.fromObject(object.FillSymbols[i]);
                }
            }
            if (object.ChangeSpecial != null)
                message.ChangeSpecial = object.ChangeSpecial | 0;
            if (object.NeedEliminate != null)
                message.NeedEliminate = Boolean(object.NeedEliminate);
            if (object.Eliminate) {
                if (!Array.isArray(object.Eliminate))
                    throw TypeError(".jlzs.ComboStageInfo.Eliminate: array expected");
                message.Eliminate = [];
                for (let i = 0; i < object.Eliminate.length; ++i) {
                    if (typeof object.Eliminate[i] !== "object")
                        throw TypeError(".jlzs.ComboStageInfo.Eliminate: object expected");
                    message.Eliminate[i] = $root.jlzs.Column.fromObject(object.Eliminate[i]);
                }
            }
            if (object.ScatterCount != null)
                message.ScatterCount = object.ScatterCount | 0;
            if (object.NowExtend != null)
                message.NowExtend = object.NowExtend | 0;
            if (object.GetExtend != null)
                message.GetExtend = object.GetExtend | 0;
            if (object.GetPlusRound != null)
                message.GetPlusRound = object.GetPlusRound | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a ComboStageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {jlzs.ComboStageInfo} message ComboStageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComboStageInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ComboStageSymbol = [];
                object.AwardDataVec = [];
                object.FillSymbols = [];
                object.Eliminate = [];
            }
            if (options.defaults) {
                object.ComboStageWin = 0;
                object.ChangeSpecial = 0;
                object.NeedEliminate = false;
                object.ScatterCount = 0;
                object.NowExtend = 0;
                object.GetExtend = 0;
                object.GetPlusRound = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageSymbol && message.ComboStageSymbol.length) {
                object.ComboStageSymbol = [];
                for (let j = 0; j < message.ComboStageSymbol.length; ++j)
                    object.ComboStageSymbol[j] = $root.jlzs.Column.toObject(message.ComboStageSymbol[j], options);
            }
            if (message.ComboStageWin != null && message.hasOwnProperty("ComboStageWin"))
                object.ComboStageWin = options.json && !isFinite(message.ComboStageWin) ? String(message.ComboStageWin) : message.ComboStageWin;
            if (message.AwardDataVec && message.AwardDataVec.length) {
                object.AwardDataVec = [];
                for (let j = 0; j < message.AwardDataVec.length; ++j)
                    object.AwardDataVec[j] = $root.jlzs.AwardData.toObject(message.AwardDataVec[j], options);
            }
            if (message.FillSymbols && message.FillSymbols.length) {
                object.FillSymbols = [];
                for (let j = 0; j < message.FillSymbols.length; ++j)
                    object.FillSymbols[j] = $root.jlzs.Column.toObject(message.FillSymbols[j], options);
            }
            if (message.ChangeSpecial != null && message.hasOwnProperty("ChangeSpecial"))
                object.ChangeSpecial = message.ChangeSpecial;
            if (message.NeedEliminate != null && message.hasOwnProperty("NeedEliminate"))
                object.NeedEliminate = message.NeedEliminate;
            if (message.Eliminate && message.Eliminate.length) {
                object.Eliminate = [];
                for (let j = 0; j < message.Eliminate.length; ++j)
                    object.Eliminate[j] = $root.jlzs.Column.toObject(message.Eliminate[j], options);
            }
            if (message.ScatterCount != null && message.hasOwnProperty("ScatterCount"))
                object.ScatterCount = message.ScatterCount;
            if (message.NowExtend != null && message.hasOwnProperty("NowExtend"))
                object.NowExtend = message.NowExtend;
            if (message.GetExtend != null && message.hasOwnProperty("GetExtend"))
                object.GetExtend = message.GetExtend;
            if (message.GetPlusRound != null && message.hasOwnProperty("GetPlusRound"))
                object.GetPlusRound = message.GetPlusRound;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this ComboStageInfo to JSON.
         * @function toJSON
         * @memberof jlzs.ComboStageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComboStageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComboStageInfo
         * @function getTypeUrl
         * @memberof jlzs.ComboStageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComboStageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlzs.ComboStageInfo";
        };

        return ComboStageInfo;
    })();

    jlzs.RoundInfo = (function() {

        /**
         * Properties of a RoundInfo.
         * @memberof jlzs
         * @interface IRoundInfo
         * @property {Array.<jlzs.IComboStageInfo>|null} [ComboStageData] RoundInfo ComboStageData
         * @property {number|null} [RoundWin] RoundInfo RoundWin
         * @property {number|null} [FreeRemainRound] RoundInfo FreeRemainRound
         * @property {number|null} [FreeTotalRound] RoundInfo FreeTotalRound
         * @property {number|null} [AddRounds] RoundInfo AddRounds
         * @property {number|null} [AwardTypeFlag] RoundInfo AwardTypeFlag
         * @property {boolean|null} [MaxFlag] RoundInfo MaxFlag
         */

        /**
         * Constructs a new RoundInfo.
         * @memberof jlzs
         * @classdesc Represents a RoundInfo.
         * @implements IRoundInfo
         * @constructor
         * @param {jlzs.IRoundInfo=} [properties] Properties to set
         */
        function RoundInfo(properties) {
            this.ComboStageData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundInfo ComboStageData.
         * @member {Array.<jlzs.IComboStageInfo>} ComboStageData
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.ComboStageData = $util.emptyArray;

        /**
         * RoundInfo RoundWin.
         * @member {number} RoundWin
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.RoundWin = 0;

        /**
         * RoundInfo FreeRemainRound.
         * @member {number} FreeRemainRound
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeRemainRound = 0;

        /**
         * RoundInfo FreeTotalRound.
         * @member {number} FreeTotalRound
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.FreeTotalRound = 0;

        /**
         * RoundInfo AddRounds.
         * @member {number} AddRounds
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AddRounds = 0;

        /**
         * RoundInfo AwardTypeFlag.
         * @member {number} AwardTypeFlag
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.AwardTypeFlag = 0;

        /**
         * RoundInfo MaxFlag.
         * @member {boolean} MaxFlag
         * @memberof jlzs.RoundInfo
         * @instance
         */
        RoundInfo.prototype.MaxFlag = false;

        /**
         * Creates a new RoundInfo instance using the specified properties.
         * @function create
         * @memberof jlzs.RoundInfo
         * @static
         * @param {jlzs.IRoundInfo=} [properties] Properties to set
         * @returns {jlzs.RoundInfo} RoundInfo instance
         */
        RoundInfo.create = function create(properties) {
            return new RoundInfo(properties);
        };

        /**
         * Encodes the specified RoundInfo message. Does not implicitly {@link jlzs.RoundInfo.verify|verify} messages.
         * @function encode
         * @memberof jlzs.RoundInfo
         * @static
         * @param {jlzs.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ComboStageData != null && message.ComboStageData.length)
                for (let i = 0; i < message.ComboStageData.length; ++i)
                    $root.jlzs.ComboStageInfo.encode(message.ComboStageData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.RoundWin != null && Object.hasOwnProperty.call(message, "RoundWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.RoundWin);
            if (message.FreeRemainRound != null && Object.hasOwnProperty.call(message, "FreeRemainRound"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.FreeRemainRound);
            if (message.FreeTotalRound != null && Object.hasOwnProperty.call(message, "FreeTotalRound"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.FreeTotalRound);
            if (message.AddRounds != null && Object.hasOwnProperty.call(message, "AddRounds"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.AddRounds);
            if (message.AwardTypeFlag != null && Object.hasOwnProperty.call(message, "AwardTypeFlag"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AwardTypeFlag);
            if (message.MaxFlag != null && Object.hasOwnProperty.call(message, "MaxFlag"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.MaxFlag);
            return writer;
        };

        /**
         * Encodes the specified RoundInfo message, length delimited. Does not implicitly {@link jlzs.RoundInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlzs.RoundInfo
         * @static
         * @param {jlzs.IRoundInfo} message RoundInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer.
         * @function decode
         * @memberof jlzs.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlzs.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlzs.RoundInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ComboStageData && message.ComboStageData.length))
                            message.ComboStageData = [];
                        message.ComboStageData.push($root.jlzs.ComboStageInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.RoundWin = reader.double();
                        break;
                    }
                case 3: {
                        message.FreeRemainRound = reader.int32();
                        break;
                    }
                case 4: {
                        message.FreeTotalRound = reader.int32();
                        break;
                    }
                case 5: {
                        message.AddRounds = reader.int32();
                        break;
                    }
                case 6: {
                        message.AwardTypeFlag = reader.int32();
                        break;
                    }
                case 7: {
                        message.MaxFlag = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlzs.RoundInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlzs.RoundInfo} RoundInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundInfo message.
         * @function verify
         * @memberof jlzs.RoundInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ComboStageData != null && message.hasOwnProperty("ComboStageData")) {
                if (!Array.isArray(message.ComboStageData))
                    return "ComboStageData: array expected";
                for (let i = 0; i < message.ComboStageData.length; ++i) {
                    let error = $root.jlzs.ComboStageInfo.verify(message.ComboStageData[i]);
                    if (error)
                        return "ComboStageData." + error;
                }
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                if (typeof message.RoundWin !== "number")
                    return "RoundWin: number expected";
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                if (!$util.isInteger(message.FreeRemainRound))
                    return "FreeRemainRound: integer expected";
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                if (!$util.isInteger(message.FreeTotalRound))
                    return "FreeTotalRound: integer expected";
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                if (!$util.isInteger(message.AddRounds))
                    return "AddRounds: integer expected";
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                if (!$util.isInteger(message.AwardTypeFlag))
                    return "AwardTypeFlag: integer expected";
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                if (typeof message.MaxFlag !== "boolean")
                    return "MaxFlag: boolean expected";
            return null;
        };

        /**
         * Creates a RoundInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlzs.RoundInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlzs.RoundInfo} RoundInfo
         */
        RoundInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.jlzs.RoundInfo)
                return object;
            let message = new $root.jlzs.RoundInfo();
            if (object.ComboStageData) {
                if (!Array.isArray(object.ComboStageData))
                    throw TypeError(".jlzs.RoundInfo.ComboStageData: array expected");
                message.ComboStageData = [];
                for (let i = 0; i < object.ComboStageData.length; ++i) {
                    if (typeof object.ComboStageData[i] !== "object")
                        throw TypeError(".jlzs.RoundInfo.ComboStageData: object expected");
                    message.ComboStageData[i] = $root.jlzs.ComboStageInfo.fromObject(object.ComboStageData[i]);
                }
            }
            if (object.RoundWin != null)
                message.RoundWin = Number(object.RoundWin);
            if (object.FreeRemainRound != null)
                message.FreeRemainRound = object.FreeRemainRound | 0;
            if (object.FreeTotalRound != null)
                message.FreeTotalRound = object.FreeTotalRound | 0;
            if (object.AddRounds != null)
                message.AddRounds = object.AddRounds | 0;
            if (object.AwardTypeFlag != null)
                message.AwardTypeFlag = object.AwardTypeFlag | 0;
            if (object.MaxFlag != null)
                message.MaxFlag = Boolean(object.MaxFlag);
            return message;
        };

        /**
         * Creates a plain object from a RoundInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlzs.RoundInfo
         * @static
         * @param {jlzs.RoundInfo} message RoundInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ComboStageData = [];
            if (options.defaults) {
                object.RoundWin = 0;
                object.FreeRemainRound = 0;
                object.FreeTotalRound = 0;
                object.AddRounds = 0;
                object.AwardTypeFlag = 0;
                object.MaxFlag = false;
            }
            if (message.ComboStageData && message.ComboStageData.length) {
                object.ComboStageData = [];
                for (let j = 0; j < message.ComboStageData.length; ++j)
                    object.ComboStageData[j] = $root.jlzs.ComboStageInfo.toObject(message.ComboStageData[j], options);
            }
            if (message.RoundWin != null && message.hasOwnProperty("RoundWin"))
                object.RoundWin = options.json && !isFinite(message.RoundWin) ? String(message.RoundWin) : message.RoundWin;
            if (message.FreeRemainRound != null && message.hasOwnProperty("FreeRemainRound"))
                object.FreeRemainRound = message.FreeRemainRound;
            if (message.FreeTotalRound != null && message.hasOwnProperty("FreeTotalRound"))
                object.FreeTotalRound = message.FreeTotalRound;
            if (message.AddRounds != null && message.hasOwnProperty("AddRounds"))
                object.AddRounds = message.AddRounds;
            if (message.AwardTypeFlag != null && message.hasOwnProperty("AwardTypeFlag"))
                object.AwardTypeFlag = message.AwardTypeFlag;
            if (message.MaxFlag != null && message.hasOwnProperty("MaxFlag"))
                object.MaxFlag = message.MaxFlag;
            return object;
        };

        /**
         * Converts this RoundInfo to JSON.
         * @function toJSON
         * @memberof jlzs.RoundInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundInfo
         * @function getTypeUrl
         * @memberof jlzs.RoundInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlzs.RoundInfo";
        };

        return RoundInfo;
    })();

    jlzs.SpinAck = (function() {

        /**
         * Properties of a SpinAck.
         * @memberof jlzs
         * @interface ISpinAck
         * @property {Array.<jlzs.IRoundInfo>|null} [RoundQueue] SpinAck RoundQueue
         * @property {number|null} [FreeTotalWin] SpinAck FreeTotalWin
         * @property {number|null} [TotalWin] SpinAck TotalWin
         * @property {string|null} [ShowIndex] SpinAck ShowIndex
         * @property {number|null} [NowMoney] SpinAck NowMoney
         * @property {number|null} [AckType] SpinAck AckType
         * @property {number|null} [RTP] SpinAck RTP
         * @property {number|null} [LID] SpinAck LID
         * @property {number|null} [RID] SpinAck RID
         */

        /**
         * Constructs a new SpinAck.
         * @memberof jlzs
         * @classdesc Represents a SpinAck.
         * @implements ISpinAck
         * @constructor
         * @param {jlzs.ISpinAck=} [properties] Properties to set
         */
        function SpinAck(properties) {
            this.RoundQueue = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SpinAck RoundQueue.
         * @member {Array.<jlzs.IRoundInfo>} RoundQueue
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.RoundQueue = $util.emptyArray;

        /**
         * SpinAck FreeTotalWin.
         * @member {number} FreeTotalWin
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.FreeTotalWin = 0;

        /**
         * SpinAck TotalWin.
         * @member {number} TotalWin
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.TotalWin = 0;

        /**
         * SpinAck ShowIndex.
         * @member {string} ShowIndex
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.ShowIndex = "";

        /**
         * SpinAck NowMoney.
         * @member {number} NowMoney
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.NowMoney = 0;

        /**
         * SpinAck AckType.
         * @member {number} AckType
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.AckType = 0;

        /**
         * SpinAck RTP.
         * @member {number} RTP
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.RTP = 0;

        /**
         * SpinAck LID.
         * @member {number} LID
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.LID = 0;

        /**
         * SpinAck RID.
         * @member {number} RID
         * @memberof jlzs.SpinAck
         * @instance
         */
        SpinAck.prototype.RID = 0;

        /**
         * Creates a new SpinAck instance using the specified properties.
         * @function create
         * @memberof jlzs.SpinAck
         * @static
         * @param {jlzs.ISpinAck=} [properties] Properties to set
         * @returns {jlzs.SpinAck} SpinAck instance
         */
        SpinAck.create = function create(properties) {
            return new SpinAck(properties);
        };

        /**
         * Encodes the specified SpinAck message. Does not implicitly {@link jlzs.SpinAck.verify|verify} messages.
         * @function encode
         * @memberof jlzs.SpinAck
         * @static
         * @param {jlzs.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.RoundQueue != null && message.RoundQueue.length)
                for (let i = 0; i < message.RoundQueue.length; ++i)
                    $root.jlzs.RoundInfo.encode(message.RoundQueue[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.FreeTotalWin != null && Object.hasOwnProperty.call(message, "FreeTotalWin"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.FreeTotalWin);
            if (message.TotalWin != null && Object.hasOwnProperty.call(message, "TotalWin"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.TotalWin);
            if (message.ShowIndex != null && Object.hasOwnProperty.call(message, "ShowIndex"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ShowIndex);
            if (message.NowMoney != null && Object.hasOwnProperty.call(message, "NowMoney"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.NowMoney);
            if (message.AckType != null && Object.hasOwnProperty.call(message, "AckType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.AckType);
            if (message.RTP != null && Object.hasOwnProperty.call(message, "RTP"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.RTP);
            if (message.LID != null && Object.hasOwnProperty.call(message, "LID"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.LID);
            if (message.RID != null && Object.hasOwnProperty.call(message, "RID"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.RID);
            return writer;
        };

        /**
         * Encodes the specified SpinAck message, length delimited. Does not implicitly {@link jlzs.SpinAck.verify|verify} messages.
         * @function encodeDelimited
         * @memberof jlzs.SpinAck
         * @static
         * @param {jlzs.ISpinAck} message SpinAck message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SpinAck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer.
         * @function decode
         * @memberof jlzs.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {jlzs.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.jlzs.SpinAck();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.RoundQueue && message.RoundQueue.length))
                            message.RoundQueue = [];
                        message.RoundQueue.push($root.jlzs.RoundInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.FreeTotalWin = reader.double();
                        break;
                    }
                case 3: {
                        message.TotalWin = reader.double();
                        break;
                    }
                case 4: {
                        message.ShowIndex = reader.string();
                        break;
                    }
                case 5: {
                        message.NowMoney = reader.double();
                        break;
                    }
                case 6: {
                        message.AckType = reader.int32();
                        break;
                    }
                case 7: {
                        message.RTP = reader.double();
                        break;
                    }
                case 8: {
                        message.LID = reader.int32();
                        break;
                    }
                case 9: {
                        message.RID = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SpinAck message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof jlzs.SpinAck
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {jlzs.SpinAck} SpinAck
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SpinAck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SpinAck message.
         * @function verify
         * @memberof jlzs.SpinAck
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SpinAck.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.RoundQueue != null && message.hasOwnProperty("RoundQueue")) {
                if (!Array.isArray(message.RoundQueue))
                    return "RoundQueue: array expected";
                for (let i = 0; i < message.RoundQueue.length; ++i) {
                    let error = $root.jlzs.RoundInfo.verify(message.RoundQueue[i]);
                    if (error)
                        return "RoundQueue." + error;
                }
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                if (typeof message.FreeTotalWin !== "number")
                    return "FreeTotalWin: number expected";
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                if (typeof message.TotalWin !== "number")
                    return "TotalWin: number expected";
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                if (!$util.isString(message.ShowIndex))
                    return "ShowIndex: string expected";
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                if (typeof message.NowMoney !== "number")
                    return "NowMoney: number expected";
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                if (!$util.isInteger(message.AckType))
                    return "AckType: integer expected";
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                if (typeof message.RTP !== "number")
                    return "RTP: number expected";
            if (message.LID != null && message.hasOwnProperty("LID"))
                if (!$util.isInteger(message.LID))
                    return "LID: integer expected";
            if (message.RID != null && message.hasOwnProperty("RID"))
                if (!$util.isInteger(message.RID))
                    return "RID: integer expected";
            return null;
        };

        /**
         * Creates a SpinAck message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof jlzs.SpinAck
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {jlzs.SpinAck} SpinAck
         */
        SpinAck.fromObject = function fromObject(object) {
            if (object instanceof $root.jlzs.SpinAck)
                return object;
            let message = new $root.jlzs.SpinAck();
            if (object.RoundQueue) {
                if (!Array.isArray(object.RoundQueue))
                    throw TypeError(".jlzs.SpinAck.RoundQueue: array expected");
                message.RoundQueue = [];
                for (let i = 0; i < object.RoundQueue.length; ++i) {
                    if (typeof object.RoundQueue[i] !== "object")
                        throw TypeError(".jlzs.SpinAck.RoundQueue: object expected");
                    message.RoundQueue[i] = $root.jlzs.RoundInfo.fromObject(object.RoundQueue[i]);
                }
            }
            if (object.FreeTotalWin != null)
                message.FreeTotalWin = Number(object.FreeTotalWin);
            if (object.TotalWin != null)
                message.TotalWin = Number(object.TotalWin);
            if (object.ShowIndex != null)
                message.ShowIndex = String(object.ShowIndex);
            if (object.NowMoney != null)
                message.NowMoney = Number(object.NowMoney);
            if (object.AckType != null)
                message.AckType = object.AckType | 0;
            if (object.RTP != null)
                message.RTP = Number(object.RTP);
            if (object.LID != null)
                message.LID = object.LID | 0;
            if (object.RID != null)
                message.RID = object.RID | 0;
            return message;
        };

        /**
         * Creates a plain object from a SpinAck message. Also converts values to other types if specified.
         * @function toObject
         * @memberof jlzs.SpinAck
         * @static
         * @param {jlzs.SpinAck} message SpinAck
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SpinAck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.RoundQueue = [];
            if (options.defaults) {
                object.FreeTotalWin = 0;
                object.TotalWin = 0;
                object.ShowIndex = "";
                object.NowMoney = 0;
                object.AckType = 0;
                object.RTP = 0;
                object.LID = 0;
                object.RID = 0;
            }
            if (message.RoundQueue && message.RoundQueue.length) {
                object.RoundQueue = [];
                for (let j = 0; j < message.RoundQueue.length; ++j)
                    object.RoundQueue[j] = $root.jlzs.RoundInfo.toObject(message.RoundQueue[j], options);
            }
            if (message.FreeTotalWin != null && message.hasOwnProperty("FreeTotalWin"))
                object.FreeTotalWin = options.json && !isFinite(message.FreeTotalWin) ? String(message.FreeTotalWin) : message.FreeTotalWin;
            if (message.TotalWin != null && message.hasOwnProperty("TotalWin"))
                object.TotalWin = options.json && !isFinite(message.TotalWin) ? String(message.TotalWin) : message.TotalWin;
            if (message.ShowIndex != null && message.hasOwnProperty("ShowIndex"))
                object.ShowIndex = message.ShowIndex;
            if (message.NowMoney != null && message.hasOwnProperty("NowMoney"))
                object.NowMoney = options.json && !isFinite(message.NowMoney) ? String(message.NowMoney) : message.NowMoney;
            if (message.AckType != null && message.hasOwnProperty("AckType"))
                object.AckType = message.AckType;
            if (message.RTP != null && message.hasOwnProperty("RTP"))
                object.RTP = options.json && !isFinite(message.RTP) ? String(message.RTP) : message.RTP;
            if (message.LID != null && message.hasOwnProperty("LID"))
                object.LID = message.LID;
            if (message.RID != null && message.hasOwnProperty("RID"))
                object.RID = message.RID;
            return object;
        };

        /**
         * Converts this SpinAck to JSON.
         * @function toJSON
         * @memberof jlzs.SpinAck
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SpinAck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SpinAck
         * @function getTypeUrl
         * @memberof jlzs.SpinAck
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SpinAck.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/jlzs.SpinAck";
        };

        return SpinAck;
    })();

    return jlzs;
})();

export { $root as default };
